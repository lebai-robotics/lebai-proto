/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-mixed-operators, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars, default-case, jsdoc/require-param*/
"use strict";

var $protobuf = require("protobufjs/minimal");

// Common aliases
var $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;
var $Object = $util.global.Object, $undefined = $util.global.undefined, $Error = $util.global.Error, $RangeError = $util.global.RangeError, $TypeError = $util.global.TypeError, $Boolean = $util.global.Boolean, $String = $util.global.String, $Number = $util.global.Number, $parseInt = $util.global.parseInt, $BigInt = $util.global.BigInt, $Array = $util.global.Array, $isFinite = $util.global.isFinite;

// Exported root namespace
var $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

$root.lebai = (function() {

    /**
     * Namespace lebai.
     * @exports lebai
     * @namespace
     */
    var lebai = {};

    lebai.auto = (function() {

        /**
         * Namespace auto.
         * @memberof lebai
         * @namespace
         */
        var auto = {};

        /**
         * AutoCfg enum.
         * @name lebai.auto.AutoCfg
         * @enum {number}
         * @property {number} ARM_POWER=0 ARM_POWER value
         * @property {number} ENABLE_JOINT=1 ENABLE_JOINT value
         * @property {number} INIT_CLAW=2 INIT_CLAW value
         */
        auto.AutoCfg = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "ARM_POWER"] = 0;
            values[valuesById[1] = "ENABLE_JOINT"] = 1;
            values[valuesById[2] = "INIT_CLAW"] = 2;
            return values;
        })();

        auto.SetAutoRequest = (function() {

            /**
             * Properties of a SetAutoRequest.
             * @typedef {Object} lebai.auto.SetAutoRequest.$Properties
             * @property {lebai.auto.AutoCfg|null} [name] SetAutoRequest name
             * @property {boolean|null} [value] SetAutoRequest value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetAutoRequest.
             * @memberof lebai.auto
             * @interface ISetAutoRequest
             * @augments lebai.auto.SetAutoRequest.$Properties
             * @deprecated Use lebai.auto.SetAutoRequest.$Properties instead.
             */

            /**
             * Shape of a SetAutoRequest.
             * @typedef {lebai.auto.SetAutoRequest.$Properties} lebai.auto.SetAutoRequest.$Shape
             */

            /**
             * Constructs a new SetAutoRequest.
             * @memberof lebai.auto
             * @classdesc Represents a SetAutoRequest.
             * @constructor
             * @param {lebai.auto.SetAutoRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetAutoRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetAutoRequest name.
             * @member {lebai.auto.AutoCfg} name
             * @memberof lebai.auto.SetAutoRequest
             * @instance
             */
            SetAutoRequest.prototype.name = 0;

            /**
             * SetAutoRequest value.
             * @member {boolean} value
             * @memberof lebai.auto.SetAutoRequest
             * @instance
             */
            SetAutoRequest.prototype.value = false;

            /**
             * Creates a new SetAutoRequest instance using the specified properties.
             * @function create
             * @memberof lebai.auto.SetAutoRequest
             * @static
             * @param {lebai.auto.SetAutoRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.auto.SetAutoRequest} SetAutoRequest instance
             * @type {{
             *   (properties: lebai.auto.SetAutoRequest.$Shape): lebai.auto.SetAutoRequest & lebai.auto.SetAutoRequest.$Shape;
             *   (properties?: lebai.auto.SetAutoRequest.$Properties): lebai.auto.SetAutoRequest;
             * }}
             */
            SetAutoRequest.create = function(properties) {
                return new SetAutoRequest(properties);
            };

            /**
             * Encodes the specified SetAutoRequest message. Does not implicitly {@link lebai.auto.SetAutoRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.auto.SetAutoRequest
             * @static
             * @param {lebai.auto.SetAutoRequest.$Properties} message SetAutoRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetAutoRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.name);
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== false)
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetAutoRequest message, length delimited. Does not implicitly {@link lebai.auto.SetAutoRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.auto.SetAutoRequest
             * @static
             * @param {lebai.auto.SetAutoRequest.$Properties} message SetAutoRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetAutoRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetAutoRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.auto.SetAutoRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.auto.SetAutoRequest & lebai.auto.SetAutoRequest.$Shape} SetAutoRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetAutoRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.auto.SetAutoRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetAutoRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.auto.SetAutoRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.auto.SetAutoRequest & lebai.auto.SetAutoRequest.$Shape} SetAutoRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetAutoRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetAutoRequest message.
             * @function verify
             * @memberof lebai.auto.SetAutoRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetAutoRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (typeof message.name !== "number" || (message.name | 0) !== message.name)
                        return "name: enum value expected";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (typeof message.value !== "boolean")
                        return "value: boolean expected";
                return null;
            };

            /**
             * Creates a SetAutoRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.auto.SetAutoRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.auto.SetAutoRequest} SetAutoRequest
             */
            SetAutoRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.auto.SetAutoRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.auto.SetAutoRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.auto.SetAutoRequest();
                if (object.name !== 0 && (typeof object.name !== "string" || $root.lebai.auto.AutoCfg[object.name] !== 0))
                    switch (object.name) {
                    case "ARM_POWER":
                    case 0:
                        message.name = 0;
                        break;
                    case "ENABLE_JOINT":
                    case 1:
                        message.name = 1;
                        break;
                    case "INIT_CLAW":
                    case 2:
                        message.name = 2;
                        break;
                    default:
                        if (typeof object.name === "number" && (object.name | 0) === object.name)
                            message.name = object.name;
                    }
                if (object.value != null)
                    if (object.value)
                        message.value = $Boolean(object.value);
                return message;
            };

            /**
             * Creates a plain object from a SetAutoRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.auto.SetAutoRequest
             * @static
             * @param {lebai.auto.SetAutoRequest} message SetAutoRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetAutoRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = options.enums === $String ? "ARM_POWER" : 0;
                    object.value = false;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = options.enums === $String ? $root.lebai.auto.AutoCfg[message.name] === $undefined ? message.name : $root.lebai.auto.AutoCfg[message.name] : message.name;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this SetAutoRequest to JSON.
             * @function toJSON
             * @memberof lebai.auto.SetAutoRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetAutoRequest.prototype.toJSON = function() {
                return SetAutoRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetAutoRequest
             * @function getTypeUrl
             * @memberof lebai.auto.SetAutoRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetAutoRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.auto.SetAutoRequest";
            };

            return SetAutoRequest;
        })();

        auto.GetAutoRequest = (function() {

            /**
             * Properties of a GetAutoRequest.
             * @typedef {Object} lebai.auto.GetAutoRequest.$Properties
             * @property {lebai.auto.AutoCfg|null} [name] GetAutoRequest name
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetAutoRequest.
             * @memberof lebai.auto
             * @interface IGetAutoRequest
             * @augments lebai.auto.GetAutoRequest.$Properties
             * @deprecated Use lebai.auto.GetAutoRequest.$Properties instead.
             */

            /**
             * Shape of a GetAutoRequest.
             * @typedef {lebai.auto.GetAutoRequest.$Properties} lebai.auto.GetAutoRequest.$Shape
             */

            /**
             * Constructs a new GetAutoRequest.
             * @memberof lebai.auto
             * @classdesc Represents a GetAutoRequest.
             * @constructor
             * @param {lebai.auto.GetAutoRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetAutoRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetAutoRequest name.
             * @member {lebai.auto.AutoCfg} name
             * @memberof lebai.auto.GetAutoRequest
             * @instance
             */
            GetAutoRequest.prototype.name = 0;

            /**
             * Creates a new GetAutoRequest instance using the specified properties.
             * @function create
             * @memberof lebai.auto.GetAutoRequest
             * @static
             * @param {lebai.auto.GetAutoRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.auto.GetAutoRequest} GetAutoRequest instance
             * @type {{
             *   (properties: lebai.auto.GetAutoRequest.$Shape): lebai.auto.GetAutoRequest & lebai.auto.GetAutoRequest.$Shape;
             *   (properties?: lebai.auto.GetAutoRequest.$Properties): lebai.auto.GetAutoRequest;
             * }}
             */
            GetAutoRequest.create = function(properties) {
                return new GetAutoRequest(properties);
            };

            /**
             * Encodes the specified GetAutoRequest message. Does not implicitly {@link lebai.auto.GetAutoRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.auto.GetAutoRequest
             * @static
             * @param {lebai.auto.GetAutoRequest.$Properties} message GetAutoRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetAutoRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.name);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetAutoRequest message, length delimited. Does not implicitly {@link lebai.auto.GetAutoRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.auto.GetAutoRequest
             * @static
             * @param {lebai.auto.GetAutoRequest.$Properties} message GetAutoRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetAutoRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetAutoRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.auto.GetAutoRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.auto.GetAutoRequest & lebai.auto.GetAutoRequest.$Shape} GetAutoRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetAutoRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.auto.GetAutoRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetAutoRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.auto.GetAutoRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.auto.GetAutoRequest & lebai.auto.GetAutoRequest.$Shape} GetAutoRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetAutoRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetAutoRequest message.
             * @function verify
             * @memberof lebai.auto.GetAutoRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetAutoRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (typeof message.name !== "number" || (message.name | 0) !== message.name)
                        return "name: enum value expected";
                return null;
            };

            /**
             * Creates a GetAutoRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.auto.GetAutoRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.auto.GetAutoRequest} GetAutoRequest
             */
            GetAutoRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.auto.GetAutoRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.auto.GetAutoRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.auto.GetAutoRequest();
                if (object.name !== 0 && (typeof object.name !== "string" || $root.lebai.auto.AutoCfg[object.name] !== 0))
                    switch (object.name) {
                    case "ARM_POWER":
                    case 0:
                        message.name = 0;
                        break;
                    case "ENABLE_JOINT":
                    case 1:
                        message.name = 1;
                        break;
                    case "INIT_CLAW":
                    case 2:
                        message.name = 2;
                        break;
                    default:
                        if (typeof object.name === "number" && (object.name | 0) === object.name)
                            message.name = object.name;
                    }
                return message;
            };

            /**
             * Creates a plain object from a GetAutoRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.auto.GetAutoRequest
             * @static
             * @param {lebai.auto.GetAutoRequest} message GetAutoRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetAutoRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.name = options.enums === $String ? "ARM_POWER" : 0;
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = options.enums === $String ? $root.lebai.auto.AutoCfg[message.name] === $undefined ? message.name : $root.lebai.auto.AutoCfg[message.name] : message.name;
                return object;
            };

            /**
             * Converts this GetAutoRequest to JSON.
             * @function toJSON
             * @memberof lebai.auto.GetAutoRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetAutoRequest.prototype.toJSON = function() {
                return GetAutoRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetAutoRequest
             * @function getTypeUrl
             * @memberof lebai.auto.GetAutoRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetAutoRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.auto.GetAutoRequest";
            };

            return GetAutoRequest;
        })();

        auto.GetAutoResponse = (function() {

            /**
             * Properties of a GetAutoResponse.
             * @typedef {Object} lebai.auto.GetAutoResponse.$Properties
             * @property {boolean|null} [value] GetAutoResponse value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetAutoResponse.
             * @memberof lebai.auto
             * @interface IGetAutoResponse
             * @augments lebai.auto.GetAutoResponse.$Properties
             * @deprecated Use lebai.auto.GetAutoResponse.$Properties instead.
             */

            /**
             * Shape of a GetAutoResponse.
             * @typedef {lebai.auto.GetAutoResponse.$Properties} lebai.auto.GetAutoResponse.$Shape
             */

            /**
             * Constructs a new GetAutoResponse.
             * @memberof lebai.auto
             * @classdesc Represents a GetAutoResponse.
             * @constructor
             * @param {lebai.auto.GetAutoResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetAutoResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetAutoResponse value.
             * @member {boolean|null|undefined} value
             * @memberof lebai.auto.GetAutoResponse
             * @instance
             */
            GetAutoResponse.prototype.value = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GetAutoResponse.prototype, "_value", {
                get: $util.oneOfGetter($oneOfFields = ["value"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new GetAutoResponse instance using the specified properties.
             * @function create
             * @memberof lebai.auto.GetAutoResponse
             * @static
             * @param {lebai.auto.GetAutoResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.auto.GetAutoResponse} GetAutoResponse instance
             * @type {{
             *   (properties: lebai.auto.GetAutoResponse.$Shape): lebai.auto.GetAutoResponse & lebai.auto.GetAutoResponse.$Shape;
             *   (properties?: lebai.auto.GetAutoResponse.$Properties): lebai.auto.GetAutoResponse;
             * }}
             */
            GetAutoResponse.create = function(properties) {
                return new GetAutoResponse(properties);
            };

            /**
             * Encodes the specified GetAutoResponse message. Does not implicitly {@link lebai.auto.GetAutoResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.auto.GetAutoResponse
             * @static
             * @param {lebai.auto.GetAutoResponse.$Properties} message GetAutoResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetAutoResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetAutoResponse message, length delimited. Does not implicitly {@link lebai.auto.GetAutoResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.auto.GetAutoResponse
             * @static
             * @param {lebai.auto.GetAutoResponse.$Properties} message GetAutoResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetAutoResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetAutoResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.auto.GetAutoResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.auto.GetAutoResponse & lebai.auto.GetAutoResponse.$Shape} GetAutoResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetAutoResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.auto.GetAutoResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 0)
                                break;
                            message.value = reader.bool();
                            message._value = "value";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetAutoResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.auto.GetAutoResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.auto.GetAutoResponse & lebai.auto.GetAutoResponse.$Shape} GetAutoResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetAutoResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetAutoResponse message.
             * @function verify
             * @memberof lebai.auto.GetAutoResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetAutoResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.value != null && $Object.hasOwnProperty.call(message, "value")) {
                    properties._value = 1;
                    if (typeof message.value !== "boolean")
                        return "value: boolean expected";
                }
                return null;
            };

            /**
             * Creates a GetAutoResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.auto.GetAutoResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.auto.GetAutoResponse} GetAutoResponse
             */
            GetAutoResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.auto.GetAutoResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.auto.GetAutoResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.auto.GetAutoResponse();
                if (object.value != null)
                    message.value = $Boolean(object.value);
                return message;
            };

            /**
             * Creates a plain object from a GetAutoResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.auto.GetAutoResponse
             * @static
             * @param {lebai.auto.GetAutoResponse} message GetAutoResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetAutoResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this GetAutoResponse to JSON.
             * @function toJSON
             * @memberof lebai.auto.GetAutoResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetAutoResponse.prototype.toJSON = function() {
                return GetAutoResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetAutoResponse
             * @function getTypeUrl
             * @memberof lebai.auto.GetAutoResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetAutoResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.auto.GetAutoResponse";
            };

            return GetAutoResponse;
        })();

        auto.AutoService = (function() {

            /**
             * Constructs a new AutoService service.
             * @memberof lebai.auto
             * @classdesc Represents an AutoService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var AutoService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(AutoService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: AutoService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new AutoService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.auto.AutoService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {AutoService} RPC service. Useful where requests and/or responses are streamed.
             */
            AutoService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.auto.AutoService#setAuto}.
             * @memberof lebai.auto.AutoService
             * @typedef SetAutoCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetAuto.
             * @memberof lebai.auto.AutoService
             * @typedef SetAuto
             * @type {{
             *   (request: lebai.auto.ISetAutoRequest, callback: lebai.auto.AutoService.SetAutoCallback): void;
             *   (request: lebai.auto.ISetAutoRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetAuto";
             *   readonly path: "/lebai.auto.AutoService/SetAuto";
             *   readonly requestType: "SetAutoRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetAuto.
             * @name lebai.auto.AutoService#setAuto
             * @type {lebai.auto.AutoService.SetAuto}
             */
            $Object.defineProperties(AutoService.prototype.setAuto = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, AutoService.prototype.setAuto, $root.lebai.auto.SetAutoRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetAuto" },
                path: { value: "/lebai.auto.AutoService/SetAuto" },
                requestType: { value: "SetAutoRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.auto.AutoService#getAuto}.
             * @memberof lebai.auto.AutoService
             * @typedef GetAutoCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.auto.GetAutoResponse} [response] GetAutoResponse
             */

            /**
             * Calls GetAuto.
             * @memberof lebai.auto.AutoService
             * @typedef GetAuto
             * @type {{
             *   (request: lebai.auto.IGetAutoRequest, callback: lebai.auto.AutoService.GetAutoCallback): void;
             *   (request: lebai.auto.IGetAutoRequest): Promise<lebai.auto.GetAutoResponse>;
             *   readonly name: "GetAuto";
             *   readonly path: "/lebai.auto.AutoService/GetAuto";
             *   readonly requestType: "GetAutoRequest";
             *   readonly responseType: "GetAutoResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetAuto.
             * @name lebai.auto.AutoService#getAuto
             * @type {lebai.auto.AutoService.GetAuto}
             */
            $Object.defineProperties(AutoService.prototype.getAuto = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, AutoService.prototype.getAuto, $root.lebai.auto.GetAutoRequest, $root.lebai.auto.GetAutoResponse, request, callback);
            }, {
                name: { value: "GetAuto" },
                path: { value: "/lebai.auto.AutoService/GetAuto" },
                requestType: { value: "GetAutoRequest" },
                responseType: { value: "GetAutoResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return AutoService;
        })();

        return auto;
    })();

    lebai.backup = (function() {

        /**
         * Namespace backup.
         * @memberof lebai
         * @namespace
         */
        var backup = {};

        backup.BackupInfo = (function() {

            /**
             * Properties of a BackupInfo.
             * @typedef {Object} lebai.backup.BackupInfo.$Properties
             * @property {lebai.system.SystemInfo.$Properties|null} [system] BackupInfo system
             * @property {lebai.system.RobotInfo.$Properties|null} [robot] BackupInfo robot
             * @property {lebai.system.HardwareInfo.$Properties|null} [hardware] BackupInfo hardware
             * @property {lebai.system.SoftwareInfo.$Properties|null} [software] BackupInfo software
             * @property {google.protobuf.Timestamp.$Properties|null} [timestamp] BackupInfo timestamp
             * @property {lebai.backup.Options.$Properties|null} [option] BackupInfo option
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a BackupInfo.
             * @memberof lebai.backup
             * @interface IBackupInfo
             * @augments lebai.backup.BackupInfo.$Properties
             * @deprecated Use lebai.backup.BackupInfo.$Properties instead.
             */

            /**
             * Shape of a BackupInfo.
             * @typedef {lebai.backup.BackupInfo.$Properties} lebai.backup.BackupInfo.$Shape
             */

            /**
             * Constructs a new BackupInfo.
             * @memberof lebai.backup
             * @classdesc Represents a BackupInfo.
             * @constructor
             * @param {lebai.backup.BackupInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var BackupInfo = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * BackupInfo system.
             * @member {lebai.system.SystemInfo.$Properties|null|undefined} system
             * @memberof lebai.backup.BackupInfo
             * @instance
             */
            BackupInfo.prototype.system = null;

            /**
             * BackupInfo robot.
             * @member {lebai.system.RobotInfo.$Properties|null|undefined} robot
             * @memberof lebai.backup.BackupInfo
             * @instance
             */
            BackupInfo.prototype.robot = null;

            /**
             * BackupInfo hardware.
             * @member {lebai.system.HardwareInfo.$Properties|null|undefined} hardware
             * @memberof lebai.backup.BackupInfo
             * @instance
             */
            BackupInfo.prototype.hardware = null;

            /**
             * BackupInfo software.
             * @member {lebai.system.SoftwareInfo.$Properties|null|undefined} software
             * @memberof lebai.backup.BackupInfo
             * @instance
             */
            BackupInfo.prototype.software = null;

            /**
             * BackupInfo timestamp.
             * @member {google.protobuf.Timestamp.$Properties|null|undefined} timestamp
             * @memberof lebai.backup.BackupInfo
             * @instance
             */
            BackupInfo.prototype.timestamp = null;

            /**
             * BackupInfo option.
             * @member {lebai.backup.Options.$Properties|null|undefined} option
             * @memberof lebai.backup.BackupInfo
             * @instance
             */
            BackupInfo.prototype.option = null;

            /**
             * Creates a new BackupInfo instance using the specified properties.
             * @function create
             * @memberof lebai.backup.BackupInfo
             * @static
             * @param {lebai.backup.BackupInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.backup.BackupInfo} BackupInfo instance
             * @type {{
             *   (properties: lebai.backup.BackupInfo.$Shape): lebai.backup.BackupInfo & lebai.backup.BackupInfo.$Shape;
             *   (properties?: lebai.backup.BackupInfo.$Properties): lebai.backup.BackupInfo;
             * }}
             */
            BackupInfo.create = function(properties) {
                return new BackupInfo(properties);
            };

            /**
             * Encodes the specified BackupInfo message. Does not implicitly {@link lebai.backup.BackupInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.backup.BackupInfo
             * @static
             * @param {lebai.backup.BackupInfo.$Properties} message BackupInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BackupInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.system != null && $Object.hasOwnProperty.call(message, "system"))
                    $root.lebai.system.SystemInfo.encode(message.system, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.robot != null && $Object.hasOwnProperty.call(message, "robot"))
                    $root.lebai.system.RobotInfo.encode(message.robot, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.hardware != null && $Object.hasOwnProperty.call(message, "hardware"))
                    $root.lebai.system.HardwareInfo.encode(message.hardware, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.software != null && $Object.hasOwnProperty.call(message, "software"))
                    $root.lebai.system.SoftwareInfo.encode(message.software, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                    $root.google.protobuf.Timestamp.encode(message.timestamp, writer.uint32(/* id 21, wireType 2 =*/170).fork(), _depth + 1).ldelim();
                if (message.option != null && $Object.hasOwnProperty.call(message, "option"))
                    $root.lebai.backup.Options.encode(message.option, writer.uint32(/* id 22, wireType 2 =*/178).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BackupInfo message, length delimited. Does not implicitly {@link lebai.backup.BackupInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.backup.BackupInfo
             * @static
             * @param {lebai.backup.BackupInfo.$Properties} message BackupInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BackupInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a BackupInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.backup.BackupInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.backup.BackupInfo & lebai.backup.BackupInfo.$Shape} BackupInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BackupInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.backup.BackupInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.system = $root.lebai.system.SystemInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.system);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.robot = $root.lebai.system.RobotInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.robot);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.hardware = $root.lebai.system.HardwareInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hardware);
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.software = $root.lebai.system.SoftwareInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.software);
                            continue;
                        }
                    case 21: {
                            if (wireType !== 2)
                                break;
                            message.timestamp = $root.google.protobuf.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.timestamp);
                            continue;
                        }
                    case 22: {
                            if (wireType !== 2)
                                break;
                            message.option = $root.lebai.backup.Options.decode(reader, reader.uint32(), $undefined, _depth + 1, message.option);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a BackupInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.backup.BackupInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.backup.BackupInfo & lebai.backup.BackupInfo.$Shape} BackupInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BackupInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BackupInfo message.
             * @function verify
             * @memberof lebai.backup.BackupInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BackupInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.system != null && $Object.hasOwnProperty.call(message, "system")) {
                    var error = $root.lebai.system.SystemInfo.verify(message.system, _depth + 1);
                    if (error)
                        return "system." + error;
                }
                if (message.robot != null && $Object.hasOwnProperty.call(message, "robot")) {
                    var error = $root.lebai.system.RobotInfo.verify(message.robot, _depth + 1);
                    if (error)
                        return "robot." + error;
                }
                if (message.hardware != null && $Object.hasOwnProperty.call(message, "hardware")) {
                    var error = $root.lebai.system.HardwareInfo.verify(message.hardware, _depth + 1);
                    if (error)
                        return "hardware." + error;
                }
                if (message.software != null && $Object.hasOwnProperty.call(message, "software")) {
                    var error = $root.lebai.system.SoftwareInfo.verify(message.software, _depth + 1);
                    if (error)
                        return "software." + error;
                }
                if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp")) {
                    var error = $root.google.protobuf.Timestamp.verify(message.timestamp, _depth + 1);
                    if (error)
                        return "timestamp." + error;
                }
                if (message.option != null && $Object.hasOwnProperty.call(message, "option")) {
                    var error = $root.lebai.backup.Options.verify(message.option, _depth + 1);
                    if (error)
                        return "option." + error;
                }
                return null;
            };

            /**
             * Creates a BackupInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.backup.BackupInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.backup.BackupInfo} BackupInfo
             */
            BackupInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.backup.BackupInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.backup.BackupInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.backup.BackupInfo();
                if (object.system != null) {
                    if (!$util.isObject(object.system))
                        throw $TypeError(".lebai.backup.BackupInfo.system: object expected");
                    message.system = $root.lebai.system.SystemInfo.fromObject(object.system, _depth + 1);
                }
                if (object.robot != null) {
                    if (!$util.isObject(object.robot))
                        throw $TypeError(".lebai.backup.BackupInfo.robot: object expected");
                    message.robot = $root.lebai.system.RobotInfo.fromObject(object.robot, _depth + 1);
                }
                if (object.hardware != null) {
                    if (!$util.isObject(object.hardware))
                        throw $TypeError(".lebai.backup.BackupInfo.hardware: object expected");
                    message.hardware = $root.lebai.system.HardwareInfo.fromObject(object.hardware, _depth + 1);
                }
                if (object.software != null) {
                    if (!$util.isObject(object.software))
                        throw $TypeError(".lebai.backup.BackupInfo.software: object expected");
                    message.software = $root.lebai.system.SoftwareInfo.fromObject(object.software, _depth + 1);
                }
                if (object.timestamp != null) {
                    if (!$util.isObject(object.timestamp))
                        throw $TypeError(".lebai.backup.BackupInfo.timestamp: object expected");
                    message.timestamp = $root.google.protobuf.Timestamp.fromObject(object.timestamp, _depth + 1);
                }
                if (object.option != null) {
                    if (!$util.isObject(object.option))
                        throw $TypeError(".lebai.backup.BackupInfo.option: object expected");
                    message.option = $root.lebai.backup.Options.fromObject(object.option, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a BackupInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.backup.BackupInfo
             * @static
             * @param {lebai.backup.BackupInfo} message BackupInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BackupInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.system = null;
                    object.robot = null;
                    object.hardware = null;
                    object.software = null;
                    object.timestamp = null;
                    object.option = null;
                }
                if (message.system != null && $Object.hasOwnProperty.call(message, "system"))
                    object.system = $root.lebai.system.SystemInfo.toObject(message.system, options, _depth + 1);
                if (message.robot != null && $Object.hasOwnProperty.call(message, "robot"))
                    object.robot = $root.lebai.system.RobotInfo.toObject(message.robot, options, _depth + 1);
                if (message.hardware != null && $Object.hasOwnProperty.call(message, "hardware"))
                    object.hardware = $root.lebai.system.HardwareInfo.toObject(message.hardware, options, _depth + 1);
                if (message.software != null && $Object.hasOwnProperty.call(message, "software"))
                    object.software = $root.lebai.system.SoftwareInfo.toObject(message.software, options, _depth + 1);
                if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
                    object.timestamp = $root.google.protobuf.Timestamp.toObject(message.timestamp, options, _depth + 1);
                if (message.option != null && $Object.hasOwnProperty.call(message, "option"))
                    object.option = $root.lebai.backup.Options.toObject(message.option, options, _depth + 1);
                return object;
            };

            /**
             * Converts this BackupInfo to JSON.
             * @function toJSON
             * @memberof lebai.backup.BackupInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BackupInfo.prototype.toJSON = function() {
                return BackupInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for BackupInfo
             * @function getTypeUrl
             * @memberof lebai.backup.BackupInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            BackupInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.backup.BackupInfo";
            };

            return BackupInfo;
        })();

        backup.Options = (function() {

            /**
             * Properties of an Options.
             * @typedef {Object} lebai.backup.Options.$Properties
             * @property {boolean|null} [tmp] Options tmp
             * @property {boolean|null} [syslog] Options syslog
             * @property {boolean|null} [arm] Options arm
             * @property {boolean|null} [config] Options config
             * @property {boolean|null} [data] Options data
             * @property {boolean|null} [file] Options file
             * @property {boolean|null} [docker] Options docker
             * @property {boolean|null} [ds] Options ds
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an Options.
             * @memberof lebai.backup
             * @interface IOptions
             * @augments lebai.backup.Options.$Properties
             * @deprecated Use lebai.backup.Options.$Properties instead.
             */

            /**
             * Shape of an Options.
             * @typedef {lebai.backup.Options.$Properties} lebai.backup.Options.$Shape
             */

            /**
             * Constructs a new Options.
             * @memberof lebai.backup
             * @classdesc Represents an Options.
             * @constructor
             * @param {lebai.backup.Options.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Options = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Options tmp.
             * @member {boolean} tmp
             * @memberof lebai.backup.Options
             * @instance
             */
            Options.prototype.tmp = false;

            /**
             * Options syslog.
             * @member {boolean} syslog
             * @memberof lebai.backup.Options
             * @instance
             */
            Options.prototype.syslog = false;

            /**
             * Options arm.
             * @member {boolean} arm
             * @memberof lebai.backup.Options
             * @instance
             */
            Options.prototype.arm = false;

            /**
             * Options config.
             * @member {boolean} config
             * @memberof lebai.backup.Options
             * @instance
             */
            Options.prototype.config = false;

            /**
             * Options data.
             * @member {boolean} data
             * @memberof lebai.backup.Options
             * @instance
             */
            Options.prototype.data = false;

            /**
             * Options file.
             * @member {boolean} file
             * @memberof lebai.backup.Options
             * @instance
             */
            Options.prototype.file = false;

            /**
             * Options docker.
             * @member {boolean} docker
             * @memberof lebai.backup.Options
             * @instance
             */
            Options.prototype.docker = false;

            /**
             * Options ds.
             * @member {boolean} ds
             * @memberof lebai.backup.Options
             * @instance
             */
            Options.prototype.ds = false;

            /**
             * Creates a new Options instance using the specified properties.
             * @function create
             * @memberof lebai.backup.Options
             * @static
             * @param {lebai.backup.Options.$Properties=} [properties] Properties to set
             * @returns {lebai.backup.Options} Options instance
             * @type {{
             *   (properties: lebai.backup.Options.$Shape): lebai.backup.Options & lebai.backup.Options.$Shape;
             *   (properties?: lebai.backup.Options.$Properties): lebai.backup.Options;
             * }}
             */
            Options.create = function(properties) {
                return new Options(properties);
            };

            /**
             * Encodes the specified Options message. Does not implicitly {@link lebai.backup.Options.verify|verify} messages.
             * @function encode
             * @memberof lebai.backup.Options
             * @static
             * @param {lebai.backup.Options.$Properties} message Options message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Options.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.tmp != null && $Object.hasOwnProperty.call(message, "tmp") && message.tmp !== false)
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.tmp);
                if (message.syslog != null && $Object.hasOwnProperty.call(message, "syslog") && message.syslog !== false)
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.syslog);
                if (message.arm != null && $Object.hasOwnProperty.call(message, "arm") && message.arm !== false)
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.arm);
                if (message.config != null && $Object.hasOwnProperty.call(message, "config") && message.config !== false)
                    writer.uint32(/* id 21, wireType 0 =*/168).bool(message.config);
                if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data !== false)
                    writer.uint32(/* id 22, wireType 0 =*/176).bool(message.data);
                if (message.file != null && $Object.hasOwnProperty.call(message, "file") && message.file !== false)
                    writer.uint32(/* id 23, wireType 0 =*/184).bool(message.file);
                if (message.docker != null && $Object.hasOwnProperty.call(message, "docker") && message.docker !== false)
                    writer.uint32(/* id 24, wireType 0 =*/192).bool(message.docker);
                if (message.ds != null && $Object.hasOwnProperty.call(message, "ds") && message.ds !== false)
                    writer.uint32(/* id 81, wireType 0 =*/648).bool(message.ds);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Options message, length delimited. Does not implicitly {@link lebai.backup.Options.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.backup.Options
             * @static
             * @param {lebai.backup.Options.$Properties} message Options message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Options.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an Options message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.backup.Options
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.backup.Options & lebai.backup.Options.$Shape} Options
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Options.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.backup.Options();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.tmp = value;
                            else
                                delete message.tmp;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.syslog = value;
                            else
                                delete message.syslog;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.arm = value;
                            else
                                delete message.arm;
                            continue;
                        }
                    case 21: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.config = value;
                            else
                                delete message.config;
                            continue;
                        }
                    case 22: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.data = value;
                            else
                                delete message.data;
                            continue;
                        }
                    case 23: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.file = value;
                            else
                                delete message.file;
                            continue;
                        }
                    case 24: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.docker = value;
                            else
                                delete message.docker;
                            continue;
                        }
                    case 81: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.ds = value;
                            else
                                delete message.ds;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an Options message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.backup.Options
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.backup.Options & lebai.backup.Options.$Shape} Options
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Options.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an Options message.
             * @function verify
             * @memberof lebai.backup.Options
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Options.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.tmp != null && $Object.hasOwnProperty.call(message, "tmp"))
                    if (typeof message.tmp !== "boolean")
                        return "tmp: boolean expected";
                if (message.syslog != null && $Object.hasOwnProperty.call(message, "syslog"))
                    if (typeof message.syslog !== "boolean")
                        return "syslog: boolean expected";
                if (message.arm != null && $Object.hasOwnProperty.call(message, "arm"))
                    if (typeof message.arm !== "boolean")
                        return "arm: boolean expected";
                if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                    if (typeof message.config !== "boolean")
                        return "config: boolean expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    if (typeof message.data !== "boolean")
                        return "data: boolean expected";
                if (message.file != null && $Object.hasOwnProperty.call(message, "file"))
                    if (typeof message.file !== "boolean")
                        return "file: boolean expected";
                if (message.docker != null && $Object.hasOwnProperty.call(message, "docker"))
                    if (typeof message.docker !== "boolean")
                        return "docker: boolean expected";
                if (message.ds != null && $Object.hasOwnProperty.call(message, "ds"))
                    if (typeof message.ds !== "boolean")
                        return "ds: boolean expected";
                return null;
            };

            /**
             * Creates an Options message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.backup.Options
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.backup.Options} Options
             */
            Options.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.backup.Options)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.backup.Options: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.backup.Options();
                if (object.tmp != null)
                    if (object.tmp)
                        message.tmp = $Boolean(object.tmp);
                if (object.syslog != null)
                    if (object.syslog)
                        message.syslog = $Boolean(object.syslog);
                if (object.arm != null)
                    if (object.arm)
                        message.arm = $Boolean(object.arm);
                if (object.config != null)
                    if (object.config)
                        message.config = $Boolean(object.config);
                if (object.data != null)
                    if (object.data)
                        message.data = $Boolean(object.data);
                if (object.file != null)
                    if (object.file)
                        message.file = $Boolean(object.file);
                if (object.docker != null)
                    if (object.docker)
                        message.docker = $Boolean(object.docker);
                if (object.ds != null)
                    if (object.ds)
                        message.ds = $Boolean(object.ds);
                return message;
            };

            /**
             * Creates a plain object from an Options message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.backup.Options
             * @static
             * @param {lebai.backup.Options} message Options
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Options.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.tmp = false;
                    object.syslog = false;
                    object.arm = false;
                    object.config = false;
                    object.data = false;
                    object.file = false;
                    object.docker = false;
                    object.ds = false;
                }
                if (message.tmp != null && $Object.hasOwnProperty.call(message, "tmp"))
                    object.tmp = message.tmp;
                if (message.syslog != null && $Object.hasOwnProperty.call(message, "syslog"))
                    object.syslog = message.syslog;
                if (message.arm != null && $Object.hasOwnProperty.call(message, "arm"))
                    object.arm = message.arm;
                if (message.config != null && $Object.hasOwnProperty.call(message, "config"))
                    object.config = message.config;
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = message.data;
                if (message.file != null && $Object.hasOwnProperty.call(message, "file"))
                    object.file = message.file;
                if (message.docker != null && $Object.hasOwnProperty.call(message, "docker"))
                    object.docker = message.docker;
                if (message.ds != null && $Object.hasOwnProperty.call(message, "ds"))
                    object.ds = message.ds;
                return object;
            };

            /**
             * Converts this Options to JSON.
             * @function toJSON
             * @memberof lebai.backup.Options
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Options.prototype.toJSON = function() {
                return Options.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Options
             * @function getTypeUrl
             * @memberof lebai.backup.Options
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Options.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.backup.Options";
            };

            return Options;
        })();

        backup.BackupRequest = (function() {

            /**
             * Properties of a BackupRequest.
             * @typedef {Object} lebai.backup.BackupRequest.$Properties
             * @property {string|null} [file] BackupRequest file
             * @property {lebai.backup.Options.$Properties|null} [option] BackupRequest option
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a BackupRequest.
             * @memberof lebai.backup
             * @interface IBackupRequest
             * @augments lebai.backup.BackupRequest.$Properties
             * @deprecated Use lebai.backup.BackupRequest.$Properties instead.
             */

            /**
             * Shape of a BackupRequest.
             * @typedef {lebai.backup.BackupRequest.$Properties} lebai.backup.BackupRequest.$Shape
             */

            /**
             * Constructs a new BackupRequest.
             * @memberof lebai.backup
             * @classdesc Represents a BackupRequest.
             * @constructor
             * @param {lebai.backup.BackupRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var BackupRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * BackupRequest file.
             * @member {string} file
             * @memberof lebai.backup.BackupRequest
             * @instance
             */
            BackupRequest.prototype.file = "";

            /**
             * BackupRequest option.
             * @member {lebai.backup.Options.$Properties|null|undefined} option
             * @memberof lebai.backup.BackupRequest
             * @instance
             */
            BackupRequest.prototype.option = null;

            /**
             * Creates a new BackupRequest instance using the specified properties.
             * @function create
             * @memberof lebai.backup.BackupRequest
             * @static
             * @param {lebai.backup.BackupRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.backup.BackupRequest} BackupRequest instance
             * @type {{
             *   (properties: lebai.backup.BackupRequest.$Shape): lebai.backup.BackupRequest & lebai.backup.BackupRequest.$Shape;
             *   (properties?: lebai.backup.BackupRequest.$Properties): lebai.backup.BackupRequest;
             * }}
             */
            BackupRequest.create = function(properties) {
                return new BackupRequest(properties);
            };

            /**
             * Encodes the specified BackupRequest message. Does not implicitly {@link lebai.backup.BackupRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.backup.BackupRequest
             * @static
             * @param {lebai.backup.BackupRequest.$Properties} message BackupRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BackupRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.file != null && $Object.hasOwnProperty.call(message, "file") && message.file !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.file);
                if (message.option != null && $Object.hasOwnProperty.call(message, "option"))
                    $root.lebai.backup.Options.encode(message.option, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BackupRequest message, length delimited. Does not implicitly {@link lebai.backup.BackupRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.backup.BackupRequest
             * @static
             * @param {lebai.backup.BackupRequest.$Properties} message BackupRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BackupRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a BackupRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.backup.BackupRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.backup.BackupRequest & lebai.backup.BackupRequest.$Shape} BackupRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BackupRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.backup.BackupRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.file = value;
                            else
                                delete message.file;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            message.option = $root.lebai.backup.Options.decode(reader, reader.uint32(), $undefined, _depth + 1, message.option);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a BackupRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.backup.BackupRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.backup.BackupRequest & lebai.backup.BackupRequest.$Shape} BackupRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BackupRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BackupRequest message.
             * @function verify
             * @memberof lebai.backup.BackupRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BackupRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.file != null && $Object.hasOwnProperty.call(message, "file"))
                    if (!$util.isString(message.file))
                        return "file: string expected";
                if (message.option != null && $Object.hasOwnProperty.call(message, "option")) {
                    var error = $root.lebai.backup.Options.verify(message.option, _depth + 1);
                    if (error)
                        return "option." + error;
                }
                return null;
            };

            /**
             * Creates a BackupRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.backup.BackupRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.backup.BackupRequest} BackupRequest
             */
            BackupRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.backup.BackupRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.backup.BackupRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.backup.BackupRequest();
                if (object.file != null)
                    if (typeof object.file !== "string" || object.file.length)
                        message.file = $String(object.file);
                if (object.option != null) {
                    if (!$util.isObject(object.option))
                        throw $TypeError(".lebai.backup.BackupRequest.option: object expected");
                    message.option = $root.lebai.backup.Options.fromObject(object.option, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a BackupRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.backup.BackupRequest
             * @static
             * @param {lebai.backup.BackupRequest} message BackupRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BackupRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.file = "";
                    object.option = null;
                }
                if (message.file != null && $Object.hasOwnProperty.call(message, "file"))
                    object.file = message.file;
                if (message.option != null && $Object.hasOwnProperty.call(message, "option"))
                    object.option = $root.lebai.backup.Options.toObject(message.option, options, _depth + 1);
                return object;
            };

            /**
             * Converts this BackupRequest to JSON.
             * @function toJSON
             * @memberof lebai.backup.BackupRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BackupRequest.prototype.toJSON = function() {
                return BackupRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for BackupRequest
             * @function getTypeUrl
             * @memberof lebai.backup.BackupRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            BackupRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.backup.BackupRequest";
            };

            return BackupRequest;
        })();

        backup.GetBackupInfoRequest = (function() {

            /**
             * Properties of a GetBackupInfoRequest.
             * @typedef {Object} lebai.backup.GetBackupInfoRequest.$Properties
             * @property {string|null} [file] GetBackupInfoRequest file
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetBackupInfoRequest.
             * @memberof lebai.backup
             * @interface IGetBackupInfoRequest
             * @augments lebai.backup.GetBackupInfoRequest.$Properties
             * @deprecated Use lebai.backup.GetBackupInfoRequest.$Properties instead.
             */

            /**
             * Shape of a GetBackupInfoRequest.
             * @typedef {lebai.backup.GetBackupInfoRequest.$Properties} lebai.backup.GetBackupInfoRequest.$Shape
             */

            /**
             * Constructs a new GetBackupInfoRequest.
             * @memberof lebai.backup
             * @classdesc Represents a GetBackupInfoRequest.
             * @constructor
             * @param {lebai.backup.GetBackupInfoRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetBackupInfoRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetBackupInfoRequest file.
             * @member {string} file
             * @memberof lebai.backup.GetBackupInfoRequest
             * @instance
             */
            GetBackupInfoRequest.prototype.file = "";

            /**
             * Creates a new GetBackupInfoRequest instance using the specified properties.
             * @function create
             * @memberof lebai.backup.GetBackupInfoRequest
             * @static
             * @param {lebai.backup.GetBackupInfoRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.backup.GetBackupInfoRequest} GetBackupInfoRequest instance
             * @type {{
             *   (properties: lebai.backup.GetBackupInfoRequest.$Shape): lebai.backup.GetBackupInfoRequest & lebai.backup.GetBackupInfoRequest.$Shape;
             *   (properties?: lebai.backup.GetBackupInfoRequest.$Properties): lebai.backup.GetBackupInfoRequest;
             * }}
             */
            GetBackupInfoRequest.create = function(properties) {
                return new GetBackupInfoRequest(properties);
            };

            /**
             * Encodes the specified GetBackupInfoRequest message. Does not implicitly {@link lebai.backup.GetBackupInfoRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.backup.GetBackupInfoRequest
             * @static
             * @param {lebai.backup.GetBackupInfoRequest.$Properties} message GetBackupInfoRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetBackupInfoRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.file != null && $Object.hasOwnProperty.call(message, "file") && message.file !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.file);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetBackupInfoRequest message, length delimited. Does not implicitly {@link lebai.backup.GetBackupInfoRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.backup.GetBackupInfoRequest
             * @static
             * @param {lebai.backup.GetBackupInfoRequest.$Properties} message GetBackupInfoRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetBackupInfoRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetBackupInfoRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.backup.GetBackupInfoRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.backup.GetBackupInfoRequest & lebai.backup.GetBackupInfoRequest.$Shape} GetBackupInfoRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetBackupInfoRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.backup.GetBackupInfoRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.file = value;
                            else
                                delete message.file;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetBackupInfoRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.backup.GetBackupInfoRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.backup.GetBackupInfoRequest & lebai.backup.GetBackupInfoRequest.$Shape} GetBackupInfoRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetBackupInfoRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetBackupInfoRequest message.
             * @function verify
             * @memberof lebai.backup.GetBackupInfoRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetBackupInfoRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.file != null && $Object.hasOwnProperty.call(message, "file"))
                    if (!$util.isString(message.file))
                        return "file: string expected";
                return null;
            };

            /**
             * Creates a GetBackupInfoRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.backup.GetBackupInfoRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.backup.GetBackupInfoRequest} GetBackupInfoRequest
             */
            GetBackupInfoRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.backup.GetBackupInfoRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.backup.GetBackupInfoRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.backup.GetBackupInfoRequest();
                if (object.file != null)
                    if (typeof object.file !== "string" || object.file.length)
                        message.file = $String(object.file);
                return message;
            };

            /**
             * Creates a plain object from a GetBackupInfoRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.backup.GetBackupInfoRequest
             * @static
             * @param {lebai.backup.GetBackupInfoRequest} message GetBackupInfoRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetBackupInfoRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.file = "";
                if (message.file != null && $Object.hasOwnProperty.call(message, "file"))
                    object.file = message.file;
                return object;
            };

            /**
             * Converts this GetBackupInfoRequest to JSON.
             * @function toJSON
             * @memberof lebai.backup.GetBackupInfoRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetBackupInfoRequest.prototype.toJSON = function() {
                return GetBackupInfoRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetBackupInfoRequest
             * @function getTypeUrl
             * @memberof lebai.backup.GetBackupInfoRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetBackupInfoRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.backup.GetBackupInfoRequest";
            };

            return GetBackupInfoRequest;
        })();

        backup.RestoreRequest = (function() {

            /**
             * Properties of a RestoreRequest.
             * @typedef {Object} lebai.backup.RestoreRequest.$Properties
             * @property {string|null} [file] RestoreRequest file
             * @property {lebai.backup.Options.$Properties|null} [option] RestoreRequest option
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a RestoreRequest.
             * @memberof lebai.backup
             * @interface IRestoreRequest
             * @augments lebai.backup.RestoreRequest.$Properties
             * @deprecated Use lebai.backup.RestoreRequest.$Properties instead.
             */

            /**
             * Shape of a RestoreRequest.
             * @typedef {lebai.backup.RestoreRequest.$Properties} lebai.backup.RestoreRequest.$Shape
             */

            /**
             * Constructs a new RestoreRequest.
             * @memberof lebai.backup
             * @classdesc Represents a RestoreRequest.
             * @constructor
             * @param {lebai.backup.RestoreRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var RestoreRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * RestoreRequest file.
             * @member {string} file
             * @memberof lebai.backup.RestoreRequest
             * @instance
             */
            RestoreRequest.prototype.file = "";

            /**
             * RestoreRequest option.
             * @member {lebai.backup.Options.$Properties|null|undefined} option
             * @memberof lebai.backup.RestoreRequest
             * @instance
             */
            RestoreRequest.prototype.option = null;

            /**
             * Creates a new RestoreRequest instance using the specified properties.
             * @function create
             * @memberof lebai.backup.RestoreRequest
             * @static
             * @param {lebai.backup.RestoreRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.backup.RestoreRequest} RestoreRequest instance
             * @type {{
             *   (properties: lebai.backup.RestoreRequest.$Shape): lebai.backup.RestoreRequest & lebai.backup.RestoreRequest.$Shape;
             *   (properties?: lebai.backup.RestoreRequest.$Properties): lebai.backup.RestoreRequest;
             * }}
             */
            RestoreRequest.create = function(properties) {
                return new RestoreRequest(properties);
            };

            /**
             * Encodes the specified RestoreRequest message. Does not implicitly {@link lebai.backup.RestoreRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.backup.RestoreRequest
             * @static
             * @param {lebai.backup.RestoreRequest.$Properties} message RestoreRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RestoreRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.file != null && $Object.hasOwnProperty.call(message, "file") && message.file !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.file);
                if (message.option != null && $Object.hasOwnProperty.call(message, "option"))
                    $root.lebai.backup.Options.encode(message.option, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified RestoreRequest message, length delimited. Does not implicitly {@link lebai.backup.RestoreRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.backup.RestoreRequest
             * @static
             * @param {lebai.backup.RestoreRequest.$Properties} message RestoreRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RestoreRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a RestoreRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.backup.RestoreRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.backup.RestoreRequest & lebai.backup.RestoreRequest.$Shape} RestoreRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RestoreRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.backup.RestoreRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.file = value;
                            else
                                delete message.file;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            message.option = $root.lebai.backup.Options.decode(reader, reader.uint32(), $undefined, _depth + 1, message.option);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a RestoreRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.backup.RestoreRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.backup.RestoreRequest & lebai.backup.RestoreRequest.$Shape} RestoreRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RestoreRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RestoreRequest message.
             * @function verify
             * @memberof lebai.backup.RestoreRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RestoreRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.file != null && $Object.hasOwnProperty.call(message, "file"))
                    if (!$util.isString(message.file))
                        return "file: string expected";
                if (message.option != null && $Object.hasOwnProperty.call(message, "option")) {
                    var error = $root.lebai.backup.Options.verify(message.option, _depth + 1);
                    if (error)
                        return "option." + error;
                }
                return null;
            };

            /**
             * Creates a RestoreRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.backup.RestoreRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.backup.RestoreRequest} RestoreRequest
             */
            RestoreRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.backup.RestoreRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.backup.RestoreRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.backup.RestoreRequest();
                if (object.file != null)
                    if (typeof object.file !== "string" || object.file.length)
                        message.file = $String(object.file);
                if (object.option != null) {
                    if (!$util.isObject(object.option))
                        throw $TypeError(".lebai.backup.RestoreRequest.option: object expected");
                    message.option = $root.lebai.backup.Options.fromObject(object.option, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a RestoreRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.backup.RestoreRequest
             * @static
             * @param {lebai.backup.RestoreRequest} message RestoreRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RestoreRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.file = "";
                    object.option = null;
                }
                if (message.file != null && $Object.hasOwnProperty.call(message, "file"))
                    object.file = message.file;
                if (message.option != null && $Object.hasOwnProperty.call(message, "option"))
                    object.option = $root.lebai.backup.Options.toObject(message.option, options, _depth + 1);
                return object;
            };

            /**
             * Converts this RestoreRequest to JSON.
             * @function toJSON
             * @memberof lebai.backup.RestoreRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RestoreRequest.prototype.toJSON = function() {
                return RestoreRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for RestoreRequest
             * @function getTypeUrl
             * @memberof lebai.backup.RestoreRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            RestoreRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.backup.RestoreRequest";
            };

            return RestoreRequest;
        })();

        backup.BackupService = (function() {

            /**
             * Constructs a new BackupService service.
             * @memberof lebai.backup
             * @classdesc Represents a BackupService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var BackupService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(BackupService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: BackupService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new BackupService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.backup.BackupService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {BackupService} RPC service. Useful where requests and/or responses are streamed.
             */
            BackupService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.backup.BackupService#clean}.
             * @memberof lebai.backup.BackupService
             * @typedef CleanCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls Clean.
             * @memberof lebai.backup.BackupService
             * @typedef Clean
             * @type {{
             *   (request: lebai.backup.IOptions, callback: lebai.backup.BackupService.CleanCallback): void;
             *   (request: lebai.backup.IOptions): Promise<google.protobuf.Empty>;
             *   readonly name: "Clean";
             *   readonly path: "/lebai.backup.BackupService/Clean";
             *   readonly requestType: "Options";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls Clean.
             * @name lebai.backup.BackupService#clean
             * @type {lebai.backup.BackupService.Clean}
             */
            $Object.defineProperties(BackupService.prototype.clean = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, BackupService.prototype.clean, $root.lebai.backup.Options, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "Clean" },
                path: { value: "/lebai.backup.BackupService/Clean" },
                requestType: { value: "Options" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.backup.BackupService#backup}.
             * @memberof lebai.backup.BackupService
             * @typedef BackupCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls Backup.
             * @memberof lebai.backup.BackupService
             * @typedef Backup
             * @type {{
             *   (request: lebai.backup.IBackupRequest, callback: lebai.backup.BackupService.BackupCallback): void;
             *   (request: lebai.backup.IBackupRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "Backup";
             *   readonly path: "/lebai.backup.BackupService/Backup";
             *   readonly requestType: "BackupRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls Backup.
             * @name lebai.backup.BackupService#backup
             * @type {lebai.backup.BackupService.Backup}
             */
            $Object.defineProperties(BackupService.prototype.backup = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, BackupService.prototype.backup, $root.lebai.backup.BackupRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "Backup" },
                path: { value: "/lebai.backup.BackupService/Backup" },
                requestType: { value: "BackupRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.backup.BackupService#getBackupInfo}.
             * @memberof lebai.backup.BackupService
             * @typedef GetBackupInfoCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.backup.BackupInfo} [response] BackupInfo
             */

            /**
             * Calls GetBackupInfo.
             * @memberof lebai.backup.BackupService
             * @typedef GetBackupInfo
             * @type {{
             *   (request: lebai.backup.IGetBackupInfoRequest, callback: lebai.backup.BackupService.GetBackupInfoCallback): void;
             *   (request: lebai.backup.IGetBackupInfoRequest): Promise<lebai.backup.BackupInfo>;
             *   readonly name: "GetBackupInfo";
             *   readonly path: "/lebai.backup.BackupService/GetBackupInfo";
             *   readonly requestType: "GetBackupInfoRequest";
             *   readonly responseType: "BackupInfo";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetBackupInfo.
             * @name lebai.backup.BackupService#getBackupInfo
             * @type {lebai.backup.BackupService.GetBackupInfo}
             */
            $Object.defineProperties(BackupService.prototype.getBackupInfo = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, BackupService.prototype.getBackupInfo, $root.lebai.backup.GetBackupInfoRequest, $root.lebai.backup.BackupInfo, request, callback);
            }, {
                name: { value: "GetBackupInfo" },
                path: { value: "/lebai.backup.BackupService/GetBackupInfo" },
                requestType: { value: "GetBackupInfoRequest" },
                responseType: { value: "BackupInfo" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.backup.BackupService#restore}.
             * @memberof lebai.backup.BackupService
             * @typedef RestoreCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls Restore.
             * @memberof lebai.backup.BackupService
             * @typedef Restore
             * @type {{
             *   (request: lebai.backup.IRestoreRequest, callback: lebai.backup.BackupService.RestoreCallback): void;
             *   (request: lebai.backup.IRestoreRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "Restore";
             *   readonly path: "/lebai.backup.BackupService/Restore";
             *   readonly requestType: "RestoreRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls Restore.
             * @name lebai.backup.BackupService#restore
             * @type {lebai.backup.BackupService.Restore}
             */
            $Object.defineProperties(BackupService.prototype.restore = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, BackupService.prototype.restore, $root.lebai.backup.RestoreRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "Restore" },
                path: { value: "/lebai.backup.BackupService/Restore" },
                requestType: { value: "RestoreRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return BackupService;
        })();

        return backup;
    })();

    lebai.system = (function() {

        /**
         * Namespace system.
         * @memberof lebai
         * @namespace
         */
        var system = {};

        system.HelloData = (function() {

            /**
             * Properties of a HelloData.
             * @typedef {Object} lebai.system.HelloData.$Properties
             * @property {string|null} [data] HelloData data
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a HelloData.
             * @memberof lebai.system
             * @interface IHelloData
             * @augments lebai.system.HelloData.$Properties
             * @deprecated Use lebai.system.HelloData.$Properties instead.
             */

            /**
             * Shape of a HelloData.
             * @typedef {lebai.system.HelloData.$Properties} lebai.system.HelloData.$Shape
             */

            /**
             * Constructs a new HelloData.
             * @memberof lebai.system
             * @classdesc Represents a HelloData.
             * @constructor
             * @param {lebai.system.HelloData.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var HelloData = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * HelloData data.
             * @member {string} data
             * @memberof lebai.system.HelloData
             * @instance
             */
            HelloData.prototype.data = "";

            /**
             * Creates a new HelloData instance using the specified properties.
             * @function create
             * @memberof lebai.system.HelloData
             * @static
             * @param {lebai.system.HelloData.$Properties=} [properties] Properties to set
             * @returns {lebai.system.HelloData} HelloData instance
             * @type {{
             *   (properties: lebai.system.HelloData.$Shape): lebai.system.HelloData & lebai.system.HelloData.$Shape;
             *   (properties?: lebai.system.HelloData.$Properties): lebai.system.HelloData;
             * }}
             */
            HelloData.create = function(properties) {
                return new HelloData(properties);
            };

            /**
             * Encodes the specified HelloData message. Does not implicitly {@link lebai.system.HelloData.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.HelloData
             * @static
             * @param {lebai.system.HelloData.$Properties} message HelloData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HelloData.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.data);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified HelloData message, length delimited. Does not implicitly {@link lebai.system.HelloData.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.HelloData
             * @static
             * @param {lebai.system.HelloData.$Properties} message HelloData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HelloData.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a HelloData message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.HelloData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.HelloData & lebai.system.HelloData.$Shape} HelloData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HelloData.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.HelloData();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.data = value;
                            else
                                delete message.data;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a HelloData message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.HelloData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.HelloData & lebai.system.HelloData.$Shape} HelloData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HelloData.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a HelloData message.
             * @function verify
             * @memberof lebai.system.HelloData
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HelloData.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    if (!$util.isString(message.data))
                        return "data: string expected";
                return null;
            };

            /**
             * Creates a HelloData message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.HelloData
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.HelloData} HelloData
             */
            HelloData.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.HelloData)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.HelloData: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.HelloData();
                if (object.data != null)
                    if (typeof object.data !== "string" || object.data.length)
                        message.data = $String(object.data);
                return message;
            };

            /**
             * Creates a plain object from a HelloData message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.HelloData
             * @static
             * @param {lebai.system.HelloData} message HelloData
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HelloData.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.data = "";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = message.data;
                return object;
            };

            /**
             * Converts this HelloData to JSON.
             * @function toJSON
             * @memberof lebai.system.HelloData
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HelloData.prototype.toJSON = function() {
                return HelloData.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for HelloData
             * @function getTypeUrl
             * @memberof lebai.system.HelloData
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            HelloData.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.HelloData";
            };

            return HelloData;
        })();

        system.MemoryInfo = (function() {

            /**
             * Properties of a MemoryInfo.
             * @typedef {Object} lebai.system.MemoryInfo.$Properties
             * @property {number|Long|null} [used] MemoryInfo used
             * @property {number|Long|null} [total] MemoryInfo total
             * @property {number|Long|null} [usedSwap] MemoryInfo usedSwap
             * @property {number|Long|null} [totalSwap] MemoryInfo totalSwap
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a MemoryInfo.
             * @memberof lebai.system
             * @interface IMemoryInfo
             * @augments lebai.system.MemoryInfo.$Properties
             * @deprecated Use lebai.system.MemoryInfo.$Properties instead.
             */

            /**
             * Shape of a MemoryInfo.
             * @typedef {lebai.system.MemoryInfo.$Properties} lebai.system.MemoryInfo.$Shape
             */

            /**
             * Constructs a new MemoryInfo.
             * @memberof lebai.system
             * @classdesc Represents a MemoryInfo.
             * @constructor
             * @param {lebai.system.MemoryInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var MemoryInfo = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * MemoryInfo used.
             * @member {number|Long} used
             * @memberof lebai.system.MemoryInfo
             * @instance
             */
            MemoryInfo.prototype.used = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * MemoryInfo total.
             * @member {number|Long} total
             * @memberof lebai.system.MemoryInfo
             * @instance
             */
            MemoryInfo.prototype.total = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * MemoryInfo usedSwap.
             * @member {number|Long} usedSwap
             * @memberof lebai.system.MemoryInfo
             * @instance
             */
            MemoryInfo.prototype.usedSwap = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * MemoryInfo totalSwap.
             * @member {number|Long} totalSwap
             * @memberof lebai.system.MemoryInfo
             * @instance
             */
            MemoryInfo.prototype.totalSwap = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * Creates a new MemoryInfo instance using the specified properties.
             * @function create
             * @memberof lebai.system.MemoryInfo
             * @static
             * @param {lebai.system.MemoryInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.system.MemoryInfo} MemoryInfo instance
             * @type {{
             *   (properties: lebai.system.MemoryInfo.$Shape): lebai.system.MemoryInfo & lebai.system.MemoryInfo.$Shape;
             *   (properties?: lebai.system.MemoryInfo.$Properties): lebai.system.MemoryInfo;
             * }}
             */
            MemoryInfo.create = function(properties) {
                return new MemoryInfo(properties);
            };

            /**
             * Encodes the specified MemoryInfo message. Does not implicitly {@link lebai.system.MemoryInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.MemoryInfo
             * @static
             * @param {lebai.system.MemoryInfo.$Properties} message MemoryInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MemoryInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.used != null && $Object.hasOwnProperty.call(message, "used") && (typeof message.used === "object" ? message.used.low || message.used.high : message.used !== 0))
                    writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.used);
                if (message.total != null && $Object.hasOwnProperty.call(message, "total") && (typeof message.total === "object" ? message.total.low || message.total.high : message.total !== 0))
                    writer.uint32(/* id 12, wireType 0 =*/96).uint64(message.total);
                if (message.usedSwap != null && $Object.hasOwnProperty.call(message, "usedSwap") && (typeof message.usedSwap === "object" ? message.usedSwap.low || message.usedSwap.high : message.usedSwap !== 0))
                    writer.uint32(/* id 21, wireType 0 =*/168).uint64(message.usedSwap);
                if (message.totalSwap != null && $Object.hasOwnProperty.call(message, "totalSwap") && (typeof message.totalSwap === "object" ? message.totalSwap.low || message.totalSwap.high : message.totalSwap !== 0))
                    writer.uint32(/* id 22, wireType 0 =*/176).uint64(message.totalSwap);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MemoryInfo message, length delimited. Does not implicitly {@link lebai.system.MemoryInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.MemoryInfo
             * @static
             * @param {lebai.system.MemoryInfo.$Properties} message MemoryInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MemoryInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a MemoryInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.MemoryInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.MemoryInfo & lebai.system.MemoryInfo.$Shape} MemoryInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MemoryInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.MemoryInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.used = value;
                            else
                                delete message.used;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.total = value;
                            else
                                delete message.total;
                            continue;
                        }
                    case 21: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.usedSwap = value;
                            else
                                delete message.usedSwap;
                            continue;
                        }
                    case 22: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.totalSwap = value;
                            else
                                delete message.totalSwap;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a MemoryInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.MemoryInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.MemoryInfo & lebai.system.MemoryInfo.$Shape} MemoryInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MemoryInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MemoryInfo message.
             * @function verify
             * @memberof lebai.system.MemoryInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MemoryInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.used != null && $Object.hasOwnProperty.call(message, "used"))
                    if (!$util.isInteger(message.used) && !(message.used && $util.isInteger(message.used.low) && $util.isInteger(message.used.high)))
                        return "used: integer|Long expected";
                if (message.total != null && $Object.hasOwnProperty.call(message, "total"))
                    if (!$util.isInteger(message.total) && !(message.total && $util.isInteger(message.total.low) && $util.isInteger(message.total.high)))
                        return "total: integer|Long expected";
                if (message.usedSwap != null && $Object.hasOwnProperty.call(message, "usedSwap"))
                    if (!$util.isInteger(message.usedSwap) && !(message.usedSwap && $util.isInteger(message.usedSwap.low) && $util.isInteger(message.usedSwap.high)))
                        return "usedSwap: integer|Long expected";
                if (message.totalSwap != null && $Object.hasOwnProperty.call(message, "totalSwap"))
                    if (!$util.isInteger(message.totalSwap) && !(message.totalSwap && $util.isInteger(message.totalSwap.low) && $util.isInteger(message.totalSwap.high)))
                        return "totalSwap: integer|Long expected";
                return null;
            };

            /**
             * Creates a MemoryInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.MemoryInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.MemoryInfo} MemoryInfo
             */
            MemoryInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.MemoryInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.MemoryInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.MemoryInfo();
                if (object.used != null)
                    if (typeof object.used === "object" ? object.used.low || object.used.high : $Number(object.used) !== 0)
                        if ($util.Long)
                            message.used = $util.Long.fromValue(object.used, true);
                        else if (typeof object.used === "string")
                            message.used = $parseInt(object.used, 10);
                        else if (typeof object.used === "number")
                            message.used = object.used;
                        else if (typeof object.used === "object")
                            message.used = new $util.LongBits(object.used.low >>> 0, object.used.high >>> 0).toNumber(true);
                if (object.total != null)
                    if (typeof object.total === "object" ? object.total.low || object.total.high : $Number(object.total) !== 0)
                        if ($util.Long)
                            message.total = $util.Long.fromValue(object.total, true);
                        else if (typeof object.total === "string")
                            message.total = $parseInt(object.total, 10);
                        else if (typeof object.total === "number")
                            message.total = object.total;
                        else if (typeof object.total === "object")
                            message.total = new $util.LongBits(object.total.low >>> 0, object.total.high >>> 0).toNumber(true);
                if (object.usedSwap != null)
                    if (typeof object.usedSwap === "object" ? object.usedSwap.low || object.usedSwap.high : $Number(object.usedSwap) !== 0)
                        if ($util.Long)
                            message.usedSwap = $util.Long.fromValue(object.usedSwap, true);
                        else if (typeof object.usedSwap === "string")
                            message.usedSwap = $parseInt(object.usedSwap, 10);
                        else if (typeof object.usedSwap === "number")
                            message.usedSwap = object.usedSwap;
                        else if (typeof object.usedSwap === "object")
                            message.usedSwap = new $util.LongBits(object.usedSwap.low >>> 0, object.usedSwap.high >>> 0).toNumber(true);
                if (object.totalSwap != null)
                    if (typeof object.totalSwap === "object" ? object.totalSwap.low || object.totalSwap.high : $Number(object.totalSwap) !== 0)
                        if ($util.Long)
                            message.totalSwap = $util.Long.fromValue(object.totalSwap, true);
                        else if (typeof object.totalSwap === "string")
                            message.totalSwap = $parseInt(object.totalSwap, 10);
                        else if (typeof object.totalSwap === "number")
                            message.totalSwap = object.totalSwap;
                        else if (typeof object.totalSwap === "object")
                            message.totalSwap = new $util.LongBits(object.totalSwap.low >>> 0, object.totalSwap.high >>> 0).toNumber(true);
                return message;
            };

            /**
             * Creates a plain object from a MemoryInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.MemoryInfo
             * @static
             * @param {lebai.system.MemoryInfo} message MemoryInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MemoryInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, true);
                        object.used = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.used = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, true);
                        object.total = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.total = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, true);
                        object.usedSwap = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.usedSwap = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, true);
                        object.totalSwap = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.totalSwap = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                }
                if (message.used != null && $Object.hasOwnProperty.call(message, "used"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.used = typeof message.used === "number" ? $BigInt(message.used) : $util.Long.fromBits(message.used.low >>> 0, message.used.high >>> 0, true).toBigInt();
                    else if (typeof message.used === "number")
                        object.used = options.longs === $String ? $String(message.used) : message.used;
                    else
                        object.used = options.longs === $String ? $util.Long.prototype.toString.call(message.used) : options.longs === $Number ? new $util.LongBits(message.used.low >>> 0, message.used.high >>> 0).toNumber(true) : message.used;
                if (message.total != null && $Object.hasOwnProperty.call(message, "total"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.total = typeof message.total === "number" ? $BigInt(message.total) : $util.Long.fromBits(message.total.low >>> 0, message.total.high >>> 0, true).toBigInt();
                    else if (typeof message.total === "number")
                        object.total = options.longs === $String ? $String(message.total) : message.total;
                    else
                        object.total = options.longs === $String ? $util.Long.prototype.toString.call(message.total) : options.longs === $Number ? new $util.LongBits(message.total.low >>> 0, message.total.high >>> 0).toNumber(true) : message.total;
                if (message.usedSwap != null && $Object.hasOwnProperty.call(message, "usedSwap"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.usedSwap = typeof message.usedSwap === "number" ? $BigInt(message.usedSwap) : $util.Long.fromBits(message.usedSwap.low >>> 0, message.usedSwap.high >>> 0, true).toBigInt();
                    else if (typeof message.usedSwap === "number")
                        object.usedSwap = options.longs === $String ? $String(message.usedSwap) : message.usedSwap;
                    else
                        object.usedSwap = options.longs === $String ? $util.Long.prototype.toString.call(message.usedSwap) : options.longs === $Number ? new $util.LongBits(message.usedSwap.low >>> 0, message.usedSwap.high >>> 0).toNumber(true) : message.usedSwap;
                if (message.totalSwap != null && $Object.hasOwnProperty.call(message, "totalSwap"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.totalSwap = typeof message.totalSwap === "number" ? $BigInt(message.totalSwap) : $util.Long.fromBits(message.totalSwap.low >>> 0, message.totalSwap.high >>> 0, true).toBigInt();
                    else if (typeof message.totalSwap === "number")
                        object.totalSwap = options.longs === $String ? $String(message.totalSwap) : message.totalSwap;
                    else
                        object.totalSwap = options.longs === $String ? $util.Long.prototype.toString.call(message.totalSwap) : options.longs === $Number ? new $util.LongBits(message.totalSwap.low >>> 0, message.totalSwap.high >>> 0).toNumber(true) : message.totalSwap;
                return object;
            };

            /**
             * Converts this MemoryInfo to JSON.
             * @function toJSON
             * @memberof lebai.system.MemoryInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MemoryInfo.prototype.toJSON = function() {
                return MemoryInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for MemoryInfo
             * @function getTypeUrl
             * @memberof lebai.system.MemoryInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            MemoryInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.MemoryInfo";
            };

            return MemoryInfo;
        })();

        system.DiskInfo = (function() {

            /**
             * Properties of a DiskInfo.
             * @typedef {Object} lebai.system.DiskInfo.$Properties
             * @property {string|null} [name] DiskInfo name
             * @property {number|Long|null} [used] DiskInfo used
             * @property {number|Long|null} [total] DiskInfo total
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DiskInfo.
             * @memberof lebai.system
             * @interface IDiskInfo
             * @augments lebai.system.DiskInfo.$Properties
             * @deprecated Use lebai.system.DiskInfo.$Properties instead.
             */

            /**
             * Shape of a DiskInfo.
             * @typedef {lebai.system.DiskInfo.$Properties} lebai.system.DiskInfo.$Shape
             */

            /**
             * Constructs a new DiskInfo.
             * @memberof lebai.system
             * @classdesc Represents a DiskInfo.
             * @constructor
             * @param {lebai.system.DiskInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var DiskInfo = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DiskInfo name.
             * @member {string} name
             * @memberof lebai.system.DiskInfo
             * @instance
             */
            DiskInfo.prototype.name = "";

            /**
             * DiskInfo used.
             * @member {number|Long} used
             * @memberof lebai.system.DiskInfo
             * @instance
             */
            DiskInfo.prototype.used = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * DiskInfo total.
             * @member {number|Long} total
             * @memberof lebai.system.DiskInfo
             * @instance
             */
            DiskInfo.prototype.total = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * Creates a new DiskInfo instance using the specified properties.
             * @function create
             * @memberof lebai.system.DiskInfo
             * @static
             * @param {lebai.system.DiskInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.system.DiskInfo} DiskInfo instance
             * @type {{
             *   (properties: lebai.system.DiskInfo.$Shape): lebai.system.DiskInfo & lebai.system.DiskInfo.$Shape;
             *   (properties?: lebai.system.DiskInfo.$Properties): lebai.system.DiskInfo;
             * }}
             */
            DiskInfo.create = function(properties) {
                return new DiskInfo(properties);
            };

            /**
             * Encodes the specified DiskInfo message. Does not implicitly {@link lebai.system.DiskInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.DiskInfo
             * @static
             * @param {lebai.system.DiskInfo.$Properties} message DiskInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DiskInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.used != null && $Object.hasOwnProperty.call(message, "used") && (typeof message.used === "object" ? message.used.low || message.used.high : message.used !== 0))
                    writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.used);
                if (message.total != null && $Object.hasOwnProperty.call(message, "total") && (typeof message.total === "object" ? message.total.low || message.total.high : message.total !== 0))
                    writer.uint32(/* id 12, wireType 0 =*/96).uint64(message.total);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DiskInfo message, length delimited. Does not implicitly {@link lebai.system.DiskInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.DiskInfo
             * @static
             * @param {lebai.system.DiskInfo.$Properties} message DiskInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DiskInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a DiskInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.DiskInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.DiskInfo & lebai.system.DiskInfo.$Shape} DiskInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DiskInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.DiskInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.used = value;
                            else
                                delete message.used;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.total = value;
                            else
                                delete message.total;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a DiskInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.DiskInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.DiskInfo & lebai.system.DiskInfo.$Shape} DiskInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DiskInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DiskInfo message.
             * @function verify
             * @memberof lebai.system.DiskInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DiskInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.used != null && $Object.hasOwnProperty.call(message, "used"))
                    if (!$util.isInteger(message.used) && !(message.used && $util.isInteger(message.used.low) && $util.isInteger(message.used.high)))
                        return "used: integer|Long expected";
                if (message.total != null && $Object.hasOwnProperty.call(message, "total"))
                    if (!$util.isInteger(message.total) && !(message.total && $util.isInteger(message.total.low) && $util.isInteger(message.total.high)))
                        return "total: integer|Long expected";
                return null;
            };

            /**
             * Creates a DiskInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.DiskInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.DiskInfo} DiskInfo
             */
            DiskInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.DiskInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.DiskInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.DiskInfo();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.used != null)
                    if (typeof object.used === "object" ? object.used.low || object.used.high : $Number(object.used) !== 0)
                        if ($util.Long)
                            message.used = $util.Long.fromValue(object.used, true);
                        else if (typeof object.used === "string")
                            message.used = $parseInt(object.used, 10);
                        else if (typeof object.used === "number")
                            message.used = object.used;
                        else if (typeof object.used === "object")
                            message.used = new $util.LongBits(object.used.low >>> 0, object.used.high >>> 0).toNumber(true);
                if (object.total != null)
                    if (typeof object.total === "object" ? object.total.low || object.total.high : $Number(object.total) !== 0)
                        if ($util.Long)
                            message.total = $util.Long.fromValue(object.total, true);
                        else if (typeof object.total === "string")
                            message.total = $parseInt(object.total, 10);
                        else if (typeof object.total === "number")
                            message.total = object.total;
                        else if (typeof object.total === "object")
                            message.total = new $util.LongBits(object.total.low >>> 0, object.total.high >>> 0).toNumber(true);
                return message;
            };

            /**
             * Creates a plain object from a DiskInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.DiskInfo
             * @static
             * @param {lebai.system.DiskInfo} message DiskInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DiskInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, true);
                        object.used = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.used = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, true);
                        object.total = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.total = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.used != null && $Object.hasOwnProperty.call(message, "used"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.used = typeof message.used === "number" ? $BigInt(message.used) : $util.Long.fromBits(message.used.low >>> 0, message.used.high >>> 0, true).toBigInt();
                    else if (typeof message.used === "number")
                        object.used = options.longs === $String ? $String(message.used) : message.used;
                    else
                        object.used = options.longs === $String ? $util.Long.prototype.toString.call(message.used) : options.longs === $Number ? new $util.LongBits(message.used.low >>> 0, message.used.high >>> 0).toNumber(true) : message.used;
                if (message.total != null && $Object.hasOwnProperty.call(message, "total"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.total = typeof message.total === "number" ? $BigInt(message.total) : $util.Long.fromBits(message.total.low >>> 0, message.total.high >>> 0, true).toBigInt();
                    else if (typeof message.total === "number")
                        object.total = options.longs === $String ? $String(message.total) : message.total;
                    else
                        object.total = options.longs === $String ? $util.Long.prototype.toString.call(message.total) : options.longs === $Number ? new $util.LongBits(message.total.low >>> 0, message.total.high >>> 0).toNumber(true) : message.total;
                return object;
            };

            /**
             * Converts this DiskInfo to JSON.
             * @function toJSON
             * @memberof lebai.system.DiskInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DiskInfo.prototype.toJSON = function() {
                return DiskInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DiskInfo
             * @function getTypeUrl
             * @memberof lebai.system.DiskInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DiskInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.DiskInfo";
            };

            return DiskInfo;
        })();

        /**
         * NetworkKind enum.
         * @name lebai.system.NetworkKind
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} LOOPBACK=1 LOOPBACK value
         * @property {number} ETHERNET=2 ETHERNET value
         * @property {number} WIRELESS=3 WIRELESS value
         * @property {number} CELLULAR=4 CELLULAR value
         * @property {number} USB=5 USB value
         * @property {number} DOCKER=11 DOCKER value
         */
        system.NetworkKind = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "LOOPBACK"] = 1;
            values[valuesById[2] = "ETHERNET"] = 2;
            values[valuesById[3] = "WIRELESS"] = 3;
            values[valuesById[4] = "CELLULAR"] = 4;
            values[valuesById[5] = "USB"] = 5;
            values[valuesById[11] = "DOCKER"] = 11;
            return values;
        })();

        system.NetworkInfo = (function() {

            /**
             * Properties of a NetworkInfo.
             * @typedef {Object} lebai.system.NetworkInfo.$Properties
             * @property {string|null} [ifname] NetworkInfo ifname
             * @property {lebai.system.NetworkKind|null} [kind] NetworkInfo kind
             * @property {string|null} [mac] NetworkInfo mac
             * @property {Array.<string>|null} [ips] NetworkInfo ips
             * @property {Array.<string>|null} [virtualIps] NetworkInfo virtualIps
             * @property {string|null} [gateway] NetworkInfo gateway
             * @property {string|null} [netmask] NetworkInfo netmask
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a NetworkInfo.
             * @memberof lebai.system
             * @interface INetworkInfo
             * @augments lebai.system.NetworkInfo.$Properties
             * @deprecated Use lebai.system.NetworkInfo.$Properties instead.
             */

            /**
             * Shape of a NetworkInfo.
             * @typedef {lebai.system.NetworkInfo.$Properties} lebai.system.NetworkInfo.$Shape
             */

            /**
             * Constructs a new NetworkInfo.
             * @memberof lebai.system
             * @classdesc Represents a NetworkInfo.
             * @constructor
             * @param {lebai.system.NetworkInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var NetworkInfo = function (properties) {
                this.ips = [];
                this.virtualIps = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * NetworkInfo ifname.
             * @member {string} ifname
             * @memberof lebai.system.NetworkInfo
             * @instance
             */
            NetworkInfo.prototype.ifname = "";

            /**
             * NetworkInfo kind.
             * @member {lebai.system.NetworkKind} kind
             * @memberof lebai.system.NetworkInfo
             * @instance
             */
            NetworkInfo.prototype.kind = 0;

            /**
             * NetworkInfo mac.
             * @member {string} mac
             * @memberof lebai.system.NetworkInfo
             * @instance
             */
            NetworkInfo.prototype.mac = "";

            /**
             * NetworkInfo ips.
             * @member {Array.<string>} ips
             * @memberof lebai.system.NetworkInfo
             * @instance
             */
            NetworkInfo.prototype.ips = $util.emptyArray;

            /**
             * NetworkInfo virtualIps.
             * @member {Array.<string>} virtualIps
             * @memberof lebai.system.NetworkInfo
             * @instance
             */
            NetworkInfo.prototype.virtualIps = $util.emptyArray;

            /**
             * NetworkInfo gateway.
             * @member {string} gateway
             * @memberof lebai.system.NetworkInfo
             * @instance
             */
            NetworkInfo.prototype.gateway = "";

            /**
             * NetworkInfo netmask.
             * @member {string} netmask
             * @memberof lebai.system.NetworkInfo
             * @instance
             */
            NetworkInfo.prototype.netmask = "";

            /**
             * Creates a new NetworkInfo instance using the specified properties.
             * @function create
             * @memberof lebai.system.NetworkInfo
             * @static
             * @param {lebai.system.NetworkInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.system.NetworkInfo} NetworkInfo instance
             * @type {{
             *   (properties: lebai.system.NetworkInfo.$Shape): lebai.system.NetworkInfo & lebai.system.NetworkInfo.$Shape;
             *   (properties?: lebai.system.NetworkInfo.$Properties): lebai.system.NetworkInfo;
             * }}
             */
            NetworkInfo.create = function(properties) {
                return new NetworkInfo(properties);
            };

            /**
             * Encodes the specified NetworkInfo message. Does not implicitly {@link lebai.system.NetworkInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.NetworkInfo
             * @static
             * @param {lebai.system.NetworkInfo.$Properties} message NetworkInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NetworkInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.ifname != null && $Object.hasOwnProperty.call(message, "ifname") && message.ifname !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.ifname);
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind") && message.kind !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.kind);
                if (message.mac != null && $Object.hasOwnProperty.call(message, "mac") && message.mac !== "")
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.mac);
                if (message.ips != null && message.ips.length)
                    for (var i = 0; i < message.ips.length; ++i)
                        writer.uint32(/* id 11, wireType 2 =*/90).string(message.ips[i]);
                if (message.virtualIps != null && message.virtualIps.length)
                    for (var i = 0; i < message.virtualIps.length; ++i)
                        writer.uint32(/* id 12, wireType 2 =*/98).string(message.virtualIps[i]);
                if (message.gateway != null && $Object.hasOwnProperty.call(message, "gateway") && message.gateway !== "")
                    writer.uint32(/* id 21, wireType 2 =*/170).string(message.gateway);
                if (message.netmask != null && $Object.hasOwnProperty.call(message, "netmask") && message.netmask !== "")
                    writer.uint32(/* id 22, wireType 2 =*/178).string(message.netmask);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified NetworkInfo message, length delimited. Does not implicitly {@link lebai.system.NetworkInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.NetworkInfo
             * @static
             * @param {lebai.system.NetworkInfo.$Properties} message NetworkInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NetworkInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a NetworkInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.NetworkInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.NetworkInfo & lebai.system.NetworkInfo.$Shape} NetworkInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NetworkInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.NetworkInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.ifname = value;
                            else
                                delete message.ifname;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.kind = value;
                            else
                                delete message.kind;
                            continue;
                        }
                    case 9: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.mac = value;
                            else
                                delete message.mac;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if (!(message.ips && message.ips.length))
                                message.ips = [];
                            message.ips.push(reader.stringVerify());
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            if (!(message.virtualIps && message.virtualIps.length))
                                message.virtualIps = [];
                            message.virtualIps.push(reader.stringVerify());
                            continue;
                        }
                    case 21: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.gateway = value;
                            else
                                delete message.gateway;
                            continue;
                        }
                    case 22: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.netmask = value;
                            else
                                delete message.netmask;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a NetworkInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.NetworkInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.NetworkInfo & lebai.system.NetworkInfo.$Shape} NetworkInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NetworkInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a NetworkInfo message.
             * @function verify
             * @memberof lebai.system.NetworkInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            NetworkInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.ifname != null && $Object.hasOwnProperty.call(message, "ifname"))
                    if (!$util.isString(message.ifname))
                        return "ifname: string expected";
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    if (typeof message.kind !== "number" || (message.kind | 0) !== message.kind)
                        return "kind: enum value expected";
                if (message.mac != null && $Object.hasOwnProperty.call(message, "mac"))
                    if (!$util.isString(message.mac))
                        return "mac: string expected";
                if (message.ips != null && $Object.hasOwnProperty.call(message, "ips")) {
                    if (!$Array.isArray(message.ips))
                        return "ips: array expected";
                    for (var i = 0; i < message.ips.length; ++i)
                        if (!$util.isString(message.ips[i]))
                            return "ips: string[] expected";
                }
                if (message.virtualIps != null && $Object.hasOwnProperty.call(message, "virtualIps")) {
                    if (!$Array.isArray(message.virtualIps))
                        return "virtualIps: array expected";
                    for (var i = 0; i < message.virtualIps.length; ++i)
                        if (!$util.isString(message.virtualIps[i]))
                            return "virtualIps: string[] expected";
                }
                if (message.gateway != null && $Object.hasOwnProperty.call(message, "gateway"))
                    if (!$util.isString(message.gateway))
                        return "gateway: string expected";
                if (message.netmask != null && $Object.hasOwnProperty.call(message, "netmask"))
                    if (!$util.isString(message.netmask))
                        return "netmask: string expected";
                return null;
            };

            /**
             * Creates a NetworkInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.NetworkInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.NetworkInfo} NetworkInfo
             */
            NetworkInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.NetworkInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.NetworkInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.NetworkInfo();
                if (object.ifname != null)
                    if (typeof object.ifname !== "string" || object.ifname.length)
                        message.ifname = $String(object.ifname);
                if (object.kind !== 0 && (typeof object.kind !== "string" || $root.lebai.system.NetworkKind[object.kind] !== 0))
                    switch (object.kind) {
                    case "UNKNOWN":
                    case 0:
                        message.kind = 0;
                        break;
                    case "LOOPBACK":
                    case 1:
                        message.kind = 1;
                        break;
                    case "ETHERNET":
                    case 2:
                        message.kind = 2;
                        break;
                    case "WIRELESS":
                    case 3:
                        message.kind = 3;
                        break;
                    case "CELLULAR":
                    case 4:
                        message.kind = 4;
                        break;
                    case "USB":
                    case 5:
                        message.kind = 5;
                        break;
                    case "DOCKER":
                    case 11:
                        message.kind = 11;
                        break;
                    default:
                        if (typeof object.kind === "number" && (object.kind | 0) === object.kind)
                            message.kind = object.kind;
                    }
                if (object.mac != null)
                    if (typeof object.mac !== "string" || object.mac.length)
                        message.mac = $String(object.mac);
                if (object.ips) {
                    if (!$Array.isArray(object.ips))
                        throw $TypeError(".lebai.system.NetworkInfo.ips: array expected");
                    message.ips = $Array(object.ips.length);
                    for (var i = 0; i < object.ips.length; ++i)
                        message.ips[i] = $String(object.ips[i]);
                }
                if (object.virtualIps) {
                    if (!$Array.isArray(object.virtualIps))
                        throw $TypeError(".lebai.system.NetworkInfo.virtualIps: array expected");
                    message.virtualIps = $Array(object.virtualIps.length);
                    for (var i = 0; i < object.virtualIps.length; ++i)
                        message.virtualIps[i] = $String(object.virtualIps[i]);
                }
                if (object.gateway != null)
                    if (typeof object.gateway !== "string" || object.gateway.length)
                        message.gateway = $String(object.gateway);
                if (object.netmask != null)
                    if (typeof object.netmask !== "string" || object.netmask.length)
                        message.netmask = $String(object.netmask);
                return message;
            };

            /**
             * Creates a plain object from a NetworkInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.NetworkInfo
             * @static
             * @param {lebai.system.NetworkInfo} message NetworkInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            NetworkInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.ips = [];
                    object.virtualIps = [];
                }
                if (options.defaults) {
                    object.ifname = "";
                    object.kind = options.enums === $String ? "UNKNOWN" : 0;
                    object.mac = "";
                    object.gateway = "";
                    object.netmask = "";
                }
                if (message.ifname != null && $Object.hasOwnProperty.call(message, "ifname"))
                    object.ifname = message.ifname;
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    object.kind = options.enums === $String ? $root.lebai.system.NetworkKind[message.kind] === $undefined ? message.kind : $root.lebai.system.NetworkKind[message.kind] : message.kind;
                if (message.mac != null && $Object.hasOwnProperty.call(message, "mac"))
                    object.mac = message.mac;
                if (message.ips && message.ips.length) {
                    object.ips = $Array(message.ips.length);
                    for (var j = 0; j < message.ips.length; ++j)
                        object.ips[j] = message.ips[j];
                }
                if (message.virtualIps && message.virtualIps.length) {
                    object.virtualIps = $Array(message.virtualIps.length);
                    for (var j = 0; j < message.virtualIps.length; ++j)
                        object.virtualIps[j] = message.virtualIps[j];
                }
                if (message.gateway != null && $Object.hasOwnProperty.call(message, "gateway"))
                    object.gateway = message.gateway;
                if (message.netmask != null && $Object.hasOwnProperty.call(message, "netmask"))
                    object.netmask = message.netmask;
                return object;
            };

            /**
             * Converts this NetworkInfo to JSON.
             * @function toJSON
             * @memberof lebai.system.NetworkInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            NetworkInfo.prototype.toJSON = function() {
                return NetworkInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for NetworkInfo
             * @function getTypeUrl
             * @memberof lebai.system.NetworkInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            NetworkInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.NetworkInfo";
            };

            return NetworkInfo;
        })();

        system.CpuInfo = (function() {

            /**
             * Properties of a CpuInfo.
             * @typedef {Object} lebai.system.CpuInfo.$Properties
             * @property {string|null} [brand] CpuInfo brand
             * @property {number|null} [num] CpuInfo num
             * @property {number|Long|null} [frequency] CpuInfo frequency
             * @property {number|null} [temperature] CpuInfo temperature
             * @property {number|null} [critical] CpuInfo critical
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CpuInfo.
             * @memberof lebai.system
             * @interface ICpuInfo
             * @augments lebai.system.CpuInfo.$Properties
             * @deprecated Use lebai.system.CpuInfo.$Properties instead.
             */

            /**
             * Shape of a CpuInfo.
             * @typedef {lebai.system.CpuInfo.$Properties} lebai.system.CpuInfo.$Shape
             */

            /**
             * Constructs a new CpuInfo.
             * @memberof lebai.system
             * @classdesc Represents a CpuInfo.
             * @constructor
             * @param {lebai.system.CpuInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var CpuInfo = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CpuInfo brand.
             * @member {string} brand
             * @memberof lebai.system.CpuInfo
             * @instance
             */
            CpuInfo.prototype.brand = "";

            /**
             * CpuInfo num.
             * @member {number} num
             * @memberof lebai.system.CpuInfo
             * @instance
             */
            CpuInfo.prototype.num = 0;

            /**
             * CpuInfo frequency.
             * @member {number|Long} frequency
             * @memberof lebai.system.CpuInfo
             * @instance
             */
            CpuInfo.prototype.frequency = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

            /**
             * CpuInfo temperature.
             * @member {number} temperature
             * @memberof lebai.system.CpuInfo
             * @instance
             */
            CpuInfo.prototype.temperature = 0;

            /**
             * CpuInfo critical.
             * @member {number} critical
             * @memberof lebai.system.CpuInfo
             * @instance
             */
            CpuInfo.prototype.critical = 0;

            /**
             * Creates a new CpuInfo instance using the specified properties.
             * @function create
             * @memberof lebai.system.CpuInfo
             * @static
             * @param {lebai.system.CpuInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.system.CpuInfo} CpuInfo instance
             * @type {{
             *   (properties: lebai.system.CpuInfo.$Shape): lebai.system.CpuInfo & lebai.system.CpuInfo.$Shape;
             *   (properties?: lebai.system.CpuInfo.$Properties): lebai.system.CpuInfo;
             * }}
             */
            CpuInfo.create = function(properties) {
                return new CpuInfo(properties);
            };

            /**
             * Encodes the specified CpuInfo message. Does not implicitly {@link lebai.system.CpuInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.CpuInfo
             * @static
             * @param {lebai.system.CpuInfo.$Properties} message CpuInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CpuInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.brand != null && $Object.hasOwnProperty.call(message, "brand") && message.brand !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.brand);
                if (message.num != null && $Object.hasOwnProperty.call(message, "num") && message.num !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.num);
                if (message.frequency != null && $Object.hasOwnProperty.call(message, "frequency") && (typeof message.frequency === "object" ? message.frequency.low || message.frequency.high : message.frequency !== 0))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.frequency);
                if (message.temperature != null && $Object.hasOwnProperty.call(message, "temperature") && !$Object.is(message.temperature, 0))
                    writer.uint32(/* id 21, wireType 5 =*/173).float(message.temperature);
                if (message.critical != null && $Object.hasOwnProperty.call(message, "critical") && !$Object.is(message.critical, 0))
                    writer.uint32(/* id 22, wireType 5 =*/181).float(message.critical);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CpuInfo message, length delimited. Does not implicitly {@link lebai.system.CpuInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.CpuInfo
             * @static
             * @param {lebai.system.CpuInfo.$Properties} message CpuInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CpuInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a CpuInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.CpuInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.CpuInfo & lebai.system.CpuInfo.$Shape} CpuInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CpuInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.CpuInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.brand = value;
                            else
                                delete message.brand;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.num = value;
                            else
                                delete message.num;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.frequency = value;
                            else
                                delete message.frequency;
                            continue;
                        }
                    case 21: {
                            if (wireType !== 5)
                                break;
                            if (!$Object.is(value = reader.float(), 0))
                                message.temperature = value;
                            else
                                delete message.temperature;
                            continue;
                        }
                    case 22: {
                            if (wireType !== 5)
                                break;
                            if (!$Object.is(value = reader.float(), 0))
                                message.critical = value;
                            else
                                delete message.critical;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a CpuInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.CpuInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.CpuInfo & lebai.system.CpuInfo.$Shape} CpuInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CpuInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CpuInfo message.
             * @function verify
             * @memberof lebai.system.CpuInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CpuInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.brand != null && $Object.hasOwnProperty.call(message, "brand"))
                    if (!$util.isString(message.brand))
                        return "brand: string expected";
                if (message.num != null && $Object.hasOwnProperty.call(message, "num"))
                    if (!$util.isInteger(message.num))
                        return "num: integer expected";
                if (message.frequency != null && $Object.hasOwnProperty.call(message, "frequency"))
                    if (!$util.isInteger(message.frequency) && !(message.frequency && $util.isInteger(message.frequency.low) && $util.isInteger(message.frequency.high)))
                        return "frequency: integer|Long expected";
                if (message.temperature != null && $Object.hasOwnProperty.call(message, "temperature"))
                    if (typeof message.temperature !== "number")
                        return "temperature: number expected";
                if (message.critical != null && $Object.hasOwnProperty.call(message, "critical"))
                    if (typeof message.critical !== "number")
                        return "critical: number expected";
                return null;
            };

            /**
             * Creates a CpuInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.CpuInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.CpuInfo} CpuInfo
             */
            CpuInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.CpuInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.CpuInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.CpuInfo();
                if (object.brand != null)
                    if (typeof object.brand !== "string" || object.brand.length)
                        message.brand = $String(object.brand);
                if (object.num != null)
                    if ($Number(object.num) !== 0)
                        message.num = object.num >>> 0;
                if (object.frequency != null)
                    if (typeof object.frequency === "object" ? object.frequency.low || object.frequency.high : $Number(object.frequency) !== 0)
                        if ($util.Long)
                            message.frequency = $util.Long.fromValue(object.frequency, true);
                        else if (typeof object.frequency === "string")
                            message.frequency = $parseInt(object.frequency, 10);
                        else if (typeof object.frequency === "number")
                            message.frequency = object.frequency;
                        else if (typeof object.frequency === "object")
                            message.frequency = new $util.LongBits(object.frequency.low >>> 0, object.frequency.high >>> 0).toNumber(true);
                if (object.temperature != null)
                    if (!$Object.is($Number(object.temperature), 0))
                        message.temperature = $Number(object.temperature);
                if (object.critical != null)
                    if (!$Object.is($Number(object.critical), 0))
                        message.critical = $Number(object.critical);
                return message;
            };

            /**
             * Creates a plain object from a CpuInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.CpuInfo
             * @static
             * @param {lebai.system.CpuInfo} message CpuInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CpuInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.brand = "";
                    object.num = 0;
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, true);
                        object.frequency = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.frequency = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.temperature = 0;
                    object.critical = 0;
                }
                if (message.brand != null && $Object.hasOwnProperty.call(message, "brand"))
                    object.brand = message.brand;
                if (message.num != null && $Object.hasOwnProperty.call(message, "num"))
                    object.num = message.num;
                if (message.frequency != null && $Object.hasOwnProperty.call(message, "frequency"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.frequency = typeof message.frequency === "number" ? $BigInt(message.frequency) : $util.Long.fromBits(message.frequency.low >>> 0, message.frequency.high >>> 0, true).toBigInt();
                    else if (typeof message.frequency === "number")
                        object.frequency = options.longs === $String ? $String(message.frequency) : message.frequency;
                    else
                        object.frequency = options.longs === $String ? $util.Long.prototype.toString.call(message.frequency) : options.longs === $Number ? new $util.LongBits(message.frequency.low >>> 0, message.frequency.high >>> 0).toNumber(true) : message.frequency;
                if (message.temperature != null && $Object.hasOwnProperty.call(message, "temperature"))
                    object.temperature = options.json && !$isFinite(message.temperature) ? $String(message.temperature) : message.temperature;
                if (message.critical != null && $Object.hasOwnProperty.call(message, "critical"))
                    object.critical = options.json && !$isFinite(message.critical) ? $String(message.critical) : message.critical;
                return object;
            };

            /**
             * Converts this CpuInfo to JSON.
             * @function toJSON
             * @memberof lebai.system.CpuInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CpuInfo.prototype.toJSON = function() {
                return CpuInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CpuInfo
             * @function getTypeUrl
             * @memberof lebai.system.CpuInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CpuInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.CpuInfo";
            };

            return CpuInfo;
        })();

        system.SystemInfo = (function() {

            /**
             * Properties of a SystemInfo.
             * @typedef {Object} lebai.system.SystemInfo.$Properties
             * @property {string|null} [name] SystemInfo name
             * @property {string|null} [kernelVersion] SystemInfo kernelVersion
             * @property {string|null} [osVersion] SystemInfo osVersion
             * @property {string|null} [hostName] SystemInfo hostName
             * @property {lebai.system.MemoryInfo.$Properties|null} [memory] SystemInfo memory
             * @property {Array.<lebai.system.DiskInfo.$Properties>|null} [disks] SystemInfo disks
             * @property {Array.<lebai.system.NetworkInfo.$Properties>|null} [networks] SystemInfo networks
             * @property {lebai.system.CpuInfo.$Properties|null} [cpu] SystemInfo cpu
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SystemInfo.
             * @memberof lebai.system
             * @interface ISystemInfo
             * @augments lebai.system.SystemInfo.$Properties
             * @deprecated Use lebai.system.SystemInfo.$Properties instead.
             */

            /**
             * Shape of a SystemInfo.
             * @typedef {lebai.system.SystemInfo.$Properties} lebai.system.SystemInfo.$Shape
             */

            /**
             * Constructs a new SystemInfo.
             * @memberof lebai.system
             * @classdesc Represents a SystemInfo.
             * @constructor
             * @param {lebai.system.SystemInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SystemInfo = function (properties) {
                this.disks = [];
                this.networks = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SystemInfo name.
             * @member {string} name
             * @memberof lebai.system.SystemInfo
             * @instance
             */
            SystemInfo.prototype.name = "";

            /**
             * SystemInfo kernelVersion.
             * @member {string} kernelVersion
             * @memberof lebai.system.SystemInfo
             * @instance
             */
            SystemInfo.prototype.kernelVersion = "";

            /**
             * SystemInfo osVersion.
             * @member {string} osVersion
             * @memberof lebai.system.SystemInfo
             * @instance
             */
            SystemInfo.prototype.osVersion = "";

            /**
             * SystemInfo hostName.
             * @member {string} hostName
             * @memberof lebai.system.SystemInfo
             * @instance
             */
            SystemInfo.prototype.hostName = "";

            /**
             * SystemInfo memory.
             * @member {lebai.system.MemoryInfo.$Properties|null|undefined} memory
             * @memberof lebai.system.SystemInfo
             * @instance
             */
            SystemInfo.prototype.memory = null;

            /**
             * SystemInfo disks.
             * @member {Array.<lebai.system.DiskInfo.$Properties>} disks
             * @memberof lebai.system.SystemInfo
             * @instance
             */
            SystemInfo.prototype.disks = $util.emptyArray;

            /**
             * SystemInfo networks.
             * @member {Array.<lebai.system.NetworkInfo.$Properties>} networks
             * @memberof lebai.system.SystemInfo
             * @instance
             */
            SystemInfo.prototype.networks = $util.emptyArray;

            /**
             * SystemInfo cpu.
             * @member {lebai.system.CpuInfo.$Properties|null|undefined} cpu
             * @memberof lebai.system.SystemInfo
             * @instance
             */
            SystemInfo.prototype.cpu = null;

            /**
             * Creates a new SystemInfo instance using the specified properties.
             * @function create
             * @memberof lebai.system.SystemInfo
             * @static
             * @param {lebai.system.SystemInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.system.SystemInfo} SystemInfo instance
             * @type {{
             *   (properties: lebai.system.SystemInfo.$Shape): lebai.system.SystemInfo & lebai.system.SystemInfo.$Shape;
             *   (properties?: lebai.system.SystemInfo.$Properties): lebai.system.SystemInfo;
             * }}
             */
            SystemInfo.create = function(properties) {
                return new SystemInfo(properties);
            };

            /**
             * Encodes the specified SystemInfo message. Does not implicitly {@link lebai.system.SystemInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.SystemInfo
             * @static
             * @param {lebai.system.SystemInfo.$Properties} message SystemInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SystemInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.kernelVersion != null && $Object.hasOwnProperty.call(message, "kernelVersion") && message.kernelVersion !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.kernelVersion);
                if (message.osVersion != null && $Object.hasOwnProperty.call(message, "osVersion") && message.osVersion !== "")
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.osVersion);
                if (message.hostName != null && $Object.hasOwnProperty.call(message, "hostName") && message.hostName !== "")
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.hostName);
                if (message.memory != null && $Object.hasOwnProperty.call(message, "memory"))
                    $root.lebai.system.MemoryInfo.encode(message.memory, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.disks != null && message.disks.length)
                    for (var i = 0; i < message.disks.length; ++i)
                        $root.lebai.system.DiskInfo.encode(message.disks[i], writer.uint32(/* id 21, wireType 2 =*/170).fork(), _depth + 1).ldelim();
                if (message.networks != null && message.networks.length)
                    for (var i = 0; i < message.networks.length; ++i)
                        $root.lebai.system.NetworkInfo.encode(message.networks[i], writer.uint32(/* id 31, wireType 2 =*/250).fork(), _depth + 1).ldelim();
                if (message.cpu != null && $Object.hasOwnProperty.call(message, "cpu"))
                    $root.lebai.system.CpuInfo.encode(message.cpu, writer.uint32(/* id 41, wireType 2 =*/330).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SystemInfo message, length delimited. Does not implicitly {@link lebai.system.SystemInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.SystemInfo
             * @static
             * @param {lebai.system.SystemInfo.$Properties} message SystemInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SystemInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SystemInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.SystemInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.SystemInfo & lebai.system.SystemInfo.$Shape} SystemInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SystemInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.SystemInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.kernelVersion = value;
                            else
                                delete message.kernelVersion;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.osVersion = value;
                            else
                                delete message.osVersion;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.hostName = value;
                            else
                                delete message.hostName;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.memory = $root.lebai.system.MemoryInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.memory);
                            continue;
                        }
                    case 21: {
                            if (wireType !== 2)
                                break;
                            if (!(message.disks && message.disks.length))
                                message.disks = [];
                            message.disks.push($root.lebai.system.DiskInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 31: {
                            if (wireType !== 2)
                                break;
                            if (!(message.networks && message.networks.length))
                                message.networks = [];
                            message.networks.push($root.lebai.system.NetworkInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 41: {
                            if (wireType !== 2)
                                break;
                            message.cpu = $root.lebai.system.CpuInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cpu);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SystemInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.SystemInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.SystemInfo & lebai.system.SystemInfo.$Shape} SystemInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SystemInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SystemInfo message.
             * @function verify
             * @memberof lebai.system.SystemInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SystemInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.kernelVersion != null && $Object.hasOwnProperty.call(message, "kernelVersion"))
                    if (!$util.isString(message.kernelVersion))
                        return "kernelVersion: string expected";
                if (message.osVersion != null && $Object.hasOwnProperty.call(message, "osVersion"))
                    if (!$util.isString(message.osVersion))
                        return "osVersion: string expected";
                if (message.hostName != null && $Object.hasOwnProperty.call(message, "hostName"))
                    if (!$util.isString(message.hostName))
                        return "hostName: string expected";
                if (message.memory != null && $Object.hasOwnProperty.call(message, "memory")) {
                    var error = $root.lebai.system.MemoryInfo.verify(message.memory, _depth + 1);
                    if (error)
                        return "memory." + error;
                }
                if (message.disks != null && $Object.hasOwnProperty.call(message, "disks")) {
                    if (!$Array.isArray(message.disks))
                        return "disks: array expected";
                    for (var i = 0; i < message.disks.length; ++i) {
                        var error = $root.lebai.system.DiskInfo.verify(message.disks[i], _depth + 1);
                        if (error)
                            return "disks." + error;
                    }
                }
                if (message.networks != null && $Object.hasOwnProperty.call(message, "networks")) {
                    if (!$Array.isArray(message.networks))
                        return "networks: array expected";
                    for (var i = 0; i < message.networks.length; ++i) {
                        var error = $root.lebai.system.NetworkInfo.verify(message.networks[i], _depth + 1);
                        if (error)
                            return "networks." + error;
                    }
                }
                if (message.cpu != null && $Object.hasOwnProperty.call(message, "cpu")) {
                    var error = $root.lebai.system.CpuInfo.verify(message.cpu, _depth + 1);
                    if (error)
                        return "cpu." + error;
                }
                return null;
            };

            /**
             * Creates a SystemInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.SystemInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.SystemInfo} SystemInfo
             */
            SystemInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.SystemInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.SystemInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.SystemInfo();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.kernelVersion != null)
                    if (typeof object.kernelVersion !== "string" || object.kernelVersion.length)
                        message.kernelVersion = $String(object.kernelVersion);
                if (object.osVersion != null)
                    if (typeof object.osVersion !== "string" || object.osVersion.length)
                        message.osVersion = $String(object.osVersion);
                if (object.hostName != null)
                    if (typeof object.hostName !== "string" || object.hostName.length)
                        message.hostName = $String(object.hostName);
                if (object.memory != null) {
                    if (!$util.isObject(object.memory))
                        throw $TypeError(".lebai.system.SystemInfo.memory: object expected");
                    message.memory = $root.lebai.system.MemoryInfo.fromObject(object.memory, _depth + 1);
                }
                if (object.disks) {
                    if (!$Array.isArray(object.disks))
                        throw $TypeError(".lebai.system.SystemInfo.disks: array expected");
                    message.disks = $Array(object.disks.length);
                    for (var i = 0; i < object.disks.length; ++i) {
                        if (!$util.isObject(object.disks[i]))
                            throw $TypeError(".lebai.system.SystemInfo.disks: object expected");
                        message.disks[i] = $root.lebai.system.DiskInfo.fromObject(object.disks[i], _depth + 1);
                    }
                }
                if (object.networks) {
                    if (!$Array.isArray(object.networks))
                        throw $TypeError(".lebai.system.SystemInfo.networks: array expected");
                    message.networks = $Array(object.networks.length);
                    for (var i = 0; i < object.networks.length; ++i) {
                        if (!$util.isObject(object.networks[i]))
                            throw $TypeError(".lebai.system.SystemInfo.networks: object expected");
                        message.networks[i] = $root.lebai.system.NetworkInfo.fromObject(object.networks[i], _depth + 1);
                    }
                }
                if (object.cpu != null) {
                    if (!$util.isObject(object.cpu))
                        throw $TypeError(".lebai.system.SystemInfo.cpu: object expected");
                    message.cpu = $root.lebai.system.CpuInfo.fromObject(object.cpu, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a SystemInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.SystemInfo
             * @static
             * @param {lebai.system.SystemInfo} message SystemInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SystemInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.disks = [];
                    object.networks = [];
                }
                if (options.defaults) {
                    object.name = "";
                    object.kernelVersion = "";
                    object.osVersion = "";
                    object.hostName = "";
                    object.memory = null;
                    object.cpu = null;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.kernelVersion != null && $Object.hasOwnProperty.call(message, "kernelVersion"))
                    object.kernelVersion = message.kernelVersion;
                if (message.osVersion != null && $Object.hasOwnProperty.call(message, "osVersion"))
                    object.osVersion = message.osVersion;
                if (message.hostName != null && $Object.hasOwnProperty.call(message, "hostName"))
                    object.hostName = message.hostName;
                if (message.memory != null && $Object.hasOwnProperty.call(message, "memory"))
                    object.memory = $root.lebai.system.MemoryInfo.toObject(message.memory, options, _depth + 1);
                if (message.disks && message.disks.length) {
                    object.disks = $Array(message.disks.length);
                    for (var j = 0; j < message.disks.length; ++j)
                        object.disks[j] = $root.lebai.system.DiskInfo.toObject(message.disks[j], options, _depth + 1);
                }
                if (message.networks && message.networks.length) {
                    object.networks = $Array(message.networks.length);
                    for (var j = 0; j < message.networks.length; ++j)
                        object.networks[j] = $root.lebai.system.NetworkInfo.toObject(message.networks[j], options, _depth + 1);
                }
                if (message.cpu != null && $Object.hasOwnProperty.call(message, "cpu"))
                    object.cpu = $root.lebai.system.CpuInfo.toObject(message.cpu, options, _depth + 1);
                return object;
            };

            /**
             * Converts this SystemInfo to JSON.
             * @function toJSON
             * @memberof lebai.system.SystemInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SystemInfo.prototype.toJSON = function() {
                return SystemInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SystemInfo
             * @function getTypeUrl
             * @memberof lebai.system.SystemInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SystemInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.SystemInfo";
            };

            return SystemInfo;
        })();

        system.GetBoxDevicesRequest = (function() {

            /**
             * Properties of a GetBoxDevicesRequest.
             * @typedef {Object} lebai.system.GetBoxDevicesRequest.$Properties
             * @property {string|null} [prefix] GetBoxDevicesRequest prefix
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetBoxDevicesRequest.
             * @memberof lebai.system
             * @interface IGetBoxDevicesRequest
             * @augments lebai.system.GetBoxDevicesRequest.$Properties
             * @deprecated Use lebai.system.GetBoxDevicesRequest.$Properties instead.
             */

            /**
             * Shape of a GetBoxDevicesRequest.
             * @typedef {lebai.system.GetBoxDevicesRequest.$Properties} lebai.system.GetBoxDevicesRequest.$Shape
             */

            /**
             * Constructs a new GetBoxDevicesRequest.
             * @memberof lebai.system
             * @classdesc Represents a GetBoxDevicesRequest.
             * @constructor
             * @param {lebai.system.GetBoxDevicesRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetBoxDevicesRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetBoxDevicesRequest prefix.
             * @member {string} prefix
             * @memberof lebai.system.GetBoxDevicesRequest
             * @instance
             */
            GetBoxDevicesRequest.prototype.prefix = "";

            /**
             * Creates a new GetBoxDevicesRequest instance using the specified properties.
             * @function create
             * @memberof lebai.system.GetBoxDevicesRequest
             * @static
             * @param {lebai.system.GetBoxDevicesRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.system.GetBoxDevicesRequest} GetBoxDevicesRequest instance
             * @type {{
             *   (properties: lebai.system.GetBoxDevicesRequest.$Shape): lebai.system.GetBoxDevicesRequest & lebai.system.GetBoxDevicesRequest.$Shape;
             *   (properties?: lebai.system.GetBoxDevicesRequest.$Properties): lebai.system.GetBoxDevicesRequest;
             * }}
             */
            GetBoxDevicesRequest.create = function(properties) {
                return new GetBoxDevicesRequest(properties);
            };

            /**
             * Encodes the specified GetBoxDevicesRequest message. Does not implicitly {@link lebai.system.GetBoxDevicesRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.GetBoxDevicesRequest
             * @static
             * @param {lebai.system.GetBoxDevicesRequest.$Properties} message GetBoxDevicesRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetBoxDevicesRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.prefix != null && $Object.hasOwnProperty.call(message, "prefix") && message.prefix !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.prefix);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetBoxDevicesRequest message, length delimited. Does not implicitly {@link lebai.system.GetBoxDevicesRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.GetBoxDevicesRequest
             * @static
             * @param {lebai.system.GetBoxDevicesRequest.$Properties} message GetBoxDevicesRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetBoxDevicesRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetBoxDevicesRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.GetBoxDevicesRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.GetBoxDevicesRequest & lebai.system.GetBoxDevicesRequest.$Shape} GetBoxDevicesRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetBoxDevicesRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.GetBoxDevicesRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.prefix = value;
                            else
                                delete message.prefix;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetBoxDevicesRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.GetBoxDevicesRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.GetBoxDevicesRequest & lebai.system.GetBoxDevicesRequest.$Shape} GetBoxDevicesRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetBoxDevicesRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetBoxDevicesRequest message.
             * @function verify
             * @memberof lebai.system.GetBoxDevicesRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetBoxDevicesRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.prefix != null && $Object.hasOwnProperty.call(message, "prefix"))
                    if (!$util.isString(message.prefix))
                        return "prefix: string expected";
                return null;
            };

            /**
             * Creates a GetBoxDevicesRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.GetBoxDevicesRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.GetBoxDevicesRequest} GetBoxDevicesRequest
             */
            GetBoxDevicesRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.GetBoxDevicesRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.GetBoxDevicesRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.GetBoxDevicesRequest();
                if (object.prefix != null)
                    if (typeof object.prefix !== "string" || object.prefix.length)
                        message.prefix = $String(object.prefix);
                return message;
            };

            /**
             * Creates a plain object from a GetBoxDevicesRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.GetBoxDevicesRequest
             * @static
             * @param {lebai.system.GetBoxDevicesRequest} message GetBoxDevicesRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetBoxDevicesRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.prefix = "";
                if (message.prefix != null && $Object.hasOwnProperty.call(message, "prefix"))
                    object.prefix = message.prefix;
                return object;
            };

            /**
             * Converts this GetBoxDevicesRequest to JSON.
             * @function toJSON
             * @memberof lebai.system.GetBoxDevicesRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetBoxDevicesRequest.prototype.toJSON = function() {
                return GetBoxDevicesRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetBoxDevicesRequest
             * @function getTypeUrl
             * @memberof lebai.system.GetBoxDevicesRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetBoxDevicesRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.GetBoxDevicesRequest";
            };

            return GetBoxDevicesRequest;
        })();

        system.GetBoxDevicesResponse = (function() {

            /**
             * Properties of a GetBoxDevicesResponse.
             * @typedef {Object} lebai.system.GetBoxDevicesResponse.$Properties
             * @property {Array.<string>|null} [devices] GetBoxDevicesResponse devices
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetBoxDevicesResponse.
             * @memberof lebai.system
             * @interface IGetBoxDevicesResponse
             * @augments lebai.system.GetBoxDevicesResponse.$Properties
             * @deprecated Use lebai.system.GetBoxDevicesResponse.$Properties instead.
             */

            /**
             * Shape of a GetBoxDevicesResponse.
             * @typedef {lebai.system.GetBoxDevicesResponse.$Properties} lebai.system.GetBoxDevicesResponse.$Shape
             */

            /**
             * Constructs a new GetBoxDevicesResponse.
             * @memberof lebai.system
             * @classdesc Represents a GetBoxDevicesResponse.
             * @constructor
             * @param {lebai.system.GetBoxDevicesResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetBoxDevicesResponse = function (properties) {
                this.devices = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetBoxDevicesResponse devices.
             * @member {Array.<string>} devices
             * @memberof lebai.system.GetBoxDevicesResponse
             * @instance
             */
            GetBoxDevicesResponse.prototype.devices = $util.emptyArray;

            /**
             * Creates a new GetBoxDevicesResponse instance using the specified properties.
             * @function create
             * @memberof lebai.system.GetBoxDevicesResponse
             * @static
             * @param {lebai.system.GetBoxDevicesResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.system.GetBoxDevicesResponse} GetBoxDevicesResponse instance
             * @type {{
             *   (properties: lebai.system.GetBoxDevicesResponse.$Shape): lebai.system.GetBoxDevicesResponse & lebai.system.GetBoxDevicesResponse.$Shape;
             *   (properties?: lebai.system.GetBoxDevicesResponse.$Properties): lebai.system.GetBoxDevicesResponse;
             * }}
             */
            GetBoxDevicesResponse.create = function(properties) {
                return new GetBoxDevicesResponse(properties);
            };

            /**
             * Encodes the specified GetBoxDevicesResponse message. Does not implicitly {@link lebai.system.GetBoxDevicesResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.GetBoxDevicesResponse
             * @static
             * @param {lebai.system.GetBoxDevicesResponse.$Properties} message GetBoxDevicesResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetBoxDevicesResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.devices != null && message.devices.length)
                    for (var i = 0; i < message.devices.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.devices[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetBoxDevicesResponse message, length delimited. Does not implicitly {@link lebai.system.GetBoxDevicesResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.GetBoxDevicesResponse
             * @static
             * @param {lebai.system.GetBoxDevicesResponse.$Properties} message GetBoxDevicesResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetBoxDevicesResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetBoxDevicesResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.GetBoxDevicesResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.GetBoxDevicesResponse & lebai.system.GetBoxDevicesResponse.$Shape} GetBoxDevicesResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetBoxDevicesResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.GetBoxDevicesResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.devices && message.devices.length))
                                message.devices = [];
                            message.devices.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetBoxDevicesResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.GetBoxDevicesResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.GetBoxDevicesResponse & lebai.system.GetBoxDevicesResponse.$Shape} GetBoxDevicesResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetBoxDevicesResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetBoxDevicesResponse message.
             * @function verify
             * @memberof lebai.system.GetBoxDevicesResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetBoxDevicesResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.devices != null && $Object.hasOwnProperty.call(message, "devices")) {
                    if (!$Array.isArray(message.devices))
                        return "devices: array expected";
                    for (var i = 0; i < message.devices.length; ++i)
                        if (!$util.isString(message.devices[i]))
                            return "devices: string[] expected";
                }
                return null;
            };

            /**
             * Creates a GetBoxDevicesResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.GetBoxDevicesResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.GetBoxDevicesResponse} GetBoxDevicesResponse
             */
            GetBoxDevicesResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.GetBoxDevicesResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.GetBoxDevicesResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.GetBoxDevicesResponse();
                if (object.devices) {
                    if (!$Array.isArray(object.devices))
                        throw $TypeError(".lebai.system.GetBoxDevicesResponse.devices: array expected");
                    message.devices = $Array(object.devices.length);
                    for (var i = 0; i < object.devices.length; ++i)
                        message.devices[i] = $String(object.devices[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a GetBoxDevicesResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.GetBoxDevicesResponse
             * @static
             * @param {lebai.system.GetBoxDevicesResponse} message GetBoxDevicesResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetBoxDevicesResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.devices = [];
                if (message.devices && message.devices.length) {
                    object.devices = $Array(message.devices.length);
                    for (var j = 0; j < message.devices.length; ++j)
                        object.devices[j] = message.devices[j];
                }
                return object;
            };

            /**
             * Converts this GetBoxDevicesResponse to JSON.
             * @function toJSON
             * @memberof lebai.system.GetBoxDevicesResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetBoxDevicesResponse.prototype.toJSON = function() {
                return GetBoxDevicesResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetBoxDevicesResponse
             * @function getTypeUrl
             * @memberof lebai.system.GetBoxDevicesResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetBoxDevicesResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.GetBoxDevicesResponse";
            };

            return GetBoxDevicesResponse;
        })();

        system.SetVirtualIpRequest = (function() {

            /**
             * Properties of a SetVirtualIpRequest.
             * @typedef {Object} lebai.system.SetVirtualIpRequest.$Properties
             * @property {string|null} [ifname] SetVirtualIpRequest ifname
             * @property {string|null} [ip] SetVirtualIpRequest ip
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetVirtualIpRequest.
             * @memberof lebai.system
             * @interface ISetVirtualIpRequest
             * @augments lebai.system.SetVirtualIpRequest.$Properties
             * @deprecated Use lebai.system.SetVirtualIpRequest.$Properties instead.
             */

            /**
             * Shape of a SetVirtualIpRequest.
             * @typedef {lebai.system.SetVirtualIpRequest.$Properties} lebai.system.SetVirtualIpRequest.$Shape
             */

            /**
             * Constructs a new SetVirtualIpRequest.
             * @memberof lebai.system
             * @classdesc Represents a SetVirtualIpRequest.
             * @constructor
             * @param {lebai.system.SetVirtualIpRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetVirtualIpRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetVirtualIpRequest ifname.
             * @member {string} ifname
             * @memberof lebai.system.SetVirtualIpRequest
             * @instance
             */
            SetVirtualIpRequest.prototype.ifname = "";

            /**
             * SetVirtualIpRequest ip.
             * @member {string} ip
             * @memberof lebai.system.SetVirtualIpRequest
             * @instance
             */
            SetVirtualIpRequest.prototype.ip = "";

            /**
             * Creates a new SetVirtualIpRequest instance using the specified properties.
             * @function create
             * @memberof lebai.system.SetVirtualIpRequest
             * @static
             * @param {lebai.system.SetVirtualIpRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.system.SetVirtualIpRequest} SetVirtualIpRequest instance
             * @type {{
             *   (properties: lebai.system.SetVirtualIpRequest.$Shape): lebai.system.SetVirtualIpRequest & lebai.system.SetVirtualIpRequest.$Shape;
             *   (properties?: lebai.system.SetVirtualIpRequest.$Properties): lebai.system.SetVirtualIpRequest;
             * }}
             */
            SetVirtualIpRequest.create = function(properties) {
                return new SetVirtualIpRequest(properties);
            };

            /**
             * Encodes the specified SetVirtualIpRequest message. Does not implicitly {@link lebai.system.SetVirtualIpRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.SetVirtualIpRequest
             * @static
             * @param {lebai.system.SetVirtualIpRequest.$Properties} message SetVirtualIpRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetVirtualIpRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.ifname != null && $Object.hasOwnProperty.call(message, "ifname") && message.ifname !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.ifname);
                if (message.ip != null && $Object.hasOwnProperty.call(message, "ip") && message.ip !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.ip);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetVirtualIpRequest message, length delimited. Does not implicitly {@link lebai.system.SetVirtualIpRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.SetVirtualIpRequest
             * @static
             * @param {lebai.system.SetVirtualIpRequest.$Properties} message SetVirtualIpRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetVirtualIpRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetVirtualIpRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.SetVirtualIpRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.SetVirtualIpRequest & lebai.system.SetVirtualIpRequest.$Shape} SetVirtualIpRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetVirtualIpRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.SetVirtualIpRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.ifname = value;
                            else
                                delete message.ifname;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.ip = value;
                            else
                                delete message.ip;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetVirtualIpRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.SetVirtualIpRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.SetVirtualIpRequest & lebai.system.SetVirtualIpRequest.$Shape} SetVirtualIpRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetVirtualIpRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetVirtualIpRequest message.
             * @function verify
             * @memberof lebai.system.SetVirtualIpRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetVirtualIpRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.ifname != null && $Object.hasOwnProperty.call(message, "ifname"))
                    if (!$util.isString(message.ifname))
                        return "ifname: string expected";
                if (message.ip != null && $Object.hasOwnProperty.call(message, "ip"))
                    if (!$util.isString(message.ip))
                        return "ip: string expected";
                return null;
            };

            /**
             * Creates a SetVirtualIpRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.SetVirtualIpRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.SetVirtualIpRequest} SetVirtualIpRequest
             */
            SetVirtualIpRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.SetVirtualIpRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.SetVirtualIpRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.SetVirtualIpRequest();
                if (object.ifname != null)
                    if (typeof object.ifname !== "string" || object.ifname.length)
                        message.ifname = $String(object.ifname);
                if (object.ip != null)
                    if (typeof object.ip !== "string" || object.ip.length)
                        message.ip = $String(object.ip);
                return message;
            };

            /**
             * Creates a plain object from a SetVirtualIpRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.SetVirtualIpRequest
             * @static
             * @param {lebai.system.SetVirtualIpRequest} message SetVirtualIpRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetVirtualIpRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.ifname = "";
                    object.ip = "";
                }
                if (message.ifname != null && $Object.hasOwnProperty.call(message, "ifname"))
                    object.ifname = message.ifname;
                if (message.ip != null && $Object.hasOwnProperty.call(message, "ip"))
                    object.ip = message.ip;
                return object;
            };

            /**
             * Converts this SetVirtualIpRequest to JSON.
             * @function toJSON
             * @memberof lebai.system.SetVirtualIpRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetVirtualIpRequest.prototype.toJSON = function() {
                return SetVirtualIpRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetVirtualIpRequest
             * @function getTypeUrl
             * @memberof lebai.system.SetVirtualIpRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetVirtualIpRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.SetVirtualIpRequest";
            };

            return SetVirtualIpRequest;
        })();

        /**
         * BoxModel enum.
         * @name lebai.system.BoxModel
         * @enum {number}
         * @property {number} LM3=0 LM3 value
         * @property {number} LA3=10 LA3 value
         */
        system.BoxModel = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "LM3"] = 0;
            values[valuesById[10] = "LA3"] = 10;
            return values;
        })();

        /**
         * ArmModel enum.
         * @name lebai.system.ArmModel
         * @enum {number}
         * @property {number} J6M1=0 J6M1 value
         * @property {number} J6L1=1 J6L1 value
         * @property {number} J5L1=11 J5L1 value
         * @property {number} J5T1=51 J5T1 value
         */
        system.ArmModel = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "J6M1"] = 0;
            values[valuesById[1] = "J6L1"] = 1;
            values[valuesById[11] = "J5L1"] = 11;
            values[valuesById[51] = "J5T1"] = 51;
            return values;
        })();

        system.RobotInfo = (function() {

            /**
             * Properties of a RobotInfo.
             * @typedef {Object} lebai.system.RobotInfo.$Properties
             * @property {string|null} [name] RobotInfo name
             * @property {string|null} [mac] RobotInfo mac
             * @property {lebai.system.BoxModel|null} [boxModel] RobotInfo boxModel
             * @property {string|null} [boxSn] RobotInfo boxSn
             * @property {lebai.system.ArmModel|null} [armModel] RobotInfo armModel
             * @property {string|null} [armSn] RobotInfo armSn
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a RobotInfo.
             * @memberof lebai.system
             * @interface IRobotInfo
             * @augments lebai.system.RobotInfo.$Properties
             * @deprecated Use lebai.system.RobotInfo.$Properties instead.
             */

            /**
             * Shape of a RobotInfo.
             * @typedef {lebai.system.RobotInfo.$Properties} lebai.system.RobotInfo.$Shape
             */

            /**
             * Constructs a new RobotInfo.
             * @memberof lebai.system
             * @classdesc Represents a RobotInfo.
             * @constructor
             * @param {lebai.system.RobotInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var RobotInfo = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * RobotInfo name.
             * @member {string} name
             * @memberof lebai.system.RobotInfo
             * @instance
             */
            RobotInfo.prototype.name = "";

            /**
             * RobotInfo mac.
             * @member {string} mac
             * @memberof lebai.system.RobotInfo
             * @instance
             */
            RobotInfo.prototype.mac = "";

            /**
             * RobotInfo boxModel.
             * @member {lebai.system.BoxModel} boxModel
             * @memberof lebai.system.RobotInfo
             * @instance
             */
            RobotInfo.prototype.boxModel = 0;

            /**
             * RobotInfo boxSn.
             * @member {string} boxSn
             * @memberof lebai.system.RobotInfo
             * @instance
             */
            RobotInfo.prototype.boxSn = "";

            /**
             * RobotInfo armModel.
             * @member {lebai.system.ArmModel} armModel
             * @memberof lebai.system.RobotInfo
             * @instance
             */
            RobotInfo.prototype.armModel = 0;

            /**
             * RobotInfo armSn.
             * @member {string} armSn
             * @memberof lebai.system.RobotInfo
             * @instance
             */
            RobotInfo.prototype.armSn = "";

            /**
             * Creates a new RobotInfo instance using the specified properties.
             * @function create
             * @memberof lebai.system.RobotInfo
             * @static
             * @param {lebai.system.RobotInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.system.RobotInfo} RobotInfo instance
             * @type {{
             *   (properties: lebai.system.RobotInfo.$Shape): lebai.system.RobotInfo & lebai.system.RobotInfo.$Shape;
             *   (properties?: lebai.system.RobotInfo.$Properties): lebai.system.RobotInfo;
             * }}
             */
            RobotInfo.create = function(properties) {
                return new RobotInfo(properties);
            };

            /**
             * Encodes the specified RobotInfo message. Does not implicitly {@link lebai.system.RobotInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.RobotInfo
             * @static
             * @param {lebai.system.RobotInfo.$Properties} message RobotInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RobotInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.mac != null && $Object.hasOwnProperty.call(message, "mac") && message.mac !== "")
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.mac);
                if (message.boxModel != null && $Object.hasOwnProperty.call(message, "boxModel") && message.boxModel !== 0)
                    writer.uint32(/* id 21, wireType 0 =*/168).int32(message.boxModel);
                if (message.boxSn != null && $Object.hasOwnProperty.call(message, "boxSn") && message.boxSn !== "")
                    writer.uint32(/* id 22, wireType 2 =*/178).string(message.boxSn);
                if (message.armModel != null && $Object.hasOwnProperty.call(message, "armModel") && message.armModel !== 0)
                    writer.uint32(/* id 31, wireType 0 =*/248).int32(message.armModel);
                if (message.armSn != null && $Object.hasOwnProperty.call(message, "armSn") && message.armSn !== "")
                    writer.uint32(/* id 32, wireType 2 =*/258).string(message.armSn);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified RobotInfo message, length delimited. Does not implicitly {@link lebai.system.RobotInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.RobotInfo
             * @static
             * @param {lebai.system.RobotInfo.$Properties} message RobotInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RobotInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a RobotInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.RobotInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.RobotInfo & lebai.system.RobotInfo.$Shape} RobotInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RobotInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.RobotInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.mac = value;
                            else
                                delete message.mac;
                            continue;
                        }
                    case 21: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.boxModel = value;
                            else
                                delete message.boxModel;
                            continue;
                        }
                    case 22: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.boxSn = value;
                            else
                                delete message.boxSn;
                            continue;
                        }
                    case 31: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.armModel = value;
                            else
                                delete message.armModel;
                            continue;
                        }
                    case 32: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.armSn = value;
                            else
                                delete message.armSn;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a RobotInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.RobotInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.RobotInfo & lebai.system.RobotInfo.$Shape} RobotInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RobotInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RobotInfo message.
             * @function verify
             * @memberof lebai.system.RobotInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RobotInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.mac != null && $Object.hasOwnProperty.call(message, "mac"))
                    if (!$util.isString(message.mac))
                        return "mac: string expected";
                if (message.boxModel != null && $Object.hasOwnProperty.call(message, "boxModel"))
                    if (typeof message.boxModel !== "number" || (message.boxModel | 0) !== message.boxModel)
                        return "boxModel: enum value expected";
                if (message.boxSn != null && $Object.hasOwnProperty.call(message, "boxSn"))
                    if (!$util.isString(message.boxSn))
                        return "boxSn: string expected";
                if (message.armModel != null && $Object.hasOwnProperty.call(message, "armModel"))
                    if (typeof message.armModel !== "number" || (message.armModel | 0) !== message.armModel)
                        return "armModel: enum value expected";
                if (message.armSn != null && $Object.hasOwnProperty.call(message, "armSn"))
                    if (!$util.isString(message.armSn))
                        return "armSn: string expected";
                return null;
            };

            /**
             * Creates a RobotInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.RobotInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.RobotInfo} RobotInfo
             */
            RobotInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.RobotInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.RobotInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.RobotInfo();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.mac != null)
                    if (typeof object.mac !== "string" || object.mac.length)
                        message.mac = $String(object.mac);
                if (object.boxModel !== 0 && (typeof object.boxModel !== "string" || $root.lebai.system.BoxModel[object.boxModel] !== 0))
                    switch (object.boxModel) {
                    case "LM3":
                    case 0:
                        message.boxModel = 0;
                        break;
                    case "LA3":
                    case 10:
                        message.boxModel = 10;
                        break;
                    default:
                        if (typeof object.boxModel === "number" && (object.boxModel | 0) === object.boxModel)
                            message.boxModel = object.boxModel;
                    }
                if (object.boxSn != null)
                    if (typeof object.boxSn !== "string" || object.boxSn.length)
                        message.boxSn = $String(object.boxSn);
                if (object.armModel !== 0 && (typeof object.armModel !== "string" || $root.lebai.system.ArmModel[object.armModel] !== 0))
                    switch (object.armModel) {
                    case "J6M1":
                    case 0:
                        message.armModel = 0;
                        break;
                    case "J6L1":
                    case 1:
                        message.armModel = 1;
                        break;
                    case "J5L1":
                    case 11:
                        message.armModel = 11;
                        break;
                    case "J5T1":
                    case 51:
                        message.armModel = 51;
                        break;
                    default:
                        if (typeof object.armModel === "number" && (object.armModel | 0) === object.armModel)
                            message.armModel = object.armModel;
                    }
                if (object.armSn != null)
                    if (typeof object.armSn !== "string" || object.armSn.length)
                        message.armSn = $String(object.armSn);
                return message;
            };

            /**
             * Creates a plain object from a RobotInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.RobotInfo
             * @static
             * @param {lebai.system.RobotInfo} message RobotInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RobotInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.mac = "";
                    object.boxModel = options.enums === $String ? "LM3" : 0;
                    object.boxSn = "";
                    object.armModel = options.enums === $String ? "J6M1" : 0;
                    object.armSn = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.mac != null && $Object.hasOwnProperty.call(message, "mac"))
                    object.mac = message.mac;
                if (message.boxModel != null && $Object.hasOwnProperty.call(message, "boxModel"))
                    object.boxModel = options.enums === $String ? $root.lebai.system.BoxModel[message.boxModel] === $undefined ? message.boxModel : $root.lebai.system.BoxModel[message.boxModel] : message.boxModel;
                if (message.boxSn != null && $Object.hasOwnProperty.call(message, "boxSn"))
                    object.boxSn = message.boxSn;
                if (message.armModel != null && $Object.hasOwnProperty.call(message, "armModel"))
                    object.armModel = options.enums === $String ? $root.lebai.system.ArmModel[message.armModel] === $undefined ? message.armModel : $root.lebai.system.ArmModel[message.armModel] : message.armModel;
                if (message.armSn != null && $Object.hasOwnProperty.call(message, "armSn"))
                    object.armSn = message.armSn;
                return object;
            };

            /**
             * Converts this RobotInfo to JSON.
             * @function toJSON
             * @memberof lebai.system.RobotInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RobotInfo.prototype.toJSON = function() {
                return RobotInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for RobotInfo
             * @function getTypeUrl
             * @memberof lebai.system.RobotInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            RobotInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.RobotInfo";
            };

            return RobotInfo;
        })();

        system.DeviceInfo = (function() {

            /**
             * Properties of a DeviceInfo.
             * @typedef {Object} lebai.system.DeviceInfo.$Properties
             * @property {boolean|null} [invalid] DeviceInfo invalid
             * @property {string|null} [sn] DeviceInfo sn
             * @property {string|null} [version] DeviceInfo version
             * @property {lebai.hardware.FirmwarePartition|null} [partition] DeviceInfo partition
             * @property {number|null} [diNum] DeviceInfo diNum
             * @property {number|null} [doNum] DeviceInfo doNum
             * @property {number|null} [dioNum] DeviceInfo dioNum
             * @property {number|null} [aiNum] DeviceInfo aiNum
             * @property {number|null} [aoNum] DeviceInfo aoNum
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DeviceInfo.
             * @memberof lebai.system
             * @interface IDeviceInfo
             * @augments lebai.system.DeviceInfo.$Properties
             * @deprecated Use lebai.system.DeviceInfo.$Properties instead.
             */

            /**
             * Shape of a DeviceInfo.
             * @typedef {lebai.system.DeviceInfo.$Properties} lebai.system.DeviceInfo.$Shape
             */

            /**
             * Constructs a new DeviceInfo.
             * @memberof lebai.system
             * @classdesc Represents a DeviceInfo.
             * @constructor
             * @param {lebai.system.DeviceInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var DeviceInfo = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DeviceInfo invalid.
             * @member {boolean} invalid
             * @memberof lebai.system.DeviceInfo
             * @instance
             */
            DeviceInfo.prototype.invalid = false;

            /**
             * DeviceInfo sn.
             * @member {string} sn
             * @memberof lebai.system.DeviceInfo
             * @instance
             */
            DeviceInfo.prototype.sn = "";

            /**
             * DeviceInfo version.
             * @member {string} version
             * @memberof lebai.system.DeviceInfo
             * @instance
             */
            DeviceInfo.prototype.version = "";

            /**
             * DeviceInfo partition.
             * @member {lebai.hardware.FirmwarePartition} partition
             * @memberof lebai.system.DeviceInfo
             * @instance
             */
            DeviceInfo.prototype.partition = 0;

            /**
             * DeviceInfo diNum.
             * @member {number} diNum
             * @memberof lebai.system.DeviceInfo
             * @instance
             */
            DeviceInfo.prototype.diNum = 0;

            /**
             * DeviceInfo doNum.
             * @member {number} doNum
             * @memberof lebai.system.DeviceInfo
             * @instance
             */
            DeviceInfo.prototype.doNum = 0;

            /**
             * DeviceInfo dioNum.
             * @member {number} dioNum
             * @memberof lebai.system.DeviceInfo
             * @instance
             */
            DeviceInfo.prototype.dioNum = 0;

            /**
             * DeviceInfo aiNum.
             * @member {number} aiNum
             * @memberof lebai.system.DeviceInfo
             * @instance
             */
            DeviceInfo.prototype.aiNum = 0;

            /**
             * DeviceInfo aoNum.
             * @member {number} aoNum
             * @memberof lebai.system.DeviceInfo
             * @instance
             */
            DeviceInfo.prototype.aoNum = 0;

            /**
             * Creates a new DeviceInfo instance using the specified properties.
             * @function create
             * @memberof lebai.system.DeviceInfo
             * @static
             * @param {lebai.system.DeviceInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.system.DeviceInfo} DeviceInfo instance
             * @type {{
             *   (properties: lebai.system.DeviceInfo.$Shape): lebai.system.DeviceInfo & lebai.system.DeviceInfo.$Shape;
             *   (properties?: lebai.system.DeviceInfo.$Properties): lebai.system.DeviceInfo;
             * }}
             */
            DeviceInfo.create = function(properties) {
                return new DeviceInfo(properties);
            };

            /**
             * Encodes the specified DeviceInfo message. Does not implicitly {@link lebai.system.DeviceInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.DeviceInfo
             * @static
             * @param {lebai.system.DeviceInfo.$Properties} message DeviceInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeviceInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.invalid != null && $Object.hasOwnProperty.call(message, "invalid") && message.invalid !== false)
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.invalid);
                if (message.sn != null && $Object.hasOwnProperty.call(message, "sn") && message.sn !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.sn);
                if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== "")
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.version);
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition") && message.partition !== 0)
                    writer.uint32(/* id 13, wireType 0 =*/104).int32(message.partition);
                if (message.diNum != null && $Object.hasOwnProperty.call(message, "diNum") && message.diNum !== 0)
                    writer.uint32(/* id 21, wireType 0 =*/168).uint32(message.diNum);
                if (message.doNum != null && $Object.hasOwnProperty.call(message, "doNum") && message.doNum !== 0)
                    writer.uint32(/* id 22, wireType 0 =*/176).uint32(message.doNum);
                if (message.dioNum != null && $Object.hasOwnProperty.call(message, "dioNum") && message.dioNum !== 0)
                    writer.uint32(/* id 23, wireType 0 =*/184).uint32(message.dioNum);
                if (message.aiNum != null && $Object.hasOwnProperty.call(message, "aiNum") && message.aiNum !== 0)
                    writer.uint32(/* id 26, wireType 0 =*/208).uint32(message.aiNum);
                if (message.aoNum != null && $Object.hasOwnProperty.call(message, "aoNum") && message.aoNum !== 0)
                    writer.uint32(/* id 27, wireType 0 =*/216).uint32(message.aoNum);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DeviceInfo message, length delimited. Does not implicitly {@link lebai.system.DeviceInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.DeviceInfo
             * @static
             * @param {lebai.system.DeviceInfo.$Properties} message DeviceInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeviceInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a DeviceInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.DeviceInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.DeviceInfo & lebai.system.DeviceInfo.$Shape} DeviceInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeviceInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.DeviceInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.invalid = value;
                            else
                                delete message.invalid;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.sn = value;
                            else
                                delete message.sn;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.version = value;
                            else
                                delete message.version;
                            continue;
                        }
                    case 13: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.partition = value;
                            else
                                delete message.partition;
                            continue;
                        }
                    case 21: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.diNum = value;
                            else
                                delete message.diNum;
                            continue;
                        }
                    case 22: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.doNum = value;
                            else
                                delete message.doNum;
                            continue;
                        }
                    case 23: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.dioNum = value;
                            else
                                delete message.dioNum;
                            continue;
                        }
                    case 26: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.aiNum = value;
                            else
                                delete message.aiNum;
                            continue;
                        }
                    case 27: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.aoNum = value;
                            else
                                delete message.aoNum;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a DeviceInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.DeviceInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.DeviceInfo & lebai.system.DeviceInfo.$Shape} DeviceInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeviceInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DeviceInfo message.
             * @function verify
             * @memberof lebai.system.DeviceInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DeviceInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.invalid != null && $Object.hasOwnProperty.call(message, "invalid"))
                    if (typeof message.invalid !== "boolean")
                        return "invalid: boolean expected";
                if (message.sn != null && $Object.hasOwnProperty.call(message, "sn"))
                    if (!$util.isString(message.sn))
                        return "sn: string expected";
                if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isString(message.version))
                        return "version: string expected";
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition"))
                    if (typeof message.partition !== "number" || (message.partition | 0) !== message.partition)
                        return "partition: enum value expected";
                if (message.diNum != null && $Object.hasOwnProperty.call(message, "diNum"))
                    if (!$util.isInteger(message.diNum))
                        return "diNum: integer expected";
                if (message.doNum != null && $Object.hasOwnProperty.call(message, "doNum"))
                    if (!$util.isInteger(message.doNum))
                        return "doNum: integer expected";
                if (message.dioNum != null && $Object.hasOwnProperty.call(message, "dioNum"))
                    if (!$util.isInteger(message.dioNum))
                        return "dioNum: integer expected";
                if (message.aiNum != null && $Object.hasOwnProperty.call(message, "aiNum"))
                    if (!$util.isInteger(message.aiNum))
                        return "aiNum: integer expected";
                if (message.aoNum != null && $Object.hasOwnProperty.call(message, "aoNum"))
                    if (!$util.isInteger(message.aoNum))
                        return "aoNum: integer expected";
                return null;
            };

            /**
             * Creates a DeviceInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.DeviceInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.DeviceInfo} DeviceInfo
             */
            DeviceInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.DeviceInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.DeviceInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.DeviceInfo();
                if (object.invalid != null)
                    if (object.invalid)
                        message.invalid = $Boolean(object.invalid);
                if (object.sn != null)
                    if (typeof object.sn !== "string" || object.sn.length)
                        message.sn = $String(object.sn);
                if (object.version != null)
                    if (typeof object.version !== "string" || object.version.length)
                        message.version = $String(object.version);
                if (object.partition !== 0 && (typeof object.partition !== "string" || $root.lebai.hardware.FirmwarePartition[object.partition] !== 0))
                    switch (object.partition) {
                    case "A":
                    case 0:
                        message.partition = 0;
                        break;
                    case "B":
                    case 1:
                        message.partition = 1;
                        break;
                    case "C":
                    case 2:
                        message.partition = 2;
                        break;
                    case "UNKNOWN":
                    case 3:
                        message.partition = 3;
                        break;
                    default:
                        if (typeof object.partition === "number" && (object.partition | 0) === object.partition)
                            message.partition = object.partition;
                    }
                if (object.diNum != null)
                    if ($Number(object.diNum) !== 0)
                        message.diNum = object.diNum >>> 0;
                if (object.doNum != null)
                    if ($Number(object.doNum) !== 0)
                        message.doNum = object.doNum >>> 0;
                if (object.dioNum != null)
                    if ($Number(object.dioNum) !== 0)
                        message.dioNum = object.dioNum >>> 0;
                if (object.aiNum != null)
                    if ($Number(object.aiNum) !== 0)
                        message.aiNum = object.aiNum >>> 0;
                if (object.aoNum != null)
                    if ($Number(object.aoNum) !== 0)
                        message.aoNum = object.aoNum >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a DeviceInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.DeviceInfo
             * @static
             * @param {lebai.system.DeviceInfo} message DeviceInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DeviceInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.invalid = false;
                    object.sn = "";
                    object.version = "";
                    object.partition = options.enums === $String ? "A" : 0;
                    object.diNum = 0;
                    object.doNum = 0;
                    object.dioNum = 0;
                    object.aiNum = 0;
                    object.aoNum = 0;
                }
                if (message.invalid != null && $Object.hasOwnProperty.call(message, "invalid"))
                    object.invalid = message.invalid;
                if (message.sn != null && $Object.hasOwnProperty.call(message, "sn"))
                    object.sn = message.sn;
                if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition"))
                    object.partition = options.enums === $String ? $root.lebai.hardware.FirmwarePartition[message.partition] === $undefined ? message.partition : $root.lebai.hardware.FirmwarePartition[message.partition] : message.partition;
                if (message.diNum != null && $Object.hasOwnProperty.call(message, "diNum"))
                    object.diNum = message.diNum;
                if (message.doNum != null && $Object.hasOwnProperty.call(message, "doNum"))
                    object.doNum = message.doNum;
                if (message.dioNum != null && $Object.hasOwnProperty.call(message, "dioNum"))
                    object.dioNum = message.dioNum;
                if (message.aiNum != null && $Object.hasOwnProperty.call(message, "aiNum"))
                    object.aiNum = message.aiNum;
                if (message.aoNum != null && $Object.hasOwnProperty.call(message, "aoNum"))
                    object.aoNum = message.aoNum;
                return object;
            };

            /**
             * Converts this DeviceInfo to JSON.
             * @function toJSON
             * @memberof lebai.system.DeviceInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DeviceInfo.prototype.toJSON = function() {
                return DeviceInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DeviceInfo
             * @function getTypeUrl
             * @memberof lebai.system.DeviceInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DeviceInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.DeviceInfo";
            };

            return DeviceInfo;
        })();

        system.HardwareInfo = (function() {

            /**
             * Properties of a HardwareInfo.
             * @typedef {Object} lebai.system.HardwareInfo.$Properties
             * @property {lebai.system.DeviceInfo.$Properties|null} [comboard] HardwareInfo comboard
             * @property {Array.<lebai.system.DeviceInfo.$Properties>|null} [joints] HardwareInfo joints
             * @property {lebai.system.DeviceInfo.$Properties|null} [flange] HardwareInfo flange
             * @property {lebai.system.DeviceInfo.$Properties|null} [led] HardwareInfo led
             * @property {lebai.system.DeviceInfo.$Properties|null} [extraIo] HardwareInfo extraIo
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a HardwareInfo.
             * @memberof lebai.system
             * @interface IHardwareInfo
             * @augments lebai.system.HardwareInfo.$Properties
             * @deprecated Use lebai.system.HardwareInfo.$Properties instead.
             */

            /**
             * Shape of a HardwareInfo.
             * @typedef {lebai.system.HardwareInfo.$Properties} lebai.system.HardwareInfo.$Shape
             */

            /**
             * Constructs a new HardwareInfo.
             * @memberof lebai.system
             * @classdesc Represents a HardwareInfo.
             * @constructor
             * @param {lebai.system.HardwareInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var HardwareInfo = function (properties) {
                this.joints = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * HardwareInfo comboard.
             * @member {lebai.system.DeviceInfo.$Properties|null|undefined} comboard
             * @memberof lebai.system.HardwareInfo
             * @instance
             */
            HardwareInfo.prototype.comboard = null;

            /**
             * HardwareInfo joints.
             * @member {Array.<lebai.system.DeviceInfo.$Properties>} joints
             * @memberof lebai.system.HardwareInfo
             * @instance
             */
            HardwareInfo.prototype.joints = $util.emptyArray;

            /**
             * HardwareInfo flange.
             * @member {lebai.system.DeviceInfo.$Properties|null|undefined} flange
             * @memberof lebai.system.HardwareInfo
             * @instance
             */
            HardwareInfo.prototype.flange = null;

            /**
             * HardwareInfo led.
             * @member {lebai.system.DeviceInfo.$Properties|null|undefined} led
             * @memberof lebai.system.HardwareInfo
             * @instance
             */
            HardwareInfo.prototype.led = null;

            /**
             * HardwareInfo extraIo.
             * @member {lebai.system.DeviceInfo.$Properties|null|undefined} extraIo
             * @memberof lebai.system.HardwareInfo
             * @instance
             */
            HardwareInfo.prototype.extraIo = null;

            /**
             * Creates a new HardwareInfo instance using the specified properties.
             * @function create
             * @memberof lebai.system.HardwareInfo
             * @static
             * @param {lebai.system.HardwareInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.system.HardwareInfo} HardwareInfo instance
             * @type {{
             *   (properties: lebai.system.HardwareInfo.$Shape): lebai.system.HardwareInfo & lebai.system.HardwareInfo.$Shape;
             *   (properties?: lebai.system.HardwareInfo.$Properties): lebai.system.HardwareInfo;
             * }}
             */
            HardwareInfo.create = function(properties) {
                return new HardwareInfo(properties);
            };

            /**
             * Encodes the specified HardwareInfo message. Does not implicitly {@link lebai.system.HardwareInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.HardwareInfo
             * @static
             * @param {lebai.system.HardwareInfo.$Properties} message HardwareInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HardwareInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.comboard != null && $Object.hasOwnProperty.call(message, "comboard"))
                    $root.lebai.system.DeviceInfo.encode(message.comboard, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.joints != null && message.joints.length)
                    for (var i = 0; i < message.joints.length; ++i)
                        $root.lebai.system.DeviceInfo.encode(message.joints[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.flange != null && $Object.hasOwnProperty.call(message, "flange"))
                    $root.lebai.system.DeviceInfo.encode(message.flange, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.led != null && $Object.hasOwnProperty.call(message, "led"))
                    $root.lebai.system.DeviceInfo.encode(message.led, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.extraIo != null && $Object.hasOwnProperty.call(message, "extraIo"))
                    $root.lebai.system.DeviceInfo.encode(message.extraIo, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified HardwareInfo message, length delimited. Does not implicitly {@link lebai.system.HardwareInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.HardwareInfo
             * @static
             * @param {lebai.system.HardwareInfo.$Properties} message HardwareInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HardwareInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a HardwareInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.HardwareInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.HardwareInfo & lebai.system.HardwareInfo.$Shape} HardwareInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HardwareInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.HardwareInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.comboard = $root.lebai.system.DeviceInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.comboard);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.joints && message.joints.length))
                                message.joints = [];
                            message.joints.push($root.lebai.system.DeviceInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.flange = $root.lebai.system.DeviceInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.flange);
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.led = $root.lebai.system.DeviceInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.led);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.extraIo = $root.lebai.system.DeviceInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.extraIo);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a HardwareInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.HardwareInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.HardwareInfo & lebai.system.HardwareInfo.$Shape} HardwareInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HardwareInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a HardwareInfo message.
             * @function verify
             * @memberof lebai.system.HardwareInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HardwareInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.comboard != null && $Object.hasOwnProperty.call(message, "comboard")) {
                    var error = $root.lebai.system.DeviceInfo.verify(message.comboard, _depth + 1);
                    if (error)
                        return "comboard." + error;
                }
                if (message.joints != null && $Object.hasOwnProperty.call(message, "joints")) {
                    if (!$Array.isArray(message.joints))
                        return "joints: array expected";
                    for (var i = 0; i < message.joints.length; ++i) {
                        var error = $root.lebai.system.DeviceInfo.verify(message.joints[i], _depth + 1);
                        if (error)
                            return "joints." + error;
                    }
                }
                if (message.flange != null && $Object.hasOwnProperty.call(message, "flange")) {
                    var error = $root.lebai.system.DeviceInfo.verify(message.flange, _depth + 1);
                    if (error)
                        return "flange." + error;
                }
                if (message.led != null && $Object.hasOwnProperty.call(message, "led")) {
                    var error = $root.lebai.system.DeviceInfo.verify(message.led, _depth + 1);
                    if (error)
                        return "led." + error;
                }
                if (message.extraIo != null && $Object.hasOwnProperty.call(message, "extraIo")) {
                    var error = $root.lebai.system.DeviceInfo.verify(message.extraIo, _depth + 1);
                    if (error)
                        return "extraIo." + error;
                }
                return null;
            };

            /**
             * Creates a HardwareInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.HardwareInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.HardwareInfo} HardwareInfo
             */
            HardwareInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.HardwareInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.HardwareInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.HardwareInfo();
                if (object.comboard != null) {
                    if (!$util.isObject(object.comboard))
                        throw $TypeError(".lebai.system.HardwareInfo.comboard: object expected");
                    message.comboard = $root.lebai.system.DeviceInfo.fromObject(object.comboard, _depth + 1);
                }
                if (object.joints) {
                    if (!$Array.isArray(object.joints))
                        throw $TypeError(".lebai.system.HardwareInfo.joints: array expected");
                    message.joints = $Array(object.joints.length);
                    for (var i = 0; i < object.joints.length; ++i) {
                        if (!$util.isObject(object.joints[i]))
                            throw $TypeError(".lebai.system.HardwareInfo.joints: object expected");
                        message.joints[i] = $root.lebai.system.DeviceInfo.fromObject(object.joints[i], _depth + 1);
                    }
                }
                if (object.flange != null) {
                    if (!$util.isObject(object.flange))
                        throw $TypeError(".lebai.system.HardwareInfo.flange: object expected");
                    message.flange = $root.lebai.system.DeviceInfo.fromObject(object.flange, _depth + 1);
                }
                if (object.led != null) {
                    if (!$util.isObject(object.led))
                        throw $TypeError(".lebai.system.HardwareInfo.led: object expected");
                    message.led = $root.lebai.system.DeviceInfo.fromObject(object.led, _depth + 1);
                }
                if (object.extraIo != null) {
                    if (!$util.isObject(object.extraIo))
                        throw $TypeError(".lebai.system.HardwareInfo.extraIo: object expected");
                    message.extraIo = $root.lebai.system.DeviceInfo.fromObject(object.extraIo, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a HardwareInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.HardwareInfo
             * @static
             * @param {lebai.system.HardwareInfo} message HardwareInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HardwareInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.joints = [];
                if (options.defaults) {
                    object.comboard = null;
                    object.flange = null;
                    object.led = null;
                    object.extraIo = null;
                }
                if (message.comboard != null && $Object.hasOwnProperty.call(message, "comboard"))
                    object.comboard = $root.lebai.system.DeviceInfo.toObject(message.comboard, options, _depth + 1);
                if (message.joints && message.joints.length) {
                    object.joints = $Array(message.joints.length);
                    for (var j = 0; j < message.joints.length; ++j)
                        object.joints[j] = $root.lebai.system.DeviceInfo.toObject(message.joints[j], options, _depth + 1);
                }
                if (message.flange != null && $Object.hasOwnProperty.call(message, "flange"))
                    object.flange = $root.lebai.system.DeviceInfo.toObject(message.flange, options, _depth + 1);
                if (message.led != null && $Object.hasOwnProperty.call(message, "led"))
                    object.led = $root.lebai.system.DeviceInfo.toObject(message.led, options, _depth + 1);
                if (message.extraIo != null && $Object.hasOwnProperty.call(message, "extraIo"))
                    object.extraIo = $root.lebai.system.DeviceInfo.toObject(message.extraIo, options, _depth + 1);
                return object;
            };

            /**
             * Converts this HardwareInfo to JSON.
             * @function toJSON
             * @memberof lebai.system.HardwareInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HardwareInfo.prototype.toJSON = function() {
                return HardwareInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for HardwareInfo
             * @function getTypeUrl
             * @memberof lebai.system.HardwareInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            HardwareInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.HardwareInfo";
            };

            return HardwareInfo;
        })();

        system.SoftwareItemInfo = (function() {

            /**
             * Properties of a SoftwareItemInfo.
             * @typedef {Object} lebai.system.SoftwareItemInfo.$Properties
             * @property {string|null} [version] SoftwareItemInfo version
             * @property {string|null} [branch] SoftwareItemInfo branch
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SoftwareItemInfo.
             * @memberof lebai.system
             * @interface ISoftwareItemInfo
             * @augments lebai.system.SoftwareItemInfo.$Properties
             * @deprecated Use lebai.system.SoftwareItemInfo.$Properties instead.
             */

            /**
             * Shape of a SoftwareItemInfo.
             * @typedef {lebai.system.SoftwareItemInfo.$Properties} lebai.system.SoftwareItemInfo.$Shape
             */

            /**
             * Constructs a new SoftwareItemInfo.
             * @memberof lebai.system
             * @classdesc Represents a SoftwareItemInfo.
             * @constructor
             * @param {lebai.system.SoftwareItemInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SoftwareItemInfo = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SoftwareItemInfo version.
             * @member {string} version
             * @memberof lebai.system.SoftwareItemInfo
             * @instance
             */
            SoftwareItemInfo.prototype.version = "";

            /**
             * SoftwareItemInfo branch.
             * @member {string} branch
             * @memberof lebai.system.SoftwareItemInfo
             * @instance
             */
            SoftwareItemInfo.prototype.branch = "";

            /**
             * Creates a new SoftwareItemInfo instance using the specified properties.
             * @function create
             * @memberof lebai.system.SoftwareItemInfo
             * @static
             * @param {lebai.system.SoftwareItemInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.system.SoftwareItemInfo} SoftwareItemInfo instance
             * @type {{
             *   (properties: lebai.system.SoftwareItemInfo.$Shape): lebai.system.SoftwareItemInfo & lebai.system.SoftwareItemInfo.$Shape;
             *   (properties?: lebai.system.SoftwareItemInfo.$Properties): lebai.system.SoftwareItemInfo;
             * }}
             */
            SoftwareItemInfo.create = function(properties) {
                return new SoftwareItemInfo(properties);
            };

            /**
             * Encodes the specified SoftwareItemInfo message. Does not implicitly {@link lebai.system.SoftwareItemInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.SoftwareItemInfo
             * @static
             * @param {lebai.system.SoftwareItemInfo.$Properties} message SoftwareItemInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SoftwareItemInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.version);
                if (message.branch != null && $Object.hasOwnProperty.call(message, "branch") && message.branch !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.branch);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SoftwareItemInfo message, length delimited. Does not implicitly {@link lebai.system.SoftwareItemInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.SoftwareItemInfo
             * @static
             * @param {lebai.system.SoftwareItemInfo.$Properties} message SoftwareItemInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SoftwareItemInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SoftwareItemInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.SoftwareItemInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.SoftwareItemInfo & lebai.system.SoftwareItemInfo.$Shape} SoftwareItemInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SoftwareItemInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.SoftwareItemInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.version = value;
                            else
                                delete message.version;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.branch = value;
                            else
                                delete message.branch;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SoftwareItemInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.SoftwareItemInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.SoftwareItemInfo & lebai.system.SoftwareItemInfo.$Shape} SoftwareItemInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SoftwareItemInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SoftwareItemInfo message.
             * @function verify
             * @memberof lebai.system.SoftwareItemInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SoftwareItemInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isString(message.version))
                        return "version: string expected";
                if (message.branch != null && $Object.hasOwnProperty.call(message, "branch"))
                    if (!$util.isString(message.branch))
                        return "branch: string expected";
                return null;
            };

            /**
             * Creates a SoftwareItemInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.SoftwareItemInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.SoftwareItemInfo} SoftwareItemInfo
             */
            SoftwareItemInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.SoftwareItemInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.SoftwareItemInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.SoftwareItemInfo();
                if (object.version != null)
                    if (typeof object.version !== "string" || object.version.length)
                        message.version = $String(object.version);
                if (object.branch != null)
                    if (typeof object.branch !== "string" || object.branch.length)
                        message.branch = $String(object.branch);
                return message;
            };

            /**
             * Creates a plain object from a SoftwareItemInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.SoftwareItemInfo
             * @static
             * @param {lebai.system.SoftwareItemInfo} message SoftwareItemInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SoftwareItemInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.version = "";
                    object.branch = "";
                }
                if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.branch != null && $Object.hasOwnProperty.call(message, "branch"))
                    object.branch = message.branch;
                return object;
            };

            /**
             * Converts this SoftwareItemInfo to JSON.
             * @function toJSON
             * @memberof lebai.system.SoftwareItemInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SoftwareItemInfo.prototype.toJSON = function() {
                return SoftwareItemInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SoftwareItemInfo
             * @function getTypeUrl
             * @memberof lebai.system.SoftwareItemInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SoftwareItemInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.SoftwareItemInfo";
            };

            return SoftwareItemInfo;
        })();

        system.SoftwareInfo = (function() {

            /**
             * Properties of a SoftwareInfo.
             * @typedef {Object} lebai.system.SoftwareInfo.$Properties
             * @property {Object.<string,lebai.system.SoftwareItemInfo.$Properties>|null} [software] SoftwareInfo software
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SoftwareInfo.
             * @memberof lebai.system
             * @interface ISoftwareInfo
             * @augments lebai.system.SoftwareInfo.$Properties
             * @deprecated Use lebai.system.SoftwareInfo.$Properties instead.
             */

            /**
             * Shape of a SoftwareInfo.
             * @typedef {lebai.system.SoftwareInfo.$Properties} lebai.system.SoftwareInfo.$Shape
             */

            /**
             * Constructs a new SoftwareInfo.
             * @memberof lebai.system
             * @classdesc Represents a SoftwareInfo.
             * @constructor
             * @param {lebai.system.SoftwareInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SoftwareInfo = function (properties) {
                this.software = {};
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SoftwareInfo software.
             * @member {Object.<string,lebai.system.SoftwareItemInfo.$Properties>} software
             * @memberof lebai.system.SoftwareInfo
             * @instance
             */
            SoftwareInfo.prototype.software = $util.emptyObject;

            /**
             * Creates a new SoftwareInfo instance using the specified properties.
             * @function create
             * @memberof lebai.system.SoftwareInfo
             * @static
             * @param {lebai.system.SoftwareInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.system.SoftwareInfo} SoftwareInfo instance
             * @type {{
             *   (properties: lebai.system.SoftwareInfo.$Shape): lebai.system.SoftwareInfo & lebai.system.SoftwareInfo.$Shape;
             *   (properties?: lebai.system.SoftwareInfo.$Properties): lebai.system.SoftwareInfo;
             * }}
             */
            SoftwareInfo.create = function(properties) {
                return new SoftwareInfo(properties);
            };

            /**
             * Encodes the specified SoftwareInfo message. Does not implicitly {@link lebai.system.SoftwareInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.SoftwareInfo
             * @static
             * @param {lebai.system.SoftwareInfo.$Properties} message SoftwareInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SoftwareInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.software != null && $Object.hasOwnProperty.call(message, "software"))
                    for (var keys = $Object.keys(message.software), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.lebai.system.SoftwareItemInfo.encode(message.software[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SoftwareInfo message, length delimited. Does not implicitly {@link lebai.system.SoftwareInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.SoftwareInfo
             * @static
             * @param {lebai.system.SoftwareInfo.$Properties} message SoftwareInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SoftwareInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SoftwareInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.SoftwareInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.SoftwareInfo & lebai.system.SoftwareInfo.$Shape} SoftwareInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SoftwareInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.SoftwareInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (message.software === $util.emptyObject)
                                message.software = {};
                            var end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                var tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.lebai.system.SoftwareItemInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.software, key);
                            message.software[key] = value || new $root.lebai.system.SoftwareItemInfo();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SoftwareInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.SoftwareInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.SoftwareInfo & lebai.system.SoftwareInfo.$Shape} SoftwareInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SoftwareInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SoftwareInfo message.
             * @function verify
             * @memberof lebai.system.SoftwareInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SoftwareInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.software != null && $Object.hasOwnProperty.call(message, "software")) {
                    if (!$util.isObject(message.software))
                        return "software: object expected";
                    var key = $Object.keys(message.software);
                    for (var i = 0; i < key.length; ++i) {
                        var error = $root.lebai.system.SoftwareItemInfo.verify(message.software[key[i]], _depth + 1);
                        if (error)
                            return "software." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a SoftwareInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.SoftwareInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.SoftwareInfo} SoftwareInfo
             */
            SoftwareInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.SoftwareInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.SoftwareInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.SoftwareInfo();
                if (object.software) {
                    if (!$util.isObject(object.software))
                        throw $TypeError(".lebai.system.SoftwareInfo.software: object expected");
                    message.software = {};
                    for (var keys = $Object.keys(object.software), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.software, keys[i]);
                        if (!$util.isObject(object.software[keys[i]]))
                            throw $TypeError(".lebai.system.SoftwareInfo.software: object expected");
                        message.software[keys[i]] = $root.lebai.system.SoftwareItemInfo.fromObject(object.software[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a SoftwareInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.SoftwareInfo
             * @static
             * @param {lebai.system.SoftwareInfo} message SoftwareInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SoftwareInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.objects || options.defaults)
                    object.software = {};
                var keys2;
                if (message.software && (keys2 = $Object.keys(message.software)).length) {
                    object.software = {};
                    for (var j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.software, keys2[j]);
                        object.software[keys2[j]] = $root.lebai.system.SoftwareItemInfo.toObject(message.software[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this SoftwareInfo to JSON.
             * @function toJSON
             * @memberof lebai.system.SoftwareInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SoftwareInfo.prototype.toJSON = function() {
                return SoftwareInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SoftwareInfo
             * @function getTypeUrl
             * @memberof lebai.system.SoftwareInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SoftwareInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.SoftwareInfo";
            };

            return SoftwareInfo;
        })();

        /**
         * RobotState enum.
         * @name lebai.system.RobotState
         * @enum {number}
         * @property {number} DISCONNECTED=0 DISCONNECTED value
         * @property {number} ESTOP=1 ESTOP value
         * @property {number} BOOTING=2 BOOTING value
         * @property {number} ROBOT_OFF=3 ROBOT_OFF value
         * @property {number} ROBOT_ON=4 ROBOT_ON value
         * @property {number} IDLE=5 IDLE value
         * @property {number} PAUSED=6 PAUSED value
         * @property {number} MOVING=7 MOVING value
         * @property {number} UPDATING=8 UPDATING value
         * @property {number} STARTING=9 STARTING value
         * @property {number} STOPPING=10 STOPPING value
         * @property {number} TEACHING=11 TEACHING value
         * @property {number} STOP=12 STOP value
         */
        system.RobotState = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "DISCONNECTED"] = 0;
            values[valuesById[1] = "ESTOP"] = 1;
            values[valuesById[2] = "BOOTING"] = 2;
            values[valuesById[3] = "ROBOT_OFF"] = 3;
            values[valuesById[4] = "ROBOT_ON"] = 4;
            values[valuesById[5] = "IDLE"] = 5;
            values[valuesById[6] = "PAUSED"] = 6;
            values[valuesById[7] = "MOVING"] = 7;
            values[valuesById[8] = "UPDATING"] = 8;
            values[valuesById[9] = "STARTING"] = 9;
            values[valuesById[10] = "STOPPING"] = 10;
            values[valuesById[11] = "TEACHING"] = 11;
            values[valuesById[12] = "STOP"] = 12;
            return values;
        })();

        system.GetRobotStateResponse = (function() {

            /**
             * Properties of a GetRobotStateResponse.
             * @typedef {Object} lebai.system.GetRobotStateResponse.$Properties
             * @property {lebai.system.RobotState|null} [state] GetRobotStateResponse state
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetRobotStateResponse.
             * @memberof lebai.system
             * @interface IGetRobotStateResponse
             * @augments lebai.system.GetRobotStateResponse.$Properties
             * @deprecated Use lebai.system.GetRobotStateResponse.$Properties instead.
             */

            /**
             * Shape of a GetRobotStateResponse.
             * @typedef {lebai.system.GetRobotStateResponse.$Properties} lebai.system.GetRobotStateResponse.$Shape
             */

            /**
             * Constructs a new GetRobotStateResponse.
             * @memberof lebai.system
             * @classdesc Represents a GetRobotStateResponse.
             * @constructor
             * @param {lebai.system.GetRobotStateResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetRobotStateResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetRobotStateResponse state.
             * @member {lebai.system.RobotState} state
             * @memberof lebai.system.GetRobotStateResponse
             * @instance
             */
            GetRobotStateResponse.prototype.state = 0;

            /**
             * Creates a new GetRobotStateResponse instance using the specified properties.
             * @function create
             * @memberof lebai.system.GetRobotStateResponse
             * @static
             * @param {lebai.system.GetRobotStateResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.system.GetRobotStateResponse} GetRobotStateResponse instance
             * @type {{
             *   (properties: lebai.system.GetRobotStateResponse.$Shape): lebai.system.GetRobotStateResponse & lebai.system.GetRobotStateResponse.$Shape;
             *   (properties?: lebai.system.GetRobotStateResponse.$Properties): lebai.system.GetRobotStateResponse;
             * }}
             */
            GetRobotStateResponse.create = function(properties) {
                return new GetRobotStateResponse(properties);
            };

            /**
             * Encodes the specified GetRobotStateResponse message. Does not implicitly {@link lebai.system.GetRobotStateResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.GetRobotStateResponse
             * @static
             * @param {lebai.system.GetRobotStateResponse.$Properties} message GetRobotStateResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRobotStateResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.state != null && $Object.hasOwnProperty.call(message, "state") && message.state !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.state);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetRobotStateResponse message, length delimited. Does not implicitly {@link lebai.system.GetRobotStateResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.GetRobotStateResponse
             * @static
             * @param {lebai.system.GetRobotStateResponse.$Properties} message GetRobotStateResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRobotStateResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetRobotStateResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.GetRobotStateResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.GetRobotStateResponse & lebai.system.GetRobotStateResponse.$Shape} GetRobotStateResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRobotStateResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.GetRobotStateResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.state = value;
                            else
                                delete message.state;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetRobotStateResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.GetRobotStateResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.GetRobotStateResponse & lebai.system.GetRobotStateResponse.$Shape} GetRobotStateResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRobotStateResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetRobotStateResponse message.
             * @function verify
             * @memberof lebai.system.GetRobotStateResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetRobotStateResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                    if (typeof message.state !== "number" || (message.state | 0) !== message.state)
                        return "state: enum value expected";
                return null;
            };

            /**
             * Creates a GetRobotStateResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.GetRobotStateResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.GetRobotStateResponse} GetRobotStateResponse
             */
            GetRobotStateResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.GetRobotStateResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.GetRobotStateResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.GetRobotStateResponse();
                if (object.state !== 0 && (typeof object.state !== "string" || $root.lebai.system.RobotState[object.state] !== 0))
                    switch (object.state) {
                    case "DISCONNECTED":
                    case 0:
                        message.state = 0;
                        break;
                    case "ESTOP":
                    case 1:
                        message.state = 1;
                        break;
                    case "BOOTING":
                    case 2:
                        message.state = 2;
                        break;
                    case "ROBOT_OFF":
                    case 3:
                        message.state = 3;
                        break;
                    case "ROBOT_ON":
                    case 4:
                        message.state = 4;
                        break;
                    case "IDLE":
                    case 5:
                        message.state = 5;
                        break;
                    case "PAUSED":
                    case 6:
                        message.state = 6;
                        break;
                    case "MOVING":
                    case 7:
                        message.state = 7;
                        break;
                    case "UPDATING":
                    case 8:
                        message.state = 8;
                        break;
                    case "STARTING":
                    case 9:
                        message.state = 9;
                        break;
                    case "STOPPING":
                    case 10:
                        message.state = 10;
                        break;
                    case "TEACHING":
                    case 11:
                        message.state = 11;
                        break;
                    case "STOP":
                    case 12:
                        message.state = 12;
                        break;
                    default:
                        if (typeof object.state === "number" && (object.state | 0) === object.state)
                            message.state = object.state;
                    }
                return message;
            };

            /**
             * Creates a plain object from a GetRobotStateResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.GetRobotStateResponse
             * @static
             * @param {lebai.system.GetRobotStateResponse} message GetRobotStateResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetRobotStateResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.state = options.enums === $String ? "DISCONNECTED" : 0;
                if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                    object.state = options.enums === $String ? $root.lebai.system.RobotState[message.state] === $undefined ? message.state : $root.lebai.system.RobotState[message.state] : message.state;
                return object;
            };

            /**
             * Converts this GetRobotStateResponse to JSON.
             * @function toJSON
             * @memberof lebai.system.GetRobotStateResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetRobotStateResponse.prototype.toJSON = function() {
                return GetRobotStateResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetRobotStateResponse
             * @function getTypeUrl
             * @memberof lebai.system.GetRobotStateResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetRobotStateResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.GetRobotStateResponse";
            };

            return GetRobotStateResponse;
        })();

        system.PhyData = (function() {

            /**
             * Properties of a PhyData.
             * @typedef {Object} lebai.system.PhyData.$Properties
             * @property {Array.<number>|null} [jointTemp] PhyData jointTemp
             * @property {Array.<number>|null} [jointVoltage] PhyData jointVoltage
             * @property {number|null} [flangeVoltage] PhyData flangeVoltage
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PhyData.
             * @memberof lebai.system
             * @interface IPhyData
             * @augments lebai.system.PhyData.$Properties
             * @deprecated Use lebai.system.PhyData.$Properties instead.
             */

            /**
             * Shape of a PhyData.
             * @typedef {lebai.system.PhyData.$Properties} lebai.system.PhyData.$Shape
             */

            /**
             * Constructs a new PhyData.
             * @memberof lebai.system
             * @classdesc Represents a PhyData.
             * @constructor
             * @param {lebai.system.PhyData.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var PhyData = function (properties) {
                this.jointTemp = [];
                this.jointVoltage = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PhyData jointTemp.
             * @member {Array.<number>} jointTemp
             * @memberof lebai.system.PhyData
             * @instance
             */
            PhyData.prototype.jointTemp = $util.emptyArray;

            /**
             * PhyData jointVoltage.
             * @member {Array.<number>} jointVoltage
             * @memberof lebai.system.PhyData
             * @instance
             */
            PhyData.prototype.jointVoltage = $util.emptyArray;

            /**
             * PhyData flangeVoltage.
             * @member {number} flangeVoltage
             * @memberof lebai.system.PhyData
             * @instance
             */
            PhyData.prototype.flangeVoltage = 0;

            /**
             * Creates a new PhyData instance using the specified properties.
             * @function create
             * @memberof lebai.system.PhyData
             * @static
             * @param {lebai.system.PhyData.$Properties=} [properties] Properties to set
             * @returns {lebai.system.PhyData} PhyData instance
             * @type {{
             *   (properties: lebai.system.PhyData.$Shape): lebai.system.PhyData & lebai.system.PhyData.$Shape;
             *   (properties?: lebai.system.PhyData.$Properties): lebai.system.PhyData;
             * }}
             */
            PhyData.create = function(properties) {
                return new PhyData(properties);
            };

            /**
             * Encodes the specified PhyData message. Does not implicitly {@link lebai.system.PhyData.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.PhyData
             * @static
             * @param {lebai.system.PhyData.$Properties} message PhyData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PhyData.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.jointTemp != null && message.jointTemp.length)
                    writer.uint32(/* id 2, wireType 2 =*/18).doubles(message.jointTemp);
                if (message.jointVoltage != null && message.jointVoltage.length)
                    writer.uint32(/* id 11, wireType 2 =*/90).doubles(message.jointVoltage);
                if (message.flangeVoltage != null && $Object.hasOwnProperty.call(message, "flangeVoltage") && !$Object.is(message.flangeVoltage, 0))
                    writer.uint32(/* id 12, wireType 1 =*/97).double(message.flangeVoltage);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PhyData message, length delimited. Does not implicitly {@link lebai.system.PhyData.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.PhyData
             * @static
             * @param {lebai.system.PhyData.$Properties} message PhyData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PhyData.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a PhyData message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.PhyData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.PhyData & lebai.system.PhyData.$Shape} PhyData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PhyData.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.PhyData();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.jointTemp && message.jointTemp.length))
                                    message.jointTemp = [];
                                reader.doubles(message.jointTemp);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.jointTemp && message.jointTemp.length))
                                message.jointTemp = [];
                            message.jointTemp.push(reader.double());
                            continue;
                        }
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.jointVoltage && message.jointVoltage.length))
                                    message.jointVoltage = [];
                                reader.doubles(message.jointVoltage);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.jointVoltage && message.jointVoltage.length))
                                message.jointVoltage = [];
                            message.jointVoltage.push(reader.double());
                            continue;
                        }
                    case 12: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.flangeVoltage = value;
                            else
                                delete message.flangeVoltage;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a PhyData message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.PhyData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.PhyData & lebai.system.PhyData.$Shape} PhyData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PhyData.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PhyData message.
             * @function verify
             * @memberof lebai.system.PhyData
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PhyData.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.jointTemp != null && $Object.hasOwnProperty.call(message, "jointTemp")) {
                    if (!$Array.isArray(message.jointTemp))
                        return "jointTemp: array expected";
                    for (var i = 0; i < message.jointTemp.length; ++i)
                        if (typeof message.jointTemp[i] !== "number")
                            return "jointTemp: number[] expected";
                }
                if (message.jointVoltage != null && $Object.hasOwnProperty.call(message, "jointVoltage")) {
                    if (!$Array.isArray(message.jointVoltage))
                        return "jointVoltage: array expected";
                    for (var i = 0; i < message.jointVoltage.length; ++i)
                        if (typeof message.jointVoltage[i] !== "number")
                            return "jointVoltage: number[] expected";
                }
                if (message.flangeVoltage != null && $Object.hasOwnProperty.call(message, "flangeVoltage"))
                    if (typeof message.flangeVoltage !== "number")
                        return "flangeVoltage: number expected";
                return null;
            };

            /**
             * Creates a PhyData message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.PhyData
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.PhyData} PhyData
             */
            PhyData.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.PhyData)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.PhyData: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.PhyData();
                if (object.jointTemp) {
                    if (!$Array.isArray(object.jointTemp))
                        throw $TypeError(".lebai.system.PhyData.jointTemp: array expected");
                    message.jointTemp = $Array(object.jointTemp.length);
                    for (var i = 0; i < object.jointTemp.length; ++i)
                        message.jointTemp[i] = $Number(object.jointTemp[i]);
                }
                if (object.jointVoltage) {
                    if (!$Array.isArray(object.jointVoltage))
                        throw $TypeError(".lebai.system.PhyData.jointVoltage: array expected");
                    message.jointVoltage = $Array(object.jointVoltage.length);
                    for (var i = 0; i < object.jointVoltage.length; ++i)
                        message.jointVoltage[i] = $Number(object.jointVoltage[i]);
                }
                if (object.flangeVoltage != null)
                    if (!$Object.is($Number(object.flangeVoltage), 0))
                        message.flangeVoltage = $Number(object.flangeVoltage);
                return message;
            };

            /**
             * Creates a plain object from a PhyData message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.PhyData
             * @static
             * @param {lebai.system.PhyData} message PhyData
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PhyData.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.jointTemp = [];
                    object.jointVoltage = [];
                }
                if (options.defaults)
                    object.flangeVoltage = 0;
                if (message.jointTemp && message.jointTemp.length) {
                    object.jointTemp = $Array(message.jointTemp.length);
                    for (var j = 0; j < message.jointTemp.length; ++j)
                        object.jointTemp[j] = options.json && !$isFinite(message.jointTemp[j]) ? $String(message.jointTemp[j]) : message.jointTemp[j];
                }
                if (message.jointVoltage && message.jointVoltage.length) {
                    object.jointVoltage = $Array(message.jointVoltage.length);
                    for (var j = 0; j < message.jointVoltage.length; ++j)
                        object.jointVoltage[j] = options.json && !$isFinite(message.jointVoltage[j]) ? $String(message.jointVoltage[j]) : message.jointVoltage[j];
                }
                if (message.flangeVoltage != null && $Object.hasOwnProperty.call(message, "flangeVoltage"))
                    object.flangeVoltage = options.json && !$isFinite(message.flangeVoltage) ? $String(message.flangeVoltage) : message.flangeVoltage;
                return object;
            };

            /**
             * Converts this PhyData to JSON.
             * @function toJSON
             * @memberof lebai.system.PhyData
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PhyData.prototype.toJSON = function() {
                return PhyData.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PhyData
             * @function getTypeUrl
             * @memberof lebai.system.PhyData
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PhyData.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.PhyData";
            };

            return PhyData;
        })();

        /**
         * EstopReason enum.
         * @name lebai.system.EstopReason
         * @enum {number}
         * @property {number} NONE=0 NONE value
         * @property {number} SYSTEM=2 SYSTEM value
         * @property {number} MANUAL=3 MANUAL value
         * @property {number} HARD_ESTOP=4 HARD_ESTOP value
         * @property {number} COLLISION=5 COLLISION value
         * @property {number} JOINT_LIMIT=6 JOINT_LIMIT value
         * @property {number} EXCEED=7 EXCEED value
         * @property {number} TRAJECTORY_ERROR=8 TRAJECTORY_ERROR value
         * @property {number} COMM_ERROR=11 COMM_ERROR value
         * @property {number} CAN_ERROR=12 CAN_ERROR value
         * @property {number} JOINT_ERROR=13 JOINT_ERROR value
         */
        system.EstopReason = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[2] = "SYSTEM"] = 2;
            values[valuesById[3] = "MANUAL"] = 3;
            values[valuesById[4] = "HARD_ESTOP"] = 4;
            values[valuesById[5] = "COLLISION"] = 5;
            values[valuesById[6] = "JOINT_LIMIT"] = 6;
            values[valuesById[7] = "EXCEED"] = 7;
            values[valuesById[8] = "TRAJECTORY_ERROR"] = 8;
            values[valuesById[11] = "COMM_ERROR"] = 11;
            values[valuesById[12] = "CAN_ERROR"] = 12;
            values[valuesById[13] = "JOINT_ERROR"] = 13;
            return values;
        })();

        system.GetEstopReasonResponse = (function() {

            /**
             * Properties of a GetEstopReasonResponse.
             * @typedef {Object} lebai.system.GetEstopReasonResponse.$Properties
             * @property {lebai.system.EstopReason|null} [reason] GetEstopReasonResponse reason
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetEstopReasonResponse.
             * @memberof lebai.system
             * @interface IGetEstopReasonResponse
             * @augments lebai.system.GetEstopReasonResponse.$Properties
             * @deprecated Use lebai.system.GetEstopReasonResponse.$Properties instead.
             */

            /**
             * Shape of a GetEstopReasonResponse.
             * @typedef {lebai.system.GetEstopReasonResponse.$Properties} lebai.system.GetEstopReasonResponse.$Shape
             */

            /**
             * Constructs a new GetEstopReasonResponse.
             * @memberof lebai.system
             * @classdesc Represents a GetEstopReasonResponse.
             * @constructor
             * @param {lebai.system.GetEstopReasonResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetEstopReasonResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetEstopReasonResponse reason.
             * @member {lebai.system.EstopReason} reason
             * @memberof lebai.system.GetEstopReasonResponse
             * @instance
             */
            GetEstopReasonResponse.prototype.reason = 0;

            /**
             * Creates a new GetEstopReasonResponse instance using the specified properties.
             * @function create
             * @memberof lebai.system.GetEstopReasonResponse
             * @static
             * @param {lebai.system.GetEstopReasonResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.system.GetEstopReasonResponse} GetEstopReasonResponse instance
             * @type {{
             *   (properties: lebai.system.GetEstopReasonResponse.$Shape): lebai.system.GetEstopReasonResponse & lebai.system.GetEstopReasonResponse.$Shape;
             *   (properties?: lebai.system.GetEstopReasonResponse.$Properties): lebai.system.GetEstopReasonResponse;
             * }}
             */
            GetEstopReasonResponse.create = function(properties) {
                return new GetEstopReasonResponse(properties);
            };

            /**
             * Encodes the specified GetEstopReasonResponse message. Does not implicitly {@link lebai.system.GetEstopReasonResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.system.GetEstopReasonResponse
             * @static
             * @param {lebai.system.GetEstopReasonResponse.$Properties} message GetEstopReasonResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetEstopReasonResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.reason != null && $Object.hasOwnProperty.call(message, "reason") && message.reason !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.reason);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetEstopReasonResponse message, length delimited. Does not implicitly {@link lebai.system.GetEstopReasonResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.system.GetEstopReasonResponse
             * @static
             * @param {lebai.system.GetEstopReasonResponse.$Properties} message GetEstopReasonResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetEstopReasonResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetEstopReasonResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.system.GetEstopReasonResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.system.GetEstopReasonResponse & lebai.system.GetEstopReasonResponse.$Shape} GetEstopReasonResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetEstopReasonResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.system.GetEstopReasonResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.reason = value;
                            else
                                delete message.reason;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetEstopReasonResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.system.GetEstopReasonResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.system.GetEstopReasonResponse & lebai.system.GetEstopReasonResponse.$Shape} GetEstopReasonResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetEstopReasonResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetEstopReasonResponse message.
             * @function verify
             * @memberof lebai.system.GetEstopReasonResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetEstopReasonResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.reason != null && $Object.hasOwnProperty.call(message, "reason"))
                    if (typeof message.reason !== "number" || (message.reason | 0) !== message.reason)
                        return "reason: enum value expected";
                return null;
            };

            /**
             * Creates a GetEstopReasonResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.system.GetEstopReasonResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.system.GetEstopReasonResponse} GetEstopReasonResponse
             */
            GetEstopReasonResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.system.GetEstopReasonResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.system.GetEstopReasonResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.system.GetEstopReasonResponse();
                if (object.reason !== 0 && (typeof object.reason !== "string" || $root.lebai.system.EstopReason[object.reason] !== 0))
                    switch (object.reason) {
                    case "NONE":
                    case 0:
                        message.reason = 0;
                        break;
                    case "SYSTEM":
                    case 2:
                        message.reason = 2;
                        break;
                    case "MANUAL":
                    case 3:
                        message.reason = 3;
                        break;
                    case "HARD_ESTOP":
                    case 4:
                        message.reason = 4;
                        break;
                    case "COLLISION":
                    case 5:
                        message.reason = 5;
                        break;
                    case "JOINT_LIMIT":
                    case 6:
                        message.reason = 6;
                        break;
                    case "EXCEED":
                    case 7:
                        message.reason = 7;
                        break;
                    case "TRAJECTORY_ERROR":
                    case 8:
                        message.reason = 8;
                        break;
                    case "COMM_ERROR":
                    case 11:
                        message.reason = 11;
                        break;
                    case "CAN_ERROR":
                    case 12:
                        message.reason = 12;
                        break;
                    case "JOINT_ERROR":
                    case 13:
                        message.reason = 13;
                        break;
                    default:
                        if (typeof object.reason === "number" && (object.reason | 0) === object.reason)
                            message.reason = object.reason;
                    }
                return message;
            };

            /**
             * Creates a plain object from a GetEstopReasonResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.system.GetEstopReasonResponse
             * @static
             * @param {lebai.system.GetEstopReasonResponse} message GetEstopReasonResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetEstopReasonResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.reason = options.enums === $String ? "NONE" : 0;
                if (message.reason != null && $Object.hasOwnProperty.call(message, "reason"))
                    object.reason = options.enums === $String ? $root.lebai.system.EstopReason[message.reason] === $undefined ? message.reason : $root.lebai.system.EstopReason[message.reason] : message.reason;
                return object;
            };

            /**
             * Converts this GetEstopReasonResponse to JSON.
             * @function toJSON
             * @memberof lebai.system.GetEstopReasonResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetEstopReasonResponse.prototype.toJSON = function() {
                return GetEstopReasonResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetEstopReasonResponse
             * @function getTypeUrl
             * @memberof lebai.system.GetEstopReasonResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetEstopReasonResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.system.GetEstopReasonResponse";
            };

            return GetEstopReasonResponse;
        })();

        system.SystemService = (function() {

            /**
             * Constructs a new SystemService service.
             * @memberof lebai.system
             * @classdesc Represents a SystemService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var SystemService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(SystemService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: SystemService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new SystemService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.system.SystemService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {SystemService} RPC service. Useful where requests and/or responses are streamed.
             */
            SystemService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.system.SystemService#hello}.
             * @memberof lebai.system.SystemService
             * @typedef HelloCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.system.HelloData} [response] HelloData
             */

            /**
             * Calls Hello.
             * @memberof lebai.system.SystemService
             * @typedef Hello
             * @type {{
             *   (request: lebai.system.IHelloData, callback: lebai.system.SystemService.HelloCallback): void;
             *   (request: lebai.system.IHelloData): Promise<lebai.system.HelloData>;
             *   readonly name: "Hello";
             *   readonly path: "/lebai.system.SystemService/Hello";
             *   readonly requestType: "HelloData";
             *   readonly responseType: "HelloData";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls Hello.
             * @name lebai.system.SystemService#hello
             * @type {lebai.system.SystemService.Hello}
             */
            $Object.defineProperties(SystemService.prototype.hello = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.hello, $root.lebai.system.HelloData, $root.lebai.system.HelloData, request, callback);
            }, {
                name: { value: "Hello" },
                path: { value: "/lebai.system.SystemService/Hello" },
                requestType: { value: "HelloData" },
                responseType: { value: "HelloData" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#getSystemInfo}.
             * @memberof lebai.system.SystemService
             * @typedef GetSystemInfoCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.system.SystemInfo} [response] SystemInfo
             */

            /**
             * Calls GetSystemInfo.
             * @memberof lebai.system.SystemService
             * @typedef GetSystemInfo
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.GetSystemInfoCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.system.SystemInfo>;
             *   readonly name: "GetSystemInfo";
             *   readonly path: "/lebai.system.SystemService/GetSystemInfo";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "SystemInfo";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetSystemInfo.
             * @name lebai.system.SystemService#getSystemInfo
             * @type {lebai.system.SystemService.GetSystemInfo}
             */
            $Object.defineProperties(SystemService.prototype.getSystemInfo = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.getSystemInfo, $root.google.protobuf.Empty, $root.lebai.system.SystemInfo, request, callback);
            }, {
                name: { value: "GetSystemInfo" },
                path: { value: "/lebai.system.SystemService/GetSystemInfo" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "SystemInfo" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#getRobotInfo}.
             * @memberof lebai.system.SystemService
             * @typedef GetRobotInfoCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.system.RobotInfo} [response] RobotInfo
             */

            /**
             * Calls GetRobotInfo.
             * @memberof lebai.system.SystemService
             * @typedef GetRobotInfo
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.GetRobotInfoCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.system.RobotInfo>;
             *   readonly name: "GetRobotInfo";
             *   readonly path: "/lebai.system.SystemService/GetRobotInfo";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "RobotInfo";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetRobotInfo.
             * @name lebai.system.SystemService#getRobotInfo
             * @type {lebai.system.SystemService.GetRobotInfo}
             */
            $Object.defineProperties(SystemService.prototype.getRobotInfo = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.getRobotInfo, $root.google.protobuf.Empty, $root.lebai.system.RobotInfo, request, callback);
            }, {
                name: { value: "GetRobotInfo" },
                path: { value: "/lebai.system.SystemService/GetRobotInfo" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "RobotInfo" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#getHardwareInfo}.
             * @memberof lebai.system.SystemService
             * @typedef GetHardwareInfoCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.system.HardwareInfo} [response] HardwareInfo
             */

            /**
             * Calls GetHardwareInfo.
             * @memberof lebai.system.SystemService
             * @typedef GetHardwareInfo
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.GetHardwareInfoCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.system.HardwareInfo>;
             *   readonly name: "GetHardwareInfo";
             *   readonly path: "/lebai.system.SystemService/GetHardwareInfo";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "HardwareInfo";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetHardwareInfo.
             * @name lebai.system.SystemService#getHardwareInfo
             * @type {lebai.system.SystemService.GetHardwareInfo}
             */
            $Object.defineProperties(SystemService.prototype.getHardwareInfo = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.getHardwareInfo, $root.google.protobuf.Empty, $root.lebai.system.HardwareInfo, request, callback);
            }, {
                name: { value: "GetHardwareInfo" },
                path: { value: "/lebai.system.SystemService/GetHardwareInfo" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "HardwareInfo" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#getSoftwareInfo}.
             * @memberof lebai.system.SystemService
             * @typedef GetSoftwareInfoCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.system.SoftwareInfo} [response] SoftwareInfo
             */

            /**
             * Calls GetSoftwareInfo.
             * @memberof lebai.system.SystemService
             * @typedef GetSoftwareInfo
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.GetSoftwareInfoCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.system.SoftwareInfo>;
             *   readonly name: "GetSoftwareInfo";
             *   readonly path: "/lebai.system.SystemService/GetSoftwareInfo";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "SoftwareInfo";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetSoftwareInfo.
             * @name lebai.system.SystemService#getSoftwareInfo
             * @type {lebai.system.SystemService.GetSoftwareInfo}
             */
            $Object.defineProperties(SystemService.prototype.getSoftwareInfo = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.getSoftwareInfo, $root.google.protobuf.Empty, $root.lebai.system.SoftwareInfo, request, callback);
            }, {
                name: { value: "GetSoftwareInfo" },
                path: { value: "/lebai.system.SystemService/GetSoftwareInfo" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "SoftwareInfo" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#getBoxDevices}.
             * @memberof lebai.system.SystemService
             * @typedef GetBoxDevicesCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.system.GetBoxDevicesResponse} [response] GetBoxDevicesResponse
             */

            /**
             * Calls GetBoxDevices.
             * @memberof lebai.system.SystemService
             * @typedef GetBoxDevices
             * @type {{
             *   (request: lebai.system.IGetBoxDevicesRequest, callback: lebai.system.SystemService.GetBoxDevicesCallback): void;
             *   (request: lebai.system.IGetBoxDevicesRequest): Promise<lebai.system.GetBoxDevicesResponse>;
             *   readonly name: "GetBoxDevices";
             *   readonly path: "/lebai.system.SystemService/GetBoxDevices";
             *   readonly requestType: "GetBoxDevicesRequest";
             *   readonly responseType: "GetBoxDevicesResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetBoxDevices.
             * @name lebai.system.SystemService#getBoxDevices
             * @type {lebai.system.SystemService.GetBoxDevices}
             */
            $Object.defineProperties(SystemService.prototype.getBoxDevices = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.getBoxDevices, $root.lebai.system.GetBoxDevicesRequest, $root.lebai.system.GetBoxDevicesResponse, request, callback);
            }, {
                name: { value: "GetBoxDevices" },
                path: { value: "/lebai.system.SystemService/GetBoxDevices" },
                requestType: { value: "GetBoxDevicesRequest" },
                responseType: { value: "GetBoxDevicesResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#setVirtualIp}.
             * @memberof lebai.system.SystemService
             * @typedef SetVirtualIpCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetVirtualIp.
             * @memberof lebai.system.SystemService
             * @typedef SetVirtualIp
             * @type {{
             *   (request: lebai.system.ISetVirtualIpRequest, callback: lebai.system.SystemService.SetVirtualIpCallback): void;
             *   (request: lebai.system.ISetVirtualIpRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetVirtualIp";
             *   readonly path: "/lebai.system.SystemService/SetVirtualIp";
             *   readonly requestType: "SetVirtualIpRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetVirtualIp.
             * @name lebai.system.SystemService#setVirtualIp
             * @type {lebai.system.SystemService.SetVirtualIp}
             */
            $Object.defineProperties(SystemService.prototype.setVirtualIp = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.setVirtualIp, $root.lebai.system.SetVirtualIpRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetVirtualIp" },
                path: { value: "/lebai.system.SystemService/SetVirtualIp" },
                requestType: { value: "SetVirtualIpRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#getRobotState}.
             * @memberof lebai.system.SystemService
             * @typedef GetRobotStateCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.system.GetRobotStateResponse} [response] GetRobotStateResponse
             */

            /**
             * Calls GetRobotState.
             * @memberof lebai.system.SystemService
             * @typedef GetRobotState
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.GetRobotStateCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.system.GetRobotStateResponse>;
             *   readonly name: "GetRobotState";
             *   readonly path: "/lebai.system.SystemService/GetRobotState";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "GetRobotStateResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetRobotState.
             * @name lebai.system.SystemService#getRobotState
             * @type {lebai.system.SystemService.GetRobotState}
             */
            $Object.defineProperties(SystemService.prototype.getRobotState = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.getRobotState, $root.google.protobuf.Empty, $root.lebai.system.GetRobotStateResponse, request, callback);
            }, {
                name: { value: "GetRobotState" },
                path: { value: "/lebai.system.SystemService/GetRobotState" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "GetRobotStateResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#subRobotState}.
             * @memberof lebai.system.SystemService
             * @typedef SubRobotStateCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.system.GetRobotStateResponse} [response] GetRobotStateResponse
             */

            /**
             * Calls SubRobotState.
             * @memberof lebai.system.SystemService
             * @typedef SubRobotState
             * @type {{
             *   (request: lebai.ISubscribeRequest, callback: lebai.system.SystemService.SubRobotStateCallback): void;
             *   (request: lebai.ISubscribeRequest): Promise<lebai.system.GetRobotStateResponse>;
             *   readonly name: "SubRobotState";
             *   readonly path: "/lebai.system.SystemService/SubRobotState";
             *   readonly requestType: "SubscribeRequest";
             *   readonly responseType: "GetRobotStateResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: true;
             * }}
             */

            /**
             * Calls SubRobotState.
             * @name lebai.system.SystemService#subRobotState
             * @type {lebai.system.SystemService.SubRobotState}
             */
            $Object.defineProperties(SystemService.prototype.subRobotState = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.subRobotState, $root.lebai.SubscribeRequest, $root.lebai.system.GetRobotStateResponse, request, callback);
            }, {
                name: { value: "SubRobotState" },
                path: { value: "/lebai.system.SystemService/SubRobotState" },
                requestType: { value: "SubscribeRequest" },
                responseType: { value: "GetRobotStateResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: true }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#getPhyData}.
             * @memberof lebai.system.SystemService
             * @typedef GetPhyDataCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.system.PhyData} [response] PhyData
             */

            /**
             * Calls GetPhyData.
             * @memberof lebai.system.SystemService
             * @typedef GetPhyData
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.GetPhyDataCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.system.PhyData>;
             *   readonly name: "GetPhyData";
             *   readonly path: "/lebai.system.SystemService/GetPhyData";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "PhyData";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetPhyData.
             * @name lebai.system.SystemService#getPhyData
             * @type {lebai.system.SystemService.GetPhyData}
             */
            $Object.defineProperties(SystemService.prototype.getPhyData = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.getPhyData, $root.google.protobuf.Empty, $root.lebai.system.PhyData, request, callback);
            }, {
                name: { value: "GetPhyData" },
                path: { value: "/lebai.system.SystemService/GetPhyData" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "PhyData" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#subPhyData}.
             * @memberof lebai.system.SystemService
             * @typedef SubPhyDataCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.system.PhyData} [response] PhyData
             */

            /**
             * Calls SubPhyData.
             * @memberof lebai.system.SystemService
             * @typedef SubPhyData
             * @type {{
             *   (request: lebai.ISubscribeRequest, callback: lebai.system.SystemService.SubPhyDataCallback): void;
             *   (request: lebai.ISubscribeRequest): Promise<lebai.system.PhyData>;
             *   readonly name: "SubPhyData";
             *   readonly path: "/lebai.system.SystemService/SubPhyData";
             *   readonly requestType: "SubscribeRequest";
             *   readonly responseType: "PhyData";
             *   readonly requestStream: undefined;
             *   readonly responseStream: true;
             * }}
             */

            /**
             * Calls SubPhyData.
             * @name lebai.system.SystemService#subPhyData
             * @type {lebai.system.SystemService.SubPhyData}
             */
            $Object.defineProperties(SystemService.prototype.subPhyData = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.subPhyData, $root.lebai.SubscribeRequest, $root.lebai.system.PhyData, request, callback);
            }, {
                name: { value: "SubPhyData" },
                path: { value: "/lebai.system.SystemService/SubPhyData" },
                requestType: { value: "SubscribeRequest" },
                responseType: { value: "PhyData" },
                requestStream: { value: $undefined },
                responseStream: { value: true }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#powerdown}.
             * @memberof lebai.system.SystemService
             * @typedef PowerdownCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls Powerdown.
             * @memberof lebai.system.SystemService
             * @typedef Powerdown
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.PowerdownCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "Powerdown";
             *   readonly path: "/lebai.system.SystemService/Powerdown";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls Powerdown.
             * @name lebai.system.SystemService#powerdown
             * @type {lebai.system.SystemService.Powerdown}
             */
            $Object.defineProperties(SystemService.prototype.powerdown = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.powerdown, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "Powerdown" },
                path: { value: "/lebai.system.SystemService/Powerdown" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#poweron}.
             * @memberof lebai.system.SystemService
             * @typedef PoweronCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls Poweron.
             * @memberof lebai.system.SystemService
             * @typedef Poweron
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.PoweronCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "Poweron";
             *   readonly path: "/lebai.system.SystemService/Poweron";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls Poweron.
             * @name lebai.system.SystemService#poweron
             * @type {lebai.system.SystemService.Poweron}
             */
            $Object.defineProperties(SystemService.prototype.poweron = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.poweron, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "Poweron" },
                path: { value: "/lebai.system.SystemService/Poweron" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#reboot}.
             * @memberof lebai.system.SystemService
             * @typedef RebootCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls Reboot.
             * @memberof lebai.system.SystemService
             * @typedef Reboot
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.RebootCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "Reboot";
             *   readonly path: "/lebai.system.SystemService/Reboot";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls Reboot.
             * @name lebai.system.SystemService#reboot
             * @type {lebai.system.SystemService.Reboot}
             */
            $Object.defineProperties(SystemService.prototype.reboot = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.reboot, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "Reboot" },
                path: { value: "/lebai.system.SystemService/Reboot" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#startSys}.
             * @memberof lebai.system.SystemService
             * @typedef StartSysCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls StartSys.
             * @memberof lebai.system.SystemService
             * @typedef StartSys
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.StartSysCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "StartSys";
             *   readonly path: "/lebai.system.SystemService/StartSys";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls StartSys.
             * @name lebai.system.SystemService#startSys
             * @type {lebai.system.SystemService.StartSys}
             */
            $Object.defineProperties(SystemService.prototype.startSys = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.startSys, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "StartSys" },
                path: { value: "/lebai.system.SystemService/StartSys" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#stopSys}.
             * @memberof lebai.system.SystemService
             * @typedef StopSysCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls StopSys.
             * @memberof lebai.system.SystemService
             * @typedef StopSys
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.StopSysCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "StopSys";
             *   readonly path: "/lebai.system.SystemService/StopSys";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls StopSys.
             * @name lebai.system.SystemService#stopSys
             * @type {lebai.system.SystemService.StopSys}
             */
            $Object.defineProperties(SystemService.prototype.stopSys = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.stopSys, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "StopSys" },
                path: { value: "/lebai.system.SystemService/StopSys" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#stop}.
             * @memberof lebai.system.SystemService
             * @typedef StopCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls Stop.
             * @memberof lebai.system.SystemService
             * @typedef Stop
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.StopCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "Stop";
             *   readonly path: "/lebai.system.SystemService/Stop";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls Stop.
             * @name lebai.system.SystemService#stop
             * @type {lebai.system.SystemService.Stop}
             */
            $Object.defineProperties(SystemService.prototype.stop = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.stop, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "Stop" },
                path: { value: "/lebai.system.SystemService/Stop" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#estop}.
             * @memberof lebai.system.SystemService
             * @typedef EstopCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls Estop.
             * @memberof lebai.system.SystemService
             * @typedef Estop
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.EstopCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "Estop";
             *   readonly path: "/lebai.system.SystemService/Estop";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls Estop.
             * @name lebai.system.SystemService#estop
             * @type {lebai.system.SystemService.Estop}
             */
            $Object.defineProperties(SystemService.prototype.estop = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.estop, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "Estop" },
                path: { value: "/lebai.system.SystemService/Estop" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.system.SystemService#getEstopReason}.
             * @memberof lebai.system.SystemService
             * @typedef GetEstopReasonCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.system.GetEstopReasonResponse} [response] GetEstopReasonResponse
             */

            /**
             * Calls GetEstopReason.
             * @memberof lebai.system.SystemService
             * @typedef GetEstopReason
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.system.SystemService.GetEstopReasonCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.system.GetEstopReasonResponse>;
             *   readonly name: "GetEstopReason";
             *   readonly path: "/lebai.system.SystemService/GetEstopReason";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "GetEstopReasonResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetEstopReason.
             * @name lebai.system.SystemService#getEstopReason
             * @type {lebai.system.SystemService.GetEstopReason}
             */
            $Object.defineProperties(SystemService.prototype.getEstopReason = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SystemService.prototype.getEstopReason, $root.google.protobuf.Empty, $root.lebai.system.GetEstopReasonResponse, request, callback);
            }, {
                name: { value: "GetEstopReason" },
                path: { value: "/lebai.system.SystemService/GetEstopReason" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "GetEstopReasonResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return SystemService;
        })();

        return system;
    })();

    lebai.SubscribeRequest = (function() {

        /**
         * Properties of a SubscribeRequest.
         * @typedef {Object} lebai.SubscribeRequest.$Properties
         * @property {number|Long|null} [intervalMin] SubscribeRequest intervalMin
         * @property {number|Long|null} [intervalMax] SubscribeRequest intervalMax
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a SubscribeRequest.
         * @memberof lebai
         * @interface ISubscribeRequest
         * @augments lebai.SubscribeRequest.$Properties
         * @deprecated Use lebai.SubscribeRequest.$Properties instead.
         */

        /**
         * Shape of a SubscribeRequest.
         * @typedef {lebai.SubscribeRequest.$Properties} lebai.SubscribeRequest.$Shape
         */

        /**
         * Constructs a new SubscribeRequest.
         * @memberof lebai
         * @classdesc Represents a SubscribeRequest.
         * @constructor
         * @param {lebai.SubscribeRequest.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        var SubscribeRequest = function (properties) {
            if (properties)
                for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * SubscribeRequest intervalMin.
         * @member {number|Long} intervalMin
         * @memberof lebai.SubscribeRequest
         * @instance
         */
        SubscribeRequest.prototype.intervalMin = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * SubscribeRequest intervalMax.
         * @member {number|Long} intervalMax
         * @memberof lebai.SubscribeRequest
         * @instance
         */
        SubscribeRequest.prototype.intervalMax = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new SubscribeRequest instance using the specified properties.
         * @function create
         * @memberof lebai.SubscribeRequest
         * @static
         * @param {lebai.SubscribeRequest.$Properties=} [properties] Properties to set
         * @returns {lebai.SubscribeRequest} SubscribeRequest instance
         * @type {{
         *   (properties: lebai.SubscribeRequest.$Shape): lebai.SubscribeRequest & lebai.SubscribeRequest.$Shape;
         *   (properties?: lebai.SubscribeRequest.$Properties): lebai.SubscribeRequest;
         * }}
         */
        SubscribeRequest.create = function(properties) {
            return new SubscribeRequest(properties);
        };

        /**
         * Encodes the specified SubscribeRequest message. Does not implicitly {@link lebai.SubscribeRequest.verify|verify} messages.
         * @function encode
         * @memberof lebai.SubscribeRequest
         * @static
         * @param {lebai.SubscribeRequest.$Properties} message SubscribeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribeRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.intervalMin != null && $Object.hasOwnProperty.call(message, "intervalMin") && (typeof message.intervalMin === "object" ? message.intervalMin.low || message.intervalMin.high : message.intervalMin !== 0))
                writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.intervalMin);
            if (message.intervalMax != null && $Object.hasOwnProperty.call(message, "intervalMax") && (typeof message.intervalMax === "object" ? message.intervalMax.low || message.intervalMax.high : message.intervalMax !== 0))
                writer.uint32(/* id 12, wireType 0 =*/96).uint64(message.intervalMax);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SubscribeRequest message, length delimited. Does not implicitly {@link lebai.SubscribeRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lebai.SubscribeRequest
         * @static
         * @param {lebai.SubscribeRequest.$Properties} message SubscribeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SubscribeRequest.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a SubscribeRequest message from the specified reader or buffer.
         * @function decode
         * @memberof lebai.SubscribeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lebai.SubscribeRequest & lebai.SubscribeRequest.$Shape} SubscribeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribeRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.lebai.SubscribeRequest();
            while (reader.pos < end) {
                var start = reader.pos;
                var tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                var wireType = tag & 7;
                switch (tag >>>= 3) {
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.intervalMin = value;
                        else
                            delete message.intervalMin;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.intervalMax = value;
                        else
                            delete message.intervalMax;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a SubscribeRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lebai.SubscribeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lebai.SubscribeRequest & lebai.SubscribeRequest.$Shape} SubscribeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SubscribeRequest.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SubscribeRequest message.
         * @function verify
         * @memberof lebai.SubscribeRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SubscribeRequest.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.intervalMin != null && $Object.hasOwnProperty.call(message, "intervalMin"))
                if (!$util.isInteger(message.intervalMin) && !(message.intervalMin && $util.isInteger(message.intervalMin.low) && $util.isInteger(message.intervalMin.high)))
                    return "intervalMin: integer|Long expected";
            if (message.intervalMax != null && $Object.hasOwnProperty.call(message, "intervalMax"))
                if (!$util.isInteger(message.intervalMax) && !(message.intervalMax && $util.isInteger(message.intervalMax.low) && $util.isInteger(message.intervalMax.high)))
                    return "intervalMax: integer|Long expected";
            return null;
        };

        /**
         * Creates a SubscribeRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lebai.SubscribeRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lebai.SubscribeRequest} SubscribeRequest
         */
        SubscribeRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.lebai.SubscribeRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lebai.SubscribeRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var message = new $root.lebai.SubscribeRequest();
            if (object.intervalMin != null)
                if (typeof object.intervalMin === "object" ? object.intervalMin.low || object.intervalMin.high : $Number(object.intervalMin) !== 0)
                    if ($util.Long)
                        message.intervalMin = $util.Long.fromValue(object.intervalMin, true);
                    else if (typeof object.intervalMin === "string")
                        message.intervalMin = $parseInt(object.intervalMin, 10);
                    else if (typeof object.intervalMin === "number")
                        message.intervalMin = object.intervalMin;
                    else if (typeof object.intervalMin === "object")
                        message.intervalMin = new $util.LongBits(object.intervalMin.low >>> 0, object.intervalMin.high >>> 0).toNumber(true);
            if (object.intervalMax != null)
                if (typeof object.intervalMax === "object" ? object.intervalMax.low || object.intervalMax.high : $Number(object.intervalMax) !== 0)
                    if ($util.Long)
                        message.intervalMax = $util.Long.fromValue(object.intervalMax, true);
                    else if (typeof object.intervalMax === "string")
                        message.intervalMax = $parseInt(object.intervalMax, 10);
                    else if (typeof object.intervalMax === "number")
                        message.intervalMax = object.intervalMax;
                    else if (typeof object.intervalMax === "object")
                        message.intervalMax = new $util.LongBits(object.intervalMax.low >>> 0, object.intervalMax.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a SubscribeRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lebai.SubscribeRequest
         * @static
         * @param {lebai.SubscribeRequest} message SubscribeRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SubscribeRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                if ($util.Long) {
                    var long = new $util.Long(0, 0, true);
                    object.intervalMin = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.intervalMin = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    var long = new $util.Long(0, 0, true);
                    object.intervalMax = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.intervalMax = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.intervalMin != null && $Object.hasOwnProperty.call(message, "intervalMin"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.intervalMin = typeof message.intervalMin === "number" ? $BigInt(message.intervalMin) : $util.Long.fromBits(message.intervalMin.low >>> 0, message.intervalMin.high >>> 0, true).toBigInt();
                else if (typeof message.intervalMin === "number")
                    object.intervalMin = options.longs === $String ? $String(message.intervalMin) : message.intervalMin;
                else
                    object.intervalMin = options.longs === $String ? $util.Long.prototype.toString.call(message.intervalMin) : options.longs === $Number ? new $util.LongBits(message.intervalMin.low >>> 0, message.intervalMin.high >>> 0).toNumber(true) : message.intervalMin;
            if (message.intervalMax != null && $Object.hasOwnProperty.call(message, "intervalMax"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.intervalMax = typeof message.intervalMax === "number" ? $BigInt(message.intervalMax) : $util.Long.fromBits(message.intervalMax.low >>> 0, message.intervalMax.high >>> 0, true).toBigInt();
                else if (typeof message.intervalMax === "number")
                    object.intervalMax = options.longs === $String ? $String(message.intervalMax) : message.intervalMax;
                else
                    object.intervalMax = options.longs === $String ? $util.Long.prototype.toString.call(message.intervalMax) : options.longs === $Number ? new $util.LongBits(message.intervalMax.low >>> 0, message.intervalMax.high >>> 0).toNumber(true) : message.intervalMax;
            return object;
        };

        /**
         * Converts this SubscribeRequest to JSON.
         * @function toJSON
         * @memberof lebai.SubscribeRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SubscribeRequest.prototype.toJSON = function() {
            return SubscribeRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for SubscribeRequest
         * @function getTypeUrl
         * @memberof lebai.SubscribeRequest
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        SubscribeRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lebai.SubscribeRequest";
        };

        return SubscribeRequest;
    })();

    lebai.CommandStdout = (function() {

        /**
         * Properties of a CommandStdout.
         * @typedef {Object} lebai.CommandStdout.$Properties
         * @property {boolean|null} [done] CommandStdout done
         * @property {string|null} [stdout] CommandStdout stdout
         * @property {string|null} [stderr] CommandStdout stderr
         * @property {number|null} [code] CommandStdout code
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */

        /**
         * Properties of a CommandStdout.
         * @memberof lebai
         * @interface ICommandStdout
         * @augments lebai.CommandStdout.$Properties
         * @deprecated Use lebai.CommandStdout.$Properties instead.
         */

        /**
         * Shape of a CommandStdout.
         * @typedef {lebai.CommandStdout.$Properties} lebai.CommandStdout.$Shape
         */

        /**
         * Constructs a new CommandStdout.
         * @memberof lebai
         * @classdesc Represents a CommandStdout.
         * @constructor
         * @param {lebai.CommandStdout.$Properties=} [properties] Properties to set
         * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
         */
        var CommandStdout = function (properties) {
            if (properties)
                for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        /**
         * CommandStdout done.
         * @member {boolean} done
         * @memberof lebai.CommandStdout
         * @instance
         */
        CommandStdout.prototype.done = false;

        /**
         * CommandStdout stdout.
         * @member {string} stdout
         * @memberof lebai.CommandStdout
         * @instance
         */
        CommandStdout.prototype.stdout = "";

        /**
         * CommandStdout stderr.
         * @member {string} stderr
         * @memberof lebai.CommandStdout
         * @instance
         */
        CommandStdout.prototype.stderr = "";

        /**
         * CommandStdout code.
         * @member {number} code
         * @memberof lebai.CommandStdout
         * @instance
         */
        CommandStdout.prototype.code = 0;

        /**
         * Creates a new CommandStdout instance using the specified properties.
         * @function create
         * @memberof lebai.CommandStdout
         * @static
         * @param {lebai.CommandStdout.$Properties=} [properties] Properties to set
         * @returns {lebai.CommandStdout} CommandStdout instance
         * @type {{
         *   (properties: lebai.CommandStdout.$Shape): lebai.CommandStdout & lebai.CommandStdout.$Shape;
         *   (properties?: lebai.CommandStdout.$Properties): lebai.CommandStdout;
         * }}
         */
        CommandStdout.create = function(properties) {
            return new CommandStdout(properties);
        };

        /**
         * Encodes the specified CommandStdout message. Does not implicitly {@link lebai.CommandStdout.verify|verify} messages.
         * @function encode
         * @memberof lebai.CommandStdout
         * @static
         * @param {lebai.CommandStdout.$Properties} message CommandStdout message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CommandStdout.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.done != null && $Object.hasOwnProperty.call(message, "done") && message.done !== false)
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.done);
            if (message.stdout != null && $Object.hasOwnProperty.call(message, "stdout") && message.stdout !== "")
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.stdout);
            if (message.stderr != null && $Object.hasOwnProperty.call(message, "stderr") && message.stderr !== "")
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.stderr);
            if (message.code != null && $Object.hasOwnProperty.call(message, "code") && message.code !== 0)
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.code);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CommandStdout message, length delimited. Does not implicitly {@link lebai.CommandStdout.verify|verify} messages.
         * @function encodeDelimited
         * @memberof lebai.CommandStdout
         * @static
         * @param {lebai.CommandStdout.$Properties} message CommandStdout message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CommandStdout.encodeDelimited = function(message, writer) {
            return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
        };

        /**
         * Decodes a CommandStdout message from the specified reader or buffer.
         * @function decode
         * @memberof lebai.CommandStdout
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {lebai.CommandStdout & lebai.CommandStdout.$Shape} CommandStdout
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CommandStdout.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var end, message, value;
            if (length === $undefined)
                end = reader.len;
            else {
                end = reader.pos + length;
                if (end > reader.len)
                    throw $RangeError("index out of range");
                length = reader.len;
                reader.len = end;
            }
            message = _target || new $root.lebai.CommandStdout();
            while (reader.pos < end) {
                var start = reader.pos;
                var tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                var wireType = tag & 7;
                switch (tag >>>= 3) {
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.done = value;
                        else
                            delete message.done;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.stdout = value;
                        else
                            delete message.stdout;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.stderr = value;
                        else
                            delete message.stderr;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (length !== $undefined) {
                if (reader.pos !== end)
                    throw $RangeError("index out of range");
                reader.len = length;
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        /**
         * Decodes a CommandStdout message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof lebai.CommandStdout
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {lebai.CommandStdout & lebai.CommandStdout.$Shape} CommandStdout
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CommandStdout.decodeDelimited = function(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CommandStdout message.
         * @function verify
         * @memberof lebai.CommandStdout
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CommandStdout.verify = function (message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.done != null && $Object.hasOwnProperty.call(message, "done"))
                if (typeof message.done !== "boolean")
                    return "done: boolean expected";
            if (message.stdout != null && $Object.hasOwnProperty.call(message, "stdout"))
                if (!$util.isString(message.stdout))
                    return "stdout: string expected";
            if (message.stderr != null && $Object.hasOwnProperty.call(message, "stderr"))
                if (!$util.isString(message.stderr))
                    return "stderr: string expected";
            if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                if (!$util.isInteger(message.code))
                    return "code: integer expected";
            return null;
        };

        /**
         * Creates a CommandStdout message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof lebai.CommandStdout
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {lebai.CommandStdout} CommandStdout
         */
        CommandStdout.fromObject = function (object, _depth) {
            if (object instanceof $root.lebai.CommandStdout)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".lebai.CommandStdout: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var message = new $root.lebai.CommandStdout();
            if (object.done != null)
                if (object.done)
                    message.done = $Boolean(object.done);
            if (object.stdout != null)
                if (typeof object.stdout !== "string" || object.stdout.length)
                    message.stdout = $String(object.stdout);
            if (object.stderr != null)
                if (typeof object.stderr !== "string" || object.stderr.length)
                    message.stderr = $String(object.stderr);
            if (object.code != null)
                if ($Number(object.code) !== 0)
                    message.code = object.code | 0;
            return message;
        };

        /**
         * Creates a plain object from a CommandStdout message. Also converts values to other types if specified.
         * @function toObject
         * @memberof lebai.CommandStdout
         * @static
         * @param {lebai.CommandStdout} message CommandStdout
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CommandStdout.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var object = {};
            if (options.defaults) {
                object.done = false;
                object.stdout = "";
                object.stderr = "";
                object.code = 0;
            }
            if (message.done != null && $Object.hasOwnProperty.call(message, "done"))
                object.done = message.done;
            if (message.stdout != null && $Object.hasOwnProperty.call(message, "stdout"))
                object.stdout = message.stdout;
            if (message.stderr != null && $Object.hasOwnProperty.call(message, "stderr"))
                object.stderr = message.stderr;
            if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                object.code = message.code;
            return object;
        };

        /**
         * Converts this CommandStdout to JSON.
         * @function toJSON
         * @memberof lebai.CommandStdout
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CommandStdout.prototype.toJSON = function() {
            return CommandStdout.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the type url for CommandStdout
         * @function getTypeUrl
         * @memberof lebai.CommandStdout
         * @static
         * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
         * @returns {string} The type url
         */
        CommandStdout.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lebai.CommandStdout";
        };

        return CommandStdout;
    })();

    lebai.hardware = (function() {

        /**
         * Namespace hardware.
         * @memberof lebai
         * @namespace
         */
        var hardware = {};

        /**
         * Address enum.
         * @name lebai.hardware.Address
         * @enum {number}
         * @property {number} EMPTY=0 发送时代表广播，接收时代表无设备
         * @property {number} JOINT1=1 关节1（离基座最近）
         * @property {number} JOINT2=2 关节2
         * @property {number} JOINT3=3 关节3
         * @property {number} JOINT4=4 关节4
         * @property {number} JOINT5=5 关节5
         * @property {number} JOINT6=6 关节6
         * @property {number} JOINT7=7 关节7
         * @property {number} FLANGE=8 法兰
         * @property {number} CLAW=9 夹爪
         * @property {number} LED=10 灯板
         * @property {number} DIAL=13 百分表
         * @property {number} COMM_BOARD=14 通信板
         * @property {number} TEST_BOARD=15 测试板
         */
        hardware.Address = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "EMPTY"] = 0;
            values[valuesById[1] = "JOINT1"] = 1;
            values[valuesById[2] = "JOINT2"] = 2;
            values[valuesById[3] = "JOINT3"] = 3;
            values[valuesById[4] = "JOINT4"] = 4;
            values[valuesById[5] = "JOINT5"] = 5;
            values[valuesById[6] = "JOINT6"] = 6;
            values[valuesById[7] = "JOINT7"] = 7;
            values[valuesById[8] = "FLANGE"] = 8;
            values[valuesById[9] = "CLAW"] = 9;
            values[valuesById[10] = "LED"] = 10;
            values[valuesById[13] = "DIAL"] = 13;
            values[valuesById[14] = "COMM_BOARD"] = 14;
            values[valuesById[15] = "TEST_BOARD"] = 15;
            return values;
        })();

        /**
         * FirmwarePartition enum.
         * @name lebai.hardware.FirmwarePartition
         * @enum {number}
         * @property {number} A=0 A value
         * @property {number} B=1 B value
         * @property {number} C=2 C value
         * @property {number} UNKNOWN=3 UNKNOWN value
         */
        hardware.FirmwarePartition = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "A"] = 0;
            values[valuesById[1] = "B"] = 1;
            values[valuesById[2] = "C"] = 2;
            values[valuesById[3] = "UNKNOWN"] = 3;
            return values;
        })();

        hardware.FirmwareInfo = (function() {

            /**
             * Properties of a FirmwareInfo.
             * @typedef {Object} lebai.hardware.FirmwareInfo.$Properties
             * @property {string|null} [sn] FirmwareInfo sn
             * @property {string|null} [version] FirmwareInfo version
             * @property {lebai.hardware.FirmwarePartition|null} [partition] FirmwareInfo partition
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FirmwareInfo.
             * @memberof lebai.hardware
             * @interface IFirmwareInfo
             * @augments lebai.hardware.FirmwareInfo.$Properties
             * @deprecated Use lebai.hardware.FirmwareInfo.$Properties instead.
             */

            /**
             * Shape of a FirmwareInfo.
             * @typedef {lebai.hardware.FirmwareInfo.$Properties} lebai.hardware.FirmwareInfo.$Shape
             */

            /**
             * Constructs a new FirmwareInfo.
             * @memberof lebai.hardware
             * @classdesc Represents a FirmwareInfo.
             * @constructor
             * @param {lebai.hardware.FirmwareInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var FirmwareInfo = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FirmwareInfo sn.
             * @member {string} sn
             * @memberof lebai.hardware.FirmwareInfo
             * @instance
             */
            FirmwareInfo.prototype.sn = "";

            /**
             * FirmwareInfo version.
             * @member {string} version
             * @memberof lebai.hardware.FirmwareInfo
             * @instance
             */
            FirmwareInfo.prototype.version = "";

            /**
             * FirmwareInfo partition.
             * @member {lebai.hardware.FirmwarePartition} partition
             * @memberof lebai.hardware.FirmwareInfo
             * @instance
             */
            FirmwareInfo.prototype.partition = 0;

            /**
             * Creates a new FirmwareInfo instance using the specified properties.
             * @function create
             * @memberof lebai.hardware.FirmwareInfo
             * @static
             * @param {lebai.hardware.FirmwareInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.hardware.FirmwareInfo} FirmwareInfo instance
             * @type {{
             *   (properties: lebai.hardware.FirmwareInfo.$Shape): lebai.hardware.FirmwareInfo & lebai.hardware.FirmwareInfo.$Shape;
             *   (properties?: lebai.hardware.FirmwareInfo.$Properties): lebai.hardware.FirmwareInfo;
             * }}
             */
            FirmwareInfo.create = function(properties) {
                return new FirmwareInfo(properties);
            };

            /**
             * Encodes the specified FirmwareInfo message. Does not implicitly {@link lebai.hardware.FirmwareInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.hardware.FirmwareInfo
             * @static
             * @param {lebai.hardware.FirmwareInfo.$Properties} message FirmwareInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FirmwareInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.sn != null && $Object.hasOwnProperty.call(message, "sn") && message.sn !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.sn);
                if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.version);
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition") && message.partition !== 0)
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.partition);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FirmwareInfo message, length delimited. Does not implicitly {@link lebai.hardware.FirmwareInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.hardware.FirmwareInfo
             * @static
             * @param {lebai.hardware.FirmwareInfo.$Properties} message FirmwareInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FirmwareInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a FirmwareInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.hardware.FirmwareInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.hardware.FirmwareInfo & lebai.hardware.FirmwareInfo.$Shape} FirmwareInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FirmwareInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.hardware.FirmwareInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.sn = value;
                            else
                                delete message.sn;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.version = value;
                            else
                                delete message.version;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.partition = value;
                            else
                                delete message.partition;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a FirmwareInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.hardware.FirmwareInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.hardware.FirmwareInfo & lebai.hardware.FirmwareInfo.$Shape} FirmwareInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FirmwareInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FirmwareInfo message.
             * @function verify
             * @memberof lebai.hardware.FirmwareInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FirmwareInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.sn != null && $Object.hasOwnProperty.call(message, "sn"))
                    if (!$util.isString(message.sn))
                        return "sn: string expected";
                if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                    if (!$util.isString(message.version))
                        return "version: string expected";
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition"))
                    if (typeof message.partition !== "number" || (message.partition | 0) !== message.partition)
                        return "partition: enum value expected";
                return null;
            };

            /**
             * Creates a FirmwareInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.hardware.FirmwareInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.hardware.FirmwareInfo} FirmwareInfo
             */
            FirmwareInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.hardware.FirmwareInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.hardware.FirmwareInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.hardware.FirmwareInfo();
                if (object.sn != null)
                    if (typeof object.sn !== "string" || object.sn.length)
                        message.sn = $String(object.sn);
                if (object.version != null)
                    if (typeof object.version !== "string" || object.version.length)
                        message.version = $String(object.version);
                if (object.partition !== 0 && (typeof object.partition !== "string" || $root.lebai.hardware.FirmwarePartition[object.partition] !== 0))
                    switch (object.partition) {
                    case "A":
                    case 0:
                        message.partition = 0;
                        break;
                    case "B":
                    case 1:
                        message.partition = 1;
                        break;
                    case "C":
                    case 2:
                        message.partition = 2;
                        break;
                    case "UNKNOWN":
                    case 3:
                        message.partition = 3;
                        break;
                    default:
                        if (typeof object.partition === "number" && (object.partition | 0) === object.partition)
                            message.partition = object.partition;
                    }
                return message;
            };

            /**
             * Creates a plain object from a FirmwareInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.hardware.FirmwareInfo
             * @static
             * @param {lebai.hardware.FirmwareInfo} message FirmwareInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FirmwareInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.sn = "";
                    object.version = "";
                    object.partition = options.enums === $String ? "A" : 0;
                }
                if (message.sn != null && $Object.hasOwnProperty.call(message, "sn"))
                    object.sn = message.sn;
                if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition"))
                    object.partition = options.enums === $String ? $root.lebai.hardware.FirmwarePartition[message.partition] === $undefined ? message.partition : $root.lebai.hardware.FirmwarePartition[message.partition] : message.partition;
                return object;
            };

            /**
             * Converts this FirmwareInfo to JSON.
             * @function toJSON
             * @memberof lebai.hardware.FirmwareInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FirmwareInfo.prototype.toJSON = function() {
                return FirmwareInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FirmwareInfo
             * @function getTypeUrl
             * @memberof lebai.hardware.FirmwareInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FirmwareInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.hardware.FirmwareInfo";
            };

            return FirmwareInfo;
        })();

        hardware.StartOtaRequest = (function() {

            /**
             * Properties of a StartOtaRequest.
             * @typedef {Object} lebai.hardware.StartOtaRequest.$Properties
             * @property {lebai.hardware.Address|null} [address] StartOtaRequest address
             * @property {lebai.hardware.FirmwarePartition|null} [partition] StartOtaRequest partition
             * @property {string|null} [file] StartOtaRequest file
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StartOtaRequest.
             * @memberof lebai.hardware
             * @interface IStartOtaRequest
             * @augments lebai.hardware.StartOtaRequest.$Properties
             * @deprecated Use lebai.hardware.StartOtaRequest.$Properties instead.
             */

            /**
             * Shape of a StartOtaRequest.
             * @typedef {lebai.hardware.StartOtaRequest.$Properties} lebai.hardware.StartOtaRequest.$Shape
             */

            /**
             * Constructs a new StartOtaRequest.
             * @memberof lebai.hardware
             * @classdesc Represents a StartOtaRequest.
             * @constructor
             * @param {lebai.hardware.StartOtaRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var StartOtaRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StartOtaRequest address.
             * @member {lebai.hardware.Address} address
             * @memberof lebai.hardware.StartOtaRequest
             * @instance
             */
            StartOtaRequest.prototype.address = 0;

            /**
             * StartOtaRequest partition.
             * @member {lebai.hardware.FirmwarePartition} partition
             * @memberof lebai.hardware.StartOtaRequest
             * @instance
             */
            StartOtaRequest.prototype.partition = 0;

            /**
             * StartOtaRequest file.
             * @member {string} file
             * @memberof lebai.hardware.StartOtaRequest
             * @instance
             */
            StartOtaRequest.prototype.file = "";

            /**
             * Creates a new StartOtaRequest instance using the specified properties.
             * @function create
             * @memberof lebai.hardware.StartOtaRequest
             * @static
             * @param {lebai.hardware.StartOtaRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.hardware.StartOtaRequest} StartOtaRequest instance
             * @type {{
             *   (properties: lebai.hardware.StartOtaRequest.$Shape): lebai.hardware.StartOtaRequest & lebai.hardware.StartOtaRequest.$Shape;
             *   (properties?: lebai.hardware.StartOtaRequest.$Properties): lebai.hardware.StartOtaRequest;
             * }}
             */
            StartOtaRequest.create = function(properties) {
                return new StartOtaRequest(properties);
            };

            /**
             * Encodes the specified StartOtaRequest message. Does not implicitly {@link lebai.hardware.StartOtaRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.hardware.StartOtaRequest
             * @static
             * @param {lebai.hardware.StartOtaRequest.$Properties} message StartOtaRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StartOtaRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.address);
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition") && message.partition !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.partition);
                if (message.file != null && $Object.hasOwnProperty.call(message, "file") && message.file !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.file);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StartOtaRequest message, length delimited. Does not implicitly {@link lebai.hardware.StartOtaRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.hardware.StartOtaRequest
             * @static
             * @param {lebai.hardware.StartOtaRequest.$Properties} message StartOtaRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StartOtaRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a StartOtaRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.hardware.StartOtaRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.hardware.StartOtaRequest & lebai.hardware.StartOtaRequest.$Shape} StartOtaRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StartOtaRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.hardware.StartOtaRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.address = value;
                            else
                                delete message.address;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.partition = value;
                            else
                                delete message.partition;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.file = value;
                            else
                                delete message.file;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a StartOtaRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.hardware.StartOtaRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.hardware.StartOtaRequest & lebai.hardware.StartOtaRequest.$Shape} StartOtaRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StartOtaRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StartOtaRequest message.
             * @function verify
             * @memberof lebai.hardware.StartOtaRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StartOtaRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    if (typeof message.address !== "number" || (message.address | 0) !== message.address)
                        return "address: enum value expected";
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition"))
                    if (typeof message.partition !== "number" || (message.partition | 0) !== message.partition)
                        return "partition: enum value expected";
                if (message.file != null && $Object.hasOwnProperty.call(message, "file"))
                    if (!$util.isString(message.file))
                        return "file: string expected";
                return null;
            };

            /**
             * Creates a StartOtaRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.hardware.StartOtaRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.hardware.StartOtaRequest} StartOtaRequest
             */
            StartOtaRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.hardware.StartOtaRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.hardware.StartOtaRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.hardware.StartOtaRequest();
                if (object.address !== 0 && (typeof object.address !== "string" || $root.lebai.hardware.Address[object.address] !== 0))
                    switch (object.address) {
                    case "EMPTY":
                    case 0:
                        message.address = 0;
                        break;
                    case "JOINT1":
                    case 1:
                        message.address = 1;
                        break;
                    case "JOINT2":
                    case 2:
                        message.address = 2;
                        break;
                    case "JOINT3":
                    case 3:
                        message.address = 3;
                        break;
                    case "JOINT4":
                    case 4:
                        message.address = 4;
                        break;
                    case "JOINT5":
                    case 5:
                        message.address = 5;
                        break;
                    case "JOINT6":
                    case 6:
                        message.address = 6;
                        break;
                    case "JOINT7":
                    case 7:
                        message.address = 7;
                        break;
                    case "FLANGE":
                    case 8:
                        message.address = 8;
                        break;
                    case "CLAW":
                    case 9:
                        message.address = 9;
                        break;
                    case "LED":
                    case 10:
                        message.address = 10;
                        break;
                    case "DIAL":
                    case 13:
                        message.address = 13;
                        break;
                    case "COMM_BOARD":
                    case 14:
                        message.address = 14;
                        break;
                    case "TEST_BOARD":
                    case 15:
                        message.address = 15;
                        break;
                    default:
                        if (typeof object.address === "number" && (object.address | 0) === object.address)
                            message.address = object.address;
                    }
                if (object.partition !== 0 && (typeof object.partition !== "string" || $root.lebai.hardware.FirmwarePartition[object.partition] !== 0))
                    switch (object.partition) {
                    case "A":
                    case 0:
                        message.partition = 0;
                        break;
                    case "B":
                    case 1:
                        message.partition = 1;
                        break;
                    case "C":
                    case 2:
                        message.partition = 2;
                        break;
                    case "UNKNOWN":
                    case 3:
                        message.partition = 3;
                        break;
                    default:
                        if (typeof object.partition === "number" && (object.partition | 0) === object.partition)
                            message.partition = object.partition;
                    }
                if (object.file != null)
                    if (typeof object.file !== "string" || object.file.length)
                        message.file = $String(object.file);
                return message;
            };

            /**
             * Creates a plain object from a StartOtaRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.hardware.StartOtaRequest
             * @static
             * @param {lebai.hardware.StartOtaRequest} message StartOtaRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StartOtaRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.address = options.enums === $String ? "EMPTY" : 0;
                    object.partition = options.enums === $String ? "A" : 0;
                    object.file = "";
                }
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    object.address = options.enums === $String ? $root.lebai.hardware.Address[message.address] === $undefined ? message.address : $root.lebai.hardware.Address[message.address] : message.address;
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition"))
                    object.partition = options.enums === $String ? $root.lebai.hardware.FirmwarePartition[message.partition] === $undefined ? message.partition : $root.lebai.hardware.FirmwarePartition[message.partition] : message.partition;
                if (message.file != null && $Object.hasOwnProperty.call(message, "file"))
                    object.file = message.file;
                return object;
            };

            /**
             * Converts this StartOtaRequest to JSON.
             * @function toJSON
             * @memberof lebai.hardware.StartOtaRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StartOtaRequest.prototype.toJSON = function() {
                return StartOtaRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StartOtaRequest
             * @function getTypeUrl
             * @memberof lebai.hardware.StartOtaRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StartOtaRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.hardware.StartOtaRequest";
            };

            return StartOtaRequest;
        })();

        hardware.SwitchPartitionRequest = (function() {

            /**
             * Properties of a SwitchPartitionRequest.
             * @typedef {Object} lebai.hardware.SwitchPartitionRequest.$Properties
             * @property {lebai.hardware.Address|null} [address] SwitchPartitionRequest address
             * @property {lebai.hardware.FirmwarePartition|null} [partition] SwitchPartitionRequest partition
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SwitchPartitionRequest.
             * @memberof lebai.hardware
             * @interface ISwitchPartitionRequest
             * @augments lebai.hardware.SwitchPartitionRequest.$Properties
             * @deprecated Use lebai.hardware.SwitchPartitionRequest.$Properties instead.
             */

            /**
             * Shape of a SwitchPartitionRequest.
             * @typedef {lebai.hardware.SwitchPartitionRequest.$Properties} lebai.hardware.SwitchPartitionRequest.$Shape
             */

            /**
             * Constructs a new SwitchPartitionRequest.
             * @memberof lebai.hardware
             * @classdesc Represents a SwitchPartitionRequest.
             * @constructor
             * @param {lebai.hardware.SwitchPartitionRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SwitchPartitionRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SwitchPartitionRequest address.
             * @member {lebai.hardware.Address} address
             * @memberof lebai.hardware.SwitchPartitionRequest
             * @instance
             */
            SwitchPartitionRequest.prototype.address = 0;

            /**
             * SwitchPartitionRequest partition.
             * @member {lebai.hardware.FirmwarePartition} partition
             * @memberof lebai.hardware.SwitchPartitionRequest
             * @instance
             */
            SwitchPartitionRequest.prototype.partition = 0;

            /**
             * Creates a new SwitchPartitionRequest instance using the specified properties.
             * @function create
             * @memberof lebai.hardware.SwitchPartitionRequest
             * @static
             * @param {lebai.hardware.SwitchPartitionRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.hardware.SwitchPartitionRequest} SwitchPartitionRequest instance
             * @type {{
             *   (properties: lebai.hardware.SwitchPartitionRequest.$Shape): lebai.hardware.SwitchPartitionRequest & lebai.hardware.SwitchPartitionRequest.$Shape;
             *   (properties?: lebai.hardware.SwitchPartitionRequest.$Properties): lebai.hardware.SwitchPartitionRequest;
             * }}
             */
            SwitchPartitionRequest.create = function(properties) {
                return new SwitchPartitionRequest(properties);
            };

            /**
             * Encodes the specified SwitchPartitionRequest message. Does not implicitly {@link lebai.hardware.SwitchPartitionRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.hardware.SwitchPartitionRequest
             * @static
             * @param {lebai.hardware.SwitchPartitionRequest.$Properties} message SwitchPartitionRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SwitchPartitionRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.address);
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition") && message.partition !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.partition);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SwitchPartitionRequest message, length delimited. Does not implicitly {@link lebai.hardware.SwitchPartitionRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.hardware.SwitchPartitionRequest
             * @static
             * @param {lebai.hardware.SwitchPartitionRequest.$Properties} message SwitchPartitionRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SwitchPartitionRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SwitchPartitionRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.hardware.SwitchPartitionRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.hardware.SwitchPartitionRequest & lebai.hardware.SwitchPartitionRequest.$Shape} SwitchPartitionRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SwitchPartitionRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.hardware.SwitchPartitionRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.address = value;
                            else
                                delete message.address;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.partition = value;
                            else
                                delete message.partition;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SwitchPartitionRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.hardware.SwitchPartitionRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.hardware.SwitchPartitionRequest & lebai.hardware.SwitchPartitionRequest.$Shape} SwitchPartitionRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SwitchPartitionRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SwitchPartitionRequest message.
             * @function verify
             * @memberof lebai.hardware.SwitchPartitionRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SwitchPartitionRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    if (typeof message.address !== "number" || (message.address | 0) !== message.address)
                        return "address: enum value expected";
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition"))
                    if (typeof message.partition !== "number" || (message.partition | 0) !== message.partition)
                        return "partition: enum value expected";
                return null;
            };

            /**
             * Creates a SwitchPartitionRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.hardware.SwitchPartitionRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.hardware.SwitchPartitionRequest} SwitchPartitionRequest
             */
            SwitchPartitionRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.hardware.SwitchPartitionRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.hardware.SwitchPartitionRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.hardware.SwitchPartitionRequest();
                if (object.address !== 0 && (typeof object.address !== "string" || $root.lebai.hardware.Address[object.address] !== 0))
                    switch (object.address) {
                    case "EMPTY":
                    case 0:
                        message.address = 0;
                        break;
                    case "JOINT1":
                    case 1:
                        message.address = 1;
                        break;
                    case "JOINT2":
                    case 2:
                        message.address = 2;
                        break;
                    case "JOINT3":
                    case 3:
                        message.address = 3;
                        break;
                    case "JOINT4":
                    case 4:
                        message.address = 4;
                        break;
                    case "JOINT5":
                    case 5:
                        message.address = 5;
                        break;
                    case "JOINT6":
                    case 6:
                        message.address = 6;
                        break;
                    case "JOINT7":
                    case 7:
                        message.address = 7;
                        break;
                    case "FLANGE":
                    case 8:
                        message.address = 8;
                        break;
                    case "CLAW":
                    case 9:
                        message.address = 9;
                        break;
                    case "LED":
                    case 10:
                        message.address = 10;
                        break;
                    case "DIAL":
                    case 13:
                        message.address = 13;
                        break;
                    case "COMM_BOARD":
                    case 14:
                        message.address = 14;
                        break;
                    case "TEST_BOARD":
                    case 15:
                        message.address = 15;
                        break;
                    default:
                        if (typeof object.address === "number" && (object.address | 0) === object.address)
                            message.address = object.address;
                    }
                if (object.partition !== 0 && (typeof object.partition !== "string" || $root.lebai.hardware.FirmwarePartition[object.partition] !== 0))
                    switch (object.partition) {
                    case "A":
                    case 0:
                        message.partition = 0;
                        break;
                    case "B":
                    case 1:
                        message.partition = 1;
                        break;
                    case "C":
                    case 2:
                        message.partition = 2;
                        break;
                    case "UNKNOWN":
                    case 3:
                        message.partition = 3;
                        break;
                    default:
                        if (typeof object.partition === "number" && (object.partition | 0) === object.partition)
                            message.partition = object.partition;
                    }
                return message;
            };

            /**
             * Creates a plain object from a SwitchPartitionRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.hardware.SwitchPartitionRequest
             * @static
             * @param {lebai.hardware.SwitchPartitionRequest} message SwitchPartitionRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SwitchPartitionRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.address = options.enums === $String ? "EMPTY" : 0;
                    object.partition = options.enums === $String ? "A" : 0;
                }
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    object.address = options.enums === $String ? $root.lebai.hardware.Address[message.address] === $undefined ? message.address : $root.lebai.hardware.Address[message.address] : message.address;
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition"))
                    object.partition = options.enums === $String ? $root.lebai.hardware.FirmwarePartition[message.partition] === $undefined ? message.partition : $root.lebai.hardware.FirmwarePartition[message.partition] : message.partition;
                return object;
            };

            /**
             * Converts this SwitchPartitionRequest to JSON.
             * @function toJSON
             * @memberof lebai.hardware.SwitchPartitionRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SwitchPartitionRequest.prototype.toJSON = function() {
                return SwitchPartitionRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SwitchPartitionRequest
             * @function getTypeUrl
             * @memberof lebai.hardware.SwitchPartitionRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SwitchPartitionRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.hardware.SwitchPartitionRequest";
            };

            return SwitchPartitionRequest;
        })();

        /**
         * Step enum.
         * @name lebai.hardware.Step
         * @enum {number}
         * @property {number} NONE=0 NONE value
         * @property {number} WAIT=1 WAIT value
         * @property {number} DOWNLOADING=2 DOWNLOADING value
         * @property {number} FLASHING=3 FLASHING value
         * @property {number} DONE=4 DONE value
         * @property {number} FAIL=5 FAIL value
         * @property {number} RESTART=11 RESTART value
         */
        hardware.Step = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "WAIT"] = 1;
            values[valuesById[2] = "DOWNLOADING"] = 2;
            values[valuesById[3] = "FLASHING"] = 3;
            values[valuesById[4] = "DONE"] = 4;
            values[valuesById[5] = "FAIL"] = 5;
            values[valuesById[11] = "RESTART"] = 11;
            return values;
        })();

        hardware.OtaState = (function() {

            /**
             * Properties of an OtaState.
             * @typedef {Object} lebai.hardware.OtaState.$Properties
             * @property {lebai.hardware.Address|null} [address] OtaState address
             * @property {lebai.hardware.FirmwarePartition|null} [partition] OtaState partition
             * @property {lebai.hardware.Step|null} [step] OtaState step
             * @property {number|null} [progress] OtaState progress
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an OtaState.
             * @memberof lebai.hardware
             * @interface IOtaState
             * @augments lebai.hardware.OtaState.$Properties
             * @deprecated Use lebai.hardware.OtaState.$Properties instead.
             */

            /**
             * Shape of an OtaState.
             * @typedef {lebai.hardware.OtaState.$Properties} lebai.hardware.OtaState.$Shape
             */

            /**
             * Constructs a new OtaState.
             * @memberof lebai.hardware
             * @classdesc Represents an OtaState.
             * @constructor
             * @param {lebai.hardware.OtaState.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var OtaState = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * OtaState address.
             * @member {lebai.hardware.Address} address
             * @memberof lebai.hardware.OtaState
             * @instance
             */
            OtaState.prototype.address = 0;

            /**
             * OtaState partition.
             * @member {lebai.hardware.FirmwarePartition} partition
             * @memberof lebai.hardware.OtaState
             * @instance
             */
            OtaState.prototype.partition = 0;

            /**
             * OtaState step.
             * @member {lebai.hardware.Step} step
             * @memberof lebai.hardware.OtaState
             * @instance
             */
            OtaState.prototype.step = 0;

            /**
             * OtaState progress.
             * @member {number} progress
             * @memberof lebai.hardware.OtaState
             * @instance
             */
            OtaState.prototype.progress = 0;

            /**
             * Creates a new OtaState instance using the specified properties.
             * @function create
             * @memberof lebai.hardware.OtaState
             * @static
             * @param {lebai.hardware.OtaState.$Properties=} [properties] Properties to set
             * @returns {lebai.hardware.OtaState} OtaState instance
             * @type {{
             *   (properties: lebai.hardware.OtaState.$Shape): lebai.hardware.OtaState & lebai.hardware.OtaState.$Shape;
             *   (properties?: lebai.hardware.OtaState.$Properties): lebai.hardware.OtaState;
             * }}
             */
            OtaState.create = function(properties) {
                return new OtaState(properties);
            };

            /**
             * Encodes the specified OtaState message. Does not implicitly {@link lebai.hardware.OtaState.verify|verify} messages.
             * @function encode
             * @memberof lebai.hardware.OtaState
             * @static
             * @param {lebai.hardware.OtaState.$Properties} message OtaState message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OtaState.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.address);
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition") && message.partition !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.partition);
                if (message.step != null && $Object.hasOwnProperty.call(message, "step") && message.step !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).int32(message.step);
                if (message.progress != null && $Object.hasOwnProperty.call(message, "progress") && message.progress !== 0)
                    writer.uint32(/* id 12, wireType 0 =*/96).uint32(message.progress);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified OtaState message, length delimited. Does not implicitly {@link lebai.hardware.OtaState.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.hardware.OtaState
             * @static
             * @param {lebai.hardware.OtaState.$Properties} message OtaState message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OtaState.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an OtaState message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.hardware.OtaState
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.hardware.OtaState & lebai.hardware.OtaState.$Shape} OtaState
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OtaState.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.hardware.OtaState();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.address = value;
                            else
                                delete message.address;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.partition = value;
                            else
                                delete message.partition;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.step = value;
                            else
                                delete message.step;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.progress = value;
                            else
                                delete message.progress;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an OtaState message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.hardware.OtaState
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.hardware.OtaState & lebai.hardware.OtaState.$Shape} OtaState
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OtaState.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an OtaState message.
             * @function verify
             * @memberof lebai.hardware.OtaState
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            OtaState.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    if (typeof message.address !== "number" || (message.address | 0) !== message.address)
                        return "address: enum value expected";
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition"))
                    if (typeof message.partition !== "number" || (message.partition | 0) !== message.partition)
                        return "partition: enum value expected";
                if (message.step != null && $Object.hasOwnProperty.call(message, "step"))
                    if (typeof message.step !== "number" || (message.step | 0) !== message.step)
                        return "step: enum value expected";
                if (message.progress != null && $Object.hasOwnProperty.call(message, "progress"))
                    if (!$util.isInteger(message.progress))
                        return "progress: integer expected";
                return null;
            };

            /**
             * Creates an OtaState message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.hardware.OtaState
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.hardware.OtaState} OtaState
             */
            OtaState.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.hardware.OtaState)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.hardware.OtaState: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.hardware.OtaState();
                if (object.address !== 0 && (typeof object.address !== "string" || $root.lebai.hardware.Address[object.address] !== 0))
                    switch (object.address) {
                    case "EMPTY":
                    case 0:
                        message.address = 0;
                        break;
                    case "JOINT1":
                    case 1:
                        message.address = 1;
                        break;
                    case "JOINT2":
                    case 2:
                        message.address = 2;
                        break;
                    case "JOINT3":
                    case 3:
                        message.address = 3;
                        break;
                    case "JOINT4":
                    case 4:
                        message.address = 4;
                        break;
                    case "JOINT5":
                    case 5:
                        message.address = 5;
                        break;
                    case "JOINT6":
                    case 6:
                        message.address = 6;
                        break;
                    case "JOINT7":
                    case 7:
                        message.address = 7;
                        break;
                    case "FLANGE":
                    case 8:
                        message.address = 8;
                        break;
                    case "CLAW":
                    case 9:
                        message.address = 9;
                        break;
                    case "LED":
                    case 10:
                        message.address = 10;
                        break;
                    case "DIAL":
                    case 13:
                        message.address = 13;
                        break;
                    case "COMM_BOARD":
                    case 14:
                        message.address = 14;
                        break;
                    case "TEST_BOARD":
                    case 15:
                        message.address = 15;
                        break;
                    default:
                        if (typeof object.address === "number" && (object.address | 0) === object.address)
                            message.address = object.address;
                    }
                if (object.partition !== 0 && (typeof object.partition !== "string" || $root.lebai.hardware.FirmwarePartition[object.partition] !== 0))
                    switch (object.partition) {
                    case "A":
                    case 0:
                        message.partition = 0;
                        break;
                    case "B":
                    case 1:
                        message.partition = 1;
                        break;
                    case "C":
                    case 2:
                        message.partition = 2;
                        break;
                    case "UNKNOWN":
                    case 3:
                        message.partition = 3;
                        break;
                    default:
                        if (typeof object.partition === "number" && (object.partition | 0) === object.partition)
                            message.partition = object.partition;
                    }
                if (object.step !== 0 && (typeof object.step !== "string" || $root.lebai.hardware.Step[object.step] !== 0))
                    switch (object.step) {
                    case "NONE":
                    case 0:
                        message.step = 0;
                        break;
                    case "WAIT":
                    case 1:
                        message.step = 1;
                        break;
                    case "DOWNLOADING":
                    case 2:
                        message.step = 2;
                        break;
                    case "FLASHING":
                    case 3:
                        message.step = 3;
                        break;
                    case "DONE":
                    case 4:
                        message.step = 4;
                        break;
                    case "FAIL":
                    case 5:
                        message.step = 5;
                        break;
                    case "RESTART":
                    case 11:
                        message.step = 11;
                        break;
                    default:
                        if (typeof object.step === "number" && (object.step | 0) === object.step)
                            message.step = object.step;
                    }
                if (object.progress != null)
                    if ($Number(object.progress) !== 0)
                        message.progress = object.progress >>> 0;
                return message;
            };

            /**
             * Creates a plain object from an OtaState message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.hardware.OtaState
             * @static
             * @param {lebai.hardware.OtaState} message OtaState
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            OtaState.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.address = options.enums === $String ? "EMPTY" : 0;
                    object.partition = options.enums === $String ? "A" : 0;
                    object.step = options.enums === $String ? "NONE" : 0;
                    object.progress = 0;
                }
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    object.address = options.enums === $String ? $root.lebai.hardware.Address[message.address] === $undefined ? message.address : $root.lebai.hardware.Address[message.address] : message.address;
                if (message.partition != null && $Object.hasOwnProperty.call(message, "partition"))
                    object.partition = options.enums === $String ? $root.lebai.hardware.FirmwarePartition[message.partition] === $undefined ? message.partition : $root.lebai.hardware.FirmwarePartition[message.partition] : message.partition;
                if (message.step != null && $Object.hasOwnProperty.call(message, "step"))
                    object.step = options.enums === $String ? $root.lebai.hardware.Step[message.step] === $undefined ? message.step : $root.lebai.hardware.Step[message.step] : message.step;
                if (message.progress != null && $Object.hasOwnProperty.call(message, "progress"))
                    object.progress = message.progress;
                return object;
            };

            /**
             * Converts this OtaState to JSON.
             * @function toJSON
             * @memberof lebai.hardware.OtaState
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            OtaState.prototype.toJSON = function() {
                return OtaState.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for OtaState
             * @function getTypeUrl
             * @memberof lebai.hardware.OtaState
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            OtaState.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.hardware.OtaState";
            };

            return OtaState;
        })();

        hardware.HardwareService = (function() {

            /**
             * Constructs a new HardwareService service.
             * @memberof lebai.hardware
             * @classdesc Represents a HardwareService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var HardwareService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(HardwareService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: HardwareService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new HardwareService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.hardware.HardwareService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {HardwareService} RPC service. Useful where requests and/or responses are streamed.
             */
            HardwareService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.hardware.HardwareService#startOta}.
             * @memberof lebai.hardware.HardwareService
             * @typedef StartOtaCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls StartOta.
             * @memberof lebai.hardware.HardwareService
             * @typedef StartOta
             * @type {{
             *   (request: lebai.hardware.IStartOtaRequest, callback: lebai.hardware.HardwareService.StartOtaCallback): void;
             *   (request: lebai.hardware.IStartOtaRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "StartOta";
             *   readonly path: "/lebai.hardware.HardwareService/StartOta";
             *   readonly requestType: "StartOtaRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls StartOta.
             * @name lebai.hardware.HardwareService#startOta
             * @type {lebai.hardware.HardwareService.StartOta}
             */
            $Object.defineProperties(HardwareService.prototype.startOta = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, HardwareService.prototype.startOta, $root.lebai.hardware.StartOtaRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "StartOta" },
                path: { value: "/lebai.hardware.HardwareService/StartOta" },
                requestType: { value: "StartOtaRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.hardware.HardwareService#switchPartition}.
             * @memberof lebai.hardware.HardwareService
             * @typedef SwitchPartitionCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SwitchPartition.
             * @memberof lebai.hardware.HardwareService
             * @typedef SwitchPartition
             * @type {{
             *   (request: lebai.hardware.ISwitchPartitionRequest, callback: lebai.hardware.HardwareService.SwitchPartitionCallback): void;
             *   (request: lebai.hardware.ISwitchPartitionRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SwitchPartition";
             *   readonly path: "/lebai.hardware.HardwareService/SwitchPartition";
             *   readonly requestType: "SwitchPartitionRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SwitchPartition.
             * @name lebai.hardware.HardwareService#switchPartition
             * @type {lebai.hardware.HardwareService.SwitchPartition}
             */
            $Object.defineProperties(HardwareService.prototype.switchPartition = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, HardwareService.prototype.switchPartition, $root.lebai.hardware.SwitchPartitionRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SwitchPartition" },
                path: { value: "/lebai.hardware.HardwareService/SwitchPartition" },
                requestType: { value: "SwitchPartitionRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.hardware.HardwareService#getOtaState}.
             * @memberof lebai.hardware.HardwareService
             * @typedef GetOtaStateCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.hardware.OtaState} [response] OtaState
             */

            /**
             * Calls GetOtaState.
             * @memberof lebai.hardware.HardwareService
             * @typedef GetOtaState
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.hardware.HardwareService.GetOtaStateCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.hardware.OtaState>;
             *   readonly name: "GetOtaState";
             *   readonly path: "/lebai.hardware.HardwareService/GetOtaState";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "OtaState";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetOtaState.
             * @name lebai.hardware.HardwareService#getOtaState
             * @type {lebai.hardware.HardwareService.GetOtaState}
             */
            $Object.defineProperties(HardwareService.prototype.getOtaState = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, HardwareService.prototype.getOtaState, $root.google.protobuf.Empty, $root.lebai.hardware.OtaState, request, callback);
            }, {
                name: { value: "GetOtaState" },
                path: { value: "/lebai.hardware.HardwareService/GetOtaState" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "OtaState" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return HardwareService;
        })();

        return hardware;
    })();

    lebai.claw = (function() {

        /**
         * Namespace claw.
         * @memberof lebai
         * @namespace
         */
        var claw = {};

        /**
         * Address enum.
         * @name lebai.claw.Address
         * @enum {number}
         * @property {number} AMPLITUDE=0 AMPLITUDE value
         * @property {number} FORCE=1 FORCE value
         * @property {number} VOLTAGE=2 VOLTAGE value
         * @property {number} DISTANCE=3 DISTANCE value
         * @property {number} DO=4 DO value
         * @property {number} ACTUAL_AMPLITUDE=5 ACTUAL_AMPLITUDE value
         * @property {number} ACTUAL_FORCE=6 ACTUAL_FORCE value
         * @property {number} WEIGHT=7 WEIGHT value
         * @property {number} INIT=8 INIT value
         * @property {number} FLAG=9 FLAG value
         * @property {number} ERROR=10 ERROR value
         * @property {number} DISABLE_INIT=90 DISABLE_INIT value
         * @property {number} SAVE_TRAVEL=91 SAVE_TRAVEL value
         */
        claw.Address = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "AMPLITUDE"] = 0;
            values[valuesById[1] = "FORCE"] = 1;
            values[valuesById[2] = "VOLTAGE"] = 2;
            values[valuesById[3] = "DISTANCE"] = 3;
            values[valuesById[4] = "DO"] = 4;
            values[valuesById[5] = "ACTUAL_AMPLITUDE"] = 5;
            values[valuesById[6] = "ACTUAL_FORCE"] = 6;
            values[valuesById[7] = "WEIGHT"] = 7;
            values[valuesById[8] = "INIT"] = 8;
            values[valuesById[9] = "FLAG"] = 9;
            values[valuesById[10] = "ERROR"] = 10;
            values[valuesById[90] = "DISABLE_INIT"] = 90;
            values[valuesById[91] = "SAVE_TRAVEL"] = 91;
            return values;
        })();

        claw.Claw = (function() {

            /**
             * Properties of a Claw.
             * @typedef {Object} lebai.claw.Claw.$Properties
             * @property {number|null} [force] Claw force
             * @property {number|null} [amplitude] Claw amplitude
             * @property {number|null} [weight] Claw weight
             * @property {boolean|null} [holdOn] Claw holdOn
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Claw.
             * @memberof lebai.claw
             * @interface IClaw
             * @augments lebai.claw.Claw.$Properties
             * @deprecated Use lebai.claw.Claw.$Properties instead.
             */

            /**
             * Shape of a Claw.
             * @typedef {lebai.claw.Claw.$Properties} lebai.claw.Claw.$Shape
             */

            /**
             * Constructs a new Claw.
             * @memberof lebai.claw
             * @classdesc Represents a Claw.
             * @constructor
             * @param {lebai.claw.Claw.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Claw = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Claw force.
             * @member {number} force
             * @memberof lebai.claw.Claw
             * @instance
             */
            Claw.prototype.force = 0;

            /**
             * Claw amplitude.
             * @member {number} amplitude
             * @memberof lebai.claw.Claw
             * @instance
             */
            Claw.prototype.amplitude = 0;

            /**
             * Claw weight.
             * @member {number} weight
             * @memberof lebai.claw.Claw
             * @instance
             */
            Claw.prototype.weight = 0;

            /**
             * Claw holdOn.
             * @member {boolean} holdOn
             * @memberof lebai.claw.Claw
             * @instance
             */
            Claw.prototype.holdOn = false;

            /**
             * Creates a new Claw instance using the specified properties.
             * @function create
             * @memberof lebai.claw.Claw
             * @static
             * @param {lebai.claw.Claw.$Properties=} [properties] Properties to set
             * @returns {lebai.claw.Claw} Claw instance
             * @type {{
             *   (properties: lebai.claw.Claw.$Shape): lebai.claw.Claw & lebai.claw.Claw.$Shape;
             *   (properties?: lebai.claw.Claw.$Properties): lebai.claw.Claw;
             * }}
             */
            Claw.create = function(properties) {
                return new Claw(properties);
            };

            /**
             * Encodes the specified Claw message. Does not implicitly {@link lebai.claw.Claw.verify|verify} messages.
             * @function encode
             * @memberof lebai.claw.Claw
             * @static
             * @param {lebai.claw.Claw.$Properties} message Claw message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Claw.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.force != null && $Object.hasOwnProperty.call(message, "force") && !$Object.is(message.force, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.force);
                if (message.amplitude != null && $Object.hasOwnProperty.call(message, "amplitude") && !$Object.is(message.amplitude, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.amplitude);
                if (message.weight != null && $Object.hasOwnProperty.call(message, "weight") && !$Object.is(message.weight, 0))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.weight);
                if (message.holdOn != null && $Object.hasOwnProperty.call(message, "holdOn") && message.holdOn !== false)
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.holdOn);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Claw message, length delimited. Does not implicitly {@link lebai.claw.Claw.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.claw.Claw
             * @static
             * @param {lebai.claw.Claw.$Properties} message Claw message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Claw.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Claw message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.claw.Claw
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.claw.Claw & lebai.claw.Claw.$Shape} Claw
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Claw.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.claw.Claw();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.force = value;
                            else
                                delete message.force;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.amplitude = value;
                            else
                                delete message.amplitude;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.weight = value;
                            else
                                delete message.weight;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.holdOn = value;
                            else
                                delete message.holdOn;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Claw message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.claw.Claw
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.claw.Claw & lebai.claw.Claw.$Shape} Claw
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Claw.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Claw message.
             * @function verify
             * @memberof lebai.claw.Claw
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Claw.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                    if (typeof message.force !== "number")
                        return "force: number expected";
                if (message.amplitude != null && $Object.hasOwnProperty.call(message, "amplitude"))
                    if (typeof message.amplitude !== "number")
                        return "amplitude: number expected";
                if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                    if (typeof message.weight !== "number")
                        return "weight: number expected";
                if (message.holdOn != null && $Object.hasOwnProperty.call(message, "holdOn"))
                    if (typeof message.holdOn !== "boolean")
                        return "holdOn: boolean expected";
                return null;
            };

            /**
             * Creates a Claw message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.claw.Claw
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.claw.Claw} Claw
             */
            Claw.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.claw.Claw)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.claw.Claw: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.claw.Claw();
                if (object.force != null)
                    if (!$Object.is($Number(object.force), 0))
                        message.force = $Number(object.force);
                if (object.amplitude != null)
                    if (!$Object.is($Number(object.amplitude), 0))
                        message.amplitude = $Number(object.amplitude);
                if (object.weight != null)
                    if (!$Object.is($Number(object.weight), 0))
                        message.weight = $Number(object.weight);
                if (object.holdOn != null)
                    if (object.holdOn)
                        message.holdOn = $Boolean(object.holdOn);
                return message;
            };

            /**
             * Creates a plain object from a Claw message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.claw.Claw
             * @static
             * @param {lebai.claw.Claw} message Claw
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Claw.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.force = 0;
                    object.amplitude = 0;
                    object.weight = 0;
                    object.holdOn = false;
                }
                if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                    object.force = options.json && !$isFinite(message.force) ? $String(message.force) : message.force;
                if (message.amplitude != null && $Object.hasOwnProperty.call(message, "amplitude"))
                    object.amplitude = options.json && !$isFinite(message.amplitude) ? $String(message.amplitude) : message.amplitude;
                if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
                    object.weight = options.json && !$isFinite(message.weight) ? $String(message.weight) : message.weight;
                if (message.holdOn != null && $Object.hasOwnProperty.call(message, "holdOn"))
                    object.holdOn = message.holdOn;
                return object;
            };

            /**
             * Converts this Claw to JSON.
             * @function toJSON
             * @memberof lebai.claw.Claw
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Claw.prototype.toJSON = function() {
                return Claw.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Claw
             * @function getTypeUrl
             * @memberof lebai.claw.Claw
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Claw.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.claw.Claw";
            };

            return Claw;
        })();

        claw.InitClawRequest = (function() {

            /**
             * Properties of an InitClawRequest.
             * @typedef {Object} lebai.claw.InitClawRequest.$Properties
             * @property {boolean|null} [force] InitClawRequest force
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an InitClawRequest.
             * @memberof lebai.claw
             * @interface IInitClawRequest
             * @augments lebai.claw.InitClawRequest.$Properties
             * @deprecated Use lebai.claw.InitClawRequest.$Properties instead.
             */

            /**
             * Shape of an InitClawRequest.
             * @typedef {lebai.claw.InitClawRequest.$Properties} lebai.claw.InitClawRequest.$Shape
             */

            /**
             * Constructs a new InitClawRequest.
             * @memberof lebai.claw
             * @classdesc Represents an InitClawRequest.
             * @constructor
             * @param {lebai.claw.InitClawRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var InitClawRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * InitClawRequest force.
             * @member {boolean} force
             * @memberof lebai.claw.InitClawRequest
             * @instance
             */
            InitClawRequest.prototype.force = false;

            /**
             * Creates a new InitClawRequest instance using the specified properties.
             * @function create
             * @memberof lebai.claw.InitClawRequest
             * @static
             * @param {lebai.claw.InitClawRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.claw.InitClawRequest} InitClawRequest instance
             * @type {{
             *   (properties: lebai.claw.InitClawRequest.$Shape): lebai.claw.InitClawRequest & lebai.claw.InitClawRequest.$Shape;
             *   (properties?: lebai.claw.InitClawRequest.$Properties): lebai.claw.InitClawRequest;
             * }}
             */
            InitClawRequest.create = function(properties) {
                return new InitClawRequest(properties);
            };

            /**
             * Encodes the specified InitClawRequest message. Does not implicitly {@link lebai.claw.InitClawRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.claw.InitClawRequest
             * @static
             * @param {lebai.claw.InitClawRequest.$Properties} message InitClawRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InitClawRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.force != null && $Object.hasOwnProperty.call(message, "force") && message.force !== false)
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.force);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified InitClawRequest message, length delimited. Does not implicitly {@link lebai.claw.InitClawRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.claw.InitClawRequest
             * @static
             * @param {lebai.claw.InitClawRequest.$Properties} message InitClawRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InitClawRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an InitClawRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.claw.InitClawRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.claw.InitClawRequest & lebai.claw.InitClawRequest.$Shape} InitClawRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InitClawRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.claw.InitClawRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.force = value;
                            else
                                delete message.force;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an InitClawRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.claw.InitClawRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.claw.InitClawRequest & lebai.claw.InitClawRequest.$Shape} InitClawRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InitClawRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an InitClawRequest message.
             * @function verify
             * @memberof lebai.claw.InitClawRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            InitClawRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                    if (typeof message.force !== "boolean")
                        return "force: boolean expected";
                return null;
            };

            /**
             * Creates an InitClawRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.claw.InitClawRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.claw.InitClawRequest} InitClawRequest
             */
            InitClawRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.claw.InitClawRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.claw.InitClawRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.claw.InitClawRequest();
                if (object.force != null)
                    if (object.force)
                        message.force = $Boolean(object.force);
                return message;
            };

            /**
             * Creates a plain object from an InitClawRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.claw.InitClawRequest
             * @static
             * @param {lebai.claw.InitClawRequest} message InitClawRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            InitClawRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.force = false;
                if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                    object.force = message.force;
                return object;
            };

            /**
             * Converts this InitClawRequest to JSON.
             * @function toJSON
             * @memberof lebai.claw.InitClawRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            InitClawRequest.prototype.toJSON = function() {
                return InitClawRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for InitClawRequest
             * @function getTypeUrl
             * @memberof lebai.claw.InitClawRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            InitClawRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.claw.InitClawRequest";
            };

            return InitClawRequest;
        })();

        claw.SetClawRequest = (function() {

            /**
             * Properties of a SetClawRequest.
             * @typedef {Object} lebai.claw.SetClawRequest.$Properties
             * @property {number|null} [force] SetClawRequest force
             * @property {number|null} [amplitude] SetClawRequest amplitude
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetClawRequest.
             * @memberof lebai.claw
             * @interface ISetClawRequest
             * @augments lebai.claw.SetClawRequest.$Properties
             * @deprecated Use lebai.claw.SetClawRequest.$Properties instead.
             */

            /**
             * Shape of a SetClawRequest.
             * @typedef {lebai.claw.SetClawRequest.$Properties} lebai.claw.SetClawRequest.$Shape
             */

            /**
             * Constructs a new SetClawRequest.
             * @memberof lebai.claw
             * @classdesc Represents a SetClawRequest.
             * @constructor
             * @param {lebai.claw.SetClawRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetClawRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetClawRequest force.
             * @member {number|null|undefined} force
             * @memberof lebai.claw.SetClawRequest
             * @instance
             */
            SetClawRequest.prototype.force = null;

            /**
             * SetClawRequest amplitude.
             * @member {number|null|undefined} amplitude
             * @memberof lebai.claw.SetClawRequest
             * @instance
             */
            SetClawRequest.prototype.amplitude = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SetClawRequest.prototype, "_force", {
                get: $util.oneOfGetter($oneOfFields = ["force"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SetClawRequest.prototype, "_amplitude", {
                get: $util.oneOfGetter($oneOfFields = ["amplitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SetClawRequest instance using the specified properties.
             * @function create
             * @memberof lebai.claw.SetClawRequest
             * @static
             * @param {lebai.claw.SetClawRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.claw.SetClawRequest} SetClawRequest instance
             * @type {{
             *   (properties: lebai.claw.SetClawRequest.$Shape): lebai.claw.SetClawRequest & lebai.claw.SetClawRequest.$Shape;
             *   (properties?: lebai.claw.SetClawRequest.$Properties): lebai.claw.SetClawRequest;
             * }}
             */
            SetClawRequest.create = function(properties) {
                return new SetClawRequest(properties);
            };

            /**
             * Encodes the specified SetClawRequest message. Does not implicitly {@link lebai.claw.SetClawRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.claw.SetClawRequest
             * @static
             * @param {lebai.claw.SetClawRequest.$Properties} message SetClawRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetClawRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.force);
                if (message.amplitude != null && $Object.hasOwnProperty.call(message, "amplitude"))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.amplitude);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetClawRequest message, length delimited. Does not implicitly {@link lebai.claw.SetClawRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.claw.SetClawRequest
             * @static
             * @param {lebai.claw.SetClawRequest.$Properties} message SetClawRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetClawRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetClawRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.claw.SetClawRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.claw.SetClawRequest & lebai.claw.SetClawRequest.$Shape} SetClawRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetClawRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.claw.SetClawRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            message.force = reader.double();
                            message._force = "force";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            message.amplitude = reader.double();
                            message._amplitude = "amplitude";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetClawRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.claw.SetClawRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.claw.SetClawRequest & lebai.claw.SetClawRequest.$Shape} SetClawRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetClawRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetClawRequest message.
             * @function verify
             * @memberof lebai.claw.SetClawRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetClawRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.force != null && $Object.hasOwnProperty.call(message, "force")) {
                    properties._force = 1;
                    if (typeof message.force !== "number")
                        return "force: number expected";
                }
                if (message.amplitude != null && $Object.hasOwnProperty.call(message, "amplitude")) {
                    properties._amplitude = 1;
                    if (typeof message.amplitude !== "number")
                        return "amplitude: number expected";
                }
                return null;
            };

            /**
             * Creates a SetClawRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.claw.SetClawRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.claw.SetClawRequest} SetClawRequest
             */
            SetClawRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.claw.SetClawRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.claw.SetClawRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.claw.SetClawRequest();
                if (object.force != null)
                    message.force = $Number(object.force);
                if (object.amplitude != null)
                    message.amplitude = $Number(object.amplitude);
                return message;
            };

            /**
             * Creates a plain object from a SetClawRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.claw.SetClawRequest
             * @static
             * @param {lebai.claw.SetClawRequest} message SetClawRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetClawRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                    object.force = options.json && !$isFinite(message.force) ? $String(message.force) : message.force;
                if (message.amplitude != null && $Object.hasOwnProperty.call(message, "amplitude"))
                    object.amplitude = options.json && !$isFinite(message.amplitude) ? $String(message.amplitude) : message.amplitude;
                return object;
            };

            /**
             * Converts this SetClawRequest to JSON.
             * @function toJSON
             * @memberof lebai.claw.SetClawRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetClawRequest.prototype.toJSON = function() {
                return SetClawRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetClawRequest
             * @function getTypeUrl
             * @memberof lebai.claw.SetClawRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetClawRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.claw.SetClawRequest";
            };

            return SetClawRequest;
        })();

        claw.SetClawAoRequest = (function() {

            /**
             * Properties of a SetClawAoRequest.
             * @typedef {Object} lebai.claw.SetClawAoRequest.$Properties
             * @property {lebai.claw.Address|null} [address] SetClawAoRequest address
             * @property {number|null} [value] SetClawAoRequest value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetClawAoRequest.
             * @memberof lebai.claw
             * @interface ISetClawAoRequest
             * @augments lebai.claw.SetClawAoRequest.$Properties
             * @deprecated Use lebai.claw.SetClawAoRequest.$Properties instead.
             */

            /**
             * Shape of a SetClawAoRequest.
             * @typedef {lebai.claw.SetClawAoRequest.$Properties} lebai.claw.SetClawAoRequest.$Shape
             */

            /**
             * Constructs a new SetClawAoRequest.
             * @memberof lebai.claw
             * @classdesc Represents a SetClawAoRequest.
             * @constructor
             * @param {lebai.claw.SetClawAoRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetClawAoRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetClawAoRequest address.
             * @member {lebai.claw.Address} address
             * @memberof lebai.claw.SetClawAoRequest
             * @instance
             */
            SetClawAoRequest.prototype.address = 0;

            /**
             * SetClawAoRequest value.
             * @member {number} value
             * @memberof lebai.claw.SetClawAoRequest
             * @instance
             */
            SetClawAoRequest.prototype.value = 0;

            /**
             * Creates a new SetClawAoRequest instance using the specified properties.
             * @function create
             * @memberof lebai.claw.SetClawAoRequest
             * @static
             * @param {lebai.claw.SetClawAoRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.claw.SetClawAoRequest} SetClawAoRequest instance
             * @type {{
             *   (properties: lebai.claw.SetClawAoRequest.$Shape): lebai.claw.SetClawAoRequest & lebai.claw.SetClawAoRequest.$Shape;
             *   (properties?: lebai.claw.SetClawAoRequest.$Properties): lebai.claw.SetClawAoRequest;
             * }}
             */
            SetClawAoRequest.create = function(properties) {
                return new SetClawAoRequest(properties);
            };

            /**
             * Encodes the specified SetClawAoRequest message. Does not implicitly {@link lebai.claw.SetClawAoRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.claw.SetClawAoRequest
             * @static
             * @param {lebai.claw.SetClawAoRequest.$Properties} message SetClawAoRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetClawAoRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.address);
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && !$Object.is(message.value, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetClawAoRequest message, length delimited. Does not implicitly {@link lebai.claw.SetClawAoRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.claw.SetClawAoRequest
             * @static
             * @param {lebai.claw.SetClawAoRequest.$Properties} message SetClawAoRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetClawAoRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetClawAoRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.claw.SetClawAoRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.claw.SetClawAoRequest & lebai.claw.SetClawAoRequest.$Shape} SetClawAoRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetClawAoRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.claw.SetClawAoRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.address = value;
                            else
                                delete message.address;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetClawAoRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.claw.SetClawAoRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.claw.SetClawAoRequest & lebai.claw.SetClawAoRequest.$Shape} SetClawAoRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetClawAoRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetClawAoRequest message.
             * @function verify
             * @memberof lebai.claw.SetClawAoRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetClawAoRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    if (typeof message.address !== "number" || (message.address | 0) !== message.address)
                        return "address: enum value expected";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (typeof message.value !== "number")
                        return "value: number expected";
                return null;
            };

            /**
             * Creates a SetClawAoRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.claw.SetClawAoRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.claw.SetClawAoRequest} SetClawAoRequest
             */
            SetClawAoRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.claw.SetClawAoRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.claw.SetClawAoRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.claw.SetClawAoRequest();
                if (object.address !== 0 && (typeof object.address !== "string" || $root.lebai.claw.Address[object.address] !== 0))
                    switch (object.address) {
                    case "AMPLITUDE":
                    case 0:
                        message.address = 0;
                        break;
                    case "FORCE":
                    case 1:
                        message.address = 1;
                        break;
                    case "VOLTAGE":
                    case 2:
                        message.address = 2;
                        break;
                    case "DISTANCE":
                    case 3:
                        message.address = 3;
                        break;
                    case "DO":
                    case 4:
                        message.address = 4;
                        break;
                    case "ACTUAL_AMPLITUDE":
                    case 5:
                        message.address = 5;
                        break;
                    case "ACTUAL_FORCE":
                    case 6:
                        message.address = 6;
                        break;
                    case "WEIGHT":
                    case 7:
                        message.address = 7;
                        break;
                    case "INIT":
                    case 8:
                        message.address = 8;
                        break;
                    case "FLAG":
                    case 9:
                        message.address = 9;
                        break;
                    case "ERROR":
                    case 10:
                        message.address = 10;
                        break;
                    case "DISABLE_INIT":
                    case 90:
                        message.address = 90;
                        break;
                    case "SAVE_TRAVEL":
                    case 91:
                        message.address = 91;
                        break;
                    default:
                        if (typeof object.address === "number" && (object.address | 0) === object.address)
                            message.address = object.address;
                    }
                if (object.value != null)
                    if (!$Object.is($Number(object.value), 0))
                        message.value = $Number(object.value);
                return message;
            };

            /**
             * Creates a plain object from a SetClawAoRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.claw.SetClawAoRequest
             * @static
             * @param {lebai.claw.SetClawAoRequest} message SetClawAoRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetClawAoRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.address = options.enums === $String ? "AMPLITUDE" : 0;
                    object.value = 0;
                }
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    object.address = options.enums === $String ? $root.lebai.claw.Address[message.address] === $undefined ? message.address : $root.lebai.claw.Address[message.address] : message.address;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = options.json && !$isFinite(message.value) ? $String(message.value) : message.value;
                return object;
            };

            /**
             * Converts this SetClawAoRequest to JSON.
             * @function toJSON
             * @memberof lebai.claw.SetClawAoRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetClawAoRequest.prototype.toJSON = function() {
                return SetClawAoRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetClawAoRequest
             * @function getTypeUrl
             * @memberof lebai.claw.SetClawAoRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetClawAoRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.claw.SetClawAoRequest";
            };

            return SetClawAoRequest;
        })();

        claw.GetClawAiRequest = (function() {

            /**
             * Properties of a GetClawAiRequest.
             * @typedef {Object} lebai.claw.GetClawAiRequest.$Properties
             * @property {lebai.claw.Address|null} [address] GetClawAiRequest address
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetClawAiRequest.
             * @memberof lebai.claw
             * @interface IGetClawAiRequest
             * @augments lebai.claw.GetClawAiRequest.$Properties
             * @deprecated Use lebai.claw.GetClawAiRequest.$Properties instead.
             */

            /**
             * Shape of a GetClawAiRequest.
             * @typedef {lebai.claw.GetClawAiRequest.$Properties} lebai.claw.GetClawAiRequest.$Shape
             */

            /**
             * Constructs a new GetClawAiRequest.
             * @memberof lebai.claw
             * @classdesc Represents a GetClawAiRequest.
             * @constructor
             * @param {lebai.claw.GetClawAiRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetClawAiRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetClawAiRequest address.
             * @member {lebai.claw.Address} address
             * @memberof lebai.claw.GetClawAiRequest
             * @instance
             */
            GetClawAiRequest.prototype.address = 0;

            /**
             * Creates a new GetClawAiRequest instance using the specified properties.
             * @function create
             * @memberof lebai.claw.GetClawAiRequest
             * @static
             * @param {lebai.claw.GetClawAiRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.claw.GetClawAiRequest} GetClawAiRequest instance
             * @type {{
             *   (properties: lebai.claw.GetClawAiRequest.$Shape): lebai.claw.GetClawAiRequest & lebai.claw.GetClawAiRequest.$Shape;
             *   (properties?: lebai.claw.GetClawAiRequest.$Properties): lebai.claw.GetClawAiRequest;
             * }}
             */
            GetClawAiRequest.create = function(properties) {
                return new GetClawAiRequest(properties);
            };

            /**
             * Encodes the specified GetClawAiRequest message. Does not implicitly {@link lebai.claw.GetClawAiRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.claw.GetClawAiRequest
             * @static
             * @param {lebai.claw.GetClawAiRequest.$Properties} message GetClawAiRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetClawAiRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.address);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetClawAiRequest message, length delimited. Does not implicitly {@link lebai.claw.GetClawAiRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.claw.GetClawAiRequest
             * @static
             * @param {lebai.claw.GetClawAiRequest.$Properties} message GetClawAiRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetClawAiRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetClawAiRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.claw.GetClawAiRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.claw.GetClawAiRequest & lebai.claw.GetClawAiRequest.$Shape} GetClawAiRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetClawAiRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.claw.GetClawAiRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.address = value;
                            else
                                delete message.address;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetClawAiRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.claw.GetClawAiRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.claw.GetClawAiRequest & lebai.claw.GetClawAiRequest.$Shape} GetClawAiRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetClawAiRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetClawAiRequest message.
             * @function verify
             * @memberof lebai.claw.GetClawAiRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetClawAiRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    if (typeof message.address !== "number" || (message.address | 0) !== message.address)
                        return "address: enum value expected";
                return null;
            };

            /**
             * Creates a GetClawAiRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.claw.GetClawAiRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.claw.GetClawAiRequest} GetClawAiRequest
             */
            GetClawAiRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.claw.GetClawAiRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.claw.GetClawAiRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.claw.GetClawAiRequest();
                if (object.address !== 0 && (typeof object.address !== "string" || $root.lebai.claw.Address[object.address] !== 0))
                    switch (object.address) {
                    case "AMPLITUDE":
                    case 0:
                        message.address = 0;
                        break;
                    case "FORCE":
                    case 1:
                        message.address = 1;
                        break;
                    case "VOLTAGE":
                    case 2:
                        message.address = 2;
                        break;
                    case "DISTANCE":
                    case 3:
                        message.address = 3;
                        break;
                    case "DO":
                    case 4:
                        message.address = 4;
                        break;
                    case "ACTUAL_AMPLITUDE":
                    case 5:
                        message.address = 5;
                        break;
                    case "ACTUAL_FORCE":
                    case 6:
                        message.address = 6;
                        break;
                    case "WEIGHT":
                    case 7:
                        message.address = 7;
                        break;
                    case "INIT":
                    case 8:
                        message.address = 8;
                        break;
                    case "FLAG":
                    case 9:
                        message.address = 9;
                        break;
                    case "ERROR":
                    case 10:
                        message.address = 10;
                        break;
                    case "DISABLE_INIT":
                    case 90:
                        message.address = 90;
                        break;
                    case "SAVE_TRAVEL":
                    case 91:
                        message.address = 91;
                        break;
                    default:
                        if (typeof object.address === "number" && (object.address | 0) === object.address)
                            message.address = object.address;
                    }
                return message;
            };

            /**
             * Creates a plain object from a GetClawAiRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.claw.GetClawAiRequest
             * @static
             * @param {lebai.claw.GetClawAiRequest} message GetClawAiRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetClawAiRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.address = options.enums === $String ? "AMPLITUDE" : 0;
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    object.address = options.enums === $String ? $root.lebai.claw.Address[message.address] === $undefined ? message.address : $root.lebai.claw.Address[message.address] : message.address;
                return object;
            };

            /**
             * Converts this GetClawAiRequest to JSON.
             * @function toJSON
             * @memberof lebai.claw.GetClawAiRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetClawAiRequest.prototype.toJSON = function() {
                return GetClawAiRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetClawAiRequest
             * @function getTypeUrl
             * @memberof lebai.claw.GetClawAiRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetClawAiRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.claw.GetClawAiRequest";
            };

            return GetClawAiRequest;
        })();

        claw.GetClawAiResponse = (function() {

            /**
             * Properties of a GetClawAiResponse.
             * @typedef {Object} lebai.claw.GetClawAiResponse.$Properties
             * @property {number|null} [value] GetClawAiResponse value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetClawAiResponse.
             * @memberof lebai.claw
             * @interface IGetClawAiResponse
             * @augments lebai.claw.GetClawAiResponse.$Properties
             * @deprecated Use lebai.claw.GetClawAiResponse.$Properties instead.
             */

            /**
             * Shape of a GetClawAiResponse.
             * @typedef {lebai.claw.GetClawAiResponse.$Properties} lebai.claw.GetClawAiResponse.$Shape
             */

            /**
             * Constructs a new GetClawAiResponse.
             * @memberof lebai.claw
             * @classdesc Represents a GetClawAiResponse.
             * @constructor
             * @param {lebai.claw.GetClawAiResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetClawAiResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetClawAiResponse value.
             * @member {number} value
             * @memberof lebai.claw.GetClawAiResponse
             * @instance
             */
            GetClawAiResponse.prototype.value = 0;

            /**
             * Creates a new GetClawAiResponse instance using the specified properties.
             * @function create
             * @memberof lebai.claw.GetClawAiResponse
             * @static
             * @param {lebai.claw.GetClawAiResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.claw.GetClawAiResponse} GetClawAiResponse instance
             * @type {{
             *   (properties: lebai.claw.GetClawAiResponse.$Shape): lebai.claw.GetClawAiResponse & lebai.claw.GetClawAiResponse.$Shape;
             *   (properties?: lebai.claw.GetClawAiResponse.$Properties): lebai.claw.GetClawAiResponse;
             * }}
             */
            GetClawAiResponse.create = function(properties) {
                return new GetClawAiResponse(properties);
            };

            /**
             * Encodes the specified GetClawAiResponse message. Does not implicitly {@link lebai.claw.GetClawAiResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.claw.GetClawAiResponse
             * @static
             * @param {lebai.claw.GetClawAiResponse.$Properties} message GetClawAiResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetClawAiResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && !$Object.is(message.value, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetClawAiResponse message, length delimited. Does not implicitly {@link lebai.claw.GetClawAiResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.claw.GetClawAiResponse
             * @static
             * @param {lebai.claw.GetClawAiResponse.$Properties} message GetClawAiResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetClawAiResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetClawAiResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.claw.GetClawAiResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.claw.GetClawAiResponse & lebai.claw.GetClawAiResponse.$Shape} GetClawAiResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetClawAiResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.claw.GetClawAiResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetClawAiResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.claw.GetClawAiResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.claw.GetClawAiResponse & lebai.claw.GetClawAiResponse.$Shape} GetClawAiResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetClawAiResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetClawAiResponse message.
             * @function verify
             * @memberof lebai.claw.GetClawAiResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetClawAiResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (typeof message.value !== "number")
                        return "value: number expected";
                return null;
            };

            /**
             * Creates a GetClawAiResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.claw.GetClawAiResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.claw.GetClawAiResponse} GetClawAiResponse
             */
            GetClawAiResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.claw.GetClawAiResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.claw.GetClawAiResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.claw.GetClawAiResponse();
                if (object.value != null)
                    if (!$Object.is($Number(object.value), 0))
                        message.value = $Number(object.value);
                return message;
            };

            /**
             * Creates a plain object from a GetClawAiResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.claw.GetClawAiResponse
             * @static
             * @param {lebai.claw.GetClawAiResponse} message GetClawAiResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetClawAiResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.value = 0;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = options.json && !$isFinite(message.value) ? $String(message.value) : message.value;
                return object;
            };

            /**
             * Converts this GetClawAiResponse to JSON.
             * @function toJSON
             * @memberof lebai.claw.GetClawAiResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetClawAiResponse.prototype.toJSON = function() {
                return GetClawAiResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetClawAiResponse
             * @function getTypeUrl
             * @memberof lebai.claw.GetClawAiResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetClawAiResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.claw.GetClawAiResponse";
            };

            return GetClawAiResponse;
        })();

        claw.WaitClawAiRequest = (function() {

            /**
             * Properties of a WaitClawAiRequest.
             * @typedef {Object} lebai.claw.WaitClawAiRequest.$Properties
             * @property {lebai.claw.Address|null} [address] WaitClawAiRequest address
             * @property {number|null} [value] WaitClawAiRequest value
             * @property {lebai.cmp.Relation|null} [relation] WaitClawAiRequest relation
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a WaitClawAiRequest.
             * @memberof lebai.claw
             * @interface IWaitClawAiRequest
             * @augments lebai.claw.WaitClawAiRequest.$Properties
             * @deprecated Use lebai.claw.WaitClawAiRequest.$Properties instead.
             */

            /**
             * Shape of a WaitClawAiRequest.
             * @typedef {lebai.claw.WaitClawAiRequest.$Properties} lebai.claw.WaitClawAiRequest.$Shape
             */

            /**
             * Constructs a new WaitClawAiRequest.
             * @memberof lebai.claw
             * @classdesc Represents a WaitClawAiRequest.
             * @constructor
             * @param {lebai.claw.WaitClawAiRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var WaitClawAiRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * WaitClawAiRequest address.
             * @member {lebai.claw.Address} address
             * @memberof lebai.claw.WaitClawAiRequest
             * @instance
             */
            WaitClawAiRequest.prototype.address = 0;

            /**
             * WaitClawAiRequest value.
             * @member {number} value
             * @memberof lebai.claw.WaitClawAiRequest
             * @instance
             */
            WaitClawAiRequest.prototype.value = 0;

            /**
             * WaitClawAiRequest relation.
             * @member {lebai.cmp.Relation} relation
             * @memberof lebai.claw.WaitClawAiRequest
             * @instance
             */
            WaitClawAiRequest.prototype.relation = 0;

            /**
             * Creates a new WaitClawAiRequest instance using the specified properties.
             * @function create
             * @memberof lebai.claw.WaitClawAiRequest
             * @static
             * @param {lebai.claw.WaitClawAiRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.claw.WaitClawAiRequest} WaitClawAiRequest instance
             * @type {{
             *   (properties: lebai.claw.WaitClawAiRequest.$Shape): lebai.claw.WaitClawAiRequest & lebai.claw.WaitClawAiRequest.$Shape;
             *   (properties?: lebai.claw.WaitClawAiRequest.$Properties): lebai.claw.WaitClawAiRequest;
             * }}
             */
            WaitClawAiRequest.create = function(properties) {
                return new WaitClawAiRequest(properties);
            };

            /**
             * Encodes the specified WaitClawAiRequest message. Does not implicitly {@link lebai.claw.WaitClawAiRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.claw.WaitClawAiRequest
             * @static
             * @param {lebai.claw.WaitClawAiRequest.$Properties} message WaitClawAiRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WaitClawAiRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.address);
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && !$Object.is(message.value, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.value);
                if (message.relation != null && $Object.hasOwnProperty.call(message, "relation") && message.relation !== 0)
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.relation);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified WaitClawAiRequest message, length delimited. Does not implicitly {@link lebai.claw.WaitClawAiRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.claw.WaitClawAiRequest
             * @static
             * @param {lebai.claw.WaitClawAiRequest.$Properties} message WaitClawAiRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WaitClawAiRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a WaitClawAiRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.claw.WaitClawAiRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.claw.WaitClawAiRequest & lebai.claw.WaitClawAiRequest.$Shape} WaitClawAiRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WaitClawAiRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.claw.WaitClawAiRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.address = value;
                            else
                                delete message.address;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.relation = value;
                            else
                                delete message.relation;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a WaitClawAiRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.claw.WaitClawAiRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.claw.WaitClawAiRequest & lebai.claw.WaitClawAiRequest.$Shape} WaitClawAiRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WaitClawAiRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a WaitClawAiRequest message.
             * @function verify
             * @memberof lebai.claw.WaitClawAiRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            WaitClawAiRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    if (typeof message.address !== "number" || (message.address | 0) !== message.address)
                        return "address: enum value expected";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (typeof message.value !== "number")
                        return "value: number expected";
                if (message.relation != null && $Object.hasOwnProperty.call(message, "relation"))
                    if (typeof message.relation !== "number" || (message.relation | 0) !== message.relation)
                        return "relation: enum value expected";
                return null;
            };

            /**
             * Creates a WaitClawAiRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.claw.WaitClawAiRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.claw.WaitClawAiRequest} WaitClawAiRequest
             */
            WaitClawAiRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.claw.WaitClawAiRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.claw.WaitClawAiRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.claw.WaitClawAiRequest();
                if (object.address !== 0 && (typeof object.address !== "string" || $root.lebai.claw.Address[object.address] !== 0))
                    switch (object.address) {
                    case "AMPLITUDE":
                    case 0:
                        message.address = 0;
                        break;
                    case "FORCE":
                    case 1:
                        message.address = 1;
                        break;
                    case "VOLTAGE":
                    case 2:
                        message.address = 2;
                        break;
                    case "DISTANCE":
                    case 3:
                        message.address = 3;
                        break;
                    case "DO":
                    case 4:
                        message.address = 4;
                        break;
                    case "ACTUAL_AMPLITUDE":
                    case 5:
                        message.address = 5;
                        break;
                    case "ACTUAL_FORCE":
                    case 6:
                        message.address = 6;
                        break;
                    case "WEIGHT":
                    case 7:
                        message.address = 7;
                        break;
                    case "INIT":
                    case 8:
                        message.address = 8;
                        break;
                    case "FLAG":
                    case 9:
                        message.address = 9;
                        break;
                    case "ERROR":
                    case 10:
                        message.address = 10;
                        break;
                    case "DISABLE_INIT":
                    case 90:
                        message.address = 90;
                        break;
                    case "SAVE_TRAVEL":
                    case 91:
                        message.address = 91;
                        break;
                    default:
                        if (typeof object.address === "number" && (object.address | 0) === object.address)
                            message.address = object.address;
                    }
                if (object.value != null)
                    if (!$Object.is($Number(object.value), 0))
                        message.value = $Number(object.value);
                if (object.relation !== 0 && (typeof object.relation !== "string" || $root.lebai.cmp.Relation[object.relation] !== 0))
                    switch (object.relation) {
                    case "EQ":
                    case 0:
                        message.relation = 0;
                        break;
                    case "NEQ":
                    case 2:
                        message.relation = 2;
                        break;
                    case "LT":
                    case 3:
                        message.relation = 3;
                        break;
                    case "LTE":
                    case 4:
                        message.relation = 4;
                        break;
                    case "GT":
                    case 5:
                        message.relation = 5;
                        break;
                    case "GTE":
                    case 6:
                        message.relation = 6;
                        break;
                    default:
                        if (typeof object.relation === "number" && (object.relation | 0) === object.relation)
                            message.relation = object.relation;
                    }
                return message;
            };

            /**
             * Creates a plain object from a WaitClawAiRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.claw.WaitClawAiRequest
             * @static
             * @param {lebai.claw.WaitClawAiRequest} message WaitClawAiRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            WaitClawAiRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.address = options.enums === $String ? "AMPLITUDE" : 0;
                    object.value = 0;
                    object.relation = options.enums === $String ? "EQ" : 0;
                }
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    object.address = options.enums === $String ? $root.lebai.claw.Address[message.address] === $undefined ? message.address : $root.lebai.claw.Address[message.address] : message.address;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = options.json && !$isFinite(message.value) ? $String(message.value) : message.value;
                if (message.relation != null && $Object.hasOwnProperty.call(message, "relation"))
                    object.relation = options.enums === $String ? $root.lebai.cmp.Relation[message.relation] === $undefined ? message.relation : $root.lebai.cmp.Relation[message.relation] : message.relation;
                return object;
            };

            /**
             * Converts this WaitClawAiRequest to JSON.
             * @function toJSON
             * @memberof lebai.claw.WaitClawAiRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            WaitClawAiRequest.prototype.toJSON = function() {
                return WaitClawAiRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for WaitClawAiRequest
             * @function getTypeUrl
             * @memberof lebai.claw.WaitClawAiRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            WaitClawAiRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.claw.WaitClawAiRequest";
            };

            return WaitClawAiRequest;
        })();

        claw.ClawService = (function() {

            /**
             * Constructs a new ClawService service.
             * @memberof lebai.claw
             * @classdesc Represents a ClawService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var ClawService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(ClawService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: ClawService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new ClawService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.claw.ClawService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {ClawService} RPC service. Useful where requests and/or responses are streamed.
             */
            ClawService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.claw.ClawService#initClaw}.
             * @memberof lebai.claw.ClawService
             * @typedef InitClawCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls InitClaw.
             * @memberof lebai.claw.ClawService
             * @typedef InitClaw
             * @type {{
             *   (request: lebai.claw.IInitClawRequest, callback: lebai.claw.ClawService.InitClawCallback): void;
             *   (request: lebai.claw.IInitClawRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "InitClaw";
             *   readonly path: "/lebai.claw.ClawService/InitClaw";
             *   readonly requestType: "InitClawRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls InitClaw.
             * @name lebai.claw.ClawService#initClaw
             * @type {lebai.claw.ClawService.InitClaw}
             */
            $Object.defineProperties(ClawService.prototype.initClaw = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ClawService.prototype.initClaw, $root.lebai.claw.InitClawRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "InitClaw" },
                path: { value: "/lebai.claw.ClawService/InitClaw" },
                requestType: { value: "InitClawRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.claw.ClawService#setClaw}.
             * @memberof lebai.claw.ClawService
             * @typedef SetClawCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetClaw.
             * @memberof lebai.claw.ClawService
             * @typedef SetClaw
             * @type {{
             *   (request: lebai.claw.ISetClawRequest, callback: lebai.claw.ClawService.SetClawCallback): void;
             *   (request: lebai.claw.ISetClawRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetClaw";
             *   readonly path: "/lebai.claw.ClawService/SetClaw";
             *   readonly requestType: "SetClawRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetClaw.
             * @name lebai.claw.ClawService#setClaw
             * @type {lebai.claw.ClawService.SetClaw}
             */
            $Object.defineProperties(ClawService.prototype.setClaw = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ClawService.prototype.setClaw, $root.lebai.claw.SetClawRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetClaw" },
                path: { value: "/lebai.claw.ClawService/SetClaw" },
                requestType: { value: "SetClawRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.claw.ClawService#getClaw}.
             * @memberof lebai.claw.ClawService
             * @typedef GetClawCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.claw.Claw} [response] Claw
             */

            /**
             * Calls GetClaw.
             * @memberof lebai.claw.ClawService
             * @typedef GetClaw
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.claw.ClawService.GetClawCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.claw.Claw>;
             *   readonly name: "GetClaw";
             *   readonly path: "/lebai.claw.ClawService/GetClaw";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "Claw";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetClaw.
             * @name lebai.claw.ClawService#getClaw
             * @type {lebai.claw.ClawService.GetClaw}
             */
            $Object.defineProperties(ClawService.prototype.getClaw = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ClawService.prototype.getClaw, $root.google.protobuf.Empty, $root.lebai.claw.Claw, request, callback);
            }, {
                name: { value: "GetClaw" },
                path: { value: "/lebai.claw.ClawService/GetClaw" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "Claw" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.claw.ClawService#setClawAo}.
             * @memberof lebai.claw.ClawService
             * @typedef SetClawAoCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetClawAo.
             * @memberof lebai.claw.ClawService
             * @typedef SetClawAo
             * @type {{
             *   (request: lebai.claw.ISetClawAoRequest, callback: lebai.claw.ClawService.SetClawAoCallback): void;
             *   (request: lebai.claw.ISetClawAoRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetClawAo";
             *   readonly path: "/lebai.claw.ClawService/SetClawAo";
             *   readonly requestType: "SetClawAoRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetClawAo.
             * @name lebai.claw.ClawService#setClawAo
             * @type {lebai.claw.ClawService.SetClawAo}
             */
            $Object.defineProperties(ClawService.prototype.setClawAo = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ClawService.prototype.setClawAo, $root.lebai.claw.SetClawAoRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetClawAo" },
                path: { value: "/lebai.claw.ClawService/SetClawAo" },
                requestType: { value: "SetClawAoRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.claw.ClawService#getClawAi}.
             * @memberof lebai.claw.ClawService
             * @typedef GetClawAiCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.claw.GetClawAiResponse} [response] GetClawAiResponse
             */

            /**
             * Calls GetClawAi.
             * @memberof lebai.claw.ClawService
             * @typedef GetClawAi
             * @type {{
             *   (request: lebai.claw.IGetClawAiRequest, callback: lebai.claw.ClawService.GetClawAiCallback): void;
             *   (request: lebai.claw.IGetClawAiRequest): Promise<lebai.claw.GetClawAiResponse>;
             *   readonly name: "GetClawAi";
             *   readonly path: "/lebai.claw.ClawService/GetClawAi";
             *   readonly requestType: "GetClawAiRequest";
             *   readonly responseType: "GetClawAiResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetClawAi.
             * @name lebai.claw.ClawService#getClawAi
             * @type {lebai.claw.ClawService.GetClawAi}
             */
            $Object.defineProperties(ClawService.prototype.getClawAi = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ClawService.prototype.getClawAi, $root.lebai.claw.GetClawAiRequest, $root.lebai.claw.GetClawAiResponse, request, callback);
            }, {
                name: { value: "GetClawAi" },
                path: { value: "/lebai.claw.ClawService/GetClawAi" },
                requestType: { value: "GetClawAiRequest" },
                responseType: { value: "GetClawAiResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.claw.ClawService#waitClawAi}.
             * @memberof lebai.claw.ClawService
             * @typedef WaitClawAiCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls WaitClawAi.
             * @memberof lebai.claw.ClawService
             * @typedef WaitClawAi
             * @type {{
             *   (request: lebai.claw.IWaitClawAiRequest, callback: lebai.claw.ClawService.WaitClawAiCallback): void;
             *   (request: lebai.claw.IWaitClawAiRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "WaitClawAi";
             *   readonly path: "/lebai.claw.ClawService/WaitClawAi";
             *   readonly requestType: "WaitClawAiRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls WaitClawAi.
             * @name lebai.claw.ClawService#waitClawAi
             * @type {lebai.claw.ClawService.WaitClawAi}
             */
            $Object.defineProperties(ClawService.prototype.waitClawAi = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ClawService.prototype.waitClawAi, $root.lebai.claw.WaitClawAiRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "WaitClawAi" },
                path: { value: "/lebai.claw.ClawService/WaitClawAi" },
                requestType: { value: "WaitClawAiRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return ClawService;
        })();

        return claw;
    })();

    lebai.cmp = (function() {

        /**
         * Namespace cmp.
         * @memberof lebai
         * @namespace
         */
        var cmp = {};

        /**
         * Logic enum.
         * @name lebai.cmp.Logic
         * @enum {number}
         * @property {number} AND=0 AND value
         * @property {number} OR=2 OR value
         * @property {number} NOT=3 NOT value
         */
        cmp.Logic = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "AND"] = 0;
            values[valuesById[2] = "OR"] = 2;
            values[valuesById[3] = "NOT"] = 3;
            return values;
        })();

        /**
         * Relation enum.
         * @name lebai.cmp.Relation
         * @enum {number}
         * @property {number} EQ=0 EQ value
         * @property {number} NEQ=2 NEQ value
         * @property {number} LT=3 LT value
         * @property {number} LTE=4 LTE value
         * @property {number} GT=5 GT value
         * @property {number} GTE=6 GTE value
         */
        cmp.Relation = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "EQ"] = 0;
            values[valuesById[2] = "NEQ"] = 2;
            values[valuesById[3] = "LT"] = 3;
            values[valuesById[4] = "LTE"] = 4;
            values[valuesById[5] = "GT"] = 5;
            values[valuesById[6] = "GTE"] = 6;
            return values;
        })();

        return cmp;
    })();

    lebai.db = (function() {

        /**
         * Namespace db.
         * @memberof lebai
         * @namespace
         */
        var db = {};

        db.SaveRequest = (function() {

            /**
             * Properties of a SaveRequest.
             * @typedef {Object} lebai.db.SaveRequest.$Properties
             * @property {string|null} [name] SaveRequest name
             * @property {string|null} [data] SaveRequest data
             * @property {string|null} [dir] SaveRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SaveRequest.
             * @memberof lebai.db
             * @interface ISaveRequest
             * @augments lebai.db.SaveRequest.$Properties
             * @deprecated Use lebai.db.SaveRequest.$Properties instead.
             */

            /**
             * Shape of a SaveRequest.
             * @typedef {lebai.db.SaveRequest.$Properties} lebai.db.SaveRequest.$Shape
             */

            /**
             * Constructs a new SaveRequest.
             * @memberof lebai.db
             * @classdesc Represents a SaveRequest.
             * @constructor
             * @param {lebai.db.SaveRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SaveRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SaveRequest name.
             * @member {string} name
             * @memberof lebai.db.SaveRequest
             * @instance
             */
            SaveRequest.prototype.name = "";

            /**
             * SaveRequest data.
             * @member {string} data
             * @memberof lebai.db.SaveRequest
             * @instance
             */
            SaveRequest.prototype.data = "";

            /**
             * SaveRequest dir.
             * @member {string} dir
             * @memberof lebai.db.SaveRequest
             * @instance
             */
            SaveRequest.prototype.dir = "";

            /**
             * Creates a new SaveRequest instance using the specified properties.
             * @function create
             * @memberof lebai.db.SaveRequest
             * @static
             * @param {lebai.db.SaveRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.db.SaveRequest} SaveRequest instance
             * @type {{
             *   (properties: lebai.db.SaveRequest.$Shape): lebai.db.SaveRequest & lebai.db.SaveRequest.$Shape;
             *   (properties?: lebai.db.SaveRequest.$Properties): lebai.db.SaveRequest;
             * }}
             */
            SaveRequest.create = function(properties) {
                return new SaveRequest(properties);
            };

            /**
             * Encodes the specified SaveRequest message. Does not implicitly {@link lebai.db.SaveRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.db.SaveRequest
             * @static
             * @param {lebai.db.SaveRequest.$Properties} message SaveRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.data);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SaveRequest message, length delimited. Does not implicitly {@link lebai.db.SaveRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.db.SaveRequest
             * @static
             * @param {lebai.db.SaveRequest.$Properties} message SaveRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SaveRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.db.SaveRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.db.SaveRequest & lebai.db.SaveRequest.$Shape} SaveRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.db.SaveRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.data = value;
                            else
                                delete message.data;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SaveRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.db.SaveRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.db.SaveRequest & lebai.db.SaveRequest.$Shape} SaveRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SaveRequest message.
             * @function verify
             * @memberof lebai.db.SaveRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SaveRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    if (!$util.isString(message.data))
                        return "data: string expected";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates a SaveRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.db.SaveRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.db.SaveRequest} SaveRequest
             */
            SaveRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.db.SaveRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.db.SaveRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.db.SaveRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.data != null)
                    if (typeof object.data !== "string" || object.data.length)
                        message.data = $String(object.data);
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from a SaveRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.db.SaveRequest
             * @static
             * @param {lebai.db.SaveRequest} message SaveRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SaveRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.data = "";
                    object.dir = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = message.data;
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                return object;
            };

            /**
             * Converts this SaveRequest to JSON.
             * @function toJSON
             * @memberof lebai.db.SaveRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SaveRequest.prototype.toJSON = function() {
                return SaveRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SaveRequest
             * @function getTypeUrl
             * @memberof lebai.db.SaveRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SaveRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.db.SaveRequest";
            };

            return SaveRequest;
        })();

        db.LoadRequest = (function() {

            /**
             * Properties of a LoadRequest.
             * @typedef {Object} lebai.db.LoadRequest.$Properties
             * @property {string|null} [name] LoadRequest name
             * @property {string|null} [dir] LoadRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LoadRequest.
             * @memberof lebai.db
             * @interface ILoadRequest
             * @augments lebai.db.LoadRequest.$Properties
             * @deprecated Use lebai.db.LoadRequest.$Properties instead.
             */

            /**
             * Shape of a LoadRequest.
             * @typedef {lebai.db.LoadRequest.$Properties} lebai.db.LoadRequest.$Shape
             */

            /**
             * Constructs a new LoadRequest.
             * @memberof lebai.db
             * @classdesc Represents a LoadRequest.
             * @constructor
             * @param {lebai.db.LoadRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LoadRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LoadRequest name.
             * @member {string} name
             * @memberof lebai.db.LoadRequest
             * @instance
             */
            LoadRequest.prototype.name = "";

            /**
             * LoadRequest dir.
             * @member {string} dir
             * @memberof lebai.db.LoadRequest
             * @instance
             */
            LoadRequest.prototype.dir = "";

            /**
             * Creates a new LoadRequest instance using the specified properties.
             * @function create
             * @memberof lebai.db.LoadRequest
             * @static
             * @param {lebai.db.LoadRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.db.LoadRequest} LoadRequest instance
             * @type {{
             *   (properties: lebai.db.LoadRequest.$Shape): lebai.db.LoadRequest & lebai.db.LoadRequest.$Shape;
             *   (properties?: lebai.db.LoadRequest.$Properties): lebai.db.LoadRequest;
             * }}
             */
            LoadRequest.create = function(properties) {
                return new LoadRequest(properties);
            };

            /**
             * Encodes the specified LoadRequest message. Does not implicitly {@link lebai.db.LoadRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.db.LoadRequest
             * @static
             * @param {lebai.db.LoadRequest.$Properties} message LoadRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LoadRequest message, length delimited. Does not implicitly {@link lebai.db.LoadRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.db.LoadRequest
             * @static
             * @param {lebai.db.LoadRequest.$Properties} message LoadRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LoadRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.db.LoadRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.db.LoadRequest & lebai.db.LoadRequest.$Shape} LoadRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.db.LoadRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LoadRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.db.LoadRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.db.LoadRequest & lebai.db.LoadRequest.$Shape} LoadRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LoadRequest message.
             * @function verify
             * @memberof lebai.db.LoadRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LoadRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates a LoadRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.db.LoadRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.db.LoadRequest} LoadRequest
             */
            LoadRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.db.LoadRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.db.LoadRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.db.LoadRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from a LoadRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.db.LoadRequest
             * @static
             * @param {lebai.db.LoadRequest} message LoadRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LoadRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.dir = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                return object;
            };

            /**
             * Converts this LoadRequest to JSON.
             * @function toJSON
             * @memberof lebai.db.LoadRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LoadRequest.prototype.toJSON = function() {
                return LoadRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LoadRequest
             * @function getTypeUrl
             * @memberof lebai.db.LoadRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LoadRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.db.LoadRequest";
            };

            return LoadRequest;
        })();

        db.LoadResponse = (function() {

            /**
             * Properties of a LoadResponse.
             * @typedef {Object} lebai.db.LoadResponse.$Properties
             * @property {string|null} [data] LoadResponse data
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LoadResponse.
             * @memberof lebai.db
             * @interface ILoadResponse
             * @augments lebai.db.LoadResponse.$Properties
             * @deprecated Use lebai.db.LoadResponse.$Properties instead.
             */

            /**
             * Shape of a LoadResponse.
             * @typedef {lebai.db.LoadResponse.$Properties} lebai.db.LoadResponse.$Shape
             */

            /**
             * Constructs a new LoadResponse.
             * @memberof lebai.db
             * @classdesc Represents a LoadResponse.
             * @constructor
             * @param {lebai.db.LoadResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LoadResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LoadResponse data.
             * @member {string} data
             * @memberof lebai.db.LoadResponse
             * @instance
             */
            LoadResponse.prototype.data = "";

            /**
             * Creates a new LoadResponse instance using the specified properties.
             * @function create
             * @memberof lebai.db.LoadResponse
             * @static
             * @param {lebai.db.LoadResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.db.LoadResponse} LoadResponse instance
             * @type {{
             *   (properties: lebai.db.LoadResponse.$Shape): lebai.db.LoadResponse & lebai.db.LoadResponse.$Shape;
             *   (properties?: lebai.db.LoadResponse.$Properties): lebai.db.LoadResponse;
             * }}
             */
            LoadResponse.create = function(properties) {
                return new LoadResponse(properties);
            };

            /**
             * Encodes the specified LoadResponse message. Does not implicitly {@link lebai.db.LoadResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.db.LoadResponse
             * @static
             * @param {lebai.db.LoadResponse.$Properties} message LoadResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.data);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LoadResponse message, length delimited. Does not implicitly {@link lebai.db.LoadResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.db.LoadResponse
             * @static
             * @param {lebai.db.LoadResponse.$Properties} message LoadResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LoadResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.db.LoadResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.db.LoadResponse & lebai.db.LoadResponse.$Shape} LoadResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.db.LoadResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.data = value;
                            else
                                delete message.data;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LoadResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.db.LoadResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.db.LoadResponse & lebai.db.LoadResponse.$Shape} LoadResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LoadResponse message.
             * @function verify
             * @memberof lebai.db.LoadResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LoadResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    if (!$util.isString(message.data))
                        return "data: string expected";
                return null;
            };

            /**
             * Creates a LoadResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.db.LoadResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.db.LoadResponse} LoadResponse
             */
            LoadResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.db.LoadResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.db.LoadResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.db.LoadResponse();
                if (object.data != null)
                    if (typeof object.data !== "string" || object.data.length)
                        message.data = $String(object.data);
                return message;
            };

            /**
             * Creates a plain object from a LoadResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.db.LoadResponse
             * @static
             * @param {lebai.db.LoadResponse} message LoadResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LoadResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.data = "";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = message.data;
                return object;
            };

            /**
             * Converts this LoadResponse to JSON.
             * @function toJSON
             * @memberof lebai.db.LoadResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LoadResponse.prototype.toJSON = function() {
                return LoadResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LoadResponse
             * @function getTypeUrl
             * @memberof lebai.db.LoadResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LoadResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.db.LoadResponse";
            };

            return LoadResponse;
        })();

        db.LoadListRequest = (function() {

            /**
             * Properties of a LoadListRequest.
             * @typedef {Object} lebai.db.LoadListRequest.$Properties
             * @property {string|null} [dir] LoadListRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LoadListRequest.
             * @memberof lebai.db
             * @interface ILoadListRequest
             * @augments lebai.db.LoadListRequest.$Properties
             * @deprecated Use lebai.db.LoadListRequest.$Properties instead.
             */

            /**
             * Shape of a LoadListRequest.
             * @typedef {lebai.db.LoadListRequest.$Properties} lebai.db.LoadListRequest.$Shape
             */

            /**
             * Constructs a new LoadListRequest.
             * @memberof lebai.db
             * @classdesc Represents a LoadListRequest.
             * @constructor
             * @param {lebai.db.LoadListRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LoadListRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LoadListRequest dir.
             * @member {string} dir
             * @memberof lebai.db.LoadListRequest
             * @instance
             */
            LoadListRequest.prototype.dir = "";

            /**
             * Creates a new LoadListRequest instance using the specified properties.
             * @function create
             * @memberof lebai.db.LoadListRequest
             * @static
             * @param {lebai.db.LoadListRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.db.LoadListRequest} LoadListRequest instance
             * @type {{
             *   (properties: lebai.db.LoadListRequest.$Shape): lebai.db.LoadListRequest & lebai.db.LoadListRequest.$Shape;
             *   (properties?: lebai.db.LoadListRequest.$Properties): lebai.db.LoadListRequest;
             * }}
             */
            LoadListRequest.create = function(properties) {
                return new LoadListRequest(properties);
            };

            /**
             * Encodes the specified LoadListRequest message. Does not implicitly {@link lebai.db.LoadListRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.db.LoadListRequest
             * @static
             * @param {lebai.db.LoadListRequest.$Properties} message LoadListRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadListRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LoadListRequest message, length delimited. Does not implicitly {@link lebai.db.LoadListRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.db.LoadListRequest
             * @static
             * @param {lebai.db.LoadListRequest.$Properties} message LoadListRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadListRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LoadListRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.db.LoadListRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.db.LoadListRequest & lebai.db.LoadListRequest.$Shape} LoadListRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadListRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.db.LoadListRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LoadListRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.db.LoadListRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.db.LoadListRequest & lebai.db.LoadListRequest.$Shape} LoadListRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadListRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LoadListRequest message.
             * @function verify
             * @memberof lebai.db.LoadListRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LoadListRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates a LoadListRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.db.LoadListRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.db.LoadListRequest} LoadListRequest
             */
            LoadListRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.db.LoadListRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.db.LoadListRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.db.LoadListRequest();
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from a LoadListRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.db.LoadListRequest
             * @static
             * @param {lebai.db.LoadListRequest} message LoadListRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LoadListRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.dir = "";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                return object;
            };

            /**
             * Converts this LoadListRequest to JSON.
             * @function toJSON
             * @memberof lebai.db.LoadListRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LoadListRequest.prototype.toJSON = function() {
                return LoadListRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LoadListRequest
             * @function getTypeUrl
             * @memberof lebai.db.LoadListRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LoadListRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.db.LoadListRequest";
            };

            return LoadListRequest;
        })();

        db.LoadListResponse = (function() {

            /**
             * Properties of a LoadListResponse.
             * @typedef {Object} lebai.db.LoadListResponse.$Properties
             * @property {Array.<string>|null} [names] LoadListResponse names
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LoadListResponse.
             * @memberof lebai.db
             * @interface ILoadListResponse
             * @augments lebai.db.LoadListResponse.$Properties
             * @deprecated Use lebai.db.LoadListResponse.$Properties instead.
             */

            /**
             * Shape of a LoadListResponse.
             * @typedef {lebai.db.LoadListResponse.$Properties} lebai.db.LoadListResponse.$Shape
             */

            /**
             * Constructs a new LoadListResponse.
             * @memberof lebai.db
             * @classdesc Represents a LoadListResponse.
             * @constructor
             * @param {lebai.db.LoadListResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LoadListResponse = function (properties) {
                this.names = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LoadListResponse names.
             * @member {Array.<string>} names
             * @memberof lebai.db.LoadListResponse
             * @instance
             */
            LoadListResponse.prototype.names = $util.emptyArray;

            /**
             * Creates a new LoadListResponse instance using the specified properties.
             * @function create
             * @memberof lebai.db.LoadListResponse
             * @static
             * @param {lebai.db.LoadListResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.db.LoadListResponse} LoadListResponse instance
             * @type {{
             *   (properties: lebai.db.LoadListResponse.$Shape): lebai.db.LoadListResponse & lebai.db.LoadListResponse.$Shape;
             *   (properties?: lebai.db.LoadListResponse.$Properties): lebai.db.LoadListResponse;
             * }}
             */
            LoadListResponse.create = function(properties) {
                return new LoadListResponse(properties);
            };

            /**
             * Encodes the specified LoadListResponse message. Does not implicitly {@link lebai.db.LoadListResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.db.LoadListResponse
             * @static
             * @param {lebai.db.LoadListResponse.$Properties} message LoadListResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadListResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.names != null && message.names.length)
                    for (var i = 0; i < message.names.length; ++i)
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.names[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LoadListResponse message, length delimited. Does not implicitly {@link lebai.db.LoadListResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.db.LoadListResponse
             * @static
             * @param {lebai.db.LoadListResponse.$Properties} message LoadListResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadListResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LoadListResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.db.LoadListResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.db.LoadListResponse & lebai.db.LoadListResponse.$Shape} LoadListResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadListResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.db.LoadListResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.names && message.names.length))
                                message.names = [];
                            message.names.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LoadListResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.db.LoadListResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.db.LoadListResponse & lebai.db.LoadListResponse.$Shape} LoadListResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadListResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LoadListResponse message.
             * @function verify
             * @memberof lebai.db.LoadListResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LoadListResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.names != null && $Object.hasOwnProperty.call(message, "names")) {
                    if (!$Array.isArray(message.names))
                        return "names: array expected";
                    for (var i = 0; i < message.names.length; ++i)
                        if (!$util.isString(message.names[i]))
                            return "names: string[] expected";
                }
                return null;
            };

            /**
             * Creates a LoadListResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.db.LoadListResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.db.LoadListResponse} LoadListResponse
             */
            LoadListResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.db.LoadListResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.db.LoadListResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.db.LoadListResponse();
                if (object.names) {
                    if (!$Array.isArray(object.names))
                        throw $TypeError(".lebai.db.LoadListResponse.names: array expected");
                    message.names = $Array(object.names.length);
                    for (var i = 0; i < object.names.length; ++i)
                        message.names[i] = $String(object.names[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a LoadListResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.db.LoadListResponse
             * @static
             * @param {lebai.db.LoadListResponse} message LoadListResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LoadListResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.names = [];
                if (message.names && message.names.length) {
                    object.names = $Array(message.names.length);
                    for (var j = 0; j < message.names.length; ++j)
                        object.names[j] = message.names[j];
                }
                return object;
            };

            /**
             * Converts this LoadListResponse to JSON.
             * @function toJSON
             * @memberof lebai.db.LoadListResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LoadListResponse.prototype.toJSON = function() {
                return LoadListResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LoadListResponse
             * @function getTypeUrl
             * @memberof lebai.db.LoadListResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LoadListResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.db.LoadListResponse";
            };

            return LoadListResponse;
        })();

        db.DeleteRequest = (function() {

            /**
             * Properties of a DeleteRequest.
             * @typedef {Object} lebai.db.DeleteRequest.$Properties
             * @property {string|null} [name] DeleteRequest name
             * @property {string|null} [dir] DeleteRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DeleteRequest.
             * @memberof lebai.db
             * @interface IDeleteRequest
             * @augments lebai.db.DeleteRequest.$Properties
             * @deprecated Use lebai.db.DeleteRequest.$Properties instead.
             */

            /**
             * Shape of a DeleteRequest.
             * @typedef {lebai.db.DeleteRequest.$Properties} lebai.db.DeleteRequest.$Shape
             */

            /**
             * Constructs a new DeleteRequest.
             * @memberof lebai.db
             * @classdesc Represents a DeleteRequest.
             * @constructor
             * @param {lebai.db.DeleteRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var DeleteRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DeleteRequest name.
             * @member {string} name
             * @memberof lebai.db.DeleteRequest
             * @instance
             */
            DeleteRequest.prototype.name = "";

            /**
             * DeleteRequest dir.
             * @member {string} dir
             * @memberof lebai.db.DeleteRequest
             * @instance
             */
            DeleteRequest.prototype.dir = "";

            /**
             * Creates a new DeleteRequest instance using the specified properties.
             * @function create
             * @memberof lebai.db.DeleteRequest
             * @static
             * @param {lebai.db.DeleteRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.db.DeleteRequest} DeleteRequest instance
             * @type {{
             *   (properties: lebai.db.DeleteRequest.$Shape): lebai.db.DeleteRequest & lebai.db.DeleteRequest.$Shape;
             *   (properties?: lebai.db.DeleteRequest.$Properties): lebai.db.DeleteRequest;
             * }}
             */
            DeleteRequest.create = function(properties) {
                return new DeleteRequest(properties);
            };

            /**
             * Encodes the specified DeleteRequest message. Does not implicitly {@link lebai.db.DeleteRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.db.DeleteRequest
             * @static
             * @param {lebai.db.DeleteRequest.$Properties} message DeleteRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeleteRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DeleteRequest message, length delimited. Does not implicitly {@link lebai.db.DeleteRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.db.DeleteRequest
             * @static
             * @param {lebai.db.DeleteRequest.$Properties} message DeleteRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeleteRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a DeleteRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.db.DeleteRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.db.DeleteRequest & lebai.db.DeleteRequest.$Shape} DeleteRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeleteRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.db.DeleteRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a DeleteRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.db.DeleteRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.db.DeleteRequest & lebai.db.DeleteRequest.$Shape} DeleteRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeleteRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DeleteRequest message.
             * @function verify
             * @memberof lebai.db.DeleteRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DeleteRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates a DeleteRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.db.DeleteRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.db.DeleteRequest} DeleteRequest
             */
            DeleteRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.db.DeleteRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.db.DeleteRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.db.DeleteRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from a DeleteRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.db.DeleteRequest
             * @static
             * @param {lebai.db.DeleteRequest} message DeleteRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DeleteRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.dir = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                return object;
            };

            /**
             * Converts this DeleteRequest to JSON.
             * @function toJSON
             * @memberof lebai.db.DeleteRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DeleteRequest.prototype.toJSON = function() {
                return DeleteRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DeleteRequest
             * @function getTypeUrl
             * @memberof lebai.db.DeleteRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DeleteRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.db.DeleteRequest";
            };

            return DeleteRequest;
        })();

        db.Dir = (function() {

            /**
             * Properties of a Dir.
             * @typedef {Object} lebai.db.Dir.$Properties
             * @property {string|null} [name] Dir name
             * @property {number|null} [id] Dir id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Dir.
             * @memberof lebai.db
             * @interface IDir
             * @augments lebai.db.Dir.$Properties
             * @deprecated Use lebai.db.Dir.$Properties instead.
             */

            /**
             * Shape of a Dir.
             * @typedef {lebai.db.Dir.$Properties} lebai.db.Dir.$Shape
             */

            /**
             * Constructs a new Dir.
             * @memberof lebai.db
             * @classdesc Represents a Dir.
             * @constructor
             * @param {lebai.db.Dir.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Dir = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Dir name.
             * @member {string} name
             * @memberof lebai.db.Dir
             * @instance
             */
            Dir.prototype.name = "";

            /**
             * Dir id.
             * @member {number} id
             * @memberof lebai.db.Dir
             * @instance
             */
            Dir.prototype.id = 0;

            /**
             * Creates a new Dir instance using the specified properties.
             * @function create
             * @memberof lebai.db.Dir
             * @static
             * @param {lebai.db.Dir.$Properties=} [properties] Properties to set
             * @returns {lebai.db.Dir} Dir instance
             * @type {{
             *   (properties: lebai.db.Dir.$Shape): lebai.db.Dir & lebai.db.Dir.$Shape;
             *   (properties?: lebai.db.Dir.$Properties): lebai.db.Dir;
             * }}
             */
            Dir.create = function(properties) {
                return new Dir(properties);
            };

            /**
             * Encodes the specified Dir message. Does not implicitly {@link lebai.db.Dir.verify|verify} messages.
             * @function encode
             * @memberof lebai.db.Dir
             * @static
             * @param {lebai.db.Dir.$Properties} message Dir message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Dir.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Dir message, length delimited. Does not implicitly {@link lebai.db.Dir.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.db.Dir
             * @static
             * @param {lebai.db.Dir.$Properties} message Dir message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Dir.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Dir message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.db.Dir
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.db.Dir & lebai.db.Dir.$Shape} Dir
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Dir.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.db.Dir();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Dir message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.db.Dir
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.db.Dir & lebai.db.Dir.$Shape} Dir
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Dir.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Dir message.
             * @function verify
             * @memberof lebai.db.Dir
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Dir.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                return null;
            };

            /**
             * Creates a Dir message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.db.Dir
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.db.Dir} Dir
             */
            Dir.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.db.Dir)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.db.Dir: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.db.Dir();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.id != null)
                    if ($Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a Dir message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.db.Dir
             * @static
             * @param {lebai.db.Dir} message Dir
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Dir.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.id = 0;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    object.id = message.id;
                return object;
            };

            /**
             * Converts this Dir to JSON.
             * @function toJSON
             * @memberof lebai.db.Dir
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Dir.prototype.toJSON = function() {
                return Dir.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Dir
             * @function getTypeUrl
             * @memberof lebai.db.Dir
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Dir.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.db.Dir";
            };

            return Dir;
        })();

        db.UpdateDirRequest = (function() {

            /**
             * Properties of an UpdateDirRequest.
             * @typedef {Object} lebai.db.UpdateDirRequest.$Properties
             * @property {string|null} [from] UpdateDirRequest from
             * @property {string|null} [to] UpdateDirRequest to
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an UpdateDirRequest.
             * @memberof lebai.db
             * @interface IUpdateDirRequest
             * @augments lebai.db.UpdateDirRequest.$Properties
             * @deprecated Use lebai.db.UpdateDirRequest.$Properties instead.
             */

            /**
             * Shape of an UpdateDirRequest.
             * @typedef {lebai.db.UpdateDirRequest.$Properties} lebai.db.UpdateDirRequest.$Shape
             */

            /**
             * Constructs a new UpdateDirRequest.
             * @memberof lebai.db
             * @classdesc Represents an UpdateDirRequest.
             * @constructor
             * @param {lebai.db.UpdateDirRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var UpdateDirRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * UpdateDirRequest from.
             * @member {string} from
             * @memberof lebai.db.UpdateDirRequest
             * @instance
             */
            UpdateDirRequest.prototype.from = "";

            /**
             * UpdateDirRequest to.
             * @member {string} to
             * @memberof lebai.db.UpdateDirRequest
             * @instance
             */
            UpdateDirRequest.prototype.to = "";

            /**
             * Creates a new UpdateDirRequest instance using the specified properties.
             * @function create
             * @memberof lebai.db.UpdateDirRequest
             * @static
             * @param {lebai.db.UpdateDirRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.db.UpdateDirRequest} UpdateDirRequest instance
             * @type {{
             *   (properties: lebai.db.UpdateDirRequest.$Shape): lebai.db.UpdateDirRequest & lebai.db.UpdateDirRequest.$Shape;
             *   (properties?: lebai.db.UpdateDirRequest.$Properties): lebai.db.UpdateDirRequest;
             * }}
             */
            UpdateDirRequest.create = function(properties) {
                return new UpdateDirRequest(properties);
            };

            /**
             * Encodes the specified UpdateDirRequest message. Does not implicitly {@link lebai.db.UpdateDirRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.db.UpdateDirRequest
             * @static
             * @param {lebai.db.UpdateDirRequest.$Properties} message UpdateDirRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UpdateDirRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.from != null && $Object.hasOwnProperty.call(message, "from") && message.from !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.from);
                if (message.to != null && $Object.hasOwnProperty.call(message, "to") && message.to !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.to);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified UpdateDirRequest message, length delimited. Does not implicitly {@link lebai.db.UpdateDirRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.db.UpdateDirRequest
             * @static
             * @param {lebai.db.UpdateDirRequest.$Properties} message UpdateDirRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UpdateDirRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an UpdateDirRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.db.UpdateDirRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.db.UpdateDirRequest & lebai.db.UpdateDirRequest.$Shape} UpdateDirRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UpdateDirRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.db.UpdateDirRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.from = value;
                            else
                                delete message.from;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.to = value;
                            else
                                delete message.to;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an UpdateDirRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.db.UpdateDirRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.db.UpdateDirRequest & lebai.db.UpdateDirRequest.$Shape} UpdateDirRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UpdateDirRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an UpdateDirRequest message.
             * @function verify
             * @memberof lebai.db.UpdateDirRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            UpdateDirRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.from != null && $Object.hasOwnProperty.call(message, "from"))
                    if (!$util.isString(message.from))
                        return "from: string expected";
                if (message.to != null && $Object.hasOwnProperty.call(message, "to"))
                    if (!$util.isString(message.to))
                        return "to: string expected";
                return null;
            };

            /**
             * Creates an UpdateDirRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.db.UpdateDirRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.db.UpdateDirRequest} UpdateDirRequest
             */
            UpdateDirRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.db.UpdateDirRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.db.UpdateDirRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.db.UpdateDirRequest();
                if (object.from != null)
                    if (typeof object.from !== "string" || object.from.length)
                        message.from = $String(object.from);
                if (object.to != null)
                    if (typeof object.to !== "string" || object.to.length)
                        message.to = $String(object.to);
                return message;
            };

            /**
             * Creates a plain object from an UpdateDirRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.db.UpdateDirRequest
             * @static
             * @param {lebai.db.UpdateDirRequest} message UpdateDirRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            UpdateDirRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.from = "";
                    object.to = "";
                }
                if (message.from != null && $Object.hasOwnProperty.call(message, "from"))
                    object.from = message.from;
                if (message.to != null && $Object.hasOwnProperty.call(message, "to"))
                    object.to = message.to;
                return object;
            };

            /**
             * Converts this UpdateDirRequest to JSON.
             * @function toJSON
             * @memberof lebai.db.UpdateDirRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            UpdateDirRequest.prototype.toJSON = function() {
                return UpdateDirRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for UpdateDirRequest
             * @function getTypeUrl
             * @memberof lebai.db.UpdateDirRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            UpdateDirRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.db.UpdateDirRequest";
            };

            return UpdateDirRequest;
        })();

        db.Dirs = (function() {

            /**
             * Properties of a Dirs.
             * @typedef {Object} lebai.db.Dirs.$Properties
             * @property {Array.<lebai.db.Dir.$Properties>|null} [dirs] Dirs dirs
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Dirs.
             * @memberof lebai.db
             * @interface IDirs
             * @augments lebai.db.Dirs.$Properties
             * @deprecated Use lebai.db.Dirs.$Properties instead.
             */

            /**
             * Shape of a Dirs.
             * @typedef {lebai.db.Dirs.$Properties} lebai.db.Dirs.$Shape
             */

            /**
             * Constructs a new Dirs.
             * @memberof lebai.db
             * @classdesc Represents a Dirs.
             * @constructor
             * @param {lebai.db.Dirs.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Dirs = function (properties) {
                this.dirs = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Dirs dirs.
             * @member {Array.<lebai.db.Dir.$Properties>} dirs
             * @memberof lebai.db.Dirs
             * @instance
             */
            Dirs.prototype.dirs = $util.emptyArray;

            /**
             * Creates a new Dirs instance using the specified properties.
             * @function create
             * @memberof lebai.db.Dirs
             * @static
             * @param {lebai.db.Dirs.$Properties=} [properties] Properties to set
             * @returns {lebai.db.Dirs} Dirs instance
             * @type {{
             *   (properties: lebai.db.Dirs.$Shape): lebai.db.Dirs & lebai.db.Dirs.$Shape;
             *   (properties?: lebai.db.Dirs.$Properties): lebai.db.Dirs;
             * }}
             */
            Dirs.create = function(properties) {
                return new Dirs(properties);
            };

            /**
             * Encodes the specified Dirs message. Does not implicitly {@link lebai.db.Dirs.verify|verify} messages.
             * @function encode
             * @memberof lebai.db.Dirs
             * @static
             * @param {lebai.db.Dirs.$Properties} message Dirs message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Dirs.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.dirs != null && message.dirs.length)
                    for (var i = 0; i < message.dirs.length; ++i)
                        $root.lebai.db.Dir.encode(message.dirs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Dirs message, length delimited. Does not implicitly {@link lebai.db.Dirs.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.db.Dirs
             * @static
             * @param {lebai.db.Dirs.$Properties} message Dirs message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Dirs.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Dirs message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.db.Dirs
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.db.Dirs & lebai.db.Dirs.$Shape} Dirs
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Dirs.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.db.Dirs();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.dirs && message.dirs.length))
                                message.dirs = [];
                            message.dirs.push($root.lebai.db.Dir.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Dirs message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.db.Dirs
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.db.Dirs & lebai.db.Dirs.$Shape} Dirs
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Dirs.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Dirs message.
             * @function verify
             * @memberof lebai.db.Dirs
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Dirs.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.dirs != null && $Object.hasOwnProperty.call(message, "dirs")) {
                    if (!$Array.isArray(message.dirs))
                        return "dirs: array expected";
                    for (var i = 0; i < message.dirs.length; ++i) {
                        var error = $root.lebai.db.Dir.verify(message.dirs[i], _depth + 1);
                        if (error)
                            return "dirs." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Dirs message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.db.Dirs
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.db.Dirs} Dirs
             */
            Dirs.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.db.Dirs)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.db.Dirs: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.db.Dirs();
                if (object.dirs) {
                    if (!$Array.isArray(object.dirs))
                        throw $TypeError(".lebai.db.Dirs.dirs: array expected");
                    message.dirs = $Array(object.dirs.length);
                    for (var i = 0; i < object.dirs.length; ++i) {
                        if (!$util.isObject(object.dirs[i]))
                            throw $TypeError(".lebai.db.Dirs.dirs: object expected");
                        message.dirs[i] = $root.lebai.db.Dir.fromObject(object.dirs[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Dirs message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.db.Dirs
             * @static
             * @param {lebai.db.Dirs} message Dirs
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Dirs.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.dirs = [];
                if (message.dirs && message.dirs.length) {
                    object.dirs = $Array(message.dirs.length);
                    for (var j = 0; j < message.dirs.length; ++j)
                        object.dirs[j] = $root.lebai.db.Dir.toObject(message.dirs[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this Dirs to JSON.
             * @function toJSON
             * @memberof lebai.db.Dirs
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Dirs.prototype.toJSON = function() {
                return Dirs.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Dirs
             * @function getTypeUrl
             * @memberof lebai.db.Dirs
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Dirs.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.db.Dirs";
            };

            return Dirs;
        })();

        db.DbService = (function() {

            /**
             * Constructs a new DbService service.
             * @memberof lebai.db
             * @classdesc Represents a DbService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var DbService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(DbService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: DbService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new DbService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.db.DbService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {DbService} RPC service. Useful where requests and/or responses are streamed.
             */
            DbService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.db.DbService#createDir}.
             * @memberof lebai.db.DbService
             * @typedef CreateDirCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls CreateDir.
             * @memberof lebai.db.DbService
             * @typedef CreateDir
             * @type {{
             *   (request: lebai.db.IDir, callback: lebai.db.DbService.CreateDirCallback): void;
             *   (request: lebai.db.IDir): Promise<google.protobuf.Empty>;
             *   readonly name: "CreateDir";
             *   readonly path: "/lebai.db.DbService/CreateDir";
             *   readonly requestType: "Dir";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls CreateDir.
             * @name lebai.db.DbService#createDir
             * @type {lebai.db.DbService.CreateDir}
             */
            $Object.defineProperties(DbService.prototype.createDir = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, DbService.prototype.createDir, $root.lebai.db.Dir, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "CreateDir" },
                path: { value: "/lebai.db.DbService/CreateDir" },
                requestType: { value: "Dir" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.db.DbService#updateDir}.
             * @memberof lebai.db.DbService
             * @typedef UpdateDirCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls UpdateDir.
             * @memberof lebai.db.DbService
             * @typedef UpdateDir
             * @type {{
             *   (request: lebai.db.IUpdateDirRequest, callback: lebai.db.DbService.UpdateDirCallback): void;
             *   (request: lebai.db.IUpdateDirRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "UpdateDir";
             *   readonly path: "/lebai.db.DbService/UpdateDir";
             *   readonly requestType: "UpdateDirRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls UpdateDir.
             * @name lebai.db.DbService#updateDir
             * @type {lebai.db.DbService.UpdateDir}
             */
            $Object.defineProperties(DbService.prototype.updateDir = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, DbService.prototype.updateDir, $root.lebai.db.UpdateDirRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "UpdateDir" },
                path: { value: "/lebai.db.DbService/UpdateDir" },
                requestType: { value: "UpdateDirRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.db.DbService#getDirs}.
             * @memberof lebai.db.DbService
             * @typedef GetDirsCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.db.Dirs} [response] Dirs
             */

            /**
             * Calls GetDirs.
             * @memberof lebai.db.DbService
             * @typedef GetDirs
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.db.DbService.GetDirsCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.db.Dirs>;
             *   readonly name: "GetDirs";
             *   readonly path: "/lebai.db.DbService/GetDirs";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "Dirs";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetDirs.
             * @name lebai.db.DbService#getDirs
             * @type {lebai.db.DbService.GetDirs}
             */
            $Object.defineProperties(DbService.prototype.getDirs = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, DbService.prototype.getDirs, $root.google.protobuf.Empty, $root.lebai.db.Dirs, request, callback);
            }, {
                name: { value: "GetDirs" },
                path: { value: "/lebai.db.DbService/GetDirs" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "Dirs" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return DbService;
        })();

        return db;
    })();

    lebai.dynamic = (function() {

        /**
         * Namespace dynamic.
         * @memberof lebai
         * @namespace
         */
        var dynamic = {};

        dynamic.Payload = (function() {

            /**
             * Properties of a Payload.
             * @typedef {Object} lebai.dynamic.Payload.$Properties
             * @property {number|null} [mass] Payload mass
             * @property {lebai.posture.Position.$Properties|null} [cog] Payload cog
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Payload.
             * @memberof lebai.dynamic
             * @interface IPayload
             * @augments lebai.dynamic.Payload.$Properties
             * @deprecated Use lebai.dynamic.Payload.$Properties instead.
             */

            /**
             * Shape of a Payload.
             * @typedef {lebai.dynamic.Payload.$Properties} lebai.dynamic.Payload.$Shape
             */

            /**
             * Constructs a new Payload.
             * @memberof lebai.dynamic
             * @classdesc Represents a Payload.
             * @constructor
             * @param {lebai.dynamic.Payload.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Payload = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Payload mass.
             * @member {number} mass
             * @memberof lebai.dynamic.Payload
             * @instance
             */
            Payload.prototype.mass = 0;

            /**
             * Payload cog.
             * @member {lebai.posture.Position.$Properties|null|undefined} cog
             * @memberof lebai.dynamic.Payload
             * @instance
             */
            Payload.prototype.cog = null;

            /**
             * Creates a new Payload instance using the specified properties.
             * @function create
             * @memberof lebai.dynamic.Payload
             * @static
             * @param {lebai.dynamic.Payload.$Properties=} [properties] Properties to set
             * @returns {lebai.dynamic.Payload} Payload instance
             * @type {{
             *   (properties: lebai.dynamic.Payload.$Shape): lebai.dynamic.Payload & lebai.dynamic.Payload.$Shape;
             *   (properties?: lebai.dynamic.Payload.$Properties): lebai.dynamic.Payload;
             * }}
             */
            Payload.create = function(properties) {
                return new Payload(properties);
            };

            /**
             * Encodes the specified Payload message. Does not implicitly {@link lebai.dynamic.Payload.verify|verify} messages.
             * @function encode
             * @memberof lebai.dynamic.Payload
             * @static
             * @param {lebai.dynamic.Payload.$Properties} message Payload message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Payload.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.mass != null && $Object.hasOwnProperty.call(message, "mass") && !$Object.is(message.mass, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.mass);
                if (message.cog != null && $Object.hasOwnProperty.call(message, "cog"))
                    $root.lebai.posture.Position.encode(message.cog, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Payload message, length delimited. Does not implicitly {@link lebai.dynamic.Payload.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.dynamic.Payload
             * @static
             * @param {lebai.dynamic.Payload.$Properties} message Payload message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Payload.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Payload message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.dynamic.Payload
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.dynamic.Payload & lebai.dynamic.Payload.$Shape} Payload
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Payload.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.dynamic.Payload();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.mass = value;
                            else
                                delete message.mass;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.cog = $root.lebai.posture.Position.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cog);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Payload message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.dynamic.Payload
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.dynamic.Payload & lebai.dynamic.Payload.$Shape} Payload
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Payload.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Payload message.
             * @function verify
             * @memberof lebai.dynamic.Payload
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Payload.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.mass != null && $Object.hasOwnProperty.call(message, "mass"))
                    if (typeof message.mass !== "number")
                        return "mass: number expected";
                if (message.cog != null && $Object.hasOwnProperty.call(message, "cog")) {
                    var error = $root.lebai.posture.Position.verify(message.cog, _depth + 1);
                    if (error)
                        return "cog." + error;
                }
                return null;
            };

            /**
             * Creates a Payload message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.dynamic.Payload
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.dynamic.Payload} Payload
             */
            Payload.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.dynamic.Payload)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.dynamic.Payload: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.dynamic.Payload();
                if (object.mass != null)
                    if (!$Object.is($Number(object.mass), 0))
                        message.mass = $Number(object.mass);
                if (object.cog != null) {
                    if (!$util.isObject(object.cog))
                        throw $TypeError(".lebai.dynamic.Payload.cog: object expected");
                    message.cog = $root.lebai.posture.Position.fromObject(object.cog, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Payload message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.dynamic.Payload
             * @static
             * @param {lebai.dynamic.Payload} message Payload
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Payload.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.mass = 0;
                    object.cog = null;
                }
                if (message.mass != null && $Object.hasOwnProperty.call(message, "mass"))
                    object.mass = options.json && !$isFinite(message.mass) ? $String(message.mass) : message.mass;
                if (message.cog != null && $Object.hasOwnProperty.call(message, "cog"))
                    object.cog = $root.lebai.posture.Position.toObject(message.cog, options, _depth + 1);
                return object;
            };

            /**
             * Converts this Payload to JSON.
             * @function toJSON
             * @memberof lebai.dynamic.Payload
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Payload.prototype.toJSON = function() {
                return Payload.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Payload
             * @function getTypeUrl
             * @memberof lebai.dynamic.Payload
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Payload.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.dynamic.Payload";
            };

            return Payload;
        })();

        dynamic.SetPayloadRequest = (function() {

            /**
             * Properties of a SetPayloadRequest.
             * @typedef {Object} lebai.dynamic.SetPayloadRequest.$Properties
             * @property {number|null} [mass] SetPayloadRequest mass
             * @property {lebai.posture.Position.$Properties|null} [cog] SetPayloadRequest cog
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetPayloadRequest.
             * @memberof lebai.dynamic
             * @interface ISetPayloadRequest
             * @augments lebai.dynamic.SetPayloadRequest.$Properties
             * @deprecated Use lebai.dynamic.SetPayloadRequest.$Properties instead.
             */

            /**
             * Shape of a SetPayloadRequest.
             * @typedef {lebai.dynamic.SetPayloadRequest.$Properties} lebai.dynamic.SetPayloadRequest.$Shape
             */

            /**
             * Constructs a new SetPayloadRequest.
             * @memberof lebai.dynamic
             * @classdesc Represents a SetPayloadRequest.
             * @constructor
             * @param {lebai.dynamic.SetPayloadRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetPayloadRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetPayloadRequest mass.
             * @member {number|null|undefined} mass
             * @memberof lebai.dynamic.SetPayloadRequest
             * @instance
             */
            SetPayloadRequest.prototype.mass = null;

            /**
             * SetPayloadRequest cog.
             * @member {lebai.posture.Position.$Properties|null|undefined} cog
             * @memberof lebai.dynamic.SetPayloadRequest
             * @instance
             */
            SetPayloadRequest.prototype.cog = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SetPayloadRequest.prototype, "_mass", {
                get: $util.oneOfGetter($oneOfFields = ["mass"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SetPayloadRequest instance using the specified properties.
             * @function create
             * @memberof lebai.dynamic.SetPayloadRequest
             * @static
             * @param {lebai.dynamic.SetPayloadRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.dynamic.SetPayloadRequest} SetPayloadRequest instance
             * @type {{
             *   (properties: lebai.dynamic.SetPayloadRequest.$Shape): lebai.dynamic.SetPayloadRequest & lebai.dynamic.SetPayloadRequest.$Shape;
             *   (properties?: lebai.dynamic.SetPayloadRequest.$Properties): lebai.dynamic.SetPayloadRequest;
             * }}
             */
            SetPayloadRequest.create = function(properties) {
                return new SetPayloadRequest(properties);
            };

            /**
             * Encodes the specified SetPayloadRequest message. Does not implicitly {@link lebai.dynamic.SetPayloadRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.dynamic.SetPayloadRequest
             * @static
             * @param {lebai.dynamic.SetPayloadRequest.$Properties} message SetPayloadRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetPayloadRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.mass != null && $Object.hasOwnProperty.call(message, "mass"))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.mass);
                if (message.cog != null && $Object.hasOwnProperty.call(message, "cog"))
                    $root.lebai.posture.Position.encode(message.cog, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetPayloadRequest message, length delimited. Does not implicitly {@link lebai.dynamic.SetPayloadRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.dynamic.SetPayloadRequest
             * @static
             * @param {lebai.dynamic.SetPayloadRequest.$Properties} message SetPayloadRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetPayloadRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetPayloadRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.dynamic.SetPayloadRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.dynamic.SetPayloadRequest & lebai.dynamic.SetPayloadRequest.$Shape} SetPayloadRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetPayloadRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.dynamic.SetPayloadRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            message.mass = reader.double();
                            message._mass = "mass";
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.cog = $root.lebai.posture.Position.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cog);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetPayloadRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.dynamic.SetPayloadRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.dynamic.SetPayloadRequest & lebai.dynamic.SetPayloadRequest.$Shape} SetPayloadRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetPayloadRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetPayloadRequest message.
             * @function verify
             * @memberof lebai.dynamic.SetPayloadRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetPayloadRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.mass != null && $Object.hasOwnProperty.call(message, "mass")) {
                    properties._mass = 1;
                    if (typeof message.mass !== "number")
                        return "mass: number expected";
                }
                if (message.cog != null && $Object.hasOwnProperty.call(message, "cog")) {
                    var error = $root.lebai.posture.Position.verify(message.cog, _depth + 1);
                    if (error)
                        return "cog." + error;
                }
                return null;
            };

            /**
             * Creates a SetPayloadRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.dynamic.SetPayloadRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.dynamic.SetPayloadRequest} SetPayloadRequest
             */
            SetPayloadRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.dynamic.SetPayloadRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.dynamic.SetPayloadRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.dynamic.SetPayloadRequest();
                if (object.mass != null)
                    message.mass = $Number(object.mass);
                if (object.cog != null) {
                    if (!$util.isObject(object.cog))
                        throw $TypeError(".lebai.dynamic.SetPayloadRequest.cog: object expected");
                    message.cog = $root.lebai.posture.Position.fromObject(object.cog, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a SetPayloadRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.dynamic.SetPayloadRequest
             * @static
             * @param {lebai.dynamic.SetPayloadRequest} message SetPayloadRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetPayloadRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.cog = null;
                if (message.mass != null && $Object.hasOwnProperty.call(message, "mass"))
                    object.mass = options.json && !$isFinite(message.mass) ? $String(message.mass) : message.mass;
                if (message.cog != null && $Object.hasOwnProperty.call(message, "cog"))
                    object.cog = $root.lebai.posture.Position.toObject(message.cog, options, _depth + 1);
                return object;
            };

            /**
             * Converts this SetPayloadRequest to JSON.
             * @function toJSON
             * @memberof lebai.dynamic.SetPayloadRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetPayloadRequest.prototype.toJSON = function() {
                return SetPayloadRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetPayloadRequest
             * @function getTypeUrl
             * @memberof lebai.dynamic.SetPayloadRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetPayloadRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.dynamic.SetPayloadRequest";
            };

            return SetPayloadRequest;
        })();

        dynamic.SavePayloadRequest = (function() {

            /**
             * Properties of a SavePayloadRequest.
             * @typedef {Object} lebai.dynamic.SavePayloadRequest.$Properties
             * @property {string|null} [name] SavePayloadRequest name
             * @property {lebai.dynamic.Payload.$Properties|null} [data] SavePayloadRequest data
             * @property {string|null} [dir] SavePayloadRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SavePayloadRequest.
             * @memberof lebai.dynamic
             * @interface ISavePayloadRequest
             * @augments lebai.dynamic.SavePayloadRequest.$Properties
             * @deprecated Use lebai.dynamic.SavePayloadRequest.$Properties instead.
             */

            /**
             * Shape of a SavePayloadRequest.
             * @typedef {lebai.dynamic.SavePayloadRequest.$Properties} lebai.dynamic.SavePayloadRequest.$Shape
             */

            /**
             * Constructs a new SavePayloadRequest.
             * @memberof lebai.dynamic
             * @classdesc Represents a SavePayloadRequest.
             * @constructor
             * @param {lebai.dynamic.SavePayloadRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SavePayloadRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SavePayloadRequest name.
             * @member {string} name
             * @memberof lebai.dynamic.SavePayloadRequest
             * @instance
             */
            SavePayloadRequest.prototype.name = "";

            /**
             * SavePayloadRequest data.
             * @member {lebai.dynamic.Payload.$Properties|null|undefined} data
             * @memberof lebai.dynamic.SavePayloadRequest
             * @instance
             */
            SavePayloadRequest.prototype.data = null;

            /**
             * SavePayloadRequest dir.
             * @member {string} dir
             * @memberof lebai.dynamic.SavePayloadRequest
             * @instance
             */
            SavePayloadRequest.prototype.dir = "";

            /**
             * Creates a new SavePayloadRequest instance using the specified properties.
             * @function create
             * @memberof lebai.dynamic.SavePayloadRequest
             * @static
             * @param {lebai.dynamic.SavePayloadRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.dynamic.SavePayloadRequest} SavePayloadRequest instance
             * @type {{
             *   (properties: lebai.dynamic.SavePayloadRequest.$Shape): lebai.dynamic.SavePayloadRequest & lebai.dynamic.SavePayloadRequest.$Shape;
             *   (properties?: lebai.dynamic.SavePayloadRequest.$Properties): lebai.dynamic.SavePayloadRequest;
             * }}
             */
            SavePayloadRequest.create = function(properties) {
                return new SavePayloadRequest(properties);
            };

            /**
             * Encodes the specified SavePayloadRequest message. Does not implicitly {@link lebai.dynamic.SavePayloadRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.dynamic.SavePayloadRequest
             * @static
             * @param {lebai.dynamic.SavePayloadRequest.$Properties} message SavePayloadRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SavePayloadRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    $root.lebai.dynamic.Payload.encode(message.data, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SavePayloadRequest message, length delimited. Does not implicitly {@link lebai.dynamic.SavePayloadRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.dynamic.SavePayloadRequest
             * @static
             * @param {lebai.dynamic.SavePayloadRequest.$Properties} message SavePayloadRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SavePayloadRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SavePayloadRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.dynamic.SavePayloadRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.dynamic.SavePayloadRequest & lebai.dynamic.SavePayloadRequest.$Shape} SavePayloadRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SavePayloadRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.dynamic.SavePayloadRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.data = $root.lebai.dynamic.Payload.decode(reader, reader.uint32(), $undefined, _depth + 1, message.data);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SavePayloadRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.dynamic.SavePayloadRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.dynamic.SavePayloadRequest & lebai.dynamic.SavePayloadRequest.$Shape} SavePayloadRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SavePayloadRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SavePayloadRequest message.
             * @function verify
             * @memberof lebai.dynamic.SavePayloadRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SavePayloadRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    var error = $root.lebai.dynamic.Payload.verify(message.data, _depth + 1);
                    if (error)
                        return "data." + error;
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates a SavePayloadRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.dynamic.SavePayloadRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.dynamic.SavePayloadRequest} SavePayloadRequest
             */
            SavePayloadRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.dynamic.SavePayloadRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.dynamic.SavePayloadRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.dynamic.SavePayloadRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.data != null) {
                    if (!$util.isObject(object.data))
                        throw $TypeError(".lebai.dynamic.SavePayloadRequest.data: object expected");
                    message.data = $root.lebai.dynamic.Payload.fromObject(object.data, _depth + 1);
                }
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from a SavePayloadRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.dynamic.SavePayloadRequest
             * @static
             * @param {lebai.dynamic.SavePayloadRequest} message SavePayloadRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SavePayloadRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.data = null;
                    object.dir = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = $root.lebai.dynamic.Payload.toObject(message.data, options, _depth + 1);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                return object;
            };

            /**
             * Converts this SavePayloadRequest to JSON.
             * @function toJSON
             * @memberof lebai.dynamic.SavePayloadRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SavePayloadRequest.prototype.toJSON = function() {
                return SavePayloadRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SavePayloadRequest
             * @function getTypeUrl
             * @memberof lebai.dynamic.SavePayloadRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SavePayloadRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.dynamic.SavePayloadRequest";
            };

            return SavePayloadRequest;
        })();

        dynamic.DynamicService = (function() {

            /**
             * Constructs a new DynamicService service.
             * @memberof lebai.dynamic
             * @classdesc Represents a DynamicService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var DynamicService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(DynamicService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: DynamicService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new DynamicService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.dynamic.DynamicService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {DynamicService} RPC service. Useful where requests and/or responses are streamed.
             */
            DynamicService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.dynamic.DynamicService#savePayload}.
             * @memberof lebai.dynamic.DynamicService
             * @typedef SavePayloadCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SavePayload.
             * @memberof lebai.dynamic.DynamicService
             * @typedef SavePayload
             * @type {{
             *   (request: lebai.dynamic.ISavePayloadRequest, callback: lebai.dynamic.DynamicService.SavePayloadCallback): void;
             *   (request: lebai.dynamic.ISavePayloadRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SavePayload";
             *   readonly path: "/lebai.dynamic.DynamicService/SavePayload";
             *   readonly requestType: "SavePayloadRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SavePayload.
             * @name lebai.dynamic.DynamicService#savePayload
             * @type {lebai.dynamic.DynamicService.SavePayload}
             */
            $Object.defineProperties(DynamicService.prototype.savePayload = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, DynamicService.prototype.savePayload, $root.lebai.dynamic.SavePayloadRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SavePayload" },
                path: { value: "/lebai.dynamic.DynamicService/SavePayload" },
                requestType: { value: "SavePayloadRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.dynamic.DynamicService#loadPayload}.
             * @memberof lebai.dynamic.DynamicService
             * @typedef LoadPayloadCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.dynamic.Payload} [response] Payload
             */

            /**
             * Calls LoadPayload.
             * @memberof lebai.dynamic.DynamicService
             * @typedef LoadPayload
             * @type {{
             *   (request: lebai.db.ILoadRequest, callback: lebai.dynamic.DynamicService.LoadPayloadCallback): void;
             *   (request: lebai.db.ILoadRequest): Promise<lebai.dynamic.Payload>;
             *   readonly name: "LoadPayload";
             *   readonly path: "/lebai.dynamic.DynamicService/LoadPayload";
             *   readonly requestType: "db.LoadRequest";
             *   readonly responseType: "Payload";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadPayload.
             * @name lebai.dynamic.DynamicService#loadPayload
             * @type {lebai.dynamic.DynamicService.LoadPayload}
             */
            $Object.defineProperties(DynamicService.prototype.loadPayload = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, DynamicService.prototype.loadPayload, $root.lebai.db.LoadRequest, $root.lebai.dynamic.Payload, request, callback);
            }, {
                name: { value: "LoadPayload" },
                path: { value: "/lebai.dynamic.DynamicService/LoadPayload" },
                requestType: { value: "db.LoadRequest" },
                responseType: { value: "Payload" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.dynamic.DynamicService#loadPayloadList}.
             * @memberof lebai.dynamic.DynamicService
             * @typedef LoadPayloadListCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.db.LoadListResponse} [response] LoadListResponse
             */

            /**
             * Calls LoadPayloadList.
             * @memberof lebai.dynamic.DynamicService
             * @typedef LoadPayloadList
             * @type {{
             *   (request: lebai.db.ILoadListRequest, callback: lebai.dynamic.DynamicService.LoadPayloadListCallback): void;
             *   (request: lebai.db.ILoadListRequest): Promise<lebai.db.LoadListResponse>;
             *   readonly name: "LoadPayloadList";
             *   readonly path: "/lebai.dynamic.DynamicService/LoadPayloadList";
             *   readonly requestType: "db.LoadListRequest";
             *   readonly responseType: "db.LoadListResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadPayloadList.
             * @name lebai.dynamic.DynamicService#loadPayloadList
             * @type {lebai.dynamic.DynamicService.LoadPayloadList}
             */
            $Object.defineProperties(DynamicService.prototype.loadPayloadList = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, DynamicService.prototype.loadPayloadList, $root.lebai.db.LoadListRequest, $root.lebai.db.LoadListResponse, request, callback);
            }, {
                name: { value: "LoadPayloadList" },
                path: { value: "/lebai.dynamic.DynamicService/LoadPayloadList" },
                requestType: { value: "db.LoadListRequest" },
                responseType: { value: "db.LoadListResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.dynamic.DynamicService#setPayload}.
             * @memberof lebai.dynamic.DynamicService
             * @typedef SetPayloadCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetPayload.
             * @memberof lebai.dynamic.DynamicService
             * @typedef SetPayload
             * @type {{
             *   (request: lebai.dynamic.ISetPayloadRequest, callback: lebai.dynamic.DynamicService.SetPayloadCallback): void;
             *   (request: lebai.dynamic.ISetPayloadRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetPayload";
             *   readonly path: "/lebai.dynamic.DynamicService/SetPayload";
             *   readonly requestType: "SetPayloadRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetPayload.
             * @name lebai.dynamic.DynamicService#setPayload
             * @type {lebai.dynamic.DynamicService.SetPayload}
             */
            $Object.defineProperties(DynamicService.prototype.setPayload = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, DynamicService.prototype.setPayload, $root.lebai.dynamic.SetPayloadRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetPayload" },
                path: { value: "/lebai.dynamic.DynamicService/SetPayload" },
                requestType: { value: "SetPayloadRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.dynamic.DynamicService#getPayload}.
             * @memberof lebai.dynamic.DynamicService
             * @typedef GetPayloadCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.dynamic.Payload} [response] Payload
             */

            /**
             * Calls GetPayload.
             * @memberof lebai.dynamic.DynamicService
             * @typedef GetPayload
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.dynamic.DynamicService.GetPayloadCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.dynamic.Payload>;
             *   readonly name: "GetPayload";
             *   readonly path: "/lebai.dynamic.DynamicService/GetPayload";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "Payload";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetPayload.
             * @name lebai.dynamic.DynamicService#getPayload
             * @type {lebai.dynamic.DynamicService.GetPayload}
             */
            $Object.defineProperties(DynamicService.prototype.getPayload = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, DynamicService.prototype.getPayload, $root.google.protobuf.Empty, $root.lebai.dynamic.Payload, request, callback);
            }, {
                name: { value: "GetPayload" },
                path: { value: "/lebai.dynamic.DynamicService/GetPayload" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "Payload" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.dynamic.DynamicService#setGravity}.
             * @memberof lebai.dynamic.DynamicService
             * @typedef SetGravityCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetGravity.
             * @memberof lebai.dynamic.DynamicService
             * @typedef SetGravity
             * @type {{
             *   (request: lebai.posture.IPosition, callback: lebai.dynamic.DynamicService.SetGravityCallback): void;
             *   (request: lebai.posture.IPosition): Promise<google.protobuf.Empty>;
             *   readonly name: "SetGravity";
             *   readonly path: "/lebai.dynamic.DynamicService/SetGravity";
             *   readonly requestType: "posture.Position";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetGravity.
             * @name lebai.dynamic.DynamicService#setGravity
             * @type {lebai.dynamic.DynamicService.SetGravity}
             */
            $Object.defineProperties(DynamicService.prototype.setGravity = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, DynamicService.prototype.setGravity, $root.lebai.posture.Position, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetGravity" },
                path: { value: "/lebai.dynamic.DynamicService/SetGravity" },
                requestType: { value: "posture.Position" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.dynamic.DynamicService#getGravity}.
             * @memberof lebai.dynamic.DynamicService
             * @typedef GetGravityCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.posture.Position} [response] Position
             */

            /**
             * Calls GetGravity.
             * @memberof lebai.dynamic.DynamicService
             * @typedef GetGravity
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.dynamic.DynamicService.GetGravityCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.posture.Position>;
             *   readonly name: "GetGravity";
             *   readonly path: "/lebai.dynamic.DynamicService/GetGravity";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "posture.Position";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetGravity.
             * @name lebai.dynamic.DynamicService#getGravity
             * @type {lebai.dynamic.DynamicService.GetGravity}
             */
            $Object.defineProperties(DynamicService.prototype.getGravity = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, DynamicService.prototype.getGravity, $root.google.protobuf.Empty, $root.lebai.posture.Position, request, callback);
            }, {
                name: { value: "GetGravity" },
                path: { value: "/lebai.dynamic.DynamicService/GetGravity" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "posture.Position" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return DynamicService;
        })();

        return dynamic;
    })();

    lebai.posture = (function() {

        /**
         * Namespace posture.
         * @memberof lebai
         * @namespace
         */
        var posture = {};

        posture.JointPose = (function() {

            /**
             * Properties of a JointPose.
             * @typedef {Object} lebai.posture.JointPose.$Properties
             * @property {Array.<number>|null} [joint] JointPose joint
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a JointPose.
             * @memberof lebai.posture
             * @interface IJointPose
             * @augments lebai.posture.JointPose.$Properties
             * @deprecated Use lebai.posture.JointPose.$Properties instead.
             */

            /**
             * Shape of a JointPose.
             * @typedef {lebai.posture.JointPose.$Properties} lebai.posture.JointPose.$Shape
             */

            /**
             * Constructs a new JointPose.
             * @memberof lebai.posture
             * @classdesc Represents a JointPose.
             * @constructor
             * @param {lebai.posture.JointPose.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var JointPose = function (properties) {
                this.joint = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * JointPose joint.
             * @member {Array.<number>} joint
             * @memberof lebai.posture.JointPose
             * @instance
             */
            JointPose.prototype.joint = $util.emptyArray;

            /**
             * Creates a new JointPose instance using the specified properties.
             * @function create
             * @memberof lebai.posture.JointPose
             * @static
             * @param {lebai.posture.JointPose.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.JointPose} JointPose instance
             * @type {{
             *   (properties: lebai.posture.JointPose.$Shape): lebai.posture.JointPose & lebai.posture.JointPose.$Shape;
             *   (properties?: lebai.posture.JointPose.$Properties): lebai.posture.JointPose;
             * }}
             */
            JointPose.create = function(properties) {
                return new JointPose(properties);
            };

            /**
             * Encodes the specified JointPose message. Does not implicitly {@link lebai.posture.JointPose.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.JointPose
             * @static
             * @param {lebai.posture.JointPose.$Properties} message JointPose message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JointPose.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.joint != null && message.joint.length)
                    writer.uint32(/* id 1, wireType 2 =*/10).doubles(message.joint);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified JointPose message, length delimited. Does not implicitly {@link lebai.posture.JointPose.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.JointPose
             * @static
             * @param {lebai.posture.JointPose.$Properties} message JointPose message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JointPose.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a JointPose message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.JointPose
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.JointPose & lebai.posture.JointPose.$Shape} JointPose
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JointPose.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.JointPose();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.joint && message.joint.length))
                                    message.joint = [];
                                reader.doubles(message.joint);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.joint && message.joint.length))
                                message.joint = [];
                            message.joint.push(reader.double());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a JointPose message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.JointPose
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.JointPose & lebai.posture.JointPose.$Shape} JointPose
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JointPose.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a JointPose message.
             * @function verify
             * @memberof lebai.posture.JointPose
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            JointPose.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.joint != null && $Object.hasOwnProperty.call(message, "joint")) {
                    if (!$Array.isArray(message.joint))
                        return "joint: array expected";
                    for (var i = 0; i < message.joint.length; ++i)
                        if (typeof message.joint[i] !== "number")
                            return "joint: number[] expected";
                }
                return null;
            };

            /**
             * Creates a JointPose message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.JointPose
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.JointPose} JointPose
             */
            JointPose.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.JointPose)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.JointPose: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.JointPose();
                if (object.joint) {
                    if (!$Array.isArray(object.joint))
                        throw $TypeError(".lebai.posture.JointPose.joint: array expected");
                    message.joint = $Array(object.joint.length);
                    for (var i = 0; i < object.joint.length; ++i)
                        message.joint[i] = $Number(object.joint[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a JointPose message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.JointPose
             * @static
             * @param {lebai.posture.JointPose} message JointPose
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            JointPose.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.joint = [];
                if (message.joint && message.joint.length) {
                    object.joint = $Array(message.joint.length);
                    for (var j = 0; j < message.joint.length; ++j)
                        object.joint[j] = options.json && !$isFinite(message.joint[j]) ? $String(message.joint[j]) : message.joint[j];
                }
                return object;
            };

            /**
             * Converts this JointPose to JSON.
             * @function toJSON
             * @memberof lebai.posture.JointPose
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            JointPose.prototype.toJSON = function() {
                return JointPose.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for JointPose
             * @function getTypeUrl
             * @memberof lebai.posture.JointPose
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            JointPose.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.JointPose";
            };

            return JointPose;
        })();

        posture.JointFrame = (function() {

            /**
             * Properties of a JointFrame.
             * @typedef {Object} lebai.posture.JointFrame.$Properties
             * @property {lebai.posture.JointFrame.Kind|null} [kind] JointFrame kind
             * @property {lebai.posture.JointPose.$Properties|null} [pose] JointFrame pose
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a JointFrame.
             * @memberof lebai.posture
             * @interface IJointFrame
             * @augments lebai.posture.JointFrame.$Properties
             * @deprecated Use lebai.posture.JointFrame.$Properties instead.
             */

            /**
             * Shape of a JointFrame.
             * @typedef {lebai.posture.JointFrame.$Properties} lebai.posture.JointFrame.$Shape
             */

            /**
             * Constructs a new JointFrame.
             * @memberof lebai.posture
             * @classdesc Represents a JointFrame.
             * @constructor
             * @param {lebai.posture.JointFrame.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var JointFrame = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * JointFrame kind.
             * @member {lebai.posture.JointFrame.Kind} kind
             * @memberof lebai.posture.JointFrame
             * @instance
             */
            JointFrame.prototype.kind = 0;

            /**
             * JointFrame pose.
             * @member {lebai.posture.JointPose.$Properties|null|undefined} pose
             * @memberof lebai.posture.JointFrame
             * @instance
             */
            JointFrame.prototype.pose = null;

            /**
             * Creates a new JointFrame instance using the specified properties.
             * @function create
             * @memberof lebai.posture.JointFrame
             * @static
             * @param {lebai.posture.JointFrame.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.JointFrame} JointFrame instance
             * @type {{
             *   (properties: lebai.posture.JointFrame.$Shape): lebai.posture.JointFrame & lebai.posture.JointFrame.$Shape;
             *   (properties?: lebai.posture.JointFrame.$Properties): lebai.posture.JointFrame;
             * }}
             */
            JointFrame.create = function(properties) {
                return new JointFrame(properties);
            };

            /**
             * Encodes the specified JointFrame message. Does not implicitly {@link lebai.posture.JointFrame.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.JointFrame
             * @static
             * @param {lebai.posture.JointFrame.$Properties} message JointFrame message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JointFrame.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind") && message.kind !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.kind);
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    $root.lebai.posture.JointPose.encode(message.pose, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified JointFrame message, length delimited. Does not implicitly {@link lebai.posture.JointFrame.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.JointFrame
             * @static
             * @param {lebai.posture.JointFrame.$Properties} message JointFrame message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JointFrame.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a JointFrame message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.JointFrame
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.JointFrame & lebai.posture.JointFrame.$Shape} JointFrame
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JointFrame.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.JointFrame();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.kind = value;
                            else
                                delete message.kind;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.pose = $root.lebai.posture.JointPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pose);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a JointFrame message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.JointFrame
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.JointFrame & lebai.posture.JointFrame.$Shape} JointFrame
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JointFrame.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a JointFrame message.
             * @function verify
             * @memberof lebai.posture.JointFrame
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            JointFrame.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    if (typeof message.kind !== "number" || (message.kind | 0) !== message.kind)
                        return "kind: enum value expected";
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose")) {
                    var error = $root.lebai.posture.JointPose.verify(message.pose, _depth + 1);
                    if (error)
                        return "pose." + error;
                }
                return null;
            };

            /**
             * Creates a JointFrame message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.JointFrame
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.JointFrame} JointFrame
             */
            JointFrame.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.JointFrame)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.JointFrame: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.JointFrame();
                if (object.kind !== 0 && (typeof object.kind !== "string" || $root.lebai.posture.JointFrame.Kind[object.kind] !== 0))
                    switch (object.kind) {
                    case "BASE":
                    case 0:
                        message.kind = 0;
                        break;
                    case "CURRENT":
                    case 1:
                        message.kind = 1;
                        break;
                    case "LAST":
                    case 11:
                        message.kind = 11;
                        break;
                    case "CUSTOM":
                    case 99:
                        message.kind = 99;
                        break;
                    default:
                        if (typeof object.kind === "number" && (object.kind | 0) === object.kind)
                            message.kind = object.kind;
                    }
                if (object.pose != null) {
                    if (!$util.isObject(object.pose))
                        throw $TypeError(".lebai.posture.JointFrame.pose: object expected");
                    message.pose = $root.lebai.posture.JointPose.fromObject(object.pose, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a JointFrame message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.JointFrame
             * @static
             * @param {lebai.posture.JointFrame} message JointFrame
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            JointFrame.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.kind = options.enums === $String ? "BASE" : 0;
                    object.pose = null;
                }
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    object.kind = options.enums === $String ? $root.lebai.posture.JointFrame.Kind[message.kind] === $undefined ? message.kind : $root.lebai.posture.JointFrame.Kind[message.kind] : message.kind;
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    object.pose = $root.lebai.posture.JointPose.toObject(message.pose, options, _depth + 1);
                return object;
            };

            /**
             * Converts this JointFrame to JSON.
             * @function toJSON
             * @memberof lebai.posture.JointFrame
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            JointFrame.prototype.toJSON = function() {
                return JointFrame.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for JointFrame
             * @function getTypeUrl
             * @memberof lebai.posture.JointFrame
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            JointFrame.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.JointFrame";
            };

            /**
             * Kind enum.
             * @name lebai.posture.JointFrame.Kind
             * @enum {number}
             * @property {number} BASE=0 BASE value
             * @property {number} CURRENT=1 CURRENT value
             * @property {number} LAST=11 LAST value
             * @property {number} CUSTOM=99 CUSTOM value
             */
            JointFrame.Kind = (function() {
                var valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "BASE"] = 0;
                values[valuesById[1] = "CURRENT"] = 1;
                values[valuesById[11] = "LAST"] = 11;
                values[valuesById[99] = "CUSTOM"] = 99;
                return values;
            })();

            return JointFrame;
        })();

        posture.RotationMatrix = (function() {

            /**
             * Properties of a RotationMatrix.
             * @typedef {Object} lebai.posture.RotationMatrix.$Properties
             * @property {number|null} [m11] RotationMatrix m11
             * @property {number|null} [m12] RotationMatrix m12
             * @property {number|null} [m13] RotationMatrix m13
             * @property {number|null} [m21] RotationMatrix m21
             * @property {number|null} [m22] RotationMatrix m22
             * @property {number|null} [m23] RotationMatrix m23
             * @property {number|null} [m31] RotationMatrix m31
             * @property {number|null} [m32] RotationMatrix m32
             * @property {number|null} [m33] RotationMatrix m33
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a RotationMatrix.
             * @memberof lebai.posture
             * @interface IRotationMatrix
             * @augments lebai.posture.RotationMatrix.$Properties
             * @deprecated Use lebai.posture.RotationMatrix.$Properties instead.
             */

            /**
             * Shape of a RotationMatrix.
             * @typedef {lebai.posture.RotationMatrix.$Properties} lebai.posture.RotationMatrix.$Shape
             */

            /**
             * Constructs a new RotationMatrix.
             * @memberof lebai.posture
             * @classdesc Represents a RotationMatrix.
             * @constructor
             * @param {lebai.posture.RotationMatrix.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var RotationMatrix = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * RotationMatrix m11.
             * @member {number} m11
             * @memberof lebai.posture.RotationMatrix
             * @instance
             */
            RotationMatrix.prototype.m11 = 0;

            /**
             * RotationMatrix m12.
             * @member {number} m12
             * @memberof lebai.posture.RotationMatrix
             * @instance
             */
            RotationMatrix.prototype.m12 = 0;

            /**
             * RotationMatrix m13.
             * @member {number} m13
             * @memberof lebai.posture.RotationMatrix
             * @instance
             */
            RotationMatrix.prototype.m13 = 0;

            /**
             * RotationMatrix m21.
             * @member {number} m21
             * @memberof lebai.posture.RotationMatrix
             * @instance
             */
            RotationMatrix.prototype.m21 = 0;

            /**
             * RotationMatrix m22.
             * @member {number} m22
             * @memberof lebai.posture.RotationMatrix
             * @instance
             */
            RotationMatrix.prototype.m22 = 0;

            /**
             * RotationMatrix m23.
             * @member {number} m23
             * @memberof lebai.posture.RotationMatrix
             * @instance
             */
            RotationMatrix.prototype.m23 = 0;

            /**
             * RotationMatrix m31.
             * @member {number} m31
             * @memberof lebai.posture.RotationMatrix
             * @instance
             */
            RotationMatrix.prototype.m31 = 0;

            /**
             * RotationMatrix m32.
             * @member {number} m32
             * @memberof lebai.posture.RotationMatrix
             * @instance
             */
            RotationMatrix.prototype.m32 = 0;

            /**
             * RotationMatrix m33.
             * @member {number} m33
             * @memberof lebai.posture.RotationMatrix
             * @instance
             */
            RotationMatrix.prototype.m33 = 0;

            /**
             * Creates a new RotationMatrix instance using the specified properties.
             * @function create
             * @memberof lebai.posture.RotationMatrix
             * @static
             * @param {lebai.posture.RotationMatrix.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.RotationMatrix} RotationMatrix instance
             * @type {{
             *   (properties: lebai.posture.RotationMatrix.$Shape): lebai.posture.RotationMatrix & lebai.posture.RotationMatrix.$Shape;
             *   (properties?: lebai.posture.RotationMatrix.$Properties): lebai.posture.RotationMatrix;
             * }}
             */
            RotationMatrix.create = function(properties) {
                return new RotationMatrix(properties);
            };

            /**
             * Encodes the specified RotationMatrix message. Does not implicitly {@link lebai.posture.RotationMatrix.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.RotationMatrix
             * @static
             * @param {lebai.posture.RotationMatrix.$Properties} message RotationMatrix message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RotationMatrix.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.m11 != null && $Object.hasOwnProperty.call(message, "m11") && !$Object.is(message.m11, 0))
                    writer.uint32(/* id 11, wireType 1 =*/89).double(message.m11);
                if (message.m12 != null && $Object.hasOwnProperty.call(message, "m12") && !$Object.is(message.m12, 0))
                    writer.uint32(/* id 12, wireType 1 =*/97).double(message.m12);
                if (message.m13 != null && $Object.hasOwnProperty.call(message, "m13") && !$Object.is(message.m13, 0))
                    writer.uint32(/* id 13, wireType 1 =*/105).double(message.m13);
                if (message.m21 != null && $Object.hasOwnProperty.call(message, "m21") && !$Object.is(message.m21, 0))
                    writer.uint32(/* id 21, wireType 1 =*/169).double(message.m21);
                if (message.m22 != null && $Object.hasOwnProperty.call(message, "m22") && !$Object.is(message.m22, 0))
                    writer.uint32(/* id 22, wireType 1 =*/177).double(message.m22);
                if (message.m23 != null && $Object.hasOwnProperty.call(message, "m23") && !$Object.is(message.m23, 0))
                    writer.uint32(/* id 23, wireType 1 =*/185).double(message.m23);
                if (message.m31 != null && $Object.hasOwnProperty.call(message, "m31") && !$Object.is(message.m31, 0))
                    writer.uint32(/* id 31, wireType 1 =*/249).double(message.m31);
                if (message.m32 != null && $Object.hasOwnProperty.call(message, "m32") && !$Object.is(message.m32, 0))
                    writer.uint32(/* id 32, wireType 1 =*/257).double(message.m32);
                if (message.m33 != null && $Object.hasOwnProperty.call(message, "m33") && !$Object.is(message.m33, 0))
                    writer.uint32(/* id 33, wireType 1 =*/265).double(message.m33);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified RotationMatrix message, length delimited. Does not implicitly {@link lebai.posture.RotationMatrix.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.RotationMatrix
             * @static
             * @param {lebai.posture.RotationMatrix.$Properties} message RotationMatrix message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RotationMatrix.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a RotationMatrix message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.RotationMatrix
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.RotationMatrix & lebai.posture.RotationMatrix.$Shape} RotationMatrix
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RotationMatrix.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.RotationMatrix();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m11 = value;
                            else
                                delete message.m11;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m12 = value;
                            else
                                delete message.m12;
                            continue;
                        }
                    case 13: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m13 = value;
                            else
                                delete message.m13;
                            continue;
                        }
                    case 21: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m21 = value;
                            else
                                delete message.m21;
                            continue;
                        }
                    case 22: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m22 = value;
                            else
                                delete message.m22;
                            continue;
                        }
                    case 23: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m23 = value;
                            else
                                delete message.m23;
                            continue;
                        }
                    case 31: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m31 = value;
                            else
                                delete message.m31;
                            continue;
                        }
                    case 32: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m32 = value;
                            else
                                delete message.m32;
                            continue;
                        }
                    case 33: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m33 = value;
                            else
                                delete message.m33;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a RotationMatrix message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.RotationMatrix
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.RotationMatrix & lebai.posture.RotationMatrix.$Shape} RotationMatrix
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RotationMatrix.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RotationMatrix message.
             * @function verify
             * @memberof lebai.posture.RotationMatrix
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RotationMatrix.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.m11 != null && $Object.hasOwnProperty.call(message, "m11"))
                    if (typeof message.m11 !== "number")
                        return "m11: number expected";
                if (message.m12 != null && $Object.hasOwnProperty.call(message, "m12"))
                    if (typeof message.m12 !== "number")
                        return "m12: number expected";
                if (message.m13 != null && $Object.hasOwnProperty.call(message, "m13"))
                    if (typeof message.m13 !== "number")
                        return "m13: number expected";
                if (message.m21 != null && $Object.hasOwnProperty.call(message, "m21"))
                    if (typeof message.m21 !== "number")
                        return "m21: number expected";
                if (message.m22 != null && $Object.hasOwnProperty.call(message, "m22"))
                    if (typeof message.m22 !== "number")
                        return "m22: number expected";
                if (message.m23 != null && $Object.hasOwnProperty.call(message, "m23"))
                    if (typeof message.m23 !== "number")
                        return "m23: number expected";
                if (message.m31 != null && $Object.hasOwnProperty.call(message, "m31"))
                    if (typeof message.m31 !== "number")
                        return "m31: number expected";
                if (message.m32 != null && $Object.hasOwnProperty.call(message, "m32"))
                    if (typeof message.m32 !== "number")
                        return "m32: number expected";
                if (message.m33 != null && $Object.hasOwnProperty.call(message, "m33"))
                    if (typeof message.m33 !== "number")
                        return "m33: number expected";
                return null;
            };

            /**
             * Creates a RotationMatrix message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.RotationMatrix
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.RotationMatrix} RotationMatrix
             */
            RotationMatrix.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.RotationMatrix)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.RotationMatrix: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.RotationMatrix();
                if (object.m11 != null)
                    if (!$Object.is($Number(object.m11), 0))
                        message.m11 = $Number(object.m11);
                if (object.m12 != null)
                    if (!$Object.is($Number(object.m12), 0))
                        message.m12 = $Number(object.m12);
                if (object.m13 != null)
                    if (!$Object.is($Number(object.m13), 0))
                        message.m13 = $Number(object.m13);
                if (object.m21 != null)
                    if (!$Object.is($Number(object.m21), 0))
                        message.m21 = $Number(object.m21);
                if (object.m22 != null)
                    if (!$Object.is($Number(object.m22), 0))
                        message.m22 = $Number(object.m22);
                if (object.m23 != null)
                    if (!$Object.is($Number(object.m23), 0))
                        message.m23 = $Number(object.m23);
                if (object.m31 != null)
                    if (!$Object.is($Number(object.m31), 0))
                        message.m31 = $Number(object.m31);
                if (object.m32 != null)
                    if (!$Object.is($Number(object.m32), 0))
                        message.m32 = $Number(object.m32);
                if (object.m33 != null)
                    if (!$Object.is($Number(object.m33), 0))
                        message.m33 = $Number(object.m33);
                return message;
            };

            /**
             * Creates a plain object from a RotationMatrix message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.RotationMatrix
             * @static
             * @param {lebai.posture.RotationMatrix} message RotationMatrix
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RotationMatrix.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.m11 = 0;
                    object.m12 = 0;
                    object.m13 = 0;
                    object.m21 = 0;
                    object.m22 = 0;
                    object.m23 = 0;
                    object.m31 = 0;
                    object.m32 = 0;
                    object.m33 = 0;
                }
                if (message.m11 != null && $Object.hasOwnProperty.call(message, "m11"))
                    object.m11 = options.json && !$isFinite(message.m11) ? $String(message.m11) : message.m11;
                if (message.m12 != null && $Object.hasOwnProperty.call(message, "m12"))
                    object.m12 = options.json && !$isFinite(message.m12) ? $String(message.m12) : message.m12;
                if (message.m13 != null && $Object.hasOwnProperty.call(message, "m13"))
                    object.m13 = options.json && !$isFinite(message.m13) ? $String(message.m13) : message.m13;
                if (message.m21 != null && $Object.hasOwnProperty.call(message, "m21"))
                    object.m21 = options.json && !$isFinite(message.m21) ? $String(message.m21) : message.m21;
                if (message.m22 != null && $Object.hasOwnProperty.call(message, "m22"))
                    object.m22 = options.json && !$isFinite(message.m22) ? $String(message.m22) : message.m22;
                if (message.m23 != null && $Object.hasOwnProperty.call(message, "m23"))
                    object.m23 = options.json && !$isFinite(message.m23) ? $String(message.m23) : message.m23;
                if (message.m31 != null && $Object.hasOwnProperty.call(message, "m31"))
                    object.m31 = options.json && !$isFinite(message.m31) ? $String(message.m31) : message.m31;
                if (message.m32 != null && $Object.hasOwnProperty.call(message, "m32"))
                    object.m32 = options.json && !$isFinite(message.m32) ? $String(message.m32) : message.m32;
                if (message.m33 != null && $Object.hasOwnProperty.call(message, "m33"))
                    object.m33 = options.json && !$isFinite(message.m33) ? $String(message.m33) : message.m33;
                return object;
            };

            /**
             * Converts this RotationMatrix to JSON.
             * @function toJSON
             * @memberof lebai.posture.RotationMatrix
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RotationMatrix.prototype.toJSON = function() {
                return RotationMatrix.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for RotationMatrix
             * @function getTypeUrl
             * @memberof lebai.posture.RotationMatrix
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            RotationMatrix.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.RotationMatrix";
            };

            return RotationMatrix;
        })();

        posture.EulerZyx = (function() {

            /**
             * Properties of an EulerZyx.
             * @typedef {Object} lebai.posture.EulerZyx.$Properties
             * @property {number|null} [x] EulerZyx x
             * @property {number|null} [y] EulerZyx y
             * @property {number|null} [z] EulerZyx z
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an EulerZyx.
             * @memberof lebai.posture
             * @interface IEulerZyx
             * @augments lebai.posture.EulerZyx.$Properties
             * @deprecated Use lebai.posture.EulerZyx.$Properties instead.
             */

            /**
             * Shape of an EulerZyx.
             * @typedef {lebai.posture.EulerZyx.$Properties} lebai.posture.EulerZyx.$Shape
             */

            /**
             * Constructs a new EulerZyx.
             * @memberof lebai.posture
             * @classdesc Represents an EulerZyx.
             * @constructor
             * @param {lebai.posture.EulerZyx.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var EulerZyx = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * EulerZyx x.
             * @member {number} x
             * @memberof lebai.posture.EulerZyx
             * @instance
             */
            EulerZyx.prototype.x = 0;

            /**
             * EulerZyx y.
             * @member {number} y
             * @memberof lebai.posture.EulerZyx
             * @instance
             */
            EulerZyx.prototype.y = 0;

            /**
             * EulerZyx z.
             * @member {number} z
             * @memberof lebai.posture.EulerZyx
             * @instance
             */
            EulerZyx.prototype.z = 0;

            /**
             * Creates a new EulerZyx instance using the specified properties.
             * @function create
             * @memberof lebai.posture.EulerZyx
             * @static
             * @param {lebai.posture.EulerZyx.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.EulerZyx} EulerZyx instance
             * @type {{
             *   (properties: lebai.posture.EulerZyx.$Shape): lebai.posture.EulerZyx & lebai.posture.EulerZyx.$Shape;
             *   (properties?: lebai.posture.EulerZyx.$Properties): lebai.posture.EulerZyx;
             * }}
             */
            EulerZyx.create = function(properties) {
                return new EulerZyx(properties);
            };

            /**
             * Encodes the specified EulerZyx message. Does not implicitly {@link lebai.posture.EulerZyx.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.EulerZyx
             * @static
             * @param {lebai.posture.EulerZyx.$Properties} message EulerZyx message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EulerZyx.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.x != null && $Object.hasOwnProperty.call(message, "x") && !$Object.is(message.x, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.x);
                if (message.y != null && $Object.hasOwnProperty.call(message, "y") && !$Object.is(message.y, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.y);
                if (message.z != null && $Object.hasOwnProperty.call(message, "z") && !$Object.is(message.z, 0))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.z);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EulerZyx message, length delimited. Does not implicitly {@link lebai.posture.EulerZyx.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.EulerZyx
             * @static
             * @param {lebai.posture.EulerZyx.$Properties} message EulerZyx message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EulerZyx.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an EulerZyx message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.EulerZyx
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.EulerZyx & lebai.posture.EulerZyx.$Shape} EulerZyx
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EulerZyx.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.EulerZyx();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.x = value;
                            else
                                delete message.x;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.y = value;
                            else
                                delete message.y;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.z = value;
                            else
                                delete message.z;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an EulerZyx message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.EulerZyx
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.EulerZyx & lebai.posture.EulerZyx.$Shape} EulerZyx
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EulerZyx.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EulerZyx message.
             * @function verify
             * @memberof lebai.posture.EulerZyx
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EulerZyx.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.x != null && $Object.hasOwnProperty.call(message, "x"))
                    if (typeof message.x !== "number")
                        return "x: number expected";
                if (message.y != null && $Object.hasOwnProperty.call(message, "y"))
                    if (typeof message.y !== "number")
                        return "y: number expected";
                if (message.z != null && $Object.hasOwnProperty.call(message, "z"))
                    if (typeof message.z !== "number")
                        return "z: number expected";
                return null;
            };

            /**
             * Creates an EulerZyx message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.EulerZyx
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.EulerZyx} EulerZyx
             */
            EulerZyx.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.EulerZyx)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.EulerZyx: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.EulerZyx();
                if (object.x != null)
                    if (!$Object.is($Number(object.x), 0))
                        message.x = $Number(object.x);
                if (object.y != null)
                    if (!$Object.is($Number(object.y), 0))
                        message.y = $Number(object.y);
                if (object.z != null)
                    if (!$Object.is($Number(object.z), 0))
                        message.z = $Number(object.z);
                return message;
            };

            /**
             * Creates a plain object from an EulerZyx message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.EulerZyx
             * @static
             * @param {lebai.posture.EulerZyx} message EulerZyx
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EulerZyx.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.x = 0;
                    object.y = 0;
                    object.z = 0;
                }
                if (message.x != null && $Object.hasOwnProperty.call(message, "x"))
                    object.x = options.json && !$isFinite(message.x) ? $String(message.x) : message.x;
                if (message.y != null && $Object.hasOwnProperty.call(message, "y"))
                    object.y = options.json && !$isFinite(message.y) ? $String(message.y) : message.y;
                if (message.z != null && $Object.hasOwnProperty.call(message, "z"))
                    object.z = options.json && !$isFinite(message.z) ? $String(message.z) : message.z;
                return object;
            };

            /**
             * Converts this EulerZyx to JSON.
             * @function toJSON
             * @memberof lebai.posture.EulerZyx
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EulerZyx.prototype.toJSON = function() {
                return EulerZyx.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for EulerZyx
             * @function getTypeUrl
             * @memberof lebai.posture.EulerZyx
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            EulerZyx.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.EulerZyx";
            };

            return EulerZyx;
        })();

        posture.Quaternion = (function() {

            /**
             * Properties of a Quaternion.
             * @typedef {Object} lebai.posture.Quaternion.$Properties
             * @property {number|null} [w] Quaternion w
             * @property {number|null} [i] Quaternion i
             * @property {number|null} [j] Quaternion j
             * @property {number|null} [k] Quaternion k
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Quaternion.
             * @memberof lebai.posture
             * @interface IQuaternion
             * @augments lebai.posture.Quaternion.$Properties
             * @deprecated Use lebai.posture.Quaternion.$Properties instead.
             */

            /**
             * Shape of a Quaternion.
             * @typedef {lebai.posture.Quaternion.$Properties} lebai.posture.Quaternion.$Shape
             */

            /**
             * Constructs a new Quaternion.
             * @memberof lebai.posture
             * @classdesc Represents a Quaternion.
             * @constructor
             * @param {lebai.posture.Quaternion.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Quaternion = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Quaternion w.
             * @member {number} w
             * @memberof lebai.posture.Quaternion
             * @instance
             */
            Quaternion.prototype.w = 0;

            /**
             * Quaternion i.
             * @member {number} i
             * @memberof lebai.posture.Quaternion
             * @instance
             */
            Quaternion.prototype.i = 0;

            /**
             * Quaternion j.
             * @member {number} j
             * @memberof lebai.posture.Quaternion
             * @instance
             */
            Quaternion.prototype.j = 0;

            /**
             * Quaternion k.
             * @member {number} k
             * @memberof lebai.posture.Quaternion
             * @instance
             */
            Quaternion.prototype.k = 0;

            /**
             * Creates a new Quaternion instance using the specified properties.
             * @function create
             * @memberof lebai.posture.Quaternion
             * @static
             * @param {lebai.posture.Quaternion.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.Quaternion} Quaternion instance
             * @type {{
             *   (properties: lebai.posture.Quaternion.$Shape): lebai.posture.Quaternion & lebai.posture.Quaternion.$Shape;
             *   (properties?: lebai.posture.Quaternion.$Properties): lebai.posture.Quaternion;
             * }}
             */
            Quaternion.create = function(properties) {
                return new Quaternion(properties);
            };

            /**
             * Encodes the specified Quaternion message. Does not implicitly {@link lebai.posture.Quaternion.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.Quaternion
             * @static
             * @param {lebai.posture.Quaternion.$Properties} message Quaternion message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Quaternion.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.w != null && $Object.hasOwnProperty.call(message, "w") && !$Object.is(message.w, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.w);
                if (message.i != null && $Object.hasOwnProperty.call(message, "i") && !$Object.is(message.i, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.i);
                if (message.j != null && $Object.hasOwnProperty.call(message, "j") && !$Object.is(message.j, 0))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.j);
                if (message.k != null && $Object.hasOwnProperty.call(message, "k") && !$Object.is(message.k, 0))
                    writer.uint32(/* id 4, wireType 1 =*/33).double(message.k);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Quaternion message, length delimited. Does not implicitly {@link lebai.posture.Quaternion.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.Quaternion
             * @static
             * @param {lebai.posture.Quaternion.$Properties} message Quaternion message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Quaternion.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Quaternion message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.Quaternion
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.Quaternion & lebai.posture.Quaternion.$Shape} Quaternion
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Quaternion.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.Quaternion();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.w = value;
                            else
                                delete message.w;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.i = value;
                            else
                                delete message.i;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.j = value;
                            else
                                delete message.j;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.k = value;
                            else
                                delete message.k;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Quaternion message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.Quaternion
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.Quaternion & lebai.posture.Quaternion.$Shape} Quaternion
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Quaternion.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Quaternion message.
             * @function verify
             * @memberof lebai.posture.Quaternion
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Quaternion.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.w != null && $Object.hasOwnProperty.call(message, "w"))
                    if (typeof message.w !== "number")
                        return "w: number expected";
                if (message.i != null && $Object.hasOwnProperty.call(message, "i"))
                    if (typeof message.i !== "number")
                        return "i: number expected";
                if (message.j != null && $Object.hasOwnProperty.call(message, "j"))
                    if (typeof message.j !== "number")
                        return "j: number expected";
                if (message.k != null && $Object.hasOwnProperty.call(message, "k"))
                    if (typeof message.k !== "number")
                        return "k: number expected";
                return null;
            };

            /**
             * Creates a Quaternion message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.Quaternion
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.Quaternion} Quaternion
             */
            Quaternion.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.Quaternion)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.Quaternion: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.Quaternion();
                if (object.w != null)
                    if (!$Object.is($Number(object.w), 0))
                        message.w = $Number(object.w);
                if (object.i != null)
                    if (!$Object.is($Number(object.i), 0))
                        message.i = $Number(object.i);
                if (object.j != null)
                    if (!$Object.is($Number(object.j), 0))
                        message.j = $Number(object.j);
                if (object.k != null)
                    if (!$Object.is($Number(object.k), 0))
                        message.k = $Number(object.k);
                return message;
            };

            /**
             * Creates a plain object from a Quaternion message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.Quaternion
             * @static
             * @param {lebai.posture.Quaternion} message Quaternion
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Quaternion.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.w = 0;
                    object.i = 0;
                    object.j = 0;
                    object.k = 0;
                }
                if (message.w != null && $Object.hasOwnProperty.call(message, "w"))
                    object.w = options.json && !$isFinite(message.w) ? $String(message.w) : message.w;
                if (message.i != null && $Object.hasOwnProperty.call(message, "i"))
                    object.i = options.json && !$isFinite(message.i) ? $String(message.i) : message.i;
                if (message.j != null && $Object.hasOwnProperty.call(message, "j"))
                    object.j = options.json && !$isFinite(message.j) ? $String(message.j) : message.j;
                if (message.k != null && $Object.hasOwnProperty.call(message, "k"))
                    object.k = options.json && !$isFinite(message.k) ? $String(message.k) : message.k;
                return object;
            };

            /**
             * Converts this Quaternion to JSON.
             * @function toJSON
             * @memberof lebai.posture.Quaternion
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Quaternion.prototype.toJSON = function() {
                return Quaternion.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Quaternion
             * @function getTypeUrl
             * @memberof lebai.posture.Quaternion
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Quaternion.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.Quaternion";
            };

            return Quaternion;
        })();

        posture.Rotation = (function() {

            /**
             * Properties of a Rotation.
             * @typedef {Object} lebai.posture.Rotation.$Properties
             * @property {lebai.posture.EulerZyx.$Properties|null} [eulerZyx] Rotation eulerZyx
             * @property {lebai.posture.Quaternion.$Properties|null} [quaternion] Rotation quaternion
             * @property {lebai.posture.RotationMatrix.$Properties|null} [matrix] Rotation matrix
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Rotation.
             * @memberof lebai.posture
             * @interface IRotation
             * @augments lebai.posture.Rotation.$Properties
             * @deprecated Use lebai.posture.Rotation.$Properties instead.
             */

            /**
             * Shape of a Rotation.
             * @typedef {lebai.posture.Rotation.$Properties} lebai.posture.Rotation.$Shape
             */

            /**
             * Constructs a new Rotation.
             * @memberof lebai.posture
             * @classdesc Represents a Rotation.
             * @constructor
             * @param {lebai.posture.Rotation.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Rotation = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Rotation eulerZyx.
             * @member {lebai.posture.EulerZyx.$Properties|null|undefined} eulerZyx
             * @memberof lebai.posture.Rotation
             * @instance
             */
            Rotation.prototype.eulerZyx = null;

            /**
             * Rotation quaternion.
             * @member {lebai.posture.Quaternion.$Properties|null|undefined} quaternion
             * @memberof lebai.posture.Rotation
             * @instance
             */
            Rotation.prototype.quaternion = null;

            /**
             * Rotation matrix.
             * @member {lebai.posture.RotationMatrix.$Properties|null|undefined} matrix
             * @memberof lebai.posture.Rotation
             * @instance
             */
            Rotation.prototype.matrix = null;

            /**
             * Creates a new Rotation instance using the specified properties.
             * @function create
             * @memberof lebai.posture.Rotation
             * @static
             * @param {lebai.posture.Rotation.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.Rotation} Rotation instance
             * @type {{
             *   (properties: lebai.posture.Rotation.$Shape): lebai.posture.Rotation & lebai.posture.Rotation.$Shape;
             *   (properties?: lebai.posture.Rotation.$Properties): lebai.posture.Rotation;
             * }}
             */
            Rotation.create = function(properties) {
                return new Rotation(properties);
            };

            /**
             * Encodes the specified Rotation message. Does not implicitly {@link lebai.posture.Rotation.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.Rotation
             * @static
             * @param {lebai.posture.Rotation.$Properties} message Rotation message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Rotation.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.eulerZyx != null && $Object.hasOwnProperty.call(message, "eulerZyx"))
                    $root.lebai.posture.EulerZyx.encode(message.eulerZyx, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.quaternion != null && $Object.hasOwnProperty.call(message, "quaternion"))
                    $root.lebai.posture.Quaternion.encode(message.quaternion, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.matrix != null && $Object.hasOwnProperty.call(message, "matrix"))
                    $root.lebai.posture.RotationMatrix.encode(message.matrix, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Rotation message, length delimited. Does not implicitly {@link lebai.posture.Rotation.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.Rotation
             * @static
             * @param {lebai.posture.Rotation.$Properties} message Rotation message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Rotation.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Rotation message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.Rotation
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.Rotation & lebai.posture.Rotation.$Shape} Rotation
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Rotation.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.Rotation();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.eulerZyx = $root.lebai.posture.EulerZyx.decode(reader, reader.uint32(), $undefined, _depth + 1, message.eulerZyx);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.quaternion = $root.lebai.posture.Quaternion.decode(reader, reader.uint32(), $undefined, _depth + 1, message.quaternion);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.matrix = $root.lebai.posture.RotationMatrix.decode(reader, reader.uint32(), $undefined, _depth + 1, message.matrix);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Rotation message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.Rotation
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.Rotation & lebai.posture.Rotation.$Shape} Rotation
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Rotation.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Rotation message.
             * @function verify
             * @memberof lebai.posture.Rotation
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Rotation.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.eulerZyx != null && $Object.hasOwnProperty.call(message, "eulerZyx")) {
                    var error = $root.lebai.posture.EulerZyx.verify(message.eulerZyx, _depth + 1);
                    if (error)
                        return "eulerZyx." + error;
                }
                if (message.quaternion != null && $Object.hasOwnProperty.call(message, "quaternion")) {
                    var error = $root.lebai.posture.Quaternion.verify(message.quaternion, _depth + 1);
                    if (error)
                        return "quaternion." + error;
                }
                if (message.matrix != null && $Object.hasOwnProperty.call(message, "matrix")) {
                    var error = $root.lebai.posture.RotationMatrix.verify(message.matrix, _depth + 1);
                    if (error)
                        return "matrix." + error;
                }
                return null;
            };

            /**
             * Creates a Rotation message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.Rotation
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.Rotation} Rotation
             */
            Rotation.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.Rotation)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.Rotation: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.Rotation();
                if (object.eulerZyx != null) {
                    if (!$util.isObject(object.eulerZyx))
                        throw $TypeError(".lebai.posture.Rotation.eulerZyx: object expected");
                    message.eulerZyx = $root.lebai.posture.EulerZyx.fromObject(object.eulerZyx, _depth + 1);
                }
                if (object.quaternion != null) {
                    if (!$util.isObject(object.quaternion))
                        throw $TypeError(".lebai.posture.Rotation.quaternion: object expected");
                    message.quaternion = $root.lebai.posture.Quaternion.fromObject(object.quaternion, _depth + 1);
                }
                if (object.matrix != null) {
                    if (!$util.isObject(object.matrix))
                        throw $TypeError(".lebai.posture.Rotation.matrix: object expected");
                    message.matrix = $root.lebai.posture.RotationMatrix.fromObject(object.matrix, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Rotation message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.Rotation
             * @static
             * @param {lebai.posture.Rotation} message Rotation
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Rotation.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.eulerZyx = null;
                    object.quaternion = null;
                    object.matrix = null;
                }
                if (message.eulerZyx != null && $Object.hasOwnProperty.call(message, "eulerZyx"))
                    object.eulerZyx = $root.lebai.posture.EulerZyx.toObject(message.eulerZyx, options, _depth + 1);
                if (message.quaternion != null && $Object.hasOwnProperty.call(message, "quaternion"))
                    object.quaternion = $root.lebai.posture.Quaternion.toObject(message.quaternion, options, _depth + 1);
                if (message.matrix != null && $Object.hasOwnProperty.call(message, "matrix"))
                    object.matrix = $root.lebai.posture.RotationMatrix.toObject(message.matrix, options, _depth + 1);
                return object;
            };

            /**
             * Converts this Rotation to JSON.
             * @function toJSON
             * @memberof lebai.posture.Rotation
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Rotation.prototype.toJSON = function() {
                return Rotation.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Rotation
             * @function getTypeUrl
             * @memberof lebai.posture.Rotation
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Rotation.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.Rotation";
            };

            return Rotation;
        })();

        posture.Position = (function() {

            /**
             * Properties of a Position.
             * @typedef {Object} lebai.posture.Position.$Properties
             * @property {number|null} [x] Position x
             * @property {number|null} [y] Position y
             * @property {number|null} [z] Position z
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Position.
             * @memberof lebai.posture
             * @interface IPosition
             * @augments lebai.posture.Position.$Properties
             * @deprecated Use lebai.posture.Position.$Properties instead.
             */

            /**
             * Shape of a Position.
             * @typedef {lebai.posture.Position.$Properties} lebai.posture.Position.$Shape
             */

            /**
             * Constructs a new Position.
             * @memberof lebai.posture
             * @classdesc Represents a Position.
             * @constructor
             * @param {lebai.posture.Position.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Position = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Position x.
             * @member {number} x
             * @memberof lebai.posture.Position
             * @instance
             */
            Position.prototype.x = 0;

            /**
             * Position y.
             * @member {number} y
             * @memberof lebai.posture.Position
             * @instance
             */
            Position.prototype.y = 0;

            /**
             * Position z.
             * @member {number} z
             * @memberof lebai.posture.Position
             * @instance
             */
            Position.prototype.z = 0;

            /**
             * Creates a new Position instance using the specified properties.
             * @function create
             * @memberof lebai.posture.Position
             * @static
             * @param {lebai.posture.Position.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.Position} Position instance
             * @type {{
             *   (properties: lebai.posture.Position.$Shape): lebai.posture.Position & lebai.posture.Position.$Shape;
             *   (properties?: lebai.posture.Position.$Properties): lebai.posture.Position;
             * }}
             */
            Position.create = function(properties) {
                return new Position(properties);
            };

            /**
             * Encodes the specified Position message. Does not implicitly {@link lebai.posture.Position.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.Position
             * @static
             * @param {lebai.posture.Position.$Properties} message Position message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Position.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.x != null && $Object.hasOwnProperty.call(message, "x") && !$Object.is(message.x, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.x);
                if (message.y != null && $Object.hasOwnProperty.call(message, "y") && !$Object.is(message.y, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.y);
                if (message.z != null && $Object.hasOwnProperty.call(message, "z") && !$Object.is(message.z, 0))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.z);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Position message, length delimited. Does not implicitly {@link lebai.posture.Position.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.Position
             * @static
             * @param {lebai.posture.Position.$Properties} message Position message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Position.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Position message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.Position
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.Position & lebai.posture.Position.$Shape} Position
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Position.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.Position();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.x = value;
                            else
                                delete message.x;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.y = value;
                            else
                                delete message.y;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.z = value;
                            else
                                delete message.z;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Position message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.Position
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.Position & lebai.posture.Position.$Shape} Position
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Position.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Position message.
             * @function verify
             * @memberof lebai.posture.Position
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Position.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.x != null && $Object.hasOwnProperty.call(message, "x"))
                    if (typeof message.x !== "number")
                        return "x: number expected";
                if (message.y != null && $Object.hasOwnProperty.call(message, "y"))
                    if (typeof message.y !== "number")
                        return "y: number expected";
                if (message.z != null && $Object.hasOwnProperty.call(message, "z"))
                    if (typeof message.z !== "number")
                        return "z: number expected";
                return null;
            };

            /**
             * Creates a Position message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.Position
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.Position} Position
             */
            Position.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.Position)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.Position: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.Position();
                if (object.x != null)
                    if (!$Object.is($Number(object.x), 0))
                        message.x = $Number(object.x);
                if (object.y != null)
                    if (!$Object.is($Number(object.y), 0))
                        message.y = $Number(object.y);
                if (object.z != null)
                    if (!$Object.is($Number(object.z), 0))
                        message.z = $Number(object.z);
                return message;
            };

            /**
             * Creates a plain object from a Position message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.Position
             * @static
             * @param {lebai.posture.Position} message Position
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Position.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.x = 0;
                    object.y = 0;
                    object.z = 0;
                }
                if (message.x != null && $Object.hasOwnProperty.call(message, "x"))
                    object.x = options.json && !$isFinite(message.x) ? $String(message.x) : message.x;
                if (message.y != null && $Object.hasOwnProperty.call(message, "y"))
                    object.y = options.json && !$isFinite(message.y) ? $String(message.y) : message.y;
                if (message.z != null && $Object.hasOwnProperty.call(message, "z"))
                    object.z = options.json && !$isFinite(message.z) ? $String(message.z) : message.z;
                return object;
            };

            /**
             * Converts this Position to JSON.
             * @function toJSON
             * @memberof lebai.posture.Position
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Position.prototype.toJSON = function() {
                return Position.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Position
             * @function getTypeUrl
             * @memberof lebai.posture.Position
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Position.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.Position";
            };

            return Position;
        })();

        posture.CartesianPose = (function() {

            /**
             * Properties of a CartesianPose.
             * @typedef {Object} lebai.posture.CartesianPose.$Properties
             * @property {lebai.posture.Position.$Properties|null} [position] CartesianPose position
             * @property {lebai.posture.Rotation.$Properties|null} [rotation] CartesianPose rotation
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CartesianPose.
             * @memberof lebai.posture
             * @interface ICartesianPose
             * @augments lebai.posture.CartesianPose.$Properties
             * @deprecated Use lebai.posture.CartesianPose.$Properties instead.
             */

            /**
             * Shape of a CartesianPose.
             * @typedef {lebai.posture.CartesianPose.$Properties} lebai.posture.CartesianPose.$Shape
             */

            /**
             * Constructs a new CartesianPose.
             * @memberof lebai.posture
             * @classdesc Represents a CartesianPose.
             * @constructor
             * @param {lebai.posture.CartesianPose.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var CartesianPose = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CartesianPose position.
             * @member {lebai.posture.Position.$Properties|null|undefined} position
             * @memberof lebai.posture.CartesianPose
             * @instance
             */
            CartesianPose.prototype.position = null;

            /**
             * CartesianPose rotation.
             * @member {lebai.posture.Rotation.$Properties|null|undefined} rotation
             * @memberof lebai.posture.CartesianPose
             * @instance
             */
            CartesianPose.prototype.rotation = null;

            /**
             * Creates a new CartesianPose instance using the specified properties.
             * @function create
             * @memberof lebai.posture.CartesianPose
             * @static
             * @param {lebai.posture.CartesianPose.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.CartesianPose} CartesianPose instance
             * @type {{
             *   (properties: lebai.posture.CartesianPose.$Shape): lebai.posture.CartesianPose & lebai.posture.CartesianPose.$Shape;
             *   (properties?: lebai.posture.CartesianPose.$Properties): lebai.posture.CartesianPose;
             * }}
             */
            CartesianPose.create = function(properties) {
                return new CartesianPose(properties);
            };

            /**
             * Encodes the specified CartesianPose message. Does not implicitly {@link lebai.posture.CartesianPose.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.CartesianPose
             * @static
             * @param {lebai.posture.CartesianPose.$Properties} message CartesianPose message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CartesianPose.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.position != null && $Object.hasOwnProperty.call(message, "position"))
                    $root.lebai.posture.Position.encode(message.position, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.rotation != null && $Object.hasOwnProperty.call(message, "rotation"))
                    $root.lebai.posture.Rotation.encode(message.rotation, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CartesianPose message, length delimited. Does not implicitly {@link lebai.posture.CartesianPose.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.CartesianPose
             * @static
             * @param {lebai.posture.CartesianPose.$Properties} message CartesianPose message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CartesianPose.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a CartesianPose message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.CartesianPose
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.CartesianPose & lebai.posture.CartesianPose.$Shape} CartesianPose
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CartesianPose.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.CartesianPose();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.position = $root.lebai.posture.Position.decode(reader, reader.uint32(), $undefined, _depth + 1, message.position);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.rotation = $root.lebai.posture.Rotation.decode(reader, reader.uint32(), $undefined, _depth + 1, message.rotation);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a CartesianPose message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.CartesianPose
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.CartesianPose & lebai.posture.CartesianPose.$Shape} CartesianPose
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CartesianPose.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CartesianPose message.
             * @function verify
             * @memberof lebai.posture.CartesianPose
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CartesianPose.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.position != null && $Object.hasOwnProperty.call(message, "position")) {
                    var error = $root.lebai.posture.Position.verify(message.position, _depth + 1);
                    if (error)
                        return "position." + error;
                }
                if (message.rotation != null && $Object.hasOwnProperty.call(message, "rotation")) {
                    var error = $root.lebai.posture.Rotation.verify(message.rotation, _depth + 1);
                    if (error)
                        return "rotation." + error;
                }
                return null;
            };

            /**
             * Creates a CartesianPose message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.CartesianPose
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.CartesianPose} CartesianPose
             */
            CartesianPose.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.CartesianPose)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.CartesianPose: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.CartesianPose();
                if (object.position != null) {
                    if (!$util.isObject(object.position))
                        throw $TypeError(".lebai.posture.CartesianPose.position: object expected");
                    message.position = $root.lebai.posture.Position.fromObject(object.position, _depth + 1);
                }
                if (object.rotation != null) {
                    if (!$util.isObject(object.rotation))
                        throw $TypeError(".lebai.posture.CartesianPose.rotation: object expected");
                    message.rotation = $root.lebai.posture.Rotation.fromObject(object.rotation, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a CartesianPose message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.CartesianPose
             * @static
             * @param {lebai.posture.CartesianPose} message CartesianPose
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CartesianPose.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.position = null;
                    object.rotation = null;
                }
                if (message.position != null && $Object.hasOwnProperty.call(message, "position"))
                    object.position = $root.lebai.posture.Position.toObject(message.position, options, _depth + 1);
                if (message.rotation != null && $Object.hasOwnProperty.call(message, "rotation"))
                    object.rotation = $root.lebai.posture.Rotation.toObject(message.rotation, options, _depth + 1);
                return object;
            };

            /**
             * Converts this CartesianPose to JSON.
             * @function toJSON
             * @memberof lebai.posture.CartesianPose
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CartesianPose.prototype.toJSON = function() {
                return CartesianPose.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CartesianPose
             * @function getTypeUrl
             * @memberof lebai.posture.CartesianPose
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CartesianPose.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.CartesianPose";
            };

            return CartesianPose;
        })();

        posture.CartesianMatrix = (function() {

            /**
             * Properties of a CartesianMatrix.
             * @typedef {Object} lebai.posture.CartesianMatrix.$Properties
             * @property {number|null} [m11] CartesianMatrix m11
             * @property {number|null} [m12] CartesianMatrix m12
             * @property {number|null} [m13] CartesianMatrix m13
             * @property {number|null} [m14] CartesianMatrix m14
             * @property {number|null} [m21] CartesianMatrix m21
             * @property {number|null} [m22] CartesianMatrix m22
             * @property {number|null} [m23] CartesianMatrix m23
             * @property {number|null} [m24] CartesianMatrix m24
             * @property {number|null} [m31] CartesianMatrix m31
             * @property {number|null} [m32] CartesianMatrix m32
             * @property {number|null} [m33] CartesianMatrix m33
             * @property {number|null} [m34] CartesianMatrix m34
             * @property {number|null} [m41] CartesianMatrix m41
             * @property {number|null} [m42] CartesianMatrix m42
             * @property {number|null} [m43] CartesianMatrix m43
             * @property {number|null} [m44] CartesianMatrix m44
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CartesianMatrix.
             * @memberof lebai.posture
             * @interface ICartesianMatrix
             * @augments lebai.posture.CartesianMatrix.$Properties
             * @deprecated Use lebai.posture.CartesianMatrix.$Properties instead.
             */

            /**
             * Shape of a CartesianMatrix.
             * @typedef {lebai.posture.CartesianMatrix.$Properties} lebai.posture.CartesianMatrix.$Shape
             */

            /**
             * Constructs a new CartesianMatrix.
             * @memberof lebai.posture
             * @classdesc Represents a CartesianMatrix.
             * @constructor
             * @param {lebai.posture.CartesianMatrix.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var CartesianMatrix = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CartesianMatrix m11.
             * @member {number} m11
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m11 = 0;

            /**
             * CartesianMatrix m12.
             * @member {number} m12
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m12 = 0;

            /**
             * CartesianMatrix m13.
             * @member {number} m13
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m13 = 0;

            /**
             * CartesianMatrix m14.
             * @member {number} m14
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m14 = 0;

            /**
             * CartesianMatrix m21.
             * @member {number} m21
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m21 = 0;

            /**
             * CartesianMatrix m22.
             * @member {number} m22
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m22 = 0;

            /**
             * CartesianMatrix m23.
             * @member {number} m23
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m23 = 0;

            /**
             * CartesianMatrix m24.
             * @member {number} m24
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m24 = 0;

            /**
             * CartesianMatrix m31.
             * @member {number} m31
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m31 = 0;

            /**
             * CartesianMatrix m32.
             * @member {number} m32
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m32 = 0;

            /**
             * CartesianMatrix m33.
             * @member {number} m33
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m33 = 0;

            /**
             * CartesianMatrix m34.
             * @member {number} m34
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m34 = 0;

            /**
             * CartesianMatrix m41.
             * @member {number} m41
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m41 = 0;

            /**
             * CartesianMatrix m42.
             * @member {number} m42
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m42 = 0;

            /**
             * CartesianMatrix m43.
             * @member {number} m43
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m43 = 0;

            /**
             * CartesianMatrix m44.
             * @member {number} m44
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             */
            CartesianMatrix.prototype.m44 = 0;

            /**
             * Creates a new CartesianMatrix instance using the specified properties.
             * @function create
             * @memberof lebai.posture.CartesianMatrix
             * @static
             * @param {lebai.posture.CartesianMatrix.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.CartesianMatrix} CartesianMatrix instance
             * @type {{
             *   (properties: lebai.posture.CartesianMatrix.$Shape): lebai.posture.CartesianMatrix & lebai.posture.CartesianMatrix.$Shape;
             *   (properties?: lebai.posture.CartesianMatrix.$Properties): lebai.posture.CartesianMatrix;
             * }}
             */
            CartesianMatrix.create = function(properties) {
                return new CartesianMatrix(properties);
            };

            /**
             * Encodes the specified CartesianMatrix message. Does not implicitly {@link lebai.posture.CartesianMatrix.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.CartesianMatrix
             * @static
             * @param {lebai.posture.CartesianMatrix.$Properties} message CartesianMatrix message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CartesianMatrix.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.m11 != null && $Object.hasOwnProperty.call(message, "m11") && !$Object.is(message.m11, 0))
                    writer.uint32(/* id 11, wireType 1 =*/89).double(message.m11);
                if (message.m12 != null && $Object.hasOwnProperty.call(message, "m12") && !$Object.is(message.m12, 0))
                    writer.uint32(/* id 12, wireType 1 =*/97).double(message.m12);
                if (message.m13 != null && $Object.hasOwnProperty.call(message, "m13") && !$Object.is(message.m13, 0))
                    writer.uint32(/* id 13, wireType 1 =*/105).double(message.m13);
                if (message.m14 != null && $Object.hasOwnProperty.call(message, "m14") && !$Object.is(message.m14, 0))
                    writer.uint32(/* id 14, wireType 1 =*/113).double(message.m14);
                if (message.m21 != null && $Object.hasOwnProperty.call(message, "m21") && !$Object.is(message.m21, 0))
                    writer.uint32(/* id 21, wireType 1 =*/169).double(message.m21);
                if (message.m22 != null && $Object.hasOwnProperty.call(message, "m22") && !$Object.is(message.m22, 0))
                    writer.uint32(/* id 22, wireType 1 =*/177).double(message.m22);
                if (message.m23 != null && $Object.hasOwnProperty.call(message, "m23") && !$Object.is(message.m23, 0))
                    writer.uint32(/* id 23, wireType 1 =*/185).double(message.m23);
                if (message.m24 != null && $Object.hasOwnProperty.call(message, "m24") && !$Object.is(message.m24, 0))
                    writer.uint32(/* id 24, wireType 1 =*/193).double(message.m24);
                if (message.m31 != null && $Object.hasOwnProperty.call(message, "m31") && !$Object.is(message.m31, 0))
                    writer.uint32(/* id 31, wireType 1 =*/249).double(message.m31);
                if (message.m32 != null && $Object.hasOwnProperty.call(message, "m32") && !$Object.is(message.m32, 0))
                    writer.uint32(/* id 32, wireType 1 =*/257).double(message.m32);
                if (message.m33 != null && $Object.hasOwnProperty.call(message, "m33") && !$Object.is(message.m33, 0))
                    writer.uint32(/* id 33, wireType 1 =*/265).double(message.m33);
                if (message.m34 != null && $Object.hasOwnProperty.call(message, "m34") && !$Object.is(message.m34, 0))
                    writer.uint32(/* id 34, wireType 1 =*/273).double(message.m34);
                if (message.m41 != null && $Object.hasOwnProperty.call(message, "m41") && !$Object.is(message.m41, 0))
                    writer.uint32(/* id 41, wireType 1 =*/329).double(message.m41);
                if (message.m42 != null && $Object.hasOwnProperty.call(message, "m42") && !$Object.is(message.m42, 0))
                    writer.uint32(/* id 42, wireType 1 =*/337).double(message.m42);
                if (message.m43 != null && $Object.hasOwnProperty.call(message, "m43") && !$Object.is(message.m43, 0))
                    writer.uint32(/* id 43, wireType 1 =*/345).double(message.m43);
                if (message.m44 != null && $Object.hasOwnProperty.call(message, "m44") && !$Object.is(message.m44, 0))
                    writer.uint32(/* id 44, wireType 1 =*/353).double(message.m44);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CartesianMatrix message, length delimited. Does not implicitly {@link lebai.posture.CartesianMatrix.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.CartesianMatrix
             * @static
             * @param {lebai.posture.CartesianMatrix.$Properties} message CartesianMatrix message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CartesianMatrix.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a CartesianMatrix message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.CartesianMatrix
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.CartesianMatrix & lebai.posture.CartesianMatrix.$Shape} CartesianMatrix
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CartesianMatrix.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.CartesianMatrix();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m11 = value;
                            else
                                delete message.m11;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m12 = value;
                            else
                                delete message.m12;
                            continue;
                        }
                    case 13: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m13 = value;
                            else
                                delete message.m13;
                            continue;
                        }
                    case 14: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m14 = value;
                            else
                                delete message.m14;
                            continue;
                        }
                    case 21: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m21 = value;
                            else
                                delete message.m21;
                            continue;
                        }
                    case 22: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m22 = value;
                            else
                                delete message.m22;
                            continue;
                        }
                    case 23: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m23 = value;
                            else
                                delete message.m23;
                            continue;
                        }
                    case 24: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m24 = value;
                            else
                                delete message.m24;
                            continue;
                        }
                    case 31: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m31 = value;
                            else
                                delete message.m31;
                            continue;
                        }
                    case 32: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m32 = value;
                            else
                                delete message.m32;
                            continue;
                        }
                    case 33: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m33 = value;
                            else
                                delete message.m33;
                            continue;
                        }
                    case 34: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m34 = value;
                            else
                                delete message.m34;
                            continue;
                        }
                    case 41: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m41 = value;
                            else
                                delete message.m41;
                            continue;
                        }
                    case 42: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m42 = value;
                            else
                                delete message.m42;
                            continue;
                        }
                    case 43: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m43 = value;
                            else
                                delete message.m43;
                            continue;
                        }
                    case 44: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.m44 = value;
                            else
                                delete message.m44;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a CartesianMatrix message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.CartesianMatrix
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.CartesianMatrix & lebai.posture.CartesianMatrix.$Shape} CartesianMatrix
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CartesianMatrix.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CartesianMatrix message.
             * @function verify
             * @memberof lebai.posture.CartesianMatrix
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CartesianMatrix.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.m11 != null && $Object.hasOwnProperty.call(message, "m11"))
                    if (typeof message.m11 !== "number")
                        return "m11: number expected";
                if (message.m12 != null && $Object.hasOwnProperty.call(message, "m12"))
                    if (typeof message.m12 !== "number")
                        return "m12: number expected";
                if (message.m13 != null && $Object.hasOwnProperty.call(message, "m13"))
                    if (typeof message.m13 !== "number")
                        return "m13: number expected";
                if (message.m14 != null && $Object.hasOwnProperty.call(message, "m14"))
                    if (typeof message.m14 !== "number")
                        return "m14: number expected";
                if (message.m21 != null && $Object.hasOwnProperty.call(message, "m21"))
                    if (typeof message.m21 !== "number")
                        return "m21: number expected";
                if (message.m22 != null && $Object.hasOwnProperty.call(message, "m22"))
                    if (typeof message.m22 !== "number")
                        return "m22: number expected";
                if (message.m23 != null && $Object.hasOwnProperty.call(message, "m23"))
                    if (typeof message.m23 !== "number")
                        return "m23: number expected";
                if (message.m24 != null && $Object.hasOwnProperty.call(message, "m24"))
                    if (typeof message.m24 !== "number")
                        return "m24: number expected";
                if (message.m31 != null && $Object.hasOwnProperty.call(message, "m31"))
                    if (typeof message.m31 !== "number")
                        return "m31: number expected";
                if (message.m32 != null && $Object.hasOwnProperty.call(message, "m32"))
                    if (typeof message.m32 !== "number")
                        return "m32: number expected";
                if (message.m33 != null && $Object.hasOwnProperty.call(message, "m33"))
                    if (typeof message.m33 !== "number")
                        return "m33: number expected";
                if (message.m34 != null && $Object.hasOwnProperty.call(message, "m34"))
                    if (typeof message.m34 !== "number")
                        return "m34: number expected";
                if (message.m41 != null && $Object.hasOwnProperty.call(message, "m41"))
                    if (typeof message.m41 !== "number")
                        return "m41: number expected";
                if (message.m42 != null && $Object.hasOwnProperty.call(message, "m42"))
                    if (typeof message.m42 !== "number")
                        return "m42: number expected";
                if (message.m43 != null && $Object.hasOwnProperty.call(message, "m43"))
                    if (typeof message.m43 !== "number")
                        return "m43: number expected";
                if (message.m44 != null && $Object.hasOwnProperty.call(message, "m44"))
                    if (typeof message.m44 !== "number")
                        return "m44: number expected";
                return null;
            };

            /**
             * Creates a CartesianMatrix message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.CartesianMatrix
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.CartesianMatrix} CartesianMatrix
             */
            CartesianMatrix.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.CartesianMatrix)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.CartesianMatrix: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.CartesianMatrix();
                if (object.m11 != null)
                    if (!$Object.is($Number(object.m11), 0))
                        message.m11 = $Number(object.m11);
                if (object.m12 != null)
                    if (!$Object.is($Number(object.m12), 0))
                        message.m12 = $Number(object.m12);
                if (object.m13 != null)
                    if (!$Object.is($Number(object.m13), 0))
                        message.m13 = $Number(object.m13);
                if (object.m14 != null)
                    if (!$Object.is($Number(object.m14), 0))
                        message.m14 = $Number(object.m14);
                if (object.m21 != null)
                    if (!$Object.is($Number(object.m21), 0))
                        message.m21 = $Number(object.m21);
                if (object.m22 != null)
                    if (!$Object.is($Number(object.m22), 0))
                        message.m22 = $Number(object.m22);
                if (object.m23 != null)
                    if (!$Object.is($Number(object.m23), 0))
                        message.m23 = $Number(object.m23);
                if (object.m24 != null)
                    if (!$Object.is($Number(object.m24), 0))
                        message.m24 = $Number(object.m24);
                if (object.m31 != null)
                    if (!$Object.is($Number(object.m31), 0))
                        message.m31 = $Number(object.m31);
                if (object.m32 != null)
                    if (!$Object.is($Number(object.m32), 0))
                        message.m32 = $Number(object.m32);
                if (object.m33 != null)
                    if (!$Object.is($Number(object.m33), 0))
                        message.m33 = $Number(object.m33);
                if (object.m34 != null)
                    if (!$Object.is($Number(object.m34), 0))
                        message.m34 = $Number(object.m34);
                if (object.m41 != null)
                    if (!$Object.is($Number(object.m41), 0))
                        message.m41 = $Number(object.m41);
                if (object.m42 != null)
                    if (!$Object.is($Number(object.m42), 0))
                        message.m42 = $Number(object.m42);
                if (object.m43 != null)
                    if (!$Object.is($Number(object.m43), 0))
                        message.m43 = $Number(object.m43);
                if (object.m44 != null)
                    if (!$Object.is($Number(object.m44), 0))
                        message.m44 = $Number(object.m44);
                return message;
            };

            /**
             * Creates a plain object from a CartesianMatrix message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.CartesianMatrix
             * @static
             * @param {lebai.posture.CartesianMatrix} message CartesianMatrix
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CartesianMatrix.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.m11 = 0;
                    object.m12 = 0;
                    object.m13 = 0;
                    object.m14 = 0;
                    object.m21 = 0;
                    object.m22 = 0;
                    object.m23 = 0;
                    object.m24 = 0;
                    object.m31 = 0;
                    object.m32 = 0;
                    object.m33 = 0;
                    object.m34 = 0;
                    object.m41 = 0;
                    object.m42 = 0;
                    object.m43 = 0;
                    object.m44 = 0;
                }
                if (message.m11 != null && $Object.hasOwnProperty.call(message, "m11"))
                    object.m11 = options.json && !$isFinite(message.m11) ? $String(message.m11) : message.m11;
                if (message.m12 != null && $Object.hasOwnProperty.call(message, "m12"))
                    object.m12 = options.json && !$isFinite(message.m12) ? $String(message.m12) : message.m12;
                if (message.m13 != null && $Object.hasOwnProperty.call(message, "m13"))
                    object.m13 = options.json && !$isFinite(message.m13) ? $String(message.m13) : message.m13;
                if (message.m14 != null && $Object.hasOwnProperty.call(message, "m14"))
                    object.m14 = options.json && !$isFinite(message.m14) ? $String(message.m14) : message.m14;
                if (message.m21 != null && $Object.hasOwnProperty.call(message, "m21"))
                    object.m21 = options.json && !$isFinite(message.m21) ? $String(message.m21) : message.m21;
                if (message.m22 != null && $Object.hasOwnProperty.call(message, "m22"))
                    object.m22 = options.json && !$isFinite(message.m22) ? $String(message.m22) : message.m22;
                if (message.m23 != null && $Object.hasOwnProperty.call(message, "m23"))
                    object.m23 = options.json && !$isFinite(message.m23) ? $String(message.m23) : message.m23;
                if (message.m24 != null && $Object.hasOwnProperty.call(message, "m24"))
                    object.m24 = options.json && !$isFinite(message.m24) ? $String(message.m24) : message.m24;
                if (message.m31 != null && $Object.hasOwnProperty.call(message, "m31"))
                    object.m31 = options.json && !$isFinite(message.m31) ? $String(message.m31) : message.m31;
                if (message.m32 != null && $Object.hasOwnProperty.call(message, "m32"))
                    object.m32 = options.json && !$isFinite(message.m32) ? $String(message.m32) : message.m32;
                if (message.m33 != null && $Object.hasOwnProperty.call(message, "m33"))
                    object.m33 = options.json && !$isFinite(message.m33) ? $String(message.m33) : message.m33;
                if (message.m34 != null && $Object.hasOwnProperty.call(message, "m34"))
                    object.m34 = options.json && !$isFinite(message.m34) ? $String(message.m34) : message.m34;
                if (message.m41 != null && $Object.hasOwnProperty.call(message, "m41"))
                    object.m41 = options.json && !$isFinite(message.m41) ? $String(message.m41) : message.m41;
                if (message.m42 != null && $Object.hasOwnProperty.call(message, "m42"))
                    object.m42 = options.json && !$isFinite(message.m42) ? $String(message.m42) : message.m42;
                if (message.m43 != null && $Object.hasOwnProperty.call(message, "m43"))
                    object.m43 = options.json && !$isFinite(message.m43) ? $String(message.m43) : message.m43;
                if (message.m44 != null && $Object.hasOwnProperty.call(message, "m44"))
                    object.m44 = options.json && !$isFinite(message.m44) ? $String(message.m44) : message.m44;
                return object;
            };

            /**
             * Converts this CartesianMatrix to JSON.
             * @function toJSON
             * @memberof lebai.posture.CartesianMatrix
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CartesianMatrix.prototype.toJSON = function() {
                return CartesianMatrix.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CartesianMatrix
             * @function getTypeUrl
             * @memberof lebai.posture.CartesianMatrix
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CartesianMatrix.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.CartesianMatrix";
            };

            return CartesianMatrix;
        })();

        posture.CartesianFrame = (function() {

            /**
             * Properties of a CartesianFrame.
             * @typedef {Object} lebai.posture.CartesianFrame.$Properties
             * @property {lebai.posture.CartesianFrame.Kind|null} [positionKind] CartesianFrame positionKind
             * @property {lebai.posture.Position.$Properties|null} [position] CartesianFrame position
             * @property {lebai.posture.CartesianFrame.Kind|null} [rotationKind] CartesianFrame rotationKind
             * @property {lebai.posture.Rotation.$Properties|null} [rotation] CartesianFrame rotation
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CartesianFrame.
             * @memberof lebai.posture
             * @interface ICartesianFrame
             * @augments lebai.posture.CartesianFrame.$Properties
             * @deprecated Use lebai.posture.CartesianFrame.$Properties instead.
             */

            /**
             * Shape of a CartesianFrame.
             * @typedef {lebai.posture.CartesianFrame.$Properties} lebai.posture.CartesianFrame.$Shape
             */

            /**
             * Constructs a new CartesianFrame.
             * @memberof lebai.posture
             * @classdesc Represents a CartesianFrame.
             * @constructor
             * @param {lebai.posture.CartesianFrame.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var CartesianFrame = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CartesianFrame positionKind.
             * @member {lebai.posture.CartesianFrame.Kind} positionKind
             * @memberof lebai.posture.CartesianFrame
             * @instance
             */
            CartesianFrame.prototype.positionKind = 0;

            /**
             * CartesianFrame position.
             * @member {lebai.posture.Position.$Properties|null|undefined} position
             * @memberof lebai.posture.CartesianFrame
             * @instance
             */
            CartesianFrame.prototype.position = null;

            /**
             * CartesianFrame rotationKind.
             * @member {lebai.posture.CartesianFrame.Kind} rotationKind
             * @memberof lebai.posture.CartesianFrame
             * @instance
             */
            CartesianFrame.prototype.rotationKind = 0;

            /**
             * CartesianFrame rotation.
             * @member {lebai.posture.Rotation.$Properties|null|undefined} rotation
             * @memberof lebai.posture.CartesianFrame
             * @instance
             */
            CartesianFrame.prototype.rotation = null;

            /**
             * Creates a new CartesianFrame instance using the specified properties.
             * @function create
             * @memberof lebai.posture.CartesianFrame
             * @static
             * @param {lebai.posture.CartesianFrame.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.CartesianFrame} CartesianFrame instance
             * @type {{
             *   (properties: lebai.posture.CartesianFrame.$Shape): lebai.posture.CartesianFrame & lebai.posture.CartesianFrame.$Shape;
             *   (properties?: lebai.posture.CartesianFrame.$Properties): lebai.posture.CartesianFrame;
             * }}
             */
            CartesianFrame.create = function(properties) {
                return new CartesianFrame(properties);
            };

            /**
             * Encodes the specified CartesianFrame message. Does not implicitly {@link lebai.posture.CartesianFrame.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.CartesianFrame
             * @static
             * @param {lebai.posture.CartesianFrame.$Properties} message CartesianFrame message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CartesianFrame.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.positionKind != null && $Object.hasOwnProperty.call(message, "positionKind") && message.positionKind !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.positionKind);
                if (message.position != null && $Object.hasOwnProperty.call(message, "position"))
                    $root.lebai.posture.Position.encode(message.position, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.rotationKind != null && $Object.hasOwnProperty.call(message, "rotationKind") && message.rotationKind !== 0)
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.rotationKind);
                if (message.rotation != null && $Object.hasOwnProperty.call(message, "rotation"))
                    $root.lebai.posture.Rotation.encode(message.rotation, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CartesianFrame message, length delimited. Does not implicitly {@link lebai.posture.CartesianFrame.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.CartesianFrame
             * @static
             * @param {lebai.posture.CartesianFrame.$Properties} message CartesianFrame message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CartesianFrame.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a CartesianFrame message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.CartesianFrame
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.CartesianFrame & lebai.posture.CartesianFrame.$Shape} CartesianFrame
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CartesianFrame.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.CartesianFrame();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.positionKind = value;
                            else
                                delete message.positionKind;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.position = $root.lebai.posture.Position.decode(reader, reader.uint32(), $undefined, _depth + 1, message.position);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.rotationKind = value;
                            else
                                delete message.rotationKind;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.rotation = $root.lebai.posture.Rotation.decode(reader, reader.uint32(), $undefined, _depth + 1, message.rotation);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a CartesianFrame message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.CartesianFrame
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.CartesianFrame & lebai.posture.CartesianFrame.$Shape} CartesianFrame
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CartesianFrame.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CartesianFrame message.
             * @function verify
             * @memberof lebai.posture.CartesianFrame
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CartesianFrame.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.positionKind != null && $Object.hasOwnProperty.call(message, "positionKind"))
                    if (typeof message.positionKind !== "number" || (message.positionKind | 0) !== message.positionKind)
                        return "positionKind: enum value expected";
                if (message.position != null && $Object.hasOwnProperty.call(message, "position")) {
                    var error = $root.lebai.posture.Position.verify(message.position, _depth + 1);
                    if (error)
                        return "position." + error;
                }
                if (message.rotationKind != null && $Object.hasOwnProperty.call(message, "rotationKind"))
                    if (typeof message.rotationKind !== "number" || (message.rotationKind | 0) !== message.rotationKind)
                        return "rotationKind: enum value expected";
                if (message.rotation != null && $Object.hasOwnProperty.call(message, "rotation")) {
                    var error = $root.lebai.posture.Rotation.verify(message.rotation, _depth + 1);
                    if (error)
                        return "rotation." + error;
                }
                return null;
            };

            /**
             * Creates a CartesianFrame message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.CartesianFrame
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.CartesianFrame} CartesianFrame
             */
            CartesianFrame.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.CartesianFrame)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.CartesianFrame: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.CartesianFrame();
                if (object.positionKind !== 0 && (typeof object.positionKind !== "string" || $root.lebai.posture.CartesianFrame.Kind[object.positionKind] !== 0))
                    switch (object.positionKind) {
                    case "BASE":
                    case 0:
                        message.positionKind = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.positionKind = 1;
                        break;
                    case "TCP":
                    case 2:
                        message.positionKind = 2;
                        break;
                    case "LAST_FLANGE":
                    case 11:
                        message.positionKind = 11;
                        break;
                    case "LAST_TCP":
                    case 12:
                        message.positionKind = 12;
                        break;
                    case "CUSTOM":
                    case 99:
                        message.positionKind = 99;
                        break;
                    default:
                        if (typeof object.positionKind === "number" && (object.positionKind | 0) === object.positionKind)
                            message.positionKind = object.positionKind;
                    }
                if (object.position != null) {
                    if (!$util.isObject(object.position))
                        throw $TypeError(".lebai.posture.CartesianFrame.position: object expected");
                    message.position = $root.lebai.posture.Position.fromObject(object.position, _depth + 1);
                }
                if (object.rotationKind !== 0 && (typeof object.rotationKind !== "string" || $root.lebai.posture.CartesianFrame.Kind[object.rotationKind] !== 0))
                    switch (object.rotationKind) {
                    case "BASE":
                    case 0:
                        message.rotationKind = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.rotationKind = 1;
                        break;
                    case "TCP":
                    case 2:
                        message.rotationKind = 2;
                        break;
                    case "LAST_FLANGE":
                    case 11:
                        message.rotationKind = 11;
                        break;
                    case "LAST_TCP":
                    case 12:
                        message.rotationKind = 12;
                        break;
                    case "CUSTOM":
                    case 99:
                        message.rotationKind = 99;
                        break;
                    default:
                        if (typeof object.rotationKind === "number" && (object.rotationKind | 0) === object.rotationKind)
                            message.rotationKind = object.rotationKind;
                    }
                if (object.rotation != null) {
                    if (!$util.isObject(object.rotation))
                        throw $TypeError(".lebai.posture.CartesianFrame.rotation: object expected");
                    message.rotation = $root.lebai.posture.Rotation.fromObject(object.rotation, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a CartesianFrame message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.CartesianFrame
             * @static
             * @param {lebai.posture.CartesianFrame} message CartesianFrame
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CartesianFrame.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.positionKind = options.enums === $String ? "BASE" : 0;
                    object.position = null;
                    object.rotationKind = options.enums === $String ? "BASE" : 0;
                    object.rotation = null;
                }
                if (message.positionKind != null && $Object.hasOwnProperty.call(message, "positionKind"))
                    object.positionKind = options.enums === $String ? $root.lebai.posture.CartesianFrame.Kind[message.positionKind] === $undefined ? message.positionKind : $root.lebai.posture.CartesianFrame.Kind[message.positionKind] : message.positionKind;
                if (message.position != null && $Object.hasOwnProperty.call(message, "position"))
                    object.position = $root.lebai.posture.Position.toObject(message.position, options, _depth + 1);
                if (message.rotationKind != null && $Object.hasOwnProperty.call(message, "rotationKind"))
                    object.rotationKind = options.enums === $String ? $root.lebai.posture.CartesianFrame.Kind[message.rotationKind] === $undefined ? message.rotationKind : $root.lebai.posture.CartesianFrame.Kind[message.rotationKind] : message.rotationKind;
                if (message.rotation != null && $Object.hasOwnProperty.call(message, "rotation"))
                    object.rotation = $root.lebai.posture.Rotation.toObject(message.rotation, options, _depth + 1);
                return object;
            };

            /**
             * Converts this CartesianFrame to JSON.
             * @function toJSON
             * @memberof lebai.posture.CartesianFrame
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CartesianFrame.prototype.toJSON = function() {
                return CartesianFrame.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CartesianFrame
             * @function getTypeUrl
             * @memberof lebai.posture.CartesianFrame
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CartesianFrame.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.CartesianFrame";
            };

            /**
             * Kind enum.
             * @name lebai.posture.CartesianFrame.Kind
             * @enum {number}
             * @property {number} BASE=0 BASE value
             * @property {number} FLANGE=1 FLANGE value
             * @property {number} TCP=2 TCP value
             * @property {number} LAST_FLANGE=11 LAST_FLANGE value
             * @property {number} LAST_TCP=12 LAST_TCP value
             * @property {number} CUSTOM=99 CUSTOM value
             */
            CartesianFrame.Kind = (function() {
                var valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "BASE"] = 0;
                values[valuesById[1] = "FLANGE"] = 1;
                values[valuesById[2] = "TCP"] = 2;
                values[valuesById[11] = "LAST_FLANGE"] = 11;
                values[valuesById[12] = "LAST_TCP"] = 12;
                values[valuesById[99] = "CUSTOM"] = 99;
                return values;
            })();

            return CartesianFrame;
        })();

        posture.Pose = (function() {

            /**
             * Properties of a Pose.
             * @typedef {Object} lebai.posture.Pose.$Properties
             * @property {lebai.posture.Pose.Kind|null} [kind] Pose kind
             * @property {lebai.posture.CartesianPose.$Properties|null} [cart] Pose cart
             * @property {lebai.db.LoadRequest.$Properties|null} [cartFrameIndex] Pose cartFrameIndex
             * @property {lebai.posture.CartesianFrame.$Properties|null} [cartFrame] Pose cartFrame
             * @property {lebai.posture.JointPose.$Properties|null} [joint] Pose joint
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Pose.
             * @memberof lebai.posture
             * @interface IPose
             * @augments lebai.posture.Pose.$Properties
             * @deprecated Use lebai.posture.Pose.$Properties instead.
             */

            /**
             * Shape of a Pose.
             * @typedef {lebai.posture.Pose.$Properties} lebai.posture.Pose.$Shape
             */

            /**
             * Constructs a new Pose.
             * @memberof lebai.posture
             * @classdesc Represents a Pose.
             * @constructor
             * @param {lebai.posture.Pose.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Pose = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Pose kind.
             * @member {lebai.posture.Pose.Kind} kind
             * @memberof lebai.posture.Pose
             * @instance
             */
            Pose.prototype.kind = 0;

            /**
             * Pose cart.
             * @member {lebai.posture.CartesianPose.$Properties|null|undefined} cart
             * @memberof lebai.posture.Pose
             * @instance
             */
            Pose.prototype.cart = null;

            /**
             * Pose cartFrameIndex.
             * @member {lebai.db.LoadRequest.$Properties|null|undefined} cartFrameIndex
             * @memberof lebai.posture.Pose
             * @instance
             */
            Pose.prototype.cartFrameIndex = null;

            /**
             * Pose cartFrame.
             * @member {lebai.posture.CartesianFrame.$Properties|null|undefined} cartFrame
             * @memberof lebai.posture.Pose
             * @instance
             */
            Pose.prototype.cartFrame = null;

            /**
             * Pose joint.
             * @member {lebai.posture.JointPose.$Properties|null|undefined} joint
             * @memberof lebai.posture.Pose
             * @instance
             */
            Pose.prototype.joint = null;

            /**
             * Creates a new Pose instance using the specified properties.
             * @function create
             * @memberof lebai.posture.Pose
             * @static
             * @param {lebai.posture.Pose.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.Pose} Pose instance
             * @type {{
             *   (properties: lebai.posture.Pose.$Shape): lebai.posture.Pose & lebai.posture.Pose.$Shape;
             *   (properties?: lebai.posture.Pose.$Properties): lebai.posture.Pose;
             * }}
             */
            Pose.create = function(properties) {
                return new Pose(properties);
            };

            /**
             * Encodes the specified Pose message. Does not implicitly {@link lebai.posture.Pose.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.Pose
             * @static
             * @param {lebai.posture.Pose.$Properties} message Pose message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Pose.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind") && message.kind !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.kind);
                if (message.cart != null && $Object.hasOwnProperty.call(message, "cart"))
                    $root.lebai.posture.CartesianPose.encode(message.cart, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.cartFrameIndex != null && $Object.hasOwnProperty.call(message, "cartFrameIndex"))
                    $root.lebai.db.LoadRequest.encode(message.cartFrameIndex, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
                if (message.cartFrame != null && $Object.hasOwnProperty.call(message, "cartFrame"))
                    $root.lebai.posture.CartesianFrame.encode(message.cartFrame, writer.uint32(/* id 13, wireType 2 =*/106).fork(), _depth + 1).ldelim();
                if (message.joint != null && $Object.hasOwnProperty.call(message, "joint"))
                    $root.lebai.posture.JointPose.encode(message.joint, writer.uint32(/* id 21, wireType 2 =*/170).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Pose message, length delimited. Does not implicitly {@link lebai.posture.Pose.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.Pose
             * @static
             * @param {lebai.posture.Pose.$Properties} message Pose message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Pose.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Pose message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.Pose
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.Pose & lebai.posture.Pose.$Shape} Pose
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Pose.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.Pose();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.kind = value;
                            else
                                delete message.kind;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.cart = $root.lebai.posture.CartesianPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cart);
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            message.cartFrameIndex = $root.lebai.db.LoadRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cartFrameIndex);
                            continue;
                        }
                    case 13: {
                            if (wireType !== 2)
                                break;
                            message.cartFrame = $root.lebai.posture.CartesianFrame.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cartFrame);
                            continue;
                        }
                    case 21: {
                            if (wireType !== 2)
                                break;
                            message.joint = $root.lebai.posture.JointPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.joint);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Pose message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.Pose
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.Pose & lebai.posture.Pose.$Shape} Pose
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Pose.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Pose message.
             * @function verify
             * @memberof lebai.posture.Pose
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Pose.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    if (typeof message.kind !== "number" || (message.kind | 0) !== message.kind)
                        return "kind: enum value expected";
                if (message.cart != null && $Object.hasOwnProperty.call(message, "cart")) {
                    var error = $root.lebai.posture.CartesianPose.verify(message.cart, _depth + 1);
                    if (error)
                        return "cart." + error;
                }
                if (message.cartFrameIndex != null && $Object.hasOwnProperty.call(message, "cartFrameIndex")) {
                    var error = $root.lebai.db.LoadRequest.verify(message.cartFrameIndex, _depth + 1);
                    if (error)
                        return "cartFrameIndex." + error;
                }
                if (message.cartFrame != null && $Object.hasOwnProperty.call(message, "cartFrame")) {
                    var error = $root.lebai.posture.CartesianFrame.verify(message.cartFrame, _depth + 1);
                    if (error)
                        return "cartFrame." + error;
                }
                if (message.joint != null && $Object.hasOwnProperty.call(message, "joint")) {
                    var error = $root.lebai.posture.JointPose.verify(message.joint, _depth + 1);
                    if (error)
                        return "joint." + error;
                }
                return null;
            };

            /**
             * Creates a Pose message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.Pose
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.Pose} Pose
             */
            Pose.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.Pose)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.Pose: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.Pose();
                if (object.kind !== 0 && (typeof object.kind !== "string" || $root.lebai.posture.Pose.Kind[object.kind] !== 0))
                    switch (object.kind) {
                    case "UNKNOWN":
                    case 0:
                        message.kind = 0;
                        break;
                    case "JOINT":
                    case 1:
                        message.kind = 1;
                        break;
                    case "CARTESIAN":
                    case 2:
                        message.kind = 2;
                        break;
                    default:
                        if (typeof object.kind === "number" && (object.kind | 0) === object.kind)
                            message.kind = object.kind;
                    }
                if (object.cart != null) {
                    if (!$util.isObject(object.cart))
                        throw $TypeError(".lebai.posture.Pose.cart: object expected");
                    message.cart = $root.lebai.posture.CartesianPose.fromObject(object.cart, _depth + 1);
                }
                if (object.cartFrameIndex != null) {
                    if (!$util.isObject(object.cartFrameIndex))
                        throw $TypeError(".lebai.posture.Pose.cartFrameIndex: object expected");
                    message.cartFrameIndex = $root.lebai.db.LoadRequest.fromObject(object.cartFrameIndex, _depth + 1);
                }
                if (object.cartFrame != null) {
                    if (!$util.isObject(object.cartFrame))
                        throw $TypeError(".lebai.posture.Pose.cartFrame: object expected");
                    message.cartFrame = $root.lebai.posture.CartesianFrame.fromObject(object.cartFrame, _depth + 1);
                }
                if (object.joint != null) {
                    if (!$util.isObject(object.joint))
                        throw $TypeError(".lebai.posture.Pose.joint: object expected");
                    message.joint = $root.lebai.posture.JointPose.fromObject(object.joint, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Pose message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.Pose
             * @static
             * @param {lebai.posture.Pose} message Pose
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Pose.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.kind = options.enums === $String ? "UNKNOWN" : 0;
                    object.cart = null;
                    object.cartFrameIndex = null;
                    object.cartFrame = null;
                    object.joint = null;
                }
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    object.kind = options.enums === $String ? $root.lebai.posture.Pose.Kind[message.kind] === $undefined ? message.kind : $root.lebai.posture.Pose.Kind[message.kind] : message.kind;
                if (message.cart != null && $Object.hasOwnProperty.call(message, "cart"))
                    object.cart = $root.lebai.posture.CartesianPose.toObject(message.cart, options, _depth + 1);
                if (message.cartFrameIndex != null && $Object.hasOwnProperty.call(message, "cartFrameIndex"))
                    object.cartFrameIndex = $root.lebai.db.LoadRequest.toObject(message.cartFrameIndex, options, _depth + 1);
                if (message.cartFrame != null && $Object.hasOwnProperty.call(message, "cartFrame"))
                    object.cartFrame = $root.lebai.posture.CartesianFrame.toObject(message.cartFrame, options, _depth + 1);
                if (message.joint != null && $Object.hasOwnProperty.call(message, "joint"))
                    object.joint = $root.lebai.posture.JointPose.toObject(message.joint, options, _depth + 1);
                return object;
            };

            /**
             * Converts this Pose to JSON.
             * @function toJSON
             * @memberof lebai.posture.Pose
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Pose.prototype.toJSON = function() {
                return Pose.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Pose
             * @function getTypeUrl
             * @memberof lebai.posture.Pose
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Pose.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.Pose";
            };

            /**
             * Kind enum.
             * @name lebai.posture.Pose.Kind
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} JOINT=1 JOINT value
             * @property {number} CARTESIAN=2 CARTESIAN value
             */
            Pose.Kind = (function() {
                var valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "JOINT"] = 1;
                values[valuesById[2] = "CARTESIAN"] = 2;
                return values;
            })();

            return Pose;
        })();

        posture.SavePoseRequest = (function() {

            /**
             * Properties of a SavePoseRequest.
             * @typedef {Object} lebai.posture.SavePoseRequest.$Properties
             * @property {string|null} [name] SavePoseRequest name
             * @property {lebai.posture.Pose.$Properties|null} [data] SavePoseRequest data
             * @property {string|null} [dir] SavePoseRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SavePoseRequest.
             * @memberof lebai.posture
             * @interface ISavePoseRequest
             * @augments lebai.posture.SavePoseRequest.$Properties
             * @deprecated Use lebai.posture.SavePoseRequest.$Properties instead.
             */

            /**
             * Shape of a SavePoseRequest.
             * @typedef {lebai.posture.SavePoseRequest.$Properties} lebai.posture.SavePoseRequest.$Shape
             */

            /**
             * Constructs a new SavePoseRequest.
             * @memberof lebai.posture
             * @classdesc Represents a SavePoseRequest.
             * @constructor
             * @param {lebai.posture.SavePoseRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SavePoseRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SavePoseRequest name.
             * @member {string} name
             * @memberof lebai.posture.SavePoseRequest
             * @instance
             */
            SavePoseRequest.prototype.name = "";

            /**
             * SavePoseRequest data.
             * @member {lebai.posture.Pose.$Properties|null|undefined} data
             * @memberof lebai.posture.SavePoseRequest
             * @instance
             */
            SavePoseRequest.prototype.data = null;

            /**
             * SavePoseRequest dir.
             * @member {string} dir
             * @memberof lebai.posture.SavePoseRequest
             * @instance
             */
            SavePoseRequest.prototype.dir = "";

            /**
             * Creates a new SavePoseRequest instance using the specified properties.
             * @function create
             * @memberof lebai.posture.SavePoseRequest
             * @static
             * @param {lebai.posture.SavePoseRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.SavePoseRequest} SavePoseRequest instance
             * @type {{
             *   (properties: lebai.posture.SavePoseRequest.$Shape): lebai.posture.SavePoseRequest & lebai.posture.SavePoseRequest.$Shape;
             *   (properties?: lebai.posture.SavePoseRequest.$Properties): lebai.posture.SavePoseRequest;
             * }}
             */
            SavePoseRequest.create = function(properties) {
                return new SavePoseRequest(properties);
            };

            /**
             * Encodes the specified SavePoseRequest message. Does not implicitly {@link lebai.posture.SavePoseRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.SavePoseRequest
             * @static
             * @param {lebai.posture.SavePoseRequest.$Properties} message SavePoseRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SavePoseRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    $root.lebai.posture.Pose.encode(message.data, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SavePoseRequest message, length delimited. Does not implicitly {@link lebai.posture.SavePoseRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.SavePoseRequest
             * @static
             * @param {lebai.posture.SavePoseRequest.$Properties} message SavePoseRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SavePoseRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SavePoseRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.SavePoseRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.SavePoseRequest & lebai.posture.SavePoseRequest.$Shape} SavePoseRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SavePoseRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.SavePoseRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.data = $root.lebai.posture.Pose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.data);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SavePoseRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.SavePoseRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.SavePoseRequest & lebai.posture.SavePoseRequest.$Shape} SavePoseRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SavePoseRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SavePoseRequest message.
             * @function verify
             * @memberof lebai.posture.SavePoseRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SavePoseRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    var error = $root.lebai.posture.Pose.verify(message.data, _depth + 1);
                    if (error)
                        return "data." + error;
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates a SavePoseRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.SavePoseRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.SavePoseRequest} SavePoseRequest
             */
            SavePoseRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.SavePoseRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.SavePoseRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.SavePoseRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.data != null) {
                    if (!$util.isObject(object.data))
                        throw $TypeError(".lebai.posture.SavePoseRequest.data: object expected");
                    message.data = $root.lebai.posture.Pose.fromObject(object.data, _depth + 1);
                }
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from a SavePoseRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.SavePoseRequest
             * @static
             * @param {lebai.posture.SavePoseRequest} message SavePoseRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SavePoseRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.data = null;
                    object.dir = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = $root.lebai.posture.Pose.toObject(message.data, options, _depth + 1);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                return object;
            };

            /**
             * Converts this SavePoseRequest to JSON.
             * @function toJSON
             * @memberof lebai.posture.SavePoseRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SavePoseRequest.prototype.toJSON = function() {
                return SavePoseRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SavePoseRequest
             * @function getTypeUrl
             * @memberof lebai.posture.SavePoseRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SavePoseRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.SavePoseRequest";
            };

            return SavePoseRequest;
        })();

        posture.SaveFrameRequest = (function() {

            /**
             * Properties of a SaveFrameRequest.
             * @typedef {Object} lebai.posture.SaveFrameRequest.$Properties
             * @property {string|null} [name] SaveFrameRequest name
             * @property {lebai.posture.CartesianFrame.$Properties|null} [data] SaveFrameRequest data
             * @property {string|null} [dir] SaveFrameRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SaveFrameRequest.
             * @memberof lebai.posture
             * @interface ISaveFrameRequest
             * @augments lebai.posture.SaveFrameRequest.$Properties
             * @deprecated Use lebai.posture.SaveFrameRequest.$Properties instead.
             */

            /**
             * Shape of a SaveFrameRequest.
             * @typedef {lebai.posture.SaveFrameRequest.$Properties} lebai.posture.SaveFrameRequest.$Shape
             */

            /**
             * Constructs a new SaveFrameRequest.
             * @memberof lebai.posture
             * @classdesc Represents a SaveFrameRequest.
             * @constructor
             * @param {lebai.posture.SaveFrameRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SaveFrameRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SaveFrameRequest name.
             * @member {string} name
             * @memberof lebai.posture.SaveFrameRequest
             * @instance
             */
            SaveFrameRequest.prototype.name = "";

            /**
             * SaveFrameRequest data.
             * @member {lebai.posture.CartesianFrame.$Properties|null|undefined} data
             * @memberof lebai.posture.SaveFrameRequest
             * @instance
             */
            SaveFrameRequest.prototype.data = null;

            /**
             * SaveFrameRequest dir.
             * @member {string} dir
             * @memberof lebai.posture.SaveFrameRequest
             * @instance
             */
            SaveFrameRequest.prototype.dir = "";

            /**
             * Creates a new SaveFrameRequest instance using the specified properties.
             * @function create
             * @memberof lebai.posture.SaveFrameRequest
             * @static
             * @param {lebai.posture.SaveFrameRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.SaveFrameRequest} SaveFrameRequest instance
             * @type {{
             *   (properties: lebai.posture.SaveFrameRequest.$Shape): lebai.posture.SaveFrameRequest & lebai.posture.SaveFrameRequest.$Shape;
             *   (properties?: lebai.posture.SaveFrameRequest.$Properties): lebai.posture.SaveFrameRequest;
             * }}
             */
            SaveFrameRequest.create = function(properties) {
                return new SaveFrameRequest(properties);
            };

            /**
             * Encodes the specified SaveFrameRequest message. Does not implicitly {@link lebai.posture.SaveFrameRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.SaveFrameRequest
             * @static
             * @param {lebai.posture.SaveFrameRequest.$Properties} message SaveFrameRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveFrameRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    $root.lebai.posture.CartesianFrame.encode(message.data, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SaveFrameRequest message, length delimited. Does not implicitly {@link lebai.posture.SaveFrameRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.SaveFrameRequest
             * @static
             * @param {lebai.posture.SaveFrameRequest.$Properties} message SaveFrameRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveFrameRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SaveFrameRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.SaveFrameRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.SaveFrameRequest & lebai.posture.SaveFrameRequest.$Shape} SaveFrameRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveFrameRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.SaveFrameRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.data = $root.lebai.posture.CartesianFrame.decode(reader, reader.uint32(), $undefined, _depth + 1, message.data);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SaveFrameRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.SaveFrameRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.SaveFrameRequest & lebai.posture.SaveFrameRequest.$Shape} SaveFrameRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveFrameRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SaveFrameRequest message.
             * @function verify
             * @memberof lebai.posture.SaveFrameRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SaveFrameRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    var error = $root.lebai.posture.CartesianFrame.verify(message.data, _depth + 1);
                    if (error)
                        return "data." + error;
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates a SaveFrameRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.SaveFrameRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.SaveFrameRequest} SaveFrameRequest
             */
            SaveFrameRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.SaveFrameRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.SaveFrameRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.SaveFrameRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.data != null) {
                    if (!$util.isObject(object.data))
                        throw $TypeError(".lebai.posture.SaveFrameRequest.data: object expected");
                    message.data = $root.lebai.posture.CartesianFrame.fromObject(object.data, _depth + 1);
                }
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from a SaveFrameRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.SaveFrameRequest
             * @static
             * @param {lebai.posture.SaveFrameRequest} message SaveFrameRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SaveFrameRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.data = null;
                    object.dir = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = $root.lebai.posture.CartesianFrame.toObject(message.data, options, _depth + 1);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                return object;
            };

            /**
             * Converts this SaveFrameRequest to JSON.
             * @function toJSON
             * @memberof lebai.posture.SaveFrameRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SaveFrameRequest.prototype.toJSON = function() {
                return SaveFrameRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SaveFrameRequest
             * @function getTypeUrl
             * @memberof lebai.posture.SaveFrameRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SaveFrameRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.SaveFrameRequest";
            };

            return SaveFrameRequest;
        })();

        posture.PoseRequest = (function() {

            /**
             * Properties of a PoseRequest.
             * @typedef {Object} lebai.posture.PoseRequest.$Properties
             * @property {lebai.posture.Pose.$Properties|null} [pose] PoseRequest pose
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PoseRequest.
             * @memberof lebai.posture
             * @interface IPoseRequest
             * @augments lebai.posture.PoseRequest.$Properties
             * @deprecated Use lebai.posture.PoseRequest.$Properties instead.
             */

            /**
             * Shape of a PoseRequest.
             * @typedef {lebai.posture.PoseRequest.$Properties} lebai.posture.PoseRequest.$Shape
             */

            /**
             * Constructs a new PoseRequest.
             * @memberof lebai.posture
             * @classdesc Represents a PoseRequest.
             * @constructor
             * @param {lebai.posture.PoseRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var PoseRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PoseRequest pose.
             * @member {lebai.posture.Pose.$Properties|null|undefined} pose
             * @memberof lebai.posture.PoseRequest
             * @instance
             */
            PoseRequest.prototype.pose = null;

            /**
             * Creates a new PoseRequest instance using the specified properties.
             * @function create
             * @memberof lebai.posture.PoseRequest
             * @static
             * @param {lebai.posture.PoseRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.PoseRequest} PoseRequest instance
             * @type {{
             *   (properties: lebai.posture.PoseRequest.$Shape): lebai.posture.PoseRequest & lebai.posture.PoseRequest.$Shape;
             *   (properties?: lebai.posture.PoseRequest.$Properties): lebai.posture.PoseRequest;
             * }}
             */
            PoseRequest.create = function(properties) {
                return new PoseRequest(properties);
            };

            /**
             * Encodes the specified PoseRequest message. Does not implicitly {@link lebai.posture.PoseRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.PoseRequest
             * @static
             * @param {lebai.posture.PoseRequest.$Properties} message PoseRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PoseRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    $root.lebai.posture.Pose.encode(message.pose, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PoseRequest message, length delimited. Does not implicitly {@link lebai.posture.PoseRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.PoseRequest
             * @static
             * @param {lebai.posture.PoseRequest.$Properties} message PoseRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PoseRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a PoseRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.PoseRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.PoseRequest & lebai.posture.PoseRequest.$Shape} PoseRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PoseRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.PoseRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.pose = $root.lebai.posture.Pose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pose);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a PoseRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.PoseRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.PoseRequest & lebai.posture.PoseRequest.$Shape} PoseRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PoseRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PoseRequest message.
             * @function verify
             * @memberof lebai.posture.PoseRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PoseRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose")) {
                    var error = $root.lebai.posture.Pose.verify(message.pose, _depth + 1);
                    if (error)
                        return "pose." + error;
                }
                return null;
            };

            /**
             * Creates a PoseRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.PoseRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.PoseRequest} PoseRequest
             */
            PoseRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.PoseRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.PoseRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.PoseRequest();
                if (object.pose != null) {
                    if (!$util.isObject(object.pose))
                        throw $TypeError(".lebai.posture.PoseRequest.pose: object expected");
                    message.pose = $root.lebai.posture.Pose.fromObject(object.pose, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a PoseRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.PoseRequest
             * @static
             * @param {lebai.posture.PoseRequest} message PoseRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PoseRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.pose = null;
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    object.pose = $root.lebai.posture.Pose.toObject(message.pose, options, _depth + 1);
                return object;
            };

            /**
             * Converts this PoseRequest to JSON.
             * @function toJSON
             * @memberof lebai.posture.PoseRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PoseRequest.prototype.toJSON = function() {
                return PoseRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PoseRequest
             * @function getTypeUrl
             * @memberof lebai.posture.PoseRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PoseRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.PoseRequest";
            };

            return PoseRequest;
        })();

        posture.GetInverseKinRequest = (function() {

            /**
             * Properties of a GetInverseKinRequest.
             * @typedef {Object} lebai.posture.GetInverseKinRequest.$Properties
             * @property {lebai.posture.Pose.$Properties|null} [pose] GetInverseKinRequest pose
             * @property {lebai.posture.JointPose.$Properties|null} [refer] GetInverseKinRequest refer
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetInverseKinRequest.
             * @memberof lebai.posture
             * @interface IGetInverseKinRequest
             * @augments lebai.posture.GetInverseKinRequest.$Properties
             * @deprecated Use lebai.posture.GetInverseKinRequest.$Properties instead.
             */

            /**
             * Shape of a GetInverseKinRequest.
             * @typedef {lebai.posture.GetInverseKinRequest.$Properties} lebai.posture.GetInverseKinRequest.$Shape
             */

            /**
             * Constructs a new GetInverseKinRequest.
             * @memberof lebai.posture
             * @classdesc Represents a GetInverseKinRequest.
             * @constructor
             * @param {lebai.posture.GetInverseKinRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetInverseKinRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetInverseKinRequest pose.
             * @member {lebai.posture.Pose.$Properties|null|undefined} pose
             * @memberof lebai.posture.GetInverseKinRequest
             * @instance
             */
            GetInverseKinRequest.prototype.pose = null;

            /**
             * GetInverseKinRequest refer.
             * @member {lebai.posture.JointPose.$Properties|null|undefined} refer
             * @memberof lebai.posture.GetInverseKinRequest
             * @instance
             */
            GetInverseKinRequest.prototype.refer = null;

            /**
             * Creates a new GetInverseKinRequest instance using the specified properties.
             * @function create
             * @memberof lebai.posture.GetInverseKinRequest
             * @static
             * @param {lebai.posture.GetInverseKinRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.GetInverseKinRequest} GetInverseKinRequest instance
             * @type {{
             *   (properties: lebai.posture.GetInverseKinRequest.$Shape): lebai.posture.GetInverseKinRequest & lebai.posture.GetInverseKinRequest.$Shape;
             *   (properties?: lebai.posture.GetInverseKinRequest.$Properties): lebai.posture.GetInverseKinRequest;
             * }}
             */
            GetInverseKinRequest.create = function(properties) {
                return new GetInverseKinRequest(properties);
            };

            /**
             * Encodes the specified GetInverseKinRequest message. Does not implicitly {@link lebai.posture.GetInverseKinRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.GetInverseKinRequest
             * @static
             * @param {lebai.posture.GetInverseKinRequest.$Properties} message GetInverseKinRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetInverseKinRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    $root.lebai.posture.Pose.encode(message.pose, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.refer != null && $Object.hasOwnProperty.call(message, "refer"))
                    $root.lebai.posture.JointPose.encode(message.refer, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetInverseKinRequest message, length delimited. Does not implicitly {@link lebai.posture.GetInverseKinRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.GetInverseKinRequest
             * @static
             * @param {lebai.posture.GetInverseKinRequest.$Properties} message GetInverseKinRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetInverseKinRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetInverseKinRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.GetInverseKinRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.GetInverseKinRequest & lebai.posture.GetInverseKinRequest.$Shape} GetInverseKinRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetInverseKinRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.GetInverseKinRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.pose = $root.lebai.posture.Pose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pose);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.refer = $root.lebai.posture.JointPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.refer);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetInverseKinRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.GetInverseKinRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.GetInverseKinRequest & lebai.posture.GetInverseKinRequest.$Shape} GetInverseKinRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetInverseKinRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetInverseKinRequest message.
             * @function verify
             * @memberof lebai.posture.GetInverseKinRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetInverseKinRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose")) {
                    var error = $root.lebai.posture.Pose.verify(message.pose, _depth + 1);
                    if (error)
                        return "pose." + error;
                }
                if (message.refer != null && $Object.hasOwnProperty.call(message, "refer")) {
                    var error = $root.lebai.posture.JointPose.verify(message.refer, _depth + 1);
                    if (error)
                        return "refer." + error;
                }
                return null;
            };

            /**
             * Creates a GetInverseKinRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.GetInverseKinRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.GetInverseKinRequest} GetInverseKinRequest
             */
            GetInverseKinRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.GetInverseKinRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.GetInverseKinRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.GetInverseKinRequest();
                if (object.pose != null) {
                    if (!$util.isObject(object.pose))
                        throw $TypeError(".lebai.posture.GetInverseKinRequest.pose: object expected");
                    message.pose = $root.lebai.posture.Pose.fromObject(object.pose, _depth + 1);
                }
                if (object.refer != null) {
                    if (!$util.isObject(object.refer))
                        throw $TypeError(".lebai.posture.GetInverseKinRequest.refer: object expected");
                    message.refer = $root.lebai.posture.JointPose.fromObject(object.refer, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a GetInverseKinRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.GetInverseKinRequest
             * @static
             * @param {lebai.posture.GetInverseKinRequest} message GetInverseKinRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetInverseKinRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.pose = null;
                    object.refer = null;
                }
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    object.pose = $root.lebai.posture.Pose.toObject(message.pose, options, _depth + 1);
                if (message.refer != null && $Object.hasOwnProperty.call(message, "refer"))
                    object.refer = $root.lebai.posture.JointPose.toObject(message.refer, options, _depth + 1);
                return object;
            };

            /**
             * Converts this GetInverseKinRequest to JSON.
             * @function toJSON
             * @memberof lebai.posture.GetInverseKinRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetInverseKinRequest.prototype.toJSON = function() {
                return GetInverseKinRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetInverseKinRequest
             * @function getTypeUrl
             * @memberof lebai.posture.GetInverseKinRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetInverseKinRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.GetInverseKinRequest";
            };

            return GetInverseKinRequest;
        })();

        posture.GetPoseTransRequest = (function() {

            /**
             * Properties of a GetPoseTransRequest.
             * @typedef {Object} lebai.posture.GetPoseTransRequest.$Properties
             * @property {lebai.posture.Pose.$Properties|null} [from] GetPoseTransRequest from
             * @property {lebai.posture.Pose.$Properties|null} [fromTo] GetPoseTransRequest fromTo
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetPoseTransRequest.
             * @memberof lebai.posture
             * @interface IGetPoseTransRequest
             * @augments lebai.posture.GetPoseTransRequest.$Properties
             * @deprecated Use lebai.posture.GetPoseTransRequest.$Properties instead.
             */

            /**
             * Shape of a GetPoseTransRequest.
             * @typedef {lebai.posture.GetPoseTransRequest.$Properties} lebai.posture.GetPoseTransRequest.$Shape
             */

            /**
             * Constructs a new GetPoseTransRequest.
             * @memberof lebai.posture
             * @classdesc Represents a GetPoseTransRequest.
             * @constructor
             * @param {lebai.posture.GetPoseTransRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetPoseTransRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetPoseTransRequest from.
             * @member {lebai.posture.Pose.$Properties|null|undefined} from
             * @memberof lebai.posture.GetPoseTransRequest
             * @instance
             */
            GetPoseTransRequest.prototype.from = null;

            /**
             * GetPoseTransRequest fromTo.
             * @member {lebai.posture.Pose.$Properties|null|undefined} fromTo
             * @memberof lebai.posture.GetPoseTransRequest
             * @instance
             */
            GetPoseTransRequest.prototype.fromTo = null;

            /**
             * Creates a new GetPoseTransRequest instance using the specified properties.
             * @function create
             * @memberof lebai.posture.GetPoseTransRequest
             * @static
             * @param {lebai.posture.GetPoseTransRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.GetPoseTransRequest} GetPoseTransRequest instance
             * @type {{
             *   (properties: lebai.posture.GetPoseTransRequest.$Shape): lebai.posture.GetPoseTransRequest & lebai.posture.GetPoseTransRequest.$Shape;
             *   (properties?: lebai.posture.GetPoseTransRequest.$Properties): lebai.posture.GetPoseTransRequest;
             * }}
             */
            GetPoseTransRequest.create = function(properties) {
                return new GetPoseTransRequest(properties);
            };

            /**
             * Encodes the specified GetPoseTransRequest message. Does not implicitly {@link lebai.posture.GetPoseTransRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.GetPoseTransRequest
             * @static
             * @param {lebai.posture.GetPoseTransRequest.$Properties} message GetPoseTransRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetPoseTransRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.from != null && $Object.hasOwnProperty.call(message, "from"))
                    $root.lebai.posture.Pose.encode(message.from, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.fromTo != null && $Object.hasOwnProperty.call(message, "fromTo"))
                    $root.lebai.posture.Pose.encode(message.fromTo, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetPoseTransRequest message, length delimited. Does not implicitly {@link lebai.posture.GetPoseTransRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.GetPoseTransRequest
             * @static
             * @param {lebai.posture.GetPoseTransRequest.$Properties} message GetPoseTransRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetPoseTransRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetPoseTransRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.GetPoseTransRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.GetPoseTransRequest & lebai.posture.GetPoseTransRequest.$Shape} GetPoseTransRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetPoseTransRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.GetPoseTransRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.from = $root.lebai.posture.Pose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.from);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.fromTo = $root.lebai.posture.Pose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fromTo);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetPoseTransRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.GetPoseTransRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.GetPoseTransRequest & lebai.posture.GetPoseTransRequest.$Shape} GetPoseTransRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetPoseTransRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetPoseTransRequest message.
             * @function verify
             * @memberof lebai.posture.GetPoseTransRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetPoseTransRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.from != null && $Object.hasOwnProperty.call(message, "from")) {
                    var error = $root.lebai.posture.Pose.verify(message.from, _depth + 1);
                    if (error)
                        return "from." + error;
                }
                if (message.fromTo != null && $Object.hasOwnProperty.call(message, "fromTo")) {
                    var error = $root.lebai.posture.Pose.verify(message.fromTo, _depth + 1);
                    if (error)
                        return "fromTo." + error;
                }
                return null;
            };

            /**
             * Creates a GetPoseTransRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.GetPoseTransRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.GetPoseTransRequest} GetPoseTransRequest
             */
            GetPoseTransRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.GetPoseTransRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.GetPoseTransRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.GetPoseTransRequest();
                if (object.from != null) {
                    if (!$util.isObject(object.from))
                        throw $TypeError(".lebai.posture.GetPoseTransRequest.from: object expected");
                    message.from = $root.lebai.posture.Pose.fromObject(object.from, _depth + 1);
                }
                if (object.fromTo != null) {
                    if (!$util.isObject(object.fromTo))
                        throw $TypeError(".lebai.posture.GetPoseTransRequest.fromTo: object expected");
                    message.fromTo = $root.lebai.posture.Pose.fromObject(object.fromTo, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a GetPoseTransRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.GetPoseTransRequest
             * @static
             * @param {lebai.posture.GetPoseTransRequest} message GetPoseTransRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetPoseTransRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.from = null;
                    object.fromTo = null;
                }
                if (message.from != null && $Object.hasOwnProperty.call(message, "from"))
                    object.from = $root.lebai.posture.Pose.toObject(message.from, options, _depth + 1);
                if (message.fromTo != null && $Object.hasOwnProperty.call(message, "fromTo"))
                    object.fromTo = $root.lebai.posture.Pose.toObject(message.fromTo, options, _depth + 1);
                return object;
            };

            /**
             * Converts this GetPoseTransRequest to JSON.
             * @function toJSON
             * @memberof lebai.posture.GetPoseTransRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetPoseTransRequest.prototype.toJSON = function() {
                return GetPoseTransRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetPoseTransRequest
             * @function getTypeUrl
             * @memberof lebai.posture.GetPoseTransRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetPoseTransRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.GetPoseTransRequest";
            };

            return GetPoseTransRequest;
        })();

        posture.GetPoseAddRequest = (function() {

            /**
             * Properties of a GetPoseAddRequest.
             * @typedef {Object} lebai.posture.GetPoseAddRequest.$Properties
             * @property {lebai.posture.Pose.$Properties|null} [pose] GetPoseAddRequest pose
             * @property {lebai.posture.Pose.$Properties|null} [delta] GetPoseAddRequest delta
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetPoseAddRequest.
             * @memberof lebai.posture
             * @interface IGetPoseAddRequest
             * @augments lebai.posture.GetPoseAddRequest.$Properties
             * @deprecated Use lebai.posture.GetPoseAddRequest.$Properties instead.
             */

            /**
             * Shape of a GetPoseAddRequest.
             * @typedef {lebai.posture.GetPoseAddRequest.$Properties} lebai.posture.GetPoseAddRequest.$Shape
             */

            /**
             * Constructs a new GetPoseAddRequest.
             * @memberof lebai.posture
             * @classdesc Represents a GetPoseAddRequest.
             * @constructor
             * @param {lebai.posture.GetPoseAddRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetPoseAddRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetPoseAddRequest pose.
             * @member {lebai.posture.Pose.$Properties|null|undefined} pose
             * @memberof lebai.posture.GetPoseAddRequest
             * @instance
             */
            GetPoseAddRequest.prototype.pose = null;

            /**
             * GetPoseAddRequest delta.
             * @member {lebai.posture.Pose.$Properties|null|undefined} delta
             * @memberof lebai.posture.GetPoseAddRequest
             * @instance
             */
            GetPoseAddRequest.prototype.delta = null;

            /**
             * Creates a new GetPoseAddRequest instance using the specified properties.
             * @function create
             * @memberof lebai.posture.GetPoseAddRequest
             * @static
             * @param {lebai.posture.GetPoseAddRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.GetPoseAddRequest} GetPoseAddRequest instance
             * @type {{
             *   (properties: lebai.posture.GetPoseAddRequest.$Shape): lebai.posture.GetPoseAddRequest & lebai.posture.GetPoseAddRequest.$Shape;
             *   (properties?: lebai.posture.GetPoseAddRequest.$Properties): lebai.posture.GetPoseAddRequest;
             * }}
             */
            GetPoseAddRequest.create = function(properties) {
                return new GetPoseAddRequest(properties);
            };

            /**
             * Encodes the specified GetPoseAddRequest message. Does not implicitly {@link lebai.posture.GetPoseAddRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.GetPoseAddRequest
             * @static
             * @param {lebai.posture.GetPoseAddRequest.$Properties} message GetPoseAddRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetPoseAddRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    $root.lebai.posture.Pose.encode(message.pose, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.delta != null && $Object.hasOwnProperty.call(message, "delta"))
                    $root.lebai.posture.Pose.encode(message.delta, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetPoseAddRequest message, length delimited. Does not implicitly {@link lebai.posture.GetPoseAddRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.GetPoseAddRequest
             * @static
             * @param {lebai.posture.GetPoseAddRequest.$Properties} message GetPoseAddRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetPoseAddRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetPoseAddRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.GetPoseAddRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.GetPoseAddRequest & lebai.posture.GetPoseAddRequest.$Shape} GetPoseAddRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetPoseAddRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.GetPoseAddRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.pose = $root.lebai.posture.Pose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pose);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.delta = $root.lebai.posture.Pose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.delta);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetPoseAddRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.GetPoseAddRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.GetPoseAddRequest & lebai.posture.GetPoseAddRequest.$Shape} GetPoseAddRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetPoseAddRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetPoseAddRequest message.
             * @function verify
             * @memberof lebai.posture.GetPoseAddRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetPoseAddRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose")) {
                    var error = $root.lebai.posture.Pose.verify(message.pose, _depth + 1);
                    if (error)
                        return "pose." + error;
                }
                if (message.delta != null && $Object.hasOwnProperty.call(message, "delta")) {
                    var error = $root.lebai.posture.Pose.verify(message.delta, _depth + 1);
                    if (error)
                        return "delta." + error;
                }
                return null;
            };

            /**
             * Creates a GetPoseAddRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.GetPoseAddRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.GetPoseAddRequest} GetPoseAddRequest
             */
            GetPoseAddRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.GetPoseAddRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.GetPoseAddRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.GetPoseAddRequest();
                if (object.pose != null) {
                    if (!$util.isObject(object.pose))
                        throw $TypeError(".lebai.posture.GetPoseAddRequest.pose: object expected");
                    message.pose = $root.lebai.posture.Pose.fromObject(object.pose, _depth + 1);
                }
                if (object.delta != null) {
                    if (!$util.isObject(object.delta))
                        throw $TypeError(".lebai.posture.GetPoseAddRequest.delta: object expected");
                    message.delta = $root.lebai.posture.Pose.fromObject(object.delta, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a GetPoseAddRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.GetPoseAddRequest
             * @static
             * @param {lebai.posture.GetPoseAddRequest} message GetPoseAddRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetPoseAddRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.pose = null;
                    object.delta = null;
                }
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    object.pose = $root.lebai.posture.Pose.toObject(message.pose, options, _depth + 1);
                if (message.delta != null && $Object.hasOwnProperty.call(message, "delta"))
                    object.delta = $root.lebai.posture.Pose.toObject(message.delta, options, _depth + 1);
                return object;
            };

            /**
             * Converts this GetPoseAddRequest to JSON.
             * @function toJSON
             * @memberof lebai.posture.GetPoseAddRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetPoseAddRequest.prototype.toJSON = function() {
                return GetPoseAddRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetPoseAddRequest
             * @function getTypeUrl
             * @memberof lebai.posture.GetPoseAddRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetPoseAddRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.GetPoseAddRequest";
            };

            return GetPoseAddRequest;
        })();

        posture.Manipulation = (function() {

            /**
             * Properties of a Manipulation.
             * @typedef {Object} lebai.posture.Manipulation.$Properties
             * @property {number|null} [manipulation] Manipulation manipulation
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Manipulation.
             * @memberof lebai.posture
             * @interface IManipulation
             * @augments lebai.posture.Manipulation.$Properties
             * @deprecated Use lebai.posture.Manipulation.$Properties instead.
             */

            /**
             * Shape of a Manipulation.
             * @typedef {lebai.posture.Manipulation.$Properties} lebai.posture.Manipulation.$Shape
             */

            /**
             * Constructs a new Manipulation.
             * @memberof lebai.posture
             * @classdesc Represents a Manipulation.
             * @constructor
             * @param {lebai.posture.Manipulation.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Manipulation = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Manipulation manipulation.
             * @member {number} manipulation
             * @memberof lebai.posture.Manipulation
             * @instance
             */
            Manipulation.prototype.manipulation = 0;

            /**
             * Creates a new Manipulation instance using the specified properties.
             * @function create
             * @memberof lebai.posture.Manipulation
             * @static
             * @param {lebai.posture.Manipulation.$Properties=} [properties] Properties to set
             * @returns {lebai.posture.Manipulation} Manipulation instance
             * @type {{
             *   (properties: lebai.posture.Manipulation.$Shape): lebai.posture.Manipulation & lebai.posture.Manipulation.$Shape;
             *   (properties?: lebai.posture.Manipulation.$Properties): lebai.posture.Manipulation;
             * }}
             */
            Manipulation.create = function(properties) {
                return new Manipulation(properties);
            };

            /**
             * Encodes the specified Manipulation message. Does not implicitly {@link lebai.posture.Manipulation.verify|verify} messages.
             * @function encode
             * @memberof lebai.posture.Manipulation
             * @static
             * @param {lebai.posture.Manipulation.$Properties} message Manipulation message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Manipulation.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.manipulation != null && $Object.hasOwnProperty.call(message, "manipulation") && !$Object.is(message.manipulation, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.manipulation);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Manipulation message, length delimited. Does not implicitly {@link lebai.posture.Manipulation.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.posture.Manipulation
             * @static
             * @param {lebai.posture.Manipulation.$Properties} message Manipulation message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Manipulation.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Manipulation message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.posture.Manipulation
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.posture.Manipulation & lebai.posture.Manipulation.$Shape} Manipulation
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Manipulation.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.posture.Manipulation();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.manipulation = value;
                            else
                                delete message.manipulation;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Manipulation message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.posture.Manipulation
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.posture.Manipulation & lebai.posture.Manipulation.$Shape} Manipulation
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Manipulation.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Manipulation message.
             * @function verify
             * @memberof lebai.posture.Manipulation
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Manipulation.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.manipulation != null && $Object.hasOwnProperty.call(message, "manipulation"))
                    if (typeof message.manipulation !== "number")
                        return "manipulation: number expected";
                return null;
            };

            /**
             * Creates a Manipulation message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.posture.Manipulation
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.posture.Manipulation} Manipulation
             */
            Manipulation.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.posture.Manipulation)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.posture.Manipulation: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.posture.Manipulation();
                if (object.manipulation != null)
                    if (!$Object.is($Number(object.manipulation), 0))
                        message.manipulation = $Number(object.manipulation);
                return message;
            };

            /**
             * Creates a plain object from a Manipulation message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.posture.Manipulation
             * @static
             * @param {lebai.posture.Manipulation} message Manipulation
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Manipulation.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.manipulation = 0;
                if (message.manipulation != null && $Object.hasOwnProperty.call(message, "manipulation"))
                    object.manipulation = options.json && !$isFinite(message.manipulation) ? $String(message.manipulation) : message.manipulation;
                return object;
            };

            /**
             * Converts this Manipulation to JSON.
             * @function toJSON
             * @memberof lebai.posture.Manipulation
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Manipulation.prototype.toJSON = function() {
                return Manipulation.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Manipulation
             * @function getTypeUrl
             * @memberof lebai.posture.Manipulation
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Manipulation.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.posture.Manipulation";
            };

            return Manipulation;
        })();

        posture.PostureService = (function() {

            /**
             * Constructs a new PostureService service.
             * @memberof lebai.posture
             * @classdesc Represents a PostureService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var PostureService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(PostureService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: PostureService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new PostureService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.posture.PostureService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {PostureService} RPC service. Useful where requests and/or responses are streamed.
             */
            PostureService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.posture.PostureService#savePose}.
             * @memberof lebai.posture.PostureService
             * @typedef SavePoseCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SavePose.
             * @memberof lebai.posture.PostureService
             * @typedef SavePose
             * @type {{
             *   (request: lebai.posture.ISavePoseRequest, callback: lebai.posture.PostureService.SavePoseCallback): void;
             *   (request: lebai.posture.ISavePoseRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SavePose";
             *   readonly path: "/lebai.posture.PostureService/SavePose";
             *   readonly requestType: "SavePoseRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SavePose.
             * @name lebai.posture.PostureService#savePose
             * @type {lebai.posture.PostureService.SavePose}
             */
            $Object.defineProperties(PostureService.prototype.savePose = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PostureService.prototype.savePose, $root.lebai.posture.SavePoseRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SavePose" },
                path: { value: "/lebai.posture.PostureService/SavePose" },
                requestType: { value: "SavePoseRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.posture.PostureService#loadPose}.
             * @memberof lebai.posture.PostureService
             * @typedef LoadPoseCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.posture.Pose} [response] Pose
             */

            /**
             * Calls LoadPose.
             * @memberof lebai.posture.PostureService
             * @typedef LoadPose
             * @type {{
             *   (request: lebai.db.ILoadRequest, callback: lebai.posture.PostureService.LoadPoseCallback): void;
             *   (request: lebai.db.ILoadRequest): Promise<lebai.posture.Pose>;
             *   readonly name: "LoadPose";
             *   readonly path: "/lebai.posture.PostureService/LoadPose";
             *   readonly requestType: "db.LoadRequest";
             *   readonly responseType: "Pose";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadPose.
             * @name lebai.posture.PostureService#loadPose
             * @type {lebai.posture.PostureService.LoadPose}
             */
            $Object.defineProperties(PostureService.prototype.loadPose = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PostureService.prototype.loadPose, $root.lebai.db.LoadRequest, $root.lebai.posture.Pose, request, callback);
            }, {
                name: { value: "LoadPose" },
                path: { value: "/lebai.posture.PostureService/LoadPose" },
                requestType: { value: "db.LoadRequest" },
                responseType: { value: "Pose" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.posture.PostureService#loadPoseList}.
             * @memberof lebai.posture.PostureService
             * @typedef LoadPoseListCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.db.LoadListResponse} [response] LoadListResponse
             */

            /**
             * Calls LoadPoseList.
             * @memberof lebai.posture.PostureService
             * @typedef LoadPoseList
             * @type {{
             *   (request: lebai.db.ILoadListRequest, callback: lebai.posture.PostureService.LoadPoseListCallback): void;
             *   (request: lebai.db.ILoadListRequest): Promise<lebai.db.LoadListResponse>;
             *   readonly name: "LoadPoseList";
             *   readonly path: "/lebai.posture.PostureService/LoadPoseList";
             *   readonly requestType: "db.LoadListRequest";
             *   readonly responseType: "db.LoadListResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadPoseList.
             * @name lebai.posture.PostureService#loadPoseList
             * @type {lebai.posture.PostureService.LoadPoseList}
             */
            $Object.defineProperties(PostureService.prototype.loadPoseList = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PostureService.prototype.loadPoseList, $root.lebai.db.LoadListRequest, $root.lebai.db.LoadListResponse, request, callback);
            }, {
                name: { value: "LoadPoseList" },
                path: { value: "/lebai.posture.PostureService/LoadPoseList" },
                requestType: { value: "db.LoadListRequest" },
                responseType: { value: "db.LoadListResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.posture.PostureService#saveFrame}.
             * @memberof lebai.posture.PostureService
             * @typedef SaveFrameCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SaveFrame.
             * @memberof lebai.posture.PostureService
             * @typedef SaveFrame
             * @type {{
             *   (request: lebai.posture.ISaveFrameRequest, callback: lebai.posture.PostureService.SaveFrameCallback): void;
             *   (request: lebai.posture.ISaveFrameRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SaveFrame";
             *   readonly path: "/lebai.posture.PostureService/SaveFrame";
             *   readonly requestType: "SaveFrameRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SaveFrame.
             * @name lebai.posture.PostureService#saveFrame
             * @type {lebai.posture.PostureService.SaveFrame}
             */
            $Object.defineProperties(PostureService.prototype.saveFrame = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PostureService.prototype.saveFrame, $root.lebai.posture.SaveFrameRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SaveFrame" },
                path: { value: "/lebai.posture.PostureService/SaveFrame" },
                requestType: { value: "SaveFrameRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.posture.PostureService#loadFrame}.
             * @memberof lebai.posture.PostureService
             * @typedef LoadFrameCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.posture.CartesianFrame} [response] CartesianFrame
             */

            /**
             * Calls LoadFrame.
             * @memberof lebai.posture.PostureService
             * @typedef LoadFrame
             * @type {{
             *   (request: lebai.db.ILoadRequest, callback: lebai.posture.PostureService.LoadFrameCallback): void;
             *   (request: lebai.db.ILoadRequest): Promise<lebai.posture.CartesianFrame>;
             *   readonly name: "LoadFrame";
             *   readonly path: "/lebai.posture.PostureService/LoadFrame";
             *   readonly requestType: "db.LoadRequest";
             *   readonly responseType: "CartesianFrame";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadFrame.
             * @name lebai.posture.PostureService#loadFrame
             * @type {lebai.posture.PostureService.LoadFrame}
             */
            $Object.defineProperties(PostureService.prototype.loadFrame = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PostureService.prototype.loadFrame, $root.lebai.db.LoadRequest, $root.lebai.posture.CartesianFrame, request, callback);
            }, {
                name: { value: "LoadFrame" },
                path: { value: "/lebai.posture.PostureService/LoadFrame" },
                requestType: { value: "db.LoadRequest" },
                responseType: { value: "CartesianFrame" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.posture.PostureService#loadFrameList}.
             * @memberof lebai.posture.PostureService
             * @typedef LoadFrameListCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.db.LoadListResponse} [response] LoadListResponse
             */

            /**
             * Calls LoadFrameList.
             * @memberof lebai.posture.PostureService
             * @typedef LoadFrameList
             * @type {{
             *   (request: lebai.db.ILoadListRequest, callback: lebai.posture.PostureService.LoadFrameListCallback): void;
             *   (request: lebai.db.ILoadListRequest): Promise<lebai.db.LoadListResponse>;
             *   readonly name: "LoadFrameList";
             *   readonly path: "/lebai.posture.PostureService/LoadFrameList";
             *   readonly requestType: "db.LoadListRequest";
             *   readonly responseType: "db.LoadListResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadFrameList.
             * @name lebai.posture.PostureService#loadFrameList
             * @type {lebai.posture.PostureService.LoadFrameList}
             */
            $Object.defineProperties(PostureService.prototype.loadFrameList = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PostureService.prototype.loadFrameList, $root.lebai.db.LoadListRequest, $root.lebai.db.LoadListResponse, request, callback);
            }, {
                name: { value: "LoadFrameList" },
                path: { value: "/lebai.posture.PostureService/LoadFrameList" },
                requestType: { value: "db.LoadListRequest" },
                responseType: { value: "db.LoadListResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.posture.PostureService#measureManipulation}.
             * @memberof lebai.posture.PostureService
             * @typedef MeasureManipulationCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.posture.Manipulation} [response] Manipulation
             */

            /**
             * Calls MeasureManipulation.
             * @memberof lebai.posture.PostureService
             * @typedef MeasureManipulation
             * @type {{
             *   (request: lebai.posture.IJointPose, callback: lebai.posture.PostureService.MeasureManipulationCallback): void;
             *   (request: lebai.posture.IJointPose): Promise<lebai.posture.Manipulation>;
             *   readonly name: "MeasureManipulation";
             *   readonly path: "/lebai.posture.PostureService/MeasureManipulation";
             *   readonly requestType: "JointPose";
             *   readonly responseType: "Manipulation";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls MeasureManipulation.
             * @name lebai.posture.PostureService#measureManipulation
             * @type {lebai.posture.PostureService.MeasureManipulation}
             */
            $Object.defineProperties(PostureService.prototype.measureManipulation = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PostureService.prototype.measureManipulation, $root.lebai.posture.JointPose, $root.lebai.posture.Manipulation, request, callback);
            }, {
                name: { value: "MeasureManipulation" },
                path: { value: "/lebai.posture.PostureService/MeasureManipulation" },
                requestType: { value: "JointPose" },
                responseType: { value: "Manipulation" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.posture.PostureService#getForwardKin}.
             * @memberof lebai.posture.PostureService
             * @typedef GetForwardKinCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.posture.CartesianPose} [response] CartesianPose
             */

            /**
             * Calls GetForwardKin.
             * @memberof lebai.posture.PostureService
             * @typedef GetForwardKin
             * @type {{
             *   (request: lebai.posture.IPoseRequest, callback: lebai.posture.PostureService.GetForwardKinCallback): void;
             *   (request: lebai.posture.IPoseRequest): Promise<lebai.posture.CartesianPose>;
             *   readonly name: "GetForwardKin";
             *   readonly path: "/lebai.posture.PostureService/GetForwardKin";
             *   readonly requestType: "PoseRequest";
             *   readonly responseType: "CartesianPose";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetForwardKin.
             * @name lebai.posture.PostureService#getForwardKin
             * @type {lebai.posture.PostureService.GetForwardKin}
             */
            $Object.defineProperties(PostureService.prototype.getForwardKin = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PostureService.prototype.getForwardKin, $root.lebai.posture.PoseRequest, $root.lebai.posture.CartesianPose, request, callback);
            }, {
                name: { value: "GetForwardKin" },
                path: { value: "/lebai.posture.PostureService/GetForwardKin" },
                requestType: { value: "PoseRequest" },
                responseType: { value: "CartesianPose" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.posture.PostureService#getInverseKin}.
             * @memberof lebai.posture.PostureService
             * @typedef GetInverseKinCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.posture.JointPose} [response] JointPose
             */

            /**
             * Calls GetInverseKin.
             * @memberof lebai.posture.PostureService
             * @typedef GetInverseKin
             * @type {{
             *   (request: lebai.posture.IGetInverseKinRequest, callback: lebai.posture.PostureService.GetInverseKinCallback): void;
             *   (request: lebai.posture.IGetInverseKinRequest): Promise<lebai.posture.JointPose>;
             *   readonly name: "GetInverseKin";
             *   readonly path: "/lebai.posture.PostureService/GetInverseKin";
             *   readonly requestType: "GetInverseKinRequest";
             *   readonly responseType: "JointPose";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetInverseKin.
             * @name lebai.posture.PostureService#getInverseKin
             * @type {lebai.posture.PostureService.GetInverseKin}
             */
            $Object.defineProperties(PostureService.prototype.getInverseKin = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PostureService.prototype.getInverseKin, $root.lebai.posture.GetInverseKinRequest, $root.lebai.posture.JointPose, request, callback);
            }, {
                name: { value: "GetInverseKin" },
                path: { value: "/lebai.posture.PostureService/GetInverseKin" },
                requestType: { value: "GetInverseKinRequest" },
                responseType: { value: "JointPose" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.posture.PostureService#getPoseTrans}.
             * @memberof lebai.posture.PostureService
             * @typedef GetPoseTransCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.posture.CartesianPose} [response] CartesianPose
             */

            /**
             * Calls GetPoseTrans.
             * @memberof lebai.posture.PostureService
             * @typedef GetPoseTrans
             * @type {{
             *   (request: lebai.posture.IGetPoseTransRequest, callback: lebai.posture.PostureService.GetPoseTransCallback): void;
             *   (request: lebai.posture.IGetPoseTransRequest): Promise<lebai.posture.CartesianPose>;
             *   readonly name: "GetPoseTrans";
             *   readonly path: "/lebai.posture.PostureService/GetPoseTrans";
             *   readonly requestType: "GetPoseTransRequest";
             *   readonly responseType: "CartesianPose";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetPoseTrans.
             * @name lebai.posture.PostureService#getPoseTrans
             * @type {lebai.posture.PostureService.GetPoseTrans}
             */
            $Object.defineProperties(PostureService.prototype.getPoseTrans = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PostureService.prototype.getPoseTrans, $root.lebai.posture.GetPoseTransRequest, $root.lebai.posture.CartesianPose, request, callback);
            }, {
                name: { value: "GetPoseTrans" },
                path: { value: "/lebai.posture.PostureService/GetPoseTrans" },
                requestType: { value: "GetPoseTransRequest" },
                responseType: { value: "CartesianPose" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.posture.PostureService#getPoseAdd}.
             * @memberof lebai.posture.PostureService
             * @typedef GetPoseAddCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.posture.CartesianPose} [response] CartesianPose
             */

            /**
             * Calls GetPoseAdd.
             * @memberof lebai.posture.PostureService
             * @typedef GetPoseAdd
             * @type {{
             *   (request: lebai.posture.IGetPoseAddRequest, callback: lebai.posture.PostureService.GetPoseAddCallback): void;
             *   (request: lebai.posture.IGetPoseAddRequest): Promise<lebai.posture.CartesianPose>;
             *   readonly name: "GetPoseAdd";
             *   readonly path: "/lebai.posture.PostureService/GetPoseAdd";
             *   readonly requestType: "GetPoseAddRequest";
             *   readonly responseType: "CartesianPose";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetPoseAdd.
             * @name lebai.posture.PostureService#getPoseAdd
             * @type {lebai.posture.PostureService.GetPoseAdd}
             */
            $Object.defineProperties(PostureService.prototype.getPoseAdd = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PostureService.prototype.getPoseAdd, $root.lebai.posture.GetPoseAddRequest, $root.lebai.posture.CartesianPose, request, callback);
            }, {
                name: { value: "GetPoseAdd" },
                path: { value: "/lebai.posture.PostureService/GetPoseAdd" },
                requestType: { value: "GetPoseAddRequest" },
                responseType: { value: "CartesianPose" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.posture.PostureService#getPoseInverse}.
             * @memberof lebai.posture.PostureService
             * @typedef GetPoseInverseCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.posture.CartesianPose} [response] CartesianPose
             */

            /**
             * Calls GetPoseInverse.
             * @memberof lebai.posture.PostureService
             * @typedef GetPoseInverse
             * @type {{
             *   (request: lebai.posture.IPoseRequest, callback: lebai.posture.PostureService.GetPoseInverseCallback): void;
             *   (request: lebai.posture.IPoseRequest): Promise<lebai.posture.CartesianPose>;
             *   readonly name: "GetPoseInverse";
             *   readonly path: "/lebai.posture.PostureService/GetPoseInverse";
             *   readonly requestType: "PoseRequest";
             *   readonly responseType: "CartesianPose";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetPoseInverse.
             * @name lebai.posture.PostureService#getPoseInverse
             * @type {lebai.posture.PostureService.GetPoseInverse}
             */
            $Object.defineProperties(PostureService.prototype.getPoseInverse = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PostureService.prototype.getPoseInverse, $root.lebai.posture.PoseRequest, $root.lebai.posture.CartesianPose, request, callback);
            }, {
                name: { value: "GetPoseInverse" },
                path: { value: "/lebai.posture.PostureService/GetPoseInverse" },
                requestType: { value: "PoseRequest" },
                responseType: { value: "CartesianPose" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return PostureService;
        })();

        return posture;
    })();

    lebai.file = (function() {

        /**
         * Namespace file.
         * @memberof lebai
         * @namespace
         */
        var file = {};

        file.File = (function() {

            /**
             * Properties of a File.
             * @typedef {Object} lebai.file.File.$Properties
             * @property {boolean|null} [isDir] File isDir
             * @property {Uint8Array|null} [data] File data
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a File.
             * @memberof lebai.file
             * @interface IFile
             * @augments lebai.file.File.$Properties
             * @deprecated Use lebai.file.File.$Properties instead.
             */

            /**
             * Shape of a File.
             * @typedef {lebai.file.File.$Properties} lebai.file.File.$Shape
             */

            /**
             * Constructs a new File.
             * @memberof lebai.file
             * @classdesc Represents a File.
             * @constructor
             * @param {lebai.file.File.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var File = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * File isDir.
             * @member {boolean} isDir
             * @memberof lebai.file.File
             * @instance
             */
            File.prototype.isDir = false;

            /**
             * File data.
             * @member {Uint8Array} data
             * @memberof lebai.file.File
             * @instance
             */
            File.prototype.data = $util.newBuffer([]);

            /**
             * Creates a new File instance using the specified properties.
             * @function create
             * @memberof lebai.file.File
             * @static
             * @param {lebai.file.File.$Properties=} [properties] Properties to set
             * @returns {lebai.file.File} File instance
             * @type {{
             *   (properties: lebai.file.File.$Shape): lebai.file.File & lebai.file.File.$Shape;
             *   (properties?: lebai.file.File.$Properties): lebai.file.File;
             * }}
             */
            File.create = function(properties) {
                return new File(properties);
            };

            /**
             * Encodes the specified File message. Does not implicitly {@link lebai.file.File.verify|verify} messages.
             * @function encode
             * @memberof lebai.file.File
             * @static
             * @param {lebai.file.File.$Properties} message File message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            File.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.isDir != null && $Object.hasOwnProperty.call(message, "isDir") && message.isDir !== false)
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.isDir);
                if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data.length)
                    writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.data);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified File message, length delimited. Does not implicitly {@link lebai.file.File.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.file.File
             * @static
             * @param {lebai.file.File.$Properties} message File message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            File.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a File message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.file.File
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.file.File & lebai.file.File.$Shape} File
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            File.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.file.File();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.isDir = value;
                            else
                                delete message.isDir;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.bytes()).length)
                                message.data = value;
                            else
                                delete message.data;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a File message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.file.File
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.file.File & lebai.file.File.$Shape} File
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            File.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a File message.
             * @function verify
             * @memberof lebai.file.File
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            File.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.isDir != null && $Object.hasOwnProperty.call(message, "isDir"))
                    if (typeof message.isDir !== "boolean")
                        return "isDir: boolean expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                        return "data: buffer expected";
                return null;
            };

            /**
             * Creates a File message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.file.File
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.file.File} File
             */
            File.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.file.File)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.file.File: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.file.File();
                if (object.isDir != null)
                    if (object.isDir)
                        message.isDir = $Boolean(object.isDir);
                if (object.data != null)
                    if (object.data.length)
                        if (typeof object.data === "string")
                            $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                        else if (object.data.length >= 0)
                            message.data = object.data;
                return message;
            };

            /**
             * Creates a plain object from a File message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.file.File
             * @static
             * @param {lebai.file.File} message File
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            File.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.isDir = false;
                    if (options.bytes === $String)
                        object.data = "";
                    else {
                        object.data = [];
                        if (options.bytes !== $Array)
                            object.data = $util.newBuffer(object.data);
                    }
                }
                if (message.isDir != null && $Object.hasOwnProperty.call(message, "isDir"))
                    object.isDir = message.isDir;
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = options.bytes === $String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.data) : message.data;
                return object;
            };

            /**
             * Converts this File to JSON.
             * @function toJSON
             * @memberof lebai.file.File
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            File.prototype.toJSON = function() {
                return File.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for File
             * @function getTypeUrl
             * @memberof lebai.file.File
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            File.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.file.File";
            };

            return File;
        })();

        file.FileIndex = (function() {

            /**
             * Properties of a FileIndex.
             * @typedef {Object} lebai.file.FileIndex.$Properties
             * @property {string|null} [dir] FileIndex dir
             * @property {string|null} [name] FileIndex name
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FileIndex.
             * @memberof lebai.file
             * @interface IFileIndex
             * @augments lebai.file.FileIndex.$Properties
             * @deprecated Use lebai.file.FileIndex.$Properties instead.
             */

            /**
             * Shape of a FileIndex.
             * @typedef {lebai.file.FileIndex.$Properties} lebai.file.FileIndex.$Shape
             */

            /**
             * Constructs a new FileIndex.
             * @memberof lebai.file
             * @classdesc Represents a FileIndex.
             * @constructor
             * @param {lebai.file.FileIndex.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var FileIndex = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FileIndex dir.
             * @member {string} dir
             * @memberof lebai.file.FileIndex
             * @instance
             */
            FileIndex.prototype.dir = "";

            /**
             * FileIndex name.
             * @member {string} name
             * @memberof lebai.file.FileIndex
             * @instance
             */
            FileIndex.prototype.name = "";

            /**
             * Creates a new FileIndex instance using the specified properties.
             * @function create
             * @memberof lebai.file.FileIndex
             * @static
             * @param {lebai.file.FileIndex.$Properties=} [properties] Properties to set
             * @returns {lebai.file.FileIndex} FileIndex instance
             * @type {{
             *   (properties: lebai.file.FileIndex.$Shape): lebai.file.FileIndex & lebai.file.FileIndex.$Shape;
             *   (properties?: lebai.file.FileIndex.$Properties): lebai.file.FileIndex;
             * }}
             */
            FileIndex.create = function(properties) {
                return new FileIndex(properties);
            };

            /**
             * Encodes the specified FileIndex message. Does not implicitly {@link lebai.file.FileIndex.verify|verify} messages.
             * @function encode
             * @memberof lebai.file.FileIndex
             * @static
             * @param {lebai.file.FileIndex.$Properties} message FileIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileIndex.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.dir);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FileIndex message, length delimited. Does not implicitly {@link lebai.file.FileIndex.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.file.FileIndex
             * @static
             * @param {lebai.file.FileIndex.$Properties} message FileIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileIndex.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a FileIndex message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.file.FileIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.file.FileIndex & lebai.file.FileIndex.$Shape} FileIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileIndex.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.file.FileIndex();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a FileIndex message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.file.FileIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.file.FileIndex & lebai.file.FileIndex.$Shape} FileIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileIndex.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FileIndex message.
             * @function verify
             * @memberof lebai.file.FileIndex
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FileIndex.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                return null;
            };

            /**
             * Creates a FileIndex message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.file.FileIndex
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.file.FileIndex} FileIndex
             */
            FileIndex.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.file.FileIndex)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.file.FileIndex: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.file.FileIndex();
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                return message;
            };

            /**
             * Creates a plain object from a FileIndex message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.file.FileIndex
             * @static
             * @param {lebai.file.FileIndex} message FileIndex
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FileIndex.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.dir = "";
                    object.name = "";
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                return object;
            };

            /**
             * Converts this FileIndex to JSON.
             * @function toJSON
             * @memberof lebai.file.FileIndex
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FileIndex.prototype.toJSON = function() {
                return FileIndex.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FileIndex
             * @function getTypeUrl
             * @memberof lebai.file.FileIndex
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FileIndex.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.file.FileIndex";
            };

            return FileIndex;
        })();

        file.FileName = (function() {

            /**
             * Properties of a FileName.
             * @typedef {Object} lebai.file.FileName.$Properties
             * @property {string|null} [name] FileName name
             * @property {boolean|null} [isDir] FileName isDir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FileName.
             * @memberof lebai.file
             * @interface IFileName
             * @augments lebai.file.FileName.$Properties
             * @deprecated Use lebai.file.FileName.$Properties instead.
             */

            /**
             * Shape of a FileName.
             * @typedef {lebai.file.FileName.$Properties} lebai.file.FileName.$Shape
             */

            /**
             * Constructs a new FileName.
             * @memberof lebai.file
             * @classdesc Represents a FileName.
             * @constructor
             * @param {lebai.file.FileName.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var FileName = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FileName name.
             * @member {string} name
             * @memberof lebai.file.FileName
             * @instance
             */
            FileName.prototype.name = "";

            /**
             * FileName isDir.
             * @member {boolean} isDir
             * @memberof lebai.file.FileName
             * @instance
             */
            FileName.prototype.isDir = false;

            /**
             * Creates a new FileName instance using the specified properties.
             * @function create
             * @memberof lebai.file.FileName
             * @static
             * @param {lebai.file.FileName.$Properties=} [properties] Properties to set
             * @returns {lebai.file.FileName} FileName instance
             * @type {{
             *   (properties: lebai.file.FileName.$Shape): lebai.file.FileName & lebai.file.FileName.$Shape;
             *   (properties?: lebai.file.FileName.$Properties): lebai.file.FileName;
             * }}
             */
            FileName.create = function(properties) {
                return new FileName(properties);
            };

            /**
             * Encodes the specified FileName message. Does not implicitly {@link lebai.file.FileName.verify|verify} messages.
             * @function encode
             * @memberof lebai.file.FileName
             * @static
             * @param {lebai.file.FileName.$Properties} message FileName message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileName.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
                if (message.isDir != null && $Object.hasOwnProperty.call(message, "isDir") && message.isDir !== false)
                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message.isDir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FileName message, length delimited. Does not implicitly {@link lebai.file.FileName.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.file.FileName
             * @static
             * @param {lebai.file.FileName.$Properties} message FileName message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FileName.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a FileName message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.file.FileName
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.file.FileName & lebai.file.FileName.$Shape} FileName
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileName.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.file.FileName();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.isDir = value;
                            else
                                delete message.isDir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a FileName message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.file.FileName
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.file.FileName & lebai.file.FileName.$Shape} FileName
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FileName.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FileName message.
             * @function verify
             * @memberof lebai.file.FileName
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FileName.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.isDir != null && $Object.hasOwnProperty.call(message, "isDir"))
                    if (typeof message.isDir !== "boolean")
                        return "isDir: boolean expected";
                return null;
            };

            /**
             * Creates a FileName message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.file.FileName
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.file.FileName} FileName
             */
            FileName.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.file.FileName)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.file.FileName: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.file.FileName();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.isDir != null)
                    if (object.isDir)
                        message.isDir = $Boolean(object.isDir);
                return message;
            };

            /**
             * Creates a plain object from a FileName message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.file.FileName
             * @static
             * @param {lebai.file.FileName} message FileName
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FileName.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.isDir = false;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.isDir != null && $Object.hasOwnProperty.call(message, "isDir"))
                    object.isDir = message.isDir;
                return object;
            };

            /**
             * Converts this FileName to JSON.
             * @function toJSON
             * @memberof lebai.file.FileName
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FileName.prototype.toJSON = function() {
                return FileName.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FileName
             * @function getTypeUrl
             * @memberof lebai.file.FileName
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FileName.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.file.FileName";
            };

            return FileName;
        })();

        file.SaveFileRequest = (function() {

            /**
             * Properties of a SaveFileRequest.
             * @typedef {Object} lebai.file.SaveFileRequest.$Properties
             * @property {string|null} [dir] SaveFileRequest dir
             * @property {string|null} [name] SaveFileRequest name
             * @property {lebai.file.File.$Properties|null} [file] SaveFileRequest file
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SaveFileRequest.
             * @memberof lebai.file
             * @interface ISaveFileRequest
             * @augments lebai.file.SaveFileRequest.$Properties
             * @deprecated Use lebai.file.SaveFileRequest.$Properties instead.
             */

            /**
             * Shape of a SaveFileRequest.
             * @typedef {lebai.file.SaveFileRequest.$Properties} lebai.file.SaveFileRequest.$Shape
             */

            /**
             * Constructs a new SaveFileRequest.
             * @memberof lebai.file
             * @classdesc Represents a SaveFileRequest.
             * @constructor
             * @param {lebai.file.SaveFileRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SaveFileRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SaveFileRequest dir.
             * @member {string} dir
             * @memberof lebai.file.SaveFileRequest
             * @instance
             */
            SaveFileRequest.prototype.dir = "";

            /**
             * SaveFileRequest name.
             * @member {string} name
             * @memberof lebai.file.SaveFileRequest
             * @instance
             */
            SaveFileRequest.prototype.name = "";

            /**
             * SaveFileRequest file.
             * @member {lebai.file.File.$Properties|null|undefined} file
             * @memberof lebai.file.SaveFileRequest
             * @instance
             */
            SaveFileRequest.prototype.file = null;

            /**
             * Creates a new SaveFileRequest instance using the specified properties.
             * @function create
             * @memberof lebai.file.SaveFileRequest
             * @static
             * @param {lebai.file.SaveFileRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.file.SaveFileRequest} SaveFileRequest instance
             * @type {{
             *   (properties: lebai.file.SaveFileRequest.$Shape): lebai.file.SaveFileRequest & lebai.file.SaveFileRequest.$Shape;
             *   (properties?: lebai.file.SaveFileRequest.$Properties): lebai.file.SaveFileRequest;
             * }}
             */
            SaveFileRequest.create = function(properties) {
                return new SaveFileRequest(properties);
            };

            /**
             * Encodes the specified SaveFileRequest message. Does not implicitly {@link lebai.file.SaveFileRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.file.SaveFileRequest
             * @static
             * @param {lebai.file.SaveFileRequest.$Properties} message SaveFileRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveFileRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.dir);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
                if (message.file != null && $Object.hasOwnProperty.call(message, "file"))
                    $root.lebai.file.File.encode(message.file, writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SaveFileRequest message, length delimited. Does not implicitly {@link lebai.file.SaveFileRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.file.SaveFileRequest
             * @static
             * @param {lebai.file.SaveFileRequest.$Properties} message SaveFileRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveFileRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SaveFileRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.file.SaveFileRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.file.SaveFileRequest & lebai.file.SaveFileRequest.$Shape} SaveFileRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveFileRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.file.SaveFileRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            message.file = $root.lebai.file.File.decode(reader, reader.uint32(), $undefined, _depth + 1, message.file);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SaveFileRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.file.SaveFileRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.file.SaveFileRequest & lebai.file.SaveFileRequest.$Shape} SaveFileRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveFileRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SaveFileRequest message.
             * @function verify
             * @memberof lebai.file.SaveFileRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SaveFileRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.file != null && $Object.hasOwnProperty.call(message, "file")) {
                    var error = $root.lebai.file.File.verify(message.file, _depth + 1);
                    if (error)
                        return "file." + error;
                }
                return null;
            };

            /**
             * Creates a SaveFileRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.file.SaveFileRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.file.SaveFileRequest} SaveFileRequest
             */
            SaveFileRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.file.SaveFileRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.file.SaveFileRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.file.SaveFileRequest();
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.file != null) {
                    if (!$util.isObject(object.file))
                        throw $TypeError(".lebai.file.SaveFileRequest.file: object expected");
                    message.file = $root.lebai.file.File.fromObject(object.file, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a SaveFileRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.file.SaveFileRequest
             * @static
             * @param {lebai.file.SaveFileRequest} message SaveFileRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SaveFileRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.dir = "";
                    object.name = "";
                    object.file = null;
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.file != null && $Object.hasOwnProperty.call(message, "file"))
                    object.file = $root.lebai.file.File.toObject(message.file, options, _depth + 1);
                return object;
            };

            /**
             * Converts this SaveFileRequest to JSON.
             * @function toJSON
             * @memberof lebai.file.SaveFileRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SaveFileRequest.prototype.toJSON = function() {
                return SaveFileRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SaveFileRequest
             * @function getTypeUrl
             * @memberof lebai.file.SaveFileRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SaveFileRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.file.SaveFileRequest";
            };

            return SaveFileRequest;
        })();

        file.DownloadFileRequest = (function() {

            /**
             * Properties of a DownloadFileRequest.
             * @typedef {Object} lebai.file.DownloadFileRequest.$Properties
             * @property {string|null} [dir] DownloadFileRequest dir
             * @property {string|null} [name] DownloadFileRequest name
             * @property {string|null} [url] DownloadFileRequest url
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DownloadFileRequest.
             * @memberof lebai.file
             * @interface IDownloadFileRequest
             * @augments lebai.file.DownloadFileRequest.$Properties
             * @deprecated Use lebai.file.DownloadFileRequest.$Properties instead.
             */

            /**
             * Shape of a DownloadFileRequest.
             * @typedef {lebai.file.DownloadFileRequest.$Properties} lebai.file.DownloadFileRequest.$Shape
             */

            /**
             * Constructs a new DownloadFileRequest.
             * @memberof lebai.file
             * @classdesc Represents a DownloadFileRequest.
             * @constructor
             * @param {lebai.file.DownloadFileRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var DownloadFileRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DownloadFileRequest dir.
             * @member {string} dir
             * @memberof lebai.file.DownloadFileRequest
             * @instance
             */
            DownloadFileRequest.prototype.dir = "";

            /**
             * DownloadFileRequest name.
             * @member {string} name
             * @memberof lebai.file.DownloadFileRequest
             * @instance
             */
            DownloadFileRequest.prototype.name = "";

            /**
             * DownloadFileRequest url.
             * @member {string} url
             * @memberof lebai.file.DownloadFileRequest
             * @instance
             */
            DownloadFileRequest.prototype.url = "";

            /**
             * Creates a new DownloadFileRequest instance using the specified properties.
             * @function create
             * @memberof lebai.file.DownloadFileRequest
             * @static
             * @param {lebai.file.DownloadFileRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.file.DownloadFileRequest} DownloadFileRequest instance
             * @type {{
             *   (properties: lebai.file.DownloadFileRequest.$Shape): lebai.file.DownloadFileRequest & lebai.file.DownloadFileRequest.$Shape;
             *   (properties?: lebai.file.DownloadFileRequest.$Properties): lebai.file.DownloadFileRequest;
             * }}
             */
            DownloadFileRequest.create = function(properties) {
                return new DownloadFileRequest(properties);
            };

            /**
             * Encodes the specified DownloadFileRequest message. Does not implicitly {@link lebai.file.DownloadFileRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.file.DownloadFileRequest
             * @static
             * @param {lebai.file.DownloadFileRequest.$Properties} message DownloadFileRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DownloadFileRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.dir);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
                if (message.url != null && $Object.hasOwnProperty.call(message, "url") && message.url !== "")
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.url);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DownloadFileRequest message, length delimited. Does not implicitly {@link lebai.file.DownloadFileRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.file.DownloadFileRequest
             * @static
             * @param {lebai.file.DownloadFileRequest.$Properties} message DownloadFileRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DownloadFileRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a DownloadFileRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.file.DownloadFileRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.file.DownloadFileRequest & lebai.file.DownloadFileRequest.$Shape} DownloadFileRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DownloadFileRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.file.DownloadFileRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.url = value;
                            else
                                delete message.url;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a DownloadFileRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.file.DownloadFileRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.file.DownloadFileRequest & lebai.file.DownloadFileRequest.$Shape} DownloadFileRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DownloadFileRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DownloadFileRequest message.
             * @function verify
             * @memberof lebai.file.DownloadFileRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DownloadFileRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
                    if (!$util.isString(message.url))
                        return "url: string expected";
                return null;
            };

            /**
             * Creates a DownloadFileRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.file.DownloadFileRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.file.DownloadFileRequest} DownloadFileRequest
             */
            DownloadFileRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.file.DownloadFileRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.file.DownloadFileRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.file.DownloadFileRequest();
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.url != null)
                    if (typeof object.url !== "string" || object.url.length)
                        message.url = $String(object.url);
                return message;
            };

            /**
             * Creates a plain object from a DownloadFileRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.file.DownloadFileRequest
             * @static
             * @param {lebai.file.DownloadFileRequest} message DownloadFileRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DownloadFileRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.dir = "";
                    object.name = "";
                    object.url = "";
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                return object;
            };

            /**
             * Converts this DownloadFileRequest to JSON.
             * @function toJSON
             * @memberof lebai.file.DownloadFileRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DownloadFileRequest.prototype.toJSON = function() {
                return DownloadFileRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DownloadFileRequest
             * @function getTypeUrl
             * @memberof lebai.file.DownloadFileRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DownloadFileRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.file.DownloadFileRequest";
            };

            return DownloadFileRequest;
        })();

        file.RenameFileRequest = (function() {

            /**
             * Properties of a RenameFileRequest.
             * @typedef {Object} lebai.file.RenameFileRequest.$Properties
             * @property {lebai.file.FileIndex.$Properties|null} [from] RenameFileRequest from
             * @property {lebai.file.FileIndex.$Properties|null} [to] RenameFileRequest to
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a RenameFileRequest.
             * @memberof lebai.file
             * @interface IRenameFileRequest
             * @augments lebai.file.RenameFileRequest.$Properties
             * @deprecated Use lebai.file.RenameFileRequest.$Properties instead.
             */

            /**
             * Shape of a RenameFileRequest.
             * @typedef {lebai.file.RenameFileRequest.$Properties} lebai.file.RenameFileRequest.$Shape
             */

            /**
             * Constructs a new RenameFileRequest.
             * @memberof lebai.file
             * @classdesc Represents a RenameFileRequest.
             * @constructor
             * @param {lebai.file.RenameFileRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var RenameFileRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * RenameFileRequest from.
             * @member {lebai.file.FileIndex.$Properties|null|undefined} from
             * @memberof lebai.file.RenameFileRequest
             * @instance
             */
            RenameFileRequest.prototype.from = null;

            /**
             * RenameFileRequest to.
             * @member {lebai.file.FileIndex.$Properties|null|undefined} to
             * @memberof lebai.file.RenameFileRequest
             * @instance
             */
            RenameFileRequest.prototype.to = null;

            /**
             * Creates a new RenameFileRequest instance using the specified properties.
             * @function create
             * @memberof lebai.file.RenameFileRequest
             * @static
             * @param {lebai.file.RenameFileRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.file.RenameFileRequest} RenameFileRequest instance
             * @type {{
             *   (properties: lebai.file.RenameFileRequest.$Shape): lebai.file.RenameFileRequest & lebai.file.RenameFileRequest.$Shape;
             *   (properties?: lebai.file.RenameFileRequest.$Properties): lebai.file.RenameFileRequest;
             * }}
             */
            RenameFileRequest.create = function(properties) {
                return new RenameFileRequest(properties);
            };

            /**
             * Encodes the specified RenameFileRequest message. Does not implicitly {@link lebai.file.RenameFileRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.file.RenameFileRequest
             * @static
             * @param {lebai.file.RenameFileRequest.$Properties} message RenameFileRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RenameFileRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.from != null && $Object.hasOwnProperty.call(message, "from"))
                    $root.lebai.file.FileIndex.encode(message.from, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.to != null && $Object.hasOwnProperty.call(message, "to"))
                    $root.lebai.file.FileIndex.encode(message.to, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified RenameFileRequest message, length delimited. Does not implicitly {@link lebai.file.RenameFileRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.file.RenameFileRequest
             * @static
             * @param {lebai.file.RenameFileRequest.$Properties} message RenameFileRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RenameFileRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a RenameFileRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.file.RenameFileRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.file.RenameFileRequest & lebai.file.RenameFileRequest.$Shape} RenameFileRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RenameFileRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.file.RenameFileRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.from = $root.lebai.file.FileIndex.decode(reader, reader.uint32(), $undefined, _depth + 1, message.from);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.to = $root.lebai.file.FileIndex.decode(reader, reader.uint32(), $undefined, _depth + 1, message.to);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a RenameFileRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.file.RenameFileRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.file.RenameFileRequest & lebai.file.RenameFileRequest.$Shape} RenameFileRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RenameFileRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RenameFileRequest message.
             * @function verify
             * @memberof lebai.file.RenameFileRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RenameFileRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.from != null && $Object.hasOwnProperty.call(message, "from")) {
                    var error = $root.lebai.file.FileIndex.verify(message.from, _depth + 1);
                    if (error)
                        return "from." + error;
                }
                if (message.to != null && $Object.hasOwnProperty.call(message, "to")) {
                    var error = $root.lebai.file.FileIndex.verify(message.to, _depth + 1);
                    if (error)
                        return "to." + error;
                }
                return null;
            };

            /**
             * Creates a RenameFileRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.file.RenameFileRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.file.RenameFileRequest} RenameFileRequest
             */
            RenameFileRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.file.RenameFileRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.file.RenameFileRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.file.RenameFileRequest();
                if (object.from != null) {
                    if (!$util.isObject(object.from))
                        throw $TypeError(".lebai.file.RenameFileRequest.from: object expected");
                    message.from = $root.lebai.file.FileIndex.fromObject(object.from, _depth + 1);
                }
                if (object.to != null) {
                    if (!$util.isObject(object.to))
                        throw $TypeError(".lebai.file.RenameFileRequest.to: object expected");
                    message.to = $root.lebai.file.FileIndex.fromObject(object.to, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a RenameFileRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.file.RenameFileRequest
             * @static
             * @param {lebai.file.RenameFileRequest} message RenameFileRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RenameFileRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.from = null;
                    object.to = null;
                }
                if (message.from != null && $Object.hasOwnProperty.call(message, "from"))
                    object.from = $root.lebai.file.FileIndex.toObject(message.from, options, _depth + 1);
                if (message.to != null && $Object.hasOwnProperty.call(message, "to"))
                    object.to = $root.lebai.file.FileIndex.toObject(message.to, options, _depth + 1);
                return object;
            };

            /**
             * Converts this RenameFileRequest to JSON.
             * @function toJSON
             * @memberof lebai.file.RenameFileRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RenameFileRequest.prototype.toJSON = function() {
                return RenameFileRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for RenameFileRequest
             * @function getTypeUrl
             * @memberof lebai.file.RenameFileRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            RenameFileRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.file.RenameFileRequest";
            };

            return RenameFileRequest;
        })();

        file.LoadFileListRequest = (function() {

            /**
             * Properties of a LoadFileListRequest.
             * @typedef {Object} lebai.file.LoadFileListRequest.$Properties
             * @property {string|null} [dir] LoadFileListRequest dir
             * @property {string|null} [prefix] LoadFileListRequest prefix
             * @property {string|null} [suffix] LoadFileListRequest suffix
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LoadFileListRequest.
             * @memberof lebai.file
             * @interface ILoadFileListRequest
             * @augments lebai.file.LoadFileListRequest.$Properties
             * @deprecated Use lebai.file.LoadFileListRequest.$Properties instead.
             */

            /**
             * Shape of a LoadFileListRequest.
             * @typedef {lebai.file.LoadFileListRequest.$Properties} lebai.file.LoadFileListRequest.$Shape
             */

            /**
             * Constructs a new LoadFileListRequest.
             * @memberof lebai.file
             * @classdesc Represents a LoadFileListRequest.
             * @constructor
             * @param {lebai.file.LoadFileListRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LoadFileListRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LoadFileListRequest dir.
             * @member {string} dir
             * @memberof lebai.file.LoadFileListRequest
             * @instance
             */
            LoadFileListRequest.prototype.dir = "";

            /**
             * LoadFileListRequest prefix.
             * @member {string} prefix
             * @memberof lebai.file.LoadFileListRequest
             * @instance
             */
            LoadFileListRequest.prototype.prefix = "";

            /**
             * LoadFileListRequest suffix.
             * @member {string} suffix
             * @memberof lebai.file.LoadFileListRequest
             * @instance
             */
            LoadFileListRequest.prototype.suffix = "";

            /**
             * Creates a new LoadFileListRequest instance using the specified properties.
             * @function create
             * @memberof lebai.file.LoadFileListRequest
             * @static
             * @param {lebai.file.LoadFileListRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.file.LoadFileListRequest} LoadFileListRequest instance
             * @type {{
             *   (properties: lebai.file.LoadFileListRequest.$Shape): lebai.file.LoadFileListRequest & lebai.file.LoadFileListRequest.$Shape;
             *   (properties?: lebai.file.LoadFileListRequest.$Properties): lebai.file.LoadFileListRequest;
             * }}
             */
            LoadFileListRequest.create = function(properties) {
                return new LoadFileListRequest(properties);
            };

            /**
             * Encodes the specified LoadFileListRequest message. Does not implicitly {@link lebai.file.LoadFileListRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.file.LoadFileListRequest
             * @static
             * @param {lebai.file.LoadFileListRequest.$Properties} message LoadFileListRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadFileListRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.dir);
                if (message.prefix != null && $Object.hasOwnProperty.call(message, "prefix") && message.prefix !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.prefix);
                if (message.suffix != null && $Object.hasOwnProperty.call(message, "suffix") && message.suffix !== "")
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.suffix);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LoadFileListRequest message, length delimited. Does not implicitly {@link lebai.file.LoadFileListRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.file.LoadFileListRequest
             * @static
             * @param {lebai.file.LoadFileListRequest.$Properties} message LoadFileListRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadFileListRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LoadFileListRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.file.LoadFileListRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.file.LoadFileListRequest & lebai.file.LoadFileListRequest.$Shape} LoadFileListRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadFileListRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.file.LoadFileListRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.prefix = value;
                            else
                                delete message.prefix;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.suffix = value;
                            else
                                delete message.suffix;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LoadFileListRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.file.LoadFileListRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.file.LoadFileListRequest & lebai.file.LoadFileListRequest.$Shape} LoadFileListRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadFileListRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LoadFileListRequest message.
             * @function verify
             * @memberof lebai.file.LoadFileListRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LoadFileListRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                if (message.prefix != null && $Object.hasOwnProperty.call(message, "prefix"))
                    if (!$util.isString(message.prefix))
                        return "prefix: string expected";
                if (message.suffix != null && $Object.hasOwnProperty.call(message, "suffix"))
                    if (!$util.isString(message.suffix))
                        return "suffix: string expected";
                return null;
            };

            /**
             * Creates a LoadFileListRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.file.LoadFileListRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.file.LoadFileListRequest} LoadFileListRequest
             */
            LoadFileListRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.file.LoadFileListRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.file.LoadFileListRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.file.LoadFileListRequest();
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                if (object.prefix != null)
                    if (typeof object.prefix !== "string" || object.prefix.length)
                        message.prefix = $String(object.prefix);
                if (object.suffix != null)
                    if (typeof object.suffix !== "string" || object.suffix.length)
                        message.suffix = $String(object.suffix);
                return message;
            };

            /**
             * Creates a plain object from a LoadFileListRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.file.LoadFileListRequest
             * @static
             * @param {lebai.file.LoadFileListRequest} message LoadFileListRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LoadFileListRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.dir = "";
                    object.prefix = "";
                    object.suffix = "";
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                if (message.prefix != null && $Object.hasOwnProperty.call(message, "prefix"))
                    object.prefix = message.prefix;
                if (message.suffix != null && $Object.hasOwnProperty.call(message, "suffix"))
                    object.suffix = message.suffix;
                return object;
            };

            /**
             * Converts this LoadFileListRequest to JSON.
             * @function toJSON
             * @memberof lebai.file.LoadFileListRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LoadFileListRequest.prototype.toJSON = function() {
                return LoadFileListRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LoadFileListRequest
             * @function getTypeUrl
             * @memberof lebai.file.LoadFileListRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LoadFileListRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.file.LoadFileListRequest";
            };

            return LoadFileListRequest;
        })();

        file.LoadFileListResponse = (function() {

            /**
             * Properties of a LoadFileListResponse.
             * @typedef {Object} lebai.file.LoadFileListResponse.$Properties
             * @property {Array.<lebai.file.FileName.$Properties>|null} [files] LoadFileListResponse files
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LoadFileListResponse.
             * @memberof lebai.file
             * @interface ILoadFileListResponse
             * @augments lebai.file.LoadFileListResponse.$Properties
             * @deprecated Use lebai.file.LoadFileListResponse.$Properties instead.
             */

            /**
             * Shape of a LoadFileListResponse.
             * @typedef {lebai.file.LoadFileListResponse.$Properties} lebai.file.LoadFileListResponse.$Shape
             */

            /**
             * Constructs a new LoadFileListResponse.
             * @memberof lebai.file
             * @classdesc Represents a LoadFileListResponse.
             * @constructor
             * @param {lebai.file.LoadFileListResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LoadFileListResponse = function (properties) {
                this.files = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LoadFileListResponse files.
             * @member {Array.<lebai.file.FileName.$Properties>} files
             * @memberof lebai.file.LoadFileListResponse
             * @instance
             */
            LoadFileListResponse.prototype.files = $util.emptyArray;

            /**
             * Creates a new LoadFileListResponse instance using the specified properties.
             * @function create
             * @memberof lebai.file.LoadFileListResponse
             * @static
             * @param {lebai.file.LoadFileListResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.file.LoadFileListResponse} LoadFileListResponse instance
             * @type {{
             *   (properties: lebai.file.LoadFileListResponse.$Shape): lebai.file.LoadFileListResponse & lebai.file.LoadFileListResponse.$Shape;
             *   (properties?: lebai.file.LoadFileListResponse.$Properties): lebai.file.LoadFileListResponse;
             * }}
             */
            LoadFileListResponse.create = function(properties) {
                return new LoadFileListResponse(properties);
            };

            /**
             * Encodes the specified LoadFileListResponse message. Does not implicitly {@link lebai.file.LoadFileListResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.file.LoadFileListResponse
             * @static
             * @param {lebai.file.LoadFileListResponse.$Properties} message LoadFileListResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadFileListResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.files != null && message.files.length)
                    for (var i = 0; i < message.files.length; ++i)
                        $root.lebai.file.FileName.encode(message.files[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LoadFileListResponse message, length delimited. Does not implicitly {@link lebai.file.LoadFileListResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.file.LoadFileListResponse
             * @static
             * @param {lebai.file.LoadFileListResponse.$Properties} message LoadFileListResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadFileListResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LoadFileListResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.file.LoadFileListResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.file.LoadFileListResponse & lebai.file.LoadFileListResponse.$Shape} LoadFileListResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadFileListResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.file.LoadFileListResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.files && message.files.length))
                                message.files = [];
                            message.files.push($root.lebai.file.FileName.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LoadFileListResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.file.LoadFileListResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.file.LoadFileListResponse & lebai.file.LoadFileListResponse.$Shape} LoadFileListResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadFileListResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LoadFileListResponse message.
             * @function verify
             * @memberof lebai.file.LoadFileListResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LoadFileListResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.files != null && $Object.hasOwnProperty.call(message, "files")) {
                    if (!$Array.isArray(message.files))
                        return "files: array expected";
                    for (var i = 0; i < message.files.length; ++i) {
                        var error = $root.lebai.file.FileName.verify(message.files[i], _depth + 1);
                        if (error)
                            return "files." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a LoadFileListResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.file.LoadFileListResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.file.LoadFileListResponse} LoadFileListResponse
             */
            LoadFileListResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.file.LoadFileListResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.file.LoadFileListResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.file.LoadFileListResponse();
                if (object.files) {
                    if (!$Array.isArray(object.files))
                        throw $TypeError(".lebai.file.LoadFileListResponse.files: array expected");
                    message.files = $Array(object.files.length);
                    for (var i = 0; i < object.files.length; ++i) {
                        if (!$util.isObject(object.files[i]))
                            throw $TypeError(".lebai.file.LoadFileListResponse.files: object expected");
                        message.files[i] = $root.lebai.file.FileName.fromObject(object.files[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a LoadFileListResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.file.LoadFileListResponse
             * @static
             * @param {lebai.file.LoadFileListResponse} message LoadFileListResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LoadFileListResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.files = [];
                if (message.files && message.files.length) {
                    object.files = $Array(message.files.length);
                    for (var j = 0; j < message.files.length; ++j)
                        object.files[j] = $root.lebai.file.FileName.toObject(message.files[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this LoadFileListResponse to JSON.
             * @function toJSON
             * @memberof lebai.file.LoadFileListResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LoadFileListResponse.prototype.toJSON = function() {
                return LoadFileListResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LoadFileListResponse
             * @function getTypeUrl
             * @memberof lebai.file.LoadFileListResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LoadFileListResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.file.LoadFileListResponse";
            };

            return LoadFileListResponse;
        })();

        file.ZipRequest = (function() {

            /**
             * Properties of a ZipRequest.
             * @typedef {Object} lebai.file.ZipRequest.$Properties
             * @property {lebai.file.FileIndex.$Properties|null} [zip] ZipRequest zip
             * @property {Array.<string>|null} [files] ZipRequest files
             * @property {string|null} [dir] ZipRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ZipRequest.
             * @memberof lebai.file
             * @interface IZipRequest
             * @augments lebai.file.ZipRequest.$Properties
             * @deprecated Use lebai.file.ZipRequest.$Properties instead.
             */

            /**
             * Shape of a ZipRequest.
             * @typedef {lebai.file.ZipRequest.$Properties} lebai.file.ZipRequest.$Shape
             */

            /**
             * Constructs a new ZipRequest.
             * @memberof lebai.file
             * @classdesc Represents a ZipRequest.
             * @constructor
             * @param {lebai.file.ZipRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ZipRequest = function (properties) {
                this.files = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ZipRequest zip.
             * @member {lebai.file.FileIndex.$Properties|null|undefined} zip
             * @memberof lebai.file.ZipRequest
             * @instance
             */
            ZipRequest.prototype.zip = null;

            /**
             * ZipRequest files.
             * @member {Array.<string>} files
             * @memberof lebai.file.ZipRequest
             * @instance
             */
            ZipRequest.prototype.files = $util.emptyArray;

            /**
             * ZipRequest dir.
             * @member {string} dir
             * @memberof lebai.file.ZipRequest
             * @instance
             */
            ZipRequest.prototype.dir = "";

            /**
             * Creates a new ZipRequest instance using the specified properties.
             * @function create
             * @memberof lebai.file.ZipRequest
             * @static
             * @param {lebai.file.ZipRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.file.ZipRequest} ZipRequest instance
             * @type {{
             *   (properties: lebai.file.ZipRequest.$Shape): lebai.file.ZipRequest & lebai.file.ZipRequest.$Shape;
             *   (properties?: lebai.file.ZipRequest.$Properties): lebai.file.ZipRequest;
             * }}
             */
            ZipRequest.create = function(properties) {
                return new ZipRequest(properties);
            };

            /**
             * Encodes the specified ZipRequest message. Does not implicitly {@link lebai.file.ZipRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.file.ZipRequest
             * @static
             * @param {lebai.file.ZipRequest.$Properties} message ZipRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ZipRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.zip != null && $Object.hasOwnProperty.call(message, "zip"))
                    $root.lebai.file.FileIndex.encode(message.zip, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.dir);
                if (message.files != null && message.files.length)
                    for (var i = 0; i < message.files.length; ++i)
                        writer.uint32(/* id 11, wireType 2 =*/90).string(message.files[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ZipRequest message, length delimited. Does not implicitly {@link lebai.file.ZipRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.file.ZipRequest
             * @static
             * @param {lebai.file.ZipRequest.$Properties} message ZipRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ZipRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ZipRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.file.ZipRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.file.ZipRequest & lebai.file.ZipRequest.$Shape} ZipRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ZipRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.file.ZipRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.zip = $root.lebai.file.FileIndex.decode(reader, reader.uint32(), $undefined, _depth + 1, message.zip);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if (!(message.files && message.files.length))
                                message.files = [];
                            message.files.push(reader.stringVerify());
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ZipRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.file.ZipRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.file.ZipRequest & lebai.file.ZipRequest.$Shape} ZipRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ZipRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ZipRequest message.
             * @function verify
             * @memberof lebai.file.ZipRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ZipRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.zip != null && $Object.hasOwnProperty.call(message, "zip")) {
                    var error = $root.lebai.file.FileIndex.verify(message.zip, _depth + 1);
                    if (error)
                        return "zip." + error;
                }
                if (message.files != null && $Object.hasOwnProperty.call(message, "files")) {
                    if (!$Array.isArray(message.files))
                        return "files: array expected";
                    for (var i = 0; i < message.files.length; ++i)
                        if (!$util.isString(message.files[i]))
                            return "files: string[] expected";
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates a ZipRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.file.ZipRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.file.ZipRequest} ZipRequest
             */
            ZipRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.file.ZipRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.file.ZipRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.file.ZipRequest();
                if (object.zip != null) {
                    if (!$util.isObject(object.zip))
                        throw $TypeError(".lebai.file.ZipRequest.zip: object expected");
                    message.zip = $root.lebai.file.FileIndex.fromObject(object.zip, _depth + 1);
                }
                if (object.files) {
                    if (!$Array.isArray(object.files))
                        throw $TypeError(".lebai.file.ZipRequest.files: array expected");
                    message.files = $Array(object.files.length);
                    for (var i = 0; i < object.files.length; ++i)
                        message.files[i] = $String(object.files[i]);
                }
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from a ZipRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.file.ZipRequest
             * @static
             * @param {lebai.file.ZipRequest} message ZipRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ZipRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.files = [];
                if (options.defaults) {
                    object.zip = null;
                    object.dir = "";
                }
                if (message.zip != null && $Object.hasOwnProperty.call(message, "zip"))
                    object.zip = $root.lebai.file.FileIndex.toObject(message.zip, options, _depth + 1);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                if (message.files && message.files.length) {
                    object.files = $Array(message.files.length);
                    for (var j = 0; j < message.files.length; ++j)
                        object.files[j] = message.files[j];
                }
                return object;
            };

            /**
             * Converts this ZipRequest to JSON.
             * @function toJSON
             * @memberof lebai.file.ZipRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ZipRequest.prototype.toJSON = function() {
                return ZipRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ZipRequest
             * @function getTypeUrl
             * @memberof lebai.file.ZipRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ZipRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.file.ZipRequest";
            };

            return ZipRequest;
        })();

        file.UnzipRequest = (function() {

            /**
             * Properties of an UnzipRequest.
             * @typedef {Object} lebai.file.UnzipRequest.$Properties
             * @property {lebai.file.FileIndex.$Properties|null} [zip] UnzipRequest zip
             * @property {Array.<string>|null} [files] UnzipRequest files
             * @property {string|null} [dir] UnzipRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an UnzipRequest.
             * @memberof lebai.file
             * @interface IUnzipRequest
             * @augments lebai.file.UnzipRequest.$Properties
             * @deprecated Use lebai.file.UnzipRequest.$Properties instead.
             */

            /**
             * Shape of an UnzipRequest.
             * @typedef {lebai.file.UnzipRequest.$Properties} lebai.file.UnzipRequest.$Shape
             */

            /**
             * Constructs a new UnzipRequest.
             * @memberof lebai.file
             * @classdesc Represents an UnzipRequest.
             * @constructor
             * @param {lebai.file.UnzipRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var UnzipRequest = function (properties) {
                this.files = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * UnzipRequest zip.
             * @member {lebai.file.FileIndex.$Properties|null|undefined} zip
             * @memberof lebai.file.UnzipRequest
             * @instance
             */
            UnzipRequest.prototype.zip = null;

            /**
             * UnzipRequest files.
             * @member {Array.<string>} files
             * @memberof lebai.file.UnzipRequest
             * @instance
             */
            UnzipRequest.prototype.files = $util.emptyArray;

            /**
             * UnzipRequest dir.
             * @member {string} dir
             * @memberof lebai.file.UnzipRequest
             * @instance
             */
            UnzipRequest.prototype.dir = "";

            /**
             * Creates a new UnzipRequest instance using the specified properties.
             * @function create
             * @memberof lebai.file.UnzipRequest
             * @static
             * @param {lebai.file.UnzipRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.file.UnzipRequest} UnzipRequest instance
             * @type {{
             *   (properties: lebai.file.UnzipRequest.$Shape): lebai.file.UnzipRequest & lebai.file.UnzipRequest.$Shape;
             *   (properties?: lebai.file.UnzipRequest.$Properties): lebai.file.UnzipRequest;
             * }}
             */
            UnzipRequest.create = function(properties) {
                return new UnzipRequest(properties);
            };

            /**
             * Encodes the specified UnzipRequest message. Does not implicitly {@link lebai.file.UnzipRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.file.UnzipRequest
             * @static
             * @param {lebai.file.UnzipRequest.$Properties} message UnzipRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UnzipRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.zip != null && $Object.hasOwnProperty.call(message, "zip"))
                    $root.lebai.file.FileIndex.encode(message.zip, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.files != null && message.files.length)
                    for (var i = 0; i < message.files.length; ++i)
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.files[i]);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified UnzipRequest message, length delimited. Does not implicitly {@link lebai.file.UnzipRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.file.UnzipRequest
             * @static
             * @param {lebai.file.UnzipRequest.$Properties} message UnzipRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UnzipRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an UnzipRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.file.UnzipRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.file.UnzipRequest & lebai.file.UnzipRequest.$Shape} UnzipRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UnzipRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.file.UnzipRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.zip = $root.lebai.file.FileIndex.decode(reader, reader.uint32(), $undefined, _depth + 1, message.zip);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.files && message.files.length))
                                message.files = [];
                            message.files.push(reader.stringVerify());
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an UnzipRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.file.UnzipRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.file.UnzipRequest & lebai.file.UnzipRequest.$Shape} UnzipRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UnzipRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an UnzipRequest message.
             * @function verify
             * @memberof lebai.file.UnzipRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            UnzipRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.zip != null && $Object.hasOwnProperty.call(message, "zip")) {
                    var error = $root.lebai.file.FileIndex.verify(message.zip, _depth + 1);
                    if (error)
                        return "zip." + error;
                }
                if (message.files != null && $Object.hasOwnProperty.call(message, "files")) {
                    if (!$Array.isArray(message.files))
                        return "files: array expected";
                    for (var i = 0; i < message.files.length; ++i)
                        if (!$util.isString(message.files[i]))
                            return "files: string[] expected";
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates an UnzipRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.file.UnzipRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.file.UnzipRequest} UnzipRequest
             */
            UnzipRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.file.UnzipRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.file.UnzipRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.file.UnzipRequest();
                if (object.zip != null) {
                    if (!$util.isObject(object.zip))
                        throw $TypeError(".lebai.file.UnzipRequest.zip: object expected");
                    message.zip = $root.lebai.file.FileIndex.fromObject(object.zip, _depth + 1);
                }
                if (object.files) {
                    if (!$Array.isArray(object.files))
                        throw $TypeError(".lebai.file.UnzipRequest.files: array expected");
                    message.files = $Array(object.files.length);
                    for (var i = 0; i < object.files.length; ++i)
                        message.files[i] = $String(object.files[i]);
                }
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from an UnzipRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.file.UnzipRequest
             * @static
             * @param {lebai.file.UnzipRequest} message UnzipRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            UnzipRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.files = [];
                if (options.defaults) {
                    object.zip = null;
                    object.dir = "";
                }
                if (message.zip != null && $Object.hasOwnProperty.call(message, "zip"))
                    object.zip = $root.lebai.file.FileIndex.toObject(message.zip, options, _depth + 1);
                if (message.files && message.files.length) {
                    object.files = $Array(message.files.length);
                    for (var j = 0; j < message.files.length; ++j)
                        object.files[j] = message.files[j];
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                return object;
            };

            /**
             * Converts this UnzipRequest to JSON.
             * @function toJSON
             * @memberof lebai.file.UnzipRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            UnzipRequest.prototype.toJSON = function() {
                return UnzipRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for UnzipRequest
             * @function getTypeUrl
             * @memberof lebai.file.UnzipRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            UnzipRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.file.UnzipRequest";
            };

            return UnzipRequest;
        })();

        file.LoadZipListRequest = (function() {

            /**
             * Properties of a LoadZipListRequest.
             * @typedef {Object} lebai.file.LoadZipListRequest.$Properties
             * @property {lebai.file.FileIndex.$Properties|null} [zip] LoadZipListRequest zip
             * @property {string|null} [dir] LoadZipListRequest dir
             * @property {string|null} [prefix] LoadZipListRequest prefix
             * @property {string|null} [suffix] LoadZipListRequest suffix
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LoadZipListRequest.
             * @memberof lebai.file
             * @interface ILoadZipListRequest
             * @augments lebai.file.LoadZipListRequest.$Properties
             * @deprecated Use lebai.file.LoadZipListRequest.$Properties instead.
             */

            /**
             * Shape of a LoadZipListRequest.
             * @typedef {lebai.file.LoadZipListRequest.$Properties} lebai.file.LoadZipListRequest.$Shape
             */

            /**
             * Constructs a new LoadZipListRequest.
             * @memberof lebai.file
             * @classdesc Represents a LoadZipListRequest.
             * @constructor
             * @param {lebai.file.LoadZipListRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LoadZipListRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LoadZipListRequest zip.
             * @member {lebai.file.FileIndex.$Properties|null|undefined} zip
             * @memberof lebai.file.LoadZipListRequest
             * @instance
             */
            LoadZipListRequest.prototype.zip = null;

            /**
             * LoadZipListRequest dir.
             * @member {string} dir
             * @memberof lebai.file.LoadZipListRequest
             * @instance
             */
            LoadZipListRequest.prototype.dir = "";

            /**
             * LoadZipListRequest prefix.
             * @member {string} prefix
             * @memberof lebai.file.LoadZipListRequest
             * @instance
             */
            LoadZipListRequest.prototype.prefix = "";

            /**
             * LoadZipListRequest suffix.
             * @member {string} suffix
             * @memberof lebai.file.LoadZipListRequest
             * @instance
             */
            LoadZipListRequest.prototype.suffix = "";

            /**
             * Creates a new LoadZipListRequest instance using the specified properties.
             * @function create
             * @memberof lebai.file.LoadZipListRequest
             * @static
             * @param {lebai.file.LoadZipListRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.file.LoadZipListRequest} LoadZipListRequest instance
             * @type {{
             *   (properties: lebai.file.LoadZipListRequest.$Shape): lebai.file.LoadZipListRequest & lebai.file.LoadZipListRequest.$Shape;
             *   (properties?: lebai.file.LoadZipListRequest.$Properties): lebai.file.LoadZipListRequest;
             * }}
             */
            LoadZipListRequest.create = function(properties) {
                return new LoadZipListRequest(properties);
            };

            /**
             * Encodes the specified LoadZipListRequest message. Does not implicitly {@link lebai.file.LoadZipListRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.file.LoadZipListRequest
             * @static
             * @param {lebai.file.LoadZipListRequest.$Properties} message LoadZipListRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadZipListRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.zip != null && $Object.hasOwnProperty.call(message, "zip"))
                    $root.lebai.file.FileIndex.encode(message.zip, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.dir);
                if (message.prefix != null && $Object.hasOwnProperty.call(message, "prefix") && message.prefix !== "")
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.prefix);
                if (message.suffix != null && $Object.hasOwnProperty.call(message, "suffix") && message.suffix !== "")
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.suffix);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LoadZipListRequest message, length delimited. Does not implicitly {@link lebai.file.LoadZipListRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.file.LoadZipListRequest
             * @static
             * @param {lebai.file.LoadZipListRequest.$Properties} message LoadZipListRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadZipListRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LoadZipListRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.file.LoadZipListRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.file.LoadZipListRequest & lebai.file.LoadZipListRequest.$Shape} LoadZipListRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadZipListRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.file.LoadZipListRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.zip = $root.lebai.file.FileIndex.decode(reader, reader.uint32(), $undefined, _depth + 1, message.zip);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.prefix = value;
                            else
                                delete message.prefix;
                            continue;
                        }
                    case 13: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.suffix = value;
                            else
                                delete message.suffix;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LoadZipListRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.file.LoadZipListRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.file.LoadZipListRequest & lebai.file.LoadZipListRequest.$Shape} LoadZipListRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadZipListRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LoadZipListRequest message.
             * @function verify
             * @memberof lebai.file.LoadZipListRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LoadZipListRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.zip != null && $Object.hasOwnProperty.call(message, "zip")) {
                    var error = $root.lebai.file.FileIndex.verify(message.zip, _depth + 1);
                    if (error)
                        return "zip." + error;
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                if (message.prefix != null && $Object.hasOwnProperty.call(message, "prefix"))
                    if (!$util.isString(message.prefix))
                        return "prefix: string expected";
                if (message.suffix != null && $Object.hasOwnProperty.call(message, "suffix"))
                    if (!$util.isString(message.suffix))
                        return "suffix: string expected";
                return null;
            };

            /**
             * Creates a LoadZipListRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.file.LoadZipListRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.file.LoadZipListRequest} LoadZipListRequest
             */
            LoadZipListRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.file.LoadZipListRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.file.LoadZipListRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.file.LoadZipListRequest();
                if (object.zip != null) {
                    if (!$util.isObject(object.zip))
                        throw $TypeError(".lebai.file.LoadZipListRequest.zip: object expected");
                    message.zip = $root.lebai.file.FileIndex.fromObject(object.zip, _depth + 1);
                }
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                if (object.prefix != null)
                    if (typeof object.prefix !== "string" || object.prefix.length)
                        message.prefix = $String(object.prefix);
                if (object.suffix != null)
                    if (typeof object.suffix !== "string" || object.suffix.length)
                        message.suffix = $String(object.suffix);
                return message;
            };

            /**
             * Creates a plain object from a LoadZipListRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.file.LoadZipListRequest
             * @static
             * @param {lebai.file.LoadZipListRequest} message LoadZipListRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LoadZipListRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.zip = null;
                    object.dir = "";
                    object.prefix = "";
                    object.suffix = "";
                }
                if (message.zip != null && $Object.hasOwnProperty.call(message, "zip"))
                    object.zip = $root.lebai.file.FileIndex.toObject(message.zip, options, _depth + 1);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                if (message.prefix != null && $Object.hasOwnProperty.call(message, "prefix"))
                    object.prefix = message.prefix;
                if (message.suffix != null && $Object.hasOwnProperty.call(message, "suffix"))
                    object.suffix = message.suffix;
                return object;
            };

            /**
             * Converts this LoadZipListRequest to JSON.
             * @function toJSON
             * @memberof lebai.file.LoadZipListRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LoadZipListRequest.prototype.toJSON = function() {
                return LoadZipListRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LoadZipListRequest
             * @function getTypeUrl
             * @memberof lebai.file.LoadZipListRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LoadZipListRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.file.LoadZipListRequest";
            };

            return LoadZipListRequest;
        })();

        file.LoadZipListResponse = (function() {

            /**
             * Properties of a LoadZipListResponse.
             * @typedef {Object} lebai.file.LoadZipListResponse.$Properties
             * @property {Array.<lebai.file.FileName.$Properties>|null} [files] LoadZipListResponse files
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LoadZipListResponse.
             * @memberof lebai.file
             * @interface ILoadZipListResponse
             * @augments lebai.file.LoadZipListResponse.$Properties
             * @deprecated Use lebai.file.LoadZipListResponse.$Properties instead.
             */

            /**
             * Shape of a LoadZipListResponse.
             * @typedef {lebai.file.LoadZipListResponse.$Properties} lebai.file.LoadZipListResponse.$Shape
             */

            /**
             * Constructs a new LoadZipListResponse.
             * @memberof lebai.file
             * @classdesc Represents a LoadZipListResponse.
             * @constructor
             * @param {lebai.file.LoadZipListResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LoadZipListResponse = function (properties) {
                this.files = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LoadZipListResponse files.
             * @member {Array.<lebai.file.FileName.$Properties>} files
             * @memberof lebai.file.LoadZipListResponse
             * @instance
             */
            LoadZipListResponse.prototype.files = $util.emptyArray;

            /**
             * Creates a new LoadZipListResponse instance using the specified properties.
             * @function create
             * @memberof lebai.file.LoadZipListResponse
             * @static
             * @param {lebai.file.LoadZipListResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.file.LoadZipListResponse} LoadZipListResponse instance
             * @type {{
             *   (properties: lebai.file.LoadZipListResponse.$Shape): lebai.file.LoadZipListResponse & lebai.file.LoadZipListResponse.$Shape;
             *   (properties?: lebai.file.LoadZipListResponse.$Properties): lebai.file.LoadZipListResponse;
             * }}
             */
            LoadZipListResponse.create = function(properties) {
                return new LoadZipListResponse(properties);
            };

            /**
             * Encodes the specified LoadZipListResponse message. Does not implicitly {@link lebai.file.LoadZipListResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.file.LoadZipListResponse
             * @static
             * @param {lebai.file.LoadZipListResponse.$Properties} message LoadZipListResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadZipListResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.files != null && message.files.length)
                    for (var i = 0; i < message.files.length; ++i)
                        $root.lebai.file.FileName.encode(message.files[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LoadZipListResponse message, length delimited. Does not implicitly {@link lebai.file.LoadZipListResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.file.LoadZipListResponse
             * @static
             * @param {lebai.file.LoadZipListResponse.$Properties} message LoadZipListResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadZipListResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LoadZipListResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.file.LoadZipListResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.file.LoadZipListResponse & lebai.file.LoadZipListResponse.$Shape} LoadZipListResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadZipListResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.file.LoadZipListResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.files && message.files.length))
                                message.files = [];
                            message.files.push($root.lebai.file.FileName.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LoadZipListResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.file.LoadZipListResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.file.LoadZipListResponse & lebai.file.LoadZipListResponse.$Shape} LoadZipListResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadZipListResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LoadZipListResponse message.
             * @function verify
             * @memberof lebai.file.LoadZipListResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LoadZipListResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.files != null && $Object.hasOwnProperty.call(message, "files")) {
                    if (!$Array.isArray(message.files))
                        return "files: array expected";
                    for (var i = 0; i < message.files.length; ++i) {
                        var error = $root.lebai.file.FileName.verify(message.files[i], _depth + 1);
                        if (error)
                            return "files." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a LoadZipListResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.file.LoadZipListResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.file.LoadZipListResponse} LoadZipListResponse
             */
            LoadZipListResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.file.LoadZipListResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.file.LoadZipListResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.file.LoadZipListResponse();
                if (object.files) {
                    if (!$Array.isArray(object.files))
                        throw $TypeError(".lebai.file.LoadZipListResponse.files: array expected");
                    message.files = $Array(object.files.length);
                    for (var i = 0; i < object.files.length; ++i) {
                        if (!$util.isObject(object.files[i]))
                            throw $TypeError(".lebai.file.LoadZipListResponse.files: object expected");
                        message.files[i] = $root.lebai.file.FileName.fromObject(object.files[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a LoadZipListResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.file.LoadZipListResponse
             * @static
             * @param {lebai.file.LoadZipListResponse} message LoadZipListResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LoadZipListResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.files = [];
                if (message.files && message.files.length) {
                    object.files = $Array(message.files.length);
                    for (var j = 0; j < message.files.length; ++j)
                        object.files[j] = $root.lebai.file.FileName.toObject(message.files[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this LoadZipListResponse to JSON.
             * @function toJSON
             * @memberof lebai.file.LoadZipListResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LoadZipListResponse.prototype.toJSON = function() {
                return LoadZipListResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LoadZipListResponse
             * @function getTypeUrl
             * @memberof lebai.file.LoadZipListResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LoadZipListResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.file.LoadZipListResponse";
            };

            return LoadZipListResponse;
        })();

        file.FileService = (function() {

            /**
             * Constructs a new FileService service.
             * @memberof lebai.file
             * @classdesc Represents a FileService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var FileService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(FileService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: FileService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new FileService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.file.FileService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {FileService} RPC service. Useful where requests and/or responses are streamed.
             */
            FileService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.file.FileService#saveFile}.
             * @memberof lebai.file.FileService
             * @typedef SaveFileCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SaveFile.
             * @memberof lebai.file.FileService
             * @typedef SaveFile
             * @type {{
             *   (request: lebai.file.ISaveFileRequest, callback: lebai.file.FileService.SaveFileCallback): void;
             *   (request: lebai.file.ISaveFileRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SaveFile";
             *   readonly path: "/lebai.file.FileService/SaveFile";
             *   readonly requestType: "SaveFileRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SaveFile.
             * @name lebai.file.FileService#saveFile
             * @type {lebai.file.FileService.SaveFile}
             */
            $Object.defineProperties(FileService.prototype.saveFile = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.saveFile, $root.lebai.file.SaveFileRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SaveFile" },
                path: { value: "/lebai.file.FileService/SaveFile" },
                requestType: { value: "SaveFileRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.file.FileService#downloadFile}.
             * @memberof lebai.file.FileService
             * @typedef DownloadFileCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls DownloadFile.
             * @memberof lebai.file.FileService
             * @typedef DownloadFile
             * @type {{
             *   (request: lebai.file.IDownloadFileRequest, callback: lebai.file.FileService.DownloadFileCallback): void;
             *   (request: lebai.file.IDownloadFileRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "DownloadFile";
             *   readonly path: "/lebai.file.FileService/DownloadFile";
             *   readonly requestType: "DownloadFileRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls DownloadFile.
             * @name lebai.file.FileService#downloadFile
             * @type {lebai.file.FileService.DownloadFile}
             */
            $Object.defineProperties(FileService.prototype.downloadFile = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.downloadFile, $root.lebai.file.DownloadFileRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "DownloadFile" },
                path: { value: "/lebai.file.FileService/DownloadFile" },
                requestType: { value: "DownloadFileRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.file.FileService#renameFile}.
             * @memberof lebai.file.FileService
             * @typedef RenameFileCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls RenameFile.
             * @memberof lebai.file.FileService
             * @typedef RenameFile
             * @type {{
             *   (request: lebai.file.IRenameFileRequest, callback: lebai.file.FileService.RenameFileCallback): void;
             *   (request: lebai.file.IRenameFileRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "RenameFile";
             *   readonly path: "/lebai.file.FileService/RenameFile";
             *   readonly requestType: "RenameFileRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls RenameFile.
             * @name lebai.file.FileService#renameFile
             * @type {lebai.file.FileService.RenameFile}
             */
            $Object.defineProperties(FileService.prototype.renameFile = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.renameFile, $root.lebai.file.RenameFileRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "RenameFile" },
                path: { value: "/lebai.file.FileService/RenameFile" },
                requestType: { value: "RenameFileRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.file.FileService#loadFile}.
             * @memberof lebai.file.FileService
             * @typedef LoadFileCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.file.File} [response] File
             */

            /**
             * Calls LoadFile.
             * @memberof lebai.file.FileService
             * @typedef LoadFile
             * @type {{
             *   (request: lebai.file.IFileIndex, callback: lebai.file.FileService.LoadFileCallback): void;
             *   (request: lebai.file.IFileIndex): Promise<lebai.file.File>;
             *   readonly name: "LoadFile";
             *   readonly path: "/lebai.file.FileService/LoadFile";
             *   readonly requestType: "FileIndex";
             *   readonly responseType: "File";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadFile.
             * @name lebai.file.FileService#loadFile
             * @type {lebai.file.FileService.LoadFile}
             */
            $Object.defineProperties(FileService.prototype.loadFile = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.loadFile, $root.lebai.file.FileIndex, $root.lebai.file.File, request, callback);
            }, {
                name: { value: "LoadFile" },
                path: { value: "/lebai.file.FileService/LoadFile" },
                requestType: { value: "FileIndex" },
                responseType: { value: "File" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.file.FileService#loadFileList}.
             * @memberof lebai.file.FileService
             * @typedef LoadFileListCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.file.LoadFileListResponse} [response] LoadFileListResponse
             */

            /**
             * Calls LoadFileList.
             * @memberof lebai.file.FileService
             * @typedef LoadFileList
             * @type {{
             *   (request: lebai.file.ILoadFileListRequest, callback: lebai.file.FileService.LoadFileListCallback): void;
             *   (request: lebai.file.ILoadFileListRequest): Promise<lebai.file.LoadFileListResponse>;
             *   readonly name: "LoadFileList";
             *   readonly path: "/lebai.file.FileService/LoadFileList";
             *   readonly requestType: "LoadFileListRequest";
             *   readonly responseType: "LoadFileListResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadFileList.
             * @name lebai.file.FileService#loadFileList
             * @type {lebai.file.FileService.LoadFileList}
             */
            $Object.defineProperties(FileService.prototype.loadFileList = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.loadFileList, $root.lebai.file.LoadFileListRequest, $root.lebai.file.LoadFileListResponse, request, callback);
            }, {
                name: { value: "LoadFileList" },
                path: { value: "/lebai.file.FileService/LoadFileList" },
                requestType: { value: "LoadFileListRequest" },
                responseType: { value: "LoadFileListResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.file.FileService#zip}.
             * @memberof lebai.file.FileService
             * @typedef ZipCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls Zip.
             * @memberof lebai.file.FileService
             * @typedef Zip
             * @type {{
             *   (request: lebai.file.IZipRequest, callback: lebai.file.FileService.ZipCallback): void;
             *   (request: lebai.file.IZipRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "Zip";
             *   readonly path: "/lebai.file.FileService/Zip";
             *   readonly requestType: "ZipRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls Zip.
             * @name lebai.file.FileService#zip
             * @type {lebai.file.FileService.Zip}
             */
            $Object.defineProperties(FileService.prototype.zip = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.zip, $root.lebai.file.ZipRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "Zip" },
                path: { value: "/lebai.file.FileService/Zip" },
                requestType: { value: "ZipRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.file.FileService#unzip}.
             * @memberof lebai.file.FileService
             * @typedef UnzipCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls Unzip.
             * @memberof lebai.file.FileService
             * @typedef Unzip
             * @type {{
             *   (request: lebai.file.IUnzipRequest, callback: lebai.file.FileService.UnzipCallback): void;
             *   (request: lebai.file.IUnzipRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "Unzip";
             *   readonly path: "/lebai.file.FileService/Unzip";
             *   readonly requestType: "UnzipRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls Unzip.
             * @name lebai.file.FileService#unzip
             * @type {lebai.file.FileService.Unzip}
             */
            $Object.defineProperties(FileService.prototype.unzip = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.unzip, $root.lebai.file.UnzipRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "Unzip" },
                path: { value: "/lebai.file.FileService/Unzip" },
                requestType: { value: "UnzipRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.file.FileService#loadZipList}.
             * @memberof lebai.file.FileService
             * @typedef LoadZipListCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.file.LoadZipListResponse} [response] LoadZipListResponse
             */

            /**
             * Calls LoadZipList.
             * @memberof lebai.file.FileService
             * @typedef LoadZipList
             * @type {{
             *   (request: lebai.file.ILoadZipListRequest, callback: lebai.file.FileService.LoadZipListCallback): void;
             *   (request: lebai.file.ILoadZipListRequest): Promise<lebai.file.LoadZipListResponse>;
             *   readonly name: "LoadZipList";
             *   readonly path: "/lebai.file.FileService/LoadZipList";
             *   readonly requestType: "LoadZipListRequest";
             *   readonly responseType: "LoadZipListResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadZipList.
             * @name lebai.file.FileService#loadZipList
             * @type {lebai.file.FileService.LoadZipList}
             */
            $Object.defineProperties(FileService.prototype.loadZipList = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, FileService.prototype.loadZipList, $root.lebai.file.LoadZipListRequest, $root.lebai.file.LoadZipListResponse, request, callback);
            }, {
                name: { value: "LoadZipList" },
                path: { value: "/lebai.file.FileService/LoadZipList" },
                requestType: { value: "LoadZipListRequest" },
                responseType: { value: "LoadZipListResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return FileService;
        })();

        return file;
    })();

    lebai.flange = (function() {

        /**
         * Namespace flange.
         * @memberof lebai
         * @namespace
         */
        var flange = {};

        flange.SetFlangeBaudRateRequest = (function() {

            /**
             * Properties of a SetFlangeBaudRateRequest.
             * @typedef {Object} lebai.flange.SetFlangeBaudRateRequest.$Properties
             * @property {number|null} [baudRate] SetFlangeBaudRateRequest baudRate
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetFlangeBaudRateRequest.
             * @memberof lebai.flange
             * @interface ISetFlangeBaudRateRequest
             * @augments lebai.flange.SetFlangeBaudRateRequest.$Properties
             * @deprecated Use lebai.flange.SetFlangeBaudRateRequest.$Properties instead.
             */

            /**
             * Shape of a SetFlangeBaudRateRequest.
             * @typedef {lebai.flange.SetFlangeBaudRateRequest.$Properties} lebai.flange.SetFlangeBaudRateRequest.$Shape
             */

            /**
             * Constructs a new SetFlangeBaudRateRequest.
             * @memberof lebai.flange
             * @classdesc Represents a SetFlangeBaudRateRequest.
             * @constructor
             * @param {lebai.flange.SetFlangeBaudRateRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetFlangeBaudRateRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetFlangeBaudRateRequest baudRate.
             * @member {number} baudRate
             * @memberof lebai.flange.SetFlangeBaudRateRequest
             * @instance
             */
            SetFlangeBaudRateRequest.prototype.baudRate = 0;

            /**
             * Creates a new SetFlangeBaudRateRequest instance using the specified properties.
             * @function create
             * @memberof lebai.flange.SetFlangeBaudRateRequest
             * @static
             * @param {lebai.flange.SetFlangeBaudRateRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.flange.SetFlangeBaudRateRequest} SetFlangeBaudRateRequest instance
             * @type {{
             *   (properties: lebai.flange.SetFlangeBaudRateRequest.$Shape): lebai.flange.SetFlangeBaudRateRequest & lebai.flange.SetFlangeBaudRateRequest.$Shape;
             *   (properties?: lebai.flange.SetFlangeBaudRateRequest.$Properties): lebai.flange.SetFlangeBaudRateRequest;
             * }}
             */
            SetFlangeBaudRateRequest.create = function(properties) {
                return new SetFlangeBaudRateRequest(properties);
            };

            /**
             * Encodes the specified SetFlangeBaudRateRequest message. Does not implicitly {@link lebai.flange.SetFlangeBaudRateRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.flange.SetFlangeBaudRateRequest
             * @static
             * @param {lebai.flange.SetFlangeBaudRateRequest.$Properties} message SetFlangeBaudRateRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetFlangeBaudRateRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.baudRate != null && $Object.hasOwnProperty.call(message, "baudRate") && message.baudRate !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.baudRate);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetFlangeBaudRateRequest message, length delimited. Does not implicitly {@link lebai.flange.SetFlangeBaudRateRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.flange.SetFlangeBaudRateRequest
             * @static
             * @param {lebai.flange.SetFlangeBaudRateRequest.$Properties} message SetFlangeBaudRateRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetFlangeBaudRateRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetFlangeBaudRateRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.flange.SetFlangeBaudRateRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.flange.SetFlangeBaudRateRequest & lebai.flange.SetFlangeBaudRateRequest.$Shape} SetFlangeBaudRateRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetFlangeBaudRateRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.flange.SetFlangeBaudRateRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.baudRate = value;
                            else
                                delete message.baudRate;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetFlangeBaudRateRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.flange.SetFlangeBaudRateRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.flange.SetFlangeBaudRateRequest & lebai.flange.SetFlangeBaudRateRequest.$Shape} SetFlangeBaudRateRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetFlangeBaudRateRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetFlangeBaudRateRequest message.
             * @function verify
             * @memberof lebai.flange.SetFlangeBaudRateRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetFlangeBaudRateRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.baudRate != null && $Object.hasOwnProperty.call(message, "baudRate"))
                    if (!$util.isInteger(message.baudRate))
                        return "baudRate: integer expected";
                return null;
            };

            /**
             * Creates a SetFlangeBaudRateRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.flange.SetFlangeBaudRateRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.flange.SetFlangeBaudRateRequest} SetFlangeBaudRateRequest
             */
            SetFlangeBaudRateRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.flange.SetFlangeBaudRateRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.flange.SetFlangeBaudRateRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.flange.SetFlangeBaudRateRequest();
                if (object.baudRate != null)
                    if ($Number(object.baudRate) !== 0)
                        message.baudRate = object.baudRate >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a SetFlangeBaudRateRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.flange.SetFlangeBaudRateRequest
             * @static
             * @param {lebai.flange.SetFlangeBaudRateRequest} message SetFlangeBaudRateRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetFlangeBaudRateRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.baudRate = 0;
                if (message.baudRate != null && $Object.hasOwnProperty.call(message, "baudRate"))
                    object.baudRate = message.baudRate;
                return object;
            };

            /**
             * Converts this SetFlangeBaudRateRequest to JSON.
             * @function toJSON
             * @memberof lebai.flange.SetFlangeBaudRateRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetFlangeBaudRateRequest.prototype.toJSON = function() {
                return SetFlangeBaudRateRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetFlangeBaudRateRequest
             * @function getTypeUrl
             * @memberof lebai.flange.SetFlangeBaudRateRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetFlangeBaudRateRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.flange.SetFlangeBaudRateRequest";
            };

            return SetFlangeBaudRateRequest;
        })();

        flange.FlangeService = (function() {

            /**
             * Constructs a new FlangeService service.
             * @memberof lebai.flange
             * @classdesc Represents a FlangeService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var FlangeService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(FlangeService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: FlangeService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new FlangeService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.flange.FlangeService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {FlangeService} RPC service. Useful where requests and/or responses are streamed.
             */
            FlangeService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.flange.FlangeService#setFlangeBaudRate}.
             * @memberof lebai.flange.FlangeService
             * @typedef SetFlangeBaudRateCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetFlangeBaudRate.
             * @memberof lebai.flange.FlangeService
             * @typedef SetFlangeBaudRate
             * @type {{
             *   (request: lebai.flange.ISetFlangeBaudRateRequest, callback: lebai.flange.FlangeService.SetFlangeBaudRateCallback): void;
             *   (request: lebai.flange.ISetFlangeBaudRateRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetFlangeBaudRate";
             *   readonly path: "/lebai.flange.FlangeService/SetFlangeBaudRate";
             *   readonly requestType: "SetFlangeBaudRateRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetFlangeBaudRate.
             * @name lebai.flange.FlangeService#setFlangeBaudRate
             * @type {lebai.flange.FlangeService.SetFlangeBaudRate}
             */
            $Object.defineProperties(FlangeService.prototype.setFlangeBaudRate = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, FlangeService.prototype.setFlangeBaudRate, $root.lebai.flange.SetFlangeBaudRateRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetFlangeBaudRate" },
                path: { value: "/lebai.flange.FlangeService/SetFlangeBaudRate" },
                requestType: { value: "SetFlangeBaudRateRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return FlangeService;
        })();

        return flange;
    })();

    lebai.io = (function() {

        /**
         * Namespace io.
         * @memberof lebai
         * @namespace
         */
        var io = {};

        /**
         * IoDevice enum.
         * @name lebai.io.IoDevice
         * @enum {number}
         * @property {number} ROBOT=0 ROBOT value
         * @property {number} FLANGE=1 FLANGE value
         * @property {number} EXTRA=2 EXTRA value
         * @property {number} ROBOT_BTN=10 ROBOT_BTN value
         * @property {number} SHOULDER=11 SHOULDER value
         * @property {number} FLANGE_BTN=12 FLANGE_BTN value
         */
        io.IoDevice = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "ROBOT"] = 0;
            values[valuesById[1] = "FLANGE"] = 1;
            values[valuesById[2] = "EXTRA"] = 2;
            values[valuesById[10] = "ROBOT_BTN"] = 10;
            values[valuesById[11] = "SHOULDER"] = 11;
            values[valuesById[12] = "FLANGE_BTN"] = 12;
            return values;
        })();

        /**
         * IoPinKind enum.
         * @name lebai.io.IoPinKind
         * @enum {number}
         * @property {number} DI=0 DI value
         * @property {number} DO=1 DO value
         * @property {number} AI=2 AI value
         * @property {number} AO=3 AO value
         */
        io.IoPinKind = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "DI"] = 0;
            values[valuesById[1] = "DO"] = 1;
            values[valuesById[2] = "AI"] = 2;
            values[valuesById[3] = "AO"] = 3;
            return values;
        })();

        /**
         * DigitalMode enum.
         * @name lebai.io.DigitalMode
         * @enum {number}
         * @property {number} INPUT=0 INPUT value
         * @property {number} OUTPUT=1 OUTPUT value
         */
        io.DigitalMode = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "INPUT"] = 0;
            values[valuesById[1] = "OUTPUT"] = 1;
            return values;
        })();

        /**
         * AnalogMode enum.
         * @name lebai.io.AnalogMode
         * @enum {number}
         * @property {number} VOLTAGE=0 VOLTAGE value
         * @property {number} CURRENT=1 CURRENT value
         */
        io.AnalogMode = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "VOLTAGE"] = 0;
            values[valuesById[1] = "CURRENT"] = 1;
            return values;
        })();

        io.SetDoPinRequest = (function() {

            /**
             * Properties of a SetDoPinRequest.
             * @typedef {Object} lebai.io.SetDoPinRequest.$Properties
             * @property {lebai.io.IoDevice|null} [device] SetDoPinRequest device
             * @property {number|null} [pin] SetDoPinRequest pin
             * @property {number|null} [value] SetDoPinRequest value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetDoPinRequest.
             * @memberof lebai.io
             * @interface ISetDoPinRequest
             * @augments lebai.io.SetDoPinRequest.$Properties
             * @deprecated Use lebai.io.SetDoPinRequest.$Properties instead.
             */

            /**
             * Shape of a SetDoPinRequest.
             * @typedef {lebai.io.SetDoPinRequest.$Properties} lebai.io.SetDoPinRequest.$Shape
             */

            /**
             * Constructs a new SetDoPinRequest.
             * @memberof lebai.io
             * @classdesc Represents a SetDoPinRequest.
             * @constructor
             * @param {lebai.io.SetDoPinRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetDoPinRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetDoPinRequest device.
             * @member {lebai.io.IoDevice} device
             * @memberof lebai.io.SetDoPinRequest
             * @instance
             */
            SetDoPinRequest.prototype.device = 0;

            /**
             * SetDoPinRequest pin.
             * @member {number} pin
             * @memberof lebai.io.SetDoPinRequest
             * @instance
             */
            SetDoPinRequest.prototype.pin = 0;

            /**
             * SetDoPinRequest value.
             * @member {number} value
             * @memberof lebai.io.SetDoPinRequest
             * @instance
             */
            SetDoPinRequest.prototype.value = 0;

            /**
             * Creates a new SetDoPinRequest instance using the specified properties.
             * @function create
             * @memberof lebai.io.SetDoPinRequest
             * @static
             * @param {lebai.io.SetDoPinRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.io.SetDoPinRequest} SetDoPinRequest instance
             * @type {{
             *   (properties: lebai.io.SetDoPinRequest.$Shape): lebai.io.SetDoPinRequest & lebai.io.SetDoPinRequest.$Shape;
             *   (properties?: lebai.io.SetDoPinRequest.$Properties): lebai.io.SetDoPinRequest;
             * }}
             */
            SetDoPinRequest.create = function(properties) {
                return new SetDoPinRequest(properties);
            };

            /**
             * Encodes the specified SetDoPinRequest message. Does not implicitly {@link lebai.io.SetDoPinRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.SetDoPinRequest
             * @static
             * @param {lebai.io.SetDoPinRequest.$Properties} message SetDoPinRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetDoPinRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.pin);
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetDoPinRequest message, length delimited. Does not implicitly {@link lebai.io.SetDoPinRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.SetDoPinRequest
             * @static
             * @param {lebai.io.SetDoPinRequest.$Properties} message SetDoPinRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetDoPinRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetDoPinRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.SetDoPinRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.SetDoPinRequest & lebai.io.SetDoPinRequest.$Shape} SetDoPinRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetDoPinRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.SetDoPinRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetDoPinRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.SetDoPinRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.SetDoPinRequest & lebai.io.SetDoPinRequest.$Shape} SetDoPinRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetDoPinRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetDoPinRequest message.
             * @function verify
             * @memberof lebai.io.SetDoPinRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetDoPinRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (typeof message.device !== "number" || (message.device | 0) !== message.device)
                        return "device: enum value expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isInteger(message.pin))
                        return "pin: integer expected";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                return null;
            };

            /**
             * Creates a SetDoPinRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.SetDoPinRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.SetDoPinRequest} SetDoPinRequest
             */
            SetDoPinRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.SetDoPinRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.SetDoPinRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.SetDoPinRequest();
                if (object.device !== 0 && (typeof object.device !== "string" || $root.lebai.io.IoDevice[object.device] !== 0))
                    switch (object.device) {
                    case "ROBOT":
                    case 0:
                        message.device = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.device = 1;
                        break;
                    case "EXTRA":
                    case 2:
                        message.device = 2;
                        break;
                    case "ROBOT_BTN":
                    case 10:
                        message.device = 10;
                        break;
                    case "SHOULDER":
                    case 11:
                        message.device = 11;
                        break;
                    case "FLANGE_BTN":
                    case 12:
                        message.device = 12;
                        break;
                    default:
                        if (typeof object.device === "number" && (object.device | 0) === object.device)
                            message.device = object.device;
                    }
                if (object.pin != null)
                    if ($Number(object.pin) !== 0)
                        message.pin = object.pin >>> 0;
                if (object.value != null)
                    if ($Number(object.value) !== 0)
                        message.value = object.value >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a SetDoPinRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.SetDoPinRequest
             * @static
             * @param {lebai.io.SetDoPinRequest} message SetDoPinRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetDoPinRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = options.enums === $String ? "ROBOT" : 0;
                    object.pin = 0;
                    object.value = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = options.enums === $String ? $root.lebai.io.IoDevice[message.device] === $undefined ? message.device : $root.lebai.io.IoDevice[message.device] : message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this SetDoPinRequest to JSON.
             * @function toJSON
             * @memberof lebai.io.SetDoPinRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetDoPinRequest.prototype.toJSON = function() {
                return SetDoPinRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetDoPinRequest
             * @function getTypeUrl
             * @memberof lebai.io.SetDoPinRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetDoPinRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.SetDoPinRequest";
            };

            return SetDoPinRequest;
        })();

        io.SetDoPinsRequest = (function() {

            /**
             * Properties of a SetDoPinsRequest.
             * @typedef {Object} lebai.io.SetDoPinsRequest.$Properties
             * @property {lebai.io.IoDevice|null} [device] SetDoPinsRequest device
             * @property {number|null} [pin] SetDoPinsRequest pin
             * @property {Array.<number>|null} [values] SetDoPinsRequest values
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetDoPinsRequest.
             * @memberof lebai.io
             * @interface ISetDoPinsRequest
             * @augments lebai.io.SetDoPinsRequest.$Properties
             * @deprecated Use lebai.io.SetDoPinsRequest.$Properties instead.
             */

            /**
             * Shape of a SetDoPinsRequest.
             * @typedef {lebai.io.SetDoPinsRequest.$Properties} lebai.io.SetDoPinsRequest.$Shape
             */

            /**
             * Constructs a new SetDoPinsRequest.
             * @memberof lebai.io
             * @classdesc Represents a SetDoPinsRequest.
             * @constructor
             * @param {lebai.io.SetDoPinsRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetDoPinsRequest = function (properties) {
                this.values = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetDoPinsRequest device.
             * @member {lebai.io.IoDevice} device
             * @memberof lebai.io.SetDoPinsRequest
             * @instance
             */
            SetDoPinsRequest.prototype.device = 0;

            /**
             * SetDoPinsRequest pin.
             * @member {number} pin
             * @memberof lebai.io.SetDoPinsRequest
             * @instance
             */
            SetDoPinsRequest.prototype.pin = 0;

            /**
             * SetDoPinsRequest values.
             * @member {Array.<number>} values
             * @memberof lebai.io.SetDoPinsRequest
             * @instance
             */
            SetDoPinsRequest.prototype.values = $util.emptyArray;

            /**
             * Creates a new SetDoPinsRequest instance using the specified properties.
             * @function create
             * @memberof lebai.io.SetDoPinsRequest
             * @static
             * @param {lebai.io.SetDoPinsRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.io.SetDoPinsRequest} SetDoPinsRequest instance
             * @type {{
             *   (properties: lebai.io.SetDoPinsRequest.$Shape): lebai.io.SetDoPinsRequest & lebai.io.SetDoPinsRequest.$Shape;
             *   (properties?: lebai.io.SetDoPinsRequest.$Properties): lebai.io.SetDoPinsRequest;
             * }}
             */
            SetDoPinsRequest.create = function(properties) {
                return new SetDoPinsRequest(properties);
            };

            /**
             * Encodes the specified SetDoPinsRequest message. Does not implicitly {@link lebai.io.SetDoPinsRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.SetDoPinsRequest
             * @static
             * @param {lebai.io.SetDoPinsRequest.$Properties} message SetDoPinsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetDoPinsRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.pin);
                if (message.values != null && message.values.length)
                    writer.uint32(/* id 11, wireType 2 =*/90).uint32s(message.values);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetDoPinsRequest message, length delimited. Does not implicitly {@link lebai.io.SetDoPinsRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.SetDoPinsRequest
             * @static
             * @param {lebai.io.SetDoPinsRequest.$Properties} message SetDoPinsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetDoPinsRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetDoPinsRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.SetDoPinsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.SetDoPinsRequest & lebai.io.SetDoPinsRequest.$Shape} SetDoPinsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetDoPinsRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.SetDoPinsRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.values && message.values.length))
                                    message.values = [];
                                reader.uint32s(message.values);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetDoPinsRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.SetDoPinsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.SetDoPinsRequest & lebai.io.SetDoPinsRequest.$Shape} SetDoPinsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetDoPinsRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetDoPinsRequest message.
             * @function verify
             * @memberof lebai.io.SetDoPinsRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetDoPinsRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (typeof message.device !== "number" || (message.device | 0) !== message.device)
                        return "device: enum value expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isInteger(message.pin))
                        return "pin: integer expected";
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (var i = 0; i < message.values.length; ++i)
                        if (!$util.isInteger(message.values[i]))
                            return "values: integer[] expected";
                }
                return null;
            };

            /**
             * Creates a SetDoPinsRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.SetDoPinsRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.SetDoPinsRequest} SetDoPinsRequest
             */
            SetDoPinsRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.SetDoPinsRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.SetDoPinsRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.SetDoPinsRequest();
                if (object.device !== 0 && (typeof object.device !== "string" || $root.lebai.io.IoDevice[object.device] !== 0))
                    switch (object.device) {
                    case "ROBOT":
                    case 0:
                        message.device = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.device = 1;
                        break;
                    case "EXTRA":
                    case 2:
                        message.device = 2;
                        break;
                    case "ROBOT_BTN":
                    case 10:
                        message.device = 10;
                        break;
                    case "SHOULDER":
                    case 11:
                        message.device = 11;
                        break;
                    case "FLANGE_BTN":
                    case 12:
                        message.device = 12;
                        break;
                    default:
                        if (typeof object.device === "number" && (object.device | 0) === object.device)
                            message.device = object.device;
                    }
                if (object.pin != null)
                    if ($Number(object.pin) !== 0)
                        message.pin = object.pin >>> 0;
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".lebai.io.SetDoPinsRequest.values: array expected");
                    message.values = $Array(object.values.length);
                    for (var i = 0; i < object.values.length; ++i)
                        message.values[i] = object.values[i] >>> 0;
                }
                return message;
            };

            /**
             * Creates a plain object from a SetDoPinsRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.SetDoPinsRequest
             * @static
             * @param {lebai.io.SetDoPinsRequest} message SetDoPinsRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetDoPinsRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (options.defaults) {
                    object.device = options.enums === $String ? "ROBOT" : 0;
                    object.pin = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = options.enums === $String ? $root.lebai.io.IoDevice[message.device] === $undefined ? message.device : $root.lebai.io.IoDevice[message.device] : message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (var j = 0; j < message.values.length; ++j)
                        object.values[j] = message.values[j];
                }
                return object;
            };

            /**
             * Converts this SetDoPinsRequest to JSON.
             * @function toJSON
             * @memberof lebai.io.SetDoPinsRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetDoPinsRequest.prototype.toJSON = function() {
                return SetDoPinsRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetDoPinsRequest
             * @function getTypeUrl
             * @memberof lebai.io.SetDoPinsRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetDoPinsRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.SetDoPinsRequest";
            };

            return SetDoPinsRequest;
        })();

        io.GetDioPinRequest = (function() {

            /**
             * Properties of a GetDioPinRequest.
             * @typedef {Object} lebai.io.GetDioPinRequest.$Properties
             * @property {lebai.io.IoDevice|null} [device] GetDioPinRequest device
             * @property {number|null} [pin] GetDioPinRequest pin
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetDioPinRequest.
             * @memberof lebai.io
             * @interface IGetDioPinRequest
             * @augments lebai.io.GetDioPinRequest.$Properties
             * @deprecated Use lebai.io.GetDioPinRequest.$Properties instead.
             */

            /**
             * Shape of a GetDioPinRequest.
             * @typedef {lebai.io.GetDioPinRequest.$Properties} lebai.io.GetDioPinRequest.$Shape
             */

            /**
             * Constructs a new GetDioPinRequest.
             * @memberof lebai.io
             * @classdesc Represents a GetDioPinRequest.
             * @constructor
             * @param {lebai.io.GetDioPinRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetDioPinRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetDioPinRequest device.
             * @member {lebai.io.IoDevice} device
             * @memberof lebai.io.GetDioPinRequest
             * @instance
             */
            GetDioPinRequest.prototype.device = 0;

            /**
             * GetDioPinRequest pin.
             * @member {number} pin
             * @memberof lebai.io.GetDioPinRequest
             * @instance
             */
            GetDioPinRequest.prototype.pin = 0;

            /**
             * Creates a new GetDioPinRequest instance using the specified properties.
             * @function create
             * @memberof lebai.io.GetDioPinRequest
             * @static
             * @param {lebai.io.GetDioPinRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.io.GetDioPinRequest} GetDioPinRequest instance
             * @type {{
             *   (properties: lebai.io.GetDioPinRequest.$Shape): lebai.io.GetDioPinRequest & lebai.io.GetDioPinRequest.$Shape;
             *   (properties?: lebai.io.GetDioPinRequest.$Properties): lebai.io.GetDioPinRequest;
             * }}
             */
            GetDioPinRequest.create = function(properties) {
                return new GetDioPinRequest(properties);
            };

            /**
             * Encodes the specified GetDioPinRequest message. Does not implicitly {@link lebai.io.GetDioPinRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.GetDioPinRequest
             * @static
             * @param {lebai.io.GetDioPinRequest.$Properties} message GetDioPinRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDioPinRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.pin);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetDioPinRequest message, length delimited. Does not implicitly {@link lebai.io.GetDioPinRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.GetDioPinRequest
             * @static
             * @param {lebai.io.GetDioPinRequest.$Properties} message GetDioPinRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDioPinRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetDioPinRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.GetDioPinRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.GetDioPinRequest & lebai.io.GetDioPinRequest.$Shape} GetDioPinRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDioPinRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.GetDioPinRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetDioPinRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.GetDioPinRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.GetDioPinRequest & lebai.io.GetDioPinRequest.$Shape} GetDioPinRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDioPinRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetDioPinRequest message.
             * @function verify
             * @memberof lebai.io.GetDioPinRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetDioPinRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (typeof message.device !== "number" || (message.device | 0) !== message.device)
                        return "device: enum value expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isInteger(message.pin))
                        return "pin: integer expected";
                return null;
            };

            /**
             * Creates a GetDioPinRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.GetDioPinRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.GetDioPinRequest} GetDioPinRequest
             */
            GetDioPinRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.GetDioPinRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.GetDioPinRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.GetDioPinRequest();
                if (object.device !== 0 && (typeof object.device !== "string" || $root.lebai.io.IoDevice[object.device] !== 0))
                    switch (object.device) {
                    case "ROBOT":
                    case 0:
                        message.device = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.device = 1;
                        break;
                    case "EXTRA":
                    case 2:
                        message.device = 2;
                        break;
                    case "ROBOT_BTN":
                    case 10:
                        message.device = 10;
                        break;
                    case "SHOULDER":
                    case 11:
                        message.device = 11;
                        break;
                    case "FLANGE_BTN":
                    case 12:
                        message.device = 12;
                        break;
                    default:
                        if (typeof object.device === "number" && (object.device | 0) === object.device)
                            message.device = object.device;
                    }
                if (object.pin != null)
                    if ($Number(object.pin) !== 0)
                        message.pin = object.pin >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a GetDioPinRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.GetDioPinRequest
             * @static
             * @param {lebai.io.GetDioPinRequest} message GetDioPinRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetDioPinRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = options.enums === $String ? "ROBOT" : 0;
                    object.pin = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = options.enums === $String ? $root.lebai.io.IoDevice[message.device] === $undefined ? message.device : $root.lebai.io.IoDevice[message.device] : message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                return object;
            };

            /**
             * Converts this GetDioPinRequest to JSON.
             * @function toJSON
             * @memberof lebai.io.GetDioPinRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetDioPinRequest.prototype.toJSON = function() {
                return GetDioPinRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetDioPinRequest
             * @function getTypeUrl
             * @memberof lebai.io.GetDioPinRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetDioPinRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.GetDioPinRequest";
            };

            return GetDioPinRequest;
        })();

        io.GetDioPinResponse = (function() {

            /**
             * Properties of a GetDioPinResponse.
             * @typedef {Object} lebai.io.GetDioPinResponse.$Properties
             * @property {number|null} [value] GetDioPinResponse value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetDioPinResponse.
             * @memberof lebai.io
             * @interface IGetDioPinResponse
             * @augments lebai.io.GetDioPinResponse.$Properties
             * @deprecated Use lebai.io.GetDioPinResponse.$Properties instead.
             */

            /**
             * Shape of a GetDioPinResponse.
             * @typedef {lebai.io.GetDioPinResponse.$Properties} lebai.io.GetDioPinResponse.$Shape
             */

            /**
             * Constructs a new GetDioPinResponse.
             * @memberof lebai.io
             * @classdesc Represents a GetDioPinResponse.
             * @constructor
             * @param {lebai.io.GetDioPinResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetDioPinResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetDioPinResponse value.
             * @member {number} value
             * @memberof lebai.io.GetDioPinResponse
             * @instance
             */
            GetDioPinResponse.prototype.value = 0;

            /**
             * Creates a new GetDioPinResponse instance using the specified properties.
             * @function create
             * @memberof lebai.io.GetDioPinResponse
             * @static
             * @param {lebai.io.GetDioPinResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.io.GetDioPinResponse} GetDioPinResponse instance
             * @type {{
             *   (properties: lebai.io.GetDioPinResponse.$Shape): lebai.io.GetDioPinResponse & lebai.io.GetDioPinResponse.$Shape;
             *   (properties?: lebai.io.GetDioPinResponse.$Properties): lebai.io.GetDioPinResponse;
             * }}
             */
            GetDioPinResponse.create = function(properties) {
                return new GetDioPinResponse(properties);
            };

            /**
             * Encodes the specified GetDioPinResponse message. Does not implicitly {@link lebai.io.GetDioPinResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.GetDioPinResponse
             * @static
             * @param {lebai.io.GetDioPinResponse.$Properties} message GetDioPinResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDioPinResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetDioPinResponse message, length delimited. Does not implicitly {@link lebai.io.GetDioPinResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.GetDioPinResponse
             * @static
             * @param {lebai.io.GetDioPinResponse.$Properties} message GetDioPinResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDioPinResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetDioPinResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.GetDioPinResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.GetDioPinResponse & lebai.io.GetDioPinResponse.$Shape} GetDioPinResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDioPinResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.GetDioPinResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetDioPinResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.GetDioPinResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.GetDioPinResponse & lebai.io.GetDioPinResponse.$Shape} GetDioPinResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDioPinResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetDioPinResponse message.
             * @function verify
             * @memberof lebai.io.GetDioPinResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetDioPinResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                return null;
            };

            /**
             * Creates a GetDioPinResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.GetDioPinResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.GetDioPinResponse} GetDioPinResponse
             */
            GetDioPinResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.GetDioPinResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.GetDioPinResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.GetDioPinResponse();
                if (object.value != null)
                    if ($Number(object.value) !== 0)
                        message.value = object.value >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a GetDioPinResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.GetDioPinResponse
             * @static
             * @param {lebai.io.GetDioPinResponse} message GetDioPinResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetDioPinResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.value = 0;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this GetDioPinResponse to JSON.
             * @function toJSON
             * @memberof lebai.io.GetDioPinResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetDioPinResponse.prototype.toJSON = function() {
                return GetDioPinResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetDioPinResponse
             * @function getTypeUrl
             * @memberof lebai.io.GetDioPinResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetDioPinResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.GetDioPinResponse";
            };

            return GetDioPinResponse;
        })();

        io.GetDioPinsRequest = (function() {

            /**
             * Properties of a GetDioPinsRequest.
             * @typedef {Object} lebai.io.GetDioPinsRequest.$Properties
             * @property {lebai.io.IoDevice|null} [device] GetDioPinsRequest device
             * @property {number|null} [pin] GetDioPinsRequest pin
             * @property {number|null} [count] GetDioPinsRequest count
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetDioPinsRequest.
             * @memberof lebai.io
             * @interface IGetDioPinsRequest
             * @augments lebai.io.GetDioPinsRequest.$Properties
             * @deprecated Use lebai.io.GetDioPinsRequest.$Properties instead.
             */

            /**
             * Shape of a GetDioPinsRequest.
             * @typedef {lebai.io.GetDioPinsRequest.$Properties} lebai.io.GetDioPinsRequest.$Shape
             */

            /**
             * Constructs a new GetDioPinsRequest.
             * @memberof lebai.io
             * @classdesc Represents a GetDioPinsRequest.
             * @constructor
             * @param {lebai.io.GetDioPinsRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetDioPinsRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetDioPinsRequest device.
             * @member {lebai.io.IoDevice} device
             * @memberof lebai.io.GetDioPinsRequest
             * @instance
             */
            GetDioPinsRequest.prototype.device = 0;

            /**
             * GetDioPinsRequest pin.
             * @member {number} pin
             * @memberof lebai.io.GetDioPinsRequest
             * @instance
             */
            GetDioPinsRequest.prototype.pin = 0;

            /**
             * GetDioPinsRequest count.
             * @member {number} count
             * @memberof lebai.io.GetDioPinsRequest
             * @instance
             */
            GetDioPinsRequest.prototype.count = 0;

            /**
             * Creates a new GetDioPinsRequest instance using the specified properties.
             * @function create
             * @memberof lebai.io.GetDioPinsRequest
             * @static
             * @param {lebai.io.GetDioPinsRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.io.GetDioPinsRequest} GetDioPinsRequest instance
             * @type {{
             *   (properties: lebai.io.GetDioPinsRequest.$Shape): lebai.io.GetDioPinsRequest & lebai.io.GetDioPinsRequest.$Shape;
             *   (properties?: lebai.io.GetDioPinsRequest.$Properties): lebai.io.GetDioPinsRequest;
             * }}
             */
            GetDioPinsRequest.create = function(properties) {
                return new GetDioPinsRequest(properties);
            };

            /**
             * Encodes the specified GetDioPinsRequest message. Does not implicitly {@link lebai.io.GetDioPinsRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.GetDioPinsRequest
             * @static
             * @param {lebai.io.GetDioPinsRequest.$Properties} message GetDioPinsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDioPinsRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.pin);
                if (message.count != null && $Object.hasOwnProperty.call(message, "count") && message.count !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.count);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetDioPinsRequest message, length delimited. Does not implicitly {@link lebai.io.GetDioPinsRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.GetDioPinsRequest
             * @static
             * @param {lebai.io.GetDioPinsRequest.$Properties} message GetDioPinsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDioPinsRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetDioPinsRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.GetDioPinsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.GetDioPinsRequest & lebai.io.GetDioPinsRequest.$Shape} GetDioPinsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDioPinsRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.GetDioPinsRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetDioPinsRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.GetDioPinsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.GetDioPinsRequest & lebai.io.GetDioPinsRequest.$Shape} GetDioPinsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDioPinsRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetDioPinsRequest message.
             * @function verify
             * @memberof lebai.io.GetDioPinsRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetDioPinsRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (typeof message.device !== "number" || (message.device | 0) !== message.device)
                        return "device: enum value expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isInteger(message.pin))
                        return "pin: integer expected";
                if (message.count != null && $Object.hasOwnProperty.call(message, "count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            /**
             * Creates a GetDioPinsRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.GetDioPinsRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.GetDioPinsRequest} GetDioPinsRequest
             */
            GetDioPinsRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.GetDioPinsRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.GetDioPinsRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.GetDioPinsRequest();
                if (object.device !== 0 && (typeof object.device !== "string" || $root.lebai.io.IoDevice[object.device] !== 0))
                    switch (object.device) {
                    case "ROBOT":
                    case 0:
                        message.device = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.device = 1;
                        break;
                    case "EXTRA":
                    case 2:
                        message.device = 2;
                        break;
                    case "ROBOT_BTN":
                    case 10:
                        message.device = 10;
                        break;
                    case "SHOULDER":
                    case 11:
                        message.device = 11;
                        break;
                    case "FLANGE_BTN":
                    case 12:
                        message.device = 12;
                        break;
                    default:
                        if (typeof object.device === "number" && (object.device | 0) === object.device)
                            message.device = object.device;
                    }
                if (object.pin != null)
                    if ($Number(object.pin) !== 0)
                        message.pin = object.pin >>> 0;
                if (object.count != null)
                    if ($Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a GetDioPinsRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.GetDioPinsRequest
             * @static
             * @param {lebai.io.GetDioPinsRequest} message GetDioPinsRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetDioPinsRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = options.enums === $String ? "ROBOT" : 0;
                    object.pin = 0;
                    object.count = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = options.enums === $String ? $root.lebai.io.IoDevice[message.device] === $undefined ? message.device : $root.lebai.io.IoDevice[message.device] : message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.count != null && $Object.hasOwnProperty.call(message, "count"))
                    object.count = message.count;
                return object;
            };

            /**
             * Converts this GetDioPinsRequest to JSON.
             * @function toJSON
             * @memberof lebai.io.GetDioPinsRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetDioPinsRequest.prototype.toJSON = function() {
                return GetDioPinsRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetDioPinsRequest
             * @function getTypeUrl
             * @memberof lebai.io.GetDioPinsRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetDioPinsRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.GetDioPinsRequest";
            };

            return GetDioPinsRequest;
        })();

        io.GetDioPinsResponse = (function() {

            /**
             * Properties of a GetDioPinsResponse.
             * @typedef {Object} lebai.io.GetDioPinsResponse.$Properties
             * @property {Array.<number>|null} [values] GetDioPinsResponse values
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetDioPinsResponse.
             * @memberof lebai.io
             * @interface IGetDioPinsResponse
             * @augments lebai.io.GetDioPinsResponse.$Properties
             * @deprecated Use lebai.io.GetDioPinsResponse.$Properties instead.
             */

            /**
             * Shape of a GetDioPinsResponse.
             * @typedef {lebai.io.GetDioPinsResponse.$Properties} lebai.io.GetDioPinsResponse.$Shape
             */

            /**
             * Constructs a new GetDioPinsResponse.
             * @memberof lebai.io
             * @classdesc Represents a GetDioPinsResponse.
             * @constructor
             * @param {lebai.io.GetDioPinsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetDioPinsResponse = function (properties) {
                this.values = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetDioPinsResponse values.
             * @member {Array.<number>} values
             * @memberof lebai.io.GetDioPinsResponse
             * @instance
             */
            GetDioPinsResponse.prototype.values = $util.emptyArray;

            /**
             * Creates a new GetDioPinsResponse instance using the specified properties.
             * @function create
             * @memberof lebai.io.GetDioPinsResponse
             * @static
             * @param {lebai.io.GetDioPinsResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.io.GetDioPinsResponse} GetDioPinsResponse instance
             * @type {{
             *   (properties: lebai.io.GetDioPinsResponse.$Shape): lebai.io.GetDioPinsResponse & lebai.io.GetDioPinsResponse.$Shape;
             *   (properties?: lebai.io.GetDioPinsResponse.$Properties): lebai.io.GetDioPinsResponse;
             * }}
             */
            GetDioPinsResponse.create = function(properties) {
                return new GetDioPinsResponse(properties);
            };

            /**
             * Encodes the specified GetDioPinsResponse message. Does not implicitly {@link lebai.io.GetDioPinsResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.GetDioPinsResponse
             * @static
             * @param {lebai.io.GetDioPinsResponse.$Properties} message GetDioPinsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDioPinsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.values != null && message.values.length)
                    writer.uint32(/* id 11, wireType 2 =*/90).uint32s(message.values);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetDioPinsResponse message, length delimited. Does not implicitly {@link lebai.io.GetDioPinsResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.GetDioPinsResponse
             * @static
             * @param {lebai.io.GetDioPinsResponse.$Properties} message GetDioPinsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDioPinsResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetDioPinsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.GetDioPinsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.GetDioPinsResponse & lebai.io.GetDioPinsResponse.$Shape} GetDioPinsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDioPinsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.GetDioPinsResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.values && message.values.length))
                                    message.values = [];
                                reader.uint32s(message.values);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetDioPinsResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.GetDioPinsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.GetDioPinsResponse & lebai.io.GetDioPinsResponse.$Shape} GetDioPinsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDioPinsResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetDioPinsResponse message.
             * @function verify
             * @memberof lebai.io.GetDioPinsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetDioPinsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (var i = 0; i < message.values.length; ++i)
                        if (!$util.isInteger(message.values[i]))
                            return "values: integer[] expected";
                }
                return null;
            };

            /**
             * Creates a GetDioPinsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.GetDioPinsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.GetDioPinsResponse} GetDioPinsResponse
             */
            GetDioPinsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.GetDioPinsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.GetDioPinsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.GetDioPinsResponse();
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".lebai.io.GetDioPinsResponse.values: array expected");
                    message.values = $Array(object.values.length);
                    for (var i = 0; i < object.values.length; ++i)
                        message.values[i] = object.values[i] >>> 0;
                }
                return message;
            };

            /**
             * Creates a plain object from a GetDioPinsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.GetDioPinsResponse
             * @static
             * @param {lebai.io.GetDioPinsResponse} message GetDioPinsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetDioPinsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (var j = 0; j < message.values.length; ++j)
                        object.values[j] = message.values[j];
                }
                return object;
            };

            /**
             * Converts this GetDioPinsResponse to JSON.
             * @function toJSON
             * @memberof lebai.io.GetDioPinsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetDioPinsResponse.prototype.toJSON = function() {
                return GetDioPinsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetDioPinsResponse
             * @function getTypeUrl
             * @memberof lebai.io.GetDioPinsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetDioPinsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.GetDioPinsResponse";
            };

            return GetDioPinsResponse;
        })();

        io.SetAoPinRequest = (function() {

            /**
             * Properties of a SetAoPinRequest.
             * @typedef {Object} lebai.io.SetAoPinRequest.$Properties
             * @property {lebai.io.IoDevice|null} [device] SetAoPinRequest device
             * @property {number|null} [pin] SetAoPinRequest pin
             * @property {number|null} [value] SetAoPinRequest value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetAoPinRequest.
             * @memberof lebai.io
             * @interface ISetAoPinRequest
             * @augments lebai.io.SetAoPinRequest.$Properties
             * @deprecated Use lebai.io.SetAoPinRequest.$Properties instead.
             */

            /**
             * Shape of a SetAoPinRequest.
             * @typedef {lebai.io.SetAoPinRequest.$Properties} lebai.io.SetAoPinRequest.$Shape
             */

            /**
             * Constructs a new SetAoPinRequest.
             * @memberof lebai.io
             * @classdesc Represents a SetAoPinRequest.
             * @constructor
             * @param {lebai.io.SetAoPinRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetAoPinRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetAoPinRequest device.
             * @member {lebai.io.IoDevice} device
             * @memberof lebai.io.SetAoPinRequest
             * @instance
             */
            SetAoPinRequest.prototype.device = 0;

            /**
             * SetAoPinRequest pin.
             * @member {number} pin
             * @memberof lebai.io.SetAoPinRequest
             * @instance
             */
            SetAoPinRequest.prototype.pin = 0;

            /**
             * SetAoPinRequest value.
             * @member {number} value
             * @memberof lebai.io.SetAoPinRequest
             * @instance
             */
            SetAoPinRequest.prototype.value = 0;

            /**
             * Creates a new SetAoPinRequest instance using the specified properties.
             * @function create
             * @memberof lebai.io.SetAoPinRequest
             * @static
             * @param {lebai.io.SetAoPinRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.io.SetAoPinRequest} SetAoPinRequest instance
             * @type {{
             *   (properties: lebai.io.SetAoPinRequest.$Shape): lebai.io.SetAoPinRequest & lebai.io.SetAoPinRequest.$Shape;
             *   (properties?: lebai.io.SetAoPinRequest.$Properties): lebai.io.SetAoPinRequest;
             * }}
             */
            SetAoPinRequest.create = function(properties) {
                return new SetAoPinRequest(properties);
            };

            /**
             * Encodes the specified SetAoPinRequest message. Does not implicitly {@link lebai.io.SetAoPinRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.SetAoPinRequest
             * @static
             * @param {lebai.io.SetAoPinRequest.$Properties} message SetAoPinRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetAoPinRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.pin);
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && !$Object.is(message.value, 0))
                    writer.uint32(/* id 11, wireType 1 =*/89).double(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetAoPinRequest message, length delimited. Does not implicitly {@link lebai.io.SetAoPinRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.SetAoPinRequest
             * @static
             * @param {lebai.io.SetAoPinRequest.$Properties} message SetAoPinRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetAoPinRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetAoPinRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.SetAoPinRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.SetAoPinRequest & lebai.io.SetAoPinRequest.$Shape} SetAoPinRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetAoPinRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.SetAoPinRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetAoPinRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.SetAoPinRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.SetAoPinRequest & lebai.io.SetAoPinRequest.$Shape} SetAoPinRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetAoPinRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetAoPinRequest message.
             * @function verify
             * @memberof lebai.io.SetAoPinRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetAoPinRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (typeof message.device !== "number" || (message.device | 0) !== message.device)
                        return "device: enum value expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isInteger(message.pin))
                        return "pin: integer expected";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (typeof message.value !== "number")
                        return "value: number expected";
                return null;
            };

            /**
             * Creates a SetAoPinRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.SetAoPinRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.SetAoPinRequest} SetAoPinRequest
             */
            SetAoPinRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.SetAoPinRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.SetAoPinRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.SetAoPinRequest();
                if (object.device !== 0 && (typeof object.device !== "string" || $root.lebai.io.IoDevice[object.device] !== 0))
                    switch (object.device) {
                    case "ROBOT":
                    case 0:
                        message.device = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.device = 1;
                        break;
                    case "EXTRA":
                    case 2:
                        message.device = 2;
                        break;
                    case "ROBOT_BTN":
                    case 10:
                        message.device = 10;
                        break;
                    case "SHOULDER":
                    case 11:
                        message.device = 11;
                        break;
                    case "FLANGE_BTN":
                    case 12:
                        message.device = 12;
                        break;
                    default:
                        if (typeof object.device === "number" && (object.device | 0) === object.device)
                            message.device = object.device;
                    }
                if (object.pin != null)
                    if ($Number(object.pin) !== 0)
                        message.pin = object.pin >>> 0;
                if (object.value != null)
                    if (!$Object.is($Number(object.value), 0))
                        message.value = $Number(object.value);
                return message;
            };

            /**
             * Creates a plain object from a SetAoPinRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.SetAoPinRequest
             * @static
             * @param {lebai.io.SetAoPinRequest} message SetAoPinRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetAoPinRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = options.enums === $String ? "ROBOT" : 0;
                    object.pin = 0;
                    object.value = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = options.enums === $String ? $root.lebai.io.IoDevice[message.device] === $undefined ? message.device : $root.lebai.io.IoDevice[message.device] : message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = options.json && !$isFinite(message.value) ? $String(message.value) : message.value;
                return object;
            };

            /**
             * Converts this SetAoPinRequest to JSON.
             * @function toJSON
             * @memberof lebai.io.SetAoPinRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetAoPinRequest.prototype.toJSON = function() {
                return SetAoPinRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetAoPinRequest
             * @function getTypeUrl
             * @memberof lebai.io.SetAoPinRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetAoPinRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.SetAoPinRequest";
            };

            return SetAoPinRequest;
        })();

        io.SetAoPinsRequest = (function() {

            /**
             * Properties of a SetAoPinsRequest.
             * @typedef {Object} lebai.io.SetAoPinsRequest.$Properties
             * @property {lebai.io.IoDevice|null} [device] SetAoPinsRequest device
             * @property {number|null} [pin] SetAoPinsRequest pin
             * @property {Array.<number>|null} [values] SetAoPinsRequest values
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetAoPinsRequest.
             * @memberof lebai.io
             * @interface ISetAoPinsRequest
             * @augments lebai.io.SetAoPinsRequest.$Properties
             * @deprecated Use lebai.io.SetAoPinsRequest.$Properties instead.
             */

            /**
             * Shape of a SetAoPinsRequest.
             * @typedef {lebai.io.SetAoPinsRequest.$Properties} lebai.io.SetAoPinsRequest.$Shape
             */

            /**
             * Constructs a new SetAoPinsRequest.
             * @memberof lebai.io
             * @classdesc Represents a SetAoPinsRequest.
             * @constructor
             * @param {lebai.io.SetAoPinsRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetAoPinsRequest = function (properties) {
                this.values = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetAoPinsRequest device.
             * @member {lebai.io.IoDevice} device
             * @memberof lebai.io.SetAoPinsRequest
             * @instance
             */
            SetAoPinsRequest.prototype.device = 0;

            /**
             * SetAoPinsRequest pin.
             * @member {number} pin
             * @memberof lebai.io.SetAoPinsRequest
             * @instance
             */
            SetAoPinsRequest.prototype.pin = 0;

            /**
             * SetAoPinsRequest values.
             * @member {Array.<number>} values
             * @memberof lebai.io.SetAoPinsRequest
             * @instance
             */
            SetAoPinsRequest.prototype.values = $util.emptyArray;

            /**
             * Creates a new SetAoPinsRequest instance using the specified properties.
             * @function create
             * @memberof lebai.io.SetAoPinsRequest
             * @static
             * @param {lebai.io.SetAoPinsRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.io.SetAoPinsRequest} SetAoPinsRequest instance
             * @type {{
             *   (properties: lebai.io.SetAoPinsRequest.$Shape): lebai.io.SetAoPinsRequest & lebai.io.SetAoPinsRequest.$Shape;
             *   (properties?: lebai.io.SetAoPinsRequest.$Properties): lebai.io.SetAoPinsRequest;
             * }}
             */
            SetAoPinsRequest.create = function(properties) {
                return new SetAoPinsRequest(properties);
            };

            /**
             * Encodes the specified SetAoPinsRequest message. Does not implicitly {@link lebai.io.SetAoPinsRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.SetAoPinsRequest
             * @static
             * @param {lebai.io.SetAoPinsRequest.$Properties} message SetAoPinsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetAoPinsRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.pin);
                if (message.values != null && message.values.length)
                    writer.uint32(/* id 11, wireType 2 =*/90).doubles(message.values);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetAoPinsRequest message, length delimited. Does not implicitly {@link lebai.io.SetAoPinsRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.SetAoPinsRequest
             * @static
             * @param {lebai.io.SetAoPinsRequest.$Properties} message SetAoPinsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetAoPinsRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetAoPinsRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.SetAoPinsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.SetAoPinsRequest & lebai.io.SetAoPinsRequest.$Shape} SetAoPinsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetAoPinsRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.SetAoPinsRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.values && message.values.length))
                                    message.values = [];
                                reader.doubles(message.values);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push(reader.double());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetAoPinsRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.SetAoPinsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.SetAoPinsRequest & lebai.io.SetAoPinsRequest.$Shape} SetAoPinsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetAoPinsRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetAoPinsRequest message.
             * @function verify
             * @memberof lebai.io.SetAoPinsRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetAoPinsRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (typeof message.device !== "number" || (message.device | 0) !== message.device)
                        return "device: enum value expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isInteger(message.pin))
                        return "pin: integer expected";
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (var i = 0; i < message.values.length; ++i)
                        if (typeof message.values[i] !== "number")
                            return "values: number[] expected";
                }
                return null;
            };

            /**
             * Creates a SetAoPinsRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.SetAoPinsRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.SetAoPinsRequest} SetAoPinsRequest
             */
            SetAoPinsRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.SetAoPinsRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.SetAoPinsRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.SetAoPinsRequest();
                if (object.device !== 0 && (typeof object.device !== "string" || $root.lebai.io.IoDevice[object.device] !== 0))
                    switch (object.device) {
                    case "ROBOT":
                    case 0:
                        message.device = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.device = 1;
                        break;
                    case "EXTRA":
                    case 2:
                        message.device = 2;
                        break;
                    case "ROBOT_BTN":
                    case 10:
                        message.device = 10;
                        break;
                    case "SHOULDER":
                    case 11:
                        message.device = 11;
                        break;
                    case "FLANGE_BTN":
                    case 12:
                        message.device = 12;
                        break;
                    default:
                        if (typeof object.device === "number" && (object.device | 0) === object.device)
                            message.device = object.device;
                    }
                if (object.pin != null)
                    if ($Number(object.pin) !== 0)
                        message.pin = object.pin >>> 0;
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".lebai.io.SetAoPinsRequest.values: array expected");
                    message.values = $Array(object.values.length);
                    for (var i = 0; i < object.values.length; ++i)
                        message.values[i] = $Number(object.values[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a SetAoPinsRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.SetAoPinsRequest
             * @static
             * @param {lebai.io.SetAoPinsRequest} message SetAoPinsRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetAoPinsRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (options.defaults) {
                    object.device = options.enums === $String ? "ROBOT" : 0;
                    object.pin = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = options.enums === $String ? $root.lebai.io.IoDevice[message.device] === $undefined ? message.device : $root.lebai.io.IoDevice[message.device] : message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (var j = 0; j < message.values.length; ++j)
                        object.values[j] = options.json && !$isFinite(message.values[j]) ? $String(message.values[j]) : message.values[j];
                }
                return object;
            };

            /**
             * Converts this SetAoPinsRequest to JSON.
             * @function toJSON
             * @memberof lebai.io.SetAoPinsRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetAoPinsRequest.prototype.toJSON = function() {
                return SetAoPinsRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetAoPinsRequest
             * @function getTypeUrl
             * @memberof lebai.io.SetAoPinsRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetAoPinsRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.SetAoPinsRequest";
            };

            return SetAoPinsRequest;
        })();

        io.GetAioPinRequest = (function() {

            /**
             * Properties of a GetAioPinRequest.
             * @typedef {Object} lebai.io.GetAioPinRequest.$Properties
             * @property {lebai.io.IoDevice|null} [device] GetAioPinRequest device
             * @property {number|null} [pin] GetAioPinRequest pin
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetAioPinRequest.
             * @memberof lebai.io
             * @interface IGetAioPinRequest
             * @augments lebai.io.GetAioPinRequest.$Properties
             * @deprecated Use lebai.io.GetAioPinRequest.$Properties instead.
             */

            /**
             * Shape of a GetAioPinRequest.
             * @typedef {lebai.io.GetAioPinRequest.$Properties} lebai.io.GetAioPinRequest.$Shape
             */

            /**
             * Constructs a new GetAioPinRequest.
             * @memberof lebai.io
             * @classdesc Represents a GetAioPinRequest.
             * @constructor
             * @param {lebai.io.GetAioPinRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetAioPinRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetAioPinRequest device.
             * @member {lebai.io.IoDevice} device
             * @memberof lebai.io.GetAioPinRequest
             * @instance
             */
            GetAioPinRequest.prototype.device = 0;

            /**
             * GetAioPinRequest pin.
             * @member {number} pin
             * @memberof lebai.io.GetAioPinRequest
             * @instance
             */
            GetAioPinRequest.prototype.pin = 0;

            /**
             * Creates a new GetAioPinRequest instance using the specified properties.
             * @function create
             * @memberof lebai.io.GetAioPinRequest
             * @static
             * @param {lebai.io.GetAioPinRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.io.GetAioPinRequest} GetAioPinRequest instance
             * @type {{
             *   (properties: lebai.io.GetAioPinRequest.$Shape): lebai.io.GetAioPinRequest & lebai.io.GetAioPinRequest.$Shape;
             *   (properties?: lebai.io.GetAioPinRequest.$Properties): lebai.io.GetAioPinRequest;
             * }}
             */
            GetAioPinRequest.create = function(properties) {
                return new GetAioPinRequest(properties);
            };

            /**
             * Encodes the specified GetAioPinRequest message. Does not implicitly {@link lebai.io.GetAioPinRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.GetAioPinRequest
             * @static
             * @param {lebai.io.GetAioPinRequest.$Properties} message GetAioPinRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetAioPinRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.pin);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetAioPinRequest message, length delimited. Does not implicitly {@link lebai.io.GetAioPinRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.GetAioPinRequest
             * @static
             * @param {lebai.io.GetAioPinRequest.$Properties} message GetAioPinRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetAioPinRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetAioPinRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.GetAioPinRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.GetAioPinRequest & lebai.io.GetAioPinRequest.$Shape} GetAioPinRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetAioPinRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.GetAioPinRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetAioPinRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.GetAioPinRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.GetAioPinRequest & lebai.io.GetAioPinRequest.$Shape} GetAioPinRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetAioPinRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetAioPinRequest message.
             * @function verify
             * @memberof lebai.io.GetAioPinRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetAioPinRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (typeof message.device !== "number" || (message.device | 0) !== message.device)
                        return "device: enum value expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isInteger(message.pin))
                        return "pin: integer expected";
                return null;
            };

            /**
             * Creates a GetAioPinRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.GetAioPinRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.GetAioPinRequest} GetAioPinRequest
             */
            GetAioPinRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.GetAioPinRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.GetAioPinRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.GetAioPinRequest();
                if (object.device !== 0 && (typeof object.device !== "string" || $root.lebai.io.IoDevice[object.device] !== 0))
                    switch (object.device) {
                    case "ROBOT":
                    case 0:
                        message.device = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.device = 1;
                        break;
                    case "EXTRA":
                    case 2:
                        message.device = 2;
                        break;
                    case "ROBOT_BTN":
                    case 10:
                        message.device = 10;
                        break;
                    case "SHOULDER":
                    case 11:
                        message.device = 11;
                        break;
                    case "FLANGE_BTN":
                    case 12:
                        message.device = 12;
                        break;
                    default:
                        if (typeof object.device === "number" && (object.device | 0) === object.device)
                            message.device = object.device;
                    }
                if (object.pin != null)
                    if ($Number(object.pin) !== 0)
                        message.pin = object.pin >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a GetAioPinRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.GetAioPinRequest
             * @static
             * @param {lebai.io.GetAioPinRequest} message GetAioPinRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetAioPinRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = options.enums === $String ? "ROBOT" : 0;
                    object.pin = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = options.enums === $String ? $root.lebai.io.IoDevice[message.device] === $undefined ? message.device : $root.lebai.io.IoDevice[message.device] : message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                return object;
            };

            /**
             * Converts this GetAioPinRequest to JSON.
             * @function toJSON
             * @memberof lebai.io.GetAioPinRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetAioPinRequest.prototype.toJSON = function() {
                return GetAioPinRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetAioPinRequest
             * @function getTypeUrl
             * @memberof lebai.io.GetAioPinRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetAioPinRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.GetAioPinRequest";
            };

            return GetAioPinRequest;
        })();

        io.GetAioPinResponse = (function() {

            /**
             * Properties of a GetAioPinResponse.
             * @typedef {Object} lebai.io.GetAioPinResponse.$Properties
             * @property {number|null} [value] GetAioPinResponse value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetAioPinResponse.
             * @memberof lebai.io
             * @interface IGetAioPinResponse
             * @augments lebai.io.GetAioPinResponse.$Properties
             * @deprecated Use lebai.io.GetAioPinResponse.$Properties instead.
             */

            /**
             * Shape of a GetAioPinResponse.
             * @typedef {lebai.io.GetAioPinResponse.$Properties} lebai.io.GetAioPinResponse.$Shape
             */

            /**
             * Constructs a new GetAioPinResponse.
             * @memberof lebai.io
             * @classdesc Represents a GetAioPinResponse.
             * @constructor
             * @param {lebai.io.GetAioPinResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetAioPinResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetAioPinResponse value.
             * @member {number} value
             * @memberof lebai.io.GetAioPinResponse
             * @instance
             */
            GetAioPinResponse.prototype.value = 0;

            /**
             * Creates a new GetAioPinResponse instance using the specified properties.
             * @function create
             * @memberof lebai.io.GetAioPinResponse
             * @static
             * @param {lebai.io.GetAioPinResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.io.GetAioPinResponse} GetAioPinResponse instance
             * @type {{
             *   (properties: lebai.io.GetAioPinResponse.$Shape): lebai.io.GetAioPinResponse & lebai.io.GetAioPinResponse.$Shape;
             *   (properties?: lebai.io.GetAioPinResponse.$Properties): lebai.io.GetAioPinResponse;
             * }}
             */
            GetAioPinResponse.create = function(properties) {
                return new GetAioPinResponse(properties);
            };

            /**
             * Encodes the specified GetAioPinResponse message. Does not implicitly {@link lebai.io.GetAioPinResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.GetAioPinResponse
             * @static
             * @param {lebai.io.GetAioPinResponse.$Properties} message GetAioPinResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetAioPinResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && !$Object.is(message.value, 0))
                    writer.uint32(/* id 11, wireType 1 =*/89).double(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetAioPinResponse message, length delimited. Does not implicitly {@link lebai.io.GetAioPinResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.GetAioPinResponse
             * @static
             * @param {lebai.io.GetAioPinResponse.$Properties} message GetAioPinResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetAioPinResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetAioPinResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.GetAioPinResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.GetAioPinResponse & lebai.io.GetAioPinResponse.$Shape} GetAioPinResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetAioPinResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.GetAioPinResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetAioPinResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.GetAioPinResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.GetAioPinResponse & lebai.io.GetAioPinResponse.$Shape} GetAioPinResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetAioPinResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetAioPinResponse message.
             * @function verify
             * @memberof lebai.io.GetAioPinResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetAioPinResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (typeof message.value !== "number")
                        return "value: number expected";
                return null;
            };

            /**
             * Creates a GetAioPinResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.GetAioPinResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.GetAioPinResponse} GetAioPinResponse
             */
            GetAioPinResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.GetAioPinResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.GetAioPinResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.GetAioPinResponse();
                if (object.value != null)
                    if (!$Object.is($Number(object.value), 0))
                        message.value = $Number(object.value);
                return message;
            };

            /**
             * Creates a plain object from a GetAioPinResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.GetAioPinResponse
             * @static
             * @param {lebai.io.GetAioPinResponse} message GetAioPinResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetAioPinResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.value = 0;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = options.json && !$isFinite(message.value) ? $String(message.value) : message.value;
                return object;
            };

            /**
             * Converts this GetAioPinResponse to JSON.
             * @function toJSON
             * @memberof lebai.io.GetAioPinResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetAioPinResponse.prototype.toJSON = function() {
                return GetAioPinResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetAioPinResponse
             * @function getTypeUrl
             * @memberof lebai.io.GetAioPinResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetAioPinResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.GetAioPinResponse";
            };

            return GetAioPinResponse;
        })();

        io.GetAioPinsRequest = (function() {

            /**
             * Properties of a GetAioPinsRequest.
             * @typedef {Object} lebai.io.GetAioPinsRequest.$Properties
             * @property {lebai.io.IoDevice|null} [device] GetAioPinsRequest device
             * @property {number|null} [pin] GetAioPinsRequest pin
             * @property {number|null} [count] GetAioPinsRequest count
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetAioPinsRequest.
             * @memberof lebai.io
             * @interface IGetAioPinsRequest
             * @augments lebai.io.GetAioPinsRequest.$Properties
             * @deprecated Use lebai.io.GetAioPinsRequest.$Properties instead.
             */

            /**
             * Shape of a GetAioPinsRequest.
             * @typedef {lebai.io.GetAioPinsRequest.$Properties} lebai.io.GetAioPinsRequest.$Shape
             */

            /**
             * Constructs a new GetAioPinsRequest.
             * @memberof lebai.io
             * @classdesc Represents a GetAioPinsRequest.
             * @constructor
             * @param {lebai.io.GetAioPinsRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetAioPinsRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetAioPinsRequest device.
             * @member {lebai.io.IoDevice} device
             * @memberof lebai.io.GetAioPinsRequest
             * @instance
             */
            GetAioPinsRequest.prototype.device = 0;

            /**
             * GetAioPinsRequest pin.
             * @member {number} pin
             * @memberof lebai.io.GetAioPinsRequest
             * @instance
             */
            GetAioPinsRequest.prototype.pin = 0;

            /**
             * GetAioPinsRequest count.
             * @member {number} count
             * @memberof lebai.io.GetAioPinsRequest
             * @instance
             */
            GetAioPinsRequest.prototype.count = 0;

            /**
             * Creates a new GetAioPinsRequest instance using the specified properties.
             * @function create
             * @memberof lebai.io.GetAioPinsRequest
             * @static
             * @param {lebai.io.GetAioPinsRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.io.GetAioPinsRequest} GetAioPinsRequest instance
             * @type {{
             *   (properties: lebai.io.GetAioPinsRequest.$Shape): lebai.io.GetAioPinsRequest & lebai.io.GetAioPinsRequest.$Shape;
             *   (properties?: lebai.io.GetAioPinsRequest.$Properties): lebai.io.GetAioPinsRequest;
             * }}
             */
            GetAioPinsRequest.create = function(properties) {
                return new GetAioPinsRequest(properties);
            };

            /**
             * Encodes the specified GetAioPinsRequest message. Does not implicitly {@link lebai.io.GetAioPinsRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.GetAioPinsRequest
             * @static
             * @param {lebai.io.GetAioPinsRequest.$Properties} message GetAioPinsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetAioPinsRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.pin);
                if (message.count != null && $Object.hasOwnProperty.call(message, "count") && message.count !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.count);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetAioPinsRequest message, length delimited. Does not implicitly {@link lebai.io.GetAioPinsRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.GetAioPinsRequest
             * @static
             * @param {lebai.io.GetAioPinsRequest.$Properties} message GetAioPinsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetAioPinsRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetAioPinsRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.GetAioPinsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.GetAioPinsRequest & lebai.io.GetAioPinsRequest.$Shape} GetAioPinsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetAioPinsRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.GetAioPinsRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetAioPinsRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.GetAioPinsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.GetAioPinsRequest & lebai.io.GetAioPinsRequest.$Shape} GetAioPinsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetAioPinsRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetAioPinsRequest message.
             * @function verify
             * @memberof lebai.io.GetAioPinsRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetAioPinsRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (typeof message.device !== "number" || (message.device | 0) !== message.device)
                        return "device: enum value expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isInteger(message.pin))
                        return "pin: integer expected";
                if (message.count != null && $Object.hasOwnProperty.call(message, "count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            /**
             * Creates a GetAioPinsRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.GetAioPinsRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.GetAioPinsRequest} GetAioPinsRequest
             */
            GetAioPinsRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.GetAioPinsRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.GetAioPinsRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.GetAioPinsRequest();
                if (object.device !== 0 && (typeof object.device !== "string" || $root.lebai.io.IoDevice[object.device] !== 0))
                    switch (object.device) {
                    case "ROBOT":
                    case 0:
                        message.device = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.device = 1;
                        break;
                    case "EXTRA":
                    case 2:
                        message.device = 2;
                        break;
                    case "ROBOT_BTN":
                    case 10:
                        message.device = 10;
                        break;
                    case "SHOULDER":
                    case 11:
                        message.device = 11;
                        break;
                    case "FLANGE_BTN":
                    case 12:
                        message.device = 12;
                        break;
                    default:
                        if (typeof object.device === "number" && (object.device | 0) === object.device)
                            message.device = object.device;
                    }
                if (object.pin != null)
                    if ($Number(object.pin) !== 0)
                        message.pin = object.pin >>> 0;
                if (object.count != null)
                    if ($Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a GetAioPinsRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.GetAioPinsRequest
             * @static
             * @param {lebai.io.GetAioPinsRequest} message GetAioPinsRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetAioPinsRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = options.enums === $String ? "ROBOT" : 0;
                    object.pin = 0;
                    object.count = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = options.enums === $String ? $root.lebai.io.IoDevice[message.device] === $undefined ? message.device : $root.lebai.io.IoDevice[message.device] : message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.count != null && $Object.hasOwnProperty.call(message, "count"))
                    object.count = message.count;
                return object;
            };

            /**
             * Converts this GetAioPinsRequest to JSON.
             * @function toJSON
             * @memberof lebai.io.GetAioPinsRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetAioPinsRequest.prototype.toJSON = function() {
                return GetAioPinsRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetAioPinsRequest
             * @function getTypeUrl
             * @memberof lebai.io.GetAioPinsRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetAioPinsRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.GetAioPinsRequest";
            };

            return GetAioPinsRequest;
        })();

        io.GetAioPinsResponse = (function() {

            /**
             * Properties of a GetAioPinsResponse.
             * @typedef {Object} lebai.io.GetAioPinsResponse.$Properties
             * @property {Array.<number>|null} [values] GetAioPinsResponse values
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetAioPinsResponse.
             * @memberof lebai.io
             * @interface IGetAioPinsResponse
             * @augments lebai.io.GetAioPinsResponse.$Properties
             * @deprecated Use lebai.io.GetAioPinsResponse.$Properties instead.
             */

            /**
             * Shape of a GetAioPinsResponse.
             * @typedef {lebai.io.GetAioPinsResponse.$Properties} lebai.io.GetAioPinsResponse.$Shape
             */

            /**
             * Constructs a new GetAioPinsResponse.
             * @memberof lebai.io
             * @classdesc Represents a GetAioPinsResponse.
             * @constructor
             * @param {lebai.io.GetAioPinsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetAioPinsResponse = function (properties) {
                this.values = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetAioPinsResponse values.
             * @member {Array.<number>} values
             * @memberof lebai.io.GetAioPinsResponse
             * @instance
             */
            GetAioPinsResponse.prototype.values = $util.emptyArray;

            /**
             * Creates a new GetAioPinsResponse instance using the specified properties.
             * @function create
             * @memberof lebai.io.GetAioPinsResponse
             * @static
             * @param {lebai.io.GetAioPinsResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.io.GetAioPinsResponse} GetAioPinsResponse instance
             * @type {{
             *   (properties: lebai.io.GetAioPinsResponse.$Shape): lebai.io.GetAioPinsResponse & lebai.io.GetAioPinsResponse.$Shape;
             *   (properties?: lebai.io.GetAioPinsResponse.$Properties): lebai.io.GetAioPinsResponse;
             * }}
             */
            GetAioPinsResponse.create = function(properties) {
                return new GetAioPinsResponse(properties);
            };

            /**
             * Encodes the specified GetAioPinsResponse message. Does not implicitly {@link lebai.io.GetAioPinsResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.GetAioPinsResponse
             * @static
             * @param {lebai.io.GetAioPinsResponse.$Properties} message GetAioPinsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetAioPinsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.values != null && message.values.length)
                    writer.uint32(/* id 11, wireType 2 =*/90).doubles(message.values);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetAioPinsResponse message, length delimited. Does not implicitly {@link lebai.io.GetAioPinsResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.GetAioPinsResponse
             * @static
             * @param {lebai.io.GetAioPinsResponse.$Properties} message GetAioPinsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetAioPinsResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetAioPinsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.GetAioPinsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.GetAioPinsResponse & lebai.io.GetAioPinsResponse.$Shape} GetAioPinsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetAioPinsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.GetAioPinsResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.values && message.values.length))
                                    message.values = [];
                                reader.doubles(message.values);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push(reader.double());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetAioPinsResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.GetAioPinsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.GetAioPinsResponse & lebai.io.GetAioPinsResponse.$Shape} GetAioPinsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetAioPinsResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetAioPinsResponse message.
             * @function verify
             * @memberof lebai.io.GetAioPinsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetAioPinsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (var i = 0; i < message.values.length; ++i)
                        if (typeof message.values[i] !== "number")
                            return "values: number[] expected";
                }
                return null;
            };

            /**
             * Creates a GetAioPinsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.GetAioPinsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.GetAioPinsResponse} GetAioPinsResponse
             */
            GetAioPinsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.GetAioPinsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.GetAioPinsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.GetAioPinsResponse();
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".lebai.io.GetAioPinsResponse.values: array expected");
                    message.values = $Array(object.values.length);
                    for (var i = 0; i < object.values.length; ++i)
                        message.values[i] = $Number(object.values[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a GetAioPinsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.GetAioPinsResponse
             * @static
             * @param {lebai.io.GetAioPinsResponse} message GetAioPinsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetAioPinsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (var j = 0; j < message.values.length; ++j)
                        object.values[j] = options.json && !$isFinite(message.values[j]) ? $String(message.values[j]) : message.values[j];
                }
                return object;
            };

            /**
             * Converts this GetAioPinsResponse to JSON.
             * @function toJSON
             * @memberof lebai.io.GetAioPinsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetAioPinsResponse.prototype.toJSON = function() {
                return GetAioPinsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetAioPinsResponse
             * @function getTypeUrl
             * @memberof lebai.io.GetAioPinsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetAioPinsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.GetAioPinsResponse";
            };

            return GetAioPinsResponse;
        })();

        io.ButtonIndex = (function() {

            /**
             * Properties of a ButtonIndex.
             * @typedef {Object} lebai.io.ButtonIndex.$Properties
             * @property {lebai.io.IoDevice|null} [device] ButtonIndex device
             * @property {number|null} [pin] ButtonIndex pin
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ButtonIndex.
             * @memberof lebai.io
             * @interface IButtonIndex
             * @augments lebai.io.ButtonIndex.$Properties
             * @deprecated Use lebai.io.ButtonIndex.$Properties instead.
             */

            /**
             * Shape of a ButtonIndex.
             * @typedef {lebai.io.ButtonIndex.$Properties} lebai.io.ButtonIndex.$Shape
             */

            /**
             * Constructs a new ButtonIndex.
             * @memberof lebai.io
             * @classdesc Represents a ButtonIndex.
             * @constructor
             * @param {lebai.io.ButtonIndex.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ButtonIndex = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ButtonIndex device.
             * @member {lebai.io.IoDevice} device
             * @memberof lebai.io.ButtonIndex
             * @instance
             */
            ButtonIndex.prototype.device = 0;

            /**
             * ButtonIndex pin.
             * @member {number} pin
             * @memberof lebai.io.ButtonIndex
             * @instance
             */
            ButtonIndex.prototype.pin = 0;

            /**
             * Creates a new ButtonIndex instance using the specified properties.
             * @function create
             * @memberof lebai.io.ButtonIndex
             * @static
             * @param {lebai.io.ButtonIndex.$Properties=} [properties] Properties to set
             * @returns {lebai.io.ButtonIndex} ButtonIndex instance
             * @type {{
             *   (properties: lebai.io.ButtonIndex.$Shape): lebai.io.ButtonIndex & lebai.io.ButtonIndex.$Shape;
             *   (properties?: lebai.io.ButtonIndex.$Properties): lebai.io.ButtonIndex;
             * }}
             */
            ButtonIndex.create = function(properties) {
                return new ButtonIndex(properties);
            };

            /**
             * Encodes the specified ButtonIndex message. Does not implicitly {@link lebai.io.ButtonIndex.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.ButtonIndex
             * @static
             * @param {lebai.io.ButtonIndex.$Properties} message ButtonIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ButtonIndex.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.pin);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ButtonIndex message, length delimited. Does not implicitly {@link lebai.io.ButtonIndex.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.ButtonIndex
             * @static
             * @param {lebai.io.ButtonIndex.$Properties} message ButtonIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ButtonIndex.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ButtonIndex message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.ButtonIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.ButtonIndex & lebai.io.ButtonIndex.$Shape} ButtonIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ButtonIndex.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.ButtonIndex();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ButtonIndex message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.ButtonIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.ButtonIndex & lebai.io.ButtonIndex.$Shape} ButtonIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ButtonIndex.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ButtonIndex message.
             * @function verify
             * @memberof lebai.io.ButtonIndex
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ButtonIndex.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (typeof message.device !== "number" || (message.device | 0) !== message.device)
                        return "device: enum value expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isInteger(message.pin))
                        return "pin: integer expected";
                return null;
            };

            /**
             * Creates a ButtonIndex message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.ButtonIndex
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.ButtonIndex} ButtonIndex
             */
            ButtonIndex.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.ButtonIndex)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.ButtonIndex: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.ButtonIndex();
                if (object.device !== 0 && (typeof object.device !== "string" || $root.lebai.io.IoDevice[object.device] !== 0))
                    switch (object.device) {
                    case "ROBOT":
                    case 0:
                        message.device = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.device = 1;
                        break;
                    case "EXTRA":
                    case 2:
                        message.device = 2;
                        break;
                    case "ROBOT_BTN":
                    case 10:
                        message.device = 10;
                        break;
                    case "SHOULDER":
                    case 11:
                        message.device = 11;
                        break;
                    case "FLANGE_BTN":
                    case 12:
                        message.device = 12;
                        break;
                    default:
                        if (typeof object.device === "number" && (object.device | 0) === object.device)
                            message.device = object.device;
                    }
                if (object.pin != null)
                    if ($Number(object.pin) !== 0)
                        message.pin = object.pin >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a ButtonIndex message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.ButtonIndex
             * @static
             * @param {lebai.io.ButtonIndex} message ButtonIndex
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ButtonIndex.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = options.enums === $String ? "ROBOT" : 0;
                    object.pin = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = options.enums === $String ? $root.lebai.io.IoDevice[message.device] === $undefined ? message.device : $root.lebai.io.IoDevice[message.device] : message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                return object;
            };

            /**
             * Converts this ButtonIndex to JSON.
             * @function toJSON
             * @memberof lebai.io.ButtonIndex
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ButtonIndex.prototype.toJSON = function() {
                return ButtonIndex.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ButtonIndex
             * @function getTypeUrl
             * @memberof lebai.io.ButtonIndex
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ButtonIndex.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.ButtonIndex";
            };

            return ButtonIndex;
        })();

        /**
         * ButtonState enum.
         * @name lebai.io.ButtonState
         * @enum {number}
         * @property {number} EMPTY=0 EMPTY value
         * @property {number} UP=1 UP value
         * @property {number} LONG_DOWN=2 LONG_DOWN value
         * @property {number} CLICK=3 CLICK value
         */
        io.ButtonState = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "EMPTY"] = 0;
            values[valuesById[1] = "UP"] = 1;
            values[valuesById[2] = "LONG_DOWN"] = 2;
            values[valuesById[3] = "CLICK"] = 3;
            return values;
        })();

        io.ButtonStatus = (function() {

            /**
             * Properties of a ButtonStatus.
             * @typedef {Object} lebai.io.ButtonStatus.$Properties
             * @property {lebai.io.ButtonState|null} [state] ButtonStatus state
             * @property {number|null} [time] ButtonStatus time
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ButtonStatus.
             * @memberof lebai.io
             * @interface IButtonStatus
             * @augments lebai.io.ButtonStatus.$Properties
             * @deprecated Use lebai.io.ButtonStatus.$Properties instead.
             */

            /**
             * Shape of a ButtonStatus.
             * @typedef {lebai.io.ButtonStatus.$Properties} lebai.io.ButtonStatus.$Shape
             */

            /**
             * Constructs a new ButtonStatus.
             * @memberof lebai.io
             * @classdesc Represents a ButtonStatus.
             * @constructor
             * @param {lebai.io.ButtonStatus.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ButtonStatus = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ButtonStatus state.
             * @member {lebai.io.ButtonState} state
             * @memberof lebai.io.ButtonStatus
             * @instance
             */
            ButtonStatus.prototype.state = 0;

            /**
             * ButtonStatus time.
             * @member {number} time
             * @memberof lebai.io.ButtonStatus
             * @instance
             */
            ButtonStatus.prototype.time = 0;

            /**
             * Creates a new ButtonStatus instance using the specified properties.
             * @function create
             * @memberof lebai.io.ButtonStatus
             * @static
             * @param {lebai.io.ButtonStatus.$Properties=} [properties] Properties to set
             * @returns {lebai.io.ButtonStatus} ButtonStatus instance
             * @type {{
             *   (properties: lebai.io.ButtonStatus.$Shape): lebai.io.ButtonStatus & lebai.io.ButtonStatus.$Shape;
             *   (properties?: lebai.io.ButtonStatus.$Properties): lebai.io.ButtonStatus;
             * }}
             */
            ButtonStatus.create = function(properties) {
                return new ButtonStatus(properties);
            };

            /**
             * Encodes the specified ButtonStatus message. Does not implicitly {@link lebai.io.ButtonStatus.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.ButtonStatus
             * @static
             * @param {lebai.io.ButtonStatus.$Properties} message ButtonStatus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ButtonStatus.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.state != null && $Object.hasOwnProperty.call(message, "state") && message.state !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.state);
                if (message.time != null && $Object.hasOwnProperty.call(message, "time") && message.time !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.time);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ButtonStatus message, length delimited. Does not implicitly {@link lebai.io.ButtonStatus.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.ButtonStatus
             * @static
             * @param {lebai.io.ButtonStatus.$Properties} message ButtonStatus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ButtonStatus.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ButtonStatus message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.ButtonStatus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.ButtonStatus & lebai.io.ButtonStatus.$Shape} ButtonStatus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ButtonStatus.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.ButtonStatus();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.state = value;
                            else
                                delete message.state;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.time = value;
                            else
                                delete message.time;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ButtonStatus message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.ButtonStatus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.ButtonStatus & lebai.io.ButtonStatus.$Shape} ButtonStatus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ButtonStatus.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ButtonStatus message.
             * @function verify
             * @memberof lebai.io.ButtonStatus
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ButtonStatus.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                    if (typeof message.state !== "number" || (message.state | 0) !== message.state)
                        return "state: enum value expected";
                if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                    if (!$util.isInteger(message.time))
                        return "time: integer expected";
                return null;
            };

            /**
             * Creates a ButtonStatus message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.ButtonStatus
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.ButtonStatus} ButtonStatus
             */
            ButtonStatus.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.ButtonStatus)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.ButtonStatus: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.ButtonStatus();
                if (object.state !== 0 && (typeof object.state !== "string" || $root.lebai.io.ButtonState[object.state] !== 0))
                    switch (object.state) {
                    case "EMPTY":
                    case 0:
                        message.state = 0;
                        break;
                    case "UP":
                    case 1:
                        message.state = 1;
                        break;
                    case "LONG_DOWN":
                    case 2:
                        message.state = 2;
                        break;
                    case "CLICK":
                    case 3:
                        message.state = 3;
                        break;
                    default:
                        if (typeof object.state === "number" && (object.state | 0) === object.state)
                            message.state = object.state;
                    }
                if (object.time != null)
                    if ($Number(object.time) !== 0)
                        message.time = object.time >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a ButtonStatus message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.ButtonStatus
             * @static
             * @param {lebai.io.ButtonStatus} message ButtonStatus
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ButtonStatus.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.state = options.enums === $String ? "EMPTY" : 0;
                    object.time = 0;
                }
                if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                    object.state = options.enums === $String ? $root.lebai.io.ButtonState[message.state] === $undefined ? message.state : $root.lebai.io.ButtonState[message.state] : message.state;
                if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                    object.time = message.time;
                return object;
            };

            /**
             * Converts this ButtonStatus to JSON.
             * @function toJSON
             * @memberof lebai.io.ButtonStatus
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ButtonStatus.prototype.toJSON = function() {
                return ButtonStatus.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ButtonStatus
             * @function getTypeUrl
             * @memberof lebai.io.ButtonStatus
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ButtonStatus.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.ButtonStatus";
            };

            return ButtonStatus;
        })();

        io.ButtonsStatus = (function() {

            /**
             * Properties of a ButtonsStatus.
             * @typedef {Object} lebai.io.ButtonsStatus.$Properties
             * @property {lebai.io.ButtonIndex.$Properties|null} [button] ButtonsStatus button
             * @property {lebai.io.ButtonStatus.$Properties|null} [status] ButtonsStatus status
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ButtonsStatus.
             * @memberof lebai.io
             * @interface IButtonsStatus
             * @augments lebai.io.ButtonsStatus.$Properties
             * @deprecated Use lebai.io.ButtonsStatus.$Properties instead.
             */

            /**
             * Shape of a ButtonsStatus.
             * @typedef {lebai.io.ButtonsStatus.$Properties} lebai.io.ButtonsStatus.$Shape
             */

            /**
             * Constructs a new ButtonsStatus.
             * @memberof lebai.io
             * @classdesc Represents a ButtonsStatus.
             * @constructor
             * @param {lebai.io.ButtonsStatus.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ButtonsStatus = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ButtonsStatus button.
             * @member {lebai.io.ButtonIndex.$Properties|null|undefined} button
             * @memberof lebai.io.ButtonsStatus
             * @instance
             */
            ButtonsStatus.prototype.button = null;

            /**
             * ButtonsStatus status.
             * @member {lebai.io.ButtonStatus.$Properties|null|undefined} status
             * @memberof lebai.io.ButtonsStatus
             * @instance
             */
            ButtonsStatus.prototype.status = null;

            /**
             * Creates a new ButtonsStatus instance using the specified properties.
             * @function create
             * @memberof lebai.io.ButtonsStatus
             * @static
             * @param {lebai.io.ButtonsStatus.$Properties=} [properties] Properties to set
             * @returns {lebai.io.ButtonsStatus} ButtonsStatus instance
             * @type {{
             *   (properties: lebai.io.ButtonsStatus.$Shape): lebai.io.ButtonsStatus & lebai.io.ButtonsStatus.$Shape;
             *   (properties?: lebai.io.ButtonsStatus.$Properties): lebai.io.ButtonsStatus;
             * }}
             */
            ButtonsStatus.create = function(properties) {
                return new ButtonsStatus(properties);
            };

            /**
             * Encodes the specified ButtonsStatus message. Does not implicitly {@link lebai.io.ButtonsStatus.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.ButtonsStatus
             * @static
             * @param {lebai.io.ButtonsStatus.$Properties} message ButtonsStatus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ButtonsStatus.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.button != null && $Object.hasOwnProperty.call(message, "button"))
                    $root.lebai.io.ButtonIndex.encode(message.button, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    $root.lebai.io.ButtonStatus.encode(message.status, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ButtonsStatus message, length delimited. Does not implicitly {@link lebai.io.ButtonsStatus.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.ButtonsStatus
             * @static
             * @param {lebai.io.ButtonsStatus.$Properties} message ButtonsStatus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ButtonsStatus.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ButtonsStatus message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.ButtonsStatus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.ButtonsStatus & lebai.io.ButtonsStatus.$Shape} ButtonsStatus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ButtonsStatus.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.ButtonsStatus();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.button = $root.lebai.io.ButtonIndex.decode(reader, reader.uint32(), $undefined, _depth + 1, message.button);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.status = $root.lebai.io.ButtonStatus.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ButtonsStatus message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.ButtonsStatus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.ButtonsStatus & lebai.io.ButtonsStatus.$Shape} ButtonsStatus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ButtonsStatus.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ButtonsStatus message.
             * @function verify
             * @memberof lebai.io.ButtonsStatus
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ButtonsStatus.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.button != null && $Object.hasOwnProperty.call(message, "button")) {
                    var error = $root.lebai.io.ButtonIndex.verify(message.button, _depth + 1);
                    if (error)
                        return "button." + error;
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    var error = $root.lebai.io.ButtonStatus.verify(message.status, _depth + 1);
                    if (error)
                        return "status." + error;
                }
                return null;
            };

            /**
             * Creates a ButtonsStatus message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.ButtonsStatus
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.ButtonsStatus} ButtonsStatus
             */
            ButtonsStatus.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.ButtonsStatus)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.ButtonsStatus: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.ButtonsStatus();
                if (object.button != null) {
                    if (!$util.isObject(object.button))
                        throw $TypeError(".lebai.io.ButtonsStatus.button: object expected");
                    message.button = $root.lebai.io.ButtonIndex.fromObject(object.button, _depth + 1);
                }
                if (object.status != null) {
                    if (!$util.isObject(object.status))
                        throw $TypeError(".lebai.io.ButtonsStatus.status: object expected");
                    message.status = $root.lebai.io.ButtonStatus.fromObject(object.status, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a ButtonsStatus message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.ButtonsStatus
             * @static
             * @param {lebai.io.ButtonsStatus} message ButtonsStatus
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ButtonsStatus.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.button = null;
                    object.status = null;
                }
                if (message.button != null && $Object.hasOwnProperty.call(message, "button"))
                    object.button = $root.lebai.io.ButtonIndex.toObject(message.button, options, _depth + 1);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    object.status = $root.lebai.io.ButtonStatus.toObject(message.status, options, _depth + 1);
                return object;
            };

            /**
             * Converts this ButtonsStatus to JSON.
             * @function toJSON
             * @memberof lebai.io.ButtonsStatus
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ButtonsStatus.prototype.toJSON = function() {
                return ButtonsStatus.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ButtonsStatus
             * @function getTypeUrl
             * @memberof lebai.io.ButtonsStatus
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ButtonsStatus.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.ButtonsStatus";
            };

            return ButtonsStatus;
        })();

        io.SetDioModeRequest = (function() {

            /**
             * Properties of a SetDioModeRequest.
             * @typedef {Object} lebai.io.SetDioModeRequest.$Properties
             * @property {lebai.io.IoDevice|null} [device] SetDioModeRequest device
             * @property {number|null} [pin] SetDioModeRequest pin
             * @property {lebai.io.DigitalMode|null} [mode] SetDioModeRequest mode
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetDioModeRequest.
             * @memberof lebai.io
             * @interface ISetDioModeRequest
             * @augments lebai.io.SetDioModeRequest.$Properties
             * @deprecated Use lebai.io.SetDioModeRequest.$Properties instead.
             */

            /**
             * Shape of a SetDioModeRequest.
             * @typedef {lebai.io.SetDioModeRequest.$Properties} lebai.io.SetDioModeRequest.$Shape
             */

            /**
             * Constructs a new SetDioModeRequest.
             * @memberof lebai.io
             * @classdesc Represents a SetDioModeRequest.
             * @constructor
             * @param {lebai.io.SetDioModeRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetDioModeRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetDioModeRequest device.
             * @member {lebai.io.IoDevice} device
             * @memberof lebai.io.SetDioModeRequest
             * @instance
             */
            SetDioModeRequest.prototype.device = 0;

            /**
             * SetDioModeRequest pin.
             * @member {number} pin
             * @memberof lebai.io.SetDioModeRequest
             * @instance
             */
            SetDioModeRequest.prototype.pin = 0;

            /**
             * SetDioModeRequest mode.
             * @member {lebai.io.DigitalMode} mode
             * @memberof lebai.io.SetDioModeRequest
             * @instance
             */
            SetDioModeRequest.prototype.mode = 0;

            /**
             * Creates a new SetDioModeRequest instance using the specified properties.
             * @function create
             * @memberof lebai.io.SetDioModeRequest
             * @static
             * @param {lebai.io.SetDioModeRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.io.SetDioModeRequest} SetDioModeRequest instance
             * @type {{
             *   (properties: lebai.io.SetDioModeRequest.$Shape): lebai.io.SetDioModeRequest & lebai.io.SetDioModeRequest.$Shape;
             *   (properties?: lebai.io.SetDioModeRequest.$Properties): lebai.io.SetDioModeRequest;
             * }}
             */
            SetDioModeRequest.create = function(properties) {
                return new SetDioModeRequest(properties);
            };

            /**
             * Encodes the specified SetDioModeRequest message. Does not implicitly {@link lebai.io.SetDioModeRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.SetDioModeRequest
             * @static
             * @param {lebai.io.SetDioModeRequest.$Properties} message SetDioModeRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetDioModeRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.pin);
                if (message.mode != null && $Object.hasOwnProperty.call(message, "mode") && message.mode !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).int32(message.mode);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetDioModeRequest message, length delimited. Does not implicitly {@link lebai.io.SetDioModeRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.SetDioModeRequest
             * @static
             * @param {lebai.io.SetDioModeRequest.$Properties} message SetDioModeRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetDioModeRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetDioModeRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.SetDioModeRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.SetDioModeRequest & lebai.io.SetDioModeRequest.$Shape} SetDioModeRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetDioModeRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.SetDioModeRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.mode = value;
                            else
                                delete message.mode;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetDioModeRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.SetDioModeRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.SetDioModeRequest & lebai.io.SetDioModeRequest.$Shape} SetDioModeRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetDioModeRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetDioModeRequest message.
             * @function verify
             * @memberof lebai.io.SetDioModeRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetDioModeRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (typeof message.device !== "number" || (message.device | 0) !== message.device)
                        return "device: enum value expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isInteger(message.pin))
                        return "pin: integer expected";
                if (message.mode != null && $Object.hasOwnProperty.call(message, "mode"))
                    if (typeof message.mode !== "number" || (message.mode | 0) !== message.mode)
                        return "mode: enum value expected";
                return null;
            };

            /**
             * Creates a SetDioModeRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.SetDioModeRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.SetDioModeRequest} SetDioModeRequest
             */
            SetDioModeRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.SetDioModeRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.SetDioModeRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.SetDioModeRequest();
                if (object.device !== 0 && (typeof object.device !== "string" || $root.lebai.io.IoDevice[object.device] !== 0))
                    switch (object.device) {
                    case "ROBOT":
                    case 0:
                        message.device = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.device = 1;
                        break;
                    case "EXTRA":
                    case 2:
                        message.device = 2;
                        break;
                    case "ROBOT_BTN":
                    case 10:
                        message.device = 10;
                        break;
                    case "SHOULDER":
                    case 11:
                        message.device = 11;
                        break;
                    case "FLANGE_BTN":
                    case 12:
                        message.device = 12;
                        break;
                    default:
                        if (typeof object.device === "number" && (object.device | 0) === object.device)
                            message.device = object.device;
                    }
                if (object.pin != null)
                    if ($Number(object.pin) !== 0)
                        message.pin = object.pin >>> 0;
                if (object.mode !== 0 && (typeof object.mode !== "string" || $root.lebai.io.DigitalMode[object.mode] !== 0))
                    switch (object.mode) {
                    case "INPUT":
                    case 0:
                        message.mode = 0;
                        break;
                    case "OUTPUT":
                    case 1:
                        message.mode = 1;
                        break;
                    default:
                        if (typeof object.mode === "number" && (object.mode | 0) === object.mode)
                            message.mode = object.mode;
                    }
                return message;
            };

            /**
             * Creates a plain object from a SetDioModeRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.SetDioModeRequest
             * @static
             * @param {lebai.io.SetDioModeRequest} message SetDioModeRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetDioModeRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = options.enums === $String ? "ROBOT" : 0;
                    object.pin = 0;
                    object.mode = options.enums === $String ? "INPUT" : 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = options.enums === $String ? $root.lebai.io.IoDevice[message.device] === $undefined ? message.device : $root.lebai.io.IoDevice[message.device] : message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.mode != null && $Object.hasOwnProperty.call(message, "mode"))
                    object.mode = options.enums === $String ? $root.lebai.io.DigitalMode[message.mode] === $undefined ? message.mode : $root.lebai.io.DigitalMode[message.mode] : message.mode;
                return object;
            };

            /**
             * Converts this SetDioModeRequest to JSON.
             * @function toJSON
             * @memberof lebai.io.SetDioModeRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetDioModeRequest.prototype.toJSON = function() {
                return SetDioModeRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetDioModeRequest
             * @function getTypeUrl
             * @memberof lebai.io.SetDioModeRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetDioModeRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.SetDioModeRequest";
            };

            return SetDioModeRequest;
        })();

        io.GetDioModeRequest = (function() {

            /**
             * Properties of a GetDioModeRequest.
             * @typedef {Object} lebai.io.GetDioModeRequest.$Properties
             * @property {lebai.io.IoDevice|null} [device] GetDioModeRequest device
             * @property {number|null} [pin] GetDioModeRequest pin
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetDioModeRequest.
             * @memberof lebai.io
             * @interface IGetDioModeRequest
             * @augments lebai.io.GetDioModeRequest.$Properties
             * @deprecated Use lebai.io.GetDioModeRequest.$Properties instead.
             */

            /**
             * Shape of a GetDioModeRequest.
             * @typedef {lebai.io.GetDioModeRequest.$Properties} lebai.io.GetDioModeRequest.$Shape
             */

            /**
             * Constructs a new GetDioModeRequest.
             * @memberof lebai.io
             * @classdesc Represents a GetDioModeRequest.
             * @constructor
             * @param {lebai.io.GetDioModeRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetDioModeRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetDioModeRequest device.
             * @member {lebai.io.IoDevice} device
             * @memberof lebai.io.GetDioModeRequest
             * @instance
             */
            GetDioModeRequest.prototype.device = 0;

            /**
             * GetDioModeRequest pin.
             * @member {number} pin
             * @memberof lebai.io.GetDioModeRequest
             * @instance
             */
            GetDioModeRequest.prototype.pin = 0;

            /**
             * Creates a new GetDioModeRequest instance using the specified properties.
             * @function create
             * @memberof lebai.io.GetDioModeRequest
             * @static
             * @param {lebai.io.GetDioModeRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.io.GetDioModeRequest} GetDioModeRequest instance
             * @type {{
             *   (properties: lebai.io.GetDioModeRequest.$Shape): lebai.io.GetDioModeRequest & lebai.io.GetDioModeRequest.$Shape;
             *   (properties?: lebai.io.GetDioModeRequest.$Properties): lebai.io.GetDioModeRequest;
             * }}
             */
            GetDioModeRequest.create = function(properties) {
                return new GetDioModeRequest(properties);
            };

            /**
             * Encodes the specified GetDioModeRequest message. Does not implicitly {@link lebai.io.GetDioModeRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.GetDioModeRequest
             * @static
             * @param {lebai.io.GetDioModeRequest.$Properties} message GetDioModeRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDioModeRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.pin);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetDioModeRequest message, length delimited. Does not implicitly {@link lebai.io.GetDioModeRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.GetDioModeRequest
             * @static
             * @param {lebai.io.GetDioModeRequest.$Properties} message GetDioModeRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDioModeRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetDioModeRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.GetDioModeRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.GetDioModeRequest & lebai.io.GetDioModeRequest.$Shape} GetDioModeRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDioModeRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.GetDioModeRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetDioModeRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.GetDioModeRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.GetDioModeRequest & lebai.io.GetDioModeRequest.$Shape} GetDioModeRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDioModeRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetDioModeRequest message.
             * @function verify
             * @memberof lebai.io.GetDioModeRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetDioModeRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (typeof message.device !== "number" || (message.device | 0) !== message.device)
                        return "device: enum value expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isInteger(message.pin))
                        return "pin: integer expected";
                return null;
            };

            /**
             * Creates a GetDioModeRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.GetDioModeRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.GetDioModeRequest} GetDioModeRequest
             */
            GetDioModeRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.GetDioModeRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.GetDioModeRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.GetDioModeRequest();
                if (object.device !== 0 && (typeof object.device !== "string" || $root.lebai.io.IoDevice[object.device] !== 0))
                    switch (object.device) {
                    case "ROBOT":
                    case 0:
                        message.device = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.device = 1;
                        break;
                    case "EXTRA":
                    case 2:
                        message.device = 2;
                        break;
                    case "ROBOT_BTN":
                    case 10:
                        message.device = 10;
                        break;
                    case "SHOULDER":
                    case 11:
                        message.device = 11;
                        break;
                    case "FLANGE_BTN":
                    case 12:
                        message.device = 12;
                        break;
                    default:
                        if (typeof object.device === "number" && (object.device | 0) === object.device)
                            message.device = object.device;
                    }
                if (object.pin != null)
                    if ($Number(object.pin) !== 0)
                        message.pin = object.pin >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a GetDioModeRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.GetDioModeRequest
             * @static
             * @param {lebai.io.GetDioModeRequest} message GetDioModeRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetDioModeRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = options.enums === $String ? "ROBOT" : 0;
                    object.pin = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = options.enums === $String ? $root.lebai.io.IoDevice[message.device] === $undefined ? message.device : $root.lebai.io.IoDevice[message.device] : message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                return object;
            };

            /**
             * Converts this GetDioModeRequest to JSON.
             * @function toJSON
             * @memberof lebai.io.GetDioModeRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetDioModeRequest.prototype.toJSON = function() {
                return GetDioModeRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetDioModeRequest
             * @function getTypeUrl
             * @memberof lebai.io.GetDioModeRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetDioModeRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.GetDioModeRequest";
            };

            return GetDioModeRequest;
        })();

        io.GetDioModeResponse = (function() {

            /**
             * Properties of a GetDioModeResponse.
             * @typedef {Object} lebai.io.GetDioModeResponse.$Properties
             * @property {lebai.io.DigitalMode|null} [mode] GetDioModeResponse mode
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetDioModeResponse.
             * @memberof lebai.io
             * @interface IGetDioModeResponse
             * @augments lebai.io.GetDioModeResponse.$Properties
             * @deprecated Use lebai.io.GetDioModeResponse.$Properties instead.
             */

            /**
             * Shape of a GetDioModeResponse.
             * @typedef {lebai.io.GetDioModeResponse.$Properties} lebai.io.GetDioModeResponse.$Shape
             */

            /**
             * Constructs a new GetDioModeResponse.
             * @memberof lebai.io
             * @classdesc Represents a GetDioModeResponse.
             * @constructor
             * @param {lebai.io.GetDioModeResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetDioModeResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetDioModeResponse mode.
             * @member {lebai.io.DigitalMode} mode
             * @memberof lebai.io.GetDioModeResponse
             * @instance
             */
            GetDioModeResponse.prototype.mode = 0;

            /**
             * Creates a new GetDioModeResponse instance using the specified properties.
             * @function create
             * @memberof lebai.io.GetDioModeResponse
             * @static
             * @param {lebai.io.GetDioModeResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.io.GetDioModeResponse} GetDioModeResponse instance
             * @type {{
             *   (properties: lebai.io.GetDioModeResponse.$Shape): lebai.io.GetDioModeResponse & lebai.io.GetDioModeResponse.$Shape;
             *   (properties?: lebai.io.GetDioModeResponse.$Properties): lebai.io.GetDioModeResponse;
             * }}
             */
            GetDioModeResponse.create = function(properties) {
                return new GetDioModeResponse(properties);
            };

            /**
             * Encodes the specified GetDioModeResponse message. Does not implicitly {@link lebai.io.GetDioModeResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.GetDioModeResponse
             * @static
             * @param {lebai.io.GetDioModeResponse.$Properties} message GetDioModeResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDioModeResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.mode != null && $Object.hasOwnProperty.call(message, "mode") && message.mode !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).int32(message.mode);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetDioModeResponse message, length delimited. Does not implicitly {@link lebai.io.GetDioModeResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.GetDioModeResponse
             * @static
             * @param {lebai.io.GetDioModeResponse.$Properties} message GetDioModeResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDioModeResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetDioModeResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.GetDioModeResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.GetDioModeResponse & lebai.io.GetDioModeResponse.$Shape} GetDioModeResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDioModeResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.GetDioModeResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.mode = value;
                            else
                                delete message.mode;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetDioModeResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.GetDioModeResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.GetDioModeResponse & lebai.io.GetDioModeResponse.$Shape} GetDioModeResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDioModeResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetDioModeResponse message.
             * @function verify
             * @memberof lebai.io.GetDioModeResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetDioModeResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.mode != null && $Object.hasOwnProperty.call(message, "mode"))
                    if (typeof message.mode !== "number" || (message.mode | 0) !== message.mode)
                        return "mode: enum value expected";
                return null;
            };

            /**
             * Creates a GetDioModeResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.GetDioModeResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.GetDioModeResponse} GetDioModeResponse
             */
            GetDioModeResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.GetDioModeResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.GetDioModeResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.GetDioModeResponse();
                if (object.mode !== 0 && (typeof object.mode !== "string" || $root.lebai.io.DigitalMode[object.mode] !== 0))
                    switch (object.mode) {
                    case "INPUT":
                    case 0:
                        message.mode = 0;
                        break;
                    case "OUTPUT":
                    case 1:
                        message.mode = 1;
                        break;
                    default:
                        if (typeof object.mode === "number" && (object.mode | 0) === object.mode)
                            message.mode = object.mode;
                    }
                return message;
            };

            /**
             * Creates a plain object from a GetDioModeResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.GetDioModeResponse
             * @static
             * @param {lebai.io.GetDioModeResponse} message GetDioModeResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetDioModeResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.mode = options.enums === $String ? "INPUT" : 0;
                if (message.mode != null && $Object.hasOwnProperty.call(message, "mode"))
                    object.mode = options.enums === $String ? $root.lebai.io.DigitalMode[message.mode] === $undefined ? message.mode : $root.lebai.io.DigitalMode[message.mode] : message.mode;
                return object;
            };

            /**
             * Converts this GetDioModeResponse to JSON.
             * @function toJSON
             * @memberof lebai.io.GetDioModeResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetDioModeResponse.prototype.toJSON = function() {
                return GetDioModeResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetDioModeResponse
             * @function getTypeUrl
             * @memberof lebai.io.GetDioModeResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetDioModeResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.GetDioModeResponse";
            };

            return GetDioModeResponse;
        })();

        io.GetDiosModeRequest = (function() {

            /**
             * Properties of a GetDiosModeRequest.
             * @typedef {Object} lebai.io.GetDiosModeRequest.$Properties
             * @property {lebai.io.IoDevice|null} [device] GetDiosModeRequest device
             * @property {number|null} [pin] GetDiosModeRequest pin
             * @property {number|null} [count] GetDiosModeRequest count
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetDiosModeRequest.
             * @memberof lebai.io
             * @interface IGetDiosModeRequest
             * @augments lebai.io.GetDiosModeRequest.$Properties
             * @deprecated Use lebai.io.GetDiosModeRequest.$Properties instead.
             */

            /**
             * Shape of a GetDiosModeRequest.
             * @typedef {lebai.io.GetDiosModeRequest.$Properties} lebai.io.GetDiosModeRequest.$Shape
             */

            /**
             * Constructs a new GetDiosModeRequest.
             * @memberof lebai.io
             * @classdesc Represents a GetDiosModeRequest.
             * @constructor
             * @param {lebai.io.GetDiosModeRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetDiosModeRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetDiosModeRequest device.
             * @member {lebai.io.IoDevice} device
             * @memberof lebai.io.GetDiosModeRequest
             * @instance
             */
            GetDiosModeRequest.prototype.device = 0;

            /**
             * GetDiosModeRequest pin.
             * @member {number} pin
             * @memberof lebai.io.GetDiosModeRequest
             * @instance
             */
            GetDiosModeRequest.prototype.pin = 0;

            /**
             * GetDiosModeRequest count.
             * @member {number} count
             * @memberof lebai.io.GetDiosModeRequest
             * @instance
             */
            GetDiosModeRequest.prototype.count = 0;

            /**
             * Creates a new GetDiosModeRequest instance using the specified properties.
             * @function create
             * @memberof lebai.io.GetDiosModeRequest
             * @static
             * @param {lebai.io.GetDiosModeRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.io.GetDiosModeRequest} GetDiosModeRequest instance
             * @type {{
             *   (properties: lebai.io.GetDiosModeRequest.$Shape): lebai.io.GetDiosModeRequest & lebai.io.GetDiosModeRequest.$Shape;
             *   (properties?: lebai.io.GetDiosModeRequest.$Properties): lebai.io.GetDiosModeRequest;
             * }}
             */
            GetDiosModeRequest.create = function(properties) {
                return new GetDiosModeRequest(properties);
            };

            /**
             * Encodes the specified GetDiosModeRequest message. Does not implicitly {@link lebai.io.GetDiosModeRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.GetDiosModeRequest
             * @static
             * @param {lebai.io.GetDiosModeRequest.$Properties} message GetDiosModeRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDiosModeRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.pin);
                if (message.count != null && $Object.hasOwnProperty.call(message, "count") && message.count !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.count);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetDiosModeRequest message, length delimited. Does not implicitly {@link lebai.io.GetDiosModeRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.GetDiosModeRequest
             * @static
             * @param {lebai.io.GetDiosModeRequest.$Properties} message GetDiosModeRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDiosModeRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetDiosModeRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.GetDiosModeRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.GetDiosModeRequest & lebai.io.GetDiosModeRequest.$Shape} GetDiosModeRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDiosModeRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.GetDiosModeRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetDiosModeRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.GetDiosModeRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.GetDiosModeRequest & lebai.io.GetDiosModeRequest.$Shape} GetDiosModeRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDiosModeRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetDiosModeRequest message.
             * @function verify
             * @memberof lebai.io.GetDiosModeRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetDiosModeRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (typeof message.device !== "number" || (message.device | 0) !== message.device)
                        return "device: enum value expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isInteger(message.pin))
                        return "pin: integer expected";
                if (message.count != null && $Object.hasOwnProperty.call(message, "count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            /**
             * Creates a GetDiosModeRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.GetDiosModeRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.GetDiosModeRequest} GetDiosModeRequest
             */
            GetDiosModeRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.GetDiosModeRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.GetDiosModeRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.GetDiosModeRequest();
                if (object.device !== 0 && (typeof object.device !== "string" || $root.lebai.io.IoDevice[object.device] !== 0))
                    switch (object.device) {
                    case "ROBOT":
                    case 0:
                        message.device = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.device = 1;
                        break;
                    case "EXTRA":
                    case 2:
                        message.device = 2;
                        break;
                    case "ROBOT_BTN":
                    case 10:
                        message.device = 10;
                        break;
                    case "SHOULDER":
                    case 11:
                        message.device = 11;
                        break;
                    case "FLANGE_BTN":
                    case 12:
                        message.device = 12;
                        break;
                    default:
                        if (typeof object.device === "number" && (object.device | 0) === object.device)
                            message.device = object.device;
                    }
                if (object.pin != null)
                    if ($Number(object.pin) !== 0)
                        message.pin = object.pin >>> 0;
                if (object.count != null)
                    if ($Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a GetDiosModeRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.GetDiosModeRequest
             * @static
             * @param {lebai.io.GetDiosModeRequest} message GetDiosModeRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetDiosModeRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = options.enums === $String ? "ROBOT" : 0;
                    object.pin = 0;
                    object.count = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = options.enums === $String ? $root.lebai.io.IoDevice[message.device] === $undefined ? message.device : $root.lebai.io.IoDevice[message.device] : message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.count != null && $Object.hasOwnProperty.call(message, "count"))
                    object.count = message.count;
                return object;
            };

            /**
             * Converts this GetDiosModeRequest to JSON.
             * @function toJSON
             * @memberof lebai.io.GetDiosModeRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetDiosModeRequest.prototype.toJSON = function() {
                return GetDiosModeRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetDiosModeRequest
             * @function getTypeUrl
             * @memberof lebai.io.GetDiosModeRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetDiosModeRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.GetDiosModeRequest";
            };

            return GetDiosModeRequest;
        })();

        io.GetDiosModeResponse = (function() {

            /**
             * Properties of a GetDiosModeResponse.
             * @typedef {Object} lebai.io.GetDiosModeResponse.$Properties
             * @property {Array.<lebai.io.DigitalMode>|null} [modes] GetDiosModeResponse modes
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetDiosModeResponse.
             * @memberof lebai.io
             * @interface IGetDiosModeResponse
             * @augments lebai.io.GetDiosModeResponse.$Properties
             * @deprecated Use lebai.io.GetDiosModeResponse.$Properties instead.
             */

            /**
             * Shape of a GetDiosModeResponse.
             * @typedef {lebai.io.GetDiosModeResponse.$Properties} lebai.io.GetDiosModeResponse.$Shape
             */

            /**
             * Constructs a new GetDiosModeResponse.
             * @memberof lebai.io
             * @classdesc Represents a GetDiosModeResponse.
             * @constructor
             * @param {lebai.io.GetDiosModeResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetDiosModeResponse = function (properties) {
                this.modes = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetDiosModeResponse modes.
             * @member {Array.<lebai.io.DigitalMode>} modes
             * @memberof lebai.io.GetDiosModeResponse
             * @instance
             */
            GetDiosModeResponse.prototype.modes = $util.emptyArray;

            /**
             * Creates a new GetDiosModeResponse instance using the specified properties.
             * @function create
             * @memberof lebai.io.GetDiosModeResponse
             * @static
             * @param {lebai.io.GetDiosModeResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.io.GetDiosModeResponse} GetDiosModeResponse instance
             * @type {{
             *   (properties: lebai.io.GetDiosModeResponse.$Shape): lebai.io.GetDiosModeResponse & lebai.io.GetDiosModeResponse.$Shape;
             *   (properties?: lebai.io.GetDiosModeResponse.$Properties): lebai.io.GetDiosModeResponse;
             * }}
             */
            GetDiosModeResponse.create = function(properties) {
                return new GetDiosModeResponse(properties);
            };

            /**
             * Encodes the specified GetDiosModeResponse message. Does not implicitly {@link lebai.io.GetDiosModeResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.io.GetDiosModeResponse
             * @static
             * @param {lebai.io.GetDiosModeResponse.$Properties} message GetDiosModeResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDiosModeResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.modes != null && message.modes.length)
                    writer.uint32(/* id 11, wireType 2 =*/90).int32s(message.modes);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetDiosModeResponse message, length delimited. Does not implicitly {@link lebai.io.GetDiosModeResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.io.GetDiosModeResponse
             * @static
             * @param {lebai.io.GetDiosModeResponse.$Properties} message GetDiosModeResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetDiosModeResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetDiosModeResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.io.GetDiosModeResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.io.GetDiosModeResponse & lebai.io.GetDiosModeResponse.$Shape} GetDiosModeResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDiosModeResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.io.GetDiosModeResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.modes && message.modes.length))
                                    message.modes = [];
                                reader.int32s(message.modes);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.modes && message.modes.length))
                                message.modes = [];
                            message.modes.push(reader.int32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetDiosModeResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.io.GetDiosModeResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.io.GetDiosModeResponse & lebai.io.GetDiosModeResponse.$Shape} GetDiosModeResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetDiosModeResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetDiosModeResponse message.
             * @function verify
             * @memberof lebai.io.GetDiosModeResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetDiosModeResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.modes != null && $Object.hasOwnProperty.call(message, "modes")) {
                    if (!$Array.isArray(message.modes))
                        return "modes: array expected";
                    for (var i = 0; i < message.modes.length; ++i)
                        if (typeof message.modes[i] !== "number" || (message.modes[i] | 0) !== message.modes[i])
                            return "modes: enum value[] expected";
                }
                return null;
            };

            /**
             * Creates a GetDiosModeResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.io.GetDiosModeResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.io.GetDiosModeResponse} GetDiosModeResponse
             */
            GetDiosModeResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.io.GetDiosModeResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.io.GetDiosModeResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.io.GetDiosModeResponse();
                if (object.modes) {
                    if (!$Array.isArray(object.modes))
                        throw $TypeError(".lebai.io.GetDiosModeResponse.modes: array expected");
                    message.modes = [];
                    for (var i = 0; i < object.modes.length; ++i)
                        switch (object.modes[i]) {
                        case "INPUT":
                        case 0:
                            message.modes[message.modes.length] = 0;
                            break;
                        case "OUTPUT":
                        case 1:
                            message.modes[message.modes.length] = 1;
                            break;
                        default:
                            if (typeof object.modes[i] === "number" && (object.modes[i] | 0) === object.modes[i])
                                message.modes[message.modes.length] = object.modes[i];
                        }
                }
                return message;
            };

            /**
             * Creates a plain object from a GetDiosModeResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.io.GetDiosModeResponse
             * @static
             * @param {lebai.io.GetDiosModeResponse} message GetDiosModeResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetDiosModeResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.modes = [];
                if (message.modes && message.modes.length) {
                    object.modes = $Array(message.modes.length);
                    for (var j = 0; j < message.modes.length; ++j)
                        object.modes[j] = options.enums === $String ? $root.lebai.io.DigitalMode[message.modes[j]] === $undefined ? message.modes[j] : $root.lebai.io.DigitalMode[message.modes[j]] : message.modes[j];
                }
                return object;
            };

            /**
             * Converts this GetDiosModeResponse to JSON.
             * @function toJSON
             * @memberof lebai.io.GetDiosModeResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetDiosModeResponse.prototype.toJSON = function() {
                return GetDiosModeResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetDiosModeResponse
             * @function getTypeUrl
             * @memberof lebai.io.GetDiosModeResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetDiosModeResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.io.GetDiosModeResponse";
            };

            return GetDiosModeResponse;
        })();

        io.IoService = (function() {

            /**
             * Constructs a new IoService service.
             * @memberof lebai.io
             * @classdesc Represents an IoService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var IoService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(IoService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: IoService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new IoService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.io.IoService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {IoService} RPC service. Useful where requests and/or responses are streamed.
             */
            IoService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.io.IoService#getDi}.
             * @memberof lebai.io.IoService
             * @typedef GetDiCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.io.GetDioPinResponse} [response] GetDioPinResponse
             */

            /**
             * Calls GetDi.
             * @memberof lebai.io.IoService
             * @typedef GetDi
             * @type {{
             *   (request: lebai.io.IGetDioPinRequest, callback: lebai.io.IoService.GetDiCallback): void;
             *   (request: lebai.io.IGetDioPinRequest): Promise<lebai.io.GetDioPinResponse>;
             *   readonly name: "GetDi";
             *   readonly path: "/lebai.io.IoService/GetDi";
             *   readonly requestType: "GetDioPinRequest";
             *   readonly responseType: "GetDioPinResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetDi.
             * @name lebai.io.IoService#getDi
             * @type {lebai.io.IoService.GetDi}
             */
            $Object.defineProperties(IoService.prototype.getDi = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.getDi, $root.lebai.io.GetDioPinRequest, $root.lebai.io.GetDioPinResponse, request, callback);
            }, {
                name: { value: "GetDi" },
                path: { value: "/lebai.io.IoService/GetDi" },
                requestType: { value: "GetDioPinRequest" },
                responseType: { value: "GetDioPinResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#getDis}.
             * @memberof lebai.io.IoService
             * @typedef GetDisCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.io.GetDioPinsResponse} [response] GetDioPinsResponse
             */

            /**
             * Calls GetDis.
             * @memberof lebai.io.IoService
             * @typedef GetDis
             * @type {{
             *   (request: lebai.io.IGetDioPinsRequest, callback: lebai.io.IoService.GetDisCallback): void;
             *   (request: lebai.io.IGetDioPinsRequest): Promise<lebai.io.GetDioPinsResponse>;
             *   readonly name: "GetDis";
             *   readonly path: "/lebai.io.IoService/GetDis";
             *   readonly requestType: "GetDioPinsRequest";
             *   readonly responseType: "GetDioPinsResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetDis.
             * @name lebai.io.IoService#getDis
             * @type {lebai.io.IoService.GetDis}
             */
            $Object.defineProperties(IoService.prototype.getDis = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.getDis, $root.lebai.io.GetDioPinsRequest, $root.lebai.io.GetDioPinsResponse, request, callback);
            }, {
                name: { value: "GetDis" },
                path: { value: "/lebai.io.IoService/GetDis" },
                requestType: { value: "GetDioPinsRequest" },
                responseType: { value: "GetDioPinsResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#getDo}.
             * @memberof lebai.io.IoService
             * @typedef GetDoCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.io.GetDioPinResponse} [response] GetDioPinResponse
             */

            /**
             * Calls GetDo.
             * @memberof lebai.io.IoService
             * @typedef GetDo
             * @type {{
             *   (request: lebai.io.IGetDioPinRequest, callback: lebai.io.IoService.GetDoCallback): void;
             *   (request: lebai.io.IGetDioPinRequest): Promise<lebai.io.GetDioPinResponse>;
             *   readonly name: "GetDo";
             *   readonly path: "/lebai.io.IoService/GetDo";
             *   readonly requestType: "GetDioPinRequest";
             *   readonly responseType: "GetDioPinResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetDo.
             * @name lebai.io.IoService#getDo
             * @type {lebai.io.IoService.GetDo}
             */
            $Object.defineProperties(IoService.prototype.getDo = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.getDo, $root.lebai.io.GetDioPinRequest, $root.lebai.io.GetDioPinResponse, request, callback);
            }, {
                name: { value: "GetDo" },
                path: { value: "/lebai.io.IoService/GetDo" },
                requestType: { value: "GetDioPinRequest" },
                responseType: { value: "GetDioPinResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#getDos}.
             * @memberof lebai.io.IoService
             * @typedef GetDosCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.io.GetDioPinsResponse} [response] GetDioPinsResponse
             */

            /**
             * Calls GetDos.
             * @memberof lebai.io.IoService
             * @typedef GetDos
             * @type {{
             *   (request: lebai.io.IGetDioPinsRequest, callback: lebai.io.IoService.GetDosCallback): void;
             *   (request: lebai.io.IGetDioPinsRequest): Promise<lebai.io.GetDioPinsResponse>;
             *   readonly name: "GetDos";
             *   readonly path: "/lebai.io.IoService/GetDos";
             *   readonly requestType: "GetDioPinsRequest";
             *   readonly responseType: "GetDioPinsResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetDos.
             * @name lebai.io.IoService#getDos
             * @type {lebai.io.IoService.GetDos}
             */
            $Object.defineProperties(IoService.prototype.getDos = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.getDos, $root.lebai.io.GetDioPinsRequest, $root.lebai.io.GetDioPinsResponse, request, callback);
            }, {
                name: { value: "GetDos" },
                path: { value: "/lebai.io.IoService/GetDos" },
                requestType: { value: "GetDioPinsRequest" },
                responseType: { value: "GetDioPinsResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#setDo}.
             * @memberof lebai.io.IoService
             * @typedef SetDoCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetDo.
             * @memberof lebai.io.IoService
             * @typedef SetDo
             * @type {{
             *   (request: lebai.io.ISetDoPinRequest, callback: lebai.io.IoService.SetDoCallback): void;
             *   (request: lebai.io.ISetDoPinRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetDo";
             *   readonly path: "/lebai.io.IoService/SetDo";
             *   readonly requestType: "SetDoPinRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetDo.
             * @name lebai.io.IoService#setDo
             * @type {lebai.io.IoService.SetDo}
             */
            $Object.defineProperties(IoService.prototype.setDo = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.setDo, $root.lebai.io.SetDoPinRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetDo" },
                path: { value: "/lebai.io.IoService/SetDo" },
                requestType: { value: "SetDoPinRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#setDos}.
             * @memberof lebai.io.IoService
             * @typedef SetDosCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetDos.
             * @memberof lebai.io.IoService
             * @typedef SetDos
             * @type {{
             *   (request: lebai.io.ISetDoPinsRequest, callback: lebai.io.IoService.SetDosCallback): void;
             *   (request: lebai.io.ISetDoPinsRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetDos";
             *   readonly path: "/lebai.io.IoService/SetDos";
             *   readonly requestType: "SetDoPinsRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetDos.
             * @name lebai.io.IoService#setDos
             * @type {lebai.io.IoService.SetDos}
             */
            $Object.defineProperties(IoService.prototype.setDos = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.setDos, $root.lebai.io.SetDoPinsRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetDos" },
                path: { value: "/lebai.io.IoService/SetDos" },
                requestType: { value: "SetDoPinsRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#getAi}.
             * @memberof lebai.io.IoService
             * @typedef GetAiCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.io.GetAioPinResponse} [response] GetAioPinResponse
             */

            /**
             * Calls GetAi.
             * @memberof lebai.io.IoService
             * @typedef GetAi
             * @type {{
             *   (request: lebai.io.IGetAioPinRequest, callback: lebai.io.IoService.GetAiCallback): void;
             *   (request: lebai.io.IGetAioPinRequest): Promise<lebai.io.GetAioPinResponse>;
             *   readonly name: "GetAi";
             *   readonly path: "/lebai.io.IoService/GetAi";
             *   readonly requestType: "GetAioPinRequest";
             *   readonly responseType: "GetAioPinResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetAi.
             * @name lebai.io.IoService#getAi
             * @type {lebai.io.IoService.GetAi}
             */
            $Object.defineProperties(IoService.prototype.getAi = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.getAi, $root.lebai.io.GetAioPinRequest, $root.lebai.io.GetAioPinResponse, request, callback);
            }, {
                name: { value: "GetAi" },
                path: { value: "/lebai.io.IoService/GetAi" },
                requestType: { value: "GetAioPinRequest" },
                responseType: { value: "GetAioPinResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#getAis}.
             * @memberof lebai.io.IoService
             * @typedef GetAisCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.io.GetAioPinsResponse} [response] GetAioPinsResponse
             */

            /**
             * Calls GetAis.
             * @memberof lebai.io.IoService
             * @typedef GetAis
             * @type {{
             *   (request: lebai.io.IGetAioPinsRequest, callback: lebai.io.IoService.GetAisCallback): void;
             *   (request: lebai.io.IGetAioPinsRequest): Promise<lebai.io.GetAioPinsResponse>;
             *   readonly name: "GetAis";
             *   readonly path: "/lebai.io.IoService/GetAis";
             *   readonly requestType: "GetAioPinsRequest";
             *   readonly responseType: "GetAioPinsResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetAis.
             * @name lebai.io.IoService#getAis
             * @type {lebai.io.IoService.GetAis}
             */
            $Object.defineProperties(IoService.prototype.getAis = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.getAis, $root.lebai.io.GetAioPinsRequest, $root.lebai.io.GetAioPinsResponse, request, callback);
            }, {
                name: { value: "GetAis" },
                path: { value: "/lebai.io.IoService/GetAis" },
                requestType: { value: "GetAioPinsRequest" },
                responseType: { value: "GetAioPinsResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#getAo}.
             * @memberof lebai.io.IoService
             * @typedef GetAoCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.io.GetAioPinResponse} [response] GetAioPinResponse
             */

            /**
             * Calls GetAo.
             * @memberof lebai.io.IoService
             * @typedef GetAo
             * @type {{
             *   (request: lebai.io.IGetAioPinRequest, callback: lebai.io.IoService.GetAoCallback): void;
             *   (request: lebai.io.IGetAioPinRequest): Promise<lebai.io.GetAioPinResponse>;
             *   readonly name: "GetAo";
             *   readonly path: "/lebai.io.IoService/GetAo";
             *   readonly requestType: "GetAioPinRequest";
             *   readonly responseType: "GetAioPinResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetAo.
             * @name lebai.io.IoService#getAo
             * @type {lebai.io.IoService.GetAo}
             */
            $Object.defineProperties(IoService.prototype.getAo = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.getAo, $root.lebai.io.GetAioPinRequest, $root.lebai.io.GetAioPinResponse, request, callback);
            }, {
                name: { value: "GetAo" },
                path: { value: "/lebai.io.IoService/GetAo" },
                requestType: { value: "GetAioPinRequest" },
                responseType: { value: "GetAioPinResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#getAos}.
             * @memberof lebai.io.IoService
             * @typedef GetAosCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.io.GetAioPinsResponse} [response] GetAioPinsResponse
             */

            /**
             * Calls GetAos.
             * @memberof lebai.io.IoService
             * @typedef GetAos
             * @type {{
             *   (request: lebai.io.IGetAioPinsRequest, callback: lebai.io.IoService.GetAosCallback): void;
             *   (request: lebai.io.IGetAioPinsRequest): Promise<lebai.io.GetAioPinsResponse>;
             *   readonly name: "GetAos";
             *   readonly path: "/lebai.io.IoService/GetAos";
             *   readonly requestType: "GetAioPinsRequest";
             *   readonly responseType: "GetAioPinsResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetAos.
             * @name lebai.io.IoService#getAos
             * @type {lebai.io.IoService.GetAos}
             */
            $Object.defineProperties(IoService.prototype.getAos = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.getAos, $root.lebai.io.GetAioPinsRequest, $root.lebai.io.GetAioPinsResponse, request, callback);
            }, {
                name: { value: "GetAos" },
                path: { value: "/lebai.io.IoService/GetAos" },
                requestType: { value: "GetAioPinsRequest" },
                responseType: { value: "GetAioPinsResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#setAo}.
             * @memberof lebai.io.IoService
             * @typedef SetAoCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetAo.
             * @memberof lebai.io.IoService
             * @typedef SetAo
             * @type {{
             *   (request: lebai.io.ISetAoPinRequest, callback: lebai.io.IoService.SetAoCallback): void;
             *   (request: lebai.io.ISetAoPinRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetAo";
             *   readonly path: "/lebai.io.IoService/SetAo";
             *   readonly requestType: "SetAoPinRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetAo.
             * @name lebai.io.IoService#setAo
             * @type {lebai.io.IoService.SetAo}
             */
            $Object.defineProperties(IoService.prototype.setAo = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.setAo, $root.lebai.io.SetAoPinRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetAo" },
                path: { value: "/lebai.io.IoService/SetAo" },
                requestType: { value: "SetAoPinRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#setAos}.
             * @memberof lebai.io.IoService
             * @typedef SetAosCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetAos.
             * @memberof lebai.io.IoService
             * @typedef SetAos
             * @type {{
             *   (request: lebai.io.ISetAoPinsRequest, callback: lebai.io.IoService.SetAosCallback): void;
             *   (request: lebai.io.ISetAoPinsRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetAos";
             *   readonly path: "/lebai.io.IoService/SetAos";
             *   readonly requestType: "SetAoPinsRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetAos.
             * @name lebai.io.IoService#setAos
             * @type {lebai.io.IoService.SetAos}
             */
            $Object.defineProperties(IoService.prototype.setAos = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.setAos, $root.lebai.io.SetAoPinsRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetAos" },
                path: { value: "/lebai.io.IoService/SetAos" },
                requestType: { value: "SetAoPinsRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#enableButton}.
             * @memberof lebai.io.IoService
             * @typedef EnableButtonCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls EnableButton.
             * @memberof lebai.io.IoService
             * @typedef EnableButton
             * @type {{
             *   (request: lebai.io.IButtonIndex, callback: lebai.io.IoService.EnableButtonCallback): void;
             *   (request: lebai.io.IButtonIndex): Promise<google.protobuf.Empty>;
             *   readonly name: "EnableButton";
             *   readonly path: "/lebai.io.IoService/EnableButton";
             *   readonly requestType: "ButtonIndex";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls EnableButton.
             * @name lebai.io.IoService#enableButton
             * @type {lebai.io.IoService.EnableButton}
             */
            $Object.defineProperties(IoService.prototype.enableButton = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.enableButton, $root.lebai.io.ButtonIndex, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "EnableButton" },
                path: { value: "/lebai.io.IoService/EnableButton" },
                requestType: { value: "ButtonIndex" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#disableButton}.
             * @memberof lebai.io.IoService
             * @typedef DisableButtonCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls DisableButton.
             * @memberof lebai.io.IoService
             * @typedef DisableButton
             * @type {{
             *   (request: lebai.io.IButtonIndex, callback: lebai.io.IoService.DisableButtonCallback): void;
             *   (request: lebai.io.IButtonIndex): Promise<google.protobuf.Empty>;
             *   readonly name: "DisableButton";
             *   readonly path: "/lebai.io.IoService/DisableButton";
             *   readonly requestType: "ButtonIndex";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls DisableButton.
             * @name lebai.io.IoService#disableButton
             * @type {lebai.io.IoService.DisableButton}
             */
            $Object.defineProperties(IoService.prototype.disableButton = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.disableButton, $root.lebai.io.ButtonIndex, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "DisableButton" },
                path: { value: "/lebai.io.IoService/DisableButton" },
                requestType: { value: "ButtonIndex" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#subButtonsStatus}.
             * @memberof lebai.io.IoService
             * @typedef SubButtonsStatusCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.io.ButtonsStatus} [response] ButtonsStatus
             */

            /**
             * Calls SubButtonsStatus.
             * @memberof lebai.io.IoService
             * @typedef SubButtonsStatus
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.io.IoService.SubButtonsStatusCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.io.ButtonsStatus>;
             *   readonly name: "SubButtonsStatus";
             *   readonly path: "/lebai.io.IoService/SubButtonsStatus";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "ButtonsStatus";
             *   readonly requestStream: undefined;
             *   readonly responseStream: true;
             * }}
             */

            /**
             * Calls SubButtonsStatus.
             * @name lebai.io.IoService#subButtonsStatus
             * @type {lebai.io.IoService.SubButtonsStatus}
             */
            $Object.defineProperties(IoService.prototype.subButtonsStatus = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.subButtonsStatus, $root.google.protobuf.Empty, $root.lebai.io.ButtonsStatus, request, callback);
            }, {
                name: { value: "SubButtonsStatus" },
                path: { value: "/lebai.io.IoService/SubButtonsStatus" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "ButtonsStatus" },
                requestStream: { value: $undefined },
                responseStream: { value: true }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#setDioMode}.
             * @memberof lebai.io.IoService
             * @typedef SetDioModeCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetDioMode.
             * @memberof lebai.io.IoService
             * @typedef SetDioMode
             * @type {{
             *   (request: lebai.io.ISetDioModeRequest, callback: lebai.io.IoService.SetDioModeCallback): void;
             *   (request: lebai.io.ISetDioModeRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetDioMode";
             *   readonly path: "/lebai.io.IoService/SetDioMode";
             *   readonly requestType: "SetDioModeRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetDioMode.
             * @name lebai.io.IoService#setDioMode
             * @type {lebai.io.IoService.SetDioMode}
             */
            $Object.defineProperties(IoService.prototype.setDioMode = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.setDioMode, $root.lebai.io.SetDioModeRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetDioMode" },
                path: { value: "/lebai.io.IoService/SetDioMode" },
                requestType: { value: "SetDioModeRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#getDioMode}.
             * @memberof lebai.io.IoService
             * @typedef GetDioModeCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.io.GetDioModeResponse} [response] GetDioModeResponse
             */

            /**
             * Calls GetDioMode.
             * @memberof lebai.io.IoService
             * @typedef GetDioMode
             * @type {{
             *   (request: lebai.io.IGetDioModeRequest, callback: lebai.io.IoService.GetDioModeCallback): void;
             *   (request: lebai.io.IGetDioModeRequest): Promise<lebai.io.GetDioModeResponse>;
             *   readonly name: "GetDioMode";
             *   readonly path: "/lebai.io.IoService/GetDioMode";
             *   readonly requestType: "GetDioModeRequest";
             *   readonly responseType: "GetDioModeResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetDioMode.
             * @name lebai.io.IoService#getDioMode
             * @type {lebai.io.IoService.GetDioMode}
             */
            $Object.defineProperties(IoService.prototype.getDioMode = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.getDioMode, $root.lebai.io.GetDioModeRequest, $root.lebai.io.GetDioModeResponse, request, callback);
            }, {
                name: { value: "GetDioMode" },
                path: { value: "/lebai.io.IoService/GetDioMode" },
                requestType: { value: "GetDioModeRequest" },
                responseType: { value: "GetDioModeResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.io.IoService#getDiosMode}.
             * @memberof lebai.io.IoService
             * @typedef GetDiosModeCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.io.GetDiosModeResponse} [response] GetDiosModeResponse
             */

            /**
             * Calls GetDiosMode.
             * @memberof lebai.io.IoService
             * @typedef GetDiosMode
             * @type {{
             *   (request: lebai.io.IGetDiosModeRequest, callback: lebai.io.IoService.GetDiosModeCallback): void;
             *   (request: lebai.io.IGetDiosModeRequest): Promise<lebai.io.GetDiosModeResponse>;
             *   readonly name: "GetDiosMode";
             *   readonly path: "/lebai.io.IoService/GetDiosMode";
             *   readonly requestType: "GetDiosModeRequest";
             *   readonly responseType: "GetDiosModeResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetDiosMode.
             * @name lebai.io.IoService#getDiosMode
             * @type {lebai.io.IoService.GetDiosMode}
             */
            $Object.defineProperties(IoService.prototype.getDiosMode = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, IoService.prototype.getDiosMode, $root.lebai.io.GetDiosModeRequest, $root.lebai.io.GetDiosModeResponse, request, callback);
            }, {
                name: { value: "GetDiosMode" },
                path: { value: "/lebai.io.IoService/GetDiosMode" },
                requestType: { value: "GetDiosModeRequest" },
                responseType: { value: "GetDiosModeResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return IoService;
        })();

        return io;
    })();

    lebai.kinematic = (function() {

        /**
         * Namespace kinematic.
         * @memberof lebai
         * @namespace
         */
        var kinematic = {};

        kinematic.DhParam = (function() {

            /**
             * Properties of a DhParam.
             * @typedef {Object} lebai.kinematic.DhParam.$Properties
             * @property {number|null} [a] DhParam a
             * @property {number|null} [alpha] DhParam alpha
             * @property {number|null} [d] DhParam d
             * @property {number|null} [theta] DhParam theta
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DhParam.
             * @memberof lebai.kinematic
             * @interface IDhParam
             * @augments lebai.kinematic.DhParam.$Properties
             * @deprecated Use lebai.kinematic.DhParam.$Properties instead.
             */

            /**
             * Shape of a DhParam.
             * @typedef {lebai.kinematic.DhParam.$Properties} lebai.kinematic.DhParam.$Shape
             */

            /**
             * Constructs a new DhParam.
             * @memberof lebai.kinematic
             * @classdesc Represents a DhParam.
             * @constructor
             * @param {lebai.kinematic.DhParam.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var DhParam = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DhParam a.
             * @member {number} a
             * @memberof lebai.kinematic.DhParam
             * @instance
             */
            DhParam.prototype.a = 0;

            /**
             * DhParam alpha.
             * @member {number} alpha
             * @memberof lebai.kinematic.DhParam
             * @instance
             */
            DhParam.prototype.alpha = 0;

            /**
             * DhParam d.
             * @member {number} d
             * @memberof lebai.kinematic.DhParam
             * @instance
             */
            DhParam.prototype.d = 0;

            /**
             * DhParam theta.
             * @member {number} theta
             * @memberof lebai.kinematic.DhParam
             * @instance
             */
            DhParam.prototype.theta = 0;

            /**
             * Creates a new DhParam instance using the specified properties.
             * @function create
             * @memberof lebai.kinematic.DhParam
             * @static
             * @param {lebai.kinematic.DhParam.$Properties=} [properties] Properties to set
             * @returns {lebai.kinematic.DhParam} DhParam instance
             * @type {{
             *   (properties: lebai.kinematic.DhParam.$Shape): lebai.kinematic.DhParam & lebai.kinematic.DhParam.$Shape;
             *   (properties?: lebai.kinematic.DhParam.$Properties): lebai.kinematic.DhParam;
             * }}
             */
            DhParam.create = function(properties) {
                return new DhParam(properties);
            };

            /**
             * Encodes the specified DhParam message. Does not implicitly {@link lebai.kinematic.DhParam.verify|verify} messages.
             * @function encode
             * @memberof lebai.kinematic.DhParam
             * @static
             * @param {lebai.kinematic.DhParam.$Properties} message DhParam message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DhParam.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.a != null && $Object.hasOwnProperty.call(message, "a") && !$Object.is(message.a, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.a);
                if (message.alpha != null && $Object.hasOwnProperty.call(message, "alpha") && !$Object.is(message.alpha, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.alpha);
                if (message.d != null && $Object.hasOwnProperty.call(message, "d") && !$Object.is(message.d, 0))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.d);
                if (message.theta != null && $Object.hasOwnProperty.call(message, "theta") && !$Object.is(message.theta, 0))
                    writer.uint32(/* id 4, wireType 1 =*/33).double(message.theta);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DhParam message, length delimited. Does not implicitly {@link lebai.kinematic.DhParam.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.kinematic.DhParam
             * @static
             * @param {lebai.kinematic.DhParam.$Properties} message DhParam message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DhParam.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a DhParam message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.kinematic.DhParam
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.kinematic.DhParam & lebai.kinematic.DhParam.$Shape} DhParam
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DhParam.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.kinematic.DhParam();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.a = value;
                            else
                                delete message.a;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.alpha = value;
                            else
                                delete message.alpha;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.d = value;
                            else
                                delete message.d;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.theta = value;
                            else
                                delete message.theta;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a DhParam message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.kinematic.DhParam
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.kinematic.DhParam & lebai.kinematic.DhParam.$Shape} DhParam
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DhParam.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DhParam message.
             * @function verify
             * @memberof lebai.kinematic.DhParam
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DhParam.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.a != null && $Object.hasOwnProperty.call(message, "a"))
                    if (typeof message.a !== "number")
                        return "a: number expected";
                if (message.alpha != null && $Object.hasOwnProperty.call(message, "alpha"))
                    if (typeof message.alpha !== "number")
                        return "alpha: number expected";
                if (message.d != null && $Object.hasOwnProperty.call(message, "d"))
                    if (typeof message.d !== "number")
                        return "d: number expected";
                if (message.theta != null && $Object.hasOwnProperty.call(message, "theta"))
                    if (typeof message.theta !== "number")
                        return "theta: number expected";
                return null;
            };

            /**
             * Creates a DhParam message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.kinematic.DhParam
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.kinematic.DhParam} DhParam
             */
            DhParam.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.kinematic.DhParam)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.kinematic.DhParam: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.kinematic.DhParam();
                if (object.a != null)
                    if (!$Object.is($Number(object.a), 0))
                        message.a = $Number(object.a);
                if (object.alpha != null)
                    if (!$Object.is($Number(object.alpha), 0))
                        message.alpha = $Number(object.alpha);
                if (object.d != null)
                    if (!$Object.is($Number(object.d), 0))
                        message.d = $Number(object.d);
                if (object.theta != null)
                    if (!$Object.is($Number(object.theta), 0))
                        message.theta = $Number(object.theta);
                return message;
            };

            /**
             * Creates a plain object from a DhParam message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.kinematic.DhParam
             * @static
             * @param {lebai.kinematic.DhParam} message DhParam
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DhParam.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.a = 0;
                    object.alpha = 0;
                    object.d = 0;
                    object.theta = 0;
                }
                if (message.a != null && $Object.hasOwnProperty.call(message, "a"))
                    object.a = options.json && !$isFinite(message.a) ? $String(message.a) : message.a;
                if (message.alpha != null && $Object.hasOwnProperty.call(message, "alpha"))
                    object.alpha = options.json && !$isFinite(message.alpha) ? $String(message.alpha) : message.alpha;
                if (message.d != null && $Object.hasOwnProperty.call(message, "d"))
                    object.d = options.json && !$isFinite(message.d) ? $String(message.d) : message.d;
                if (message.theta != null && $Object.hasOwnProperty.call(message, "theta"))
                    object.theta = options.json && !$isFinite(message.theta) ? $String(message.theta) : message.theta;
                return object;
            };

            /**
             * Converts this DhParam to JSON.
             * @function toJSON
             * @memberof lebai.kinematic.DhParam
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DhParam.prototype.toJSON = function() {
                return DhParam.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DhParam
             * @function getTypeUrl
             * @memberof lebai.kinematic.DhParam
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DhParam.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.kinematic.DhParam";
            };

            return DhParam;
        })();

        kinematic.DhParams = (function() {

            /**
             * Properties of a DhParams.
             * @typedef {Object} lebai.kinematic.DhParams.$Properties
             * @property {Array.<lebai.kinematic.DhParam.$Properties>|null} [params] DhParams params
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DhParams.
             * @memberof lebai.kinematic
             * @interface IDhParams
             * @augments lebai.kinematic.DhParams.$Properties
             * @deprecated Use lebai.kinematic.DhParams.$Properties instead.
             */

            /**
             * Shape of a DhParams.
             * @typedef {lebai.kinematic.DhParams.$Properties} lebai.kinematic.DhParams.$Shape
             */

            /**
             * Constructs a new DhParams.
             * @memberof lebai.kinematic
             * @classdesc Represents a DhParams.
             * @constructor
             * @param {lebai.kinematic.DhParams.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var DhParams = function (properties) {
                this.params = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DhParams params.
             * @member {Array.<lebai.kinematic.DhParam.$Properties>} params
             * @memberof lebai.kinematic.DhParams
             * @instance
             */
            DhParams.prototype.params = $util.emptyArray;

            /**
             * Creates a new DhParams instance using the specified properties.
             * @function create
             * @memberof lebai.kinematic.DhParams
             * @static
             * @param {lebai.kinematic.DhParams.$Properties=} [properties] Properties to set
             * @returns {lebai.kinematic.DhParams} DhParams instance
             * @type {{
             *   (properties: lebai.kinematic.DhParams.$Shape): lebai.kinematic.DhParams & lebai.kinematic.DhParams.$Shape;
             *   (properties?: lebai.kinematic.DhParams.$Properties): lebai.kinematic.DhParams;
             * }}
             */
            DhParams.create = function(properties) {
                return new DhParams(properties);
            };

            /**
             * Encodes the specified DhParams message. Does not implicitly {@link lebai.kinematic.DhParams.verify|verify} messages.
             * @function encode
             * @memberof lebai.kinematic.DhParams
             * @static
             * @param {lebai.kinematic.DhParams.$Properties} message DhParams message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DhParams.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.params != null && message.params.length)
                    for (var i = 0; i < message.params.length; ++i)
                        $root.lebai.kinematic.DhParam.encode(message.params[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DhParams message, length delimited. Does not implicitly {@link lebai.kinematic.DhParams.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.kinematic.DhParams
             * @static
             * @param {lebai.kinematic.DhParams.$Properties} message DhParams message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DhParams.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a DhParams message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.kinematic.DhParams
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.kinematic.DhParams & lebai.kinematic.DhParams.$Shape} DhParams
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DhParams.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.kinematic.DhParams();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.params && message.params.length))
                                message.params = [];
                            message.params.push($root.lebai.kinematic.DhParam.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a DhParams message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.kinematic.DhParams
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.kinematic.DhParams & lebai.kinematic.DhParams.$Shape} DhParams
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DhParams.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DhParams message.
             * @function verify
             * @memberof lebai.kinematic.DhParams
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DhParams.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.params != null && $Object.hasOwnProperty.call(message, "params")) {
                    if (!$Array.isArray(message.params))
                        return "params: array expected";
                    for (var i = 0; i < message.params.length; ++i) {
                        var error = $root.lebai.kinematic.DhParam.verify(message.params[i], _depth + 1);
                        if (error)
                            return "params." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a DhParams message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.kinematic.DhParams
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.kinematic.DhParams} DhParams
             */
            DhParams.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.kinematic.DhParams)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.kinematic.DhParams: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.kinematic.DhParams();
                if (object.params) {
                    if (!$Array.isArray(object.params))
                        throw $TypeError(".lebai.kinematic.DhParams.params: array expected");
                    message.params = $Array(object.params.length);
                    for (var i = 0; i < object.params.length; ++i) {
                        if (!$util.isObject(object.params[i]))
                            throw $TypeError(".lebai.kinematic.DhParams.params: object expected");
                        message.params[i] = $root.lebai.kinematic.DhParam.fromObject(object.params[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a DhParams message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.kinematic.DhParams
             * @static
             * @param {lebai.kinematic.DhParams} message DhParams
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DhParams.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.params = [];
                if (message.params && message.params.length) {
                    object.params = $Array(message.params.length);
                    for (var j = 0; j < message.params.length; ++j)
                        object.params[j] = $root.lebai.kinematic.DhParam.toObject(message.params[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this DhParams to JSON.
             * @function toJSON
             * @memberof lebai.kinematic.DhParams
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DhParams.prototype.toJSON = function() {
                return DhParams.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DhParams
             * @function getTypeUrl
             * @memberof lebai.kinematic.DhParams
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DhParams.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.kinematic.DhParams";
            };

            return DhParams;
        })();

        kinematic.KinFactor = (function() {

            /**
             * Properties of a KinFactor.
             * @typedef {Object} lebai.kinematic.KinFactor.$Properties
             * @property {number|null} [speedFactor] KinFactor speedFactor
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a KinFactor.
             * @memberof lebai.kinematic
             * @interface IKinFactor
             * @augments lebai.kinematic.KinFactor.$Properties
             * @deprecated Use lebai.kinematic.KinFactor.$Properties instead.
             */

            /**
             * Shape of a KinFactor.
             * @typedef {lebai.kinematic.KinFactor.$Properties} lebai.kinematic.KinFactor.$Shape
             */

            /**
             * Constructs a new KinFactor.
             * @memberof lebai.kinematic
             * @classdesc Represents a KinFactor.
             * @constructor
             * @param {lebai.kinematic.KinFactor.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var KinFactor = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * KinFactor speedFactor.
             * @member {number} speedFactor
             * @memberof lebai.kinematic.KinFactor
             * @instance
             */
            KinFactor.prototype.speedFactor = 0;

            /**
             * Creates a new KinFactor instance using the specified properties.
             * @function create
             * @memberof lebai.kinematic.KinFactor
             * @static
             * @param {lebai.kinematic.KinFactor.$Properties=} [properties] Properties to set
             * @returns {lebai.kinematic.KinFactor} KinFactor instance
             * @type {{
             *   (properties: lebai.kinematic.KinFactor.$Shape): lebai.kinematic.KinFactor & lebai.kinematic.KinFactor.$Shape;
             *   (properties?: lebai.kinematic.KinFactor.$Properties): lebai.kinematic.KinFactor;
             * }}
             */
            KinFactor.create = function(properties) {
                return new KinFactor(properties);
            };

            /**
             * Encodes the specified KinFactor message. Does not implicitly {@link lebai.kinematic.KinFactor.verify|verify} messages.
             * @function encode
             * @memberof lebai.kinematic.KinFactor
             * @static
             * @param {lebai.kinematic.KinFactor.$Properties} message KinFactor message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            KinFactor.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.speedFactor != null && $Object.hasOwnProperty.call(message, "speedFactor") && message.speedFactor !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.speedFactor);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified KinFactor message, length delimited. Does not implicitly {@link lebai.kinematic.KinFactor.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.kinematic.KinFactor
             * @static
             * @param {lebai.kinematic.KinFactor.$Properties} message KinFactor message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            KinFactor.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a KinFactor message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.kinematic.KinFactor
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.kinematic.KinFactor & lebai.kinematic.KinFactor.$Shape} KinFactor
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            KinFactor.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.kinematic.KinFactor();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.speedFactor = value;
                            else
                                delete message.speedFactor;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a KinFactor message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.kinematic.KinFactor
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.kinematic.KinFactor & lebai.kinematic.KinFactor.$Shape} KinFactor
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            KinFactor.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a KinFactor message.
             * @function verify
             * @memberof lebai.kinematic.KinFactor
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            KinFactor.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.speedFactor != null && $Object.hasOwnProperty.call(message, "speedFactor"))
                    if (!$util.isInteger(message.speedFactor))
                        return "speedFactor: integer expected";
                return null;
            };

            /**
             * Creates a KinFactor message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.kinematic.KinFactor
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.kinematic.KinFactor} KinFactor
             */
            KinFactor.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.kinematic.KinFactor)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.kinematic.KinFactor: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.kinematic.KinFactor();
                if (object.speedFactor != null)
                    if ($Number(object.speedFactor) !== 0)
                        message.speedFactor = object.speedFactor | 0;
                return message;
            };

            /**
             * Creates a plain object from a KinFactor message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.kinematic.KinFactor
             * @static
             * @param {lebai.kinematic.KinFactor} message KinFactor
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            KinFactor.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.speedFactor = 0;
                if (message.speedFactor != null && $Object.hasOwnProperty.call(message, "speedFactor"))
                    object.speedFactor = message.speedFactor;
                return object;
            };

            /**
             * Converts this KinFactor to JSON.
             * @function toJSON
             * @memberof lebai.kinematic.KinFactor
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            KinFactor.prototype.toJSON = function() {
                return KinFactor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for KinFactor
             * @function getTypeUrl
             * @memberof lebai.kinematic.KinFactor
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            KinFactor.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.kinematic.KinFactor";
            };

            return KinFactor;
        })();

        kinematic.KinData = (function() {

            /**
             * Properties of a KinData.
             * @typedef {Object} lebai.kinematic.KinData.$Properties
             * @property {Array.<number>|null} [actualJointPose] KinData actualJointPose
             * @property {Array.<number>|null} [actualJointSpeed] KinData actualJointSpeed
             * @property {Array.<number>|null} [actualJointAcc] KinData actualJointAcc
             * @property {Array.<number>|null} [actualJointTorque] KinData actualJointTorque
             * @property {Array.<number>|null} [targetJointPose] KinData targetJointPose
             * @property {Array.<number>|null} [targetJointSpeed] KinData targetJointSpeed
             * @property {Array.<number>|null} [targetJointAcc] KinData targetJointAcc
             * @property {Array.<number>|null} [targetJointTorque] KinData targetJointTorque
             * @property {lebai.posture.CartesianPose.$Properties|null} [actualTcpPose] KinData actualTcpPose
             * @property {lebai.posture.CartesianPose.$Properties|null} [targetTcpPose] KinData targetTcpPose
             * @property {lebai.posture.CartesianPose.$Properties|null} [actualFlangePose] KinData actualFlangePose
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a KinData.
             * @memberof lebai.kinematic
             * @interface IKinData
             * @augments lebai.kinematic.KinData.$Properties
             * @deprecated Use lebai.kinematic.KinData.$Properties instead.
             */

            /**
             * Shape of a KinData.
             * @typedef {lebai.kinematic.KinData.$Properties} lebai.kinematic.KinData.$Shape
             */

            /**
             * Constructs a new KinData.
             * @memberof lebai.kinematic
             * @classdesc Represents a KinData.
             * @constructor
             * @param {lebai.kinematic.KinData.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var KinData = function (properties) {
                this.actualJointPose = [];
                this.actualJointSpeed = [];
                this.actualJointAcc = [];
                this.actualJointTorque = [];
                this.targetJointPose = [];
                this.targetJointSpeed = [];
                this.targetJointAcc = [];
                this.targetJointTorque = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * KinData actualJointPose.
             * @member {Array.<number>} actualJointPose
             * @memberof lebai.kinematic.KinData
             * @instance
             */
            KinData.prototype.actualJointPose = $util.emptyArray;

            /**
             * KinData actualJointSpeed.
             * @member {Array.<number>} actualJointSpeed
             * @memberof lebai.kinematic.KinData
             * @instance
             */
            KinData.prototype.actualJointSpeed = $util.emptyArray;

            /**
             * KinData actualJointAcc.
             * @member {Array.<number>} actualJointAcc
             * @memberof lebai.kinematic.KinData
             * @instance
             */
            KinData.prototype.actualJointAcc = $util.emptyArray;

            /**
             * KinData actualJointTorque.
             * @member {Array.<number>} actualJointTorque
             * @memberof lebai.kinematic.KinData
             * @instance
             */
            KinData.prototype.actualJointTorque = $util.emptyArray;

            /**
             * KinData targetJointPose.
             * @member {Array.<number>} targetJointPose
             * @memberof lebai.kinematic.KinData
             * @instance
             */
            KinData.prototype.targetJointPose = $util.emptyArray;

            /**
             * KinData targetJointSpeed.
             * @member {Array.<number>} targetJointSpeed
             * @memberof lebai.kinematic.KinData
             * @instance
             */
            KinData.prototype.targetJointSpeed = $util.emptyArray;

            /**
             * KinData targetJointAcc.
             * @member {Array.<number>} targetJointAcc
             * @memberof lebai.kinematic.KinData
             * @instance
             */
            KinData.prototype.targetJointAcc = $util.emptyArray;

            /**
             * KinData targetJointTorque.
             * @member {Array.<number>} targetJointTorque
             * @memberof lebai.kinematic.KinData
             * @instance
             */
            KinData.prototype.targetJointTorque = $util.emptyArray;

            /**
             * KinData actualTcpPose.
             * @member {lebai.posture.CartesianPose.$Properties|null|undefined} actualTcpPose
             * @memberof lebai.kinematic.KinData
             * @instance
             */
            KinData.prototype.actualTcpPose = null;

            /**
             * KinData targetTcpPose.
             * @member {lebai.posture.CartesianPose.$Properties|null|undefined} targetTcpPose
             * @memberof lebai.kinematic.KinData
             * @instance
             */
            KinData.prototype.targetTcpPose = null;

            /**
             * KinData actualFlangePose.
             * @member {lebai.posture.CartesianPose.$Properties|null|undefined} actualFlangePose
             * @memberof lebai.kinematic.KinData
             * @instance
             */
            KinData.prototype.actualFlangePose = null;

            /**
             * Creates a new KinData instance using the specified properties.
             * @function create
             * @memberof lebai.kinematic.KinData
             * @static
             * @param {lebai.kinematic.KinData.$Properties=} [properties] Properties to set
             * @returns {lebai.kinematic.KinData} KinData instance
             * @type {{
             *   (properties: lebai.kinematic.KinData.$Shape): lebai.kinematic.KinData & lebai.kinematic.KinData.$Shape;
             *   (properties?: lebai.kinematic.KinData.$Properties): lebai.kinematic.KinData;
             * }}
             */
            KinData.create = function(properties) {
                return new KinData(properties);
            };

            /**
             * Encodes the specified KinData message. Does not implicitly {@link lebai.kinematic.KinData.verify|verify} messages.
             * @function encode
             * @memberof lebai.kinematic.KinData
             * @static
             * @param {lebai.kinematic.KinData.$Properties} message KinData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            KinData.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.actualJointPose != null && message.actualJointPose.length)
                    writer.uint32(/* id 1, wireType 2 =*/10).doubles(message.actualJointPose);
                if (message.actualJointSpeed != null && message.actualJointSpeed.length)
                    writer.uint32(/* id 2, wireType 2 =*/18).doubles(message.actualJointSpeed);
                if (message.actualJointAcc != null && message.actualJointAcc.length)
                    writer.uint32(/* id 3, wireType 2 =*/26).doubles(message.actualJointAcc);
                if (message.actualJointTorque != null && message.actualJointTorque.length)
                    writer.uint32(/* id 4, wireType 2 =*/34).doubles(message.actualJointTorque);
                if (message.targetJointPose != null && message.targetJointPose.length)
                    writer.uint32(/* id 11, wireType 2 =*/90).doubles(message.targetJointPose);
                if (message.targetJointSpeed != null && message.targetJointSpeed.length)
                    writer.uint32(/* id 12, wireType 2 =*/98).doubles(message.targetJointSpeed);
                if (message.targetJointAcc != null && message.targetJointAcc.length)
                    writer.uint32(/* id 13, wireType 2 =*/106).doubles(message.targetJointAcc);
                if (message.targetJointTorque != null && message.targetJointTorque.length)
                    writer.uint32(/* id 14, wireType 2 =*/114).doubles(message.targetJointTorque);
                if (message.actualTcpPose != null && $Object.hasOwnProperty.call(message, "actualTcpPose"))
                    $root.lebai.posture.CartesianPose.encode(message.actualTcpPose, writer.uint32(/* id 21, wireType 2 =*/170).fork(), _depth + 1).ldelim();
                if (message.targetTcpPose != null && $Object.hasOwnProperty.call(message, "targetTcpPose"))
                    $root.lebai.posture.CartesianPose.encode(message.targetTcpPose, writer.uint32(/* id 31, wireType 2 =*/250).fork(), _depth + 1).ldelim();
                if (message.actualFlangePose != null && $Object.hasOwnProperty.call(message, "actualFlangePose"))
                    $root.lebai.posture.CartesianPose.encode(message.actualFlangePose, writer.uint32(/* id 41, wireType 2 =*/330).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified KinData message, length delimited. Does not implicitly {@link lebai.kinematic.KinData.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.kinematic.KinData
             * @static
             * @param {lebai.kinematic.KinData.$Properties} message KinData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            KinData.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a KinData message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.kinematic.KinData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.kinematic.KinData & lebai.kinematic.KinData.$Shape} KinData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            KinData.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.kinematic.KinData();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.actualJointPose && message.actualJointPose.length))
                                    message.actualJointPose = [];
                                reader.doubles(message.actualJointPose);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.actualJointPose && message.actualJointPose.length))
                                message.actualJointPose = [];
                            message.actualJointPose.push(reader.double());
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.actualJointSpeed && message.actualJointSpeed.length))
                                    message.actualJointSpeed = [];
                                reader.doubles(message.actualJointSpeed);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.actualJointSpeed && message.actualJointSpeed.length))
                                message.actualJointSpeed = [];
                            message.actualJointSpeed.push(reader.double());
                            continue;
                        }
                    case 3: {
                            if (wireType === 2) {
                                if (!(message.actualJointAcc && message.actualJointAcc.length))
                                    message.actualJointAcc = [];
                                reader.doubles(message.actualJointAcc);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.actualJointAcc && message.actualJointAcc.length))
                                message.actualJointAcc = [];
                            message.actualJointAcc.push(reader.double());
                            continue;
                        }
                    case 4: {
                            if (wireType === 2) {
                                if (!(message.actualJointTorque && message.actualJointTorque.length))
                                    message.actualJointTorque = [];
                                reader.doubles(message.actualJointTorque);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.actualJointTorque && message.actualJointTorque.length))
                                message.actualJointTorque = [];
                            message.actualJointTorque.push(reader.double());
                            continue;
                        }
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.targetJointPose && message.targetJointPose.length))
                                    message.targetJointPose = [];
                                reader.doubles(message.targetJointPose);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.targetJointPose && message.targetJointPose.length))
                                message.targetJointPose = [];
                            message.targetJointPose.push(reader.double());
                            continue;
                        }
                    case 12: {
                            if (wireType === 2) {
                                if (!(message.targetJointSpeed && message.targetJointSpeed.length))
                                    message.targetJointSpeed = [];
                                reader.doubles(message.targetJointSpeed);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.targetJointSpeed && message.targetJointSpeed.length))
                                message.targetJointSpeed = [];
                            message.targetJointSpeed.push(reader.double());
                            continue;
                        }
                    case 13: {
                            if (wireType === 2) {
                                if (!(message.targetJointAcc && message.targetJointAcc.length))
                                    message.targetJointAcc = [];
                                reader.doubles(message.targetJointAcc);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.targetJointAcc && message.targetJointAcc.length))
                                message.targetJointAcc = [];
                            message.targetJointAcc.push(reader.double());
                            continue;
                        }
                    case 14: {
                            if (wireType === 2) {
                                if (!(message.targetJointTorque && message.targetJointTorque.length))
                                    message.targetJointTorque = [];
                                reader.doubles(message.targetJointTorque);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.targetJointTorque && message.targetJointTorque.length))
                                message.targetJointTorque = [];
                            message.targetJointTorque.push(reader.double());
                            continue;
                        }
                    case 21: {
                            if (wireType !== 2)
                                break;
                            message.actualTcpPose = $root.lebai.posture.CartesianPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.actualTcpPose);
                            continue;
                        }
                    case 31: {
                            if (wireType !== 2)
                                break;
                            message.targetTcpPose = $root.lebai.posture.CartesianPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.targetTcpPose);
                            continue;
                        }
                    case 41: {
                            if (wireType !== 2)
                                break;
                            message.actualFlangePose = $root.lebai.posture.CartesianPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.actualFlangePose);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a KinData message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.kinematic.KinData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.kinematic.KinData & lebai.kinematic.KinData.$Shape} KinData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            KinData.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a KinData message.
             * @function verify
             * @memberof lebai.kinematic.KinData
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            KinData.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.actualJointPose != null && $Object.hasOwnProperty.call(message, "actualJointPose")) {
                    if (!$Array.isArray(message.actualJointPose))
                        return "actualJointPose: array expected";
                    for (var i = 0; i < message.actualJointPose.length; ++i)
                        if (typeof message.actualJointPose[i] !== "number")
                            return "actualJointPose: number[] expected";
                }
                if (message.actualJointSpeed != null && $Object.hasOwnProperty.call(message, "actualJointSpeed")) {
                    if (!$Array.isArray(message.actualJointSpeed))
                        return "actualJointSpeed: array expected";
                    for (var i = 0; i < message.actualJointSpeed.length; ++i)
                        if (typeof message.actualJointSpeed[i] !== "number")
                            return "actualJointSpeed: number[] expected";
                }
                if (message.actualJointAcc != null && $Object.hasOwnProperty.call(message, "actualJointAcc")) {
                    if (!$Array.isArray(message.actualJointAcc))
                        return "actualJointAcc: array expected";
                    for (var i = 0; i < message.actualJointAcc.length; ++i)
                        if (typeof message.actualJointAcc[i] !== "number")
                            return "actualJointAcc: number[] expected";
                }
                if (message.actualJointTorque != null && $Object.hasOwnProperty.call(message, "actualJointTorque")) {
                    if (!$Array.isArray(message.actualJointTorque))
                        return "actualJointTorque: array expected";
                    for (var i = 0; i < message.actualJointTorque.length; ++i)
                        if (typeof message.actualJointTorque[i] !== "number")
                            return "actualJointTorque: number[] expected";
                }
                if (message.targetJointPose != null && $Object.hasOwnProperty.call(message, "targetJointPose")) {
                    if (!$Array.isArray(message.targetJointPose))
                        return "targetJointPose: array expected";
                    for (var i = 0; i < message.targetJointPose.length; ++i)
                        if (typeof message.targetJointPose[i] !== "number")
                            return "targetJointPose: number[] expected";
                }
                if (message.targetJointSpeed != null && $Object.hasOwnProperty.call(message, "targetJointSpeed")) {
                    if (!$Array.isArray(message.targetJointSpeed))
                        return "targetJointSpeed: array expected";
                    for (var i = 0; i < message.targetJointSpeed.length; ++i)
                        if (typeof message.targetJointSpeed[i] !== "number")
                            return "targetJointSpeed: number[] expected";
                }
                if (message.targetJointAcc != null && $Object.hasOwnProperty.call(message, "targetJointAcc")) {
                    if (!$Array.isArray(message.targetJointAcc))
                        return "targetJointAcc: array expected";
                    for (var i = 0; i < message.targetJointAcc.length; ++i)
                        if (typeof message.targetJointAcc[i] !== "number")
                            return "targetJointAcc: number[] expected";
                }
                if (message.targetJointTorque != null && $Object.hasOwnProperty.call(message, "targetJointTorque")) {
                    if (!$Array.isArray(message.targetJointTorque))
                        return "targetJointTorque: array expected";
                    for (var i = 0; i < message.targetJointTorque.length; ++i)
                        if (typeof message.targetJointTorque[i] !== "number")
                            return "targetJointTorque: number[] expected";
                }
                if (message.actualTcpPose != null && $Object.hasOwnProperty.call(message, "actualTcpPose")) {
                    var error = $root.lebai.posture.CartesianPose.verify(message.actualTcpPose, _depth + 1);
                    if (error)
                        return "actualTcpPose." + error;
                }
                if (message.targetTcpPose != null && $Object.hasOwnProperty.call(message, "targetTcpPose")) {
                    var error = $root.lebai.posture.CartesianPose.verify(message.targetTcpPose, _depth + 1);
                    if (error)
                        return "targetTcpPose." + error;
                }
                if (message.actualFlangePose != null && $Object.hasOwnProperty.call(message, "actualFlangePose")) {
                    var error = $root.lebai.posture.CartesianPose.verify(message.actualFlangePose, _depth + 1);
                    if (error)
                        return "actualFlangePose." + error;
                }
                return null;
            };

            /**
             * Creates a KinData message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.kinematic.KinData
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.kinematic.KinData} KinData
             */
            KinData.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.kinematic.KinData)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.kinematic.KinData: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.kinematic.KinData();
                if (object.actualJointPose) {
                    if (!$Array.isArray(object.actualJointPose))
                        throw $TypeError(".lebai.kinematic.KinData.actualJointPose: array expected");
                    message.actualJointPose = $Array(object.actualJointPose.length);
                    for (var i = 0; i < object.actualJointPose.length; ++i)
                        message.actualJointPose[i] = $Number(object.actualJointPose[i]);
                }
                if (object.actualJointSpeed) {
                    if (!$Array.isArray(object.actualJointSpeed))
                        throw $TypeError(".lebai.kinematic.KinData.actualJointSpeed: array expected");
                    message.actualJointSpeed = $Array(object.actualJointSpeed.length);
                    for (var i = 0; i < object.actualJointSpeed.length; ++i)
                        message.actualJointSpeed[i] = $Number(object.actualJointSpeed[i]);
                }
                if (object.actualJointAcc) {
                    if (!$Array.isArray(object.actualJointAcc))
                        throw $TypeError(".lebai.kinematic.KinData.actualJointAcc: array expected");
                    message.actualJointAcc = $Array(object.actualJointAcc.length);
                    for (var i = 0; i < object.actualJointAcc.length; ++i)
                        message.actualJointAcc[i] = $Number(object.actualJointAcc[i]);
                }
                if (object.actualJointTorque) {
                    if (!$Array.isArray(object.actualJointTorque))
                        throw $TypeError(".lebai.kinematic.KinData.actualJointTorque: array expected");
                    message.actualJointTorque = $Array(object.actualJointTorque.length);
                    for (var i = 0; i < object.actualJointTorque.length; ++i)
                        message.actualJointTorque[i] = $Number(object.actualJointTorque[i]);
                }
                if (object.targetJointPose) {
                    if (!$Array.isArray(object.targetJointPose))
                        throw $TypeError(".lebai.kinematic.KinData.targetJointPose: array expected");
                    message.targetJointPose = $Array(object.targetJointPose.length);
                    for (var i = 0; i < object.targetJointPose.length; ++i)
                        message.targetJointPose[i] = $Number(object.targetJointPose[i]);
                }
                if (object.targetJointSpeed) {
                    if (!$Array.isArray(object.targetJointSpeed))
                        throw $TypeError(".lebai.kinematic.KinData.targetJointSpeed: array expected");
                    message.targetJointSpeed = $Array(object.targetJointSpeed.length);
                    for (var i = 0; i < object.targetJointSpeed.length; ++i)
                        message.targetJointSpeed[i] = $Number(object.targetJointSpeed[i]);
                }
                if (object.targetJointAcc) {
                    if (!$Array.isArray(object.targetJointAcc))
                        throw $TypeError(".lebai.kinematic.KinData.targetJointAcc: array expected");
                    message.targetJointAcc = $Array(object.targetJointAcc.length);
                    for (var i = 0; i < object.targetJointAcc.length; ++i)
                        message.targetJointAcc[i] = $Number(object.targetJointAcc[i]);
                }
                if (object.targetJointTorque) {
                    if (!$Array.isArray(object.targetJointTorque))
                        throw $TypeError(".lebai.kinematic.KinData.targetJointTorque: array expected");
                    message.targetJointTorque = $Array(object.targetJointTorque.length);
                    for (var i = 0; i < object.targetJointTorque.length; ++i)
                        message.targetJointTorque[i] = $Number(object.targetJointTorque[i]);
                }
                if (object.actualTcpPose != null) {
                    if (!$util.isObject(object.actualTcpPose))
                        throw $TypeError(".lebai.kinematic.KinData.actualTcpPose: object expected");
                    message.actualTcpPose = $root.lebai.posture.CartesianPose.fromObject(object.actualTcpPose, _depth + 1);
                }
                if (object.targetTcpPose != null) {
                    if (!$util.isObject(object.targetTcpPose))
                        throw $TypeError(".lebai.kinematic.KinData.targetTcpPose: object expected");
                    message.targetTcpPose = $root.lebai.posture.CartesianPose.fromObject(object.targetTcpPose, _depth + 1);
                }
                if (object.actualFlangePose != null) {
                    if (!$util.isObject(object.actualFlangePose))
                        throw $TypeError(".lebai.kinematic.KinData.actualFlangePose: object expected");
                    message.actualFlangePose = $root.lebai.posture.CartesianPose.fromObject(object.actualFlangePose, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a KinData message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.kinematic.KinData
             * @static
             * @param {lebai.kinematic.KinData} message KinData
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            KinData.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.actualJointPose = [];
                    object.actualJointSpeed = [];
                    object.actualJointAcc = [];
                    object.actualJointTorque = [];
                    object.targetJointPose = [];
                    object.targetJointSpeed = [];
                    object.targetJointAcc = [];
                    object.targetJointTorque = [];
                }
                if (options.defaults) {
                    object.actualTcpPose = null;
                    object.targetTcpPose = null;
                    object.actualFlangePose = null;
                }
                if (message.actualJointPose && message.actualJointPose.length) {
                    object.actualJointPose = $Array(message.actualJointPose.length);
                    for (var j = 0; j < message.actualJointPose.length; ++j)
                        object.actualJointPose[j] = options.json && !$isFinite(message.actualJointPose[j]) ? $String(message.actualJointPose[j]) : message.actualJointPose[j];
                }
                if (message.actualJointSpeed && message.actualJointSpeed.length) {
                    object.actualJointSpeed = $Array(message.actualJointSpeed.length);
                    for (var j = 0; j < message.actualJointSpeed.length; ++j)
                        object.actualJointSpeed[j] = options.json && !$isFinite(message.actualJointSpeed[j]) ? $String(message.actualJointSpeed[j]) : message.actualJointSpeed[j];
                }
                if (message.actualJointAcc && message.actualJointAcc.length) {
                    object.actualJointAcc = $Array(message.actualJointAcc.length);
                    for (var j = 0; j < message.actualJointAcc.length; ++j)
                        object.actualJointAcc[j] = options.json && !$isFinite(message.actualJointAcc[j]) ? $String(message.actualJointAcc[j]) : message.actualJointAcc[j];
                }
                if (message.actualJointTorque && message.actualJointTorque.length) {
                    object.actualJointTorque = $Array(message.actualJointTorque.length);
                    for (var j = 0; j < message.actualJointTorque.length; ++j)
                        object.actualJointTorque[j] = options.json && !$isFinite(message.actualJointTorque[j]) ? $String(message.actualJointTorque[j]) : message.actualJointTorque[j];
                }
                if (message.targetJointPose && message.targetJointPose.length) {
                    object.targetJointPose = $Array(message.targetJointPose.length);
                    for (var j = 0; j < message.targetJointPose.length; ++j)
                        object.targetJointPose[j] = options.json && !$isFinite(message.targetJointPose[j]) ? $String(message.targetJointPose[j]) : message.targetJointPose[j];
                }
                if (message.targetJointSpeed && message.targetJointSpeed.length) {
                    object.targetJointSpeed = $Array(message.targetJointSpeed.length);
                    for (var j = 0; j < message.targetJointSpeed.length; ++j)
                        object.targetJointSpeed[j] = options.json && !$isFinite(message.targetJointSpeed[j]) ? $String(message.targetJointSpeed[j]) : message.targetJointSpeed[j];
                }
                if (message.targetJointAcc && message.targetJointAcc.length) {
                    object.targetJointAcc = $Array(message.targetJointAcc.length);
                    for (var j = 0; j < message.targetJointAcc.length; ++j)
                        object.targetJointAcc[j] = options.json && !$isFinite(message.targetJointAcc[j]) ? $String(message.targetJointAcc[j]) : message.targetJointAcc[j];
                }
                if (message.targetJointTorque && message.targetJointTorque.length) {
                    object.targetJointTorque = $Array(message.targetJointTorque.length);
                    for (var j = 0; j < message.targetJointTorque.length; ++j)
                        object.targetJointTorque[j] = options.json && !$isFinite(message.targetJointTorque[j]) ? $String(message.targetJointTorque[j]) : message.targetJointTorque[j];
                }
                if (message.actualTcpPose != null && $Object.hasOwnProperty.call(message, "actualTcpPose"))
                    object.actualTcpPose = $root.lebai.posture.CartesianPose.toObject(message.actualTcpPose, options, _depth + 1);
                if (message.targetTcpPose != null && $Object.hasOwnProperty.call(message, "targetTcpPose"))
                    object.targetTcpPose = $root.lebai.posture.CartesianPose.toObject(message.targetTcpPose, options, _depth + 1);
                if (message.actualFlangePose != null && $Object.hasOwnProperty.call(message, "actualFlangePose"))
                    object.actualFlangePose = $root.lebai.posture.CartesianPose.toObject(message.actualFlangePose, options, _depth + 1);
                return object;
            };

            /**
             * Converts this KinData to JSON.
             * @function toJSON
             * @memberof lebai.kinematic.KinData
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            KinData.prototype.toJSON = function() {
                return KinData.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for KinData
             * @function getTypeUrl
             * @memberof lebai.kinematic.KinData
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            KinData.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.kinematic.KinData";
            };

            return KinData;
        })();

        kinematic.SaveTcpRequest = (function() {

            /**
             * Properties of a SaveTcpRequest.
             * @typedef {Object} lebai.kinematic.SaveTcpRequest.$Properties
             * @property {string|null} [name] SaveTcpRequest name
             * @property {lebai.posture.CartesianPose.$Properties|null} [data] SaveTcpRequest data
             * @property {string|null} [dir] SaveTcpRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SaveTcpRequest.
             * @memberof lebai.kinematic
             * @interface ISaveTcpRequest
             * @augments lebai.kinematic.SaveTcpRequest.$Properties
             * @deprecated Use lebai.kinematic.SaveTcpRequest.$Properties instead.
             */

            /**
             * Shape of a SaveTcpRequest.
             * @typedef {lebai.kinematic.SaveTcpRequest.$Properties} lebai.kinematic.SaveTcpRequest.$Shape
             */

            /**
             * Constructs a new SaveTcpRequest.
             * @memberof lebai.kinematic
             * @classdesc Represents a SaveTcpRequest.
             * @constructor
             * @param {lebai.kinematic.SaveTcpRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SaveTcpRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SaveTcpRequest name.
             * @member {string} name
             * @memberof lebai.kinematic.SaveTcpRequest
             * @instance
             */
            SaveTcpRequest.prototype.name = "";

            /**
             * SaveTcpRequest data.
             * @member {lebai.posture.CartesianPose.$Properties|null|undefined} data
             * @memberof lebai.kinematic.SaveTcpRequest
             * @instance
             */
            SaveTcpRequest.prototype.data = null;

            /**
             * SaveTcpRequest dir.
             * @member {string} dir
             * @memberof lebai.kinematic.SaveTcpRequest
             * @instance
             */
            SaveTcpRequest.prototype.dir = "";

            /**
             * Creates a new SaveTcpRequest instance using the specified properties.
             * @function create
             * @memberof lebai.kinematic.SaveTcpRequest
             * @static
             * @param {lebai.kinematic.SaveTcpRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.kinematic.SaveTcpRequest} SaveTcpRequest instance
             * @type {{
             *   (properties: lebai.kinematic.SaveTcpRequest.$Shape): lebai.kinematic.SaveTcpRequest & lebai.kinematic.SaveTcpRequest.$Shape;
             *   (properties?: lebai.kinematic.SaveTcpRequest.$Properties): lebai.kinematic.SaveTcpRequest;
             * }}
             */
            SaveTcpRequest.create = function(properties) {
                return new SaveTcpRequest(properties);
            };

            /**
             * Encodes the specified SaveTcpRequest message. Does not implicitly {@link lebai.kinematic.SaveTcpRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.kinematic.SaveTcpRequest
             * @static
             * @param {lebai.kinematic.SaveTcpRequest.$Properties} message SaveTcpRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveTcpRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    $root.lebai.posture.CartesianPose.encode(message.data, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SaveTcpRequest message, length delimited. Does not implicitly {@link lebai.kinematic.SaveTcpRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.kinematic.SaveTcpRequest
             * @static
             * @param {lebai.kinematic.SaveTcpRequest.$Properties} message SaveTcpRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveTcpRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SaveTcpRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.kinematic.SaveTcpRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.kinematic.SaveTcpRequest & lebai.kinematic.SaveTcpRequest.$Shape} SaveTcpRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveTcpRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.kinematic.SaveTcpRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.data = $root.lebai.posture.CartesianPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.data);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SaveTcpRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.kinematic.SaveTcpRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.kinematic.SaveTcpRequest & lebai.kinematic.SaveTcpRequest.$Shape} SaveTcpRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveTcpRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SaveTcpRequest message.
             * @function verify
             * @memberof lebai.kinematic.SaveTcpRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SaveTcpRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    var error = $root.lebai.posture.CartesianPose.verify(message.data, _depth + 1);
                    if (error)
                        return "data." + error;
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates a SaveTcpRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.kinematic.SaveTcpRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.kinematic.SaveTcpRequest} SaveTcpRequest
             */
            SaveTcpRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.kinematic.SaveTcpRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.kinematic.SaveTcpRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.kinematic.SaveTcpRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.data != null) {
                    if (!$util.isObject(object.data))
                        throw $TypeError(".lebai.kinematic.SaveTcpRequest.data: object expected");
                    message.data = $root.lebai.posture.CartesianPose.fromObject(object.data, _depth + 1);
                }
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from a SaveTcpRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.kinematic.SaveTcpRequest
             * @static
             * @param {lebai.kinematic.SaveTcpRequest} message SaveTcpRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SaveTcpRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.data = null;
                    object.dir = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = $root.lebai.posture.CartesianPose.toObject(message.data, options, _depth + 1);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                return object;
            };

            /**
             * Converts this SaveTcpRequest to JSON.
             * @function toJSON
             * @memberof lebai.kinematic.SaveTcpRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SaveTcpRequest.prototype.toJSON = function() {
                return SaveTcpRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SaveTcpRequest
             * @function getTypeUrl
             * @memberof lebai.kinematic.SaveTcpRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SaveTcpRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.kinematic.SaveTcpRequest";
            };

            return SaveTcpRequest;
        })();

        kinematic.CalcFrameRequest = (function() {

            /**
             * Properties of a CalcFrameRequest.
             * @typedef {Object} lebai.kinematic.CalcFrameRequest.$Properties
             * @property {lebai.posture.CartesianPose.$Properties|null} [o] CalcFrameRequest o
             * @property {lebai.posture.CartesianPose.$Properties|null} [x] CalcFrameRequest x
             * @property {lebai.posture.CartesianPose.$Properties|null} [xy] CalcFrameRequest xy
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CalcFrameRequest.
             * @memberof lebai.kinematic
             * @interface ICalcFrameRequest
             * @augments lebai.kinematic.CalcFrameRequest.$Properties
             * @deprecated Use lebai.kinematic.CalcFrameRequest.$Properties instead.
             */

            /**
             * Shape of a CalcFrameRequest.
             * @typedef {lebai.kinematic.CalcFrameRequest.$Properties} lebai.kinematic.CalcFrameRequest.$Shape
             */

            /**
             * Constructs a new CalcFrameRequest.
             * @memberof lebai.kinematic
             * @classdesc Represents a CalcFrameRequest.
             * @constructor
             * @param {lebai.kinematic.CalcFrameRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var CalcFrameRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CalcFrameRequest o.
             * @member {lebai.posture.CartesianPose.$Properties|null|undefined} o
             * @memberof lebai.kinematic.CalcFrameRequest
             * @instance
             */
            CalcFrameRequest.prototype.o = null;

            /**
             * CalcFrameRequest x.
             * @member {lebai.posture.CartesianPose.$Properties|null|undefined} x
             * @memberof lebai.kinematic.CalcFrameRequest
             * @instance
             */
            CalcFrameRequest.prototype.x = null;

            /**
             * CalcFrameRequest xy.
             * @member {lebai.posture.CartesianPose.$Properties|null|undefined} xy
             * @memberof lebai.kinematic.CalcFrameRequest
             * @instance
             */
            CalcFrameRequest.prototype.xy = null;

            /**
             * Creates a new CalcFrameRequest instance using the specified properties.
             * @function create
             * @memberof lebai.kinematic.CalcFrameRequest
             * @static
             * @param {lebai.kinematic.CalcFrameRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.kinematic.CalcFrameRequest} CalcFrameRequest instance
             * @type {{
             *   (properties: lebai.kinematic.CalcFrameRequest.$Shape): lebai.kinematic.CalcFrameRequest & lebai.kinematic.CalcFrameRequest.$Shape;
             *   (properties?: lebai.kinematic.CalcFrameRequest.$Properties): lebai.kinematic.CalcFrameRequest;
             * }}
             */
            CalcFrameRequest.create = function(properties) {
                return new CalcFrameRequest(properties);
            };

            /**
             * Encodes the specified CalcFrameRequest message. Does not implicitly {@link lebai.kinematic.CalcFrameRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.kinematic.CalcFrameRequest
             * @static
             * @param {lebai.kinematic.CalcFrameRequest.$Properties} message CalcFrameRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CalcFrameRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.o != null && $Object.hasOwnProperty.call(message, "o"))
                    $root.lebai.posture.CartesianPose.encode(message.o, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.x != null && $Object.hasOwnProperty.call(message, "x"))
                    $root.lebai.posture.CartesianPose.encode(message.x, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.xy != null && $Object.hasOwnProperty.call(message, "xy"))
                    $root.lebai.posture.CartesianPose.encode(message.xy, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CalcFrameRequest message, length delimited. Does not implicitly {@link lebai.kinematic.CalcFrameRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.kinematic.CalcFrameRequest
             * @static
             * @param {lebai.kinematic.CalcFrameRequest.$Properties} message CalcFrameRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CalcFrameRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a CalcFrameRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.kinematic.CalcFrameRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.kinematic.CalcFrameRequest & lebai.kinematic.CalcFrameRequest.$Shape} CalcFrameRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CalcFrameRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.kinematic.CalcFrameRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.o = $root.lebai.posture.CartesianPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.o);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.x = $root.lebai.posture.CartesianPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.x);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.xy = $root.lebai.posture.CartesianPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.xy);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a CalcFrameRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.kinematic.CalcFrameRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.kinematic.CalcFrameRequest & lebai.kinematic.CalcFrameRequest.$Shape} CalcFrameRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CalcFrameRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CalcFrameRequest message.
             * @function verify
             * @memberof lebai.kinematic.CalcFrameRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CalcFrameRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.o != null && $Object.hasOwnProperty.call(message, "o")) {
                    var error = $root.lebai.posture.CartesianPose.verify(message.o, _depth + 1);
                    if (error)
                        return "o." + error;
                }
                if (message.x != null && $Object.hasOwnProperty.call(message, "x")) {
                    var error = $root.lebai.posture.CartesianPose.verify(message.x, _depth + 1);
                    if (error)
                        return "x." + error;
                }
                if (message.xy != null && $Object.hasOwnProperty.call(message, "xy")) {
                    var error = $root.lebai.posture.CartesianPose.verify(message.xy, _depth + 1);
                    if (error)
                        return "xy." + error;
                }
                return null;
            };

            /**
             * Creates a CalcFrameRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.kinematic.CalcFrameRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.kinematic.CalcFrameRequest} CalcFrameRequest
             */
            CalcFrameRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.kinematic.CalcFrameRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.kinematic.CalcFrameRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.kinematic.CalcFrameRequest();
                if (object.o != null) {
                    if (!$util.isObject(object.o))
                        throw $TypeError(".lebai.kinematic.CalcFrameRequest.o: object expected");
                    message.o = $root.lebai.posture.CartesianPose.fromObject(object.o, _depth + 1);
                }
                if (object.x != null) {
                    if (!$util.isObject(object.x))
                        throw $TypeError(".lebai.kinematic.CalcFrameRequest.x: object expected");
                    message.x = $root.lebai.posture.CartesianPose.fromObject(object.x, _depth + 1);
                }
                if (object.xy != null) {
                    if (!$util.isObject(object.xy))
                        throw $TypeError(".lebai.kinematic.CalcFrameRequest.xy: object expected");
                    message.xy = $root.lebai.posture.CartesianPose.fromObject(object.xy, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a CalcFrameRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.kinematic.CalcFrameRequest
             * @static
             * @param {lebai.kinematic.CalcFrameRequest} message CalcFrameRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CalcFrameRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.o = null;
                    object.x = null;
                    object.xy = null;
                }
                if (message.o != null && $Object.hasOwnProperty.call(message, "o"))
                    object.o = $root.lebai.posture.CartesianPose.toObject(message.o, options, _depth + 1);
                if (message.x != null && $Object.hasOwnProperty.call(message, "x"))
                    object.x = $root.lebai.posture.CartesianPose.toObject(message.x, options, _depth + 1);
                if (message.xy != null && $Object.hasOwnProperty.call(message, "xy"))
                    object.xy = $root.lebai.posture.CartesianPose.toObject(message.xy, options, _depth + 1);
                return object;
            };

            /**
             * Converts this CalcFrameRequest to JSON.
             * @function toJSON
             * @memberof lebai.kinematic.CalcFrameRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CalcFrameRequest.prototype.toJSON = function() {
                return CalcFrameRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CalcFrameRequest
             * @function getTypeUrl
             * @memberof lebai.kinematic.CalcFrameRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CalcFrameRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.kinematic.CalcFrameRequest";
            };

            return CalcFrameRequest;
        })();

        kinematic.CalcTcpRequest = (function() {

            /**
             * Properties of a CalcTcpRequest.
             * @typedef {Object} lebai.kinematic.CalcTcpRequest.$Properties
             * @property {Array.<lebai.posture.CartesianPose.$Properties>|null} [poses] CalcTcpRequest poses
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CalcTcpRequest.
             * @memberof lebai.kinematic
             * @interface ICalcTcpRequest
             * @augments lebai.kinematic.CalcTcpRequest.$Properties
             * @deprecated Use lebai.kinematic.CalcTcpRequest.$Properties instead.
             */

            /**
             * Shape of a CalcTcpRequest.
             * @typedef {lebai.kinematic.CalcTcpRequest.$Properties} lebai.kinematic.CalcTcpRequest.$Shape
             */

            /**
             * Constructs a new CalcTcpRequest.
             * @memberof lebai.kinematic
             * @classdesc Represents a CalcTcpRequest.
             * @constructor
             * @param {lebai.kinematic.CalcTcpRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var CalcTcpRequest = function (properties) {
                this.poses = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CalcTcpRequest poses.
             * @member {Array.<lebai.posture.CartesianPose.$Properties>} poses
             * @memberof lebai.kinematic.CalcTcpRequest
             * @instance
             */
            CalcTcpRequest.prototype.poses = $util.emptyArray;

            /**
             * Creates a new CalcTcpRequest instance using the specified properties.
             * @function create
             * @memberof lebai.kinematic.CalcTcpRequest
             * @static
             * @param {lebai.kinematic.CalcTcpRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.kinematic.CalcTcpRequest} CalcTcpRequest instance
             * @type {{
             *   (properties: lebai.kinematic.CalcTcpRequest.$Shape): lebai.kinematic.CalcTcpRequest & lebai.kinematic.CalcTcpRequest.$Shape;
             *   (properties?: lebai.kinematic.CalcTcpRequest.$Properties): lebai.kinematic.CalcTcpRequest;
             * }}
             */
            CalcTcpRequest.create = function(properties) {
                return new CalcTcpRequest(properties);
            };

            /**
             * Encodes the specified CalcTcpRequest message. Does not implicitly {@link lebai.kinematic.CalcTcpRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.kinematic.CalcTcpRequest
             * @static
             * @param {lebai.kinematic.CalcTcpRequest.$Properties} message CalcTcpRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CalcTcpRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.poses != null && message.poses.length)
                    for (var i = 0; i < message.poses.length; ++i)
                        $root.lebai.posture.CartesianPose.encode(message.poses[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CalcTcpRequest message, length delimited. Does not implicitly {@link lebai.kinematic.CalcTcpRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.kinematic.CalcTcpRequest
             * @static
             * @param {lebai.kinematic.CalcTcpRequest.$Properties} message CalcTcpRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CalcTcpRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a CalcTcpRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.kinematic.CalcTcpRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.kinematic.CalcTcpRequest & lebai.kinematic.CalcTcpRequest.$Shape} CalcTcpRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CalcTcpRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.kinematic.CalcTcpRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.poses && message.poses.length))
                                message.poses = [];
                            message.poses.push($root.lebai.posture.CartesianPose.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a CalcTcpRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.kinematic.CalcTcpRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.kinematic.CalcTcpRequest & lebai.kinematic.CalcTcpRequest.$Shape} CalcTcpRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CalcTcpRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CalcTcpRequest message.
             * @function verify
             * @memberof lebai.kinematic.CalcTcpRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CalcTcpRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.poses != null && $Object.hasOwnProperty.call(message, "poses")) {
                    if (!$Array.isArray(message.poses))
                        return "poses: array expected";
                    for (var i = 0; i < message.poses.length; ++i) {
                        var error = $root.lebai.posture.CartesianPose.verify(message.poses[i], _depth + 1);
                        if (error)
                            return "poses." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a CalcTcpRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.kinematic.CalcTcpRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.kinematic.CalcTcpRequest} CalcTcpRequest
             */
            CalcTcpRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.kinematic.CalcTcpRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.kinematic.CalcTcpRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.kinematic.CalcTcpRequest();
                if (object.poses) {
                    if (!$Array.isArray(object.poses))
                        throw $TypeError(".lebai.kinematic.CalcTcpRequest.poses: array expected");
                    message.poses = $Array(object.poses.length);
                    for (var i = 0; i < object.poses.length; ++i) {
                        if (!$util.isObject(object.poses[i]))
                            throw $TypeError(".lebai.kinematic.CalcTcpRequest.poses: object expected");
                        message.poses[i] = $root.lebai.posture.CartesianPose.fromObject(object.poses[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a CalcTcpRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.kinematic.CalcTcpRequest
             * @static
             * @param {lebai.kinematic.CalcTcpRequest} message CalcTcpRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CalcTcpRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.poses = [];
                if (message.poses && message.poses.length) {
                    object.poses = $Array(message.poses.length);
                    for (var j = 0; j < message.poses.length; ++j)
                        object.poses[j] = $root.lebai.posture.CartesianPose.toObject(message.poses[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this CalcTcpRequest to JSON.
             * @function toJSON
             * @memberof lebai.kinematic.CalcTcpRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CalcTcpRequest.prototype.toJSON = function() {
                return CalcTcpRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CalcTcpRequest
             * @function getTypeUrl
             * @memberof lebai.kinematic.CalcTcpRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CalcTcpRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.kinematic.CalcTcpRequest";
            };

            return CalcTcpRequest;
        })();

        kinematic.KinematicService = (function() {

            /**
             * Constructs a new KinematicService service.
             * @memberof lebai.kinematic
             * @classdesc Represents a KinematicService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var KinematicService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(KinematicService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: KinematicService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new KinematicService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.kinematic.KinematicService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {KinematicService} RPC service. Useful where requests and/or responses are streamed.
             */
            KinematicService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.kinematic.KinematicService#saveTcp}.
             * @memberof lebai.kinematic.KinematicService
             * @typedef SaveTcpCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SaveTcp.
             * @memberof lebai.kinematic.KinematicService
             * @typedef SaveTcp
             * @type {{
             *   (request: lebai.kinematic.ISaveTcpRequest, callback: lebai.kinematic.KinematicService.SaveTcpCallback): void;
             *   (request: lebai.kinematic.ISaveTcpRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SaveTcp";
             *   readonly path: "/lebai.kinematic.KinematicService/SaveTcp";
             *   readonly requestType: "SaveTcpRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SaveTcp.
             * @name lebai.kinematic.KinematicService#saveTcp
             * @type {lebai.kinematic.KinematicService.SaveTcp}
             */
            $Object.defineProperties(KinematicService.prototype.saveTcp = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, KinematicService.prototype.saveTcp, $root.lebai.kinematic.SaveTcpRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SaveTcp" },
                path: { value: "/lebai.kinematic.KinematicService/SaveTcp" },
                requestType: { value: "SaveTcpRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.kinematic.KinematicService#loadTcp}.
             * @memberof lebai.kinematic.KinematicService
             * @typedef LoadTcpCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.posture.CartesianPose} [response] CartesianPose
             */

            /**
             * Calls LoadTcp.
             * @memberof lebai.kinematic.KinematicService
             * @typedef LoadTcp
             * @type {{
             *   (request: lebai.db.ILoadRequest, callback: lebai.kinematic.KinematicService.LoadTcpCallback): void;
             *   (request: lebai.db.ILoadRequest): Promise<lebai.posture.CartesianPose>;
             *   readonly name: "LoadTcp";
             *   readonly path: "/lebai.kinematic.KinematicService/LoadTcp";
             *   readonly requestType: "db.LoadRequest";
             *   readonly responseType: "posture.CartesianPose";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadTcp.
             * @name lebai.kinematic.KinematicService#loadTcp
             * @type {lebai.kinematic.KinematicService.LoadTcp}
             */
            $Object.defineProperties(KinematicService.prototype.loadTcp = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, KinematicService.prototype.loadTcp, $root.lebai.db.LoadRequest, $root.lebai.posture.CartesianPose, request, callback);
            }, {
                name: { value: "LoadTcp" },
                path: { value: "/lebai.kinematic.KinematicService/LoadTcp" },
                requestType: { value: "db.LoadRequest" },
                responseType: { value: "posture.CartesianPose" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.kinematic.KinematicService#loadTcpList}.
             * @memberof lebai.kinematic.KinematicService
             * @typedef LoadTcpListCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.db.LoadListResponse} [response] LoadListResponse
             */

            /**
             * Calls LoadTcpList.
             * @memberof lebai.kinematic.KinematicService
             * @typedef LoadTcpList
             * @type {{
             *   (request: lebai.db.ILoadListRequest, callback: lebai.kinematic.KinematicService.LoadTcpListCallback): void;
             *   (request: lebai.db.ILoadListRequest): Promise<lebai.db.LoadListResponse>;
             *   readonly name: "LoadTcpList";
             *   readonly path: "/lebai.kinematic.KinematicService/LoadTcpList";
             *   readonly requestType: "db.LoadListRequest";
             *   readonly responseType: "db.LoadListResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadTcpList.
             * @name lebai.kinematic.KinematicService#loadTcpList
             * @type {lebai.kinematic.KinematicService.LoadTcpList}
             */
            $Object.defineProperties(KinematicService.prototype.loadTcpList = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, KinematicService.prototype.loadTcpList, $root.lebai.db.LoadListRequest, $root.lebai.db.LoadListResponse, request, callback);
            }, {
                name: { value: "LoadTcpList" },
                path: { value: "/lebai.kinematic.KinematicService/LoadTcpList" },
                requestType: { value: "db.LoadListRequest" },
                responseType: { value: "db.LoadListResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.kinematic.KinematicService#setDh}.
             * @memberof lebai.kinematic.KinematicService
             * @typedef SetDhCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetDh.
             * @memberof lebai.kinematic.KinematicService
             * @typedef SetDh
             * @type {{
             *   (request: lebai.kinematic.IDhParams, callback: lebai.kinematic.KinematicService.SetDhCallback): void;
             *   (request: lebai.kinematic.IDhParams): Promise<google.protobuf.Empty>;
             *   readonly name: "SetDh";
             *   readonly path: "/lebai.kinematic.KinematicService/SetDh";
             *   readonly requestType: "DhParams";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetDh.
             * @name lebai.kinematic.KinematicService#setDh
             * @type {lebai.kinematic.KinematicService.SetDh}
             */
            $Object.defineProperties(KinematicService.prototype.setDh = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, KinematicService.prototype.setDh, $root.lebai.kinematic.DhParams, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetDh" },
                path: { value: "/lebai.kinematic.KinematicService/SetDh" },
                requestType: { value: "DhParams" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.kinematic.KinematicService#getDh}.
             * @memberof lebai.kinematic.KinematicService
             * @typedef GetDhCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.kinematic.DhParams} [response] DhParams
             */

            /**
             * Calls GetDh.
             * @memberof lebai.kinematic.KinematicService
             * @typedef GetDh
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.kinematic.KinematicService.GetDhCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.kinematic.DhParams>;
             *   readonly name: "GetDh";
             *   readonly path: "/lebai.kinematic.KinematicService/GetDh";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "DhParams";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetDh.
             * @name lebai.kinematic.KinematicService#getDh
             * @type {lebai.kinematic.KinematicService.GetDh}
             */
            $Object.defineProperties(KinematicService.prototype.getDh = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, KinematicService.prototype.getDh, $root.google.protobuf.Empty, $root.lebai.kinematic.DhParams, request, callback);
            }, {
                name: { value: "GetDh" },
                path: { value: "/lebai.kinematic.KinematicService/GetDh" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "DhParams" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.kinematic.KinematicService#setKinFactor}.
             * @memberof lebai.kinematic.KinematicService
             * @typedef SetKinFactorCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetKinFactor.
             * @memberof lebai.kinematic.KinematicService
             * @typedef SetKinFactor
             * @type {{
             *   (request: lebai.kinematic.IKinFactor, callback: lebai.kinematic.KinematicService.SetKinFactorCallback): void;
             *   (request: lebai.kinematic.IKinFactor): Promise<google.protobuf.Empty>;
             *   readonly name: "SetKinFactor";
             *   readonly path: "/lebai.kinematic.KinematicService/SetKinFactor";
             *   readonly requestType: "KinFactor";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetKinFactor.
             * @name lebai.kinematic.KinematicService#setKinFactor
             * @type {lebai.kinematic.KinematicService.SetKinFactor}
             */
            $Object.defineProperties(KinematicService.prototype.setKinFactor = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, KinematicService.prototype.setKinFactor, $root.lebai.kinematic.KinFactor, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetKinFactor" },
                path: { value: "/lebai.kinematic.KinematicService/SetKinFactor" },
                requestType: { value: "KinFactor" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.kinematic.KinematicService#getKinFactor}.
             * @memberof lebai.kinematic.KinematicService
             * @typedef GetKinFactorCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.kinematic.KinFactor} [response] KinFactor
             */

            /**
             * Calls GetKinFactor.
             * @memberof lebai.kinematic.KinematicService
             * @typedef GetKinFactor
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.kinematic.KinematicService.GetKinFactorCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.kinematic.KinFactor>;
             *   readonly name: "GetKinFactor";
             *   readonly path: "/lebai.kinematic.KinematicService/GetKinFactor";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "KinFactor";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetKinFactor.
             * @name lebai.kinematic.KinematicService#getKinFactor
             * @type {lebai.kinematic.KinematicService.GetKinFactor}
             */
            $Object.defineProperties(KinematicService.prototype.getKinFactor = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, KinematicService.prototype.getKinFactor, $root.google.protobuf.Empty, $root.lebai.kinematic.KinFactor, request, callback);
            }, {
                name: { value: "GetKinFactor" },
                path: { value: "/lebai.kinematic.KinematicService/GetKinFactor" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "KinFactor" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.kinematic.KinematicService#getKinData}.
             * @memberof lebai.kinematic.KinematicService
             * @typedef GetKinDataCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.kinematic.KinData} [response] KinData
             */

            /**
             * Calls GetKinData.
             * @memberof lebai.kinematic.KinematicService
             * @typedef GetKinData
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.kinematic.KinematicService.GetKinDataCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.kinematic.KinData>;
             *   readonly name: "GetKinData";
             *   readonly path: "/lebai.kinematic.KinematicService/GetKinData";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "KinData";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetKinData.
             * @name lebai.kinematic.KinematicService#getKinData
             * @type {lebai.kinematic.KinematicService.GetKinData}
             */
            $Object.defineProperties(KinematicService.prototype.getKinData = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, KinematicService.prototype.getKinData, $root.google.protobuf.Empty, $root.lebai.kinematic.KinData, request, callback);
            }, {
                name: { value: "GetKinData" },
                path: { value: "/lebai.kinematic.KinematicService/GetKinData" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "KinData" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.kinematic.KinematicService#subKinData}.
             * @memberof lebai.kinematic.KinematicService
             * @typedef SubKinDataCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.kinematic.KinData} [response] KinData
             */

            /**
             * Calls SubKinData.
             * @memberof lebai.kinematic.KinematicService
             * @typedef SubKinData
             * @type {{
             *   (request: lebai.ISubscribeRequest, callback: lebai.kinematic.KinematicService.SubKinDataCallback): void;
             *   (request: lebai.ISubscribeRequest): Promise<lebai.kinematic.KinData>;
             *   readonly name: "SubKinData";
             *   readonly path: "/lebai.kinematic.KinematicService/SubKinData";
             *   readonly requestType: "SubscribeRequest";
             *   readonly responseType: "KinData";
             *   readonly requestStream: undefined;
             *   readonly responseStream: true;
             * }}
             */

            /**
             * Calls SubKinData.
             * @name lebai.kinematic.KinematicService#subKinData
             * @type {lebai.kinematic.KinematicService.SubKinData}
             */
            $Object.defineProperties(KinematicService.prototype.subKinData = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, KinematicService.prototype.subKinData, $root.lebai.SubscribeRequest, $root.lebai.kinematic.KinData, request, callback);
            }, {
                name: { value: "SubKinData" },
                path: { value: "/lebai.kinematic.KinematicService/SubKinData" },
                requestType: { value: "SubscribeRequest" },
                responseType: { value: "KinData" },
                requestStream: { value: $undefined },
                responseStream: { value: true }
            });

            /**
             * Callback as used by {@link lebai.kinematic.KinematicService#calcFrame}.
             * @memberof lebai.kinematic.KinematicService
             * @typedef CalcFrameCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.posture.CartesianPose} [response] CartesianPose
             */

            /**
             * Calls CalcFrame.
             * @memberof lebai.kinematic.KinematicService
             * @typedef CalcFrame
             * @type {{
             *   (request: lebai.kinematic.ICalcFrameRequest, callback: lebai.kinematic.KinematicService.CalcFrameCallback): void;
             *   (request: lebai.kinematic.ICalcFrameRequest): Promise<lebai.posture.CartesianPose>;
             *   readonly name: "CalcFrame";
             *   readonly path: "/lebai.kinematic.KinematicService/CalcFrame";
             *   readonly requestType: "CalcFrameRequest";
             *   readonly responseType: "posture.CartesianPose";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls CalcFrame.
             * @name lebai.kinematic.KinematicService#calcFrame
             * @type {lebai.kinematic.KinematicService.CalcFrame}
             */
            $Object.defineProperties(KinematicService.prototype.calcFrame = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, KinematicService.prototype.calcFrame, $root.lebai.kinematic.CalcFrameRequest, $root.lebai.posture.CartesianPose, request, callback);
            }, {
                name: { value: "CalcFrame" },
                path: { value: "/lebai.kinematic.KinematicService/CalcFrame" },
                requestType: { value: "CalcFrameRequest" },
                responseType: { value: "posture.CartesianPose" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.kinematic.KinematicService#calcTcp}.
             * @memberof lebai.kinematic.KinematicService
             * @typedef CalcTcpCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.posture.CartesianPose} [response] CartesianPose
             */

            /**
             * Calls CalcTcp.
             * @memberof lebai.kinematic.KinematicService
             * @typedef CalcTcp
             * @type {{
             *   (request: lebai.kinematic.ICalcTcpRequest, callback: lebai.kinematic.KinematicService.CalcTcpCallback): void;
             *   (request: lebai.kinematic.ICalcTcpRequest): Promise<lebai.posture.CartesianPose>;
             *   readonly name: "CalcTcp";
             *   readonly path: "/lebai.kinematic.KinematicService/CalcTcp";
             *   readonly requestType: "CalcTcpRequest";
             *   readonly responseType: "posture.CartesianPose";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls CalcTcp.
             * @name lebai.kinematic.KinematicService#calcTcp
             * @type {lebai.kinematic.KinematicService.CalcTcp}
             */
            $Object.defineProperties(KinematicService.prototype.calcTcp = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, KinematicService.prototype.calcTcp, $root.lebai.kinematic.CalcTcpRequest, $root.lebai.posture.CartesianPose, request, callback);
            }, {
                name: { value: "CalcTcp" },
                path: { value: "/lebai.kinematic.KinematicService/CalcTcp" },
                requestType: { value: "CalcTcpRequest" },
                responseType: { value: "posture.CartesianPose" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.kinematic.KinematicService#setTcp}.
             * @memberof lebai.kinematic.KinematicService
             * @typedef SetTcpCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetTcp.
             * @memberof lebai.kinematic.KinematicService
             * @typedef SetTcp
             * @type {{
             *   (request: lebai.posture.ICartesianPose, callback: lebai.kinematic.KinematicService.SetTcpCallback): void;
             *   (request: lebai.posture.ICartesianPose): Promise<google.protobuf.Empty>;
             *   readonly name: "SetTcp";
             *   readonly path: "/lebai.kinematic.KinematicService/SetTcp";
             *   readonly requestType: "posture.CartesianPose";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetTcp.
             * @name lebai.kinematic.KinematicService#setTcp
             * @type {lebai.kinematic.KinematicService.SetTcp}
             */
            $Object.defineProperties(KinematicService.prototype.setTcp = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, KinematicService.prototype.setTcp, $root.lebai.posture.CartesianPose, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetTcp" },
                path: { value: "/lebai.kinematic.KinematicService/SetTcp" },
                requestType: { value: "posture.CartesianPose" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.kinematic.KinematicService#getTcp}.
             * @memberof lebai.kinematic.KinematicService
             * @typedef GetTcpCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.posture.CartesianPose} [response] CartesianPose
             */

            /**
             * Calls GetTcp.
             * @memberof lebai.kinematic.KinematicService
             * @typedef GetTcp
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.kinematic.KinematicService.GetTcpCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.posture.CartesianPose>;
             *   readonly name: "GetTcp";
             *   readonly path: "/lebai.kinematic.KinematicService/GetTcp";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "posture.CartesianPose";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetTcp.
             * @name lebai.kinematic.KinematicService#getTcp
             * @type {lebai.kinematic.KinematicService.GetTcp}
             */
            $Object.defineProperties(KinematicService.prototype.getTcp = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, KinematicService.prototype.getTcp, $root.google.protobuf.Empty, $root.lebai.posture.CartesianPose, request, callback);
            }, {
                name: { value: "GetTcp" },
                path: { value: "/lebai.kinematic.KinematicService/GetTcp" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "posture.CartesianPose" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return KinematicService;
        })();

        return kinematic;
    })();

    lebai.led = (function() {

        /**
         * Namespace led.
         * @memberof lebai
         * @namespace
         */
        var led = {};

        /**
         * LedMode enum.
         * @name lebai.led.LedMode
         * @enum {number}
         * @property {number} HOLD_LED=0 HOLD_LED value
         * @property {number} CLOSE_LED=1 CLOSE_LED value
         * @property {number} OPEN_LED=2 OPEN_LED value
         * @property {number} BREATH=3 BREATH value
         * @property {number} FOUR=4 FOUR value
         * @property {number} WATER=5 WATER value
         * @property {number} BLINK=6 BLINK value
         */
        led.LedMode = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "HOLD_LED"] = 0;
            values[valuesById[1] = "CLOSE_LED"] = 1;
            values[valuesById[2] = "OPEN_LED"] = 2;
            values[valuesById[3] = "BREATH"] = 3;
            values[valuesById[4] = "FOUR"] = 4;
            values[valuesById[5] = "WATER"] = 5;
            values[valuesById[6] = "BLINK"] = 6;
            return values;
        })();

        /**
         * LedSpeed enum.
         * @name lebai.led.LedSpeed
         * @enum {number}
         * @property {number} HOLD_LED_SPEED=0 HOLD_LED_SPEED value
         * @property {number} FAST=1 FAST value
         * @property {number} NORMAL=2 NORMAL value
         * @property {number} SLOW=3 SLOW value
         */
        led.LedSpeed = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "HOLD_LED_SPEED"] = 0;
            values[valuesById[1] = "FAST"] = 1;
            values[valuesById[2] = "NORMAL"] = 2;
            values[valuesById[3] = "SLOW"] = 3;
            return values;
        })();

        /**
         * LedColor enum.
         * @name lebai.led.LedColor
         * @enum {number}
         * @property {number} RED=0 RED value
         * @property {number} GREEN=1 GREEN value
         * @property {number} BLUE=2 BLUE value
         * @property {number} PINK=3 PINK value
         * @property {number} YELLOW=4 YELLOW value
         * @property {number} CYAN=5 CYAN value
         * @property {number} GRAY=6 GRAY value
         * @property {number} BROWN=7 BROWN value
         * @property {number} ORANGE=8 ORANGE value
         * @property {number} GOLD=9 GOLD value
         * @property {number} INDIGO=10 INDIGO value
         * @property {number} LIGHT_SKY_BLUE=11 LIGHT_SKY_BLUE value
         * @property {number} DARK_VIOLET=12 DARK_VIOLET value
         * @property {number} CHOCOLATE=13 CHOCOLATE value
         * @property {number} LIGHT_RED=14 LIGHT_RED value
         * @property {number} WHITE=15 WHITE value
         */
        led.LedColor = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "RED"] = 0;
            values[valuesById[1] = "GREEN"] = 1;
            values[valuesById[2] = "BLUE"] = 2;
            values[valuesById[3] = "PINK"] = 3;
            values[valuesById[4] = "YELLOW"] = 4;
            values[valuesById[5] = "CYAN"] = 5;
            values[valuesById[6] = "GRAY"] = 6;
            values[valuesById[7] = "BROWN"] = 7;
            values[valuesById[8] = "ORANGE"] = 8;
            values[valuesById[9] = "GOLD"] = 9;
            values[valuesById[10] = "INDIGO"] = 10;
            values[valuesById[11] = "LIGHT_SKY_BLUE"] = 11;
            values[valuesById[12] = "DARK_VIOLET"] = 12;
            values[valuesById[13] = "CHOCOLATE"] = 13;
            values[valuesById[14] = "LIGHT_RED"] = 14;
            values[valuesById[15] = "WHITE"] = 15;
            return values;
        })();

        led.LedData = (function() {

            /**
             * Properties of a LedData.
             * @typedef {Object} lebai.led.LedData.$Properties
             * @property {lebai.led.LedMode|null} [mode] LedData mode
             * @property {lebai.led.LedSpeed|null} [speed] LedData speed
             * @property {Array.<lebai.led.LedColor>|null} [colors] LedData colors
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LedData.
             * @memberof lebai.led
             * @interface ILedData
             * @augments lebai.led.LedData.$Properties
             * @deprecated Use lebai.led.LedData.$Properties instead.
             */

            /**
             * Shape of a LedData.
             * @typedef {lebai.led.LedData.$Properties} lebai.led.LedData.$Shape
             */

            /**
             * Constructs a new LedData.
             * @memberof lebai.led
             * @classdesc Represents a LedData.
             * @constructor
             * @param {lebai.led.LedData.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LedData = function (properties) {
                this.colors = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LedData mode.
             * @member {lebai.led.LedMode} mode
             * @memberof lebai.led.LedData
             * @instance
             */
            LedData.prototype.mode = 0;

            /**
             * LedData speed.
             * @member {lebai.led.LedSpeed} speed
             * @memberof lebai.led.LedData
             * @instance
             */
            LedData.prototype.speed = 0;

            /**
             * LedData colors.
             * @member {Array.<lebai.led.LedColor>} colors
             * @memberof lebai.led.LedData
             * @instance
             */
            LedData.prototype.colors = $util.emptyArray;

            /**
             * Creates a new LedData instance using the specified properties.
             * @function create
             * @memberof lebai.led.LedData
             * @static
             * @param {lebai.led.LedData.$Properties=} [properties] Properties to set
             * @returns {lebai.led.LedData} LedData instance
             * @type {{
             *   (properties: lebai.led.LedData.$Shape): lebai.led.LedData & lebai.led.LedData.$Shape;
             *   (properties?: lebai.led.LedData.$Properties): lebai.led.LedData;
             * }}
             */
            LedData.create = function(properties) {
                return new LedData(properties);
            };

            /**
             * Encodes the specified LedData message. Does not implicitly {@link lebai.led.LedData.verify|verify} messages.
             * @function encode
             * @memberof lebai.led.LedData
             * @static
             * @param {lebai.led.LedData.$Properties} message LedData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LedData.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.mode != null && $Object.hasOwnProperty.call(message, "mode") && message.mode !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.mode);
                if (message.speed != null && $Object.hasOwnProperty.call(message, "speed") && message.speed !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.speed);
                if (message.colors != null && message.colors.length)
                    writer.uint32(/* id 3, wireType 2 =*/26).int32s(message.colors);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LedData message, length delimited. Does not implicitly {@link lebai.led.LedData.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.led.LedData
             * @static
             * @param {lebai.led.LedData.$Properties} message LedData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LedData.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LedData message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.led.LedData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.led.LedData & lebai.led.LedData.$Shape} LedData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LedData.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.led.LedData();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.mode = value;
                            else
                                delete message.mode;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.speed = value;
                            else
                                delete message.speed;
                            continue;
                        }
                    case 3: {
                            if (wireType === 2) {
                                if (!(message.colors && message.colors.length))
                                    message.colors = [];
                                reader.int32s(message.colors);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.colors && message.colors.length))
                                message.colors = [];
                            message.colors.push(reader.int32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LedData message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.led.LedData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.led.LedData & lebai.led.LedData.$Shape} LedData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LedData.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LedData message.
             * @function verify
             * @memberof lebai.led.LedData
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LedData.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.mode != null && $Object.hasOwnProperty.call(message, "mode"))
                    if (typeof message.mode !== "number" || (message.mode | 0) !== message.mode)
                        return "mode: enum value expected";
                if (message.speed != null && $Object.hasOwnProperty.call(message, "speed"))
                    if (typeof message.speed !== "number" || (message.speed | 0) !== message.speed)
                        return "speed: enum value expected";
                if (message.colors != null && $Object.hasOwnProperty.call(message, "colors")) {
                    if (!$Array.isArray(message.colors))
                        return "colors: array expected";
                    for (var i = 0; i < message.colors.length; ++i)
                        if (typeof message.colors[i] !== "number" || (message.colors[i] | 0) !== message.colors[i])
                            return "colors: enum value[] expected";
                }
                return null;
            };

            /**
             * Creates a LedData message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.led.LedData
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.led.LedData} LedData
             */
            LedData.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.led.LedData)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.led.LedData: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.led.LedData();
                if (object.mode !== 0 && (typeof object.mode !== "string" || $root.lebai.led.LedMode[object.mode] !== 0))
                    switch (object.mode) {
                    case "HOLD_LED":
                    case 0:
                        message.mode = 0;
                        break;
                    case "CLOSE_LED":
                    case 1:
                        message.mode = 1;
                        break;
                    case "OPEN_LED":
                    case 2:
                        message.mode = 2;
                        break;
                    case "BREATH":
                    case 3:
                        message.mode = 3;
                        break;
                    case "FOUR":
                    case 4:
                        message.mode = 4;
                        break;
                    case "WATER":
                    case 5:
                        message.mode = 5;
                        break;
                    case "BLINK":
                    case 6:
                        message.mode = 6;
                        break;
                    default:
                        if (typeof object.mode === "number" && (object.mode | 0) === object.mode)
                            message.mode = object.mode;
                    }
                if (object.speed !== 0 && (typeof object.speed !== "string" || $root.lebai.led.LedSpeed[object.speed] !== 0))
                    switch (object.speed) {
                    case "HOLD_LED_SPEED":
                    case 0:
                        message.speed = 0;
                        break;
                    case "FAST":
                    case 1:
                        message.speed = 1;
                        break;
                    case "NORMAL":
                    case 2:
                        message.speed = 2;
                        break;
                    case "SLOW":
                    case 3:
                        message.speed = 3;
                        break;
                    default:
                        if (typeof object.speed === "number" && (object.speed | 0) === object.speed)
                            message.speed = object.speed;
                    }
                if (object.colors) {
                    if (!$Array.isArray(object.colors))
                        throw $TypeError(".lebai.led.LedData.colors: array expected");
                    message.colors = [];
                    for (var i = 0; i < object.colors.length; ++i)
                        switch (object.colors[i]) {
                        case "RED":
                        case 0:
                            message.colors[message.colors.length] = 0;
                            break;
                        case "GREEN":
                        case 1:
                            message.colors[message.colors.length] = 1;
                            break;
                        case "BLUE":
                        case 2:
                            message.colors[message.colors.length] = 2;
                            break;
                        case "PINK":
                        case 3:
                            message.colors[message.colors.length] = 3;
                            break;
                        case "YELLOW":
                        case 4:
                            message.colors[message.colors.length] = 4;
                            break;
                        case "CYAN":
                        case 5:
                            message.colors[message.colors.length] = 5;
                            break;
                        case "GRAY":
                        case 6:
                            message.colors[message.colors.length] = 6;
                            break;
                        case "BROWN":
                        case 7:
                            message.colors[message.colors.length] = 7;
                            break;
                        case "ORANGE":
                        case 8:
                            message.colors[message.colors.length] = 8;
                            break;
                        case "GOLD":
                        case 9:
                            message.colors[message.colors.length] = 9;
                            break;
                        case "INDIGO":
                        case 10:
                            message.colors[message.colors.length] = 10;
                            break;
                        case "LIGHT_SKY_BLUE":
                        case 11:
                            message.colors[message.colors.length] = 11;
                            break;
                        case "DARK_VIOLET":
                        case 12:
                            message.colors[message.colors.length] = 12;
                            break;
                        case "CHOCOLATE":
                        case 13:
                            message.colors[message.colors.length] = 13;
                            break;
                        case "LIGHT_RED":
                        case 14:
                            message.colors[message.colors.length] = 14;
                            break;
                        case "WHITE":
                        case 15:
                            message.colors[message.colors.length] = 15;
                            break;
                        default:
                            if (typeof object.colors[i] === "number" && (object.colors[i] | 0) === object.colors[i])
                                message.colors[message.colors.length] = object.colors[i];
                        }
                }
                return message;
            };

            /**
             * Creates a plain object from a LedData message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.led.LedData
             * @static
             * @param {lebai.led.LedData} message LedData
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LedData.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.colors = [];
                if (options.defaults) {
                    object.mode = options.enums === $String ? "HOLD_LED" : 0;
                    object.speed = options.enums === $String ? "HOLD_LED_SPEED" : 0;
                }
                if (message.mode != null && $Object.hasOwnProperty.call(message, "mode"))
                    object.mode = options.enums === $String ? $root.lebai.led.LedMode[message.mode] === $undefined ? message.mode : $root.lebai.led.LedMode[message.mode] : message.mode;
                if (message.speed != null && $Object.hasOwnProperty.call(message, "speed"))
                    object.speed = options.enums === $String ? $root.lebai.led.LedSpeed[message.speed] === $undefined ? message.speed : $root.lebai.led.LedSpeed[message.speed] : message.speed;
                if (message.colors && message.colors.length) {
                    object.colors = $Array(message.colors.length);
                    for (var j = 0; j < message.colors.length; ++j)
                        object.colors[j] = options.enums === $String ? $root.lebai.led.LedColor[message.colors[j]] === $undefined ? message.colors[j] : $root.lebai.led.LedColor[message.colors[j]] : message.colors[j];
                }
                return object;
            };

            /**
             * Converts this LedData to JSON.
             * @function toJSON
             * @memberof lebai.led.LedData
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LedData.prototype.toJSON = function() {
                return LedData.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LedData
             * @function getTypeUrl
             * @memberof lebai.led.LedData
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LedData.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.led.LedData";
            };

            return LedData;
        })();

        /**
         * FanMode enum.
         * @name lebai.led.FanMode
         * @enum {number}
         * @property {number} HOLD_FAN=0 HOLD_FAN value
         * @property {number} CLOSE_FAN=1 CLOSE_FAN value
         * @property {number} OPEN_FAN=2 OPEN_FAN value
         */
        led.FanMode = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "HOLD_FAN"] = 0;
            values[valuesById[1] = "CLOSE_FAN"] = 1;
            values[valuesById[2] = "OPEN_FAN"] = 2;
            return values;
        })();

        led.FanData = (function() {

            /**
             * Properties of a FanData.
             * @typedef {Object} lebai.led.FanData.$Properties
             * @property {lebai.led.FanMode|null} [mode] FanData mode
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a FanData.
             * @memberof lebai.led
             * @interface IFanData
             * @augments lebai.led.FanData.$Properties
             * @deprecated Use lebai.led.FanData.$Properties instead.
             */

            /**
             * Shape of a FanData.
             * @typedef {lebai.led.FanData.$Properties} lebai.led.FanData.$Shape
             */

            /**
             * Constructs a new FanData.
             * @memberof lebai.led
             * @classdesc Represents a FanData.
             * @constructor
             * @param {lebai.led.FanData.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var FanData = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * FanData mode.
             * @member {lebai.led.FanMode} mode
             * @memberof lebai.led.FanData
             * @instance
             */
            FanData.prototype.mode = 0;

            /**
             * Creates a new FanData instance using the specified properties.
             * @function create
             * @memberof lebai.led.FanData
             * @static
             * @param {lebai.led.FanData.$Properties=} [properties] Properties to set
             * @returns {lebai.led.FanData} FanData instance
             * @type {{
             *   (properties: lebai.led.FanData.$Shape): lebai.led.FanData & lebai.led.FanData.$Shape;
             *   (properties?: lebai.led.FanData.$Properties): lebai.led.FanData;
             * }}
             */
            FanData.create = function(properties) {
                return new FanData(properties);
            };

            /**
             * Encodes the specified FanData message. Does not implicitly {@link lebai.led.FanData.verify|verify} messages.
             * @function encode
             * @memberof lebai.led.FanData
             * @static
             * @param {lebai.led.FanData.$Properties} message FanData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FanData.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.mode != null && $Object.hasOwnProperty.call(message, "mode") && message.mode !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.mode);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FanData message, length delimited. Does not implicitly {@link lebai.led.FanData.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.led.FanData
             * @static
             * @param {lebai.led.FanData.$Properties} message FanData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FanData.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a FanData message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.led.FanData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.led.FanData & lebai.led.FanData.$Shape} FanData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FanData.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.led.FanData();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.mode = value;
                            else
                                delete message.mode;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a FanData message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.led.FanData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.led.FanData & lebai.led.FanData.$Shape} FanData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FanData.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FanData message.
             * @function verify
             * @memberof lebai.led.FanData
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FanData.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.mode != null && $Object.hasOwnProperty.call(message, "mode"))
                    if (typeof message.mode !== "number" || (message.mode | 0) !== message.mode)
                        return "mode: enum value expected";
                return null;
            };

            /**
             * Creates a FanData message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.led.FanData
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.led.FanData} FanData
             */
            FanData.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.led.FanData)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.led.FanData: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.led.FanData();
                if (object.mode !== 0 && (typeof object.mode !== "string" || $root.lebai.led.FanMode[object.mode] !== 0))
                    switch (object.mode) {
                    case "HOLD_FAN":
                    case 0:
                        message.mode = 0;
                        break;
                    case "CLOSE_FAN":
                    case 1:
                        message.mode = 1;
                        break;
                    case "OPEN_FAN":
                    case 2:
                        message.mode = 2;
                        break;
                    default:
                        if (typeof object.mode === "number" && (object.mode | 0) === object.mode)
                            message.mode = object.mode;
                    }
                return message;
            };

            /**
             * Creates a plain object from a FanData message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.led.FanData
             * @static
             * @param {lebai.led.FanData} message FanData
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FanData.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.mode = options.enums === $String ? "HOLD_FAN" : 0;
                if (message.mode != null && $Object.hasOwnProperty.call(message, "mode"))
                    object.mode = options.enums === $String ? $root.lebai.led.FanMode[message.mode] === $undefined ? message.mode : $root.lebai.led.FanMode[message.mode] : message.mode;
                return object;
            };

            /**
             * Converts this FanData to JSON.
             * @function toJSON
             * @memberof lebai.led.FanData
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FanData.prototype.toJSON = function() {
                return FanData.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for FanData
             * @function getTypeUrl
             * @memberof lebai.led.FanData
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            FanData.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.led.FanData";
            };

            return FanData;
        })();

        /**
         * VoiceKind enum.
         * @name lebai.led.VoiceKind
         * @enum {number}
         * @property {number} OFF=0 OFF value
         * @property {number} BOOTING=1 BOOTING value
         * @property {number} STOPING=2 STOPING value
         * @property {number} COLLISION_DETECTED=3 COLLISION_DETECTED value
         * @property {number} UPGRADE=4 UPGRADE value
         * @property {number} TEACH_MODE_ON=5 TEACH_MODE_ON value
         * @property {number} TEACH_MODE_OFF=6 TEACH_MODE_OFF value
         * @property {number} FINE_TUNNING_ON=7 FINE_TUNNING_ON value
         * @property {number} FINE_TUNNING_OFF=8 FINE_TUNNING_OFF value
         * @property {number} FINE_TUNNING_CHANGE=9 FINE_TUNNING_CHANGE value
         * @property {number} BORING=10 BORING value
         * @property {number} CUSTOM1=11 CUSTOM1 value
         * @property {number} CUSTOM2=12 CUSTOM2 value
         * @property {number} CUSTOM3=13 CUSTOM3 value
         * @property {number} CUSTOM4=14 CUSTOM4 value
         * @property {number} CUSTOM5=15 CUSTOM5 value
         */
        led.VoiceKind = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "OFF"] = 0;
            values[valuesById[1] = "BOOTING"] = 1;
            values[valuesById[2] = "STOPING"] = 2;
            values[valuesById[3] = "COLLISION_DETECTED"] = 3;
            values[valuesById[4] = "UPGRADE"] = 4;
            values[valuesById[5] = "TEACH_MODE_ON"] = 5;
            values[valuesById[6] = "TEACH_MODE_OFF"] = 6;
            values[valuesById[7] = "FINE_TUNNING_ON"] = 7;
            values[valuesById[8] = "FINE_TUNNING_OFF"] = 8;
            values[valuesById[9] = "FINE_TUNNING_CHANGE"] = 9;
            values[valuesById[10] = "BORING"] = 10;
            values[valuesById[11] = "CUSTOM1"] = 11;
            values[valuesById[12] = "CUSTOM2"] = 12;
            values[valuesById[13] = "CUSTOM3"] = 13;
            values[valuesById[14] = "CUSTOM4"] = 14;
            values[valuesById[15] = "CUSTOM5"] = 15;
            return values;
        })();

        /**
         * Volume enum.
         * @name lebai.led.Volume
         * @enum {number}
         * @property {number} MUTE=0 MUTE value
         * @property {number} LOW=1 LOW value
         * @property {number} MID=2 MID value
         * @property {number} HIGH=3 HIGH value
         */
        led.Volume = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "MUTE"] = 0;
            values[valuesById[1] = "LOW"] = 1;
            values[valuesById[2] = "MID"] = 2;
            values[valuesById[3] = "HIGH"] = 3;
            return values;
        })();

        led.VoiceData = (function() {

            /**
             * Properties of a VoiceData.
             * @typedef {Object} lebai.led.VoiceData.$Properties
             * @property {lebai.led.VoiceKind|null} [voice] VoiceData voice
             * @property {lebai.led.Volume|null} [volume] VoiceData volume
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a VoiceData.
             * @memberof lebai.led
             * @interface IVoiceData
             * @augments lebai.led.VoiceData.$Properties
             * @deprecated Use lebai.led.VoiceData.$Properties instead.
             */

            /**
             * Shape of a VoiceData.
             * @typedef {lebai.led.VoiceData.$Properties} lebai.led.VoiceData.$Shape
             */

            /**
             * Constructs a new VoiceData.
             * @memberof lebai.led
             * @classdesc Represents a VoiceData.
             * @constructor
             * @param {lebai.led.VoiceData.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var VoiceData = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * VoiceData voice.
             * @member {lebai.led.VoiceKind} voice
             * @memberof lebai.led.VoiceData
             * @instance
             */
            VoiceData.prototype.voice = 0;

            /**
             * VoiceData volume.
             * @member {lebai.led.Volume} volume
             * @memberof lebai.led.VoiceData
             * @instance
             */
            VoiceData.prototype.volume = 0;

            /**
             * Creates a new VoiceData instance using the specified properties.
             * @function create
             * @memberof lebai.led.VoiceData
             * @static
             * @param {lebai.led.VoiceData.$Properties=} [properties] Properties to set
             * @returns {lebai.led.VoiceData} VoiceData instance
             * @type {{
             *   (properties: lebai.led.VoiceData.$Shape): lebai.led.VoiceData & lebai.led.VoiceData.$Shape;
             *   (properties?: lebai.led.VoiceData.$Properties): lebai.led.VoiceData;
             * }}
             */
            VoiceData.create = function(properties) {
                return new VoiceData(properties);
            };

            /**
             * Encodes the specified VoiceData message. Does not implicitly {@link lebai.led.VoiceData.verify|verify} messages.
             * @function encode
             * @memberof lebai.led.VoiceData
             * @static
             * @param {lebai.led.VoiceData.$Properties} message VoiceData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            VoiceData.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.voice != null && $Object.hasOwnProperty.call(message, "voice") && message.voice !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.voice);
                if (message.volume != null && $Object.hasOwnProperty.call(message, "volume") && message.volume !== 0)
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.volume);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified VoiceData message, length delimited. Does not implicitly {@link lebai.led.VoiceData.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.led.VoiceData
             * @static
             * @param {lebai.led.VoiceData.$Properties} message VoiceData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            VoiceData.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a VoiceData message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.led.VoiceData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.led.VoiceData & lebai.led.VoiceData.$Shape} VoiceData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            VoiceData.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.led.VoiceData();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.voice = value;
                            else
                                delete message.voice;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.volume = value;
                            else
                                delete message.volume;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a VoiceData message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.led.VoiceData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.led.VoiceData & lebai.led.VoiceData.$Shape} VoiceData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            VoiceData.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a VoiceData message.
             * @function verify
             * @memberof lebai.led.VoiceData
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            VoiceData.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.voice != null && $Object.hasOwnProperty.call(message, "voice"))
                    if (typeof message.voice !== "number" || (message.voice | 0) !== message.voice)
                        return "voice: enum value expected";
                if (message.volume != null && $Object.hasOwnProperty.call(message, "volume"))
                    if (typeof message.volume !== "number" || (message.volume | 0) !== message.volume)
                        return "volume: enum value expected";
                return null;
            };

            /**
             * Creates a VoiceData message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.led.VoiceData
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.led.VoiceData} VoiceData
             */
            VoiceData.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.led.VoiceData)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.led.VoiceData: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.led.VoiceData();
                if (object.voice !== 0 && (typeof object.voice !== "string" || $root.lebai.led.VoiceKind[object.voice] !== 0))
                    switch (object.voice) {
                    case "OFF":
                    case 0:
                        message.voice = 0;
                        break;
                    case "BOOTING":
                    case 1:
                        message.voice = 1;
                        break;
                    case "STOPING":
                    case 2:
                        message.voice = 2;
                        break;
                    case "COLLISION_DETECTED":
                    case 3:
                        message.voice = 3;
                        break;
                    case "UPGRADE":
                    case 4:
                        message.voice = 4;
                        break;
                    case "TEACH_MODE_ON":
                    case 5:
                        message.voice = 5;
                        break;
                    case "TEACH_MODE_OFF":
                    case 6:
                        message.voice = 6;
                        break;
                    case "FINE_TUNNING_ON":
                    case 7:
                        message.voice = 7;
                        break;
                    case "FINE_TUNNING_OFF":
                    case 8:
                        message.voice = 8;
                        break;
                    case "FINE_TUNNING_CHANGE":
                    case 9:
                        message.voice = 9;
                        break;
                    case "BORING":
                    case 10:
                        message.voice = 10;
                        break;
                    case "CUSTOM1":
                    case 11:
                        message.voice = 11;
                        break;
                    case "CUSTOM2":
                    case 12:
                        message.voice = 12;
                        break;
                    case "CUSTOM3":
                    case 13:
                        message.voice = 13;
                        break;
                    case "CUSTOM4":
                    case 14:
                        message.voice = 14;
                        break;
                    case "CUSTOM5":
                    case 15:
                        message.voice = 15;
                        break;
                    default:
                        if (typeof object.voice === "number" && (object.voice | 0) === object.voice)
                            message.voice = object.voice;
                    }
                if (object.volume !== 0 && (typeof object.volume !== "string" || $root.lebai.led.Volume[object.volume] !== 0))
                    switch (object.volume) {
                    case "MUTE":
                    case 0:
                        message.volume = 0;
                        break;
                    case "LOW":
                    case 1:
                        message.volume = 1;
                        break;
                    case "MID":
                    case 2:
                        message.volume = 2;
                        break;
                    case "HIGH":
                    case 3:
                        message.volume = 3;
                        break;
                    default:
                        if (typeof object.volume === "number" && (object.volume | 0) === object.volume)
                            message.volume = object.volume;
                    }
                return message;
            };

            /**
             * Creates a plain object from a VoiceData message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.led.VoiceData
             * @static
             * @param {lebai.led.VoiceData} message VoiceData
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            VoiceData.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.voice = options.enums === $String ? "OFF" : 0;
                    object.volume = options.enums === $String ? "MUTE" : 0;
                }
                if (message.voice != null && $Object.hasOwnProperty.call(message, "voice"))
                    object.voice = options.enums === $String ? $root.lebai.led.VoiceKind[message.voice] === $undefined ? message.voice : $root.lebai.led.VoiceKind[message.voice] : message.voice;
                if (message.volume != null && $Object.hasOwnProperty.call(message, "volume"))
                    object.volume = options.enums === $String ? $root.lebai.led.Volume[message.volume] === $undefined ? message.volume : $root.lebai.led.Volume[message.volume] : message.volume;
                return object;
            };

            /**
             * Converts this VoiceData to JSON.
             * @function toJSON
             * @memberof lebai.led.VoiceData
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            VoiceData.prototype.toJSON = function() {
                return VoiceData.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for VoiceData
             * @function getTypeUrl
             * @memberof lebai.led.VoiceData
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            VoiceData.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.led.VoiceData";
            };

            return VoiceData;
        })();

        led.LedStyle = (function() {

            /**
             * Properties of a LedStyle.
             * @typedef {Object} lebai.led.LedStyle.$Properties
             * @property {lebai.led.LedData.$Properties|null} [led] LedStyle led
             * @property {lebai.led.VoiceKind|null} [voice] LedStyle voice
             * @property {lebai.led.Volume|null} [volume] LedStyle volume
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LedStyle.
             * @memberof lebai.led
             * @interface ILedStyle
             * @augments lebai.led.LedStyle.$Properties
             * @deprecated Use lebai.led.LedStyle.$Properties instead.
             */

            /**
             * Shape of a LedStyle.
             * @typedef {lebai.led.LedStyle.$Properties} lebai.led.LedStyle.$Shape
             */

            /**
             * Constructs a new LedStyle.
             * @memberof lebai.led
             * @classdesc Represents a LedStyle.
             * @constructor
             * @param {lebai.led.LedStyle.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LedStyle = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LedStyle led.
             * @member {lebai.led.LedData.$Properties|null|undefined} led
             * @memberof lebai.led.LedStyle
             * @instance
             */
            LedStyle.prototype.led = null;

            /**
             * LedStyle voice.
             * @member {lebai.led.VoiceKind} voice
             * @memberof lebai.led.LedStyle
             * @instance
             */
            LedStyle.prototype.voice = 0;

            /**
             * LedStyle volume.
             * @member {lebai.led.Volume} volume
             * @memberof lebai.led.LedStyle
             * @instance
             */
            LedStyle.prototype.volume = 0;

            /**
             * Creates a new LedStyle instance using the specified properties.
             * @function create
             * @memberof lebai.led.LedStyle
             * @static
             * @param {lebai.led.LedStyle.$Properties=} [properties] Properties to set
             * @returns {lebai.led.LedStyle} LedStyle instance
             * @type {{
             *   (properties: lebai.led.LedStyle.$Shape): lebai.led.LedStyle & lebai.led.LedStyle.$Shape;
             *   (properties?: lebai.led.LedStyle.$Properties): lebai.led.LedStyle;
             * }}
             */
            LedStyle.create = function(properties) {
                return new LedStyle(properties);
            };

            /**
             * Encodes the specified LedStyle message. Does not implicitly {@link lebai.led.LedStyle.verify|verify} messages.
             * @function encode
             * @memberof lebai.led.LedStyle
             * @static
             * @param {lebai.led.LedStyle.$Properties} message LedStyle message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LedStyle.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.led != null && $Object.hasOwnProperty.call(message, "led"))
                    $root.lebai.led.LedData.encode(message.led, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.voice != null && $Object.hasOwnProperty.call(message, "voice") && message.voice !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.voice);
                if (message.volume != null && $Object.hasOwnProperty.call(message, "volume") && message.volume !== 0)
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.volume);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LedStyle message, length delimited. Does not implicitly {@link lebai.led.LedStyle.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.led.LedStyle
             * @static
             * @param {lebai.led.LedStyle.$Properties} message LedStyle message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LedStyle.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LedStyle message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.led.LedStyle
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.led.LedStyle & lebai.led.LedStyle.$Shape} LedStyle
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LedStyle.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.led.LedStyle();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.led = $root.lebai.led.LedData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.led);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.voice = value;
                            else
                                delete message.voice;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.volume = value;
                            else
                                delete message.volume;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LedStyle message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.led.LedStyle
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.led.LedStyle & lebai.led.LedStyle.$Shape} LedStyle
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LedStyle.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LedStyle message.
             * @function verify
             * @memberof lebai.led.LedStyle
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LedStyle.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.led != null && $Object.hasOwnProperty.call(message, "led")) {
                    var error = $root.lebai.led.LedData.verify(message.led, _depth + 1);
                    if (error)
                        return "led." + error;
                }
                if (message.voice != null && $Object.hasOwnProperty.call(message, "voice"))
                    if (typeof message.voice !== "number" || (message.voice | 0) !== message.voice)
                        return "voice: enum value expected";
                if (message.volume != null && $Object.hasOwnProperty.call(message, "volume"))
                    if (typeof message.volume !== "number" || (message.volume | 0) !== message.volume)
                        return "volume: enum value expected";
                return null;
            };

            /**
             * Creates a LedStyle message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.led.LedStyle
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.led.LedStyle} LedStyle
             */
            LedStyle.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.led.LedStyle)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.led.LedStyle: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.led.LedStyle();
                if (object.led != null) {
                    if (!$util.isObject(object.led))
                        throw $TypeError(".lebai.led.LedStyle.led: object expected");
                    message.led = $root.lebai.led.LedData.fromObject(object.led, _depth + 1);
                }
                if (object.voice !== 0 && (typeof object.voice !== "string" || $root.lebai.led.VoiceKind[object.voice] !== 0))
                    switch (object.voice) {
                    case "OFF":
                    case 0:
                        message.voice = 0;
                        break;
                    case "BOOTING":
                    case 1:
                        message.voice = 1;
                        break;
                    case "STOPING":
                    case 2:
                        message.voice = 2;
                        break;
                    case "COLLISION_DETECTED":
                    case 3:
                        message.voice = 3;
                        break;
                    case "UPGRADE":
                    case 4:
                        message.voice = 4;
                        break;
                    case "TEACH_MODE_ON":
                    case 5:
                        message.voice = 5;
                        break;
                    case "TEACH_MODE_OFF":
                    case 6:
                        message.voice = 6;
                        break;
                    case "FINE_TUNNING_ON":
                    case 7:
                        message.voice = 7;
                        break;
                    case "FINE_TUNNING_OFF":
                    case 8:
                        message.voice = 8;
                        break;
                    case "FINE_TUNNING_CHANGE":
                    case 9:
                        message.voice = 9;
                        break;
                    case "BORING":
                    case 10:
                        message.voice = 10;
                        break;
                    case "CUSTOM1":
                    case 11:
                        message.voice = 11;
                        break;
                    case "CUSTOM2":
                    case 12:
                        message.voice = 12;
                        break;
                    case "CUSTOM3":
                    case 13:
                        message.voice = 13;
                        break;
                    case "CUSTOM4":
                    case 14:
                        message.voice = 14;
                        break;
                    case "CUSTOM5":
                    case 15:
                        message.voice = 15;
                        break;
                    default:
                        if (typeof object.voice === "number" && (object.voice | 0) === object.voice)
                            message.voice = object.voice;
                    }
                if (object.volume !== 0 && (typeof object.volume !== "string" || $root.lebai.led.Volume[object.volume] !== 0))
                    switch (object.volume) {
                    case "MUTE":
                    case 0:
                        message.volume = 0;
                        break;
                    case "LOW":
                    case 1:
                        message.volume = 1;
                        break;
                    case "MID":
                    case 2:
                        message.volume = 2;
                        break;
                    case "HIGH":
                    case 3:
                        message.volume = 3;
                        break;
                    default:
                        if (typeof object.volume === "number" && (object.volume | 0) === object.volume)
                            message.volume = object.volume;
                    }
                return message;
            };

            /**
             * Creates a plain object from a LedStyle message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.led.LedStyle
             * @static
             * @param {lebai.led.LedStyle} message LedStyle
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LedStyle.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.led = null;
                    object.voice = options.enums === $String ? "OFF" : 0;
                    object.volume = options.enums === $String ? "MUTE" : 0;
                }
                if (message.led != null && $Object.hasOwnProperty.call(message, "led"))
                    object.led = $root.lebai.led.LedData.toObject(message.led, options, _depth + 1);
                if (message.voice != null && $Object.hasOwnProperty.call(message, "voice"))
                    object.voice = options.enums === $String ? $root.lebai.led.VoiceKind[message.voice] === $undefined ? message.voice : $root.lebai.led.VoiceKind[message.voice] : message.voice;
                if (message.volume != null && $Object.hasOwnProperty.call(message, "volume"))
                    object.volume = options.enums === $String ? $root.lebai.led.Volume[message.volume] === $undefined ? message.volume : $root.lebai.led.Volume[message.volume] : message.volume;
                return object;
            };

            /**
             * Converts this LedStyle to JSON.
             * @function toJSON
             * @memberof lebai.led.LedStyle
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LedStyle.prototype.toJSON = function() {
                return LedStyle.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LedStyle
             * @function getTypeUrl
             * @memberof lebai.led.LedStyle
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LedStyle.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.led.LedStyle";
            };

            return LedStyle;
        })();

        led.SaveLedStyleRequest = (function() {

            /**
             * Properties of a SaveLedStyleRequest.
             * @typedef {Object} lebai.led.SaveLedStyleRequest.$Properties
             * @property {string|null} [name] SaveLedStyleRequest name
             * @property {lebai.led.LedStyle.$Properties|null} [data] SaveLedStyleRequest data
             * @property {string|null} [dir] SaveLedStyleRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SaveLedStyleRequest.
             * @memberof lebai.led
             * @interface ISaveLedStyleRequest
             * @augments lebai.led.SaveLedStyleRequest.$Properties
             * @deprecated Use lebai.led.SaveLedStyleRequest.$Properties instead.
             */

            /**
             * Shape of a SaveLedStyleRequest.
             * @typedef {lebai.led.SaveLedStyleRequest.$Properties} lebai.led.SaveLedStyleRequest.$Shape
             */

            /**
             * Constructs a new SaveLedStyleRequest.
             * @memberof lebai.led
             * @classdesc Represents a SaveLedStyleRequest.
             * @constructor
             * @param {lebai.led.SaveLedStyleRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SaveLedStyleRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SaveLedStyleRequest name.
             * @member {string} name
             * @memberof lebai.led.SaveLedStyleRequest
             * @instance
             */
            SaveLedStyleRequest.prototype.name = "";

            /**
             * SaveLedStyleRequest data.
             * @member {lebai.led.LedStyle.$Properties|null|undefined} data
             * @memberof lebai.led.SaveLedStyleRequest
             * @instance
             */
            SaveLedStyleRequest.prototype.data = null;

            /**
             * SaveLedStyleRequest dir.
             * @member {string} dir
             * @memberof lebai.led.SaveLedStyleRequest
             * @instance
             */
            SaveLedStyleRequest.prototype.dir = "";

            /**
             * Creates a new SaveLedStyleRequest instance using the specified properties.
             * @function create
             * @memberof lebai.led.SaveLedStyleRequest
             * @static
             * @param {lebai.led.SaveLedStyleRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.led.SaveLedStyleRequest} SaveLedStyleRequest instance
             * @type {{
             *   (properties: lebai.led.SaveLedStyleRequest.$Shape): lebai.led.SaveLedStyleRequest & lebai.led.SaveLedStyleRequest.$Shape;
             *   (properties?: lebai.led.SaveLedStyleRequest.$Properties): lebai.led.SaveLedStyleRequest;
             * }}
             */
            SaveLedStyleRequest.create = function(properties) {
                return new SaveLedStyleRequest(properties);
            };

            /**
             * Encodes the specified SaveLedStyleRequest message. Does not implicitly {@link lebai.led.SaveLedStyleRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.led.SaveLedStyleRequest
             * @static
             * @param {lebai.led.SaveLedStyleRequest.$Properties} message SaveLedStyleRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveLedStyleRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    $root.lebai.led.LedStyle.encode(message.data, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SaveLedStyleRequest message, length delimited. Does not implicitly {@link lebai.led.SaveLedStyleRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.led.SaveLedStyleRequest
             * @static
             * @param {lebai.led.SaveLedStyleRequest.$Properties} message SaveLedStyleRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveLedStyleRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SaveLedStyleRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.led.SaveLedStyleRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.led.SaveLedStyleRequest & lebai.led.SaveLedStyleRequest.$Shape} SaveLedStyleRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveLedStyleRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.led.SaveLedStyleRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.data = $root.lebai.led.LedStyle.decode(reader, reader.uint32(), $undefined, _depth + 1, message.data);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SaveLedStyleRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.led.SaveLedStyleRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.led.SaveLedStyleRequest & lebai.led.SaveLedStyleRequest.$Shape} SaveLedStyleRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveLedStyleRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SaveLedStyleRequest message.
             * @function verify
             * @memberof lebai.led.SaveLedStyleRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SaveLedStyleRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    var error = $root.lebai.led.LedStyle.verify(message.data, _depth + 1);
                    if (error)
                        return "data." + error;
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates a SaveLedStyleRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.led.SaveLedStyleRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.led.SaveLedStyleRequest} SaveLedStyleRequest
             */
            SaveLedStyleRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.led.SaveLedStyleRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.led.SaveLedStyleRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.led.SaveLedStyleRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.data != null) {
                    if (!$util.isObject(object.data))
                        throw $TypeError(".lebai.led.SaveLedStyleRequest.data: object expected");
                    message.data = $root.lebai.led.LedStyle.fromObject(object.data, _depth + 1);
                }
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from a SaveLedStyleRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.led.SaveLedStyleRequest
             * @static
             * @param {lebai.led.SaveLedStyleRequest} message SaveLedStyleRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SaveLedStyleRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.data = null;
                    object.dir = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = $root.lebai.led.LedStyle.toObject(message.data, options, _depth + 1);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                return object;
            };

            /**
             * Converts this SaveLedStyleRequest to JSON.
             * @function toJSON
             * @memberof lebai.led.SaveLedStyleRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SaveLedStyleRequest.prototype.toJSON = function() {
                return SaveLedStyleRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SaveLedStyleRequest
             * @function getTypeUrl
             * @memberof lebai.led.SaveLedStyleRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SaveLedStyleRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.led.SaveLedStyleRequest";
            };

            return SaveLedStyleRequest;
        })();

        led.LedStyleItem = (function() {

            /**
             * Properties of a LedStyleItem.
             * @typedef {Object} lebai.led.LedStyleItem.$Properties
             * @property {lebai.system.RobotState|null} [state] LedStyleItem state
             * @property {lebai.led.LedStyle.$Properties|null} [style] LedStyleItem style
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LedStyleItem.
             * @memberof lebai.led
             * @interface ILedStyleItem
             * @augments lebai.led.LedStyleItem.$Properties
             * @deprecated Use lebai.led.LedStyleItem.$Properties instead.
             */

            /**
             * Shape of a LedStyleItem.
             * @typedef {lebai.led.LedStyleItem.$Properties} lebai.led.LedStyleItem.$Shape
             */

            /**
             * Constructs a new LedStyleItem.
             * @memberof lebai.led
             * @classdesc Represents a LedStyleItem.
             * @constructor
             * @param {lebai.led.LedStyleItem.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LedStyleItem = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LedStyleItem state.
             * @member {lebai.system.RobotState} state
             * @memberof lebai.led.LedStyleItem
             * @instance
             */
            LedStyleItem.prototype.state = 0;

            /**
             * LedStyleItem style.
             * @member {lebai.led.LedStyle.$Properties|null|undefined} style
             * @memberof lebai.led.LedStyleItem
             * @instance
             */
            LedStyleItem.prototype.style = null;

            /**
             * Creates a new LedStyleItem instance using the specified properties.
             * @function create
             * @memberof lebai.led.LedStyleItem
             * @static
             * @param {lebai.led.LedStyleItem.$Properties=} [properties] Properties to set
             * @returns {lebai.led.LedStyleItem} LedStyleItem instance
             * @type {{
             *   (properties: lebai.led.LedStyleItem.$Shape): lebai.led.LedStyleItem & lebai.led.LedStyleItem.$Shape;
             *   (properties?: lebai.led.LedStyleItem.$Properties): lebai.led.LedStyleItem;
             * }}
             */
            LedStyleItem.create = function(properties) {
                return new LedStyleItem(properties);
            };

            /**
             * Encodes the specified LedStyleItem message. Does not implicitly {@link lebai.led.LedStyleItem.verify|verify} messages.
             * @function encode
             * @memberof lebai.led.LedStyleItem
             * @static
             * @param {lebai.led.LedStyleItem.$Properties} message LedStyleItem message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LedStyleItem.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.state != null && $Object.hasOwnProperty.call(message, "state") && message.state !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.state);
                if (message.style != null && $Object.hasOwnProperty.call(message, "style"))
                    $root.lebai.led.LedStyle.encode(message.style, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LedStyleItem message, length delimited. Does not implicitly {@link lebai.led.LedStyleItem.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.led.LedStyleItem
             * @static
             * @param {lebai.led.LedStyleItem.$Properties} message LedStyleItem message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LedStyleItem.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LedStyleItem message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.led.LedStyleItem
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.led.LedStyleItem & lebai.led.LedStyleItem.$Shape} LedStyleItem
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LedStyleItem.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.led.LedStyleItem();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.state = value;
                            else
                                delete message.state;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.style = $root.lebai.led.LedStyle.decode(reader, reader.uint32(), $undefined, _depth + 1, message.style);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LedStyleItem message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.led.LedStyleItem
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.led.LedStyleItem & lebai.led.LedStyleItem.$Shape} LedStyleItem
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LedStyleItem.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LedStyleItem message.
             * @function verify
             * @memberof lebai.led.LedStyleItem
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LedStyleItem.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                    if (typeof message.state !== "number" || (message.state | 0) !== message.state)
                        return "state: enum value expected";
                if (message.style != null && $Object.hasOwnProperty.call(message, "style")) {
                    var error = $root.lebai.led.LedStyle.verify(message.style, _depth + 1);
                    if (error)
                        return "style." + error;
                }
                return null;
            };

            /**
             * Creates a LedStyleItem message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.led.LedStyleItem
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.led.LedStyleItem} LedStyleItem
             */
            LedStyleItem.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.led.LedStyleItem)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.led.LedStyleItem: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.led.LedStyleItem();
                if (object.state !== 0 && (typeof object.state !== "string" || $root.lebai.system.RobotState[object.state] !== 0))
                    switch (object.state) {
                    case "DISCONNECTED":
                    case 0:
                        message.state = 0;
                        break;
                    case "ESTOP":
                    case 1:
                        message.state = 1;
                        break;
                    case "BOOTING":
                    case 2:
                        message.state = 2;
                        break;
                    case "ROBOT_OFF":
                    case 3:
                        message.state = 3;
                        break;
                    case "ROBOT_ON":
                    case 4:
                        message.state = 4;
                        break;
                    case "IDLE":
                    case 5:
                        message.state = 5;
                        break;
                    case "PAUSED":
                    case 6:
                        message.state = 6;
                        break;
                    case "MOVING":
                    case 7:
                        message.state = 7;
                        break;
                    case "UPDATING":
                    case 8:
                        message.state = 8;
                        break;
                    case "STARTING":
                    case 9:
                        message.state = 9;
                        break;
                    case "STOPPING":
                    case 10:
                        message.state = 10;
                        break;
                    case "TEACHING":
                    case 11:
                        message.state = 11;
                        break;
                    case "STOP":
                    case 12:
                        message.state = 12;
                        break;
                    default:
                        if (typeof object.state === "number" && (object.state | 0) === object.state)
                            message.state = object.state;
                    }
                if (object.style != null) {
                    if (!$util.isObject(object.style))
                        throw $TypeError(".lebai.led.LedStyleItem.style: object expected");
                    message.style = $root.lebai.led.LedStyle.fromObject(object.style, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a LedStyleItem message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.led.LedStyleItem
             * @static
             * @param {lebai.led.LedStyleItem} message LedStyleItem
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LedStyleItem.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.state = options.enums === $String ? "DISCONNECTED" : 0;
                    object.style = null;
                }
                if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                    object.state = options.enums === $String ? $root.lebai.system.RobotState[message.state] === $undefined ? message.state : $root.lebai.system.RobotState[message.state] : message.state;
                if (message.style != null && $Object.hasOwnProperty.call(message, "style"))
                    object.style = $root.lebai.led.LedStyle.toObject(message.style, options, _depth + 1);
                return object;
            };

            /**
             * Converts this LedStyleItem to JSON.
             * @function toJSON
             * @memberof lebai.led.LedStyleItem
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LedStyleItem.prototype.toJSON = function() {
                return LedStyleItem.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LedStyleItem
             * @function getTypeUrl
             * @memberof lebai.led.LedStyleItem
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LedStyleItem.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.led.LedStyleItem";
            };

            return LedStyleItem;
        })();

        led.LedStyles = (function() {

            /**
             * Properties of a LedStyles.
             * @typedef {Object} lebai.led.LedStyles.$Properties
             * @property {Object.<string,lebai.led.LedStyle.$Properties>|null} [styles] LedStyles styles
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LedStyles.
             * @memberof lebai.led
             * @interface ILedStyles
             * @augments lebai.led.LedStyles.$Properties
             * @deprecated Use lebai.led.LedStyles.$Properties instead.
             */

            /**
             * Shape of a LedStyles.
             * @typedef {lebai.led.LedStyles.$Properties} lebai.led.LedStyles.$Shape
             */

            /**
             * Constructs a new LedStyles.
             * @memberof lebai.led
             * @classdesc Represents a LedStyles.
             * @constructor
             * @param {lebai.led.LedStyles.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LedStyles = function (properties) {
                this.styles = {};
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LedStyles styles.
             * @member {Object.<string,lebai.led.LedStyle.$Properties>} styles
             * @memberof lebai.led.LedStyles
             * @instance
             */
            LedStyles.prototype.styles = $util.emptyObject;

            /**
             * Creates a new LedStyles instance using the specified properties.
             * @function create
             * @memberof lebai.led.LedStyles
             * @static
             * @param {lebai.led.LedStyles.$Properties=} [properties] Properties to set
             * @returns {lebai.led.LedStyles} LedStyles instance
             * @type {{
             *   (properties: lebai.led.LedStyles.$Shape): lebai.led.LedStyles & lebai.led.LedStyles.$Shape;
             *   (properties?: lebai.led.LedStyles.$Properties): lebai.led.LedStyles;
             * }}
             */
            LedStyles.create = function(properties) {
                return new LedStyles(properties);
            };

            /**
             * Encodes the specified LedStyles message. Does not implicitly {@link lebai.led.LedStyles.verify|verify} messages.
             * @function encode
             * @memberof lebai.led.LedStyles
             * @static
             * @param {lebai.led.LedStyles.$Properties} message LedStyles message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LedStyles.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.styles != null && $Object.hasOwnProperty.call(message, "styles"))
                    for (var keys = $Object.keys(message.styles), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                        $root.lebai.led.LedStyle.encode(message.styles[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
                    }
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LedStyles message, length delimited. Does not implicitly {@link lebai.led.LedStyles.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.led.LedStyles
             * @static
             * @param {lebai.led.LedStyles.$Properties} message LedStyles message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LedStyles.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LedStyles message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.led.LedStyles
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.led.LedStyles & lebai.led.LedStyles.$Shape} LedStyles
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LedStyles.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.led.LedStyles();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (message.styles === $util.emptyObject)
                                message.styles = {};
                            var end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = 0;
                            value = null;
                            while (reader.pos < end2) {
                                var tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 0)
                                        break;
                                    key = reader.int32();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = $root.lebai.led.LedStyle.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            message.styles[key] = value || new $root.lebai.led.LedStyle();
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LedStyles message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.led.LedStyles
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.led.LedStyles & lebai.led.LedStyles.$Shape} LedStyles
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LedStyles.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LedStyles message.
             * @function verify
             * @memberof lebai.led.LedStyles
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LedStyles.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.styles != null && $Object.hasOwnProperty.call(message, "styles")) {
                    if (!$util.isObject(message.styles))
                        return "styles: object expected";
                    var key = $Object.keys(message.styles);
                    for (var i = 0; i < key.length; ++i) {
                        if (!$util.key32Re.test(key[i]))
                            return "styles: integer key{k:int32} expected";
                        {
                            var error = $root.lebai.led.LedStyle.verify(message.styles[key[i]], _depth + 1);
                            if (error)
                                return "styles." + error;
                        }
                    }
                }
                return null;
            };

            /**
             * Creates a LedStyles message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.led.LedStyles
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.led.LedStyles} LedStyles
             */
            LedStyles.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.led.LedStyles)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.led.LedStyles: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.led.LedStyles();
                if (object.styles) {
                    if (!$util.isObject(object.styles))
                        throw $TypeError(".lebai.led.LedStyles.styles: object expected");
                    message.styles = {};
                    for (var keys = $Object.keys(object.styles), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.styles, keys[i]);
                        if (!$util.isObject(object.styles[keys[i]]))
                            throw $TypeError(".lebai.led.LedStyles.styles: object expected");
                        message.styles[keys[i]] = $root.lebai.led.LedStyle.fromObject(object.styles[keys[i]], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a LedStyles message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.led.LedStyles
             * @static
             * @param {lebai.led.LedStyles} message LedStyles
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LedStyles.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.objects || options.defaults)
                    object.styles = {};
                var keys2;
                if (message.styles && (keys2 = $Object.keys(message.styles)).length) {
                    object.styles = {};
                    for (var j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.styles, keys2[j]);
                        object.styles[keys2[j]] = $root.lebai.led.LedStyle.toObject(message.styles[keys2[j]], options, _depth + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this LedStyles to JSON.
             * @function toJSON
             * @memberof lebai.led.LedStyles
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LedStyles.prototype.toJSON = function() {
                return LedStyles.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LedStyles
             * @function getTypeUrl
             * @memberof lebai.led.LedStyles
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LedStyles.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.led.LedStyles";
            };

            return LedStyles;
        })();

        led.LedService = (function() {

            /**
             * Constructs a new LedService service.
             * @memberof lebai.led
             * @classdesc Represents a LedService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var LedService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(LedService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: LedService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new LedService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.led.LedService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {LedService} RPC service. Useful where requests and/or responses are streamed.
             */
            LedService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.led.LedService#saveLedStyle}.
             * @memberof lebai.led.LedService
             * @typedef SaveLedStyleCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SaveLedStyle.
             * @memberof lebai.led.LedService
             * @typedef SaveLedStyle
             * @type {{
             *   (request: lebai.led.ISaveLedStyleRequest, callback: lebai.led.LedService.SaveLedStyleCallback): void;
             *   (request: lebai.led.ISaveLedStyleRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SaveLedStyle";
             *   readonly path: "/lebai.led.LedService/SaveLedStyle";
             *   readonly requestType: "SaveLedStyleRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SaveLedStyle.
             * @name lebai.led.LedService#saveLedStyle
             * @type {lebai.led.LedService.SaveLedStyle}
             */
            $Object.defineProperties(LedService.prototype.saveLedStyle = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, LedService.prototype.saveLedStyle, $root.lebai.led.SaveLedStyleRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SaveLedStyle" },
                path: { value: "/lebai.led.LedService/SaveLedStyle" },
                requestType: { value: "SaveLedStyleRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.led.LedService#loadLedStyle}.
             * @memberof lebai.led.LedService
             * @typedef LoadLedStyleCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.led.LedStyle} [response] LedStyle
             */

            /**
             * Calls LoadLedStyle.
             * @memberof lebai.led.LedService
             * @typedef LoadLedStyle
             * @type {{
             *   (request: lebai.db.ILoadRequest, callback: lebai.led.LedService.LoadLedStyleCallback): void;
             *   (request: lebai.db.ILoadRequest): Promise<lebai.led.LedStyle>;
             *   readonly name: "LoadLedStyle";
             *   readonly path: "/lebai.led.LedService/LoadLedStyle";
             *   readonly requestType: "db.LoadRequest";
             *   readonly responseType: "LedStyle";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadLedStyle.
             * @name lebai.led.LedService#loadLedStyle
             * @type {lebai.led.LedService.LoadLedStyle}
             */
            $Object.defineProperties(LedService.prototype.loadLedStyle = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, LedService.prototype.loadLedStyle, $root.lebai.db.LoadRequest, $root.lebai.led.LedStyle, request, callback);
            }, {
                name: { value: "LoadLedStyle" },
                path: { value: "/lebai.led.LedService/LoadLedStyle" },
                requestType: { value: "db.LoadRequest" },
                responseType: { value: "LedStyle" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.led.LedService#loadLedStyleList}.
             * @memberof lebai.led.LedService
             * @typedef LoadLedStyleListCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.db.LoadListResponse} [response] LoadListResponse
             */

            /**
             * Calls LoadLedStyleList.
             * @memberof lebai.led.LedService
             * @typedef LoadLedStyleList
             * @type {{
             *   (request: lebai.db.ILoadListRequest, callback: lebai.led.LedService.LoadLedStyleListCallback): void;
             *   (request: lebai.db.ILoadListRequest): Promise<lebai.db.LoadListResponse>;
             *   readonly name: "LoadLedStyleList";
             *   readonly path: "/lebai.led.LedService/LoadLedStyleList";
             *   readonly requestType: "db.LoadListRequest";
             *   readonly responseType: "db.LoadListResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadLedStyleList.
             * @name lebai.led.LedService#loadLedStyleList
             * @type {lebai.led.LedService.LoadLedStyleList}
             */
            $Object.defineProperties(LedService.prototype.loadLedStyleList = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, LedService.prototype.loadLedStyleList, $root.lebai.db.LoadListRequest, $root.lebai.db.LoadListResponse, request, callback);
            }, {
                name: { value: "LoadLedStyleList" },
                path: { value: "/lebai.led.LedService/LoadLedStyleList" },
                requestType: { value: "db.LoadListRequest" },
                responseType: { value: "db.LoadListResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.led.LedService#setLedStyle}.
             * @memberof lebai.led.LedService
             * @typedef SetLedStyleCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetLedStyle.
             * @memberof lebai.led.LedService
             * @typedef SetLedStyle
             * @type {{
             *   (request: lebai.led.ILedStyleItem, callback: lebai.led.LedService.SetLedStyleCallback): void;
             *   (request: lebai.led.ILedStyleItem): Promise<google.protobuf.Empty>;
             *   readonly name: "SetLedStyle";
             *   readonly path: "/lebai.led.LedService/SetLedStyle";
             *   readonly requestType: "LedStyleItem";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetLedStyle.
             * @name lebai.led.LedService#setLedStyle
             * @type {lebai.led.LedService.SetLedStyle}
             */
            $Object.defineProperties(LedService.prototype.setLedStyle = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, LedService.prototype.setLedStyle, $root.lebai.led.LedStyleItem, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetLedStyle" },
                path: { value: "/lebai.led.LedService/SetLedStyle" },
                requestType: { value: "LedStyleItem" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.led.LedService#setLedStyles}.
             * @memberof lebai.led.LedService
             * @typedef SetLedStylesCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetLedStyles.
             * @memberof lebai.led.LedService
             * @typedef SetLedStyles
             * @type {{
             *   (request: lebai.led.ILedStyles, callback: lebai.led.LedService.SetLedStylesCallback): void;
             *   (request: lebai.led.ILedStyles): Promise<google.protobuf.Empty>;
             *   readonly name: "SetLedStyles";
             *   readonly path: "/lebai.led.LedService/SetLedStyles";
             *   readonly requestType: "LedStyles";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetLedStyles.
             * @name lebai.led.LedService#setLedStyles
             * @type {lebai.led.LedService.SetLedStyles}
             */
            $Object.defineProperties(LedService.prototype.setLedStyles = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, LedService.prototype.setLedStyles, $root.lebai.led.LedStyles, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetLedStyles" },
                path: { value: "/lebai.led.LedService/SetLedStyles" },
                requestType: { value: "LedStyles" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.led.LedService#getLedStyles}.
             * @memberof lebai.led.LedService
             * @typedef GetLedStylesCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.led.LedStyles} [response] LedStyles
             */

            /**
             * Calls GetLedStyles.
             * @memberof lebai.led.LedService
             * @typedef GetLedStyles
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.led.LedService.GetLedStylesCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.led.LedStyles>;
             *   readonly name: "GetLedStyles";
             *   readonly path: "/lebai.led.LedService/GetLedStyles";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "LedStyles";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetLedStyles.
             * @name lebai.led.LedService#getLedStyles
             * @type {lebai.led.LedService.GetLedStyles}
             */
            $Object.defineProperties(LedService.prototype.getLedStyles = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, LedService.prototype.getLedStyles, $root.google.protobuf.Empty, $root.lebai.led.LedStyles, request, callback);
            }, {
                name: { value: "GetLedStyles" },
                path: { value: "/lebai.led.LedService/GetLedStyles" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "LedStyles" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.led.LedService#setLed}.
             * @memberof lebai.led.LedService
             * @typedef SetLedCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetLed.
             * @memberof lebai.led.LedService
             * @typedef SetLed
             * @type {{
             *   (request: lebai.led.ILedData, callback: lebai.led.LedService.SetLedCallback): void;
             *   (request: lebai.led.ILedData): Promise<google.protobuf.Empty>;
             *   readonly name: "SetLed";
             *   readonly path: "/lebai.led.LedService/SetLed";
             *   readonly requestType: "LedData";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetLed.
             * @name lebai.led.LedService#setLed
             * @type {lebai.led.LedService.SetLed}
             */
            $Object.defineProperties(LedService.prototype.setLed = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, LedService.prototype.setLed, $root.lebai.led.LedData, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetLed" },
                path: { value: "/lebai.led.LedService/SetLed" },
                requestType: { value: "LedData" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.led.LedService#setVoice}.
             * @memberof lebai.led.LedService
             * @typedef SetVoiceCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetVoice.
             * @memberof lebai.led.LedService
             * @typedef SetVoice
             * @type {{
             *   (request: lebai.led.IVoiceData, callback: lebai.led.LedService.SetVoiceCallback): void;
             *   (request: lebai.led.IVoiceData): Promise<google.protobuf.Empty>;
             *   readonly name: "SetVoice";
             *   readonly path: "/lebai.led.LedService/SetVoice";
             *   readonly requestType: "VoiceData";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetVoice.
             * @name lebai.led.LedService#setVoice
             * @type {lebai.led.LedService.SetVoice}
             */
            $Object.defineProperties(LedService.prototype.setVoice = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, LedService.prototype.setVoice, $root.lebai.led.VoiceData, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetVoice" },
                path: { value: "/lebai.led.LedService/SetVoice" },
                requestType: { value: "VoiceData" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.led.LedService#setFan}.
             * @memberof lebai.led.LedService
             * @typedef SetFanCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetFan.
             * @memberof lebai.led.LedService
             * @typedef SetFan
             * @type {{
             *   (request: lebai.led.IFanData, callback: lebai.led.LedService.SetFanCallback): void;
             *   (request: lebai.led.IFanData): Promise<google.protobuf.Empty>;
             *   readonly name: "SetFan";
             *   readonly path: "/lebai.led.LedService/SetFan";
             *   readonly requestType: "FanData";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetFan.
             * @name lebai.led.LedService#setFan
             * @type {lebai.led.LedService.SetFan}
             */
            $Object.defineProperties(LedService.prototype.setFan = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, LedService.prototype.setFan, $root.lebai.led.FanData, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetFan" },
                path: { value: "/lebai.led.LedService/SetFan" },
                requestType: { value: "FanData" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return LedService;
        })();

        return led;
    })();

    lebai.message = (function() {

        /**
         * Namespace message.
         * @memberof lebai
         * @namespace
         */
        var message = {};

        /**
         * Level enum.
         * @name lebai.message.Level
         * @enum {number}
         * @property {number} INFO=0 INFO value
         * @property {number} WARN=1 WARN value
         * @property {number} ERROR=2 ERROR value
         */
        message.Level = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "INFO"] = 0;
            values[valuesById[1] = "WARN"] = 1;
            values[valuesById[2] = "ERROR"] = 2;
            return values;
        })();

        /**
         * Kind enum.
         * @name lebai.message.Kind
         * @enum {number}
         * @property {number} ESTOP=0 ESTOP value
         * @property {number} OTA_STATE_ERROR=1004 OTA_STATE_ERROR value
         * @property {number} OTA_FAILED=1005 OTA_FAILED value
         * @property {number} JONIT_SPEED_EXCEED=1007 JONIT_SPEED_EXCEED value
         * @property {number} REACH_JOINT_LIMIT=1008 REACH_JOINT_LIMIT value
         * @property {number} COLLISION_DETECTED=1009 COLLISION_DETECTED value
         * @property {number} HARD_ESTOP_LOCK=1011 HARD_ESTOP_LOCK value
         * @property {number} DRIVER_ERROR=1100 DRIVER_ERROR value
         * @property {number} TRAJECTOR_ERROR=1140 TRAJECTOR_ERROR value
         * @property {number} PositionAbnormal=1141 PositionAbnormal value
         */
        message.Kind = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "ESTOP"] = 0;
            values[valuesById[1004] = "OTA_STATE_ERROR"] = 1004;
            values[valuesById[1005] = "OTA_FAILED"] = 1005;
            values[valuesById[1007] = "JONIT_SPEED_EXCEED"] = 1007;
            values[valuesById[1008] = "REACH_JOINT_LIMIT"] = 1008;
            values[valuesById[1009] = "COLLISION_DETECTED"] = 1009;
            values[valuesById[1011] = "HARD_ESTOP_LOCK"] = 1011;
            values[valuesById[1100] = "DRIVER_ERROR"] = 1100;
            values[valuesById[1140] = "TRAJECTOR_ERROR"] = 1140;
            values[valuesById[1141] = "PositionAbnormal"] = 1141;
            return values;
        })();

        message.Message = (function() {

            /**
             * Properties of a Message.
             * @typedef {Object} lebai.message.Message.$Properties
             * @property {lebai.message.Level|null} [level] Message level
             * @property {lebai.message.Kind|null} [kind] Message kind
             * @property {string|null} [detail] Message detail
             * @property {google.protobuf.Timestamp.$Properties|null} [time] Message time
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Message.
             * @memberof lebai.message
             * @interface IMessage
             * @augments lebai.message.Message.$Properties
             * @deprecated Use lebai.message.Message.$Properties instead.
             */

            /**
             * Shape of a Message.
             * @typedef {lebai.message.Message.$Properties} lebai.message.Message.$Shape
             */

            /**
             * Constructs a new Message.
             * @memberof lebai.message
             * @classdesc Represents a Message.
             * @constructor
             * @param {lebai.message.Message.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Message = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Message level.
             * @member {lebai.message.Level} level
             * @memberof lebai.message.Message
             * @instance
             */
            Message.prototype.level = 0;

            /**
             * Message kind.
             * @member {lebai.message.Kind} kind
             * @memberof lebai.message.Message
             * @instance
             */
            Message.prototype.kind = 0;

            /**
             * Message detail.
             * @member {string} detail
             * @memberof lebai.message.Message
             * @instance
             */
            Message.prototype.detail = "";

            /**
             * Message time.
             * @member {google.protobuf.Timestamp.$Properties|null|undefined} time
             * @memberof lebai.message.Message
             * @instance
             */
            Message.prototype.time = null;

            /**
             * Creates a new Message instance using the specified properties.
             * @function create
             * @memberof lebai.message.Message
             * @static
             * @param {lebai.message.Message.$Properties=} [properties] Properties to set
             * @returns {lebai.message.Message} Message instance
             * @type {{
             *   (properties: lebai.message.Message.$Shape): lebai.message.Message & lebai.message.Message.$Shape;
             *   (properties?: lebai.message.Message.$Properties): lebai.message.Message;
             * }}
             */
            Message.create = function(properties) {
                return new Message(properties);
            };

            /**
             * Encodes the specified Message message. Does not implicitly {@link lebai.message.Message.verify|verify} messages.
             * @function encode
             * @memberof lebai.message.Message
             * @static
             * @param {lebai.message.Message.$Properties} message Message message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Message.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.level != null && $Object.hasOwnProperty.call(message, "level") && message.level !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.level);
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind") && message.kind !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.kind);
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail") && message.detail !== "")
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.detail);
                if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                    $root.google.protobuf.Timestamp.encode(message.time, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Message message, length delimited. Does not implicitly {@link lebai.message.Message.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.message.Message
             * @static
             * @param {lebai.message.Message.$Properties} message Message message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Message.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Message message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.message.Message
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.message.Message & lebai.message.Message.$Shape} Message
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Message.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.message.Message();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.kind = value;
                            else
                                delete message.kind;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.detail = value;
                            else
                                delete message.detail;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.time = $root.google.protobuf.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.time);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Message message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.message.Message
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.message.Message & lebai.message.Message.$Shape} Message
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Message.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Message message.
             * @function verify
             * @memberof lebai.message.Message
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Message.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
                    if (typeof message.level !== "number" || (message.level | 0) !== message.level)
                        return "level: enum value expected";
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    if (typeof message.kind !== "number" || (message.kind | 0) !== message.kind)
                        return "kind: enum value expected";
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail"))
                    if (!$util.isString(message.detail))
                        return "detail: string expected";
                if (message.time != null && $Object.hasOwnProperty.call(message, "time")) {
                    var error = $root.google.protobuf.Timestamp.verify(message.time, _depth + 1);
                    if (error)
                        return "time." + error;
                }
                return null;
            };

            /**
             * Creates a Message message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.message.Message
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.message.Message} Message
             */
            Message.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.message.Message)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.message.Message: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.message.Message();
                if (object.level !== 0 && (typeof object.level !== "string" || $root.lebai.message.Level[object.level] !== 0))
                    switch (object.level) {
                    case "INFO":
                    case 0:
                        message.level = 0;
                        break;
                    case "WARN":
                    case 1:
                        message.level = 1;
                        break;
                    case "ERROR":
                    case 2:
                        message.level = 2;
                        break;
                    default:
                        if (typeof object.level === "number" && (object.level | 0) === object.level)
                            message.level = object.level;
                    }
                if (object.kind !== 0 && (typeof object.kind !== "string" || $root.lebai.message.Kind[object.kind] !== 0))
                    switch (object.kind) {
                    case "ESTOP":
                    case 0:
                        message.kind = 0;
                        break;
                    case "OTA_STATE_ERROR":
                    case 1004:
                        message.kind = 1004;
                        break;
                    case "OTA_FAILED":
                    case 1005:
                        message.kind = 1005;
                        break;
                    case "JONIT_SPEED_EXCEED":
                    case 1007:
                        message.kind = 1007;
                        break;
                    case "REACH_JOINT_LIMIT":
                    case 1008:
                        message.kind = 1008;
                        break;
                    case "COLLISION_DETECTED":
                    case 1009:
                        message.kind = 1009;
                        break;
                    case "HARD_ESTOP_LOCK":
                    case 1011:
                        message.kind = 1011;
                        break;
                    case "DRIVER_ERROR":
                    case 1100:
                        message.kind = 1100;
                        break;
                    case "TRAJECTOR_ERROR":
                    case 1140:
                        message.kind = 1140;
                        break;
                    case "PositionAbnormal":
                    case 1141:
                        message.kind = 1141;
                        break;
                    default:
                        if (typeof object.kind === "number" && (object.kind | 0) === object.kind)
                            message.kind = object.kind;
                    }
                if (object.detail != null)
                    if (typeof object.detail !== "string" || object.detail.length)
                        message.detail = $String(object.detail);
                if (object.time != null) {
                    if (!$util.isObject(object.time))
                        throw $TypeError(".lebai.message.Message.time: object expected");
                    message.time = $root.google.protobuf.Timestamp.fromObject(object.time, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Message message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.message.Message
             * @static
             * @param {lebai.message.Message} message Message
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Message.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.level = options.enums === $String ? "INFO" : 0;
                    object.kind = options.enums === $String ? "ESTOP" : 0;
                    object.detail = "";
                    object.time = null;
                }
                if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
                    object.level = options.enums === $String ? $root.lebai.message.Level[message.level] === $undefined ? message.level : $root.lebai.message.Level[message.level] : message.level;
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    object.kind = options.enums === $String ? $root.lebai.message.Kind[message.kind] === $undefined ? message.kind : $root.lebai.message.Kind[message.kind] : message.kind;
                if (message.detail != null && $Object.hasOwnProperty.call(message, "detail"))
                    object.detail = message.detail;
                if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                    object.time = $root.google.protobuf.Timestamp.toObject(message.time, options, _depth + 1);
                return object;
            };

            /**
             * Converts this Message to JSON.
             * @function toJSON
             * @memberof lebai.message.Message
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Message.prototype.toJSON = function() {
                return Message.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Message
             * @function getTypeUrl
             * @memberof lebai.message.Message
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Message.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.message.Message";
            };

            return Message;
        })();

        message.Messages = (function() {

            /**
             * Properties of a Messages.
             * @typedef {Object} lebai.message.Messages.$Properties
             * @property {Array.<lebai.message.Message.$Properties>|null} [messages] Messages messages
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Messages.
             * @memberof lebai.message
             * @interface IMessages
             * @augments lebai.message.Messages.$Properties
             * @deprecated Use lebai.message.Messages.$Properties instead.
             */

            /**
             * Shape of a Messages.
             * @typedef {lebai.message.Messages.$Properties} lebai.message.Messages.$Shape
             */

            /**
             * Constructs a new Messages.
             * @memberof lebai.message
             * @classdesc Represents a Messages.
             * @constructor
             * @param {lebai.message.Messages.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Messages = function (properties) {
                this.messages = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Messages messages.
             * @member {Array.<lebai.message.Message.$Properties>} messages
             * @memberof lebai.message.Messages
             * @instance
             */
            Messages.prototype.messages = $util.emptyArray;

            /**
             * Creates a new Messages instance using the specified properties.
             * @function create
             * @memberof lebai.message.Messages
             * @static
             * @param {lebai.message.Messages.$Properties=} [properties] Properties to set
             * @returns {lebai.message.Messages} Messages instance
             * @type {{
             *   (properties: lebai.message.Messages.$Shape): lebai.message.Messages & lebai.message.Messages.$Shape;
             *   (properties?: lebai.message.Messages.$Properties): lebai.message.Messages;
             * }}
             */
            Messages.create = function(properties) {
                return new Messages(properties);
            };

            /**
             * Encodes the specified Messages message. Does not implicitly {@link lebai.message.Messages.verify|verify} messages.
             * @function encode
             * @memberof lebai.message.Messages
             * @static
             * @param {lebai.message.Messages.$Properties} message Messages message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Messages.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.messages != null && message.messages.length)
                    for (var i = 0; i < message.messages.length; ++i)
                        $root.lebai.message.Message.encode(message.messages[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Messages message, length delimited. Does not implicitly {@link lebai.message.Messages.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.message.Messages
             * @static
             * @param {lebai.message.Messages.$Properties} message Messages message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Messages.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Messages message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.message.Messages
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.message.Messages & lebai.message.Messages.$Shape} Messages
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Messages.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.message.Messages();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.messages && message.messages.length))
                                message.messages = [];
                            message.messages.push($root.lebai.message.Message.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Messages message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.message.Messages
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.message.Messages & lebai.message.Messages.$Shape} Messages
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Messages.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Messages message.
             * @function verify
             * @memberof lebai.message.Messages
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Messages.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.messages != null && $Object.hasOwnProperty.call(message, "messages")) {
                    if (!$Array.isArray(message.messages))
                        return "messages: array expected";
                    for (var i = 0; i < message.messages.length; ++i) {
                        var error = $root.lebai.message.Message.verify(message.messages[i], _depth + 1);
                        if (error)
                            return "messages." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Messages message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.message.Messages
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.message.Messages} Messages
             */
            Messages.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.message.Messages)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.message.Messages: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.message.Messages();
                if (object.messages) {
                    if (!$Array.isArray(object.messages))
                        throw $TypeError(".lebai.message.Messages.messages: array expected");
                    message.messages = $Array(object.messages.length);
                    for (var i = 0; i < object.messages.length; ++i) {
                        if (!$util.isObject(object.messages[i]))
                            throw $TypeError(".lebai.message.Messages.messages: object expected");
                        message.messages[i] = $root.lebai.message.Message.fromObject(object.messages[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Messages message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.message.Messages
             * @static
             * @param {lebai.message.Messages} message Messages
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Messages.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.messages = [];
                if (message.messages && message.messages.length) {
                    object.messages = $Array(message.messages.length);
                    for (var j = 0; j < message.messages.length; ++j)
                        object.messages[j] = $root.lebai.message.Message.toObject(message.messages[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this Messages to JSON.
             * @function toJSON
             * @memberof lebai.message.Messages
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Messages.prototype.toJSON = function() {
                return Messages.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Messages
             * @function getTypeUrl
             * @memberof lebai.message.Messages
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Messages.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.message.Messages";
            };

            return Messages;
        })();

        message.MessageService = (function() {

            /**
             * Constructs a new MessageService service.
             * @memberof lebai.message
             * @classdesc Represents a MessageService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var MessageService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(MessageService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: MessageService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new MessageService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.message.MessageService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {MessageService} RPC service. Useful where requests and/or responses are streamed.
             */
            MessageService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.message.MessageService#getMessages}.
             * @memberof lebai.message.MessageService
             * @typedef GetMessagesCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.message.Messages} [response] Messages
             */

            /**
             * Calls GetMessages.
             * @memberof lebai.message.MessageService
             * @typedef GetMessages
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.message.MessageService.GetMessagesCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.message.Messages>;
             *   readonly name: "GetMessages";
             *   readonly path: "/lebai.message.MessageService/GetMessages";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "Messages";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetMessages.
             * @name lebai.message.MessageService#getMessages
             * @type {lebai.message.MessageService.GetMessages}
             */
            $Object.defineProperties(MessageService.prototype.getMessages = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MessageService.prototype.getMessages, $root.google.protobuf.Empty, $root.lebai.message.Messages, request, callback);
            }, {
                name: { value: "GetMessages" },
                path: { value: "/lebai.message.MessageService/GetMessages" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "Messages" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.message.MessageService#subMessage}.
             * @memberof lebai.message.MessageService
             * @typedef SubMessageCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.message.Message} [response] Message
             */

            /**
             * Calls SubMessage.
             * @memberof lebai.message.MessageService
             * @typedef SubMessage
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.message.MessageService.SubMessageCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.message.Message>;
             *   readonly name: "SubMessage";
             *   readonly path: "/lebai.message.MessageService/SubMessage";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "Message";
             *   readonly requestStream: undefined;
             *   readonly responseStream: true;
             * }}
             */

            /**
             * Calls SubMessage.
             * @name lebai.message.MessageService#subMessage
             * @type {lebai.message.MessageService.SubMessage}
             */
            $Object.defineProperties(MessageService.prototype.subMessage = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MessageService.prototype.subMessage, $root.google.protobuf.Empty, $root.lebai.message.Message, request, callback);
            }, {
                name: { value: "SubMessage" },
                path: { value: "/lebai.message.MessageService/SubMessage" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "Message" },
                requestStream: { value: $undefined },
                responseStream: { value: true }
            });

            return MessageService;
        })();

        return message;
    })();

    lebai.modbus = (function() {

        /**
         * Namespace modbus.
         * @memberof lebai
         * @namespace
         */
        var modbus = {};

        /**
         * ModbusKind enum.
         * @name lebai.modbus.ModbusKind
         * @enum {number}
         * @property {number} ROBOT=0 ROBOT value
         * @property {number} FLANGE=1 FLANGE value
         * @property {number} EXTRA=2 EXTRA value
         * @property {number} MODBUS_FLANGE=3 MODBUS_FLANGE value
         * @property {number} MODBUS_RTU=4 MODBUS_RTU value
         * @property {number} MODBUS_TCP=5 MODBUS_TCP value
         */
        modbus.ModbusKind = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "ROBOT"] = 0;
            values[valuesById[1] = "FLANGE"] = 1;
            values[valuesById[2] = "EXTRA"] = 2;
            values[valuesById[3] = "MODBUS_FLANGE"] = 3;
            values[valuesById[4] = "MODBUS_RTU"] = 4;
            values[valuesById[5] = "MODBUS_TCP"] = 5;
            return values;
        })();

        modbus.Modbus = (function() {

            /**
             * Properties of a Modbus.
             * @typedef {Object} lebai.modbus.Modbus.$Properties
             * @property {lebai.modbus.ModbusKind|null} [kind] Modbus kind
             * @property {string|null} [address] Modbus address
             * @property {number|null} [port] Modbus port
             * @property {number|null} [slaveId] Modbus slaveId
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Modbus.
             * @memberof lebai.modbus
             * @interface IModbus
             * @augments lebai.modbus.Modbus.$Properties
             * @deprecated Use lebai.modbus.Modbus.$Properties instead.
             */

            /**
             * Shape of a Modbus.
             * @typedef {lebai.modbus.Modbus.$Properties} lebai.modbus.Modbus.$Shape
             */

            /**
             * Constructs a new Modbus.
             * @memberof lebai.modbus
             * @classdesc Represents a Modbus.
             * @constructor
             * @param {lebai.modbus.Modbus.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Modbus = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Modbus kind.
             * @member {lebai.modbus.ModbusKind} kind
             * @memberof lebai.modbus.Modbus
             * @instance
             */
            Modbus.prototype.kind = 0;

            /**
             * Modbus address.
             * @member {string} address
             * @memberof lebai.modbus.Modbus
             * @instance
             */
            Modbus.prototype.address = "";

            /**
             * Modbus port.
             * @member {number} port
             * @memberof lebai.modbus.Modbus
             * @instance
             */
            Modbus.prototype.port = 0;

            /**
             * Modbus slaveId.
             * @member {number} slaveId
             * @memberof lebai.modbus.Modbus
             * @instance
             */
            Modbus.prototype.slaveId = 0;

            /**
             * Creates a new Modbus instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.Modbus
             * @static
             * @param {lebai.modbus.Modbus.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.Modbus} Modbus instance
             * @type {{
             *   (properties: lebai.modbus.Modbus.$Shape): lebai.modbus.Modbus & lebai.modbus.Modbus.$Shape;
             *   (properties?: lebai.modbus.Modbus.$Properties): lebai.modbus.Modbus;
             * }}
             */
            Modbus.create = function(properties) {
                return new Modbus(properties);
            };

            /**
             * Encodes the specified Modbus message. Does not implicitly {@link lebai.modbus.Modbus.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.Modbus
             * @static
             * @param {lebai.modbus.Modbus.$Properties} message Modbus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Modbus.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind") && message.kind !== 0)
                    writer.uint32(/* id 12, wireType 0 =*/96).int32(message.kind);
                if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== "")
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.address);
                if (message.port != null && $Object.hasOwnProperty.call(message, "port") && message.port !== 0)
                    writer.uint32(/* id 14, wireType 0 =*/112).uint32(message.port);
                if (message.slaveId != null && $Object.hasOwnProperty.call(message, "slaveId") && message.slaveId !== 0)
                    writer.uint32(/* id 15, wireType 0 =*/120).uint32(message.slaveId);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Modbus message, length delimited. Does not implicitly {@link lebai.modbus.Modbus.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.Modbus
             * @static
             * @param {lebai.modbus.Modbus.$Properties} message Modbus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Modbus.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Modbus message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.Modbus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.Modbus & lebai.modbus.Modbus.$Shape} Modbus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Modbus.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.Modbus();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 12: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.kind = value;
                            else
                                delete message.kind;
                            continue;
                        }
                    case 13: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.address = value;
                            else
                                delete message.address;
                            continue;
                        }
                    case 14: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.port = value;
                            else
                                delete message.port;
                            continue;
                        }
                    case 15: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.slaveId = value;
                            else
                                delete message.slaveId;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Modbus message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.Modbus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.Modbus & lebai.modbus.Modbus.$Shape} Modbus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Modbus.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Modbus message.
             * @function verify
             * @memberof lebai.modbus.Modbus
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Modbus.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    if (typeof message.kind !== "number" || (message.kind | 0) !== message.kind)
                        return "kind: enum value expected";
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    if (!$util.isString(message.address))
                        return "address: string expected";
                if (message.port != null && $Object.hasOwnProperty.call(message, "port"))
                    if (!$util.isInteger(message.port))
                        return "port: integer expected";
                if (message.slaveId != null && $Object.hasOwnProperty.call(message, "slaveId"))
                    if (!$util.isInteger(message.slaveId))
                        return "slaveId: integer expected";
                return null;
            };

            /**
             * Creates a Modbus message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.Modbus
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.Modbus} Modbus
             */
            Modbus.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.Modbus)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.Modbus: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.Modbus();
                if (object.kind !== 0 && (typeof object.kind !== "string" || $root.lebai.modbus.ModbusKind[object.kind] !== 0))
                    switch (object.kind) {
                    case "ROBOT":
                    case 0:
                        message.kind = 0;
                        break;
                    case "FLANGE":
                    case 1:
                        message.kind = 1;
                        break;
                    case "EXTRA":
                    case 2:
                        message.kind = 2;
                        break;
                    case "MODBUS_FLANGE":
                    case 3:
                        message.kind = 3;
                        break;
                    case "MODBUS_RTU":
                    case 4:
                        message.kind = 4;
                        break;
                    case "MODBUS_TCP":
                    case 5:
                        message.kind = 5;
                        break;
                    default:
                        if (typeof object.kind === "number" && (object.kind | 0) === object.kind)
                            message.kind = object.kind;
                    }
                if (object.address != null)
                    if (typeof object.address !== "string" || object.address.length)
                        message.address = $String(object.address);
                if (object.port != null)
                    if ($Number(object.port) !== 0)
                        message.port = object.port >>> 0;
                if (object.slaveId != null)
                    if ($Number(object.slaveId) !== 0)
                        message.slaveId = object.slaveId >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a Modbus message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.Modbus
             * @static
             * @param {lebai.modbus.Modbus} message Modbus
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Modbus.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.kind = options.enums === $String ? "ROBOT" : 0;
                    object.address = "";
                    object.port = 0;
                    object.slaveId = 0;
                }
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    object.kind = options.enums === $String ? $root.lebai.modbus.ModbusKind[message.kind] === $undefined ? message.kind : $root.lebai.modbus.ModbusKind[message.kind] : message.kind;
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    object.address = message.address;
                if (message.port != null && $Object.hasOwnProperty.call(message, "port"))
                    object.port = message.port;
                if (message.slaveId != null && $Object.hasOwnProperty.call(message, "slaveId"))
                    object.slaveId = message.slaveId;
                return object;
            };

            /**
             * Converts this Modbus to JSON.
             * @function toJSON
             * @memberof lebai.modbus.Modbus
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Modbus.prototype.toJSON = function() {
                return Modbus.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Modbus
             * @function getTypeUrl
             * @memberof lebai.modbus.Modbus
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Modbus.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.Modbus";
            };

            return Modbus;
        })();

        modbus.SaveModbusRequest = (function() {

            /**
             * Properties of a SaveModbusRequest.
             * @typedef {Object} lebai.modbus.SaveModbusRequest.$Properties
             * @property {string|null} [name] SaveModbusRequest name
             * @property {lebai.modbus.Modbus.$Properties|null} [data] SaveModbusRequest data
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SaveModbusRequest.
             * @memberof lebai.modbus
             * @interface ISaveModbusRequest
             * @augments lebai.modbus.SaveModbusRequest.$Properties
             * @deprecated Use lebai.modbus.SaveModbusRequest.$Properties instead.
             */

            /**
             * Shape of a SaveModbusRequest.
             * @typedef {lebai.modbus.SaveModbusRequest.$Properties} lebai.modbus.SaveModbusRequest.$Shape
             */

            /**
             * Constructs a new SaveModbusRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a SaveModbusRequest.
             * @constructor
             * @param {lebai.modbus.SaveModbusRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SaveModbusRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SaveModbusRequest name.
             * @member {string} name
             * @memberof lebai.modbus.SaveModbusRequest
             * @instance
             */
            SaveModbusRequest.prototype.name = "";

            /**
             * SaveModbusRequest data.
             * @member {lebai.modbus.Modbus.$Properties|null|undefined} data
             * @memberof lebai.modbus.SaveModbusRequest
             * @instance
             */
            SaveModbusRequest.prototype.data = null;

            /**
             * Creates a new SaveModbusRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.SaveModbusRequest
             * @static
             * @param {lebai.modbus.SaveModbusRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.SaveModbusRequest} SaveModbusRequest instance
             * @type {{
             *   (properties: lebai.modbus.SaveModbusRequest.$Shape): lebai.modbus.SaveModbusRequest & lebai.modbus.SaveModbusRequest.$Shape;
             *   (properties?: lebai.modbus.SaveModbusRequest.$Properties): lebai.modbus.SaveModbusRequest;
             * }}
             */
            SaveModbusRequest.create = function(properties) {
                return new SaveModbusRequest(properties);
            };

            /**
             * Encodes the specified SaveModbusRequest message. Does not implicitly {@link lebai.modbus.SaveModbusRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.SaveModbusRequest
             * @static
             * @param {lebai.modbus.SaveModbusRequest.$Properties} message SaveModbusRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveModbusRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    $root.lebai.modbus.Modbus.encode(message.data, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SaveModbusRequest message, length delimited. Does not implicitly {@link lebai.modbus.SaveModbusRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.SaveModbusRequest
             * @static
             * @param {lebai.modbus.SaveModbusRequest.$Properties} message SaveModbusRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveModbusRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SaveModbusRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.SaveModbusRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.SaveModbusRequest & lebai.modbus.SaveModbusRequest.$Shape} SaveModbusRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveModbusRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.SaveModbusRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.data = $root.lebai.modbus.Modbus.decode(reader, reader.uint32(), $undefined, _depth + 1, message.data);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SaveModbusRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.SaveModbusRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.SaveModbusRequest & lebai.modbus.SaveModbusRequest.$Shape} SaveModbusRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveModbusRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SaveModbusRequest message.
             * @function verify
             * @memberof lebai.modbus.SaveModbusRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SaveModbusRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    var error = $root.lebai.modbus.Modbus.verify(message.data, _depth + 1);
                    if (error)
                        return "data." + error;
                }
                return null;
            };

            /**
             * Creates a SaveModbusRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.SaveModbusRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.SaveModbusRequest} SaveModbusRequest
             */
            SaveModbusRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.SaveModbusRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.SaveModbusRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.SaveModbusRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.data != null) {
                    if (!$util.isObject(object.data))
                        throw $TypeError(".lebai.modbus.SaveModbusRequest.data: object expected");
                    message.data = $root.lebai.modbus.Modbus.fromObject(object.data, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a SaveModbusRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.SaveModbusRequest
             * @static
             * @param {lebai.modbus.SaveModbusRequest} message SaveModbusRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SaveModbusRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.data = null;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = $root.lebai.modbus.Modbus.toObject(message.data, options, _depth + 1);
                return object;
            };

            /**
             * Converts this SaveModbusRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.SaveModbusRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SaveModbusRequest.prototype.toJSON = function() {
                return SaveModbusRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SaveModbusRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.SaveModbusRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SaveModbusRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.SaveModbusRequest";
            };

            return SaveModbusRequest;
        })();

        /**
         * ModbusRegisterKind enum.
         * @name lebai.modbus.ModbusRegisterKind
         * @enum {number}
         * @property {number} DISCRETE_INPUT=0 DISCRETE_INPUT value
         * @property {number} COIL=1 COIL value
         * @property {number} INPUT_REGISTER=2 INPUT_REGISTER value
         * @property {number} HOLDING_REGISTER=3 HOLDING_REGISTER value
         */
        modbus.ModbusRegisterKind = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "DISCRETE_INPUT"] = 0;
            values[valuesById[1] = "COIL"] = 1;
            values[valuesById[2] = "INPUT_REGISTER"] = 2;
            values[valuesById[3] = "HOLDING_REGISTER"] = 3;
            return values;
        })();

        modbus.ModbusRegister = (function() {

            /**
             * Properties of a ModbusRegister.
             * @typedef {Object} lebai.modbus.ModbusRegister.$Properties
             * @property {lebai.modbus.ModbusRegisterKind|null} [kind] ModbusRegister kind
             * @property {number|null} [address] ModbusRegister address
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ModbusRegister.
             * @memberof lebai.modbus
             * @interface IModbusRegister
             * @augments lebai.modbus.ModbusRegister.$Properties
             * @deprecated Use lebai.modbus.ModbusRegister.$Properties instead.
             */

            /**
             * Shape of a ModbusRegister.
             * @typedef {lebai.modbus.ModbusRegister.$Properties} lebai.modbus.ModbusRegister.$Shape
             */

            /**
             * Constructs a new ModbusRegister.
             * @memberof lebai.modbus
             * @classdesc Represents a ModbusRegister.
             * @constructor
             * @param {lebai.modbus.ModbusRegister.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ModbusRegister = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ModbusRegister kind.
             * @member {lebai.modbus.ModbusRegisterKind} kind
             * @memberof lebai.modbus.ModbusRegister
             * @instance
             */
            ModbusRegister.prototype.kind = 0;

            /**
             * ModbusRegister address.
             * @member {number} address
             * @memberof lebai.modbus.ModbusRegister
             * @instance
             */
            ModbusRegister.prototype.address = 0;

            /**
             * Creates a new ModbusRegister instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.ModbusRegister
             * @static
             * @param {lebai.modbus.ModbusRegister.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.ModbusRegister} ModbusRegister instance
             * @type {{
             *   (properties: lebai.modbus.ModbusRegister.$Shape): lebai.modbus.ModbusRegister & lebai.modbus.ModbusRegister.$Shape;
             *   (properties?: lebai.modbus.ModbusRegister.$Properties): lebai.modbus.ModbusRegister;
             * }}
             */
            ModbusRegister.create = function(properties) {
                return new ModbusRegister(properties);
            };

            /**
             * Encodes the specified ModbusRegister message. Does not implicitly {@link lebai.modbus.ModbusRegister.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.ModbusRegister
             * @static
             * @param {lebai.modbus.ModbusRegister.$Properties} message ModbusRegister message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ModbusRegister.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind") && message.kind !== 0)
                    writer.uint32(/* id 12, wireType 0 =*/96).int32(message.kind);
                if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== 0)
                    writer.uint32(/* id 13, wireType 0 =*/104).uint32(message.address);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ModbusRegister message, length delimited. Does not implicitly {@link lebai.modbus.ModbusRegister.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.ModbusRegister
             * @static
             * @param {lebai.modbus.ModbusRegister.$Properties} message ModbusRegister message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ModbusRegister.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ModbusRegister message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.ModbusRegister
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.ModbusRegister & lebai.modbus.ModbusRegister.$Shape} ModbusRegister
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ModbusRegister.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.ModbusRegister();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 12: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.kind = value;
                            else
                                delete message.kind;
                            continue;
                        }
                    case 13: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.address = value;
                            else
                                delete message.address;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ModbusRegister message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.ModbusRegister
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.ModbusRegister & lebai.modbus.ModbusRegister.$Shape} ModbusRegister
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ModbusRegister.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ModbusRegister message.
             * @function verify
             * @memberof lebai.modbus.ModbusRegister
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ModbusRegister.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    if (typeof message.kind !== "number" || (message.kind | 0) !== message.kind)
                        return "kind: enum value expected";
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    if (!$util.isInteger(message.address))
                        return "address: integer expected";
                return null;
            };

            /**
             * Creates a ModbusRegister message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.ModbusRegister
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.ModbusRegister} ModbusRegister
             */
            ModbusRegister.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.ModbusRegister)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.ModbusRegister: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.ModbusRegister();
                if (object.kind !== 0 && (typeof object.kind !== "string" || $root.lebai.modbus.ModbusRegisterKind[object.kind] !== 0))
                    switch (object.kind) {
                    case "DISCRETE_INPUT":
                    case 0:
                        message.kind = 0;
                        break;
                    case "COIL":
                    case 1:
                        message.kind = 1;
                        break;
                    case "INPUT_REGISTER":
                    case 2:
                        message.kind = 2;
                        break;
                    case "HOLDING_REGISTER":
                    case 3:
                        message.kind = 3;
                        break;
                    default:
                        if (typeof object.kind === "number" && (object.kind | 0) === object.kind)
                            message.kind = object.kind;
                    }
                if (object.address != null)
                    if ($Number(object.address) !== 0)
                        message.address = object.address >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a ModbusRegister message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.ModbusRegister
             * @static
             * @param {lebai.modbus.ModbusRegister} message ModbusRegister
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ModbusRegister.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.kind = options.enums === $String ? "DISCRETE_INPUT" : 0;
                    object.address = 0;
                }
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    object.kind = options.enums === $String ? $root.lebai.modbus.ModbusRegisterKind[message.kind] === $undefined ? message.kind : $root.lebai.modbus.ModbusRegisterKind[message.kind] : message.kind;
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    object.address = message.address;
                return object;
            };

            /**
             * Converts this ModbusRegister to JSON.
             * @function toJSON
             * @memberof lebai.modbus.ModbusRegister
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ModbusRegister.prototype.toJSON = function() {
                return ModbusRegister.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ModbusRegister
             * @function getTypeUrl
             * @memberof lebai.modbus.ModbusRegister
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ModbusRegister.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.ModbusRegister";
            };

            return ModbusRegister;
        })();

        modbus.SaveModbusRegisterRequest = (function() {

            /**
             * Properties of a SaveModbusRegisterRequest.
             * @typedef {Object} lebai.modbus.SaveModbusRegisterRequest.$Properties
             * @property {string|null} [device] SaveModbusRegisterRequest device
             * @property {string|null} [name] SaveModbusRegisterRequest name
             * @property {lebai.modbus.ModbusRegister.$Properties|null} [data] SaveModbusRegisterRequest data
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SaveModbusRegisterRequest.
             * @memberof lebai.modbus
             * @interface ISaveModbusRegisterRequest
             * @augments lebai.modbus.SaveModbusRegisterRequest.$Properties
             * @deprecated Use lebai.modbus.SaveModbusRegisterRequest.$Properties instead.
             */

            /**
             * Shape of a SaveModbusRegisterRequest.
             * @typedef {lebai.modbus.SaveModbusRegisterRequest.$Properties} lebai.modbus.SaveModbusRegisterRequest.$Shape
             */

            /**
             * Constructs a new SaveModbusRegisterRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a SaveModbusRegisterRequest.
             * @constructor
             * @param {lebai.modbus.SaveModbusRegisterRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SaveModbusRegisterRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SaveModbusRegisterRequest device.
             * @member {string} device
             * @memberof lebai.modbus.SaveModbusRegisterRequest
             * @instance
             */
            SaveModbusRegisterRequest.prototype.device = "";

            /**
             * SaveModbusRegisterRequest name.
             * @member {string} name
             * @memberof lebai.modbus.SaveModbusRegisterRequest
             * @instance
             */
            SaveModbusRegisterRequest.prototype.name = "";

            /**
             * SaveModbusRegisterRequest data.
             * @member {lebai.modbus.ModbusRegister.$Properties|null|undefined} data
             * @memberof lebai.modbus.SaveModbusRegisterRequest
             * @instance
             */
            SaveModbusRegisterRequest.prototype.data = null;

            /**
             * Creates a new SaveModbusRegisterRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.SaveModbusRegisterRequest
             * @static
             * @param {lebai.modbus.SaveModbusRegisterRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.SaveModbusRegisterRequest} SaveModbusRegisterRequest instance
             * @type {{
             *   (properties: lebai.modbus.SaveModbusRegisterRequest.$Shape): lebai.modbus.SaveModbusRegisterRequest & lebai.modbus.SaveModbusRegisterRequest.$Shape;
             *   (properties?: lebai.modbus.SaveModbusRegisterRequest.$Properties): lebai.modbus.SaveModbusRegisterRequest;
             * }}
             */
            SaveModbusRegisterRequest.create = function(properties) {
                return new SaveModbusRegisterRequest(properties);
            };

            /**
             * Encodes the specified SaveModbusRegisterRequest message. Does not implicitly {@link lebai.modbus.SaveModbusRegisterRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.SaveModbusRegisterRequest
             * @static
             * @param {lebai.modbus.SaveModbusRegisterRequest.$Properties} message SaveModbusRegisterRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveModbusRegisterRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    $root.lebai.modbus.ModbusRegister.encode(message.data, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SaveModbusRegisterRequest message, length delimited. Does not implicitly {@link lebai.modbus.SaveModbusRegisterRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.SaveModbusRegisterRequest
             * @static
             * @param {lebai.modbus.SaveModbusRegisterRequest.$Properties} message SaveModbusRegisterRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveModbusRegisterRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SaveModbusRegisterRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.SaveModbusRegisterRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.SaveModbusRegisterRequest & lebai.modbus.SaveModbusRegisterRequest.$Shape} SaveModbusRegisterRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveModbusRegisterRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.SaveModbusRegisterRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.data = $root.lebai.modbus.ModbusRegister.decode(reader, reader.uint32(), $undefined, _depth + 1, message.data);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SaveModbusRegisterRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.SaveModbusRegisterRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.SaveModbusRegisterRequest & lebai.modbus.SaveModbusRegisterRequest.$Shape} SaveModbusRegisterRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveModbusRegisterRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SaveModbusRegisterRequest message.
             * @function verify
             * @memberof lebai.modbus.SaveModbusRegisterRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SaveModbusRegisterRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    var error = $root.lebai.modbus.ModbusRegister.verify(message.data, _depth + 1);
                    if (error)
                        return "data." + error;
                }
                return null;
            };

            /**
             * Creates a SaveModbusRegisterRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.SaveModbusRegisterRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.SaveModbusRegisterRequest} SaveModbusRegisterRequest
             */
            SaveModbusRegisterRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.SaveModbusRegisterRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.SaveModbusRegisterRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.SaveModbusRegisterRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.data != null) {
                    if (!$util.isObject(object.data))
                        throw $TypeError(".lebai.modbus.SaveModbusRegisterRequest.data: object expected");
                    message.data = $root.lebai.modbus.ModbusRegister.fromObject(object.data, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a SaveModbusRegisterRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.SaveModbusRegisterRequest
             * @static
             * @param {lebai.modbus.SaveModbusRegisterRequest} message SaveModbusRegisterRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SaveModbusRegisterRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.name = "";
                    object.data = null;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = $root.lebai.modbus.ModbusRegister.toObject(message.data, options, _depth + 1);
                return object;
            };

            /**
             * Converts this SaveModbusRegisterRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.SaveModbusRegisterRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SaveModbusRegisterRequest.prototype.toJSON = function() {
                return SaveModbusRegisterRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SaveModbusRegisterRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.SaveModbusRegisterRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SaveModbusRegisterRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.SaveModbusRegisterRequest";
            };

            return SaveModbusRegisterRequest;
        })();

        modbus.LoadModbusRegisterRequest = (function() {

            /**
             * Properties of a LoadModbusRegisterRequest.
             * @typedef {Object} lebai.modbus.LoadModbusRegisterRequest.$Properties
             * @property {string|null} [device] LoadModbusRegisterRequest device
             * @property {string|null} [name] LoadModbusRegisterRequest name
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LoadModbusRegisterRequest.
             * @memberof lebai.modbus
             * @interface ILoadModbusRegisterRequest
             * @augments lebai.modbus.LoadModbusRegisterRequest.$Properties
             * @deprecated Use lebai.modbus.LoadModbusRegisterRequest.$Properties instead.
             */

            /**
             * Shape of a LoadModbusRegisterRequest.
             * @typedef {lebai.modbus.LoadModbusRegisterRequest.$Properties} lebai.modbus.LoadModbusRegisterRequest.$Shape
             */

            /**
             * Constructs a new LoadModbusRegisterRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a LoadModbusRegisterRequest.
             * @constructor
             * @param {lebai.modbus.LoadModbusRegisterRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LoadModbusRegisterRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LoadModbusRegisterRequest device.
             * @member {string} device
             * @memberof lebai.modbus.LoadModbusRegisterRequest
             * @instance
             */
            LoadModbusRegisterRequest.prototype.device = "";

            /**
             * LoadModbusRegisterRequest name.
             * @member {string} name
             * @memberof lebai.modbus.LoadModbusRegisterRequest
             * @instance
             */
            LoadModbusRegisterRequest.prototype.name = "";

            /**
             * Creates a new LoadModbusRegisterRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.LoadModbusRegisterRequest
             * @static
             * @param {lebai.modbus.LoadModbusRegisterRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.LoadModbusRegisterRequest} LoadModbusRegisterRequest instance
             * @type {{
             *   (properties: lebai.modbus.LoadModbusRegisterRequest.$Shape): lebai.modbus.LoadModbusRegisterRequest & lebai.modbus.LoadModbusRegisterRequest.$Shape;
             *   (properties?: lebai.modbus.LoadModbusRegisterRequest.$Properties): lebai.modbus.LoadModbusRegisterRequest;
             * }}
             */
            LoadModbusRegisterRequest.create = function(properties) {
                return new LoadModbusRegisterRequest(properties);
            };

            /**
             * Encodes the specified LoadModbusRegisterRequest message. Does not implicitly {@link lebai.modbus.LoadModbusRegisterRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.LoadModbusRegisterRequest
             * @static
             * @param {lebai.modbus.LoadModbusRegisterRequest.$Properties} message LoadModbusRegisterRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadModbusRegisterRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LoadModbusRegisterRequest message, length delimited. Does not implicitly {@link lebai.modbus.LoadModbusRegisterRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.LoadModbusRegisterRequest
             * @static
             * @param {lebai.modbus.LoadModbusRegisterRequest.$Properties} message LoadModbusRegisterRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadModbusRegisterRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LoadModbusRegisterRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.LoadModbusRegisterRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.LoadModbusRegisterRequest & lebai.modbus.LoadModbusRegisterRequest.$Shape} LoadModbusRegisterRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadModbusRegisterRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.LoadModbusRegisterRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LoadModbusRegisterRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.LoadModbusRegisterRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.LoadModbusRegisterRequest & lebai.modbus.LoadModbusRegisterRequest.$Shape} LoadModbusRegisterRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadModbusRegisterRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LoadModbusRegisterRequest message.
             * @function verify
             * @memberof lebai.modbus.LoadModbusRegisterRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LoadModbusRegisterRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                return null;
            };

            /**
             * Creates a LoadModbusRegisterRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.LoadModbusRegisterRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.LoadModbusRegisterRequest} LoadModbusRegisterRequest
             */
            LoadModbusRegisterRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.LoadModbusRegisterRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.LoadModbusRegisterRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.LoadModbusRegisterRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                return message;
            };

            /**
             * Creates a plain object from a LoadModbusRegisterRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.LoadModbusRegisterRequest
             * @static
             * @param {lebai.modbus.LoadModbusRegisterRequest} message LoadModbusRegisterRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LoadModbusRegisterRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.name = "";
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                return object;
            };

            /**
             * Converts this LoadModbusRegisterRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.LoadModbusRegisterRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LoadModbusRegisterRequest.prototype.toJSON = function() {
                return LoadModbusRegisterRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LoadModbusRegisterRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.LoadModbusRegisterRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LoadModbusRegisterRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.LoadModbusRegisterRequest";
            };

            return LoadModbusRegisterRequest;
        })();

        modbus.LoadModbusRegisterListRequest = (function() {

            /**
             * Properties of a LoadModbusRegisterListRequest.
             * @typedef {Object} lebai.modbus.LoadModbusRegisterListRequest.$Properties
             * @property {string|null} [device] LoadModbusRegisterListRequest device
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a LoadModbusRegisterListRequest.
             * @memberof lebai.modbus
             * @interface ILoadModbusRegisterListRequest
             * @augments lebai.modbus.LoadModbusRegisterListRequest.$Properties
             * @deprecated Use lebai.modbus.LoadModbusRegisterListRequest.$Properties instead.
             */

            /**
             * Shape of a LoadModbusRegisterListRequest.
             * @typedef {lebai.modbus.LoadModbusRegisterListRequest.$Properties} lebai.modbus.LoadModbusRegisterListRequest.$Shape
             */

            /**
             * Constructs a new LoadModbusRegisterListRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a LoadModbusRegisterListRequest.
             * @constructor
             * @param {lebai.modbus.LoadModbusRegisterListRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var LoadModbusRegisterListRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * LoadModbusRegisterListRequest device.
             * @member {string} device
             * @memberof lebai.modbus.LoadModbusRegisterListRequest
             * @instance
             */
            LoadModbusRegisterListRequest.prototype.device = "";

            /**
             * Creates a new LoadModbusRegisterListRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.LoadModbusRegisterListRequest
             * @static
             * @param {lebai.modbus.LoadModbusRegisterListRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.LoadModbusRegisterListRequest} LoadModbusRegisterListRequest instance
             * @type {{
             *   (properties: lebai.modbus.LoadModbusRegisterListRequest.$Shape): lebai.modbus.LoadModbusRegisterListRequest & lebai.modbus.LoadModbusRegisterListRequest.$Shape;
             *   (properties?: lebai.modbus.LoadModbusRegisterListRequest.$Properties): lebai.modbus.LoadModbusRegisterListRequest;
             * }}
             */
            LoadModbusRegisterListRequest.create = function(properties) {
                return new LoadModbusRegisterListRequest(properties);
            };

            /**
             * Encodes the specified LoadModbusRegisterListRequest message. Does not implicitly {@link lebai.modbus.LoadModbusRegisterListRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.LoadModbusRegisterListRequest
             * @static
             * @param {lebai.modbus.LoadModbusRegisterListRequest.$Properties} message LoadModbusRegisterListRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadModbusRegisterListRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LoadModbusRegisterListRequest message, length delimited. Does not implicitly {@link lebai.modbus.LoadModbusRegisterListRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.LoadModbusRegisterListRequest
             * @static
             * @param {lebai.modbus.LoadModbusRegisterListRequest.$Properties} message LoadModbusRegisterListRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadModbusRegisterListRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a LoadModbusRegisterListRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.LoadModbusRegisterListRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.LoadModbusRegisterListRequest & lebai.modbus.LoadModbusRegisterListRequest.$Shape} LoadModbusRegisterListRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadModbusRegisterListRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.LoadModbusRegisterListRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a LoadModbusRegisterListRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.LoadModbusRegisterListRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.LoadModbusRegisterListRequest & lebai.modbus.LoadModbusRegisterListRequest.$Shape} LoadModbusRegisterListRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadModbusRegisterListRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LoadModbusRegisterListRequest message.
             * @function verify
             * @memberof lebai.modbus.LoadModbusRegisterListRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LoadModbusRegisterListRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                return null;
            };

            /**
             * Creates a LoadModbusRegisterListRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.LoadModbusRegisterListRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.LoadModbusRegisterListRequest} LoadModbusRegisterListRequest
             */
            LoadModbusRegisterListRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.LoadModbusRegisterListRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.LoadModbusRegisterListRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.LoadModbusRegisterListRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                return message;
            };

            /**
             * Creates a plain object from a LoadModbusRegisterListRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.LoadModbusRegisterListRequest
             * @static
             * @param {lebai.modbus.LoadModbusRegisterListRequest} message LoadModbusRegisterListRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LoadModbusRegisterListRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.device = "";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                return object;
            };

            /**
             * Converts this LoadModbusRegisterListRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.LoadModbusRegisterListRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LoadModbusRegisterListRequest.prototype.toJSON = function() {
                return LoadModbusRegisterListRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for LoadModbusRegisterListRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.LoadModbusRegisterListRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            LoadModbusRegisterListRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.LoadModbusRegisterListRequest";
            };

            return LoadModbusRegisterListRequest;
        })();

        modbus.DisconnectModbusRequest = (function() {

            /**
             * Properties of a DisconnectModbusRequest.
             * @typedef {Object} lebai.modbus.DisconnectModbusRequest.$Properties
             * @property {string|null} [device] DisconnectModbusRequest device
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DisconnectModbusRequest.
             * @memberof lebai.modbus
             * @interface IDisconnectModbusRequest
             * @augments lebai.modbus.DisconnectModbusRequest.$Properties
             * @deprecated Use lebai.modbus.DisconnectModbusRequest.$Properties instead.
             */

            /**
             * Shape of a DisconnectModbusRequest.
             * @typedef {lebai.modbus.DisconnectModbusRequest.$Properties} lebai.modbus.DisconnectModbusRequest.$Shape
             */

            /**
             * Constructs a new DisconnectModbusRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a DisconnectModbusRequest.
             * @constructor
             * @param {lebai.modbus.DisconnectModbusRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var DisconnectModbusRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DisconnectModbusRequest device.
             * @member {string} device
             * @memberof lebai.modbus.DisconnectModbusRequest
             * @instance
             */
            DisconnectModbusRequest.prototype.device = "";

            /**
             * Creates a new DisconnectModbusRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.DisconnectModbusRequest
             * @static
             * @param {lebai.modbus.DisconnectModbusRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.DisconnectModbusRequest} DisconnectModbusRequest instance
             * @type {{
             *   (properties: lebai.modbus.DisconnectModbusRequest.$Shape): lebai.modbus.DisconnectModbusRequest & lebai.modbus.DisconnectModbusRequest.$Shape;
             *   (properties?: lebai.modbus.DisconnectModbusRequest.$Properties): lebai.modbus.DisconnectModbusRequest;
             * }}
             */
            DisconnectModbusRequest.create = function(properties) {
                return new DisconnectModbusRequest(properties);
            };

            /**
             * Encodes the specified DisconnectModbusRequest message. Does not implicitly {@link lebai.modbus.DisconnectModbusRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.DisconnectModbusRequest
             * @static
             * @param {lebai.modbus.DisconnectModbusRequest.$Properties} message DisconnectModbusRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DisconnectModbusRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DisconnectModbusRequest message, length delimited. Does not implicitly {@link lebai.modbus.DisconnectModbusRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.DisconnectModbusRequest
             * @static
             * @param {lebai.modbus.DisconnectModbusRequest.$Properties} message DisconnectModbusRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DisconnectModbusRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a DisconnectModbusRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.DisconnectModbusRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.DisconnectModbusRequest & lebai.modbus.DisconnectModbusRequest.$Shape} DisconnectModbusRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DisconnectModbusRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.DisconnectModbusRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a DisconnectModbusRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.DisconnectModbusRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.DisconnectModbusRequest & lebai.modbus.DisconnectModbusRequest.$Shape} DisconnectModbusRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DisconnectModbusRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DisconnectModbusRequest message.
             * @function verify
             * @memberof lebai.modbus.DisconnectModbusRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DisconnectModbusRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                return null;
            };

            /**
             * Creates a DisconnectModbusRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.DisconnectModbusRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.DisconnectModbusRequest} DisconnectModbusRequest
             */
            DisconnectModbusRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.DisconnectModbusRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.DisconnectModbusRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.DisconnectModbusRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                return message;
            };

            /**
             * Creates a plain object from a DisconnectModbusRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.DisconnectModbusRequest
             * @static
             * @param {lebai.modbus.DisconnectModbusRequest} message DisconnectModbusRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DisconnectModbusRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.device = "";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                return object;
            };

            /**
             * Converts this DisconnectModbusRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.DisconnectModbusRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DisconnectModbusRequest.prototype.toJSON = function() {
                return DisconnectModbusRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DisconnectModbusRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.DisconnectModbusRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DisconnectModbusRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.DisconnectModbusRequest";
            };

            return DisconnectModbusRequest;
        })();

        modbus.SetModbusTimeoutRequest = (function() {

            /**
             * Properties of a SetModbusTimeoutRequest.
             * @typedef {Object} lebai.modbus.SetModbusTimeoutRequest.$Properties
             * @property {string|null} [device] SetModbusTimeoutRequest device
             * @property {number|null} [timeout] SetModbusTimeoutRequest timeout
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetModbusTimeoutRequest.
             * @memberof lebai.modbus
             * @interface ISetModbusTimeoutRequest
             * @augments lebai.modbus.SetModbusTimeoutRequest.$Properties
             * @deprecated Use lebai.modbus.SetModbusTimeoutRequest.$Properties instead.
             */

            /**
             * Shape of a SetModbusTimeoutRequest.
             * @typedef {lebai.modbus.SetModbusTimeoutRequest.$Properties} lebai.modbus.SetModbusTimeoutRequest.$Shape
             */

            /**
             * Constructs a new SetModbusTimeoutRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a SetModbusTimeoutRequest.
             * @constructor
             * @param {lebai.modbus.SetModbusTimeoutRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetModbusTimeoutRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetModbusTimeoutRequest device.
             * @member {string} device
             * @memberof lebai.modbus.SetModbusTimeoutRequest
             * @instance
             */
            SetModbusTimeoutRequest.prototype.device = "";

            /**
             * SetModbusTimeoutRequest timeout.
             * @member {number} timeout
             * @memberof lebai.modbus.SetModbusTimeoutRequest
             * @instance
             */
            SetModbusTimeoutRequest.prototype.timeout = 0;

            /**
             * Creates a new SetModbusTimeoutRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.SetModbusTimeoutRequest
             * @static
             * @param {lebai.modbus.SetModbusTimeoutRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.SetModbusTimeoutRequest} SetModbusTimeoutRequest instance
             * @type {{
             *   (properties: lebai.modbus.SetModbusTimeoutRequest.$Shape): lebai.modbus.SetModbusTimeoutRequest & lebai.modbus.SetModbusTimeoutRequest.$Shape;
             *   (properties?: lebai.modbus.SetModbusTimeoutRequest.$Properties): lebai.modbus.SetModbusTimeoutRequest;
             * }}
             */
            SetModbusTimeoutRequest.create = function(properties) {
                return new SetModbusTimeoutRequest(properties);
            };

            /**
             * Encodes the specified SetModbusTimeoutRequest message. Does not implicitly {@link lebai.modbus.SetModbusTimeoutRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.SetModbusTimeoutRequest
             * @static
             * @param {lebai.modbus.SetModbusTimeoutRequest.$Properties} message SetModbusTimeoutRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetModbusTimeoutRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout") && message.timeout !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.timeout);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetModbusTimeoutRequest message, length delimited. Does not implicitly {@link lebai.modbus.SetModbusTimeoutRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.SetModbusTimeoutRequest
             * @static
             * @param {lebai.modbus.SetModbusTimeoutRequest.$Properties} message SetModbusTimeoutRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetModbusTimeoutRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetModbusTimeoutRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.SetModbusTimeoutRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.SetModbusTimeoutRequest & lebai.modbus.SetModbusTimeoutRequest.$Shape} SetModbusTimeoutRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetModbusTimeoutRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.SetModbusTimeoutRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.timeout = value;
                            else
                                delete message.timeout;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetModbusTimeoutRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.SetModbusTimeoutRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.SetModbusTimeoutRequest & lebai.modbus.SetModbusTimeoutRequest.$Shape} SetModbusTimeoutRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetModbusTimeoutRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetModbusTimeoutRequest message.
             * @function verify
             * @memberof lebai.modbus.SetModbusTimeoutRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetModbusTimeoutRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout"))
                    if (!$util.isInteger(message.timeout))
                        return "timeout: integer expected";
                return null;
            };

            /**
             * Creates a SetModbusTimeoutRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.SetModbusTimeoutRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.SetModbusTimeoutRequest} SetModbusTimeoutRequest
             */
            SetModbusTimeoutRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.SetModbusTimeoutRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.SetModbusTimeoutRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.SetModbusTimeoutRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.timeout != null)
                    if ($Number(object.timeout) !== 0)
                        message.timeout = object.timeout >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a SetModbusTimeoutRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.SetModbusTimeoutRequest
             * @static
             * @param {lebai.modbus.SetModbusTimeoutRequest} message SetModbusTimeoutRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetModbusTimeoutRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.timeout = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout"))
                    object.timeout = message.timeout;
                return object;
            };

            /**
             * Converts this SetModbusTimeoutRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.SetModbusTimeoutRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetModbusTimeoutRequest.prototype.toJSON = function() {
                return SetModbusTimeoutRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetModbusTimeoutRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.SetModbusTimeoutRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetModbusTimeoutRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.SetModbusTimeoutRequest";
            };

            return SetModbusTimeoutRequest;
        })();

        modbus.SetModbusRetryRequest = (function() {

            /**
             * Properties of a SetModbusRetryRequest.
             * @typedef {Object} lebai.modbus.SetModbusRetryRequest.$Properties
             * @property {string|null} [device] SetModbusRetryRequest device
             * @property {number|null} [retry] SetModbusRetryRequest retry
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetModbusRetryRequest.
             * @memberof lebai.modbus
             * @interface ISetModbusRetryRequest
             * @augments lebai.modbus.SetModbusRetryRequest.$Properties
             * @deprecated Use lebai.modbus.SetModbusRetryRequest.$Properties instead.
             */

            /**
             * Shape of a SetModbusRetryRequest.
             * @typedef {lebai.modbus.SetModbusRetryRequest.$Properties} lebai.modbus.SetModbusRetryRequest.$Shape
             */

            /**
             * Constructs a new SetModbusRetryRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a SetModbusRetryRequest.
             * @constructor
             * @param {lebai.modbus.SetModbusRetryRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetModbusRetryRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetModbusRetryRequest device.
             * @member {string} device
             * @memberof lebai.modbus.SetModbusRetryRequest
             * @instance
             */
            SetModbusRetryRequest.prototype.device = "";

            /**
             * SetModbusRetryRequest retry.
             * @member {number} retry
             * @memberof lebai.modbus.SetModbusRetryRequest
             * @instance
             */
            SetModbusRetryRequest.prototype.retry = 0;

            /**
             * Creates a new SetModbusRetryRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.SetModbusRetryRequest
             * @static
             * @param {lebai.modbus.SetModbusRetryRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.SetModbusRetryRequest} SetModbusRetryRequest instance
             * @type {{
             *   (properties: lebai.modbus.SetModbusRetryRequest.$Shape): lebai.modbus.SetModbusRetryRequest & lebai.modbus.SetModbusRetryRequest.$Shape;
             *   (properties?: lebai.modbus.SetModbusRetryRequest.$Properties): lebai.modbus.SetModbusRetryRequest;
             * }}
             */
            SetModbusRetryRequest.create = function(properties) {
                return new SetModbusRetryRequest(properties);
            };

            /**
             * Encodes the specified SetModbusRetryRequest message. Does not implicitly {@link lebai.modbus.SetModbusRetryRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.SetModbusRetryRequest
             * @static
             * @param {lebai.modbus.SetModbusRetryRequest.$Properties} message SetModbusRetryRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetModbusRetryRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.retry != null && $Object.hasOwnProperty.call(message, "retry") && message.retry !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.retry);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetModbusRetryRequest message, length delimited. Does not implicitly {@link lebai.modbus.SetModbusRetryRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.SetModbusRetryRequest
             * @static
             * @param {lebai.modbus.SetModbusRetryRequest.$Properties} message SetModbusRetryRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetModbusRetryRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetModbusRetryRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.SetModbusRetryRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.SetModbusRetryRequest & lebai.modbus.SetModbusRetryRequest.$Shape} SetModbusRetryRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetModbusRetryRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.SetModbusRetryRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.retry = value;
                            else
                                delete message.retry;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetModbusRetryRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.SetModbusRetryRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.SetModbusRetryRequest & lebai.modbus.SetModbusRetryRequest.$Shape} SetModbusRetryRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetModbusRetryRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetModbusRetryRequest message.
             * @function verify
             * @memberof lebai.modbus.SetModbusRetryRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetModbusRetryRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.retry != null && $Object.hasOwnProperty.call(message, "retry"))
                    if (!$util.isInteger(message.retry))
                        return "retry: integer expected";
                return null;
            };

            /**
             * Creates a SetModbusRetryRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.SetModbusRetryRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.SetModbusRetryRequest} SetModbusRetryRequest
             */
            SetModbusRetryRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.SetModbusRetryRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.SetModbusRetryRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.SetModbusRetryRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.retry != null)
                    if ($Number(object.retry) !== 0)
                        message.retry = object.retry >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a SetModbusRetryRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.SetModbusRetryRequest
             * @static
             * @param {lebai.modbus.SetModbusRetryRequest} message SetModbusRetryRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetModbusRetryRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.retry = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.retry != null && $Object.hasOwnProperty.call(message, "retry"))
                    object.retry = message.retry;
                return object;
            };

            /**
             * Converts this SetModbusRetryRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.SetModbusRetryRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetModbusRetryRequest.prototype.toJSON = function() {
                return SetModbusRetryRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetModbusRetryRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.SetModbusRetryRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetModbusRetryRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.SetModbusRetryRequest";
            };

            return SetModbusRetryRequest;
        })();

        modbus.SetCoilRequest = (function() {

            /**
             * Properties of a SetCoilRequest.
             * @typedef {Object} lebai.modbus.SetCoilRequest.$Properties
             * @property {string|null} [device] SetCoilRequest device
             * @property {string|null} [pin] SetCoilRequest pin
             * @property {boolean|null} [value] SetCoilRequest value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetCoilRequest.
             * @memberof lebai.modbus
             * @interface ISetCoilRequest
             * @augments lebai.modbus.SetCoilRequest.$Properties
             * @deprecated Use lebai.modbus.SetCoilRequest.$Properties instead.
             */

            /**
             * Shape of a SetCoilRequest.
             * @typedef {lebai.modbus.SetCoilRequest.$Properties} lebai.modbus.SetCoilRequest.$Shape
             */

            /**
             * Constructs a new SetCoilRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a SetCoilRequest.
             * @constructor
             * @param {lebai.modbus.SetCoilRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetCoilRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetCoilRequest device.
             * @member {string} device
             * @memberof lebai.modbus.SetCoilRequest
             * @instance
             */
            SetCoilRequest.prototype.device = "";

            /**
             * SetCoilRequest pin.
             * @member {string} pin
             * @memberof lebai.modbus.SetCoilRequest
             * @instance
             */
            SetCoilRequest.prototype.pin = "";

            /**
             * SetCoilRequest value.
             * @member {boolean} value
             * @memberof lebai.modbus.SetCoilRequest
             * @instance
             */
            SetCoilRequest.prototype.value = false;

            /**
             * Creates a new SetCoilRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.SetCoilRequest
             * @static
             * @param {lebai.modbus.SetCoilRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.SetCoilRequest} SetCoilRequest instance
             * @type {{
             *   (properties: lebai.modbus.SetCoilRequest.$Shape): lebai.modbus.SetCoilRequest & lebai.modbus.SetCoilRequest.$Shape;
             *   (properties?: lebai.modbus.SetCoilRequest.$Properties): lebai.modbus.SetCoilRequest;
             * }}
             */
            SetCoilRequest.create = function(properties) {
                return new SetCoilRequest(properties);
            };

            /**
             * Encodes the specified SetCoilRequest message. Does not implicitly {@link lebai.modbus.SetCoilRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.SetCoilRequest
             * @static
             * @param {lebai.modbus.SetCoilRequest.$Properties} message SetCoilRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetCoilRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.pin);
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== false)
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetCoilRequest message, length delimited. Does not implicitly {@link lebai.modbus.SetCoilRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.SetCoilRequest
             * @static
             * @param {lebai.modbus.SetCoilRequest.$Properties} message SetCoilRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetCoilRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetCoilRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.SetCoilRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.SetCoilRequest & lebai.modbus.SetCoilRequest.$Shape} SetCoilRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetCoilRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.SetCoilRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetCoilRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.SetCoilRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.SetCoilRequest & lebai.modbus.SetCoilRequest.$Shape} SetCoilRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetCoilRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetCoilRequest message.
             * @function verify
             * @memberof lebai.modbus.SetCoilRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetCoilRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isString(message.pin))
                        return "pin: string expected";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (typeof message.value !== "boolean")
                        return "value: boolean expected";
                return null;
            };

            /**
             * Creates a SetCoilRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.SetCoilRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.SetCoilRequest} SetCoilRequest
             */
            SetCoilRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.SetCoilRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.SetCoilRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.SetCoilRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.pin != null)
                    if (typeof object.pin !== "string" || object.pin.length)
                        message.pin = $String(object.pin);
                if (object.value != null)
                    if (object.value)
                        message.value = $Boolean(object.value);
                return message;
            };

            /**
             * Creates a plain object from a SetCoilRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.SetCoilRequest
             * @static
             * @param {lebai.modbus.SetCoilRequest} message SetCoilRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetCoilRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.pin = "";
                    object.value = false;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this SetCoilRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.SetCoilRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetCoilRequest.prototype.toJSON = function() {
                return SetCoilRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetCoilRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.SetCoilRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetCoilRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.SetCoilRequest";
            };

            return SetCoilRequest;
        })();

        modbus.SetCoilsRequest = (function() {

            /**
             * Properties of a SetCoilsRequest.
             * @typedef {Object} lebai.modbus.SetCoilsRequest.$Properties
             * @property {string|null} [device] SetCoilsRequest device
             * @property {string|null} [pin] SetCoilsRequest pin
             * @property {Array.<boolean>|null} [values] SetCoilsRequest values
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetCoilsRequest.
             * @memberof lebai.modbus
             * @interface ISetCoilsRequest
             * @augments lebai.modbus.SetCoilsRequest.$Properties
             * @deprecated Use lebai.modbus.SetCoilsRequest.$Properties instead.
             */

            /**
             * Shape of a SetCoilsRequest.
             * @typedef {lebai.modbus.SetCoilsRequest.$Properties} lebai.modbus.SetCoilsRequest.$Shape
             */

            /**
             * Constructs a new SetCoilsRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a SetCoilsRequest.
             * @constructor
             * @param {lebai.modbus.SetCoilsRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetCoilsRequest = function (properties) {
                this.values = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetCoilsRequest device.
             * @member {string} device
             * @memberof lebai.modbus.SetCoilsRequest
             * @instance
             */
            SetCoilsRequest.prototype.device = "";

            /**
             * SetCoilsRequest pin.
             * @member {string} pin
             * @memberof lebai.modbus.SetCoilsRequest
             * @instance
             */
            SetCoilsRequest.prototype.pin = "";

            /**
             * SetCoilsRequest values.
             * @member {Array.<boolean>} values
             * @memberof lebai.modbus.SetCoilsRequest
             * @instance
             */
            SetCoilsRequest.prototype.values = $util.emptyArray;

            /**
             * Creates a new SetCoilsRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.SetCoilsRequest
             * @static
             * @param {lebai.modbus.SetCoilsRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.SetCoilsRequest} SetCoilsRequest instance
             * @type {{
             *   (properties: lebai.modbus.SetCoilsRequest.$Shape): lebai.modbus.SetCoilsRequest & lebai.modbus.SetCoilsRequest.$Shape;
             *   (properties?: lebai.modbus.SetCoilsRequest.$Properties): lebai.modbus.SetCoilsRequest;
             * }}
             */
            SetCoilsRequest.create = function(properties) {
                return new SetCoilsRequest(properties);
            };

            /**
             * Encodes the specified SetCoilsRequest message. Does not implicitly {@link lebai.modbus.SetCoilsRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.SetCoilsRequest
             * @static
             * @param {lebai.modbus.SetCoilsRequest.$Properties} message SetCoilsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetCoilsRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.pin);
                if (message.values != null && message.values.length)
                    writer.uint32(/* id 11, wireType 2 =*/90).bools(message.values);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetCoilsRequest message, length delimited. Does not implicitly {@link lebai.modbus.SetCoilsRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.SetCoilsRequest
             * @static
             * @param {lebai.modbus.SetCoilsRequest.$Properties} message SetCoilsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetCoilsRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetCoilsRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.SetCoilsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.SetCoilsRequest & lebai.modbus.SetCoilsRequest.$Shape} SetCoilsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetCoilsRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.SetCoilsRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.values && message.values.length))
                                    message.values = [];
                                reader.bools(message.values);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push(reader.bool());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetCoilsRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.SetCoilsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.SetCoilsRequest & lebai.modbus.SetCoilsRequest.$Shape} SetCoilsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetCoilsRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetCoilsRequest message.
             * @function verify
             * @memberof lebai.modbus.SetCoilsRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetCoilsRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isString(message.pin))
                        return "pin: string expected";
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (var i = 0; i < message.values.length; ++i)
                        if (typeof message.values[i] !== "boolean")
                            return "values: boolean[] expected";
                }
                return null;
            };

            /**
             * Creates a SetCoilsRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.SetCoilsRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.SetCoilsRequest} SetCoilsRequest
             */
            SetCoilsRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.SetCoilsRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.SetCoilsRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.SetCoilsRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.pin != null)
                    if (typeof object.pin !== "string" || object.pin.length)
                        message.pin = $String(object.pin);
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".lebai.modbus.SetCoilsRequest.values: array expected");
                    message.values = $Array(object.values.length);
                    for (var i = 0; i < object.values.length; ++i)
                        message.values[i] = $Boolean(object.values[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a SetCoilsRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.SetCoilsRequest
             * @static
             * @param {lebai.modbus.SetCoilsRequest} message SetCoilsRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetCoilsRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (options.defaults) {
                    object.device = "";
                    object.pin = "";
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (var j = 0; j < message.values.length; ++j)
                        object.values[j] = message.values[j];
                }
                return object;
            };

            /**
             * Converts this SetCoilsRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.SetCoilsRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetCoilsRequest.prototype.toJSON = function() {
                return SetCoilsRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetCoilsRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.SetCoilsRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetCoilsRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.SetCoilsRequest";
            };

            return SetCoilsRequest;
        })();

        modbus.GetCoilRequest = (function() {

            /**
             * Properties of a GetCoilRequest.
             * @typedef {Object} lebai.modbus.GetCoilRequest.$Properties
             * @property {string|null} [device] GetCoilRequest device
             * @property {string|null} [pin] GetCoilRequest pin
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetCoilRequest.
             * @memberof lebai.modbus
             * @interface IGetCoilRequest
             * @augments lebai.modbus.GetCoilRequest.$Properties
             * @deprecated Use lebai.modbus.GetCoilRequest.$Properties instead.
             */

            /**
             * Shape of a GetCoilRequest.
             * @typedef {lebai.modbus.GetCoilRequest.$Properties} lebai.modbus.GetCoilRequest.$Shape
             */

            /**
             * Constructs a new GetCoilRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a GetCoilRequest.
             * @constructor
             * @param {lebai.modbus.GetCoilRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetCoilRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetCoilRequest device.
             * @member {string} device
             * @memberof lebai.modbus.GetCoilRequest
             * @instance
             */
            GetCoilRequest.prototype.device = "";

            /**
             * GetCoilRequest pin.
             * @member {string} pin
             * @memberof lebai.modbus.GetCoilRequest
             * @instance
             */
            GetCoilRequest.prototype.pin = "";

            /**
             * Creates a new GetCoilRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.GetCoilRequest
             * @static
             * @param {lebai.modbus.GetCoilRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.GetCoilRequest} GetCoilRequest instance
             * @type {{
             *   (properties: lebai.modbus.GetCoilRequest.$Shape): lebai.modbus.GetCoilRequest & lebai.modbus.GetCoilRequest.$Shape;
             *   (properties?: lebai.modbus.GetCoilRequest.$Properties): lebai.modbus.GetCoilRequest;
             * }}
             */
            GetCoilRequest.create = function(properties) {
                return new GetCoilRequest(properties);
            };

            /**
             * Encodes the specified GetCoilRequest message. Does not implicitly {@link lebai.modbus.GetCoilRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.GetCoilRequest
             * @static
             * @param {lebai.modbus.GetCoilRequest.$Properties} message GetCoilRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetCoilRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.pin);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetCoilRequest message, length delimited. Does not implicitly {@link lebai.modbus.GetCoilRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.GetCoilRequest
             * @static
             * @param {lebai.modbus.GetCoilRequest.$Properties} message GetCoilRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetCoilRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetCoilRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.GetCoilRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.GetCoilRequest & lebai.modbus.GetCoilRequest.$Shape} GetCoilRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetCoilRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.GetCoilRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetCoilRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.GetCoilRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.GetCoilRequest & lebai.modbus.GetCoilRequest.$Shape} GetCoilRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetCoilRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetCoilRequest message.
             * @function verify
             * @memberof lebai.modbus.GetCoilRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetCoilRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isString(message.pin))
                        return "pin: string expected";
                return null;
            };

            /**
             * Creates a GetCoilRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.GetCoilRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.GetCoilRequest} GetCoilRequest
             */
            GetCoilRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.GetCoilRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.GetCoilRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.GetCoilRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.pin != null)
                    if (typeof object.pin !== "string" || object.pin.length)
                        message.pin = $String(object.pin);
                return message;
            };

            /**
             * Creates a plain object from a GetCoilRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.GetCoilRequest
             * @static
             * @param {lebai.modbus.GetCoilRequest} message GetCoilRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetCoilRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.pin = "";
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                return object;
            };

            /**
             * Converts this GetCoilRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.GetCoilRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetCoilRequest.prototype.toJSON = function() {
                return GetCoilRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetCoilRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.GetCoilRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetCoilRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.GetCoilRequest";
            };

            return GetCoilRequest;
        })();

        modbus.GetCoilResponse = (function() {

            /**
             * Properties of a GetCoilResponse.
             * @typedef {Object} lebai.modbus.GetCoilResponse.$Properties
             * @property {boolean|null} [value] GetCoilResponse value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetCoilResponse.
             * @memberof lebai.modbus
             * @interface IGetCoilResponse
             * @augments lebai.modbus.GetCoilResponse.$Properties
             * @deprecated Use lebai.modbus.GetCoilResponse.$Properties instead.
             */

            /**
             * Shape of a GetCoilResponse.
             * @typedef {lebai.modbus.GetCoilResponse.$Properties} lebai.modbus.GetCoilResponse.$Shape
             */

            /**
             * Constructs a new GetCoilResponse.
             * @memberof lebai.modbus
             * @classdesc Represents a GetCoilResponse.
             * @constructor
             * @param {lebai.modbus.GetCoilResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetCoilResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetCoilResponse value.
             * @member {boolean} value
             * @memberof lebai.modbus.GetCoilResponse
             * @instance
             */
            GetCoilResponse.prototype.value = false;

            /**
             * Creates a new GetCoilResponse instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.GetCoilResponse
             * @static
             * @param {lebai.modbus.GetCoilResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.GetCoilResponse} GetCoilResponse instance
             * @type {{
             *   (properties: lebai.modbus.GetCoilResponse.$Shape): lebai.modbus.GetCoilResponse & lebai.modbus.GetCoilResponse.$Shape;
             *   (properties?: lebai.modbus.GetCoilResponse.$Properties): lebai.modbus.GetCoilResponse;
             * }}
             */
            GetCoilResponse.create = function(properties) {
                return new GetCoilResponse(properties);
            };

            /**
             * Encodes the specified GetCoilResponse message. Does not implicitly {@link lebai.modbus.GetCoilResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.GetCoilResponse
             * @static
             * @param {lebai.modbus.GetCoilResponse.$Properties} message GetCoilResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetCoilResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== false)
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetCoilResponse message, length delimited. Does not implicitly {@link lebai.modbus.GetCoilResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.GetCoilResponse
             * @static
             * @param {lebai.modbus.GetCoilResponse.$Properties} message GetCoilResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetCoilResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetCoilResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.GetCoilResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.GetCoilResponse & lebai.modbus.GetCoilResponse.$Shape} GetCoilResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetCoilResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.GetCoilResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetCoilResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.GetCoilResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.GetCoilResponse & lebai.modbus.GetCoilResponse.$Shape} GetCoilResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetCoilResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetCoilResponse message.
             * @function verify
             * @memberof lebai.modbus.GetCoilResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetCoilResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (typeof message.value !== "boolean")
                        return "value: boolean expected";
                return null;
            };

            /**
             * Creates a GetCoilResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.GetCoilResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.GetCoilResponse} GetCoilResponse
             */
            GetCoilResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.GetCoilResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.GetCoilResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.GetCoilResponse();
                if (object.value != null)
                    if (object.value)
                        message.value = $Boolean(object.value);
                return message;
            };

            /**
             * Creates a plain object from a GetCoilResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.GetCoilResponse
             * @static
             * @param {lebai.modbus.GetCoilResponse} message GetCoilResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetCoilResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.value = false;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this GetCoilResponse to JSON.
             * @function toJSON
             * @memberof lebai.modbus.GetCoilResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetCoilResponse.prototype.toJSON = function() {
                return GetCoilResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetCoilResponse
             * @function getTypeUrl
             * @memberof lebai.modbus.GetCoilResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetCoilResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.GetCoilResponse";
            };

            return GetCoilResponse;
        })();

        modbus.GetCoilsRequest = (function() {

            /**
             * Properties of a GetCoilsRequest.
             * @typedef {Object} lebai.modbus.GetCoilsRequest.$Properties
             * @property {string|null} [device] GetCoilsRequest device
             * @property {string|null} [pin] GetCoilsRequest pin
             * @property {number|null} [count] GetCoilsRequest count
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetCoilsRequest.
             * @memberof lebai.modbus
             * @interface IGetCoilsRequest
             * @augments lebai.modbus.GetCoilsRequest.$Properties
             * @deprecated Use lebai.modbus.GetCoilsRequest.$Properties instead.
             */

            /**
             * Shape of a GetCoilsRequest.
             * @typedef {lebai.modbus.GetCoilsRequest.$Properties} lebai.modbus.GetCoilsRequest.$Shape
             */

            /**
             * Constructs a new GetCoilsRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a GetCoilsRequest.
             * @constructor
             * @param {lebai.modbus.GetCoilsRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetCoilsRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetCoilsRequest device.
             * @member {string} device
             * @memberof lebai.modbus.GetCoilsRequest
             * @instance
             */
            GetCoilsRequest.prototype.device = "";

            /**
             * GetCoilsRequest pin.
             * @member {string} pin
             * @memberof lebai.modbus.GetCoilsRequest
             * @instance
             */
            GetCoilsRequest.prototype.pin = "";

            /**
             * GetCoilsRequest count.
             * @member {number} count
             * @memberof lebai.modbus.GetCoilsRequest
             * @instance
             */
            GetCoilsRequest.prototype.count = 0;

            /**
             * Creates a new GetCoilsRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.GetCoilsRequest
             * @static
             * @param {lebai.modbus.GetCoilsRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.GetCoilsRequest} GetCoilsRequest instance
             * @type {{
             *   (properties: lebai.modbus.GetCoilsRequest.$Shape): lebai.modbus.GetCoilsRequest & lebai.modbus.GetCoilsRequest.$Shape;
             *   (properties?: lebai.modbus.GetCoilsRequest.$Properties): lebai.modbus.GetCoilsRequest;
             * }}
             */
            GetCoilsRequest.create = function(properties) {
                return new GetCoilsRequest(properties);
            };

            /**
             * Encodes the specified GetCoilsRequest message. Does not implicitly {@link lebai.modbus.GetCoilsRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.GetCoilsRequest
             * @static
             * @param {lebai.modbus.GetCoilsRequest.$Properties} message GetCoilsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetCoilsRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.pin);
                if (message.count != null && $Object.hasOwnProperty.call(message, "count") && message.count !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.count);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetCoilsRequest message, length delimited. Does not implicitly {@link lebai.modbus.GetCoilsRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.GetCoilsRequest
             * @static
             * @param {lebai.modbus.GetCoilsRequest.$Properties} message GetCoilsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetCoilsRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetCoilsRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.GetCoilsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.GetCoilsRequest & lebai.modbus.GetCoilsRequest.$Shape} GetCoilsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetCoilsRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.GetCoilsRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetCoilsRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.GetCoilsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.GetCoilsRequest & lebai.modbus.GetCoilsRequest.$Shape} GetCoilsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetCoilsRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetCoilsRequest message.
             * @function verify
             * @memberof lebai.modbus.GetCoilsRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetCoilsRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isString(message.pin))
                        return "pin: string expected";
                if (message.count != null && $Object.hasOwnProperty.call(message, "count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            /**
             * Creates a GetCoilsRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.GetCoilsRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.GetCoilsRequest} GetCoilsRequest
             */
            GetCoilsRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.GetCoilsRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.GetCoilsRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.GetCoilsRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.pin != null)
                    if (typeof object.pin !== "string" || object.pin.length)
                        message.pin = $String(object.pin);
                if (object.count != null)
                    if ($Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a GetCoilsRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.GetCoilsRequest
             * @static
             * @param {lebai.modbus.GetCoilsRequest} message GetCoilsRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetCoilsRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.pin = "";
                    object.count = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.count != null && $Object.hasOwnProperty.call(message, "count"))
                    object.count = message.count;
                return object;
            };

            /**
             * Converts this GetCoilsRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.GetCoilsRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetCoilsRequest.prototype.toJSON = function() {
                return GetCoilsRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetCoilsRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.GetCoilsRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetCoilsRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.GetCoilsRequest";
            };

            return GetCoilsRequest;
        })();

        modbus.GetCoilsResponse = (function() {

            /**
             * Properties of a GetCoilsResponse.
             * @typedef {Object} lebai.modbus.GetCoilsResponse.$Properties
             * @property {Array.<boolean>|null} [values] GetCoilsResponse values
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetCoilsResponse.
             * @memberof lebai.modbus
             * @interface IGetCoilsResponse
             * @augments lebai.modbus.GetCoilsResponse.$Properties
             * @deprecated Use lebai.modbus.GetCoilsResponse.$Properties instead.
             */

            /**
             * Shape of a GetCoilsResponse.
             * @typedef {lebai.modbus.GetCoilsResponse.$Properties} lebai.modbus.GetCoilsResponse.$Shape
             */

            /**
             * Constructs a new GetCoilsResponse.
             * @memberof lebai.modbus
             * @classdesc Represents a GetCoilsResponse.
             * @constructor
             * @param {lebai.modbus.GetCoilsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetCoilsResponse = function (properties) {
                this.values = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetCoilsResponse values.
             * @member {Array.<boolean>} values
             * @memberof lebai.modbus.GetCoilsResponse
             * @instance
             */
            GetCoilsResponse.prototype.values = $util.emptyArray;

            /**
             * Creates a new GetCoilsResponse instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.GetCoilsResponse
             * @static
             * @param {lebai.modbus.GetCoilsResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.GetCoilsResponse} GetCoilsResponse instance
             * @type {{
             *   (properties: lebai.modbus.GetCoilsResponse.$Shape): lebai.modbus.GetCoilsResponse & lebai.modbus.GetCoilsResponse.$Shape;
             *   (properties?: lebai.modbus.GetCoilsResponse.$Properties): lebai.modbus.GetCoilsResponse;
             * }}
             */
            GetCoilsResponse.create = function(properties) {
                return new GetCoilsResponse(properties);
            };

            /**
             * Encodes the specified GetCoilsResponse message. Does not implicitly {@link lebai.modbus.GetCoilsResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.GetCoilsResponse
             * @static
             * @param {lebai.modbus.GetCoilsResponse.$Properties} message GetCoilsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetCoilsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.values != null && message.values.length)
                    writer.uint32(/* id 11, wireType 2 =*/90).bools(message.values);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetCoilsResponse message, length delimited. Does not implicitly {@link lebai.modbus.GetCoilsResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.GetCoilsResponse
             * @static
             * @param {lebai.modbus.GetCoilsResponse.$Properties} message GetCoilsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetCoilsResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetCoilsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.GetCoilsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.GetCoilsResponse & lebai.modbus.GetCoilsResponse.$Shape} GetCoilsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetCoilsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.GetCoilsResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.values && message.values.length))
                                    message.values = [];
                                reader.bools(message.values);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push(reader.bool());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetCoilsResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.GetCoilsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.GetCoilsResponse & lebai.modbus.GetCoilsResponse.$Shape} GetCoilsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetCoilsResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetCoilsResponse message.
             * @function verify
             * @memberof lebai.modbus.GetCoilsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetCoilsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (var i = 0; i < message.values.length; ++i)
                        if (typeof message.values[i] !== "boolean")
                            return "values: boolean[] expected";
                }
                return null;
            };

            /**
             * Creates a GetCoilsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.GetCoilsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.GetCoilsResponse} GetCoilsResponse
             */
            GetCoilsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.GetCoilsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.GetCoilsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.GetCoilsResponse();
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".lebai.modbus.GetCoilsResponse.values: array expected");
                    message.values = $Array(object.values.length);
                    for (var i = 0; i < object.values.length; ++i)
                        message.values[i] = $Boolean(object.values[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a GetCoilsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.GetCoilsResponse
             * @static
             * @param {lebai.modbus.GetCoilsResponse} message GetCoilsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetCoilsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (var j = 0; j < message.values.length; ++j)
                        object.values[j] = message.values[j];
                }
                return object;
            };

            /**
             * Converts this GetCoilsResponse to JSON.
             * @function toJSON
             * @memberof lebai.modbus.GetCoilsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetCoilsResponse.prototype.toJSON = function() {
                return GetCoilsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetCoilsResponse
             * @function getTypeUrl
             * @memberof lebai.modbus.GetCoilsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetCoilsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.GetCoilsResponse";
            };

            return GetCoilsResponse;
        })();

        modbus.SetRegisterRequest = (function() {

            /**
             * Properties of a SetRegisterRequest.
             * @typedef {Object} lebai.modbus.SetRegisterRequest.$Properties
             * @property {string|null} [device] SetRegisterRequest device
             * @property {string|null} [pin] SetRegisterRequest pin
             * @property {number|null} [value] SetRegisterRequest value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetRegisterRequest.
             * @memberof lebai.modbus
             * @interface ISetRegisterRequest
             * @augments lebai.modbus.SetRegisterRequest.$Properties
             * @deprecated Use lebai.modbus.SetRegisterRequest.$Properties instead.
             */

            /**
             * Shape of a SetRegisterRequest.
             * @typedef {lebai.modbus.SetRegisterRequest.$Properties} lebai.modbus.SetRegisterRequest.$Shape
             */

            /**
             * Constructs a new SetRegisterRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a SetRegisterRequest.
             * @constructor
             * @param {lebai.modbus.SetRegisterRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetRegisterRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetRegisterRequest device.
             * @member {string} device
             * @memberof lebai.modbus.SetRegisterRequest
             * @instance
             */
            SetRegisterRequest.prototype.device = "";

            /**
             * SetRegisterRequest pin.
             * @member {string} pin
             * @memberof lebai.modbus.SetRegisterRequest
             * @instance
             */
            SetRegisterRequest.prototype.pin = "";

            /**
             * SetRegisterRequest value.
             * @member {number} value
             * @memberof lebai.modbus.SetRegisterRequest
             * @instance
             */
            SetRegisterRequest.prototype.value = 0;

            /**
             * Creates a new SetRegisterRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.SetRegisterRequest
             * @static
             * @param {lebai.modbus.SetRegisterRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.SetRegisterRequest} SetRegisterRequest instance
             * @type {{
             *   (properties: lebai.modbus.SetRegisterRequest.$Shape): lebai.modbus.SetRegisterRequest & lebai.modbus.SetRegisterRequest.$Shape;
             *   (properties?: lebai.modbus.SetRegisterRequest.$Properties): lebai.modbus.SetRegisterRequest;
             * }}
             */
            SetRegisterRequest.create = function(properties) {
                return new SetRegisterRequest(properties);
            };

            /**
             * Encodes the specified SetRegisterRequest message. Does not implicitly {@link lebai.modbus.SetRegisterRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.SetRegisterRequest
             * @static
             * @param {lebai.modbus.SetRegisterRequest.$Properties} message SetRegisterRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetRegisterRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.pin);
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetRegisterRequest message, length delimited. Does not implicitly {@link lebai.modbus.SetRegisterRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.SetRegisterRequest
             * @static
             * @param {lebai.modbus.SetRegisterRequest.$Properties} message SetRegisterRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetRegisterRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetRegisterRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.SetRegisterRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.SetRegisterRequest & lebai.modbus.SetRegisterRequest.$Shape} SetRegisterRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetRegisterRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.SetRegisterRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetRegisterRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.SetRegisterRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.SetRegisterRequest & lebai.modbus.SetRegisterRequest.$Shape} SetRegisterRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetRegisterRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetRegisterRequest message.
             * @function verify
             * @memberof lebai.modbus.SetRegisterRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetRegisterRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isString(message.pin))
                        return "pin: string expected";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                return null;
            };

            /**
             * Creates a SetRegisterRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.SetRegisterRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.SetRegisterRequest} SetRegisterRequest
             */
            SetRegisterRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.SetRegisterRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.SetRegisterRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.SetRegisterRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.pin != null)
                    if (typeof object.pin !== "string" || object.pin.length)
                        message.pin = $String(object.pin);
                if (object.value != null)
                    if ($Number(object.value) !== 0)
                        message.value = object.value >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a SetRegisterRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.SetRegisterRequest
             * @static
             * @param {lebai.modbus.SetRegisterRequest} message SetRegisterRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetRegisterRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.pin = "";
                    object.value = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this SetRegisterRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.SetRegisterRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetRegisterRequest.prototype.toJSON = function() {
                return SetRegisterRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetRegisterRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.SetRegisterRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetRegisterRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.SetRegisterRequest";
            };

            return SetRegisterRequest;
        })();

        modbus.SetRegistersRequest = (function() {

            /**
             * Properties of a SetRegistersRequest.
             * @typedef {Object} lebai.modbus.SetRegistersRequest.$Properties
             * @property {string|null} [device] SetRegistersRequest device
             * @property {string|null} [pin] SetRegistersRequest pin
             * @property {Array.<number>|null} [values] SetRegistersRequest values
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetRegistersRequest.
             * @memberof lebai.modbus
             * @interface ISetRegistersRequest
             * @augments lebai.modbus.SetRegistersRequest.$Properties
             * @deprecated Use lebai.modbus.SetRegistersRequest.$Properties instead.
             */

            /**
             * Shape of a SetRegistersRequest.
             * @typedef {lebai.modbus.SetRegistersRequest.$Properties} lebai.modbus.SetRegistersRequest.$Shape
             */

            /**
             * Constructs a new SetRegistersRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a SetRegistersRequest.
             * @constructor
             * @param {lebai.modbus.SetRegistersRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetRegistersRequest = function (properties) {
                this.values = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetRegistersRequest device.
             * @member {string} device
             * @memberof lebai.modbus.SetRegistersRequest
             * @instance
             */
            SetRegistersRequest.prototype.device = "";

            /**
             * SetRegistersRequest pin.
             * @member {string} pin
             * @memberof lebai.modbus.SetRegistersRequest
             * @instance
             */
            SetRegistersRequest.prototype.pin = "";

            /**
             * SetRegistersRequest values.
             * @member {Array.<number>} values
             * @memberof lebai.modbus.SetRegistersRequest
             * @instance
             */
            SetRegistersRequest.prototype.values = $util.emptyArray;

            /**
             * Creates a new SetRegistersRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.SetRegistersRequest
             * @static
             * @param {lebai.modbus.SetRegistersRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.SetRegistersRequest} SetRegistersRequest instance
             * @type {{
             *   (properties: lebai.modbus.SetRegistersRequest.$Shape): lebai.modbus.SetRegistersRequest & lebai.modbus.SetRegistersRequest.$Shape;
             *   (properties?: lebai.modbus.SetRegistersRequest.$Properties): lebai.modbus.SetRegistersRequest;
             * }}
             */
            SetRegistersRequest.create = function(properties) {
                return new SetRegistersRequest(properties);
            };

            /**
             * Encodes the specified SetRegistersRequest message. Does not implicitly {@link lebai.modbus.SetRegistersRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.SetRegistersRequest
             * @static
             * @param {lebai.modbus.SetRegistersRequest.$Properties} message SetRegistersRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetRegistersRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.pin);
                if (message.values != null && message.values.length)
                    writer.uint32(/* id 11, wireType 2 =*/90).uint32s(message.values);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetRegistersRequest message, length delimited. Does not implicitly {@link lebai.modbus.SetRegistersRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.SetRegistersRequest
             * @static
             * @param {lebai.modbus.SetRegistersRequest.$Properties} message SetRegistersRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetRegistersRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetRegistersRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.SetRegistersRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.SetRegistersRequest & lebai.modbus.SetRegistersRequest.$Shape} SetRegistersRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetRegistersRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.SetRegistersRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.values && message.values.length))
                                    message.values = [];
                                reader.uint32s(message.values);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetRegistersRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.SetRegistersRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.SetRegistersRequest & lebai.modbus.SetRegistersRequest.$Shape} SetRegistersRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetRegistersRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetRegistersRequest message.
             * @function verify
             * @memberof lebai.modbus.SetRegistersRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetRegistersRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isString(message.pin))
                        return "pin: string expected";
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (var i = 0; i < message.values.length; ++i)
                        if (!$util.isInteger(message.values[i]))
                            return "values: integer[] expected";
                }
                return null;
            };

            /**
             * Creates a SetRegistersRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.SetRegistersRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.SetRegistersRequest} SetRegistersRequest
             */
            SetRegistersRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.SetRegistersRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.SetRegistersRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.SetRegistersRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.pin != null)
                    if (typeof object.pin !== "string" || object.pin.length)
                        message.pin = $String(object.pin);
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".lebai.modbus.SetRegistersRequest.values: array expected");
                    message.values = $Array(object.values.length);
                    for (var i = 0; i < object.values.length; ++i)
                        message.values[i] = object.values[i] >>> 0;
                }
                return message;
            };

            /**
             * Creates a plain object from a SetRegistersRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.SetRegistersRequest
             * @static
             * @param {lebai.modbus.SetRegistersRequest} message SetRegistersRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetRegistersRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (options.defaults) {
                    object.device = "";
                    object.pin = "";
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (var j = 0; j < message.values.length; ++j)
                        object.values[j] = message.values[j];
                }
                return object;
            };

            /**
             * Converts this SetRegistersRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.SetRegistersRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetRegistersRequest.prototype.toJSON = function() {
                return SetRegistersRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetRegistersRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.SetRegistersRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetRegistersRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.SetRegistersRequest";
            };

            return SetRegistersRequest;
        })();

        modbus.GetRegisterRequest = (function() {

            /**
             * Properties of a GetRegisterRequest.
             * @typedef {Object} lebai.modbus.GetRegisterRequest.$Properties
             * @property {string|null} [device] GetRegisterRequest device
             * @property {string|null} [pin] GetRegisterRequest pin
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetRegisterRequest.
             * @memberof lebai.modbus
             * @interface IGetRegisterRequest
             * @augments lebai.modbus.GetRegisterRequest.$Properties
             * @deprecated Use lebai.modbus.GetRegisterRequest.$Properties instead.
             */

            /**
             * Shape of a GetRegisterRequest.
             * @typedef {lebai.modbus.GetRegisterRequest.$Properties} lebai.modbus.GetRegisterRequest.$Shape
             */

            /**
             * Constructs a new GetRegisterRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a GetRegisterRequest.
             * @constructor
             * @param {lebai.modbus.GetRegisterRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetRegisterRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetRegisterRequest device.
             * @member {string} device
             * @memberof lebai.modbus.GetRegisterRequest
             * @instance
             */
            GetRegisterRequest.prototype.device = "";

            /**
             * GetRegisterRequest pin.
             * @member {string} pin
             * @memberof lebai.modbus.GetRegisterRequest
             * @instance
             */
            GetRegisterRequest.prototype.pin = "";

            /**
             * Creates a new GetRegisterRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.GetRegisterRequest
             * @static
             * @param {lebai.modbus.GetRegisterRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.GetRegisterRequest} GetRegisterRequest instance
             * @type {{
             *   (properties: lebai.modbus.GetRegisterRequest.$Shape): lebai.modbus.GetRegisterRequest & lebai.modbus.GetRegisterRequest.$Shape;
             *   (properties?: lebai.modbus.GetRegisterRequest.$Properties): lebai.modbus.GetRegisterRequest;
             * }}
             */
            GetRegisterRequest.create = function(properties) {
                return new GetRegisterRequest(properties);
            };

            /**
             * Encodes the specified GetRegisterRequest message. Does not implicitly {@link lebai.modbus.GetRegisterRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.GetRegisterRequest
             * @static
             * @param {lebai.modbus.GetRegisterRequest.$Properties} message GetRegisterRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRegisterRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.pin);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetRegisterRequest message, length delimited. Does not implicitly {@link lebai.modbus.GetRegisterRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.GetRegisterRequest
             * @static
             * @param {lebai.modbus.GetRegisterRequest.$Properties} message GetRegisterRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRegisterRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetRegisterRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.GetRegisterRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.GetRegisterRequest & lebai.modbus.GetRegisterRequest.$Shape} GetRegisterRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRegisterRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.GetRegisterRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetRegisterRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.GetRegisterRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.GetRegisterRequest & lebai.modbus.GetRegisterRequest.$Shape} GetRegisterRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRegisterRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetRegisterRequest message.
             * @function verify
             * @memberof lebai.modbus.GetRegisterRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetRegisterRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isString(message.pin))
                        return "pin: string expected";
                return null;
            };

            /**
             * Creates a GetRegisterRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.GetRegisterRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.GetRegisterRequest} GetRegisterRequest
             */
            GetRegisterRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.GetRegisterRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.GetRegisterRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.GetRegisterRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.pin != null)
                    if (typeof object.pin !== "string" || object.pin.length)
                        message.pin = $String(object.pin);
                return message;
            };

            /**
             * Creates a plain object from a GetRegisterRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.GetRegisterRequest
             * @static
             * @param {lebai.modbus.GetRegisterRequest} message GetRegisterRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetRegisterRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.pin = "";
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                return object;
            };

            /**
             * Converts this GetRegisterRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.GetRegisterRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetRegisterRequest.prototype.toJSON = function() {
                return GetRegisterRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetRegisterRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.GetRegisterRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetRegisterRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.GetRegisterRequest";
            };

            return GetRegisterRequest;
        })();

        modbus.GetRegisterResponse = (function() {

            /**
             * Properties of a GetRegisterResponse.
             * @typedef {Object} lebai.modbus.GetRegisterResponse.$Properties
             * @property {number|null} [value] GetRegisterResponse value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetRegisterResponse.
             * @memberof lebai.modbus
             * @interface IGetRegisterResponse
             * @augments lebai.modbus.GetRegisterResponse.$Properties
             * @deprecated Use lebai.modbus.GetRegisterResponse.$Properties instead.
             */

            /**
             * Shape of a GetRegisterResponse.
             * @typedef {lebai.modbus.GetRegisterResponse.$Properties} lebai.modbus.GetRegisterResponse.$Shape
             */

            /**
             * Constructs a new GetRegisterResponse.
             * @memberof lebai.modbus
             * @classdesc Represents a GetRegisterResponse.
             * @constructor
             * @param {lebai.modbus.GetRegisterResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetRegisterResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetRegisterResponse value.
             * @member {number} value
             * @memberof lebai.modbus.GetRegisterResponse
             * @instance
             */
            GetRegisterResponse.prototype.value = 0;

            /**
             * Creates a new GetRegisterResponse instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.GetRegisterResponse
             * @static
             * @param {lebai.modbus.GetRegisterResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.GetRegisterResponse} GetRegisterResponse instance
             * @type {{
             *   (properties: lebai.modbus.GetRegisterResponse.$Shape): lebai.modbus.GetRegisterResponse & lebai.modbus.GetRegisterResponse.$Shape;
             *   (properties?: lebai.modbus.GetRegisterResponse.$Properties): lebai.modbus.GetRegisterResponse;
             * }}
             */
            GetRegisterResponse.create = function(properties) {
                return new GetRegisterResponse(properties);
            };

            /**
             * Encodes the specified GetRegisterResponse message. Does not implicitly {@link lebai.modbus.GetRegisterResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.GetRegisterResponse
             * @static
             * @param {lebai.modbus.GetRegisterResponse.$Properties} message GetRegisterResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRegisterResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetRegisterResponse message, length delimited. Does not implicitly {@link lebai.modbus.GetRegisterResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.GetRegisterResponse
             * @static
             * @param {lebai.modbus.GetRegisterResponse.$Properties} message GetRegisterResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRegisterResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetRegisterResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.GetRegisterResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.GetRegisterResponse & lebai.modbus.GetRegisterResponse.$Shape} GetRegisterResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRegisterResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.GetRegisterResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetRegisterResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.GetRegisterResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.GetRegisterResponse & lebai.modbus.GetRegisterResponse.$Shape} GetRegisterResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRegisterResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetRegisterResponse message.
             * @function verify
             * @memberof lebai.modbus.GetRegisterResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetRegisterResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                return null;
            };

            /**
             * Creates a GetRegisterResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.GetRegisterResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.GetRegisterResponse} GetRegisterResponse
             */
            GetRegisterResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.GetRegisterResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.GetRegisterResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.GetRegisterResponse();
                if (object.value != null)
                    if ($Number(object.value) !== 0)
                        message.value = object.value >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a GetRegisterResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.GetRegisterResponse
             * @static
             * @param {lebai.modbus.GetRegisterResponse} message GetRegisterResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetRegisterResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.value = 0;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this GetRegisterResponse to JSON.
             * @function toJSON
             * @memberof lebai.modbus.GetRegisterResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetRegisterResponse.prototype.toJSON = function() {
                return GetRegisterResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetRegisterResponse
             * @function getTypeUrl
             * @memberof lebai.modbus.GetRegisterResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetRegisterResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.GetRegisterResponse";
            };

            return GetRegisterResponse;
        })();

        modbus.GetRegistersRequest = (function() {

            /**
             * Properties of a GetRegistersRequest.
             * @typedef {Object} lebai.modbus.GetRegistersRequest.$Properties
             * @property {string|null} [device] GetRegistersRequest device
             * @property {string|null} [pin] GetRegistersRequest pin
             * @property {number|null} [count] GetRegistersRequest count
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetRegistersRequest.
             * @memberof lebai.modbus
             * @interface IGetRegistersRequest
             * @augments lebai.modbus.GetRegistersRequest.$Properties
             * @deprecated Use lebai.modbus.GetRegistersRequest.$Properties instead.
             */

            /**
             * Shape of a GetRegistersRequest.
             * @typedef {lebai.modbus.GetRegistersRequest.$Properties} lebai.modbus.GetRegistersRequest.$Shape
             */

            /**
             * Constructs a new GetRegistersRequest.
             * @memberof lebai.modbus
             * @classdesc Represents a GetRegistersRequest.
             * @constructor
             * @param {lebai.modbus.GetRegistersRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetRegistersRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetRegistersRequest device.
             * @member {string} device
             * @memberof lebai.modbus.GetRegistersRequest
             * @instance
             */
            GetRegistersRequest.prototype.device = "";

            /**
             * GetRegistersRequest pin.
             * @member {string} pin
             * @memberof lebai.modbus.GetRegistersRequest
             * @instance
             */
            GetRegistersRequest.prototype.pin = "";

            /**
             * GetRegistersRequest count.
             * @member {number} count
             * @memberof lebai.modbus.GetRegistersRequest
             * @instance
             */
            GetRegistersRequest.prototype.count = 0;

            /**
             * Creates a new GetRegistersRequest instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.GetRegistersRequest
             * @static
             * @param {lebai.modbus.GetRegistersRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.GetRegistersRequest} GetRegistersRequest instance
             * @type {{
             *   (properties: lebai.modbus.GetRegistersRequest.$Shape): lebai.modbus.GetRegistersRequest & lebai.modbus.GetRegistersRequest.$Shape;
             *   (properties?: lebai.modbus.GetRegistersRequest.$Properties): lebai.modbus.GetRegistersRequest;
             * }}
             */
            GetRegistersRequest.create = function(properties) {
                return new GetRegistersRequest(properties);
            };

            /**
             * Encodes the specified GetRegistersRequest message. Does not implicitly {@link lebai.modbus.GetRegistersRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.GetRegistersRequest
             * @static
             * @param {lebai.modbus.GetRegistersRequest.$Properties} message GetRegistersRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRegistersRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin") && message.pin !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.pin);
                if (message.count != null && $Object.hasOwnProperty.call(message, "count") && message.count !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.count);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetRegistersRequest message, length delimited. Does not implicitly {@link lebai.modbus.GetRegistersRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.GetRegistersRequest
             * @static
             * @param {lebai.modbus.GetRegistersRequest.$Properties} message GetRegistersRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRegistersRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetRegistersRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.GetRegistersRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.GetRegistersRequest & lebai.modbus.GetRegistersRequest.$Shape} GetRegistersRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRegistersRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.GetRegistersRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.pin = value;
                            else
                                delete message.pin;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetRegistersRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.GetRegistersRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.GetRegistersRequest & lebai.modbus.GetRegistersRequest.$Shape} GetRegistersRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRegistersRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetRegistersRequest message.
             * @function verify
             * @memberof lebai.modbus.GetRegistersRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetRegistersRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    if (!$util.isString(message.pin))
                        return "pin: string expected";
                if (message.count != null && $Object.hasOwnProperty.call(message, "count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            /**
             * Creates a GetRegistersRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.GetRegistersRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.GetRegistersRequest} GetRegistersRequest
             */
            GetRegistersRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.GetRegistersRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.GetRegistersRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.GetRegistersRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.pin != null)
                    if (typeof object.pin !== "string" || object.pin.length)
                        message.pin = $String(object.pin);
                if (object.count != null)
                    if ($Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a GetRegistersRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.GetRegistersRequest
             * @static
             * @param {lebai.modbus.GetRegistersRequest} message GetRegistersRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetRegistersRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.pin = "";
                    object.count = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.pin != null && $Object.hasOwnProperty.call(message, "pin"))
                    object.pin = message.pin;
                if (message.count != null && $Object.hasOwnProperty.call(message, "count"))
                    object.count = message.count;
                return object;
            };

            /**
             * Converts this GetRegistersRequest to JSON.
             * @function toJSON
             * @memberof lebai.modbus.GetRegistersRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetRegistersRequest.prototype.toJSON = function() {
                return GetRegistersRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetRegistersRequest
             * @function getTypeUrl
             * @memberof lebai.modbus.GetRegistersRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetRegistersRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.GetRegistersRequest";
            };

            return GetRegistersRequest;
        })();

        modbus.GetRegistersResponse = (function() {

            /**
             * Properties of a GetRegistersResponse.
             * @typedef {Object} lebai.modbus.GetRegistersResponse.$Properties
             * @property {Array.<number>|null} [values] GetRegistersResponse values
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetRegistersResponse.
             * @memberof lebai.modbus
             * @interface IGetRegistersResponse
             * @augments lebai.modbus.GetRegistersResponse.$Properties
             * @deprecated Use lebai.modbus.GetRegistersResponse.$Properties instead.
             */

            /**
             * Shape of a GetRegistersResponse.
             * @typedef {lebai.modbus.GetRegistersResponse.$Properties} lebai.modbus.GetRegistersResponse.$Shape
             */

            /**
             * Constructs a new GetRegistersResponse.
             * @memberof lebai.modbus
             * @classdesc Represents a GetRegistersResponse.
             * @constructor
             * @param {lebai.modbus.GetRegistersResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetRegistersResponse = function (properties) {
                this.values = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetRegistersResponse values.
             * @member {Array.<number>} values
             * @memberof lebai.modbus.GetRegistersResponse
             * @instance
             */
            GetRegistersResponse.prototype.values = $util.emptyArray;

            /**
             * Creates a new GetRegistersResponse instance using the specified properties.
             * @function create
             * @memberof lebai.modbus.GetRegistersResponse
             * @static
             * @param {lebai.modbus.GetRegistersResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.modbus.GetRegistersResponse} GetRegistersResponse instance
             * @type {{
             *   (properties: lebai.modbus.GetRegistersResponse.$Shape): lebai.modbus.GetRegistersResponse & lebai.modbus.GetRegistersResponse.$Shape;
             *   (properties?: lebai.modbus.GetRegistersResponse.$Properties): lebai.modbus.GetRegistersResponse;
             * }}
             */
            GetRegistersResponse.create = function(properties) {
                return new GetRegistersResponse(properties);
            };

            /**
             * Encodes the specified GetRegistersResponse message. Does not implicitly {@link lebai.modbus.GetRegistersResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.modbus.GetRegistersResponse
             * @static
             * @param {lebai.modbus.GetRegistersResponse.$Properties} message GetRegistersResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRegistersResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.values != null && message.values.length)
                    writer.uint32(/* id 11, wireType 2 =*/90).uint32s(message.values);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetRegistersResponse message, length delimited. Does not implicitly {@link lebai.modbus.GetRegistersResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.modbus.GetRegistersResponse
             * @static
             * @param {lebai.modbus.GetRegistersResponse.$Properties} message GetRegistersResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetRegistersResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetRegistersResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.modbus.GetRegistersResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.modbus.GetRegistersResponse & lebai.modbus.GetRegistersResponse.$Shape} GetRegistersResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRegistersResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.modbus.GetRegistersResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.values && message.values.length))
                                    message.values = [];
                                reader.uint32s(message.values);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetRegistersResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.modbus.GetRegistersResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.modbus.GetRegistersResponse & lebai.modbus.GetRegistersResponse.$Shape} GetRegistersResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetRegistersResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetRegistersResponse message.
             * @function verify
             * @memberof lebai.modbus.GetRegistersResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetRegistersResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (var i = 0; i < message.values.length; ++i)
                        if (!$util.isInteger(message.values[i]))
                            return "values: integer[] expected";
                }
                return null;
            };

            /**
             * Creates a GetRegistersResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.modbus.GetRegistersResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.modbus.GetRegistersResponse} GetRegistersResponse
             */
            GetRegistersResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.modbus.GetRegistersResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.modbus.GetRegistersResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.modbus.GetRegistersResponse();
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".lebai.modbus.GetRegistersResponse.values: array expected");
                    message.values = $Array(object.values.length);
                    for (var i = 0; i < object.values.length; ++i)
                        message.values[i] = object.values[i] >>> 0;
                }
                return message;
            };

            /**
             * Creates a plain object from a GetRegistersResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.modbus.GetRegistersResponse
             * @static
             * @param {lebai.modbus.GetRegistersResponse} message GetRegistersResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetRegistersResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (var j = 0; j < message.values.length; ++j)
                        object.values[j] = message.values[j];
                }
                return object;
            };

            /**
             * Converts this GetRegistersResponse to JSON.
             * @function toJSON
             * @memberof lebai.modbus.GetRegistersResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetRegistersResponse.prototype.toJSON = function() {
                return GetRegistersResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetRegistersResponse
             * @function getTypeUrl
             * @memberof lebai.modbus.GetRegistersResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetRegistersResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.modbus.GetRegistersResponse";
            };

            return GetRegistersResponse;
        })();

        modbus.ModbusService = (function() {

            /**
             * Constructs a new ModbusService service.
             * @memberof lebai.modbus
             * @classdesc Represents a ModbusService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var ModbusService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(ModbusService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: ModbusService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new ModbusService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.modbus.ModbusService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {ModbusService} RPC service. Useful where requests and/or responses are streamed.
             */
            ModbusService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#saveModbus}.
             * @memberof lebai.modbus.ModbusService
             * @typedef SaveModbusCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SaveModbus.
             * @memberof lebai.modbus.ModbusService
             * @typedef SaveModbus
             * @type {{
             *   (request: lebai.modbus.ISaveModbusRequest, callback: lebai.modbus.ModbusService.SaveModbusCallback): void;
             *   (request: lebai.modbus.ISaveModbusRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SaveModbus";
             *   readonly path: "/lebai.modbus.ModbusService/SaveModbus";
             *   readonly requestType: "SaveModbusRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SaveModbus.
             * @name lebai.modbus.ModbusService#saveModbus
             * @type {lebai.modbus.ModbusService.SaveModbus}
             */
            $Object.defineProperties(ModbusService.prototype.saveModbus = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.saveModbus, $root.lebai.modbus.SaveModbusRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SaveModbus" },
                path: { value: "/lebai.modbus.ModbusService/SaveModbus" },
                requestType: { value: "SaveModbusRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#loadModbus}.
             * @memberof lebai.modbus.ModbusService
             * @typedef LoadModbusCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.modbus.Modbus} [response] Modbus
             */

            /**
             * Calls LoadModbus.
             * @memberof lebai.modbus.ModbusService
             * @typedef LoadModbus
             * @type {{
             *   (request: lebai.db.ILoadRequest, callback: lebai.modbus.ModbusService.LoadModbusCallback): void;
             *   (request: lebai.db.ILoadRequest): Promise<lebai.modbus.Modbus>;
             *   readonly name: "LoadModbus";
             *   readonly path: "/lebai.modbus.ModbusService/LoadModbus";
             *   readonly requestType: "db.LoadRequest";
             *   readonly responseType: "Modbus";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadModbus.
             * @name lebai.modbus.ModbusService#loadModbus
             * @type {lebai.modbus.ModbusService.LoadModbus}
             */
            $Object.defineProperties(ModbusService.prototype.loadModbus = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.loadModbus, $root.lebai.db.LoadRequest, $root.lebai.modbus.Modbus, request, callback);
            }, {
                name: { value: "LoadModbus" },
                path: { value: "/lebai.modbus.ModbusService/LoadModbus" },
                requestType: { value: "db.LoadRequest" },
                responseType: { value: "Modbus" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#loadModbusList}.
             * @memberof lebai.modbus.ModbusService
             * @typedef LoadModbusListCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.db.LoadListResponse} [response] LoadListResponse
             */

            /**
             * Calls LoadModbusList.
             * @memberof lebai.modbus.ModbusService
             * @typedef LoadModbusList
             * @type {{
             *   (request: lebai.db.ILoadListRequest, callback: lebai.modbus.ModbusService.LoadModbusListCallback): void;
             *   (request: lebai.db.ILoadListRequest): Promise<lebai.db.LoadListResponse>;
             *   readonly name: "LoadModbusList";
             *   readonly path: "/lebai.modbus.ModbusService/LoadModbusList";
             *   readonly requestType: "db.LoadListRequest";
             *   readonly responseType: "db.LoadListResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadModbusList.
             * @name lebai.modbus.ModbusService#loadModbusList
             * @type {lebai.modbus.ModbusService.LoadModbusList}
             */
            $Object.defineProperties(ModbusService.prototype.loadModbusList = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.loadModbusList, $root.lebai.db.LoadListRequest, $root.lebai.db.LoadListResponse, request, callback);
            }, {
                name: { value: "LoadModbusList" },
                path: { value: "/lebai.modbus.ModbusService/LoadModbusList" },
                requestType: { value: "db.LoadListRequest" },
                responseType: { value: "db.LoadListResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#saveModbusRegister}.
             * @memberof lebai.modbus.ModbusService
             * @typedef SaveModbusRegisterCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SaveModbusRegister.
             * @memberof lebai.modbus.ModbusService
             * @typedef SaveModbusRegister
             * @type {{
             *   (request: lebai.modbus.ISaveModbusRegisterRequest, callback: lebai.modbus.ModbusService.SaveModbusRegisterCallback): void;
             *   (request: lebai.modbus.ISaveModbusRegisterRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SaveModbusRegister";
             *   readonly path: "/lebai.modbus.ModbusService/SaveModbusRegister";
             *   readonly requestType: "SaveModbusRegisterRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SaveModbusRegister.
             * @name lebai.modbus.ModbusService#saveModbusRegister
             * @type {lebai.modbus.ModbusService.SaveModbusRegister}
             */
            $Object.defineProperties(ModbusService.prototype.saveModbusRegister = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.saveModbusRegister, $root.lebai.modbus.SaveModbusRegisterRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SaveModbusRegister" },
                path: { value: "/lebai.modbus.ModbusService/SaveModbusRegister" },
                requestType: { value: "SaveModbusRegisterRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#loadModbusRegister}.
             * @memberof lebai.modbus.ModbusService
             * @typedef LoadModbusRegisterCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.modbus.ModbusRegister} [response] ModbusRegister
             */

            /**
             * Calls LoadModbusRegister.
             * @memberof lebai.modbus.ModbusService
             * @typedef LoadModbusRegister
             * @type {{
             *   (request: lebai.modbus.ILoadModbusRegisterRequest, callback: lebai.modbus.ModbusService.LoadModbusRegisterCallback): void;
             *   (request: lebai.modbus.ILoadModbusRegisterRequest): Promise<lebai.modbus.ModbusRegister>;
             *   readonly name: "LoadModbusRegister";
             *   readonly path: "/lebai.modbus.ModbusService/LoadModbusRegister";
             *   readonly requestType: "LoadModbusRegisterRequest";
             *   readonly responseType: "ModbusRegister";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadModbusRegister.
             * @name lebai.modbus.ModbusService#loadModbusRegister
             * @type {lebai.modbus.ModbusService.LoadModbusRegister}
             */
            $Object.defineProperties(ModbusService.prototype.loadModbusRegister = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.loadModbusRegister, $root.lebai.modbus.LoadModbusRegisterRequest, $root.lebai.modbus.ModbusRegister, request, callback);
            }, {
                name: { value: "LoadModbusRegister" },
                path: { value: "/lebai.modbus.ModbusService/LoadModbusRegister" },
                requestType: { value: "LoadModbusRegisterRequest" },
                responseType: { value: "ModbusRegister" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#loadModbusRegisterList}.
             * @memberof lebai.modbus.ModbusService
             * @typedef LoadModbusRegisterListCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.db.LoadListResponse} [response] LoadListResponse
             */

            /**
             * Calls LoadModbusRegisterList.
             * @memberof lebai.modbus.ModbusService
             * @typedef LoadModbusRegisterList
             * @type {{
             *   (request: lebai.modbus.ILoadModbusRegisterListRequest, callback: lebai.modbus.ModbusService.LoadModbusRegisterListCallback): void;
             *   (request: lebai.modbus.ILoadModbusRegisterListRequest): Promise<lebai.db.LoadListResponse>;
             *   readonly name: "LoadModbusRegisterList";
             *   readonly path: "/lebai.modbus.ModbusService/LoadModbusRegisterList";
             *   readonly requestType: "LoadModbusRegisterListRequest";
             *   readonly responseType: "db.LoadListResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadModbusRegisterList.
             * @name lebai.modbus.ModbusService#loadModbusRegisterList
             * @type {lebai.modbus.ModbusService.LoadModbusRegisterList}
             */
            $Object.defineProperties(ModbusService.prototype.loadModbusRegisterList = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.loadModbusRegisterList, $root.lebai.modbus.LoadModbusRegisterListRequest, $root.lebai.db.LoadListResponse, request, callback);
            }, {
                name: { value: "LoadModbusRegisterList" },
                path: { value: "/lebai.modbus.ModbusService/LoadModbusRegisterList" },
                requestType: { value: "LoadModbusRegisterListRequest" },
                responseType: { value: "db.LoadListResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#disconnectModbus}.
             * @memberof lebai.modbus.ModbusService
             * @typedef DisconnectModbusCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls DisconnectModbus.
             * @memberof lebai.modbus.ModbusService
             * @typedef DisconnectModbus
             * @type {{
             *   (request: lebai.modbus.IDisconnectModbusRequest, callback: lebai.modbus.ModbusService.DisconnectModbusCallback): void;
             *   (request: lebai.modbus.IDisconnectModbusRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "DisconnectModbus";
             *   readonly path: "/lebai.modbus.ModbusService/DisconnectModbus";
             *   readonly requestType: "DisconnectModbusRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls DisconnectModbus.
             * @name lebai.modbus.ModbusService#disconnectModbus
             * @type {lebai.modbus.ModbusService.DisconnectModbus}
             */
            $Object.defineProperties(ModbusService.prototype.disconnectModbus = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.disconnectModbus, $root.lebai.modbus.DisconnectModbusRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "DisconnectModbus" },
                path: { value: "/lebai.modbus.ModbusService/DisconnectModbus" },
                requestType: { value: "DisconnectModbusRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#setModbusTimeout}.
             * @memberof lebai.modbus.ModbusService
             * @typedef SetModbusTimeoutCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetModbusTimeout.
             * @memberof lebai.modbus.ModbusService
             * @typedef SetModbusTimeout
             * @type {{
             *   (request: lebai.modbus.ISetModbusTimeoutRequest, callback: lebai.modbus.ModbusService.SetModbusTimeoutCallback): void;
             *   (request: lebai.modbus.ISetModbusTimeoutRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetModbusTimeout";
             *   readonly path: "/lebai.modbus.ModbusService/SetModbusTimeout";
             *   readonly requestType: "SetModbusTimeoutRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetModbusTimeout.
             * @name lebai.modbus.ModbusService#setModbusTimeout
             * @type {lebai.modbus.ModbusService.SetModbusTimeout}
             */
            $Object.defineProperties(ModbusService.prototype.setModbusTimeout = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.setModbusTimeout, $root.lebai.modbus.SetModbusTimeoutRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetModbusTimeout" },
                path: { value: "/lebai.modbus.ModbusService/SetModbusTimeout" },
                requestType: { value: "SetModbusTimeoutRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#setModbusRetry}.
             * @memberof lebai.modbus.ModbusService
             * @typedef SetModbusRetryCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetModbusRetry.
             * @memberof lebai.modbus.ModbusService
             * @typedef SetModbusRetry
             * @type {{
             *   (request: lebai.modbus.ISetModbusRetryRequest, callback: lebai.modbus.ModbusService.SetModbusRetryCallback): void;
             *   (request: lebai.modbus.ISetModbusRetryRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetModbusRetry";
             *   readonly path: "/lebai.modbus.ModbusService/SetModbusRetry";
             *   readonly requestType: "SetModbusRetryRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetModbusRetry.
             * @name lebai.modbus.ModbusService#setModbusRetry
             * @type {lebai.modbus.ModbusService.SetModbusRetry}
             */
            $Object.defineProperties(ModbusService.prototype.setModbusRetry = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.setModbusRetry, $root.lebai.modbus.SetModbusRetryRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetModbusRetry" },
                path: { value: "/lebai.modbus.ModbusService/SetModbusRetry" },
                requestType: { value: "SetModbusRetryRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#readDiscreteInputs}.
             * @memberof lebai.modbus.ModbusService
             * @typedef ReadDiscreteInputsCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.modbus.GetCoilsResponse} [response] GetCoilsResponse
             */

            /**
             * Calls ReadDiscreteInputs.
             * @memberof lebai.modbus.ModbusService
             * @typedef ReadDiscreteInputs
             * @type {{
             *   (request: lebai.modbus.IGetCoilsRequest, callback: lebai.modbus.ModbusService.ReadDiscreteInputsCallback): void;
             *   (request: lebai.modbus.IGetCoilsRequest): Promise<lebai.modbus.GetCoilsResponse>;
             *   readonly name: "ReadDiscreteInputs";
             *   readonly path: "/lebai.modbus.ModbusService/ReadDiscreteInputs";
             *   readonly requestType: "GetCoilsRequest";
             *   readonly responseType: "GetCoilsResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls ReadDiscreteInputs.
             * @name lebai.modbus.ModbusService#readDiscreteInputs
             * @type {lebai.modbus.ModbusService.ReadDiscreteInputs}
             */
            $Object.defineProperties(ModbusService.prototype.readDiscreteInputs = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.readDiscreteInputs, $root.lebai.modbus.GetCoilsRequest, $root.lebai.modbus.GetCoilsResponse, request, callback);
            }, {
                name: { value: "ReadDiscreteInputs" },
                path: { value: "/lebai.modbus.ModbusService/ReadDiscreteInputs" },
                requestType: { value: "GetCoilsRequest" },
                responseType: { value: "GetCoilsResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#readCoils}.
             * @memberof lebai.modbus.ModbusService
             * @typedef ReadCoilsCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.modbus.GetCoilsResponse} [response] GetCoilsResponse
             */

            /**
             * Calls ReadCoils.
             * @memberof lebai.modbus.ModbusService
             * @typedef ReadCoils
             * @type {{
             *   (request: lebai.modbus.IGetCoilsRequest, callback: lebai.modbus.ModbusService.ReadCoilsCallback): void;
             *   (request: lebai.modbus.IGetCoilsRequest): Promise<lebai.modbus.GetCoilsResponse>;
             *   readonly name: "ReadCoils";
             *   readonly path: "/lebai.modbus.ModbusService/ReadCoils";
             *   readonly requestType: "GetCoilsRequest";
             *   readonly responseType: "GetCoilsResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls ReadCoils.
             * @name lebai.modbus.ModbusService#readCoils
             * @type {lebai.modbus.ModbusService.ReadCoils}
             */
            $Object.defineProperties(ModbusService.prototype.readCoils = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.readCoils, $root.lebai.modbus.GetCoilsRequest, $root.lebai.modbus.GetCoilsResponse, request, callback);
            }, {
                name: { value: "ReadCoils" },
                path: { value: "/lebai.modbus.ModbusService/ReadCoils" },
                requestType: { value: "GetCoilsRequest" },
                responseType: { value: "GetCoilsResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#writeSingleCoil}.
             * @memberof lebai.modbus.ModbusService
             * @typedef WriteSingleCoilCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls WriteSingleCoil.
             * @memberof lebai.modbus.ModbusService
             * @typedef WriteSingleCoil
             * @type {{
             *   (request: lebai.modbus.ISetCoilRequest, callback: lebai.modbus.ModbusService.WriteSingleCoilCallback): void;
             *   (request: lebai.modbus.ISetCoilRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "WriteSingleCoil";
             *   readonly path: "/lebai.modbus.ModbusService/WriteSingleCoil";
             *   readonly requestType: "SetCoilRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls WriteSingleCoil.
             * @name lebai.modbus.ModbusService#writeSingleCoil
             * @type {lebai.modbus.ModbusService.WriteSingleCoil}
             */
            $Object.defineProperties(ModbusService.prototype.writeSingleCoil = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.writeSingleCoil, $root.lebai.modbus.SetCoilRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "WriteSingleCoil" },
                path: { value: "/lebai.modbus.ModbusService/WriteSingleCoil" },
                requestType: { value: "SetCoilRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#writeMultipleCoils}.
             * @memberof lebai.modbus.ModbusService
             * @typedef WriteMultipleCoilsCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls WriteMultipleCoils.
             * @memberof lebai.modbus.ModbusService
             * @typedef WriteMultipleCoils
             * @type {{
             *   (request: lebai.modbus.ISetCoilsRequest, callback: lebai.modbus.ModbusService.WriteMultipleCoilsCallback): void;
             *   (request: lebai.modbus.ISetCoilsRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "WriteMultipleCoils";
             *   readonly path: "/lebai.modbus.ModbusService/WriteMultipleCoils";
             *   readonly requestType: "SetCoilsRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls WriteMultipleCoils.
             * @name lebai.modbus.ModbusService#writeMultipleCoils
             * @type {lebai.modbus.ModbusService.WriteMultipleCoils}
             */
            $Object.defineProperties(ModbusService.prototype.writeMultipleCoils = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.writeMultipleCoils, $root.lebai.modbus.SetCoilsRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "WriteMultipleCoils" },
                path: { value: "/lebai.modbus.ModbusService/WriteMultipleCoils" },
                requestType: { value: "SetCoilsRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#readInputRegisters}.
             * @memberof lebai.modbus.ModbusService
             * @typedef ReadInputRegistersCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.modbus.GetRegistersResponse} [response] GetRegistersResponse
             */

            /**
             * Calls ReadInputRegisters.
             * @memberof lebai.modbus.ModbusService
             * @typedef ReadInputRegisters
             * @type {{
             *   (request: lebai.modbus.IGetRegistersRequest, callback: lebai.modbus.ModbusService.ReadInputRegistersCallback): void;
             *   (request: lebai.modbus.IGetRegistersRequest): Promise<lebai.modbus.GetRegistersResponse>;
             *   readonly name: "ReadInputRegisters";
             *   readonly path: "/lebai.modbus.ModbusService/ReadInputRegisters";
             *   readonly requestType: "GetRegistersRequest";
             *   readonly responseType: "GetRegistersResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls ReadInputRegisters.
             * @name lebai.modbus.ModbusService#readInputRegisters
             * @type {lebai.modbus.ModbusService.ReadInputRegisters}
             */
            $Object.defineProperties(ModbusService.prototype.readInputRegisters = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.readInputRegisters, $root.lebai.modbus.GetRegistersRequest, $root.lebai.modbus.GetRegistersResponse, request, callback);
            }, {
                name: { value: "ReadInputRegisters" },
                path: { value: "/lebai.modbus.ModbusService/ReadInputRegisters" },
                requestType: { value: "GetRegistersRequest" },
                responseType: { value: "GetRegistersResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#readHoldingRegisters}.
             * @memberof lebai.modbus.ModbusService
             * @typedef ReadHoldingRegistersCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.modbus.GetRegistersResponse} [response] GetRegistersResponse
             */

            /**
             * Calls ReadHoldingRegisters.
             * @memberof lebai.modbus.ModbusService
             * @typedef ReadHoldingRegisters
             * @type {{
             *   (request: lebai.modbus.IGetRegistersRequest, callback: lebai.modbus.ModbusService.ReadHoldingRegistersCallback): void;
             *   (request: lebai.modbus.IGetRegistersRequest): Promise<lebai.modbus.GetRegistersResponse>;
             *   readonly name: "ReadHoldingRegisters";
             *   readonly path: "/lebai.modbus.ModbusService/ReadHoldingRegisters";
             *   readonly requestType: "GetRegistersRequest";
             *   readonly responseType: "GetRegistersResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls ReadHoldingRegisters.
             * @name lebai.modbus.ModbusService#readHoldingRegisters
             * @type {lebai.modbus.ModbusService.ReadHoldingRegisters}
             */
            $Object.defineProperties(ModbusService.prototype.readHoldingRegisters = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.readHoldingRegisters, $root.lebai.modbus.GetRegistersRequest, $root.lebai.modbus.GetRegistersResponse, request, callback);
            }, {
                name: { value: "ReadHoldingRegisters" },
                path: { value: "/lebai.modbus.ModbusService/ReadHoldingRegisters" },
                requestType: { value: "GetRegistersRequest" },
                responseType: { value: "GetRegistersResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#writeSingleRegister}.
             * @memberof lebai.modbus.ModbusService
             * @typedef WriteSingleRegisterCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls WriteSingleRegister.
             * @memberof lebai.modbus.ModbusService
             * @typedef WriteSingleRegister
             * @type {{
             *   (request: lebai.modbus.ISetRegisterRequest, callback: lebai.modbus.ModbusService.WriteSingleRegisterCallback): void;
             *   (request: lebai.modbus.ISetRegisterRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "WriteSingleRegister";
             *   readonly path: "/lebai.modbus.ModbusService/WriteSingleRegister";
             *   readonly requestType: "SetRegisterRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls WriteSingleRegister.
             * @name lebai.modbus.ModbusService#writeSingleRegister
             * @type {lebai.modbus.ModbusService.WriteSingleRegister}
             */
            $Object.defineProperties(ModbusService.prototype.writeSingleRegister = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.writeSingleRegister, $root.lebai.modbus.SetRegisterRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "WriteSingleRegister" },
                path: { value: "/lebai.modbus.ModbusService/WriteSingleRegister" },
                requestType: { value: "SetRegisterRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.modbus.ModbusService#writeMultipleRegisters}.
             * @memberof lebai.modbus.ModbusService
             * @typedef WriteMultipleRegistersCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls WriteMultipleRegisters.
             * @memberof lebai.modbus.ModbusService
             * @typedef WriteMultipleRegisters
             * @type {{
             *   (request: lebai.modbus.ISetRegistersRequest, callback: lebai.modbus.ModbusService.WriteMultipleRegistersCallback): void;
             *   (request: lebai.modbus.ISetRegistersRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "WriteMultipleRegisters";
             *   readonly path: "/lebai.modbus.ModbusService/WriteMultipleRegisters";
             *   readonly requestType: "SetRegistersRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls WriteMultipleRegisters.
             * @name lebai.modbus.ModbusService#writeMultipleRegisters
             * @type {lebai.modbus.ModbusService.WriteMultipleRegisters}
             */
            $Object.defineProperties(ModbusService.prototype.writeMultipleRegisters = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ModbusService.prototype.writeMultipleRegisters, $root.lebai.modbus.SetRegistersRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "WriteMultipleRegisters" },
                path: { value: "/lebai.modbus.ModbusService/WriteMultipleRegisters" },
                requestType: { value: "SetRegistersRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return ModbusService;
        })();

        return modbus;
    })();

    lebai.motion = (function() {

        /**
         * Namespace motion.
         * @memberof lebai
         * @namespace
         */
        var motion = {};

        motion.MotionIndex = (function() {

            /**
             * Properties of a MotionIndex.
             * @typedef {Object} lebai.motion.MotionIndex.$Properties
             * @property {number|null} [id] MotionIndex id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a MotionIndex.
             * @memberof lebai.motion
             * @interface IMotionIndex
             * @augments lebai.motion.MotionIndex.$Properties
             * @deprecated Use lebai.motion.MotionIndex.$Properties instead.
             */

            /**
             * Shape of a MotionIndex.
             * @typedef {lebai.motion.MotionIndex.$Properties} lebai.motion.MotionIndex.$Shape
             */

            /**
             * Constructs a new MotionIndex.
             * @memberof lebai.motion
             * @classdesc Represents a MotionIndex.
             * @constructor
             * @param {lebai.motion.MotionIndex.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var MotionIndex = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * MotionIndex id.
             * @member {number} id
             * @memberof lebai.motion.MotionIndex
             * @instance
             */
            MotionIndex.prototype.id = 0;

            /**
             * Creates a new MotionIndex instance using the specified properties.
             * @function create
             * @memberof lebai.motion.MotionIndex
             * @static
             * @param {lebai.motion.MotionIndex.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.MotionIndex} MotionIndex instance
             * @type {{
             *   (properties: lebai.motion.MotionIndex.$Shape): lebai.motion.MotionIndex & lebai.motion.MotionIndex.$Shape;
             *   (properties?: lebai.motion.MotionIndex.$Properties): lebai.motion.MotionIndex;
             * }}
             */
            MotionIndex.create = function(properties) {
                return new MotionIndex(properties);
            };

            /**
             * Encodes the specified MotionIndex message. Does not implicitly {@link lebai.motion.MotionIndex.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.MotionIndex
             * @static
             * @param {lebai.motion.MotionIndex.$Properties} message MotionIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MotionIndex.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MotionIndex message, length delimited. Does not implicitly {@link lebai.motion.MotionIndex.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.MotionIndex
             * @static
             * @param {lebai.motion.MotionIndex.$Properties} message MotionIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MotionIndex.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a MotionIndex message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.MotionIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.MotionIndex & lebai.motion.MotionIndex.$Shape} MotionIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MotionIndex.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.MotionIndex();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a MotionIndex message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.MotionIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.MotionIndex & lebai.motion.MotionIndex.$Shape} MotionIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MotionIndex.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MotionIndex message.
             * @function verify
             * @memberof lebai.motion.MotionIndex
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MotionIndex.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                return null;
            };

            /**
             * Creates a MotionIndex message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.MotionIndex
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.MotionIndex} MotionIndex
             */
            MotionIndex.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.MotionIndex)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.MotionIndex: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.MotionIndex();
                if (object.id != null)
                    if ($Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a MotionIndex message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.MotionIndex
             * @static
             * @param {lebai.motion.MotionIndex} message MotionIndex
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MotionIndex.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.id = 0;
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    object.id = message.id;
                return object;
            };

            /**
             * Converts this MotionIndex to JSON.
             * @function toJSON
             * @memberof lebai.motion.MotionIndex
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MotionIndex.prototype.toJSON = function() {
                return MotionIndex.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for MotionIndex
             * @function getTypeUrl
             * @memberof lebai.motion.MotionIndex
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            MotionIndex.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.MotionIndex";
            };

            return MotionIndex;
        })();

        /**
         * MotionState enum.
         * @name lebai.motion.MotionState
         * @enum {number}
         * @property {number} WAIT=0 WAIT value
         * @property {number} RUNNING=1 RUNNING value
         * @property {number} FINISHED=2 FINISHED value
         */
        motion.MotionState = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "WAIT"] = 0;
            values[valuesById[1] = "RUNNING"] = 1;
            values[valuesById[2] = "FINISHED"] = 2;
            return values;
        })();

        motion.GetMotionStateResponse = (function() {

            /**
             * Properties of a GetMotionStateResponse.
             * @typedef {Object} lebai.motion.GetMotionStateResponse.$Properties
             * @property {lebai.motion.MotionState|null} [state] GetMotionStateResponse state
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetMotionStateResponse.
             * @memberof lebai.motion
             * @interface IGetMotionStateResponse
             * @augments lebai.motion.GetMotionStateResponse.$Properties
             * @deprecated Use lebai.motion.GetMotionStateResponse.$Properties instead.
             */

            /**
             * Shape of a GetMotionStateResponse.
             * @typedef {lebai.motion.GetMotionStateResponse.$Properties} lebai.motion.GetMotionStateResponse.$Shape
             */

            /**
             * Constructs a new GetMotionStateResponse.
             * @memberof lebai.motion
             * @classdesc Represents a GetMotionStateResponse.
             * @constructor
             * @param {lebai.motion.GetMotionStateResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetMotionStateResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetMotionStateResponse state.
             * @member {lebai.motion.MotionState} state
             * @memberof lebai.motion.GetMotionStateResponse
             * @instance
             */
            GetMotionStateResponse.prototype.state = 0;

            /**
             * Creates a new GetMotionStateResponse instance using the specified properties.
             * @function create
             * @memberof lebai.motion.GetMotionStateResponse
             * @static
             * @param {lebai.motion.GetMotionStateResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.GetMotionStateResponse} GetMotionStateResponse instance
             * @type {{
             *   (properties: lebai.motion.GetMotionStateResponse.$Shape): lebai.motion.GetMotionStateResponse & lebai.motion.GetMotionStateResponse.$Shape;
             *   (properties?: lebai.motion.GetMotionStateResponse.$Properties): lebai.motion.GetMotionStateResponse;
             * }}
             */
            GetMotionStateResponse.create = function(properties) {
                return new GetMotionStateResponse(properties);
            };

            /**
             * Encodes the specified GetMotionStateResponse message. Does not implicitly {@link lebai.motion.GetMotionStateResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.GetMotionStateResponse
             * @static
             * @param {lebai.motion.GetMotionStateResponse.$Properties} message GetMotionStateResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetMotionStateResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.state != null && $Object.hasOwnProperty.call(message, "state") && message.state !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.state);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetMotionStateResponse message, length delimited. Does not implicitly {@link lebai.motion.GetMotionStateResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.GetMotionStateResponse
             * @static
             * @param {lebai.motion.GetMotionStateResponse.$Properties} message GetMotionStateResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetMotionStateResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetMotionStateResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.GetMotionStateResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.GetMotionStateResponse & lebai.motion.GetMotionStateResponse.$Shape} GetMotionStateResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetMotionStateResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.GetMotionStateResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.state = value;
                            else
                                delete message.state;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetMotionStateResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.GetMotionStateResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.GetMotionStateResponse & lebai.motion.GetMotionStateResponse.$Shape} GetMotionStateResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetMotionStateResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetMotionStateResponse message.
             * @function verify
             * @memberof lebai.motion.GetMotionStateResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetMotionStateResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                    if (typeof message.state !== "number" || (message.state | 0) !== message.state)
                        return "state: enum value expected";
                return null;
            };

            /**
             * Creates a GetMotionStateResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.GetMotionStateResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.GetMotionStateResponse} GetMotionStateResponse
             */
            GetMotionStateResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.GetMotionStateResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.GetMotionStateResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.GetMotionStateResponse();
                if (object.state !== 0 && (typeof object.state !== "string" || $root.lebai.motion.MotionState[object.state] !== 0))
                    switch (object.state) {
                    case "WAIT":
                    case 0:
                        message.state = 0;
                        break;
                    case "RUNNING":
                    case 1:
                        message.state = 1;
                        break;
                    case "FINISHED":
                    case 2:
                        message.state = 2;
                        break;
                    default:
                        if (typeof object.state === "number" && (object.state | 0) === object.state)
                            message.state = object.state;
                    }
                return message;
            };

            /**
             * Creates a plain object from a GetMotionStateResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.GetMotionStateResponse
             * @static
             * @param {lebai.motion.GetMotionStateResponse} message GetMotionStateResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetMotionStateResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.state = options.enums === $String ? "WAIT" : 0;
                if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                    object.state = options.enums === $String ? $root.lebai.motion.MotionState[message.state] === $undefined ? message.state : $root.lebai.motion.MotionState[message.state] : message.state;
                return object;
            };

            /**
             * Converts this GetMotionStateResponse to JSON.
             * @function toJSON
             * @memberof lebai.motion.GetMotionStateResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetMotionStateResponse.prototype.toJSON = function() {
                return GetMotionStateResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetMotionStateResponse
             * @function getTypeUrl
             * @memberof lebai.motion.GetMotionStateResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetMotionStateResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.GetMotionStateResponse";
            };

            return GetMotionStateResponse;
        })();

        motion.MoveParam = (function() {

            /**
             * Properties of a MoveParam.
             * @typedef {Object} lebai.motion.MoveParam.$Properties
             * @property {number|null} [velocity] MoveParam velocity
             * @property {number|null} [acc] MoveParam acc
             * @property {number|null} [time] MoveParam time
             * @property {number|null} [radius] MoveParam radius
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a MoveParam.
             * @memberof lebai.motion
             * @interface IMoveParam
             * @augments lebai.motion.MoveParam.$Properties
             * @deprecated Use lebai.motion.MoveParam.$Properties instead.
             */

            /**
             * Shape of a MoveParam.
             * @typedef {lebai.motion.MoveParam.$Properties} lebai.motion.MoveParam.$Shape
             */

            /**
             * Constructs a new MoveParam.
             * @memberof lebai.motion
             * @classdesc Represents a MoveParam.
             * @constructor
             * @param {lebai.motion.MoveParam.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var MoveParam = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * MoveParam velocity.
             * @member {number} velocity
             * @memberof lebai.motion.MoveParam
             * @instance
             */
            MoveParam.prototype.velocity = 0;

            /**
             * MoveParam acc.
             * @member {number} acc
             * @memberof lebai.motion.MoveParam
             * @instance
             */
            MoveParam.prototype.acc = 0;

            /**
             * MoveParam time.
             * @member {number} time
             * @memberof lebai.motion.MoveParam
             * @instance
             */
            MoveParam.prototype.time = 0;

            /**
             * MoveParam radius.
             * @member {number} radius
             * @memberof lebai.motion.MoveParam
             * @instance
             */
            MoveParam.prototype.radius = 0;

            /**
             * Creates a new MoveParam instance using the specified properties.
             * @function create
             * @memberof lebai.motion.MoveParam
             * @static
             * @param {lebai.motion.MoveParam.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.MoveParam} MoveParam instance
             * @type {{
             *   (properties: lebai.motion.MoveParam.$Shape): lebai.motion.MoveParam & lebai.motion.MoveParam.$Shape;
             *   (properties?: lebai.motion.MoveParam.$Properties): lebai.motion.MoveParam;
             * }}
             */
            MoveParam.create = function(properties) {
                return new MoveParam(properties);
            };

            /**
             * Encodes the specified MoveParam message. Does not implicitly {@link lebai.motion.MoveParam.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.MoveParam
             * @static
             * @param {lebai.motion.MoveParam.$Properties} message MoveParam message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MoveParam.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.velocity != null && $Object.hasOwnProperty.call(message, "velocity") && !$Object.is(message.velocity, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.velocity);
                if (message.acc != null && $Object.hasOwnProperty.call(message, "acc") && !$Object.is(message.acc, 0))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.acc);
                if (message.time != null && $Object.hasOwnProperty.call(message, "time") && !$Object.is(message.time, 0))
                    writer.uint32(/* id 4, wireType 1 =*/33).double(message.time);
                if (message.radius != null && $Object.hasOwnProperty.call(message, "radius") && !$Object.is(message.radius, 0))
                    writer.uint32(/* id 5, wireType 1 =*/41).double(message.radius);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MoveParam message, length delimited. Does not implicitly {@link lebai.motion.MoveParam.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.MoveParam
             * @static
             * @param {lebai.motion.MoveParam.$Properties} message MoveParam message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MoveParam.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a MoveParam message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.MoveParam
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.MoveParam & lebai.motion.MoveParam.$Shape} MoveParam
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MoveParam.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.MoveParam();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.velocity = value;
                            else
                                delete message.velocity;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.acc = value;
                            else
                                delete message.acc;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.time = value;
                            else
                                delete message.time;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.radius = value;
                            else
                                delete message.radius;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a MoveParam message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.MoveParam
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.MoveParam & lebai.motion.MoveParam.$Shape} MoveParam
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MoveParam.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MoveParam message.
             * @function verify
             * @memberof lebai.motion.MoveParam
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MoveParam.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.velocity != null && $Object.hasOwnProperty.call(message, "velocity"))
                    if (typeof message.velocity !== "number")
                        return "velocity: number expected";
                if (message.acc != null && $Object.hasOwnProperty.call(message, "acc"))
                    if (typeof message.acc !== "number")
                        return "acc: number expected";
                if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                    if (typeof message.time !== "number")
                        return "time: number expected";
                if (message.radius != null && $Object.hasOwnProperty.call(message, "radius"))
                    if (typeof message.radius !== "number")
                        return "radius: number expected";
                return null;
            };

            /**
             * Creates a MoveParam message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.MoveParam
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.MoveParam} MoveParam
             */
            MoveParam.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.MoveParam)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.MoveParam: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.MoveParam();
                if (object.velocity != null)
                    if (!$Object.is($Number(object.velocity), 0))
                        message.velocity = $Number(object.velocity);
                if (object.acc != null)
                    if (!$Object.is($Number(object.acc), 0))
                        message.acc = $Number(object.acc);
                if (object.time != null)
                    if (!$Object.is($Number(object.time), 0))
                        message.time = $Number(object.time);
                if (object.radius != null)
                    if (!$Object.is($Number(object.radius), 0))
                        message.radius = $Number(object.radius);
                return message;
            };

            /**
             * Creates a plain object from a MoveParam message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.MoveParam
             * @static
             * @param {lebai.motion.MoveParam} message MoveParam
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MoveParam.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.velocity = 0;
                    object.acc = 0;
                    object.time = 0;
                    object.radius = 0;
                }
                if (message.velocity != null && $Object.hasOwnProperty.call(message, "velocity"))
                    object.velocity = options.json && !$isFinite(message.velocity) ? $String(message.velocity) : message.velocity;
                if (message.acc != null && $Object.hasOwnProperty.call(message, "acc"))
                    object.acc = options.json && !$isFinite(message.acc) ? $String(message.acc) : message.acc;
                if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                    object.time = options.json && !$isFinite(message.time) ? $String(message.time) : message.time;
                if (message.radius != null && $Object.hasOwnProperty.call(message, "radius"))
                    object.radius = options.json && !$isFinite(message.radius) ? $String(message.radius) : message.radius;
                return object;
            };

            /**
             * Converts this MoveParam to JSON.
             * @function toJSON
             * @memberof lebai.motion.MoveParam
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MoveParam.prototype.toJSON = function() {
                return MoveParam.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for MoveParam
             * @function getTypeUrl
             * @memberof lebai.motion.MoveParam
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            MoveParam.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.MoveParam";
            };

            return MoveParam;
        })();

        motion.MoveRequest = (function() {

            /**
             * Properties of a MoveRequest.
             * @typedef {Object} lebai.motion.MoveRequest.$Properties
             * @property {lebai.posture.Pose.$Properties|null} [pose] MoveRequest pose
             * @property {lebai.motion.MoveParam.$Properties|null} [param] MoveRequest param
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a MoveRequest.
             * @memberof lebai.motion
             * @interface IMoveRequest
             * @augments lebai.motion.MoveRequest.$Properties
             * @deprecated Use lebai.motion.MoveRequest.$Properties instead.
             */

            /**
             * Shape of a MoveRequest.
             * @typedef {lebai.motion.MoveRequest.$Properties} lebai.motion.MoveRequest.$Shape
             */

            /**
             * Constructs a new MoveRequest.
             * @memberof lebai.motion
             * @classdesc Represents a MoveRequest.
             * @constructor
             * @param {lebai.motion.MoveRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var MoveRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * MoveRequest pose.
             * @member {lebai.posture.Pose.$Properties|null|undefined} pose
             * @memberof lebai.motion.MoveRequest
             * @instance
             */
            MoveRequest.prototype.pose = null;

            /**
             * MoveRequest param.
             * @member {lebai.motion.MoveParam.$Properties|null|undefined} param
             * @memberof lebai.motion.MoveRequest
             * @instance
             */
            MoveRequest.prototype.param = null;

            /**
             * Creates a new MoveRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motion.MoveRequest
             * @static
             * @param {lebai.motion.MoveRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.MoveRequest} MoveRequest instance
             * @type {{
             *   (properties: lebai.motion.MoveRequest.$Shape): lebai.motion.MoveRequest & lebai.motion.MoveRequest.$Shape;
             *   (properties?: lebai.motion.MoveRequest.$Properties): lebai.motion.MoveRequest;
             * }}
             */
            MoveRequest.create = function(properties) {
                return new MoveRequest(properties);
            };

            /**
             * Encodes the specified MoveRequest message. Does not implicitly {@link lebai.motion.MoveRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.MoveRequest
             * @static
             * @param {lebai.motion.MoveRequest.$Properties} message MoveRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MoveRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    $root.lebai.posture.Pose.encode(message.pose, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.param != null && $Object.hasOwnProperty.call(message, "param"))
                    $root.lebai.motion.MoveParam.encode(message.param, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MoveRequest message, length delimited. Does not implicitly {@link lebai.motion.MoveRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.MoveRequest
             * @static
             * @param {lebai.motion.MoveRequest.$Properties} message MoveRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MoveRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a MoveRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.MoveRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.MoveRequest & lebai.motion.MoveRequest.$Shape} MoveRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MoveRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.MoveRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.pose = $root.lebai.posture.Pose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pose);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.param = $root.lebai.motion.MoveParam.decode(reader, reader.uint32(), $undefined, _depth + 1, message.param);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a MoveRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.MoveRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.MoveRequest & lebai.motion.MoveRequest.$Shape} MoveRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MoveRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MoveRequest message.
             * @function verify
             * @memberof lebai.motion.MoveRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MoveRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose")) {
                    var error = $root.lebai.posture.Pose.verify(message.pose, _depth + 1);
                    if (error)
                        return "pose." + error;
                }
                if (message.param != null && $Object.hasOwnProperty.call(message, "param")) {
                    var error = $root.lebai.motion.MoveParam.verify(message.param, _depth + 1);
                    if (error)
                        return "param." + error;
                }
                return null;
            };

            /**
             * Creates a MoveRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.MoveRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.MoveRequest} MoveRequest
             */
            MoveRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.MoveRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.MoveRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.MoveRequest();
                if (object.pose != null) {
                    if (!$util.isObject(object.pose))
                        throw $TypeError(".lebai.motion.MoveRequest.pose: object expected");
                    message.pose = $root.lebai.posture.Pose.fromObject(object.pose, _depth + 1);
                }
                if (object.param != null) {
                    if (!$util.isObject(object.param))
                        throw $TypeError(".lebai.motion.MoveRequest.param: object expected");
                    message.param = $root.lebai.motion.MoveParam.fromObject(object.param, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a MoveRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.MoveRequest
             * @static
             * @param {lebai.motion.MoveRequest} message MoveRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MoveRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.pose = null;
                    object.param = null;
                }
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    object.pose = $root.lebai.posture.Pose.toObject(message.pose, options, _depth + 1);
                if (message.param != null && $Object.hasOwnProperty.call(message, "param"))
                    object.param = $root.lebai.motion.MoveParam.toObject(message.param, options, _depth + 1);
                return object;
            };

            /**
             * Converts this MoveRequest to JSON.
             * @function toJSON
             * @memberof lebai.motion.MoveRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MoveRequest.prototype.toJSON = function() {
                return MoveRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for MoveRequest
             * @function getTypeUrl
             * @memberof lebai.motion.MoveRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            MoveRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.MoveRequest";
            };

            return MoveRequest;
        })();

        motion.MovecRequest = (function() {

            /**
             * Properties of a MovecRequest.
             * @typedef {Object} lebai.motion.MovecRequest.$Properties
             * @property {lebai.posture.Pose.$Properties|null} [poseVia] MovecRequest poseVia
             * @property {lebai.posture.Pose.$Properties|null} [pose] MovecRequest pose
             * @property {number|null} [rad] MovecRequest rad
             * @property {lebai.motion.MoveParam.$Properties|null} [param] MovecRequest param
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a MovecRequest.
             * @memberof lebai.motion
             * @interface IMovecRequest
             * @augments lebai.motion.MovecRequest.$Properties
             * @deprecated Use lebai.motion.MovecRequest.$Properties instead.
             */

            /**
             * Shape of a MovecRequest.
             * @typedef {lebai.motion.MovecRequest.$Properties} lebai.motion.MovecRequest.$Shape
             */

            /**
             * Constructs a new MovecRequest.
             * @memberof lebai.motion
             * @classdesc Represents a MovecRequest.
             * @constructor
             * @param {lebai.motion.MovecRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var MovecRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * MovecRequest poseVia.
             * @member {lebai.posture.Pose.$Properties|null|undefined} poseVia
             * @memberof lebai.motion.MovecRequest
             * @instance
             */
            MovecRequest.prototype.poseVia = null;

            /**
             * MovecRequest pose.
             * @member {lebai.posture.Pose.$Properties|null|undefined} pose
             * @memberof lebai.motion.MovecRequest
             * @instance
             */
            MovecRequest.prototype.pose = null;

            /**
             * MovecRequest rad.
             * @member {number} rad
             * @memberof lebai.motion.MovecRequest
             * @instance
             */
            MovecRequest.prototype.rad = 0;

            /**
             * MovecRequest param.
             * @member {lebai.motion.MoveParam.$Properties|null|undefined} param
             * @memberof lebai.motion.MovecRequest
             * @instance
             */
            MovecRequest.prototype.param = null;

            /**
             * Creates a new MovecRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motion.MovecRequest
             * @static
             * @param {lebai.motion.MovecRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.MovecRequest} MovecRequest instance
             * @type {{
             *   (properties: lebai.motion.MovecRequest.$Shape): lebai.motion.MovecRequest & lebai.motion.MovecRequest.$Shape;
             *   (properties?: lebai.motion.MovecRequest.$Properties): lebai.motion.MovecRequest;
             * }}
             */
            MovecRequest.create = function(properties) {
                return new MovecRequest(properties);
            };

            /**
             * Encodes the specified MovecRequest message. Does not implicitly {@link lebai.motion.MovecRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.MovecRequest
             * @static
             * @param {lebai.motion.MovecRequest.$Properties} message MovecRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MovecRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.poseVia != null && $Object.hasOwnProperty.call(message, "poseVia"))
                    $root.lebai.posture.Pose.encode(message.poseVia, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    $root.lebai.posture.Pose.encode(message.pose, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.rad != null && $Object.hasOwnProperty.call(message, "rad") && !$Object.is(message.rad, 0))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.rad);
                if (message.param != null && $Object.hasOwnProperty.call(message, "param"))
                    $root.lebai.motion.MoveParam.encode(message.param, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MovecRequest message, length delimited. Does not implicitly {@link lebai.motion.MovecRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.MovecRequest
             * @static
             * @param {lebai.motion.MovecRequest.$Properties} message MovecRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MovecRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a MovecRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.MovecRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.MovecRequest & lebai.motion.MovecRequest.$Shape} MovecRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MovecRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.MovecRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.poseVia = $root.lebai.posture.Pose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.poseVia);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.pose = $root.lebai.posture.Pose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pose);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.rad = value;
                            else
                                delete message.rad;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.param = $root.lebai.motion.MoveParam.decode(reader, reader.uint32(), $undefined, _depth + 1, message.param);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a MovecRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.MovecRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.MovecRequest & lebai.motion.MovecRequest.$Shape} MovecRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MovecRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MovecRequest message.
             * @function verify
             * @memberof lebai.motion.MovecRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MovecRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.poseVia != null && $Object.hasOwnProperty.call(message, "poseVia")) {
                    var error = $root.lebai.posture.Pose.verify(message.poseVia, _depth + 1);
                    if (error)
                        return "poseVia." + error;
                }
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose")) {
                    var error = $root.lebai.posture.Pose.verify(message.pose, _depth + 1);
                    if (error)
                        return "pose." + error;
                }
                if (message.rad != null && $Object.hasOwnProperty.call(message, "rad"))
                    if (typeof message.rad !== "number")
                        return "rad: number expected";
                if (message.param != null && $Object.hasOwnProperty.call(message, "param")) {
                    var error = $root.lebai.motion.MoveParam.verify(message.param, _depth + 1);
                    if (error)
                        return "param." + error;
                }
                return null;
            };

            /**
             * Creates a MovecRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.MovecRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.MovecRequest} MovecRequest
             */
            MovecRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.MovecRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.MovecRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.MovecRequest();
                if (object.poseVia != null) {
                    if (!$util.isObject(object.poseVia))
                        throw $TypeError(".lebai.motion.MovecRequest.poseVia: object expected");
                    message.poseVia = $root.lebai.posture.Pose.fromObject(object.poseVia, _depth + 1);
                }
                if (object.pose != null) {
                    if (!$util.isObject(object.pose))
                        throw $TypeError(".lebai.motion.MovecRequest.pose: object expected");
                    message.pose = $root.lebai.posture.Pose.fromObject(object.pose, _depth + 1);
                }
                if (object.rad != null)
                    if (!$Object.is($Number(object.rad), 0))
                        message.rad = $Number(object.rad);
                if (object.param != null) {
                    if (!$util.isObject(object.param))
                        throw $TypeError(".lebai.motion.MovecRequest.param: object expected");
                    message.param = $root.lebai.motion.MoveParam.fromObject(object.param, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a MovecRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.MovecRequest
             * @static
             * @param {lebai.motion.MovecRequest} message MovecRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MovecRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.poseVia = null;
                    object.pose = null;
                    object.rad = 0;
                    object.param = null;
                }
                if (message.poseVia != null && $Object.hasOwnProperty.call(message, "poseVia"))
                    object.poseVia = $root.lebai.posture.Pose.toObject(message.poseVia, options, _depth + 1);
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    object.pose = $root.lebai.posture.Pose.toObject(message.pose, options, _depth + 1);
                if (message.rad != null && $Object.hasOwnProperty.call(message, "rad"))
                    object.rad = options.json && !$isFinite(message.rad) ? $String(message.rad) : message.rad;
                if (message.param != null && $Object.hasOwnProperty.call(message, "param"))
                    object.param = $root.lebai.motion.MoveParam.toObject(message.param, options, _depth + 1);
                return object;
            };

            /**
             * Converts this MovecRequest to JSON.
             * @function toJSON
             * @memberof lebai.motion.MovecRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MovecRequest.prototype.toJSON = function() {
                return MovecRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for MovecRequest
             * @function getTypeUrl
             * @memberof lebai.motion.MovecRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            MovecRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.MovecRequest";
            };

            return MovecRequest;
        })();

        motion.SpeedParam = (function() {

            /**
             * Properties of a SpeedParam.
             * @typedef {Object} lebai.motion.SpeedParam.$Properties
             * @property {number|null} [acc] SpeedParam acc
             * @property {number|null} [time] SpeedParam time
             * @property {boolean|null} [constrained] SpeedParam constrained
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SpeedParam.
             * @memberof lebai.motion
             * @interface ISpeedParam
             * @augments lebai.motion.SpeedParam.$Properties
             * @deprecated Use lebai.motion.SpeedParam.$Properties instead.
             */

            /**
             * Shape of a SpeedParam.
             * @typedef {lebai.motion.SpeedParam.$Properties} lebai.motion.SpeedParam.$Shape
             */

            /**
             * Constructs a new SpeedParam.
             * @memberof lebai.motion
             * @classdesc Represents a SpeedParam.
             * @constructor
             * @param {lebai.motion.SpeedParam.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SpeedParam = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SpeedParam acc.
             * @member {number} acc
             * @memberof lebai.motion.SpeedParam
             * @instance
             */
            SpeedParam.prototype.acc = 0;

            /**
             * SpeedParam time.
             * @member {number} time
             * @memberof lebai.motion.SpeedParam
             * @instance
             */
            SpeedParam.prototype.time = 0;

            /**
             * SpeedParam constrained.
             * @member {boolean} constrained
             * @memberof lebai.motion.SpeedParam
             * @instance
             */
            SpeedParam.prototype.constrained = false;

            /**
             * Creates a new SpeedParam instance using the specified properties.
             * @function create
             * @memberof lebai.motion.SpeedParam
             * @static
             * @param {lebai.motion.SpeedParam.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.SpeedParam} SpeedParam instance
             * @type {{
             *   (properties: lebai.motion.SpeedParam.$Shape): lebai.motion.SpeedParam & lebai.motion.SpeedParam.$Shape;
             *   (properties?: lebai.motion.SpeedParam.$Properties): lebai.motion.SpeedParam;
             * }}
             */
            SpeedParam.create = function(properties) {
                return new SpeedParam(properties);
            };

            /**
             * Encodes the specified SpeedParam message. Does not implicitly {@link lebai.motion.SpeedParam.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.SpeedParam
             * @static
             * @param {lebai.motion.SpeedParam.$Properties} message SpeedParam message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SpeedParam.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.acc != null && $Object.hasOwnProperty.call(message, "acc") && !$Object.is(message.acc, 0))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.acc);
                if (message.time != null && $Object.hasOwnProperty.call(message, "time") && !$Object.is(message.time, 0))
                    writer.uint32(/* id 4, wireType 1 =*/33).double(message.time);
                if (message.constrained != null && $Object.hasOwnProperty.call(message, "constrained") && message.constrained !== false)
                    writer.uint32(/* id 6, wireType 0 =*/48).bool(message.constrained);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SpeedParam message, length delimited. Does not implicitly {@link lebai.motion.SpeedParam.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.SpeedParam
             * @static
             * @param {lebai.motion.SpeedParam.$Properties} message SpeedParam message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SpeedParam.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SpeedParam message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.SpeedParam
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.SpeedParam & lebai.motion.SpeedParam.$Shape} SpeedParam
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SpeedParam.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.SpeedParam();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 3: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.acc = value;
                            else
                                delete message.acc;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.time = value;
                            else
                                delete message.time;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.constrained = value;
                            else
                                delete message.constrained;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SpeedParam message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.SpeedParam
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.SpeedParam & lebai.motion.SpeedParam.$Shape} SpeedParam
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SpeedParam.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SpeedParam message.
             * @function verify
             * @memberof lebai.motion.SpeedParam
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SpeedParam.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.acc != null && $Object.hasOwnProperty.call(message, "acc"))
                    if (typeof message.acc !== "number")
                        return "acc: number expected";
                if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                    if (typeof message.time !== "number")
                        return "time: number expected";
                if (message.constrained != null && $Object.hasOwnProperty.call(message, "constrained"))
                    if (typeof message.constrained !== "boolean")
                        return "constrained: boolean expected";
                return null;
            };

            /**
             * Creates a SpeedParam message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.SpeedParam
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.SpeedParam} SpeedParam
             */
            SpeedParam.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.SpeedParam)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.SpeedParam: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.SpeedParam();
                if (object.acc != null)
                    if (!$Object.is($Number(object.acc), 0))
                        message.acc = $Number(object.acc);
                if (object.time != null)
                    if (!$Object.is($Number(object.time), 0))
                        message.time = $Number(object.time);
                if (object.constrained != null)
                    if (object.constrained)
                        message.constrained = $Boolean(object.constrained);
                return message;
            };

            /**
             * Creates a plain object from a SpeedParam message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.SpeedParam
             * @static
             * @param {lebai.motion.SpeedParam} message SpeedParam
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SpeedParam.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.acc = 0;
                    object.time = 0;
                    object.constrained = false;
                }
                if (message.acc != null && $Object.hasOwnProperty.call(message, "acc"))
                    object.acc = options.json && !$isFinite(message.acc) ? $String(message.acc) : message.acc;
                if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                    object.time = options.json && !$isFinite(message.time) ? $String(message.time) : message.time;
                if (message.constrained != null && $Object.hasOwnProperty.call(message, "constrained"))
                    object.constrained = message.constrained;
                return object;
            };

            /**
             * Converts this SpeedParam to JSON.
             * @function toJSON
             * @memberof lebai.motion.SpeedParam
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SpeedParam.prototype.toJSON = function() {
                return SpeedParam.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SpeedParam
             * @function getTypeUrl
             * @memberof lebai.motion.SpeedParam
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SpeedParam.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.SpeedParam";
            };

            return SpeedParam;
        })();

        motion.SpeedJRequest = (function() {

            /**
             * Properties of a SpeedJRequest.
             * @typedef {Object} lebai.motion.SpeedJRequest.$Properties
             * @property {lebai.posture.JointPose.$Properties|null} [speed] SpeedJRequest speed
             * @property {lebai.motion.SpeedParam.$Properties|null} [param] SpeedJRequest param
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SpeedJRequest.
             * @memberof lebai.motion
             * @interface ISpeedJRequest
             * @augments lebai.motion.SpeedJRequest.$Properties
             * @deprecated Use lebai.motion.SpeedJRequest.$Properties instead.
             */

            /**
             * Shape of a SpeedJRequest.
             * @typedef {lebai.motion.SpeedJRequest.$Properties} lebai.motion.SpeedJRequest.$Shape
             */

            /**
             * Constructs a new SpeedJRequest.
             * @memberof lebai.motion
             * @classdesc Represents a SpeedJRequest.
             * @constructor
             * @param {lebai.motion.SpeedJRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SpeedJRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SpeedJRequest speed.
             * @member {lebai.posture.JointPose.$Properties|null|undefined} speed
             * @memberof lebai.motion.SpeedJRequest
             * @instance
             */
            SpeedJRequest.prototype.speed = null;

            /**
             * SpeedJRequest param.
             * @member {lebai.motion.SpeedParam.$Properties|null|undefined} param
             * @memberof lebai.motion.SpeedJRequest
             * @instance
             */
            SpeedJRequest.prototype.param = null;

            /**
             * Creates a new SpeedJRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motion.SpeedJRequest
             * @static
             * @param {lebai.motion.SpeedJRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.SpeedJRequest} SpeedJRequest instance
             * @type {{
             *   (properties: lebai.motion.SpeedJRequest.$Shape): lebai.motion.SpeedJRequest & lebai.motion.SpeedJRequest.$Shape;
             *   (properties?: lebai.motion.SpeedJRequest.$Properties): lebai.motion.SpeedJRequest;
             * }}
             */
            SpeedJRequest.create = function(properties) {
                return new SpeedJRequest(properties);
            };

            /**
             * Encodes the specified SpeedJRequest message. Does not implicitly {@link lebai.motion.SpeedJRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.SpeedJRequest
             * @static
             * @param {lebai.motion.SpeedJRequest.$Properties} message SpeedJRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SpeedJRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.speed != null && $Object.hasOwnProperty.call(message, "speed"))
                    $root.lebai.posture.JointPose.encode(message.speed, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.param != null && $Object.hasOwnProperty.call(message, "param"))
                    $root.lebai.motion.SpeedParam.encode(message.param, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SpeedJRequest message, length delimited. Does not implicitly {@link lebai.motion.SpeedJRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.SpeedJRequest
             * @static
             * @param {lebai.motion.SpeedJRequest.$Properties} message SpeedJRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SpeedJRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SpeedJRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.SpeedJRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.SpeedJRequest & lebai.motion.SpeedJRequest.$Shape} SpeedJRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SpeedJRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.SpeedJRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.speed = $root.lebai.posture.JointPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.speed);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.param = $root.lebai.motion.SpeedParam.decode(reader, reader.uint32(), $undefined, _depth + 1, message.param);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SpeedJRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.SpeedJRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.SpeedJRequest & lebai.motion.SpeedJRequest.$Shape} SpeedJRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SpeedJRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SpeedJRequest message.
             * @function verify
             * @memberof lebai.motion.SpeedJRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SpeedJRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.speed != null && $Object.hasOwnProperty.call(message, "speed")) {
                    var error = $root.lebai.posture.JointPose.verify(message.speed, _depth + 1);
                    if (error)
                        return "speed." + error;
                }
                if (message.param != null && $Object.hasOwnProperty.call(message, "param")) {
                    var error = $root.lebai.motion.SpeedParam.verify(message.param, _depth + 1);
                    if (error)
                        return "param." + error;
                }
                return null;
            };

            /**
             * Creates a SpeedJRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.SpeedJRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.SpeedJRequest} SpeedJRequest
             */
            SpeedJRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.SpeedJRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.SpeedJRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.SpeedJRequest();
                if (object.speed != null) {
                    if (!$util.isObject(object.speed))
                        throw $TypeError(".lebai.motion.SpeedJRequest.speed: object expected");
                    message.speed = $root.lebai.posture.JointPose.fromObject(object.speed, _depth + 1);
                }
                if (object.param != null) {
                    if (!$util.isObject(object.param))
                        throw $TypeError(".lebai.motion.SpeedJRequest.param: object expected");
                    message.param = $root.lebai.motion.SpeedParam.fromObject(object.param, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a SpeedJRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.SpeedJRequest
             * @static
             * @param {lebai.motion.SpeedJRequest} message SpeedJRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SpeedJRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.speed = null;
                    object.param = null;
                }
                if (message.speed != null && $Object.hasOwnProperty.call(message, "speed"))
                    object.speed = $root.lebai.posture.JointPose.toObject(message.speed, options, _depth + 1);
                if (message.param != null && $Object.hasOwnProperty.call(message, "param"))
                    object.param = $root.lebai.motion.SpeedParam.toObject(message.param, options, _depth + 1);
                return object;
            };

            /**
             * Converts this SpeedJRequest to JSON.
             * @function toJSON
             * @memberof lebai.motion.SpeedJRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SpeedJRequest.prototype.toJSON = function() {
                return SpeedJRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SpeedJRequest
             * @function getTypeUrl
             * @memberof lebai.motion.SpeedJRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SpeedJRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.SpeedJRequest";
            };

            return SpeedJRequest;
        })();

        motion.SpeedLRequest = (function() {

            /**
             * Properties of a SpeedLRequest.
             * @typedef {Object} lebai.motion.SpeedLRequest.$Properties
             * @property {lebai.posture.CartesianPose.$Properties|null} [speed] SpeedLRequest speed
             * @property {lebai.motion.SpeedParam.$Properties|null} [param] SpeedLRequest param
             * @property {lebai.posture.CartesianFrame.$Properties|null} [frame] SpeedLRequest frame
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SpeedLRequest.
             * @memberof lebai.motion
             * @interface ISpeedLRequest
             * @augments lebai.motion.SpeedLRequest.$Properties
             * @deprecated Use lebai.motion.SpeedLRequest.$Properties instead.
             */

            /**
             * Shape of a SpeedLRequest.
             * @typedef {lebai.motion.SpeedLRequest.$Properties} lebai.motion.SpeedLRequest.$Shape
             */

            /**
             * Constructs a new SpeedLRequest.
             * @memberof lebai.motion
             * @classdesc Represents a SpeedLRequest.
             * @constructor
             * @param {lebai.motion.SpeedLRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SpeedLRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SpeedLRequest speed.
             * @member {lebai.posture.CartesianPose.$Properties|null|undefined} speed
             * @memberof lebai.motion.SpeedLRequest
             * @instance
             */
            SpeedLRequest.prototype.speed = null;

            /**
             * SpeedLRequest param.
             * @member {lebai.motion.SpeedParam.$Properties|null|undefined} param
             * @memberof lebai.motion.SpeedLRequest
             * @instance
             */
            SpeedLRequest.prototype.param = null;

            /**
             * SpeedLRequest frame.
             * @member {lebai.posture.CartesianFrame.$Properties|null|undefined} frame
             * @memberof lebai.motion.SpeedLRequest
             * @instance
             */
            SpeedLRequest.prototype.frame = null;

            /**
             * Creates a new SpeedLRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motion.SpeedLRequest
             * @static
             * @param {lebai.motion.SpeedLRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.SpeedLRequest} SpeedLRequest instance
             * @type {{
             *   (properties: lebai.motion.SpeedLRequest.$Shape): lebai.motion.SpeedLRequest & lebai.motion.SpeedLRequest.$Shape;
             *   (properties?: lebai.motion.SpeedLRequest.$Properties): lebai.motion.SpeedLRequest;
             * }}
             */
            SpeedLRequest.create = function(properties) {
                return new SpeedLRequest(properties);
            };

            /**
             * Encodes the specified SpeedLRequest message. Does not implicitly {@link lebai.motion.SpeedLRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.SpeedLRequest
             * @static
             * @param {lebai.motion.SpeedLRequest.$Properties} message SpeedLRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SpeedLRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.speed != null && $Object.hasOwnProperty.call(message, "speed"))
                    $root.lebai.posture.CartesianPose.encode(message.speed, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.param != null && $Object.hasOwnProperty.call(message, "param"))
                    $root.lebai.motion.SpeedParam.encode(message.param, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.frame != null && $Object.hasOwnProperty.call(message, "frame"))
                    $root.lebai.posture.CartesianFrame.encode(message.frame, writer.uint32(/* id 21, wireType 2 =*/170).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SpeedLRequest message, length delimited. Does not implicitly {@link lebai.motion.SpeedLRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.SpeedLRequest
             * @static
             * @param {lebai.motion.SpeedLRequest.$Properties} message SpeedLRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SpeedLRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SpeedLRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.SpeedLRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.SpeedLRequest & lebai.motion.SpeedLRequest.$Shape} SpeedLRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SpeedLRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.SpeedLRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.speed = $root.lebai.posture.CartesianPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.speed);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.param = $root.lebai.motion.SpeedParam.decode(reader, reader.uint32(), $undefined, _depth + 1, message.param);
                            continue;
                        }
                    case 21: {
                            if (wireType !== 2)
                                break;
                            message.frame = $root.lebai.posture.CartesianFrame.decode(reader, reader.uint32(), $undefined, _depth + 1, message.frame);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SpeedLRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.SpeedLRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.SpeedLRequest & lebai.motion.SpeedLRequest.$Shape} SpeedLRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SpeedLRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SpeedLRequest message.
             * @function verify
             * @memberof lebai.motion.SpeedLRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SpeedLRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.speed != null && $Object.hasOwnProperty.call(message, "speed")) {
                    var error = $root.lebai.posture.CartesianPose.verify(message.speed, _depth + 1);
                    if (error)
                        return "speed." + error;
                }
                if (message.param != null && $Object.hasOwnProperty.call(message, "param")) {
                    var error = $root.lebai.motion.SpeedParam.verify(message.param, _depth + 1);
                    if (error)
                        return "param." + error;
                }
                if (message.frame != null && $Object.hasOwnProperty.call(message, "frame")) {
                    var error = $root.lebai.posture.CartesianFrame.verify(message.frame, _depth + 1);
                    if (error)
                        return "frame." + error;
                }
                return null;
            };

            /**
             * Creates a SpeedLRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.SpeedLRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.SpeedLRequest} SpeedLRequest
             */
            SpeedLRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.SpeedLRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.SpeedLRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.SpeedLRequest();
                if (object.speed != null) {
                    if (!$util.isObject(object.speed))
                        throw $TypeError(".lebai.motion.SpeedLRequest.speed: object expected");
                    message.speed = $root.lebai.posture.CartesianPose.fromObject(object.speed, _depth + 1);
                }
                if (object.param != null) {
                    if (!$util.isObject(object.param))
                        throw $TypeError(".lebai.motion.SpeedLRequest.param: object expected");
                    message.param = $root.lebai.motion.SpeedParam.fromObject(object.param, _depth + 1);
                }
                if (object.frame != null) {
                    if (!$util.isObject(object.frame))
                        throw $TypeError(".lebai.motion.SpeedLRequest.frame: object expected");
                    message.frame = $root.lebai.posture.CartesianFrame.fromObject(object.frame, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a SpeedLRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.SpeedLRequest
             * @static
             * @param {lebai.motion.SpeedLRequest} message SpeedLRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SpeedLRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.speed = null;
                    object.param = null;
                    object.frame = null;
                }
                if (message.speed != null && $Object.hasOwnProperty.call(message, "speed"))
                    object.speed = $root.lebai.posture.CartesianPose.toObject(message.speed, options, _depth + 1);
                if (message.param != null && $Object.hasOwnProperty.call(message, "param"))
                    object.param = $root.lebai.motion.SpeedParam.toObject(message.param, options, _depth + 1);
                if (message.frame != null && $Object.hasOwnProperty.call(message, "frame"))
                    object.frame = $root.lebai.posture.CartesianFrame.toObject(message.frame, options, _depth + 1);
                return object;
            };

            /**
             * Converts this SpeedLRequest to JSON.
             * @function toJSON
             * @memberof lebai.motion.SpeedLRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SpeedLRequest.prototype.toJSON = function() {
                return SpeedLRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SpeedLRequest
             * @function getTypeUrl
             * @memberof lebai.motion.SpeedLRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SpeedLRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.SpeedLRequest";
            };

            return SpeedLRequest;
        })();

        motion.JointMove = (function() {

            /**
             * Properties of a JointMove.
             * @typedef {Object} lebai.motion.JointMove.$Properties
             * @property {number|null} [pose] JointMove pose
             * @property {number|null} [velocity] JointMove velocity
             * @property {number|null} [acc] JointMove acc
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a JointMove.
             * @memberof lebai.motion
             * @interface IJointMove
             * @augments lebai.motion.JointMove.$Properties
             * @deprecated Use lebai.motion.JointMove.$Properties instead.
             */

            /**
             * Shape of a JointMove.
             * @typedef {lebai.motion.JointMove.$Properties} lebai.motion.JointMove.$Shape
             */

            /**
             * Constructs a new JointMove.
             * @memberof lebai.motion
             * @classdesc Represents a JointMove.
             * @constructor
             * @param {lebai.motion.JointMove.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var JointMove = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * JointMove pose.
             * @member {number} pose
             * @memberof lebai.motion.JointMove
             * @instance
             */
            JointMove.prototype.pose = 0;

            /**
             * JointMove velocity.
             * @member {number|null|undefined} velocity
             * @memberof lebai.motion.JointMove
             * @instance
             */
            JointMove.prototype.velocity = null;

            /**
             * JointMove acc.
             * @member {number|null|undefined} acc
             * @memberof lebai.motion.JointMove
             * @instance
             */
            JointMove.prototype.acc = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(JointMove.prototype, "_velocity", {
                get: $util.oneOfGetter($oneOfFields = ["velocity"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(JointMove.prototype, "_acc", {
                get: $util.oneOfGetter($oneOfFields = ["acc"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new JointMove instance using the specified properties.
             * @function create
             * @memberof lebai.motion.JointMove
             * @static
             * @param {lebai.motion.JointMove.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.JointMove} JointMove instance
             * @type {{
             *   (properties: lebai.motion.JointMove.$Shape): lebai.motion.JointMove & lebai.motion.JointMove.$Shape;
             *   (properties?: lebai.motion.JointMove.$Properties): lebai.motion.JointMove;
             * }}
             */
            JointMove.create = function(properties) {
                return new JointMove(properties);
            };

            /**
             * Encodes the specified JointMove message. Does not implicitly {@link lebai.motion.JointMove.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.JointMove
             * @static
             * @param {lebai.motion.JointMove.$Properties} message JointMove message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JointMove.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose") && !$Object.is(message.pose, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.pose);
                if (message.velocity != null && $Object.hasOwnProperty.call(message, "velocity"))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.velocity);
                if (message.acc != null && $Object.hasOwnProperty.call(message, "acc"))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.acc);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified JointMove message, length delimited. Does not implicitly {@link lebai.motion.JointMove.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.JointMove
             * @static
             * @param {lebai.motion.JointMove.$Properties} message JointMove message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JointMove.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a JointMove message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.JointMove
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.JointMove & lebai.motion.JointMove.$Shape} JointMove
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JointMove.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.JointMove();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.pose = value;
                            else
                                delete message.pose;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            message.velocity = reader.double();
                            message._velocity = "velocity";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            message.acc = reader.double();
                            message._acc = "acc";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a JointMove message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.JointMove
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.JointMove & lebai.motion.JointMove.$Shape} JointMove
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JointMove.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a JointMove message.
             * @function verify
             * @memberof lebai.motion.JointMove
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            JointMove.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    if (typeof message.pose !== "number")
                        return "pose: number expected";
                if (message.velocity != null && $Object.hasOwnProperty.call(message, "velocity")) {
                    properties._velocity = 1;
                    if (typeof message.velocity !== "number")
                        return "velocity: number expected";
                }
                if (message.acc != null && $Object.hasOwnProperty.call(message, "acc")) {
                    properties._acc = 1;
                    if (typeof message.acc !== "number")
                        return "acc: number expected";
                }
                return null;
            };

            /**
             * Creates a JointMove message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.JointMove
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.JointMove} JointMove
             */
            JointMove.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.JointMove)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.JointMove: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.JointMove();
                if (object.pose != null)
                    if (!$Object.is($Number(object.pose), 0))
                        message.pose = $Number(object.pose);
                if (object.velocity != null)
                    message.velocity = $Number(object.velocity);
                if (object.acc != null)
                    message.acc = $Number(object.acc);
                return message;
            };

            /**
             * Creates a plain object from a JointMove message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.JointMove
             * @static
             * @param {lebai.motion.JointMove} message JointMove
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            JointMove.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.pose = 0;
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose"))
                    object.pose = options.json && !$isFinite(message.pose) ? $String(message.pose) : message.pose;
                if (message.velocity != null && $Object.hasOwnProperty.call(message, "velocity"))
                    object.velocity = options.json && !$isFinite(message.velocity) ? $String(message.velocity) : message.velocity;
                if (message.acc != null && $Object.hasOwnProperty.call(message, "acc"))
                    object.acc = options.json && !$isFinite(message.acc) ? $String(message.acc) : message.acc;
                return object;
            };

            /**
             * Converts this JointMove to JSON.
             * @function toJSON
             * @memberof lebai.motion.JointMove
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            JointMove.prototype.toJSON = function() {
                return JointMove.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for JointMove
             * @function getTypeUrl
             * @memberof lebai.motion.JointMove
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            JointMove.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.JointMove";
            };

            return JointMove;
        })();

        /**
         * PvatKind enum.
         * @name lebai.motion.PvatKind
         * @enum {number}
         * @property {number} PT=0 PT value
         * @property {number} PVT=1 PVT value
         * @property {number} PVAT=2 PVAT value
         */
        motion.PvatKind = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "PT"] = 0;
            values[valuesById[1] = "PVT"] = 1;
            values[valuesById[2] = "PVAT"] = 2;
            return values;
        })();

        motion.MovePvatRequest = (function() {

            /**
             * Properties of a MovePvatRequest.
             * @typedef {Object} lebai.motion.MovePvatRequest.$Properties
             * @property {Array.<lebai.motion.JointMove.$Properties>|null} [joints] MovePvatRequest joints
             * @property {number|null} [duration] MovePvatRequest duration
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a MovePvatRequest.
             * @memberof lebai.motion
             * @interface IMovePvatRequest
             * @augments lebai.motion.MovePvatRequest.$Properties
             * @deprecated Use lebai.motion.MovePvatRequest.$Properties instead.
             */

            /**
             * Shape of a MovePvatRequest.
             * @typedef {lebai.motion.MovePvatRequest.$Properties} lebai.motion.MovePvatRequest.$Shape
             */

            /**
             * Constructs a new MovePvatRequest.
             * @memberof lebai.motion
             * @classdesc Represents a MovePvatRequest.
             * @constructor
             * @param {lebai.motion.MovePvatRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var MovePvatRequest = function (properties) {
                this.joints = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * MovePvatRequest joints.
             * @member {Array.<lebai.motion.JointMove.$Properties>} joints
             * @memberof lebai.motion.MovePvatRequest
             * @instance
             */
            MovePvatRequest.prototype.joints = $util.emptyArray;

            /**
             * MovePvatRequest duration.
             * @member {number} duration
             * @memberof lebai.motion.MovePvatRequest
             * @instance
             */
            MovePvatRequest.prototype.duration = 0;

            /**
             * Creates a new MovePvatRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motion.MovePvatRequest
             * @static
             * @param {lebai.motion.MovePvatRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.MovePvatRequest} MovePvatRequest instance
             * @type {{
             *   (properties: lebai.motion.MovePvatRequest.$Shape): lebai.motion.MovePvatRequest & lebai.motion.MovePvatRequest.$Shape;
             *   (properties?: lebai.motion.MovePvatRequest.$Properties): lebai.motion.MovePvatRequest;
             * }}
             */
            MovePvatRequest.create = function(properties) {
                return new MovePvatRequest(properties);
            };

            /**
             * Encodes the specified MovePvatRequest message. Does not implicitly {@link lebai.motion.MovePvatRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.MovePvatRequest
             * @static
             * @param {lebai.motion.MovePvatRequest.$Properties} message MovePvatRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MovePvatRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.joints != null && message.joints.length)
                    for (var i = 0; i < message.joints.length; ++i)
                        $root.lebai.motion.JointMove.encode(message.joints[i], writer.uint32(/* id 10, wireType 2 =*/82).fork(), _depth + 1).ldelim();
                if (message.duration != null && $Object.hasOwnProperty.call(message, "duration") && !$Object.is(message.duration, 0))
                    writer.uint32(/* id 11, wireType 1 =*/89).double(message.duration);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MovePvatRequest message, length delimited. Does not implicitly {@link lebai.motion.MovePvatRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.MovePvatRequest
             * @static
             * @param {lebai.motion.MovePvatRequest.$Properties} message MovePvatRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MovePvatRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a MovePvatRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.MovePvatRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.MovePvatRequest & lebai.motion.MovePvatRequest.$Shape} MovePvatRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MovePvatRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.MovePvatRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 10: {
                            if (wireType !== 2)
                                break;
                            if (!(message.joints && message.joints.length))
                                message.joints = [];
                            message.joints.push($root.lebai.motion.JointMove.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 11: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.duration = value;
                            else
                                delete message.duration;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a MovePvatRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.MovePvatRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.MovePvatRequest & lebai.motion.MovePvatRequest.$Shape} MovePvatRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MovePvatRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MovePvatRequest message.
             * @function verify
             * @memberof lebai.motion.MovePvatRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MovePvatRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.joints != null && $Object.hasOwnProperty.call(message, "joints")) {
                    if (!$Array.isArray(message.joints))
                        return "joints: array expected";
                    for (var i = 0; i < message.joints.length; ++i) {
                        var error = $root.lebai.motion.JointMove.verify(message.joints[i], _depth + 1);
                        if (error)
                            return "joints." + error;
                    }
                }
                if (message.duration != null && $Object.hasOwnProperty.call(message, "duration"))
                    if (typeof message.duration !== "number")
                        return "duration: number expected";
                return null;
            };

            /**
             * Creates a MovePvatRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.MovePvatRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.MovePvatRequest} MovePvatRequest
             */
            MovePvatRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.MovePvatRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.MovePvatRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.MovePvatRequest();
                if (object.joints) {
                    if (!$Array.isArray(object.joints))
                        throw $TypeError(".lebai.motion.MovePvatRequest.joints: array expected");
                    message.joints = $Array(object.joints.length);
                    for (var i = 0; i < object.joints.length; ++i) {
                        if (!$util.isObject(object.joints[i]))
                            throw $TypeError(".lebai.motion.MovePvatRequest.joints: object expected");
                        message.joints[i] = $root.lebai.motion.JointMove.fromObject(object.joints[i], _depth + 1);
                    }
                }
                if (object.duration != null)
                    if (!$Object.is($Number(object.duration), 0))
                        message.duration = $Number(object.duration);
                return message;
            };

            /**
             * Creates a plain object from a MovePvatRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.MovePvatRequest
             * @static
             * @param {lebai.motion.MovePvatRequest} message MovePvatRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MovePvatRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.joints = [];
                if (options.defaults)
                    object.duration = 0;
                if (message.joints && message.joints.length) {
                    object.joints = $Array(message.joints.length);
                    for (var j = 0; j < message.joints.length; ++j)
                        object.joints[j] = $root.lebai.motion.JointMove.toObject(message.joints[j], options, _depth + 1);
                }
                if (message.duration != null && $Object.hasOwnProperty.call(message, "duration"))
                    object.duration = options.json && !$isFinite(message.duration) ? $String(message.duration) : message.duration;
                return object;
            };

            /**
             * Converts this MovePvatRequest to JSON.
             * @function toJSON
             * @memberof lebai.motion.MovePvatRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MovePvatRequest.prototype.toJSON = function() {
                return MovePvatRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for MovePvatRequest
             * @function getTypeUrl
             * @memberof lebai.motion.MovePvatRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            MovePvatRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.MovePvatRequest";
            };

            return MovePvatRequest;
        })();

        motion.Trajectory = (function() {

            /**
             * Properties of a Trajectory.
             * @typedef {Object} lebai.motion.Trajectory.$Properties
             * @property {lebai.motion.PvatKind|null} [kind] Trajectory kind
             * @property {Array.<lebai.motion.MovePvatRequest.$Properties>|null} [data] Trajectory data
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Trajectory.
             * @memberof lebai.motion
             * @interface ITrajectory
             * @augments lebai.motion.Trajectory.$Properties
             * @deprecated Use lebai.motion.Trajectory.$Properties instead.
             */

            /**
             * Shape of a Trajectory.
             * @typedef {lebai.motion.Trajectory.$Properties} lebai.motion.Trajectory.$Shape
             */

            /**
             * Constructs a new Trajectory.
             * @memberof lebai.motion
             * @classdesc Represents a Trajectory.
             * @constructor
             * @param {lebai.motion.Trajectory.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Trajectory = function (properties) {
                this.data = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Trajectory kind.
             * @member {lebai.motion.PvatKind} kind
             * @memberof lebai.motion.Trajectory
             * @instance
             */
            Trajectory.prototype.kind = 0;

            /**
             * Trajectory data.
             * @member {Array.<lebai.motion.MovePvatRequest.$Properties>} data
             * @memberof lebai.motion.Trajectory
             * @instance
             */
            Trajectory.prototype.data = $util.emptyArray;

            /**
             * Creates a new Trajectory instance using the specified properties.
             * @function create
             * @memberof lebai.motion.Trajectory
             * @static
             * @param {lebai.motion.Trajectory.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.Trajectory} Trajectory instance
             * @type {{
             *   (properties: lebai.motion.Trajectory.$Shape): lebai.motion.Trajectory & lebai.motion.Trajectory.$Shape;
             *   (properties?: lebai.motion.Trajectory.$Properties): lebai.motion.Trajectory;
             * }}
             */
            Trajectory.create = function(properties) {
                return new Trajectory(properties);
            };

            /**
             * Encodes the specified Trajectory message. Does not implicitly {@link lebai.motion.Trajectory.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.Trajectory
             * @static
             * @param {lebai.motion.Trajectory.$Properties} message Trajectory message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Trajectory.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind") && message.kind !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.kind);
                if (message.data != null && message.data.length)
                    for (var i = 0; i < message.data.length; ++i)
                        $root.lebai.motion.MovePvatRequest.encode(message.data[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Trajectory message, length delimited. Does not implicitly {@link lebai.motion.Trajectory.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.Trajectory
             * @static
             * @param {lebai.motion.Trajectory.$Properties} message Trajectory message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Trajectory.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Trajectory message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.Trajectory
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.Trajectory & lebai.motion.Trajectory.$Shape} Trajectory
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Trajectory.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.Trajectory();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.kind = value;
                            else
                                delete message.kind;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if (!(message.data && message.data.length))
                                message.data = [];
                            message.data.push($root.lebai.motion.MovePvatRequest.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Trajectory message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.Trajectory
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.Trajectory & lebai.motion.Trajectory.$Shape} Trajectory
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Trajectory.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Trajectory message.
             * @function verify
             * @memberof lebai.motion.Trajectory
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Trajectory.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    if (typeof message.kind !== "number" || (message.kind | 0) !== message.kind)
                        return "kind: enum value expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    if (!$Array.isArray(message.data))
                        return "data: array expected";
                    for (var i = 0; i < message.data.length; ++i) {
                        var error = $root.lebai.motion.MovePvatRequest.verify(message.data[i], _depth + 1);
                        if (error)
                            return "data." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Trajectory message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.Trajectory
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.Trajectory} Trajectory
             */
            Trajectory.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.Trajectory)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.Trajectory: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.Trajectory();
                if (object.kind !== 0 && (typeof object.kind !== "string" || $root.lebai.motion.PvatKind[object.kind] !== 0))
                    switch (object.kind) {
                    case "PT":
                    case 0:
                        message.kind = 0;
                        break;
                    case "PVT":
                    case 1:
                        message.kind = 1;
                        break;
                    case "PVAT":
                    case 2:
                        message.kind = 2;
                        break;
                    default:
                        if (typeof object.kind === "number" && (object.kind | 0) === object.kind)
                            message.kind = object.kind;
                    }
                if (object.data) {
                    if (!$Array.isArray(object.data))
                        throw $TypeError(".lebai.motion.Trajectory.data: array expected");
                    message.data = $Array(object.data.length);
                    for (var i = 0; i < object.data.length; ++i) {
                        if (!$util.isObject(object.data[i]))
                            throw $TypeError(".lebai.motion.Trajectory.data: object expected");
                        message.data[i] = $root.lebai.motion.MovePvatRequest.fromObject(object.data[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Trajectory message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.Trajectory
             * @static
             * @param {lebai.motion.Trajectory} message Trajectory
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Trajectory.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.data = [];
                if (options.defaults)
                    object.kind = options.enums === $String ? "PT" : 0;
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    object.kind = options.enums === $String ? $root.lebai.motion.PvatKind[message.kind] === $undefined ? message.kind : $root.lebai.motion.PvatKind[message.kind] : message.kind;
                if (message.data && message.data.length) {
                    object.data = $Array(message.data.length);
                    for (var j = 0; j < message.data.length; ++j)
                        object.data[j] = $root.lebai.motion.MovePvatRequest.toObject(message.data[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this Trajectory to JSON.
             * @function toJSON
             * @memberof lebai.motion.Trajectory
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Trajectory.prototype.toJSON = function() {
                return Trajectory.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Trajectory
             * @function getTypeUrl
             * @memberof lebai.motion.Trajectory
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Trajectory.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.Trajectory";
            };

            return Trajectory;
        })();

        motion.SaveTrajectoryRequest = (function() {

            /**
             * Properties of a SaveTrajectoryRequest.
             * @typedef {Object} lebai.motion.SaveTrajectoryRequest.$Properties
             * @property {string|null} [name] SaveTrajectoryRequest name
             * @property {lebai.motion.Trajectory.$Properties|null} [data] SaveTrajectoryRequest data
             * @property {string|null} [dir] SaveTrajectoryRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SaveTrajectoryRequest.
             * @memberof lebai.motion
             * @interface ISaveTrajectoryRequest
             * @augments lebai.motion.SaveTrajectoryRequest.$Properties
             * @deprecated Use lebai.motion.SaveTrajectoryRequest.$Properties instead.
             */

            /**
             * Shape of a SaveTrajectoryRequest.
             * @typedef {lebai.motion.SaveTrajectoryRequest.$Properties} lebai.motion.SaveTrajectoryRequest.$Shape
             */

            /**
             * Constructs a new SaveTrajectoryRequest.
             * @memberof lebai.motion
             * @classdesc Represents a SaveTrajectoryRequest.
             * @constructor
             * @param {lebai.motion.SaveTrajectoryRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SaveTrajectoryRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SaveTrajectoryRequest name.
             * @member {string} name
             * @memberof lebai.motion.SaveTrajectoryRequest
             * @instance
             */
            SaveTrajectoryRequest.prototype.name = "";

            /**
             * SaveTrajectoryRequest data.
             * @member {lebai.motion.Trajectory.$Properties|null|undefined} data
             * @memberof lebai.motion.SaveTrajectoryRequest
             * @instance
             */
            SaveTrajectoryRequest.prototype.data = null;

            /**
             * SaveTrajectoryRequest dir.
             * @member {string} dir
             * @memberof lebai.motion.SaveTrajectoryRequest
             * @instance
             */
            SaveTrajectoryRequest.prototype.dir = "";

            /**
             * Creates a new SaveTrajectoryRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motion.SaveTrajectoryRequest
             * @static
             * @param {lebai.motion.SaveTrajectoryRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.SaveTrajectoryRequest} SaveTrajectoryRequest instance
             * @type {{
             *   (properties: lebai.motion.SaveTrajectoryRequest.$Shape): lebai.motion.SaveTrajectoryRequest & lebai.motion.SaveTrajectoryRequest.$Shape;
             *   (properties?: lebai.motion.SaveTrajectoryRequest.$Properties): lebai.motion.SaveTrajectoryRequest;
             * }}
             */
            SaveTrajectoryRequest.create = function(properties) {
                return new SaveTrajectoryRequest(properties);
            };

            /**
             * Encodes the specified SaveTrajectoryRequest message. Does not implicitly {@link lebai.motion.SaveTrajectoryRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.SaveTrajectoryRequest
             * @static
             * @param {lebai.motion.SaveTrajectoryRequest.$Properties} message SaveTrajectoryRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveTrajectoryRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    $root.lebai.motion.Trajectory.encode(message.data, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SaveTrajectoryRequest message, length delimited. Does not implicitly {@link lebai.motion.SaveTrajectoryRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.SaveTrajectoryRequest
             * @static
             * @param {lebai.motion.SaveTrajectoryRequest.$Properties} message SaveTrajectoryRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveTrajectoryRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SaveTrajectoryRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.SaveTrajectoryRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.SaveTrajectoryRequest & lebai.motion.SaveTrajectoryRequest.$Shape} SaveTrajectoryRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveTrajectoryRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.SaveTrajectoryRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.data = $root.lebai.motion.Trajectory.decode(reader, reader.uint32(), $undefined, _depth + 1, message.data);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SaveTrajectoryRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.SaveTrajectoryRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.SaveTrajectoryRequest & lebai.motion.SaveTrajectoryRequest.$Shape} SaveTrajectoryRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveTrajectoryRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SaveTrajectoryRequest message.
             * @function verify
             * @memberof lebai.motion.SaveTrajectoryRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SaveTrajectoryRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    var error = $root.lebai.motion.Trajectory.verify(message.data, _depth + 1);
                    if (error)
                        return "data." + error;
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates a SaveTrajectoryRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.SaveTrajectoryRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.SaveTrajectoryRequest} SaveTrajectoryRequest
             */
            SaveTrajectoryRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.SaveTrajectoryRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.SaveTrajectoryRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.SaveTrajectoryRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.data != null) {
                    if (!$util.isObject(object.data))
                        throw $TypeError(".lebai.motion.SaveTrajectoryRequest.data: object expected");
                    message.data = $root.lebai.motion.Trajectory.fromObject(object.data, _depth + 1);
                }
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from a SaveTrajectoryRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.SaveTrajectoryRequest
             * @static
             * @param {lebai.motion.SaveTrajectoryRequest} message SaveTrajectoryRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SaveTrajectoryRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.data = null;
                    object.dir = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = $root.lebai.motion.Trajectory.toObject(message.data, options, _depth + 1);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                return object;
            };

            /**
             * Converts this SaveTrajectoryRequest to JSON.
             * @function toJSON
             * @memberof lebai.motion.SaveTrajectoryRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SaveTrajectoryRequest.prototype.toJSON = function() {
                return SaveTrajectoryRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SaveTrajectoryRequest
             * @function getTypeUrl
             * @memberof lebai.motion.SaveTrajectoryRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SaveTrajectoryRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.SaveTrajectoryRequest";
            };

            return SaveTrajectoryRequest;
        })();

        motion.StartRecordTrajectoryRequest = (function() {

            /**
             * Properties of a StartRecordTrajectoryRequest.
             * @typedef {Object} lebai.motion.StartRecordTrajectoryRequest.$Properties
             * @property {lebai.motion.PvatKind|null} [kind] StartRecordTrajectoryRequest kind
             * @property {number|null} [duration] StartRecordTrajectoryRequest duration
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StartRecordTrajectoryRequest.
             * @memberof lebai.motion
             * @interface IStartRecordTrajectoryRequest
             * @augments lebai.motion.StartRecordTrajectoryRequest.$Properties
             * @deprecated Use lebai.motion.StartRecordTrajectoryRequest.$Properties instead.
             */

            /**
             * Shape of a StartRecordTrajectoryRequest.
             * @typedef {lebai.motion.StartRecordTrajectoryRequest.$Properties} lebai.motion.StartRecordTrajectoryRequest.$Shape
             */

            /**
             * Constructs a new StartRecordTrajectoryRequest.
             * @memberof lebai.motion
             * @classdesc Represents a StartRecordTrajectoryRequest.
             * @constructor
             * @param {lebai.motion.StartRecordTrajectoryRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var StartRecordTrajectoryRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StartRecordTrajectoryRequest kind.
             * @member {lebai.motion.PvatKind} kind
             * @memberof lebai.motion.StartRecordTrajectoryRequest
             * @instance
             */
            StartRecordTrajectoryRequest.prototype.kind = 0;

            /**
             * StartRecordTrajectoryRequest duration.
             * @member {number} duration
             * @memberof lebai.motion.StartRecordTrajectoryRequest
             * @instance
             */
            StartRecordTrajectoryRequest.prototype.duration = 0;

            /**
             * Creates a new StartRecordTrajectoryRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motion.StartRecordTrajectoryRequest
             * @static
             * @param {lebai.motion.StartRecordTrajectoryRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.StartRecordTrajectoryRequest} StartRecordTrajectoryRequest instance
             * @type {{
             *   (properties: lebai.motion.StartRecordTrajectoryRequest.$Shape): lebai.motion.StartRecordTrajectoryRequest & lebai.motion.StartRecordTrajectoryRequest.$Shape;
             *   (properties?: lebai.motion.StartRecordTrajectoryRequest.$Properties): lebai.motion.StartRecordTrajectoryRequest;
             * }}
             */
            StartRecordTrajectoryRequest.create = function(properties) {
                return new StartRecordTrajectoryRequest(properties);
            };

            /**
             * Encodes the specified StartRecordTrajectoryRequest message. Does not implicitly {@link lebai.motion.StartRecordTrajectoryRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.StartRecordTrajectoryRequest
             * @static
             * @param {lebai.motion.StartRecordTrajectoryRequest.$Properties} message StartRecordTrajectoryRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StartRecordTrajectoryRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind") && message.kind !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.kind);
                if (message.duration != null && $Object.hasOwnProperty.call(message, "duration") && !$Object.is(message.duration, 0))
                    writer.uint32(/* id 11, wireType 1 =*/89).double(message.duration);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StartRecordTrajectoryRequest message, length delimited. Does not implicitly {@link lebai.motion.StartRecordTrajectoryRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.StartRecordTrajectoryRequest
             * @static
             * @param {lebai.motion.StartRecordTrajectoryRequest.$Properties} message StartRecordTrajectoryRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StartRecordTrajectoryRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a StartRecordTrajectoryRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.StartRecordTrajectoryRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.StartRecordTrajectoryRequest & lebai.motion.StartRecordTrajectoryRequest.$Shape} StartRecordTrajectoryRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StartRecordTrajectoryRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.StartRecordTrajectoryRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.kind = value;
                            else
                                delete message.kind;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.duration = value;
                            else
                                delete message.duration;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a StartRecordTrajectoryRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.StartRecordTrajectoryRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.StartRecordTrajectoryRequest & lebai.motion.StartRecordTrajectoryRequest.$Shape} StartRecordTrajectoryRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StartRecordTrajectoryRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StartRecordTrajectoryRequest message.
             * @function verify
             * @memberof lebai.motion.StartRecordTrajectoryRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StartRecordTrajectoryRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    if (typeof message.kind !== "number" || (message.kind | 0) !== message.kind)
                        return "kind: enum value expected";
                if (message.duration != null && $Object.hasOwnProperty.call(message, "duration"))
                    if (typeof message.duration !== "number")
                        return "duration: number expected";
                return null;
            };

            /**
             * Creates a StartRecordTrajectoryRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.StartRecordTrajectoryRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.StartRecordTrajectoryRequest} StartRecordTrajectoryRequest
             */
            StartRecordTrajectoryRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.StartRecordTrajectoryRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.StartRecordTrajectoryRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.StartRecordTrajectoryRequest();
                if (object.kind !== 0 && (typeof object.kind !== "string" || $root.lebai.motion.PvatKind[object.kind] !== 0))
                    switch (object.kind) {
                    case "PT":
                    case 0:
                        message.kind = 0;
                        break;
                    case "PVT":
                    case 1:
                        message.kind = 1;
                        break;
                    case "PVAT":
                    case 2:
                        message.kind = 2;
                        break;
                    default:
                        if (typeof object.kind === "number" && (object.kind | 0) === object.kind)
                            message.kind = object.kind;
                    }
                if (object.duration != null)
                    if (!$Object.is($Number(object.duration), 0))
                        message.duration = $Number(object.duration);
                return message;
            };

            /**
             * Creates a plain object from a StartRecordTrajectoryRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.StartRecordTrajectoryRequest
             * @static
             * @param {lebai.motion.StartRecordTrajectoryRequest} message StartRecordTrajectoryRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StartRecordTrajectoryRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.kind = options.enums === $String ? "PT" : 0;
                    object.duration = 0;
                }
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    object.kind = options.enums === $String ? $root.lebai.motion.PvatKind[message.kind] === $undefined ? message.kind : $root.lebai.motion.PvatKind[message.kind] : message.kind;
                if (message.duration != null && $Object.hasOwnProperty.call(message, "duration"))
                    object.duration = options.json && !$isFinite(message.duration) ? $String(message.duration) : message.duration;
                return object;
            };

            /**
             * Converts this StartRecordTrajectoryRequest to JSON.
             * @function toJSON
             * @memberof lebai.motion.StartRecordTrajectoryRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StartRecordTrajectoryRequest.prototype.toJSON = function() {
                return StartRecordTrajectoryRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StartRecordTrajectoryRequest
             * @function getTypeUrl
             * @memberof lebai.motion.StartRecordTrajectoryRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StartRecordTrajectoryRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.StartRecordTrajectoryRequest";
            };

            return StartRecordTrajectoryRequest;
        })();

        motion.EndRecordTrajectoryRequest = (function() {

            /**
             * Properties of an EndRecordTrajectoryRequest.
             * @typedef {Object} lebai.motion.EndRecordTrajectoryRequest.$Properties
             * @property {string|null} [name] EndRecordTrajectoryRequest name
             * @property {string|null} [dir] EndRecordTrajectoryRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an EndRecordTrajectoryRequest.
             * @memberof lebai.motion
             * @interface IEndRecordTrajectoryRequest
             * @augments lebai.motion.EndRecordTrajectoryRequest.$Properties
             * @deprecated Use lebai.motion.EndRecordTrajectoryRequest.$Properties instead.
             */

            /**
             * Shape of an EndRecordTrajectoryRequest.
             * @typedef {lebai.motion.EndRecordTrajectoryRequest.$Properties} lebai.motion.EndRecordTrajectoryRequest.$Shape
             */

            /**
             * Constructs a new EndRecordTrajectoryRequest.
             * @memberof lebai.motion
             * @classdesc Represents an EndRecordTrajectoryRequest.
             * @constructor
             * @param {lebai.motion.EndRecordTrajectoryRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var EndRecordTrajectoryRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * EndRecordTrajectoryRequest name.
             * @member {string} name
             * @memberof lebai.motion.EndRecordTrajectoryRequest
             * @instance
             */
            EndRecordTrajectoryRequest.prototype.name = "";

            /**
             * EndRecordTrajectoryRequest dir.
             * @member {string} dir
             * @memberof lebai.motion.EndRecordTrajectoryRequest
             * @instance
             */
            EndRecordTrajectoryRequest.prototype.dir = "";

            /**
             * Creates a new EndRecordTrajectoryRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motion.EndRecordTrajectoryRequest
             * @static
             * @param {lebai.motion.EndRecordTrajectoryRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.EndRecordTrajectoryRequest} EndRecordTrajectoryRequest instance
             * @type {{
             *   (properties: lebai.motion.EndRecordTrajectoryRequest.$Shape): lebai.motion.EndRecordTrajectoryRequest & lebai.motion.EndRecordTrajectoryRequest.$Shape;
             *   (properties?: lebai.motion.EndRecordTrajectoryRequest.$Properties): lebai.motion.EndRecordTrajectoryRequest;
             * }}
             */
            EndRecordTrajectoryRequest.create = function(properties) {
                return new EndRecordTrajectoryRequest(properties);
            };

            /**
             * Encodes the specified EndRecordTrajectoryRequest message. Does not implicitly {@link lebai.motion.EndRecordTrajectoryRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.EndRecordTrajectoryRequest
             * @static
             * @param {lebai.motion.EndRecordTrajectoryRequest.$Properties} message EndRecordTrajectoryRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EndRecordTrajectoryRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EndRecordTrajectoryRequest message, length delimited. Does not implicitly {@link lebai.motion.EndRecordTrajectoryRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.EndRecordTrajectoryRequest
             * @static
             * @param {lebai.motion.EndRecordTrajectoryRequest.$Properties} message EndRecordTrajectoryRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EndRecordTrajectoryRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an EndRecordTrajectoryRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.EndRecordTrajectoryRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.EndRecordTrajectoryRequest & lebai.motion.EndRecordTrajectoryRequest.$Shape} EndRecordTrajectoryRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EndRecordTrajectoryRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.EndRecordTrajectoryRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an EndRecordTrajectoryRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.EndRecordTrajectoryRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.EndRecordTrajectoryRequest & lebai.motion.EndRecordTrajectoryRequest.$Shape} EndRecordTrajectoryRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EndRecordTrajectoryRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EndRecordTrajectoryRequest message.
             * @function verify
             * @memberof lebai.motion.EndRecordTrajectoryRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EndRecordTrajectoryRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates an EndRecordTrajectoryRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.EndRecordTrajectoryRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.EndRecordTrajectoryRequest} EndRecordTrajectoryRequest
             */
            EndRecordTrajectoryRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.EndRecordTrajectoryRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.EndRecordTrajectoryRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.EndRecordTrajectoryRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from an EndRecordTrajectoryRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.EndRecordTrajectoryRequest
             * @static
             * @param {lebai.motion.EndRecordTrajectoryRequest} message EndRecordTrajectoryRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EndRecordTrajectoryRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.dir = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                return object;
            };

            /**
             * Converts this EndRecordTrajectoryRequest to JSON.
             * @function toJSON
             * @memberof lebai.motion.EndRecordTrajectoryRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EndRecordTrajectoryRequest.prototype.toJSON = function() {
                return EndRecordTrajectoryRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for EndRecordTrajectoryRequest
             * @function getTypeUrl
             * @memberof lebai.motion.EndRecordTrajectoryRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            EndRecordTrajectoryRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.EndRecordTrajectoryRequest";
            };

            return EndRecordTrajectoryRequest;
        })();

        motion.MoveTrajectoryRequest = (function() {

            /**
             * Properties of a MoveTrajectoryRequest.
             * @typedef {Object} lebai.motion.MoveTrajectoryRequest.$Properties
             * @property {string|null} [name] MoveTrajectoryRequest name
             * @property {string|null} [dir] MoveTrajectoryRequest dir
             * @property {boolean|null} [back] MoveTrajectoryRequest back
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a MoveTrajectoryRequest.
             * @memberof lebai.motion
             * @interface IMoveTrajectoryRequest
             * @augments lebai.motion.MoveTrajectoryRequest.$Properties
             * @deprecated Use lebai.motion.MoveTrajectoryRequest.$Properties instead.
             */

            /**
             * Shape of a MoveTrajectoryRequest.
             * @typedef {lebai.motion.MoveTrajectoryRequest.$Properties} lebai.motion.MoveTrajectoryRequest.$Shape
             */

            /**
             * Constructs a new MoveTrajectoryRequest.
             * @memberof lebai.motion
             * @classdesc Represents a MoveTrajectoryRequest.
             * @constructor
             * @param {lebai.motion.MoveTrajectoryRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var MoveTrajectoryRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * MoveTrajectoryRequest name.
             * @member {string} name
             * @memberof lebai.motion.MoveTrajectoryRequest
             * @instance
             */
            MoveTrajectoryRequest.prototype.name = "";

            /**
             * MoveTrajectoryRequest dir.
             * @member {string} dir
             * @memberof lebai.motion.MoveTrajectoryRequest
             * @instance
             */
            MoveTrajectoryRequest.prototype.dir = "";

            /**
             * MoveTrajectoryRequest back.
             * @member {boolean} back
             * @memberof lebai.motion.MoveTrajectoryRequest
             * @instance
             */
            MoveTrajectoryRequest.prototype.back = false;

            /**
             * Creates a new MoveTrajectoryRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motion.MoveTrajectoryRequest
             * @static
             * @param {lebai.motion.MoveTrajectoryRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.MoveTrajectoryRequest} MoveTrajectoryRequest instance
             * @type {{
             *   (properties: lebai.motion.MoveTrajectoryRequest.$Shape): lebai.motion.MoveTrajectoryRequest & lebai.motion.MoveTrajectoryRequest.$Shape;
             *   (properties?: lebai.motion.MoveTrajectoryRequest.$Properties): lebai.motion.MoveTrajectoryRequest;
             * }}
             */
            MoveTrajectoryRequest.create = function(properties) {
                return new MoveTrajectoryRequest(properties);
            };

            /**
             * Encodes the specified MoveTrajectoryRequest message. Does not implicitly {@link lebai.motion.MoveTrajectoryRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.MoveTrajectoryRequest
             * @static
             * @param {lebai.motion.MoveTrajectoryRequest.$Properties} message MoveTrajectoryRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MoveTrajectoryRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.back != null && $Object.hasOwnProperty.call(message, "back") && message.back !== false)
                    writer.uint32(/* id 21, wireType 0 =*/168).bool(message.back);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MoveTrajectoryRequest message, length delimited. Does not implicitly {@link lebai.motion.MoveTrajectoryRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.MoveTrajectoryRequest
             * @static
             * @param {lebai.motion.MoveTrajectoryRequest.$Properties} message MoveTrajectoryRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MoveTrajectoryRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a MoveTrajectoryRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.MoveTrajectoryRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.MoveTrajectoryRequest & lebai.motion.MoveTrajectoryRequest.$Shape} MoveTrajectoryRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MoveTrajectoryRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.MoveTrajectoryRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    case 21: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.back = value;
                            else
                                delete message.back;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a MoveTrajectoryRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.MoveTrajectoryRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.MoveTrajectoryRequest & lebai.motion.MoveTrajectoryRequest.$Shape} MoveTrajectoryRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MoveTrajectoryRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MoveTrajectoryRequest message.
             * @function verify
             * @memberof lebai.motion.MoveTrajectoryRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MoveTrajectoryRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                if (message.back != null && $Object.hasOwnProperty.call(message, "back"))
                    if (typeof message.back !== "boolean")
                        return "back: boolean expected";
                return null;
            };

            /**
             * Creates a MoveTrajectoryRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.MoveTrajectoryRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.MoveTrajectoryRequest} MoveTrajectoryRequest
             */
            MoveTrajectoryRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.MoveTrajectoryRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.MoveTrajectoryRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.MoveTrajectoryRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                if (object.back != null)
                    if (object.back)
                        message.back = $Boolean(object.back);
                return message;
            };

            /**
             * Creates a plain object from a MoveTrajectoryRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.MoveTrajectoryRequest
             * @static
             * @param {lebai.motion.MoveTrajectoryRequest} message MoveTrajectoryRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MoveTrajectoryRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.dir = "";
                    object.back = false;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                if (message.back != null && $Object.hasOwnProperty.call(message, "back"))
                    object.back = message.back;
                return object;
            };

            /**
             * Converts this MoveTrajectoryRequest to JSON.
             * @function toJSON
             * @memberof lebai.motion.MoveTrajectoryRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MoveTrajectoryRequest.prototype.toJSON = function() {
                return MoveTrajectoryRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for MoveTrajectoryRequest
             * @function getTypeUrl
             * @memberof lebai.motion.MoveTrajectoryRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            MoveTrajectoryRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.MoveTrajectoryRequest";
            };

            return MoveTrajectoryRequest;
        })();

        motion.Wrench = (function() {

            /**
             * Properties of a Wrench.
             * @typedef {Object} lebai.motion.Wrench.$Properties
             * @property {lebai.posture.Position.$Properties|null} [force] Wrench force
             * @property {lebai.posture.Position.$Properties|null} [torque] Wrench torque
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Wrench.
             * @memberof lebai.motion
             * @interface IWrench
             * @augments lebai.motion.Wrench.$Properties
             * @deprecated Use lebai.motion.Wrench.$Properties instead.
             */

            /**
             * Shape of a Wrench.
             * @typedef {lebai.motion.Wrench.$Properties} lebai.motion.Wrench.$Shape
             */

            /**
             * Constructs a new Wrench.
             * @memberof lebai.motion
             * @classdesc Represents a Wrench.
             * @constructor
             * @param {lebai.motion.Wrench.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Wrench = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Wrench force.
             * @member {lebai.posture.Position.$Properties|null|undefined} force
             * @memberof lebai.motion.Wrench
             * @instance
             */
            Wrench.prototype.force = null;

            /**
             * Wrench torque.
             * @member {lebai.posture.Position.$Properties|null|undefined} torque
             * @memberof lebai.motion.Wrench
             * @instance
             */
            Wrench.prototype.torque = null;

            /**
             * Creates a new Wrench instance using the specified properties.
             * @function create
             * @memberof lebai.motion.Wrench
             * @static
             * @param {lebai.motion.Wrench.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.Wrench} Wrench instance
             * @type {{
             *   (properties: lebai.motion.Wrench.$Shape): lebai.motion.Wrench & lebai.motion.Wrench.$Shape;
             *   (properties?: lebai.motion.Wrench.$Properties): lebai.motion.Wrench;
             * }}
             */
            Wrench.create = function(properties) {
                return new Wrench(properties);
            };

            /**
             * Encodes the specified Wrench message. Does not implicitly {@link lebai.motion.Wrench.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.Wrench
             * @static
             * @param {lebai.motion.Wrench.$Properties} message Wrench message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Wrench.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                    $root.lebai.posture.Position.encode(message.force, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.torque != null && $Object.hasOwnProperty.call(message, "torque"))
                    $root.lebai.posture.Position.encode(message.torque, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Wrench message, length delimited. Does not implicitly {@link lebai.motion.Wrench.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.Wrench
             * @static
             * @param {lebai.motion.Wrench.$Properties} message Wrench message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Wrench.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Wrench message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.Wrench
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.Wrench & lebai.motion.Wrench.$Shape} Wrench
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Wrench.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.Wrench();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.force = $root.lebai.posture.Position.decode(reader, reader.uint32(), $undefined, _depth + 1, message.force);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.torque = $root.lebai.posture.Position.decode(reader, reader.uint32(), $undefined, _depth + 1, message.torque);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Wrench message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.Wrench
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.Wrench & lebai.motion.Wrench.$Shape} Wrench
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Wrench.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Wrench message.
             * @function verify
             * @memberof lebai.motion.Wrench
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Wrench.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.force != null && $Object.hasOwnProperty.call(message, "force")) {
                    var error = $root.lebai.posture.Position.verify(message.force, _depth + 1);
                    if (error)
                        return "force." + error;
                }
                if (message.torque != null && $Object.hasOwnProperty.call(message, "torque")) {
                    var error = $root.lebai.posture.Position.verify(message.torque, _depth + 1);
                    if (error)
                        return "torque." + error;
                }
                return null;
            };

            /**
             * Creates a Wrench message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.Wrench
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.Wrench} Wrench
             */
            Wrench.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.Wrench)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.Wrench: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.Wrench();
                if (object.force != null) {
                    if (!$util.isObject(object.force))
                        throw $TypeError(".lebai.motion.Wrench.force: object expected");
                    message.force = $root.lebai.posture.Position.fromObject(object.force, _depth + 1);
                }
                if (object.torque != null) {
                    if (!$util.isObject(object.torque))
                        throw $TypeError(".lebai.motion.Wrench.torque: object expected");
                    message.torque = $root.lebai.posture.Position.fromObject(object.torque, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Wrench message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.Wrench
             * @static
             * @param {lebai.motion.Wrench} message Wrench
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Wrench.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.force = null;
                    object.torque = null;
                }
                if (message.force != null && $Object.hasOwnProperty.call(message, "force"))
                    object.force = $root.lebai.posture.Position.toObject(message.force, options, _depth + 1);
                if (message.torque != null && $Object.hasOwnProperty.call(message, "torque"))
                    object.torque = $root.lebai.posture.Position.toObject(message.torque, options, _depth + 1);
                return object;
            };

            /**
             * Converts this Wrench to JSON.
             * @function toJSON
             * @memberof lebai.motion.Wrench
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Wrench.prototype.toJSON = function() {
                return Wrench.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Wrench
             * @function getTypeUrl
             * @memberof lebai.motion.Wrench
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Wrench.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.Wrench";
            };

            return Wrench;
        })();

        /**
         * ForceSensor enum.
         * @name lebai.motion.ForceSensor
         * @enum {number}
         * @property {number} DISABLE=0 DISABLE value
         * @property {number} INSTANTANEOUS=1 INSTANTANEOUS value
         * @property {number} CONTINUOUS=2 CONTINUOUS value
         * @property {number} WEIGHT1=11 WEIGHT1 value
         * @property {number} TORQUE6=21 TORQUE6 value
         */
        motion.ForceSensor = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "DISABLE"] = 0;
            values[valuesById[1] = "INSTANTANEOUS"] = 1;
            values[valuesById[2] = "CONTINUOUS"] = 2;
            values[valuesById[11] = "WEIGHT1"] = 11;
            values[valuesById[21] = "TORQUE6"] = 21;
            return values;
        })();

        motion.SetForceSensorRequest = (function() {

            /**
             * Properties of a SetForceSensorRequest.
             * @typedef {Object} lebai.motion.SetForceSensorRequest.$Properties
             * @property {lebai.motion.ForceSensor|null} [sensor] SetForceSensorRequest sensor
             * @property {number|null} [address] SetForceSensorRequest address
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetForceSensorRequest.
             * @memberof lebai.motion
             * @interface ISetForceSensorRequest
             * @augments lebai.motion.SetForceSensorRequest.$Properties
             * @deprecated Use lebai.motion.SetForceSensorRequest.$Properties instead.
             */

            /**
             * Shape of a SetForceSensorRequest.
             * @typedef {lebai.motion.SetForceSensorRequest.$Properties} lebai.motion.SetForceSensorRequest.$Shape
             */

            /**
             * Constructs a new SetForceSensorRequest.
             * @memberof lebai.motion
             * @classdesc Represents a SetForceSensorRequest.
             * @constructor
             * @param {lebai.motion.SetForceSensorRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetForceSensorRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetForceSensorRequest sensor.
             * @member {lebai.motion.ForceSensor} sensor
             * @memberof lebai.motion.SetForceSensorRequest
             * @instance
             */
            SetForceSensorRequest.prototype.sensor = 0;

            /**
             * SetForceSensorRequest address.
             * @member {number} address
             * @memberof lebai.motion.SetForceSensorRequest
             * @instance
             */
            SetForceSensorRequest.prototype.address = 0;

            /**
             * Creates a new SetForceSensorRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motion.SetForceSensorRequest
             * @static
             * @param {lebai.motion.SetForceSensorRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.SetForceSensorRequest} SetForceSensorRequest instance
             * @type {{
             *   (properties: lebai.motion.SetForceSensorRequest.$Shape): lebai.motion.SetForceSensorRequest & lebai.motion.SetForceSensorRequest.$Shape;
             *   (properties?: lebai.motion.SetForceSensorRequest.$Properties): lebai.motion.SetForceSensorRequest;
             * }}
             */
            SetForceSensorRequest.create = function(properties) {
                return new SetForceSensorRequest(properties);
            };

            /**
             * Encodes the specified SetForceSensorRequest message. Does not implicitly {@link lebai.motion.SetForceSensorRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.SetForceSensorRequest
             * @static
             * @param {lebai.motion.SetForceSensorRequest.$Properties} message SetForceSensorRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetForceSensorRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.sensor != null && $Object.hasOwnProperty.call(message, "sensor") && message.sensor !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.sensor);
                if (message.address != null && $Object.hasOwnProperty.call(message, "address") && message.address !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.address);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetForceSensorRequest message, length delimited. Does not implicitly {@link lebai.motion.SetForceSensorRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.SetForceSensorRequest
             * @static
             * @param {lebai.motion.SetForceSensorRequest.$Properties} message SetForceSensorRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetForceSensorRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetForceSensorRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.SetForceSensorRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.SetForceSensorRequest & lebai.motion.SetForceSensorRequest.$Shape} SetForceSensorRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetForceSensorRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.SetForceSensorRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.sensor = value;
                            else
                                delete message.sensor;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.address = value;
                            else
                                delete message.address;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetForceSensorRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.SetForceSensorRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.SetForceSensorRequest & lebai.motion.SetForceSensorRequest.$Shape} SetForceSensorRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetForceSensorRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetForceSensorRequest message.
             * @function verify
             * @memberof lebai.motion.SetForceSensorRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetForceSensorRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.sensor != null && $Object.hasOwnProperty.call(message, "sensor"))
                    if (typeof message.sensor !== "number" || (message.sensor | 0) !== message.sensor)
                        return "sensor: enum value expected";
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    if (!$util.isInteger(message.address))
                        return "address: integer expected";
                return null;
            };

            /**
             * Creates a SetForceSensorRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.SetForceSensorRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.SetForceSensorRequest} SetForceSensorRequest
             */
            SetForceSensorRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.SetForceSensorRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.SetForceSensorRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.SetForceSensorRequest();
                if (object.sensor !== 0 && (typeof object.sensor !== "string" || $root.lebai.motion.ForceSensor[object.sensor] !== 0))
                    switch (object.sensor) {
                    case "DISABLE":
                    case 0:
                        message.sensor = 0;
                        break;
                    case "INSTANTANEOUS":
                    case 1:
                        message.sensor = 1;
                        break;
                    case "CONTINUOUS":
                    case 2:
                        message.sensor = 2;
                        break;
                    case "WEIGHT1":
                    case 11:
                        message.sensor = 11;
                        break;
                    case "TORQUE6":
                    case 21:
                        message.sensor = 21;
                        break;
                    default:
                        if (typeof object.sensor === "number" && (object.sensor | 0) === object.sensor)
                            message.sensor = object.sensor;
                    }
                if (object.address != null)
                    if ($Number(object.address) !== 0)
                        message.address = object.address >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a SetForceSensorRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.SetForceSensorRequest
             * @static
             * @param {lebai.motion.SetForceSensorRequest} message SetForceSensorRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetForceSensorRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.sensor = options.enums === $String ? "DISABLE" : 0;
                    object.address = 0;
                }
                if (message.sensor != null && $Object.hasOwnProperty.call(message, "sensor"))
                    object.sensor = options.enums === $String ? $root.lebai.motion.ForceSensor[message.sensor] === $undefined ? message.sensor : $root.lebai.motion.ForceSensor[message.sensor] : message.sensor;
                if (message.address != null && $Object.hasOwnProperty.call(message, "address"))
                    object.address = message.address;
                return object;
            };

            /**
             * Converts this SetForceSensorRequest to JSON.
             * @function toJSON
             * @memberof lebai.motion.SetForceSensorRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetForceSensorRequest.prototype.toJSON = function() {
                return SetForceSensorRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetForceSensorRequest
             * @function getTypeUrl
             * @memberof lebai.motion.SetForceSensorRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetForceSensorRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.SetForceSensorRequest";
            };

            return SetForceSensorRequest;
        })();

        motion.StartForceModeRequest = (function() {

            /**
             * Properties of a StartForceModeRequest.
             * @typedef {Object} lebai.motion.StartForceModeRequest.$Properties
             * @property {lebai.posture.CartesianPose.$Properties|null} [limit] StartForceModeRequest limit
             * @property {lebai.motion.Wrench.$Properties|null} [wrench] StartForceModeRequest wrench
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StartForceModeRequest.
             * @memberof lebai.motion
             * @interface IStartForceModeRequest
             * @augments lebai.motion.StartForceModeRequest.$Properties
             * @deprecated Use lebai.motion.StartForceModeRequest.$Properties instead.
             */

            /**
             * Shape of a StartForceModeRequest.
             * @typedef {lebai.motion.StartForceModeRequest.$Properties} lebai.motion.StartForceModeRequest.$Shape
             */

            /**
             * Constructs a new StartForceModeRequest.
             * @memberof lebai.motion
             * @classdesc Represents a StartForceModeRequest.
             * @constructor
             * @param {lebai.motion.StartForceModeRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var StartForceModeRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StartForceModeRequest limit.
             * @member {lebai.posture.CartesianPose.$Properties|null|undefined} limit
             * @memberof lebai.motion.StartForceModeRequest
             * @instance
             */
            StartForceModeRequest.prototype.limit = null;

            /**
             * StartForceModeRequest wrench.
             * @member {lebai.motion.Wrench.$Properties|null|undefined} wrench
             * @memberof lebai.motion.StartForceModeRequest
             * @instance
             */
            StartForceModeRequest.prototype.wrench = null;

            /**
             * Creates a new StartForceModeRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motion.StartForceModeRequest
             * @static
             * @param {lebai.motion.StartForceModeRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.StartForceModeRequest} StartForceModeRequest instance
             * @type {{
             *   (properties: lebai.motion.StartForceModeRequest.$Shape): lebai.motion.StartForceModeRequest & lebai.motion.StartForceModeRequest.$Shape;
             *   (properties?: lebai.motion.StartForceModeRequest.$Properties): lebai.motion.StartForceModeRequest;
             * }}
             */
            StartForceModeRequest.create = function(properties) {
                return new StartForceModeRequest(properties);
            };

            /**
             * Encodes the specified StartForceModeRequest message. Does not implicitly {@link lebai.motion.StartForceModeRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.StartForceModeRequest
             * @static
             * @param {lebai.motion.StartForceModeRequest.$Properties} message StartForceModeRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StartForceModeRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.limit != null && $Object.hasOwnProperty.call(message, "limit"))
                    $root.lebai.posture.CartesianPose.encode(message.limit, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.wrench != null && $Object.hasOwnProperty.call(message, "wrench"))
                    $root.lebai.motion.Wrench.encode(message.wrench, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StartForceModeRequest message, length delimited. Does not implicitly {@link lebai.motion.StartForceModeRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.StartForceModeRequest
             * @static
             * @param {lebai.motion.StartForceModeRequest.$Properties} message StartForceModeRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StartForceModeRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a StartForceModeRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.StartForceModeRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.StartForceModeRequest & lebai.motion.StartForceModeRequest.$Shape} StartForceModeRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StartForceModeRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.StartForceModeRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.limit = $root.lebai.posture.CartesianPose.decode(reader, reader.uint32(), $undefined, _depth + 1, message.limit);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.wrench = $root.lebai.motion.Wrench.decode(reader, reader.uint32(), $undefined, _depth + 1, message.wrench);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a StartForceModeRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.StartForceModeRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.StartForceModeRequest & lebai.motion.StartForceModeRequest.$Shape} StartForceModeRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StartForceModeRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StartForceModeRequest message.
             * @function verify
             * @memberof lebai.motion.StartForceModeRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StartForceModeRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.limit != null && $Object.hasOwnProperty.call(message, "limit")) {
                    var error = $root.lebai.posture.CartesianPose.verify(message.limit, _depth + 1);
                    if (error)
                        return "limit." + error;
                }
                if (message.wrench != null && $Object.hasOwnProperty.call(message, "wrench")) {
                    var error = $root.lebai.motion.Wrench.verify(message.wrench, _depth + 1);
                    if (error)
                        return "wrench." + error;
                }
                return null;
            };

            /**
             * Creates a StartForceModeRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.StartForceModeRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.StartForceModeRequest} StartForceModeRequest
             */
            StartForceModeRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.StartForceModeRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.StartForceModeRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.StartForceModeRequest();
                if (object.limit != null) {
                    if (!$util.isObject(object.limit))
                        throw $TypeError(".lebai.motion.StartForceModeRequest.limit: object expected");
                    message.limit = $root.lebai.posture.CartesianPose.fromObject(object.limit, _depth + 1);
                }
                if (object.wrench != null) {
                    if (!$util.isObject(object.wrench))
                        throw $TypeError(".lebai.motion.StartForceModeRequest.wrench: object expected");
                    message.wrench = $root.lebai.motion.Wrench.fromObject(object.wrench, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a StartForceModeRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.StartForceModeRequest
             * @static
             * @param {lebai.motion.StartForceModeRequest} message StartForceModeRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StartForceModeRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.limit = null;
                    object.wrench = null;
                }
                if (message.limit != null && $Object.hasOwnProperty.call(message, "limit"))
                    object.limit = $root.lebai.posture.CartesianPose.toObject(message.limit, options, _depth + 1);
                if (message.wrench != null && $Object.hasOwnProperty.call(message, "wrench"))
                    object.wrench = $root.lebai.motion.Wrench.toObject(message.wrench, options, _depth + 1);
                return object;
            };

            /**
             * Converts this StartForceModeRequest to JSON.
             * @function toJSON
             * @memberof lebai.motion.StartForceModeRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StartForceModeRequest.prototype.toJSON = function() {
                return StartForceModeRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StartForceModeRequest
             * @function getTypeUrl
             * @memberof lebai.motion.StartForceModeRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StartForceModeRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.StartForceModeRequest";
            };

            return StartForceModeRequest;
        })();

        motion.ForceModeParam = (function() {

            /**
             * Properties of a ForceModeParam.
             * @typedef {Object} lebai.motion.ForceModeParam.$Properties
             * @property {number|null} [threshold] ForceModeParam threshold
             * @property {number|null} [damping] ForceModeParam damping
             * @property {number|null} [mass] ForceModeParam mass
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ForceModeParam.
             * @memberof lebai.motion
             * @interface IForceModeParam
             * @augments lebai.motion.ForceModeParam.$Properties
             * @deprecated Use lebai.motion.ForceModeParam.$Properties instead.
             */

            /**
             * Shape of a ForceModeParam.
             * @typedef {lebai.motion.ForceModeParam.$Properties} lebai.motion.ForceModeParam.$Shape
             */

            /**
             * Constructs a new ForceModeParam.
             * @memberof lebai.motion
             * @classdesc Represents a ForceModeParam.
             * @constructor
             * @param {lebai.motion.ForceModeParam.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ForceModeParam = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ForceModeParam threshold.
             * @member {number} threshold
             * @memberof lebai.motion.ForceModeParam
             * @instance
             */
            ForceModeParam.prototype.threshold = 0;

            /**
             * ForceModeParam damping.
             * @member {number} damping
             * @memberof lebai.motion.ForceModeParam
             * @instance
             */
            ForceModeParam.prototype.damping = 0;

            /**
             * ForceModeParam mass.
             * @member {number} mass
             * @memberof lebai.motion.ForceModeParam
             * @instance
             */
            ForceModeParam.prototype.mass = 0;

            /**
             * Creates a new ForceModeParam instance using the specified properties.
             * @function create
             * @memberof lebai.motion.ForceModeParam
             * @static
             * @param {lebai.motion.ForceModeParam.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.ForceModeParam} ForceModeParam instance
             * @type {{
             *   (properties: lebai.motion.ForceModeParam.$Shape): lebai.motion.ForceModeParam & lebai.motion.ForceModeParam.$Shape;
             *   (properties?: lebai.motion.ForceModeParam.$Properties): lebai.motion.ForceModeParam;
             * }}
             */
            ForceModeParam.create = function(properties) {
                return new ForceModeParam(properties);
            };

            /**
             * Encodes the specified ForceModeParam message. Does not implicitly {@link lebai.motion.ForceModeParam.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.ForceModeParam
             * @static
             * @param {lebai.motion.ForceModeParam.$Properties} message ForceModeParam message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ForceModeParam.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.threshold != null && $Object.hasOwnProperty.call(message, "threshold") && !$Object.is(message.threshold, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.threshold);
                if (message.damping != null && $Object.hasOwnProperty.call(message, "damping") && !$Object.is(message.damping, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.damping);
                if (message.mass != null && $Object.hasOwnProperty.call(message, "mass") && !$Object.is(message.mass, 0))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.mass);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ForceModeParam message, length delimited. Does not implicitly {@link lebai.motion.ForceModeParam.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.ForceModeParam
             * @static
             * @param {lebai.motion.ForceModeParam.$Properties} message ForceModeParam message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ForceModeParam.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ForceModeParam message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.ForceModeParam
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.ForceModeParam & lebai.motion.ForceModeParam.$Shape} ForceModeParam
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ForceModeParam.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.ForceModeParam();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.threshold = value;
                            else
                                delete message.threshold;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.damping = value;
                            else
                                delete message.damping;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.mass = value;
                            else
                                delete message.mass;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ForceModeParam message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.ForceModeParam
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.ForceModeParam & lebai.motion.ForceModeParam.$Shape} ForceModeParam
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ForceModeParam.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ForceModeParam message.
             * @function verify
             * @memberof lebai.motion.ForceModeParam
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ForceModeParam.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.threshold != null && $Object.hasOwnProperty.call(message, "threshold"))
                    if (typeof message.threshold !== "number")
                        return "threshold: number expected";
                if (message.damping != null && $Object.hasOwnProperty.call(message, "damping"))
                    if (typeof message.damping !== "number")
                        return "damping: number expected";
                if (message.mass != null && $Object.hasOwnProperty.call(message, "mass"))
                    if (typeof message.mass !== "number")
                        return "mass: number expected";
                return null;
            };

            /**
             * Creates a ForceModeParam message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.ForceModeParam
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.ForceModeParam} ForceModeParam
             */
            ForceModeParam.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.ForceModeParam)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.ForceModeParam: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.ForceModeParam();
                if (object.threshold != null)
                    if (!$Object.is($Number(object.threshold), 0))
                        message.threshold = $Number(object.threshold);
                if (object.damping != null)
                    if (!$Object.is($Number(object.damping), 0))
                        message.damping = $Number(object.damping);
                if (object.mass != null)
                    if (!$Object.is($Number(object.mass), 0))
                        message.mass = $Number(object.mass);
                return message;
            };

            /**
             * Creates a plain object from a ForceModeParam message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.ForceModeParam
             * @static
             * @param {lebai.motion.ForceModeParam} message ForceModeParam
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ForceModeParam.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.threshold = 0;
                    object.damping = 0;
                    object.mass = 0;
                }
                if (message.threshold != null && $Object.hasOwnProperty.call(message, "threshold"))
                    object.threshold = options.json && !$isFinite(message.threshold) ? $String(message.threshold) : message.threshold;
                if (message.damping != null && $Object.hasOwnProperty.call(message, "damping"))
                    object.damping = options.json && !$isFinite(message.damping) ? $String(message.damping) : message.damping;
                if (message.mass != null && $Object.hasOwnProperty.call(message, "mass"))
                    object.mass = options.json && !$isFinite(message.mass) ? $String(message.mass) : message.mass;
                return object;
            };

            /**
             * Converts this ForceModeParam to JSON.
             * @function toJSON
             * @memberof lebai.motion.ForceModeParam
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ForceModeParam.prototype.toJSON = function() {
                return ForceModeParam.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ForceModeParam
             * @function getTypeUrl
             * @memberof lebai.motion.ForceModeParam
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ForceModeParam.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.ForceModeParam";
            };

            return ForceModeParam;
        })();

        motion.SetForceModeParamRequest = (function() {

            /**
             * Properties of a SetForceModeParamRequest.
             * @typedef {Object} lebai.motion.SetForceModeParamRequest.$Properties
             * @property {number|null} [damping] SetForceModeParamRequest damping
             * @property {number|null} [mass] SetForceModeParamRequest mass
             * @property {number|null} [forceThreshold] SetForceModeParamRequest forceThreshold
             * @property {number|null} [torqueThreshold] SetForceModeParamRequest torqueThreshold
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetForceModeParamRequest.
             * @memberof lebai.motion
             * @interface ISetForceModeParamRequest
             * @augments lebai.motion.SetForceModeParamRequest.$Properties
             * @deprecated Use lebai.motion.SetForceModeParamRequest.$Properties instead.
             */

            /**
             * Shape of a SetForceModeParamRequest.
             * @typedef {lebai.motion.SetForceModeParamRequest.$Properties} lebai.motion.SetForceModeParamRequest.$Shape
             */

            /**
             * Constructs a new SetForceModeParamRequest.
             * @memberof lebai.motion
             * @classdesc Represents a SetForceModeParamRequest.
             * @constructor
             * @param {lebai.motion.SetForceModeParamRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetForceModeParamRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetForceModeParamRequest damping.
             * @member {number|null|undefined} damping
             * @memberof lebai.motion.SetForceModeParamRequest
             * @instance
             */
            SetForceModeParamRequest.prototype.damping = null;

            /**
             * SetForceModeParamRequest mass.
             * @member {number|null|undefined} mass
             * @memberof lebai.motion.SetForceModeParamRequest
             * @instance
             */
            SetForceModeParamRequest.prototype.mass = null;

            /**
             * SetForceModeParamRequest forceThreshold.
             * @member {number|null|undefined} forceThreshold
             * @memberof lebai.motion.SetForceModeParamRequest
             * @instance
             */
            SetForceModeParamRequest.prototype.forceThreshold = null;

            /**
             * SetForceModeParamRequest torqueThreshold.
             * @member {number|null|undefined} torqueThreshold
             * @memberof lebai.motion.SetForceModeParamRequest
             * @instance
             */
            SetForceModeParamRequest.prototype.torqueThreshold = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SetForceModeParamRequest.prototype, "_damping", {
                get: $util.oneOfGetter($oneOfFields = ["damping"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SetForceModeParamRequest.prototype, "_mass", {
                get: $util.oneOfGetter($oneOfFields = ["mass"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SetForceModeParamRequest.prototype, "_forceThreshold", {
                get: $util.oneOfGetter($oneOfFields = ["forceThreshold"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SetForceModeParamRequest.prototype, "_torqueThreshold", {
                get: $util.oneOfGetter($oneOfFields = ["torqueThreshold"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SetForceModeParamRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motion.SetForceModeParamRequest
             * @static
             * @param {lebai.motion.SetForceModeParamRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motion.SetForceModeParamRequest} SetForceModeParamRequest instance
             * @type {{
             *   (properties: lebai.motion.SetForceModeParamRequest.$Shape): lebai.motion.SetForceModeParamRequest & lebai.motion.SetForceModeParamRequest.$Shape;
             *   (properties?: lebai.motion.SetForceModeParamRequest.$Properties): lebai.motion.SetForceModeParamRequest;
             * }}
             */
            SetForceModeParamRequest.create = function(properties) {
                return new SetForceModeParamRequest(properties);
            };

            /**
             * Encodes the specified SetForceModeParamRequest message. Does not implicitly {@link lebai.motion.SetForceModeParamRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motion.SetForceModeParamRequest
             * @static
             * @param {lebai.motion.SetForceModeParamRequest.$Properties} message SetForceModeParamRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetForceModeParamRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.damping != null && $Object.hasOwnProperty.call(message, "damping"))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.damping);
                if (message.mass != null && $Object.hasOwnProperty.call(message, "mass"))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.mass);
                if (message.forceThreshold != null && $Object.hasOwnProperty.call(message, "forceThreshold"))
                    writer.uint32(/* id 10, wireType 1 =*/81).double(message.forceThreshold);
                if (message.torqueThreshold != null && $Object.hasOwnProperty.call(message, "torqueThreshold"))
                    writer.uint32(/* id 11, wireType 1 =*/89).double(message.torqueThreshold);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetForceModeParamRequest message, length delimited. Does not implicitly {@link lebai.motion.SetForceModeParamRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motion.SetForceModeParamRequest
             * @static
             * @param {lebai.motion.SetForceModeParamRequest.$Properties} message SetForceModeParamRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetForceModeParamRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetForceModeParamRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motion.SetForceModeParamRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motion.SetForceModeParamRequest & lebai.motion.SetForceModeParamRequest.$Shape} SetForceModeParamRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetForceModeParamRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motion.SetForceModeParamRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 1)
                                break;
                            message.damping = reader.double();
                            message._damping = "damping";
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            message.mass = reader.double();
                            message._mass = "mass";
                            continue;
                        }
                    case 10: {
                            if (wireType !== 1)
                                break;
                            message.forceThreshold = reader.double();
                            message._forceThreshold = "forceThreshold";
                            continue;
                        }
                    case 11: {
                            if (wireType !== 1)
                                break;
                            message.torqueThreshold = reader.double();
                            message._torqueThreshold = "torqueThreshold";
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetForceModeParamRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motion.SetForceModeParamRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motion.SetForceModeParamRequest & lebai.motion.SetForceModeParamRequest.$Shape} SetForceModeParamRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetForceModeParamRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetForceModeParamRequest message.
             * @function verify
             * @memberof lebai.motion.SetForceModeParamRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetForceModeParamRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.damping != null && $Object.hasOwnProperty.call(message, "damping")) {
                    properties._damping = 1;
                    if (typeof message.damping !== "number")
                        return "damping: number expected";
                }
                if (message.mass != null && $Object.hasOwnProperty.call(message, "mass")) {
                    properties._mass = 1;
                    if (typeof message.mass !== "number")
                        return "mass: number expected";
                }
                if (message.forceThreshold != null && $Object.hasOwnProperty.call(message, "forceThreshold")) {
                    properties._forceThreshold = 1;
                    if (typeof message.forceThreshold !== "number")
                        return "forceThreshold: number expected";
                }
                if (message.torqueThreshold != null && $Object.hasOwnProperty.call(message, "torqueThreshold")) {
                    properties._torqueThreshold = 1;
                    if (typeof message.torqueThreshold !== "number")
                        return "torqueThreshold: number expected";
                }
                return null;
            };

            /**
             * Creates a SetForceModeParamRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motion.SetForceModeParamRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motion.SetForceModeParamRequest} SetForceModeParamRequest
             */
            SetForceModeParamRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motion.SetForceModeParamRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motion.SetForceModeParamRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motion.SetForceModeParamRequest();
                if (object.damping != null)
                    message.damping = $Number(object.damping);
                if (object.mass != null)
                    message.mass = $Number(object.mass);
                if (object.forceThreshold != null)
                    message.forceThreshold = $Number(object.forceThreshold);
                if (object.torqueThreshold != null)
                    message.torqueThreshold = $Number(object.torqueThreshold);
                return message;
            };

            /**
             * Creates a plain object from a SetForceModeParamRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motion.SetForceModeParamRequest
             * @static
             * @param {lebai.motion.SetForceModeParamRequest} message SetForceModeParamRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetForceModeParamRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (message.damping != null && $Object.hasOwnProperty.call(message, "damping"))
                    object.damping = options.json && !$isFinite(message.damping) ? $String(message.damping) : message.damping;
                if (message.mass != null && $Object.hasOwnProperty.call(message, "mass"))
                    object.mass = options.json && !$isFinite(message.mass) ? $String(message.mass) : message.mass;
                if (message.forceThreshold != null && $Object.hasOwnProperty.call(message, "forceThreshold"))
                    object.forceThreshold = options.json && !$isFinite(message.forceThreshold) ? $String(message.forceThreshold) : message.forceThreshold;
                if (message.torqueThreshold != null && $Object.hasOwnProperty.call(message, "torqueThreshold"))
                    object.torqueThreshold = options.json && !$isFinite(message.torqueThreshold) ? $String(message.torqueThreshold) : message.torqueThreshold;
                return object;
            };

            /**
             * Converts this SetForceModeParamRequest to JSON.
             * @function toJSON
             * @memberof lebai.motion.SetForceModeParamRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetForceModeParamRequest.prototype.toJSON = function() {
                return SetForceModeParamRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetForceModeParamRequest
             * @function getTypeUrl
             * @memberof lebai.motion.SetForceModeParamRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetForceModeParamRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motion.SetForceModeParamRequest";
            };

            return SetForceModeParamRequest;
        })();

        motion.MotionService = (function() {

            /**
             * Constructs a new MotionService service.
             * @memberof lebai.motion
             * @classdesc Represents a MotionService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var MotionService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(MotionService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: MotionService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new MotionService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.motion.MotionService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {MotionService} RPC service. Useful where requests and/or responses are streamed.
             */
            MotionService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.motion.MotionService#pauseMove}.
             * @memberof lebai.motion.MotionService
             * @typedef PauseMoveCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls PauseMove.
             * @memberof lebai.motion.MotionService
             * @typedef PauseMove
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.motion.MotionService.PauseMoveCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "PauseMove";
             *   readonly path: "/lebai.motion.MotionService/PauseMove";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls PauseMove.
             * @name lebai.motion.MotionService#pauseMove
             * @type {lebai.motion.MotionService.PauseMove}
             */
            $Object.defineProperties(MotionService.prototype.pauseMove = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.pauseMove, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "PauseMove" },
                path: { value: "/lebai.motion.MotionService/PauseMove" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#resumeMove}.
             * @memberof lebai.motion.MotionService
             * @typedef ResumeMoveCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls ResumeMove.
             * @memberof lebai.motion.MotionService
             * @typedef ResumeMove
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.motion.MotionService.ResumeMoveCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "ResumeMove";
             *   readonly path: "/lebai.motion.MotionService/ResumeMove";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls ResumeMove.
             * @name lebai.motion.MotionService#resumeMove
             * @type {lebai.motion.MotionService.ResumeMove}
             */
            $Object.defineProperties(MotionService.prototype.resumeMove = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.resumeMove, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "ResumeMove" },
                path: { value: "/lebai.motion.MotionService/ResumeMove" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#stopMove}.
             * @memberof lebai.motion.MotionService
             * @typedef StopMoveCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls StopMove.
             * @memberof lebai.motion.MotionService
             * @typedef StopMove
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.motion.MotionService.StopMoveCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "StopMove";
             *   readonly path: "/lebai.motion.MotionService/StopMove";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls StopMove.
             * @name lebai.motion.MotionService#stopMove
             * @type {lebai.motion.MotionService.StopMove}
             */
            $Object.defineProperties(MotionService.prototype.stopMove = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.stopMove, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "StopMove" },
                path: { value: "/lebai.motion.MotionService/StopMove" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#skipMove}.
             * @memberof lebai.motion.MotionService
             * @typedef SkipMoveCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SkipMove.
             * @memberof lebai.motion.MotionService
             * @typedef SkipMove
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.motion.MotionService.SkipMoveCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "SkipMove";
             *   readonly path: "/lebai.motion.MotionService/SkipMove";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SkipMove.
             * @name lebai.motion.MotionService#skipMove
             * @type {lebai.motion.MotionService.SkipMove}
             */
            $Object.defineProperties(MotionService.prototype.skipMove = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.skipMove, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SkipMove" },
                path: { value: "/lebai.motion.MotionService/SkipMove" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#waitMove}.
             * @memberof lebai.motion.MotionService
             * @typedef WaitMoveCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls WaitMove.
             * @memberof lebai.motion.MotionService
             * @typedef WaitMove
             * @type {{
             *   (request: lebai.motion.IMotionIndex, callback: lebai.motion.MotionService.WaitMoveCallback): void;
             *   (request: lebai.motion.IMotionIndex): Promise<google.protobuf.Empty>;
             *   readonly name: "WaitMove";
             *   readonly path: "/lebai.motion.MotionService/WaitMove";
             *   readonly requestType: "MotionIndex";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls WaitMove.
             * @name lebai.motion.MotionService#waitMove
             * @type {lebai.motion.MotionService.WaitMove}
             */
            $Object.defineProperties(MotionService.prototype.waitMove = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.waitMove, $root.lebai.motion.MotionIndex, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "WaitMove" },
                path: { value: "/lebai.motion.MotionService/WaitMove" },
                requestType: { value: "MotionIndex" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#getRunningMotion}.
             * @memberof lebai.motion.MotionService
             * @typedef GetRunningMotionCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.motion.MotionIndex} [response] MotionIndex
             */

            /**
             * Calls GetRunningMotion.
             * @memberof lebai.motion.MotionService
             * @typedef GetRunningMotion
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.motion.MotionService.GetRunningMotionCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.motion.MotionIndex>;
             *   readonly name: "GetRunningMotion";
             *   readonly path: "/lebai.motion.MotionService/GetRunningMotion";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "MotionIndex";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetRunningMotion.
             * @name lebai.motion.MotionService#getRunningMotion
             * @type {lebai.motion.MotionService.GetRunningMotion}
             */
            $Object.defineProperties(MotionService.prototype.getRunningMotion = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.getRunningMotion, $root.google.protobuf.Empty, $root.lebai.motion.MotionIndex, request, callback);
            }, {
                name: { value: "GetRunningMotion" },
                path: { value: "/lebai.motion.MotionService/GetRunningMotion" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "MotionIndex" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#getMotionState}.
             * @memberof lebai.motion.MotionService
             * @typedef GetMotionStateCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.motion.GetMotionStateResponse} [response] GetMotionStateResponse
             */

            /**
             * Calls GetMotionState.
             * @memberof lebai.motion.MotionService
             * @typedef GetMotionState
             * @type {{
             *   (request: lebai.motion.IMotionIndex, callback: lebai.motion.MotionService.GetMotionStateCallback): void;
             *   (request: lebai.motion.IMotionIndex): Promise<lebai.motion.GetMotionStateResponse>;
             *   readonly name: "GetMotionState";
             *   readonly path: "/lebai.motion.MotionService/GetMotionState";
             *   readonly requestType: "MotionIndex";
             *   readonly responseType: "GetMotionStateResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetMotionState.
             * @name lebai.motion.MotionService#getMotionState
             * @type {lebai.motion.MotionService.GetMotionState}
             */
            $Object.defineProperties(MotionService.prototype.getMotionState = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.getMotionState, $root.lebai.motion.MotionIndex, $root.lebai.motion.GetMotionStateResponse, request, callback);
            }, {
                name: { value: "GetMotionState" },
                path: { value: "/lebai.motion.MotionService/GetMotionState" },
                requestType: { value: "MotionIndex" },
                responseType: { value: "GetMotionStateResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#towardJoint}.
             * @memberof lebai.motion.MotionService
             * @typedef TowardJointCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.motion.MotionIndex} [response] MotionIndex
             */

            /**
             * Calls TowardJoint.
             * @memberof lebai.motion.MotionService
             * @typedef TowardJoint
             * @type {{
             *   (request: lebai.motion.IMoveRequest, callback: lebai.motion.MotionService.TowardJointCallback): void;
             *   (request: lebai.motion.IMoveRequest): Promise<lebai.motion.MotionIndex>;
             *   readonly name: "TowardJoint";
             *   readonly path: "/lebai.motion.MotionService/TowardJoint";
             *   readonly requestType: "MoveRequest";
             *   readonly responseType: "MotionIndex";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls TowardJoint.
             * @name lebai.motion.MotionService#towardJoint
             * @type {lebai.motion.MotionService.TowardJoint}
             */
            $Object.defineProperties(MotionService.prototype.towardJoint = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.towardJoint, $root.lebai.motion.MoveRequest, $root.lebai.motion.MotionIndex, request, callback);
            }, {
                name: { value: "TowardJoint" },
                path: { value: "/lebai.motion.MotionService/TowardJoint" },
                requestType: { value: "MoveRequest" },
                responseType: { value: "MotionIndex" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#moveJoint}.
             * @memberof lebai.motion.MotionService
             * @typedef MoveJointCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.motion.MotionIndex} [response] MotionIndex
             */

            /**
             * Calls MoveJoint.
             * @memberof lebai.motion.MotionService
             * @typedef MoveJoint
             * @type {{
             *   (request: lebai.motion.IMoveRequest, callback: lebai.motion.MotionService.MoveJointCallback): void;
             *   (request: lebai.motion.IMoveRequest): Promise<lebai.motion.MotionIndex>;
             *   readonly name: "MoveJoint";
             *   readonly path: "/lebai.motion.MotionService/MoveJoint";
             *   readonly requestType: "MoveRequest";
             *   readonly responseType: "MotionIndex";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls MoveJoint.
             * @name lebai.motion.MotionService#moveJoint
             * @type {lebai.motion.MotionService.MoveJoint}
             */
            $Object.defineProperties(MotionService.prototype.moveJoint = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.moveJoint, $root.lebai.motion.MoveRequest, $root.lebai.motion.MotionIndex, request, callback);
            }, {
                name: { value: "MoveJoint" },
                path: { value: "/lebai.motion.MotionService/MoveJoint" },
                requestType: { value: "MoveRequest" },
                responseType: { value: "MotionIndex" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#moveLinear}.
             * @memberof lebai.motion.MotionService
             * @typedef MoveLinearCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.motion.MotionIndex} [response] MotionIndex
             */

            /**
             * Calls MoveLinear.
             * @memberof lebai.motion.MotionService
             * @typedef MoveLinear
             * @type {{
             *   (request: lebai.motion.IMoveRequest, callback: lebai.motion.MotionService.MoveLinearCallback): void;
             *   (request: lebai.motion.IMoveRequest): Promise<lebai.motion.MotionIndex>;
             *   readonly name: "MoveLinear";
             *   readonly path: "/lebai.motion.MotionService/MoveLinear";
             *   readonly requestType: "MoveRequest";
             *   readonly responseType: "MotionIndex";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls MoveLinear.
             * @name lebai.motion.MotionService#moveLinear
             * @type {lebai.motion.MotionService.MoveLinear}
             */
            $Object.defineProperties(MotionService.prototype.moveLinear = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.moveLinear, $root.lebai.motion.MoveRequest, $root.lebai.motion.MotionIndex, request, callback);
            }, {
                name: { value: "MoveLinear" },
                path: { value: "/lebai.motion.MotionService/MoveLinear" },
                requestType: { value: "MoveRequest" },
                responseType: { value: "MotionIndex" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#moveCircular}.
             * @memberof lebai.motion.MotionService
             * @typedef MoveCircularCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.motion.MotionIndex} [response] MotionIndex
             */

            /**
             * Calls MoveCircular.
             * @memberof lebai.motion.MotionService
             * @typedef MoveCircular
             * @type {{
             *   (request: lebai.motion.IMovecRequest, callback: lebai.motion.MotionService.MoveCircularCallback): void;
             *   (request: lebai.motion.IMovecRequest): Promise<lebai.motion.MotionIndex>;
             *   readonly name: "MoveCircular";
             *   readonly path: "/lebai.motion.MotionService/MoveCircular";
             *   readonly requestType: "MovecRequest";
             *   readonly responseType: "MotionIndex";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls MoveCircular.
             * @name lebai.motion.MotionService#moveCircular
             * @type {lebai.motion.MotionService.MoveCircular}
             */
            $Object.defineProperties(MotionService.prototype.moveCircular = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.moveCircular, $root.lebai.motion.MovecRequest, $root.lebai.motion.MotionIndex, request, callback);
            }, {
                name: { value: "MoveCircular" },
                path: { value: "/lebai.motion.MotionService/MoveCircular" },
                requestType: { value: "MovecRequest" },
                responseType: { value: "MotionIndex" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#movePvat}.
             * @memberof lebai.motion.MotionService
             * @typedef MovePvatCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls MovePvat.
             * @memberof lebai.motion.MotionService
             * @typedef MovePvat
             * @type {{
             *   (request: lebai.motion.IMovePvatRequest, callback: lebai.motion.MotionService.MovePvatCallback): void;
             *   (request: lebai.motion.IMovePvatRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "MovePvat";
             *   readonly path: "/lebai.motion.MotionService/MovePvat";
             *   readonly requestType: "MovePvatRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls MovePvat.
             * @name lebai.motion.MotionService#movePvat
             * @type {lebai.motion.MotionService.MovePvat}
             */
            $Object.defineProperties(MotionService.prototype.movePvat = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.movePvat, $root.lebai.motion.MovePvatRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "MovePvat" },
                path: { value: "/lebai.motion.MotionService/MovePvat" },
                requestType: { value: "MovePvatRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#speedJoint}.
             * @memberof lebai.motion.MotionService
             * @typedef SpeedJointCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.motion.MotionIndex} [response] MotionIndex
             */

            /**
             * Calls SpeedJoint.
             * @memberof lebai.motion.MotionService
             * @typedef SpeedJoint
             * @type {{
             *   (request: lebai.motion.ISpeedJRequest, callback: lebai.motion.MotionService.SpeedJointCallback): void;
             *   (request: lebai.motion.ISpeedJRequest): Promise<lebai.motion.MotionIndex>;
             *   readonly name: "SpeedJoint";
             *   readonly path: "/lebai.motion.MotionService/SpeedJoint";
             *   readonly requestType: "SpeedJRequest";
             *   readonly responseType: "MotionIndex";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SpeedJoint.
             * @name lebai.motion.MotionService#speedJoint
             * @type {lebai.motion.MotionService.SpeedJoint}
             */
            $Object.defineProperties(MotionService.prototype.speedJoint = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.speedJoint, $root.lebai.motion.SpeedJRequest, $root.lebai.motion.MotionIndex, request, callback);
            }, {
                name: { value: "SpeedJoint" },
                path: { value: "/lebai.motion.MotionService/SpeedJoint" },
                requestType: { value: "SpeedJRequest" },
                responseType: { value: "MotionIndex" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#speedLinear}.
             * @memberof lebai.motion.MotionService
             * @typedef SpeedLinearCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.motion.MotionIndex} [response] MotionIndex
             */

            /**
             * Calls SpeedLinear.
             * @memberof lebai.motion.MotionService
             * @typedef SpeedLinear
             * @type {{
             *   (request: lebai.motion.ISpeedLRequest, callback: lebai.motion.MotionService.SpeedLinearCallback): void;
             *   (request: lebai.motion.ISpeedLRequest): Promise<lebai.motion.MotionIndex>;
             *   readonly name: "SpeedLinear";
             *   readonly path: "/lebai.motion.MotionService/SpeedLinear";
             *   readonly requestType: "SpeedLRequest";
             *   readonly responseType: "MotionIndex";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SpeedLinear.
             * @name lebai.motion.MotionService#speedLinear
             * @type {lebai.motion.MotionService.SpeedLinear}
             */
            $Object.defineProperties(MotionService.prototype.speedLinear = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.speedLinear, $root.lebai.motion.SpeedLRequest, $root.lebai.motion.MotionIndex, request, callback);
            }, {
                name: { value: "SpeedLinear" },
                path: { value: "/lebai.motion.MotionService/SpeedLinear" },
                requestType: { value: "SpeedLRequest" },
                responseType: { value: "MotionIndex" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#saveTrajectory}.
             * @memberof lebai.motion.MotionService
             * @typedef SaveTrajectoryCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SaveTrajectory.
             * @memberof lebai.motion.MotionService
             * @typedef SaveTrajectory
             * @type {{
             *   (request: lebai.motion.ISaveTrajectoryRequest, callback: lebai.motion.MotionService.SaveTrajectoryCallback): void;
             *   (request: lebai.motion.ISaveTrajectoryRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SaveTrajectory";
             *   readonly path: "/lebai.motion.MotionService/SaveTrajectory";
             *   readonly requestType: "SaveTrajectoryRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SaveTrajectory.
             * @name lebai.motion.MotionService#saveTrajectory
             * @type {lebai.motion.MotionService.SaveTrajectory}
             */
            $Object.defineProperties(MotionService.prototype.saveTrajectory = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.saveTrajectory, $root.lebai.motion.SaveTrajectoryRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SaveTrajectory" },
                path: { value: "/lebai.motion.MotionService/SaveTrajectory" },
                requestType: { value: "SaveTrajectoryRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#loadTrajectory}.
             * @memberof lebai.motion.MotionService
             * @typedef LoadTrajectoryCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.motion.Trajectory} [response] Trajectory
             */

            /**
             * Calls LoadTrajectory.
             * @memberof lebai.motion.MotionService
             * @typedef LoadTrajectory
             * @type {{
             *   (request: lebai.db.ILoadRequest, callback: lebai.motion.MotionService.LoadTrajectoryCallback): void;
             *   (request: lebai.db.ILoadRequest): Promise<lebai.motion.Trajectory>;
             *   readonly name: "LoadTrajectory";
             *   readonly path: "/lebai.motion.MotionService/LoadTrajectory";
             *   readonly requestType: "db.LoadRequest";
             *   readonly responseType: "Trajectory";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadTrajectory.
             * @name lebai.motion.MotionService#loadTrajectory
             * @type {lebai.motion.MotionService.LoadTrajectory}
             */
            $Object.defineProperties(MotionService.prototype.loadTrajectory = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.loadTrajectory, $root.lebai.db.LoadRequest, $root.lebai.motion.Trajectory, request, callback);
            }, {
                name: { value: "LoadTrajectory" },
                path: { value: "/lebai.motion.MotionService/LoadTrajectory" },
                requestType: { value: "db.LoadRequest" },
                responseType: { value: "Trajectory" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#loadTrajectoryList}.
             * @memberof lebai.motion.MotionService
             * @typedef LoadTrajectoryListCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.db.LoadListResponse} [response] LoadListResponse
             */

            /**
             * Calls LoadTrajectoryList.
             * @memberof lebai.motion.MotionService
             * @typedef LoadTrajectoryList
             * @type {{
             *   (request: lebai.db.ILoadListRequest, callback: lebai.motion.MotionService.LoadTrajectoryListCallback): void;
             *   (request: lebai.db.ILoadListRequest): Promise<lebai.db.LoadListResponse>;
             *   readonly name: "LoadTrajectoryList";
             *   readonly path: "/lebai.motion.MotionService/LoadTrajectoryList";
             *   readonly requestType: "db.LoadListRequest";
             *   readonly responseType: "db.LoadListResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadTrajectoryList.
             * @name lebai.motion.MotionService#loadTrajectoryList
             * @type {lebai.motion.MotionService.LoadTrajectoryList}
             */
            $Object.defineProperties(MotionService.prototype.loadTrajectoryList = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.loadTrajectoryList, $root.lebai.db.LoadListRequest, $root.lebai.db.LoadListResponse, request, callback);
            }, {
                name: { value: "LoadTrajectoryList" },
                path: { value: "/lebai.motion.MotionService/LoadTrajectoryList" },
                requestType: { value: "db.LoadListRequest" },
                responseType: { value: "db.LoadListResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#startRecordTrajectory}.
             * @memberof lebai.motion.MotionService
             * @typedef StartRecordTrajectoryCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls StartRecordTrajectory.
             * @memberof lebai.motion.MotionService
             * @typedef StartRecordTrajectory
             * @type {{
             *   (request: lebai.motion.IStartRecordTrajectoryRequest, callback: lebai.motion.MotionService.StartRecordTrajectoryCallback): void;
             *   (request: lebai.motion.IStartRecordTrajectoryRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "StartRecordTrajectory";
             *   readonly path: "/lebai.motion.MotionService/StartRecordTrajectory";
             *   readonly requestType: "StartRecordTrajectoryRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls StartRecordTrajectory.
             * @name lebai.motion.MotionService#startRecordTrajectory
             * @type {lebai.motion.MotionService.StartRecordTrajectory}
             */
            $Object.defineProperties(MotionService.prototype.startRecordTrajectory = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.startRecordTrajectory, $root.lebai.motion.StartRecordTrajectoryRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "StartRecordTrajectory" },
                path: { value: "/lebai.motion.MotionService/StartRecordTrajectory" },
                requestType: { value: "StartRecordTrajectoryRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#endRecordTrajectory}.
             * @memberof lebai.motion.MotionService
             * @typedef EndRecordTrajectoryCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls EndRecordTrajectory.
             * @memberof lebai.motion.MotionService
             * @typedef EndRecordTrajectory
             * @type {{
             *   (request: lebai.motion.IEndRecordTrajectoryRequest, callback: lebai.motion.MotionService.EndRecordTrajectoryCallback): void;
             *   (request: lebai.motion.IEndRecordTrajectoryRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "EndRecordTrajectory";
             *   readonly path: "/lebai.motion.MotionService/EndRecordTrajectory";
             *   readonly requestType: "EndRecordTrajectoryRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls EndRecordTrajectory.
             * @name lebai.motion.MotionService#endRecordTrajectory
             * @type {lebai.motion.MotionService.EndRecordTrajectory}
             */
            $Object.defineProperties(MotionService.prototype.endRecordTrajectory = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.endRecordTrajectory, $root.lebai.motion.EndRecordTrajectoryRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "EndRecordTrajectory" },
                path: { value: "/lebai.motion.MotionService/EndRecordTrajectory" },
                requestType: { value: "EndRecordTrajectoryRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#moveTrajectory}.
             * @memberof lebai.motion.MotionService
             * @typedef MoveTrajectoryCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.motion.MotionIndex} [response] MotionIndex
             */

            /**
             * Calls MoveTrajectory.
             * @memberof lebai.motion.MotionService
             * @typedef MoveTrajectory
             * @type {{
             *   (request: lebai.db.ILoadRequest, callback: lebai.motion.MotionService.MoveTrajectoryCallback): void;
             *   (request: lebai.db.ILoadRequest): Promise<lebai.motion.MotionIndex>;
             *   readonly name: "MoveTrajectory";
             *   readonly path: "/lebai.motion.MotionService/MoveTrajectory";
             *   readonly requestType: "db.LoadRequest";
             *   readonly responseType: "MotionIndex";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls MoveTrajectory.
             * @name lebai.motion.MotionService#moveTrajectory
             * @type {lebai.motion.MotionService.MoveTrajectory}
             */
            $Object.defineProperties(MotionService.prototype.moveTrajectory = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.moveTrajectory, $root.lebai.db.LoadRequest, $root.lebai.motion.MotionIndex, request, callback);
            }, {
                name: { value: "MoveTrajectory" },
                path: { value: "/lebai.motion.MotionService/MoveTrajectory" },
                requestType: { value: "db.LoadRequest" },
                responseType: { value: "MotionIndex" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#startTeachMode}.
             * @memberof lebai.motion.MotionService
             * @typedef StartTeachModeCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls StartTeachMode.
             * @memberof lebai.motion.MotionService
             * @typedef StartTeachMode
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.motion.MotionService.StartTeachModeCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "StartTeachMode";
             *   readonly path: "/lebai.motion.MotionService/StartTeachMode";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls StartTeachMode.
             * @name lebai.motion.MotionService#startTeachMode
             * @type {lebai.motion.MotionService.StartTeachMode}
             */
            $Object.defineProperties(MotionService.prototype.startTeachMode = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.startTeachMode, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "StartTeachMode" },
                path: { value: "/lebai.motion.MotionService/StartTeachMode" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#endTeachMode}.
             * @memberof lebai.motion.MotionService
             * @typedef EndTeachModeCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls EndTeachMode.
             * @memberof lebai.motion.MotionService
             * @typedef EndTeachMode
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.motion.MotionService.EndTeachModeCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "EndTeachMode";
             *   readonly path: "/lebai.motion.MotionService/EndTeachMode";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls EndTeachMode.
             * @name lebai.motion.MotionService#endTeachMode
             * @type {lebai.motion.MotionService.EndTeachMode}
             */
            $Object.defineProperties(MotionService.prototype.endTeachMode = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.endTeachMode, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "EndTeachMode" },
                path: { value: "/lebai.motion.MotionService/EndTeachMode" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#setTcpForce}.
             * @memberof lebai.motion.MotionService
             * @typedef SetTcpForceCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetTcpForce.
             * @memberof lebai.motion.MotionService
             * @typedef SetTcpForce
             * @type {{
             *   (request: lebai.motion.IWrench, callback: lebai.motion.MotionService.SetTcpForceCallback): void;
             *   (request: lebai.motion.IWrench): Promise<google.protobuf.Empty>;
             *   readonly name: "SetTcpForce";
             *   readonly path: "/lebai.motion.MotionService/SetTcpForce";
             *   readonly requestType: "Wrench";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetTcpForce.
             * @name lebai.motion.MotionService#setTcpForce
             * @type {lebai.motion.MotionService.SetTcpForce}
             */
            $Object.defineProperties(MotionService.prototype.setTcpForce = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.setTcpForce, $root.lebai.motion.Wrench, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetTcpForce" },
                path: { value: "/lebai.motion.MotionService/SetTcpForce" },
                requestType: { value: "Wrench" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#getTcpForce}.
             * @memberof lebai.motion.MotionService
             * @typedef GetTcpForceCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.motion.Wrench} [response] Wrench
             */

            /**
             * Calls GetTcpForce.
             * @memberof lebai.motion.MotionService
             * @typedef GetTcpForce
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.motion.MotionService.GetTcpForceCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.motion.Wrench>;
             *   readonly name: "GetTcpForce";
             *   readonly path: "/lebai.motion.MotionService/GetTcpForce";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "Wrench";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetTcpForce.
             * @name lebai.motion.MotionService#getTcpForce
             * @type {lebai.motion.MotionService.GetTcpForce}
             */
            $Object.defineProperties(MotionService.prototype.getTcpForce = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.getTcpForce, $root.google.protobuf.Empty, $root.lebai.motion.Wrench, request, callback);
            }, {
                name: { value: "GetTcpForce" },
                path: { value: "/lebai.motion.MotionService/GetTcpForce" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "Wrench" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#setForceModeSensor}.
             * @memberof lebai.motion.MotionService
             * @typedef SetForceModeSensorCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetForceModeSensor.
             * @memberof lebai.motion.MotionService
             * @typedef SetForceModeSensor
             * @type {{
             *   (request: lebai.motion.ISetForceSensorRequest, callback: lebai.motion.MotionService.SetForceModeSensorCallback): void;
             *   (request: lebai.motion.ISetForceSensorRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetForceModeSensor";
             *   readonly path: "/lebai.motion.MotionService/SetForceModeSensor";
             *   readonly requestType: "SetForceSensorRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetForceModeSensor.
             * @name lebai.motion.MotionService#setForceModeSensor
             * @type {lebai.motion.MotionService.SetForceModeSensor}
             */
            $Object.defineProperties(MotionService.prototype.setForceModeSensor = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.setForceModeSensor, $root.lebai.motion.SetForceSensorRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetForceModeSensor" },
                path: { value: "/lebai.motion.MotionService/SetForceModeSensor" },
                requestType: { value: "SetForceSensorRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#setForceModeParam}.
             * @memberof lebai.motion.MotionService
             * @typedef SetForceModeParamCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetForceModeParam.
             * @memberof lebai.motion.MotionService
             * @typedef SetForceModeParam
             * @type {{
             *   (request: lebai.motion.ISetForceModeParamRequest, callback: lebai.motion.MotionService.SetForceModeParamCallback): void;
             *   (request: lebai.motion.ISetForceModeParamRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetForceModeParam";
             *   readonly path: "/lebai.motion.MotionService/SetForceModeParam";
             *   readonly requestType: "SetForceModeParamRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetForceModeParam.
             * @name lebai.motion.MotionService#setForceModeParam
             * @type {lebai.motion.MotionService.SetForceModeParam}
             */
            $Object.defineProperties(MotionService.prototype.setForceModeParam = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.setForceModeParam, $root.lebai.motion.SetForceModeParamRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetForceModeParam" },
                path: { value: "/lebai.motion.MotionService/SetForceModeParam" },
                requestType: { value: "SetForceModeParamRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#startForceMode}.
             * @memberof lebai.motion.MotionService
             * @typedef StartForceModeCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls StartForceMode.
             * @memberof lebai.motion.MotionService
             * @typedef StartForceMode
             * @type {{
             *   (request: lebai.motion.IStartForceModeRequest, callback: lebai.motion.MotionService.StartForceModeCallback): void;
             *   (request: lebai.motion.IStartForceModeRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "StartForceMode";
             *   readonly path: "/lebai.motion.MotionService/StartForceMode";
             *   readonly requestType: "StartForceModeRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls StartForceMode.
             * @name lebai.motion.MotionService#startForceMode
             * @type {lebai.motion.MotionService.StartForceMode}
             */
            $Object.defineProperties(MotionService.prototype.startForceMode = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.startForceMode, $root.lebai.motion.StartForceModeRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "StartForceMode" },
                path: { value: "/lebai.motion.MotionService/StartForceMode" },
                requestType: { value: "StartForceModeRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motion.MotionService#endForceMode}.
             * @memberof lebai.motion.MotionService
             * @typedef EndForceModeCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls EndForceMode.
             * @memberof lebai.motion.MotionService
             * @typedef EndForceMode
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.motion.MotionService.EndForceModeCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "EndForceMode";
             *   readonly path: "/lebai.motion.MotionService/EndForceMode";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls EndForceMode.
             * @name lebai.motion.MotionService#endForceMode
             * @type {lebai.motion.MotionService.EndForceMode}
             */
            $Object.defineProperties(MotionService.prototype.endForceMode = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotionService.prototype.endForceMode, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "EndForceMode" },
                path: { value: "/lebai.motion.MotionService/EndForceMode" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return MotionService;
        })();

        return motion;
    })();

    lebai.motor = (function() {

        /**
         * Namespace motor.
         * @memberof lebai
         * @namespace
         */
        var motor = {};

        /**
         * MotorMode enum.
         * @name lebai.motor.MotorMode
         * @enum {number}
         * @property {number} POSITION=0 POSITION value
         * @property {number} TORQUE=1 TORQUE value
         * @property {number} SPEED=2 SPEED value
         */
        motor.MotorMode = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "POSITION"] = 0;
            values[valuesById[1] = "TORQUE"] = 1;
            values[valuesById[2] = "SPEED"] = 2;
            return values;
        })();

        motor.ServoParam = (function() {

            /**
             * Properties of a ServoParam.
             * @typedef {Object} lebai.motor.ServoParam.$Properties
             * @property {number|null} [positionKp] ServoParam positionKp
             * @property {number|null} [speedKp] ServoParam speedKp
             * @property {number|null} [speedIt] ServoParam speedIt
             * @property {number|null} [torqueCmdFilter] ServoParam torqueCmdFilter
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ServoParam.
             * @memberof lebai.motor
             * @interface IServoParam
             * @augments lebai.motor.ServoParam.$Properties
             * @deprecated Use lebai.motor.ServoParam.$Properties instead.
             */

            /**
             * Shape of a ServoParam.
             * @typedef {lebai.motor.ServoParam.$Properties} lebai.motor.ServoParam.$Shape
             */

            /**
             * Constructs a new ServoParam.
             * @memberof lebai.motor
             * @classdesc Represents a ServoParam.
             * @constructor
             * @param {lebai.motor.ServoParam.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ServoParam = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ServoParam positionKp.
             * @member {number} positionKp
             * @memberof lebai.motor.ServoParam
             * @instance
             */
            ServoParam.prototype.positionKp = 0;

            /**
             * ServoParam speedKp.
             * @member {number} speedKp
             * @memberof lebai.motor.ServoParam
             * @instance
             */
            ServoParam.prototype.speedKp = 0;

            /**
             * ServoParam speedIt.
             * @member {number} speedIt
             * @memberof lebai.motor.ServoParam
             * @instance
             */
            ServoParam.prototype.speedIt = 0;

            /**
             * ServoParam torqueCmdFilter.
             * @member {number} torqueCmdFilter
             * @memberof lebai.motor.ServoParam
             * @instance
             */
            ServoParam.prototype.torqueCmdFilter = 0;

            /**
             * Creates a new ServoParam instance using the specified properties.
             * @function create
             * @memberof lebai.motor.ServoParam
             * @static
             * @param {lebai.motor.ServoParam.$Properties=} [properties] Properties to set
             * @returns {lebai.motor.ServoParam} ServoParam instance
             * @type {{
             *   (properties: lebai.motor.ServoParam.$Shape): lebai.motor.ServoParam & lebai.motor.ServoParam.$Shape;
             *   (properties?: lebai.motor.ServoParam.$Properties): lebai.motor.ServoParam;
             * }}
             */
            ServoParam.create = function(properties) {
                return new ServoParam(properties);
            };

            /**
             * Encodes the specified ServoParam message. Does not implicitly {@link lebai.motor.ServoParam.verify|verify} messages.
             * @function encode
             * @memberof lebai.motor.ServoParam
             * @static
             * @param {lebai.motor.ServoParam.$Properties} message ServoParam message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ServoParam.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.positionKp != null && $Object.hasOwnProperty.call(message, "positionKp") && !$Object.is(message.positionKp, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.positionKp);
                if (message.speedKp != null && $Object.hasOwnProperty.call(message, "speedKp") && !$Object.is(message.speedKp, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.speedKp);
                if (message.speedIt != null && $Object.hasOwnProperty.call(message, "speedIt") && !$Object.is(message.speedIt, 0))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.speedIt);
                if (message.torqueCmdFilter != null && $Object.hasOwnProperty.call(message, "torqueCmdFilter") && !$Object.is(message.torqueCmdFilter, 0))
                    writer.uint32(/* id 4, wireType 1 =*/33).double(message.torqueCmdFilter);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ServoParam message, length delimited. Does not implicitly {@link lebai.motor.ServoParam.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motor.ServoParam
             * @static
             * @param {lebai.motor.ServoParam.$Properties} message ServoParam message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ServoParam.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ServoParam message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motor.ServoParam
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motor.ServoParam & lebai.motor.ServoParam.$Shape} ServoParam
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ServoParam.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motor.ServoParam();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.positionKp = value;
                            else
                                delete message.positionKp;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.speedKp = value;
                            else
                                delete message.speedKp;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.speedIt = value;
                            else
                                delete message.speedIt;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.torqueCmdFilter = value;
                            else
                                delete message.torqueCmdFilter;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ServoParam message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motor.ServoParam
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motor.ServoParam & lebai.motor.ServoParam.$Shape} ServoParam
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ServoParam.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ServoParam message.
             * @function verify
             * @memberof lebai.motor.ServoParam
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ServoParam.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.positionKp != null && $Object.hasOwnProperty.call(message, "positionKp"))
                    if (typeof message.positionKp !== "number")
                        return "positionKp: number expected";
                if (message.speedKp != null && $Object.hasOwnProperty.call(message, "speedKp"))
                    if (typeof message.speedKp !== "number")
                        return "speedKp: number expected";
                if (message.speedIt != null && $Object.hasOwnProperty.call(message, "speedIt"))
                    if (typeof message.speedIt !== "number")
                        return "speedIt: number expected";
                if (message.torqueCmdFilter != null && $Object.hasOwnProperty.call(message, "torqueCmdFilter"))
                    if (typeof message.torqueCmdFilter !== "number")
                        return "torqueCmdFilter: number expected";
                return null;
            };

            /**
             * Creates a ServoParam message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motor.ServoParam
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motor.ServoParam} ServoParam
             */
            ServoParam.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motor.ServoParam)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motor.ServoParam: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motor.ServoParam();
                if (object.positionKp != null)
                    if (!$Object.is($Number(object.positionKp), 0))
                        message.positionKp = $Number(object.positionKp);
                if (object.speedKp != null)
                    if (!$Object.is($Number(object.speedKp), 0))
                        message.speedKp = $Number(object.speedKp);
                if (object.speedIt != null)
                    if (!$Object.is($Number(object.speedIt), 0))
                        message.speedIt = $Number(object.speedIt);
                if (object.torqueCmdFilter != null)
                    if (!$Object.is($Number(object.torqueCmdFilter), 0))
                        message.torqueCmdFilter = $Number(object.torqueCmdFilter);
                return message;
            };

            /**
             * Creates a plain object from a ServoParam message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motor.ServoParam
             * @static
             * @param {lebai.motor.ServoParam} message ServoParam
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ServoParam.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.positionKp = 0;
                    object.speedKp = 0;
                    object.speedIt = 0;
                    object.torqueCmdFilter = 0;
                }
                if (message.positionKp != null && $Object.hasOwnProperty.call(message, "positionKp"))
                    object.positionKp = options.json && !$isFinite(message.positionKp) ? $String(message.positionKp) : message.positionKp;
                if (message.speedKp != null && $Object.hasOwnProperty.call(message, "speedKp"))
                    object.speedKp = options.json && !$isFinite(message.speedKp) ? $String(message.speedKp) : message.speedKp;
                if (message.speedIt != null && $Object.hasOwnProperty.call(message, "speedIt"))
                    object.speedIt = options.json && !$isFinite(message.speedIt) ? $String(message.speedIt) : message.speedIt;
                if (message.torqueCmdFilter != null && $Object.hasOwnProperty.call(message, "torqueCmdFilter"))
                    object.torqueCmdFilter = options.json && !$isFinite(message.torqueCmdFilter) ? $String(message.torqueCmdFilter) : message.torqueCmdFilter;
                return object;
            };

            /**
             * Converts this ServoParam to JSON.
             * @function toJSON
             * @memberof lebai.motor.ServoParam
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ServoParam.prototype.toJSON = function() {
                return ServoParam.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ServoParam
             * @function getTypeUrl
             * @memberof lebai.motor.ServoParam
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ServoParam.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motor.ServoParam";
            };

            return ServoParam;
        })();

        motor.ServoParams = (function() {

            /**
             * Properties of a ServoParams.
             * @typedef {Object} lebai.motor.ServoParams.$Properties
             * @property {Array.<lebai.motor.ServoParam.$Properties>|null} [params] ServoParams params
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ServoParams.
             * @memberof lebai.motor
             * @interface IServoParams
             * @augments lebai.motor.ServoParams.$Properties
             * @deprecated Use lebai.motor.ServoParams.$Properties instead.
             */

            /**
             * Shape of a ServoParams.
             * @typedef {lebai.motor.ServoParams.$Properties} lebai.motor.ServoParams.$Shape
             */

            /**
             * Constructs a new ServoParams.
             * @memberof lebai.motor
             * @classdesc Represents a ServoParams.
             * @constructor
             * @param {lebai.motor.ServoParams.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ServoParams = function (properties) {
                this.params = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ServoParams params.
             * @member {Array.<lebai.motor.ServoParam.$Properties>} params
             * @memberof lebai.motor.ServoParams
             * @instance
             */
            ServoParams.prototype.params = $util.emptyArray;

            /**
             * Creates a new ServoParams instance using the specified properties.
             * @function create
             * @memberof lebai.motor.ServoParams
             * @static
             * @param {lebai.motor.ServoParams.$Properties=} [properties] Properties to set
             * @returns {lebai.motor.ServoParams} ServoParams instance
             * @type {{
             *   (properties: lebai.motor.ServoParams.$Shape): lebai.motor.ServoParams & lebai.motor.ServoParams.$Shape;
             *   (properties?: lebai.motor.ServoParams.$Properties): lebai.motor.ServoParams;
             * }}
             */
            ServoParams.create = function(properties) {
                return new ServoParams(properties);
            };

            /**
             * Encodes the specified ServoParams message. Does not implicitly {@link lebai.motor.ServoParams.verify|verify} messages.
             * @function encode
             * @memberof lebai.motor.ServoParams
             * @static
             * @param {lebai.motor.ServoParams.$Properties} message ServoParams message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ServoParams.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.params != null && message.params.length)
                    for (var i = 0; i < message.params.length; ++i)
                        $root.lebai.motor.ServoParam.encode(message.params[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ServoParams message, length delimited. Does not implicitly {@link lebai.motor.ServoParams.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motor.ServoParams
             * @static
             * @param {lebai.motor.ServoParams.$Properties} message ServoParams message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ServoParams.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ServoParams message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motor.ServoParams
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motor.ServoParams & lebai.motor.ServoParams.$Shape} ServoParams
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ServoParams.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motor.ServoParams();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.params && message.params.length))
                                message.params = [];
                            message.params.push($root.lebai.motor.ServoParam.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ServoParams message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motor.ServoParams
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motor.ServoParams & lebai.motor.ServoParams.$Shape} ServoParams
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ServoParams.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ServoParams message.
             * @function verify
             * @memberof lebai.motor.ServoParams
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ServoParams.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.params != null && $Object.hasOwnProperty.call(message, "params")) {
                    if (!$Array.isArray(message.params))
                        return "params: array expected";
                    for (var i = 0; i < message.params.length; ++i) {
                        var error = $root.lebai.motor.ServoParam.verify(message.params[i], _depth + 1);
                        if (error)
                            return "params." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ServoParams message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motor.ServoParams
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motor.ServoParams} ServoParams
             */
            ServoParams.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motor.ServoParams)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motor.ServoParams: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motor.ServoParams();
                if (object.params) {
                    if (!$Array.isArray(object.params))
                        throw $TypeError(".lebai.motor.ServoParams.params: array expected");
                    message.params = $Array(object.params.length);
                    for (var i = 0; i < object.params.length; ++i) {
                        if (!$util.isObject(object.params[i]))
                            throw $TypeError(".lebai.motor.ServoParams.params: object expected");
                        message.params[i] = $root.lebai.motor.ServoParam.fromObject(object.params[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a ServoParams message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motor.ServoParams
             * @static
             * @param {lebai.motor.ServoParams} message ServoParams
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ServoParams.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.params = [];
                if (message.params && message.params.length) {
                    object.params = $Array(message.params.length);
                    for (var j = 0; j < message.params.length; ++j)
                        object.params[j] = $root.lebai.motor.ServoParam.toObject(message.params[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this ServoParams to JSON.
             * @function toJSON
             * @memberof lebai.motor.ServoParams
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ServoParams.prototype.toJSON = function() {
                return ServoParams.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ServoParams
             * @function getTypeUrl
             * @memberof lebai.motor.ServoParams
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ServoParams.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motor.ServoParams";
            };

            return ServoParams;
        })();

        motor.SetZeroRequest = (function() {

            /**
             * Properties of a SetZeroRequest.
             * @typedef {Object} lebai.motor.SetZeroRequest.$Properties
             * @property {Array.<number>|null} [pose] SetZeroRequest pose
             * @property {Array.<boolean>|null} [valids] SetZeroRequest valids
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetZeroRequest.
             * @memberof lebai.motor
             * @interface ISetZeroRequest
             * @augments lebai.motor.SetZeroRequest.$Properties
             * @deprecated Use lebai.motor.SetZeroRequest.$Properties instead.
             */

            /**
             * Shape of a SetZeroRequest.
             * @typedef {lebai.motor.SetZeroRequest.$Properties} lebai.motor.SetZeroRequest.$Shape
             */

            /**
             * Constructs a new SetZeroRequest.
             * @memberof lebai.motor
             * @classdesc Represents a SetZeroRequest.
             * @constructor
             * @param {lebai.motor.SetZeroRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetZeroRequest = function (properties) {
                this.pose = [];
                this.valids = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetZeroRequest pose.
             * @member {Array.<number>} pose
             * @memberof lebai.motor.SetZeroRequest
             * @instance
             */
            SetZeroRequest.prototype.pose = $util.emptyArray;

            /**
             * SetZeroRequest valids.
             * @member {Array.<boolean>} valids
             * @memberof lebai.motor.SetZeroRequest
             * @instance
             */
            SetZeroRequest.prototype.valids = $util.emptyArray;

            /**
             * Creates a new SetZeroRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motor.SetZeroRequest
             * @static
             * @param {lebai.motor.SetZeroRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motor.SetZeroRequest} SetZeroRequest instance
             * @type {{
             *   (properties: lebai.motor.SetZeroRequest.$Shape): lebai.motor.SetZeroRequest & lebai.motor.SetZeroRequest.$Shape;
             *   (properties?: lebai.motor.SetZeroRequest.$Properties): lebai.motor.SetZeroRequest;
             * }}
             */
            SetZeroRequest.create = function(properties) {
                return new SetZeroRequest(properties);
            };

            /**
             * Encodes the specified SetZeroRequest message. Does not implicitly {@link lebai.motor.SetZeroRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motor.SetZeroRequest
             * @static
             * @param {lebai.motor.SetZeroRequest.$Properties} message SetZeroRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetZeroRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.pose != null && message.pose.length)
                    writer.uint32(/* id 1, wireType 2 =*/10).doubles(message.pose);
                if (message.valids != null && message.valids.length)
                    writer.uint32(/* id 2, wireType 2 =*/18).bools(message.valids);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetZeroRequest message, length delimited. Does not implicitly {@link lebai.motor.SetZeroRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motor.SetZeroRequest
             * @static
             * @param {lebai.motor.SetZeroRequest.$Properties} message SetZeroRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetZeroRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetZeroRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motor.SetZeroRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motor.SetZeroRequest & lebai.motor.SetZeroRequest.$Shape} SetZeroRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetZeroRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motor.SetZeroRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.pose && message.pose.length))
                                    message.pose = [];
                                reader.doubles(message.pose);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.pose && message.pose.length))
                                message.pose = [];
                            message.pose.push(reader.double());
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.valids && message.valids.length))
                                    message.valids = [];
                                reader.bools(message.valids);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.valids && message.valids.length))
                                message.valids = [];
                            message.valids.push(reader.bool());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetZeroRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motor.SetZeroRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motor.SetZeroRequest & lebai.motor.SetZeroRequest.$Shape} SetZeroRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetZeroRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetZeroRequest message.
             * @function verify
             * @memberof lebai.motor.SetZeroRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetZeroRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.pose != null && $Object.hasOwnProperty.call(message, "pose")) {
                    if (!$Array.isArray(message.pose))
                        return "pose: array expected";
                    for (var i = 0; i < message.pose.length; ++i)
                        if (typeof message.pose[i] !== "number")
                            return "pose: number[] expected";
                }
                if (message.valids != null && $Object.hasOwnProperty.call(message, "valids")) {
                    if (!$Array.isArray(message.valids))
                        return "valids: array expected";
                    for (var i = 0; i < message.valids.length; ++i)
                        if (typeof message.valids[i] !== "boolean")
                            return "valids: boolean[] expected";
                }
                return null;
            };

            /**
             * Creates a SetZeroRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motor.SetZeroRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motor.SetZeroRequest} SetZeroRequest
             */
            SetZeroRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motor.SetZeroRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motor.SetZeroRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motor.SetZeroRequest();
                if (object.pose) {
                    if (!$Array.isArray(object.pose))
                        throw $TypeError(".lebai.motor.SetZeroRequest.pose: array expected");
                    message.pose = $Array(object.pose.length);
                    for (var i = 0; i < object.pose.length; ++i)
                        message.pose[i] = $Number(object.pose[i]);
                }
                if (object.valids) {
                    if (!$Array.isArray(object.valids))
                        throw $TypeError(".lebai.motor.SetZeroRequest.valids: array expected");
                    message.valids = $Array(object.valids.length);
                    for (var i = 0; i < object.valids.length; ++i)
                        message.valids[i] = $Boolean(object.valids[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a SetZeroRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motor.SetZeroRequest
             * @static
             * @param {lebai.motor.SetZeroRequest} message SetZeroRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetZeroRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.pose = [];
                    object.valids = [];
                }
                if (message.pose && message.pose.length) {
                    object.pose = $Array(message.pose.length);
                    for (var j = 0; j < message.pose.length; ++j)
                        object.pose[j] = options.json && !$isFinite(message.pose[j]) ? $String(message.pose[j]) : message.pose[j];
                }
                if (message.valids && message.valids.length) {
                    object.valids = $Array(message.valids.length);
                    for (var j = 0; j < message.valids.length; ++j)
                        object.valids[j] = message.valids[j];
                }
                return object;
            };

            /**
             * Converts this SetZeroRequest to JSON.
             * @function toJSON
             * @memberof lebai.motor.SetZeroRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetZeroRequest.prototype.toJSON = function() {
                return SetZeroRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetZeroRequest
             * @function getTypeUrl
             * @memberof lebai.motor.SetZeroRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetZeroRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motor.SetZeroRequest";
            };

            return SetZeroRequest;
        })();

        motor.ExtraServoParam = (function() {

            /**
             * Properties of an ExtraServoParam.
             * @typedef {Object} lebai.motor.ExtraServoParam.$Properties
             * @property {number|null} [accPositionKp] ExtraServoParam accPositionKp
             * @property {number|null} [accSpeedKp] ExtraServoParam accSpeedKp
             * @property {number|null} [accSpeedIt] ExtraServoParam accSpeedIt
             * @property {number|null} [uniPositionKp] ExtraServoParam uniPositionKp
             * @property {number|null} [uniSpeedKp] ExtraServoParam uniSpeedKp
             * @property {number|null} [uniSpeedIt] ExtraServoParam uniSpeedIt
             * @property {number|null} [decPositionKp] ExtraServoParam decPositionKp
             * @property {number|null} [decSpeedKp] ExtraServoParam decSpeedKp
             * @property {number|null} [decSpeedIt] ExtraServoParam decSpeedIt
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an ExtraServoParam.
             * @memberof lebai.motor
             * @interface IExtraServoParam
             * @augments lebai.motor.ExtraServoParam.$Properties
             * @deprecated Use lebai.motor.ExtraServoParam.$Properties instead.
             */

            /**
             * Shape of an ExtraServoParam.
             * @typedef {lebai.motor.ExtraServoParam.$Properties} lebai.motor.ExtraServoParam.$Shape
             */

            /**
             * Constructs a new ExtraServoParam.
             * @memberof lebai.motor
             * @classdesc Represents an ExtraServoParam.
             * @constructor
             * @param {lebai.motor.ExtraServoParam.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ExtraServoParam = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ExtraServoParam accPositionKp.
             * @member {number} accPositionKp
             * @memberof lebai.motor.ExtraServoParam
             * @instance
             */
            ExtraServoParam.prototype.accPositionKp = 0;

            /**
             * ExtraServoParam accSpeedKp.
             * @member {number} accSpeedKp
             * @memberof lebai.motor.ExtraServoParam
             * @instance
             */
            ExtraServoParam.prototype.accSpeedKp = 0;

            /**
             * ExtraServoParam accSpeedIt.
             * @member {number} accSpeedIt
             * @memberof lebai.motor.ExtraServoParam
             * @instance
             */
            ExtraServoParam.prototype.accSpeedIt = 0;

            /**
             * ExtraServoParam uniPositionKp.
             * @member {number} uniPositionKp
             * @memberof lebai.motor.ExtraServoParam
             * @instance
             */
            ExtraServoParam.prototype.uniPositionKp = 0;

            /**
             * ExtraServoParam uniSpeedKp.
             * @member {number} uniSpeedKp
             * @memberof lebai.motor.ExtraServoParam
             * @instance
             */
            ExtraServoParam.prototype.uniSpeedKp = 0;

            /**
             * ExtraServoParam uniSpeedIt.
             * @member {number} uniSpeedIt
             * @memberof lebai.motor.ExtraServoParam
             * @instance
             */
            ExtraServoParam.prototype.uniSpeedIt = 0;

            /**
             * ExtraServoParam decPositionKp.
             * @member {number} decPositionKp
             * @memberof lebai.motor.ExtraServoParam
             * @instance
             */
            ExtraServoParam.prototype.decPositionKp = 0;

            /**
             * ExtraServoParam decSpeedKp.
             * @member {number} decSpeedKp
             * @memberof lebai.motor.ExtraServoParam
             * @instance
             */
            ExtraServoParam.prototype.decSpeedKp = 0;

            /**
             * ExtraServoParam decSpeedIt.
             * @member {number} decSpeedIt
             * @memberof lebai.motor.ExtraServoParam
             * @instance
             */
            ExtraServoParam.prototype.decSpeedIt = 0;

            /**
             * Creates a new ExtraServoParam instance using the specified properties.
             * @function create
             * @memberof lebai.motor.ExtraServoParam
             * @static
             * @param {lebai.motor.ExtraServoParam.$Properties=} [properties] Properties to set
             * @returns {lebai.motor.ExtraServoParam} ExtraServoParam instance
             * @type {{
             *   (properties: lebai.motor.ExtraServoParam.$Shape): lebai.motor.ExtraServoParam & lebai.motor.ExtraServoParam.$Shape;
             *   (properties?: lebai.motor.ExtraServoParam.$Properties): lebai.motor.ExtraServoParam;
             * }}
             */
            ExtraServoParam.create = function(properties) {
                return new ExtraServoParam(properties);
            };

            /**
             * Encodes the specified ExtraServoParam message. Does not implicitly {@link lebai.motor.ExtraServoParam.verify|verify} messages.
             * @function encode
             * @memberof lebai.motor.ExtraServoParam
             * @static
             * @param {lebai.motor.ExtraServoParam.$Properties} message ExtraServoParam message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExtraServoParam.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.accPositionKp != null && $Object.hasOwnProperty.call(message, "accPositionKp") && !$Object.is(message.accPositionKp, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.accPositionKp);
                if (message.accSpeedKp != null && $Object.hasOwnProperty.call(message, "accSpeedKp") && !$Object.is(message.accSpeedKp, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.accSpeedKp);
                if (message.accSpeedIt != null && $Object.hasOwnProperty.call(message, "accSpeedIt") && !$Object.is(message.accSpeedIt, 0))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.accSpeedIt);
                if (message.uniPositionKp != null && $Object.hasOwnProperty.call(message, "uniPositionKp") && !$Object.is(message.uniPositionKp, 0))
                    writer.uint32(/* id 4, wireType 1 =*/33).double(message.uniPositionKp);
                if (message.uniSpeedKp != null && $Object.hasOwnProperty.call(message, "uniSpeedKp") && !$Object.is(message.uniSpeedKp, 0))
                    writer.uint32(/* id 5, wireType 1 =*/41).double(message.uniSpeedKp);
                if (message.uniSpeedIt != null && $Object.hasOwnProperty.call(message, "uniSpeedIt") && !$Object.is(message.uniSpeedIt, 0))
                    writer.uint32(/* id 6, wireType 1 =*/49).double(message.uniSpeedIt);
                if (message.decPositionKp != null && $Object.hasOwnProperty.call(message, "decPositionKp") && !$Object.is(message.decPositionKp, 0))
                    writer.uint32(/* id 7, wireType 1 =*/57).double(message.decPositionKp);
                if (message.decSpeedKp != null && $Object.hasOwnProperty.call(message, "decSpeedKp") && !$Object.is(message.decSpeedKp, 0))
                    writer.uint32(/* id 8, wireType 1 =*/65).double(message.decSpeedKp);
                if (message.decSpeedIt != null && $Object.hasOwnProperty.call(message, "decSpeedIt") && !$Object.is(message.decSpeedIt, 0))
                    writer.uint32(/* id 9, wireType 1 =*/73).double(message.decSpeedIt);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ExtraServoParam message, length delimited. Does not implicitly {@link lebai.motor.ExtraServoParam.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motor.ExtraServoParam
             * @static
             * @param {lebai.motor.ExtraServoParam.$Properties} message ExtraServoParam message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExtraServoParam.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an ExtraServoParam message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motor.ExtraServoParam
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motor.ExtraServoParam & lebai.motor.ExtraServoParam.$Shape} ExtraServoParam
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExtraServoParam.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motor.ExtraServoParam();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.accPositionKp = value;
                            else
                                delete message.accPositionKp;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.accSpeedKp = value;
                            else
                                delete message.accSpeedKp;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.accSpeedIt = value;
                            else
                                delete message.accSpeedIt;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.uniPositionKp = value;
                            else
                                delete message.uniPositionKp;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.uniSpeedKp = value;
                            else
                                delete message.uniSpeedKp;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.uniSpeedIt = value;
                            else
                                delete message.uniSpeedIt;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.decPositionKp = value;
                            else
                                delete message.decPositionKp;
                            continue;
                        }
                    case 8: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.decSpeedKp = value;
                            else
                                delete message.decSpeedKp;
                            continue;
                        }
                    case 9: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.decSpeedIt = value;
                            else
                                delete message.decSpeedIt;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an ExtraServoParam message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motor.ExtraServoParam
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motor.ExtraServoParam & lebai.motor.ExtraServoParam.$Shape} ExtraServoParam
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExtraServoParam.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an ExtraServoParam message.
             * @function verify
             * @memberof lebai.motor.ExtraServoParam
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ExtraServoParam.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.accPositionKp != null && $Object.hasOwnProperty.call(message, "accPositionKp"))
                    if (typeof message.accPositionKp !== "number")
                        return "accPositionKp: number expected";
                if (message.accSpeedKp != null && $Object.hasOwnProperty.call(message, "accSpeedKp"))
                    if (typeof message.accSpeedKp !== "number")
                        return "accSpeedKp: number expected";
                if (message.accSpeedIt != null && $Object.hasOwnProperty.call(message, "accSpeedIt"))
                    if (typeof message.accSpeedIt !== "number")
                        return "accSpeedIt: number expected";
                if (message.uniPositionKp != null && $Object.hasOwnProperty.call(message, "uniPositionKp"))
                    if (typeof message.uniPositionKp !== "number")
                        return "uniPositionKp: number expected";
                if (message.uniSpeedKp != null && $Object.hasOwnProperty.call(message, "uniSpeedKp"))
                    if (typeof message.uniSpeedKp !== "number")
                        return "uniSpeedKp: number expected";
                if (message.uniSpeedIt != null && $Object.hasOwnProperty.call(message, "uniSpeedIt"))
                    if (typeof message.uniSpeedIt !== "number")
                        return "uniSpeedIt: number expected";
                if (message.decPositionKp != null && $Object.hasOwnProperty.call(message, "decPositionKp"))
                    if (typeof message.decPositionKp !== "number")
                        return "decPositionKp: number expected";
                if (message.decSpeedKp != null && $Object.hasOwnProperty.call(message, "decSpeedKp"))
                    if (typeof message.decSpeedKp !== "number")
                        return "decSpeedKp: number expected";
                if (message.decSpeedIt != null && $Object.hasOwnProperty.call(message, "decSpeedIt"))
                    if (typeof message.decSpeedIt !== "number")
                        return "decSpeedIt: number expected";
                return null;
            };

            /**
             * Creates an ExtraServoParam message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motor.ExtraServoParam
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motor.ExtraServoParam} ExtraServoParam
             */
            ExtraServoParam.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motor.ExtraServoParam)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motor.ExtraServoParam: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motor.ExtraServoParam();
                if (object.accPositionKp != null)
                    if (!$Object.is($Number(object.accPositionKp), 0))
                        message.accPositionKp = $Number(object.accPositionKp);
                if (object.accSpeedKp != null)
                    if (!$Object.is($Number(object.accSpeedKp), 0))
                        message.accSpeedKp = $Number(object.accSpeedKp);
                if (object.accSpeedIt != null)
                    if (!$Object.is($Number(object.accSpeedIt), 0))
                        message.accSpeedIt = $Number(object.accSpeedIt);
                if (object.uniPositionKp != null)
                    if (!$Object.is($Number(object.uniPositionKp), 0))
                        message.uniPositionKp = $Number(object.uniPositionKp);
                if (object.uniSpeedKp != null)
                    if (!$Object.is($Number(object.uniSpeedKp), 0))
                        message.uniSpeedKp = $Number(object.uniSpeedKp);
                if (object.uniSpeedIt != null)
                    if (!$Object.is($Number(object.uniSpeedIt), 0))
                        message.uniSpeedIt = $Number(object.uniSpeedIt);
                if (object.decPositionKp != null)
                    if (!$Object.is($Number(object.decPositionKp), 0))
                        message.decPositionKp = $Number(object.decPositionKp);
                if (object.decSpeedKp != null)
                    if (!$Object.is($Number(object.decSpeedKp), 0))
                        message.decSpeedKp = $Number(object.decSpeedKp);
                if (object.decSpeedIt != null)
                    if (!$Object.is($Number(object.decSpeedIt), 0))
                        message.decSpeedIt = $Number(object.decSpeedIt);
                return message;
            };

            /**
             * Creates a plain object from an ExtraServoParam message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motor.ExtraServoParam
             * @static
             * @param {lebai.motor.ExtraServoParam} message ExtraServoParam
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ExtraServoParam.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.accPositionKp = 0;
                    object.accSpeedKp = 0;
                    object.accSpeedIt = 0;
                    object.uniPositionKp = 0;
                    object.uniSpeedKp = 0;
                    object.uniSpeedIt = 0;
                    object.decPositionKp = 0;
                    object.decSpeedKp = 0;
                    object.decSpeedIt = 0;
                }
                if (message.accPositionKp != null && $Object.hasOwnProperty.call(message, "accPositionKp"))
                    object.accPositionKp = options.json && !$isFinite(message.accPositionKp) ? $String(message.accPositionKp) : message.accPositionKp;
                if (message.accSpeedKp != null && $Object.hasOwnProperty.call(message, "accSpeedKp"))
                    object.accSpeedKp = options.json && !$isFinite(message.accSpeedKp) ? $String(message.accSpeedKp) : message.accSpeedKp;
                if (message.accSpeedIt != null && $Object.hasOwnProperty.call(message, "accSpeedIt"))
                    object.accSpeedIt = options.json && !$isFinite(message.accSpeedIt) ? $String(message.accSpeedIt) : message.accSpeedIt;
                if (message.uniPositionKp != null && $Object.hasOwnProperty.call(message, "uniPositionKp"))
                    object.uniPositionKp = options.json && !$isFinite(message.uniPositionKp) ? $String(message.uniPositionKp) : message.uniPositionKp;
                if (message.uniSpeedKp != null && $Object.hasOwnProperty.call(message, "uniSpeedKp"))
                    object.uniSpeedKp = options.json && !$isFinite(message.uniSpeedKp) ? $String(message.uniSpeedKp) : message.uniSpeedKp;
                if (message.uniSpeedIt != null && $Object.hasOwnProperty.call(message, "uniSpeedIt"))
                    object.uniSpeedIt = options.json && !$isFinite(message.uniSpeedIt) ? $String(message.uniSpeedIt) : message.uniSpeedIt;
                if (message.decPositionKp != null && $Object.hasOwnProperty.call(message, "decPositionKp"))
                    object.decPositionKp = options.json && !$isFinite(message.decPositionKp) ? $String(message.decPositionKp) : message.decPositionKp;
                if (message.decSpeedKp != null && $Object.hasOwnProperty.call(message, "decSpeedKp"))
                    object.decSpeedKp = options.json && !$isFinite(message.decSpeedKp) ? $String(message.decSpeedKp) : message.decSpeedKp;
                if (message.decSpeedIt != null && $Object.hasOwnProperty.call(message, "decSpeedIt"))
                    object.decSpeedIt = options.json && !$isFinite(message.decSpeedIt) ? $String(message.decSpeedIt) : message.decSpeedIt;
                return object;
            };

            /**
             * Converts this ExtraServoParam to JSON.
             * @function toJSON
             * @memberof lebai.motor.ExtraServoParam
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ExtraServoParam.prototype.toJSON = function() {
                return ExtraServoParam.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ExtraServoParam
             * @function getTypeUrl
             * @memberof lebai.motor.ExtraServoParam
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ExtraServoParam.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motor.ExtraServoParam";
            };

            return ExtraServoParam;
        })();

        motor.SetExtraServoParamsRequest = (function() {

            /**
             * Properties of a SetExtraServoParamsRequest.
             * @typedef {Object} lebai.motor.SetExtraServoParamsRequest.$Properties
             * @property {Array.<lebai.motor.ExtraServoParam.$Properties>|null} [params] SetExtraServoParamsRequest params
             * @property {Array.<boolean>|null} [valids] SetExtraServoParamsRequest valids
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetExtraServoParamsRequest.
             * @memberof lebai.motor
             * @interface ISetExtraServoParamsRequest
             * @augments lebai.motor.SetExtraServoParamsRequest.$Properties
             * @deprecated Use lebai.motor.SetExtraServoParamsRequest.$Properties instead.
             */

            /**
             * Shape of a SetExtraServoParamsRequest.
             * @typedef {lebai.motor.SetExtraServoParamsRequest.$Properties} lebai.motor.SetExtraServoParamsRequest.$Shape
             */

            /**
             * Constructs a new SetExtraServoParamsRequest.
             * @memberof lebai.motor
             * @classdesc Represents a SetExtraServoParamsRequest.
             * @constructor
             * @param {lebai.motor.SetExtraServoParamsRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetExtraServoParamsRequest = function (properties) {
                this.params = [];
                this.valids = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetExtraServoParamsRequest params.
             * @member {Array.<lebai.motor.ExtraServoParam.$Properties>} params
             * @memberof lebai.motor.SetExtraServoParamsRequest
             * @instance
             */
            SetExtraServoParamsRequest.prototype.params = $util.emptyArray;

            /**
             * SetExtraServoParamsRequest valids.
             * @member {Array.<boolean>} valids
             * @memberof lebai.motor.SetExtraServoParamsRequest
             * @instance
             */
            SetExtraServoParamsRequest.prototype.valids = $util.emptyArray;

            /**
             * Creates a new SetExtraServoParamsRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motor.SetExtraServoParamsRequest
             * @static
             * @param {lebai.motor.SetExtraServoParamsRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motor.SetExtraServoParamsRequest} SetExtraServoParamsRequest instance
             * @type {{
             *   (properties: lebai.motor.SetExtraServoParamsRequest.$Shape): lebai.motor.SetExtraServoParamsRequest & lebai.motor.SetExtraServoParamsRequest.$Shape;
             *   (properties?: lebai.motor.SetExtraServoParamsRequest.$Properties): lebai.motor.SetExtraServoParamsRequest;
             * }}
             */
            SetExtraServoParamsRequest.create = function(properties) {
                return new SetExtraServoParamsRequest(properties);
            };

            /**
             * Encodes the specified SetExtraServoParamsRequest message. Does not implicitly {@link lebai.motor.SetExtraServoParamsRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motor.SetExtraServoParamsRequest
             * @static
             * @param {lebai.motor.SetExtraServoParamsRequest.$Properties} message SetExtraServoParamsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetExtraServoParamsRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.params != null && message.params.length)
                    for (var i = 0; i < message.params.length; ++i)
                        $root.lebai.motor.ExtraServoParam.encode(message.params[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.valids != null && message.valids.length)
                    writer.uint32(/* id 2, wireType 2 =*/18).bools(message.valids);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetExtraServoParamsRequest message, length delimited. Does not implicitly {@link lebai.motor.SetExtraServoParamsRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motor.SetExtraServoParamsRequest
             * @static
             * @param {lebai.motor.SetExtraServoParamsRequest.$Properties} message SetExtraServoParamsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetExtraServoParamsRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetExtraServoParamsRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motor.SetExtraServoParamsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motor.SetExtraServoParamsRequest & lebai.motor.SetExtraServoParamsRequest.$Shape} SetExtraServoParamsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetExtraServoParamsRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motor.SetExtraServoParamsRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.params && message.params.length))
                                message.params = [];
                            message.params.push($root.lebai.motor.ExtraServoParam.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.valids && message.valids.length))
                                    message.valids = [];
                                reader.bools(message.valids);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.valids && message.valids.length))
                                message.valids = [];
                            message.valids.push(reader.bool());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetExtraServoParamsRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motor.SetExtraServoParamsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motor.SetExtraServoParamsRequest & lebai.motor.SetExtraServoParamsRequest.$Shape} SetExtraServoParamsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetExtraServoParamsRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetExtraServoParamsRequest message.
             * @function verify
             * @memberof lebai.motor.SetExtraServoParamsRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetExtraServoParamsRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.params != null && $Object.hasOwnProperty.call(message, "params")) {
                    if (!$Array.isArray(message.params))
                        return "params: array expected";
                    for (var i = 0; i < message.params.length; ++i) {
                        var error = $root.lebai.motor.ExtraServoParam.verify(message.params[i], _depth + 1);
                        if (error)
                            return "params." + error;
                    }
                }
                if (message.valids != null && $Object.hasOwnProperty.call(message, "valids")) {
                    if (!$Array.isArray(message.valids))
                        return "valids: array expected";
                    for (var i = 0; i < message.valids.length; ++i)
                        if (typeof message.valids[i] !== "boolean")
                            return "valids: boolean[] expected";
                }
                return null;
            };

            /**
             * Creates a SetExtraServoParamsRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motor.SetExtraServoParamsRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motor.SetExtraServoParamsRequest} SetExtraServoParamsRequest
             */
            SetExtraServoParamsRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motor.SetExtraServoParamsRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motor.SetExtraServoParamsRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motor.SetExtraServoParamsRequest();
                if (object.params) {
                    if (!$Array.isArray(object.params))
                        throw $TypeError(".lebai.motor.SetExtraServoParamsRequest.params: array expected");
                    message.params = $Array(object.params.length);
                    for (var i = 0; i < object.params.length; ++i) {
                        if (!$util.isObject(object.params[i]))
                            throw $TypeError(".lebai.motor.SetExtraServoParamsRequest.params: object expected");
                        message.params[i] = $root.lebai.motor.ExtraServoParam.fromObject(object.params[i], _depth + 1);
                    }
                }
                if (object.valids) {
                    if (!$Array.isArray(object.valids))
                        throw $TypeError(".lebai.motor.SetExtraServoParamsRequest.valids: array expected");
                    message.valids = $Array(object.valids.length);
                    for (var i = 0; i < object.valids.length; ++i)
                        message.valids[i] = $Boolean(object.valids[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a SetExtraServoParamsRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motor.SetExtraServoParamsRequest
             * @static
             * @param {lebai.motor.SetExtraServoParamsRequest} message SetExtraServoParamsRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetExtraServoParamsRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.params = [];
                    object.valids = [];
                }
                if (message.params && message.params.length) {
                    object.params = $Array(message.params.length);
                    for (var j = 0; j < message.params.length; ++j)
                        object.params[j] = $root.lebai.motor.ExtraServoParam.toObject(message.params[j], options, _depth + 1);
                }
                if (message.valids && message.valids.length) {
                    object.valids = $Array(message.valids.length);
                    for (var j = 0; j < message.valids.length; ++j)
                        object.valids[j] = message.valids[j];
                }
                return object;
            };

            /**
             * Converts this SetExtraServoParamsRequest to JSON.
             * @function toJSON
             * @memberof lebai.motor.SetExtraServoParamsRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetExtraServoParamsRequest.prototype.toJSON = function() {
                return SetExtraServoParamsRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetExtraServoParamsRequest
             * @function getTypeUrl
             * @memberof lebai.motor.SetExtraServoParamsRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetExtraServoParamsRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motor.SetExtraServoParamsRequest";
            };

            return SetExtraServoParamsRequest;
        })();

        motor.ResetExtraServoParamsRequest = (function() {

            /**
             * Properties of a ResetExtraServoParamsRequest.
             * @typedef {Object} lebai.motor.ResetExtraServoParamsRequest.$Properties
             * @property {Array.<boolean>|null} [valids] ResetExtraServoParamsRequest valids
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ResetExtraServoParamsRequest.
             * @memberof lebai.motor
             * @interface IResetExtraServoParamsRequest
             * @augments lebai.motor.ResetExtraServoParamsRequest.$Properties
             * @deprecated Use lebai.motor.ResetExtraServoParamsRequest.$Properties instead.
             */

            /**
             * Shape of a ResetExtraServoParamsRequest.
             * @typedef {lebai.motor.ResetExtraServoParamsRequest.$Properties} lebai.motor.ResetExtraServoParamsRequest.$Shape
             */

            /**
             * Constructs a new ResetExtraServoParamsRequest.
             * @memberof lebai.motor
             * @classdesc Represents a ResetExtraServoParamsRequest.
             * @constructor
             * @param {lebai.motor.ResetExtraServoParamsRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ResetExtraServoParamsRequest = function (properties) {
                this.valids = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ResetExtraServoParamsRequest valids.
             * @member {Array.<boolean>} valids
             * @memberof lebai.motor.ResetExtraServoParamsRequest
             * @instance
             */
            ResetExtraServoParamsRequest.prototype.valids = $util.emptyArray;

            /**
             * Creates a new ResetExtraServoParamsRequest instance using the specified properties.
             * @function create
             * @memberof lebai.motor.ResetExtraServoParamsRequest
             * @static
             * @param {lebai.motor.ResetExtraServoParamsRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.motor.ResetExtraServoParamsRequest} ResetExtraServoParamsRequest instance
             * @type {{
             *   (properties: lebai.motor.ResetExtraServoParamsRequest.$Shape): lebai.motor.ResetExtraServoParamsRequest & lebai.motor.ResetExtraServoParamsRequest.$Shape;
             *   (properties?: lebai.motor.ResetExtraServoParamsRequest.$Properties): lebai.motor.ResetExtraServoParamsRequest;
             * }}
             */
            ResetExtraServoParamsRequest.create = function(properties) {
                return new ResetExtraServoParamsRequest(properties);
            };

            /**
             * Encodes the specified ResetExtraServoParamsRequest message. Does not implicitly {@link lebai.motor.ResetExtraServoParamsRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.motor.ResetExtraServoParamsRequest
             * @static
             * @param {lebai.motor.ResetExtraServoParamsRequest.$Properties} message ResetExtraServoParamsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ResetExtraServoParamsRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.valids != null && message.valids.length)
                    writer.uint32(/* id 1, wireType 2 =*/10).bools(message.valids);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ResetExtraServoParamsRequest message, length delimited. Does not implicitly {@link lebai.motor.ResetExtraServoParamsRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.motor.ResetExtraServoParamsRequest
             * @static
             * @param {lebai.motor.ResetExtraServoParamsRequest.$Properties} message ResetExtraServoParamsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ResetExtraServoParamsRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ResetExtraServoParamsRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.motor.ResetExtraServoParamsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.motor.ResetExtraServoParamsRequest & lebai.motor.ResetExtraServoParamsRequest.$Shape} ResetExtraServoParamsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ResetExtraServoParamsRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.motor.ResetExtraServoParamsRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.valids && message.valids.length))
                                    message.valids = [];
                                reader.bools(message.valids);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.valids && message.valids.length))
                                message.valids = [];
                            message.valids.push(reader.bool());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ResetExtraServoParamsRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.motor.ResetExtraServoParamsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.motor.ResetExtraServoParamsRequest & lebai.motor.ResetExtraServoParamsRequest.$Shape} ResetExtraServoParamsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ResetExtraServoParamsRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ResetExtraServoParamsRequest message.
             * @function verify
             * @memberof lebai.motor.ResetExtraServoParamsRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ResetExtraServoParamsRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.valids != null && $Object.hasOwnProperty.call(message, "valids")) {
                    if (!$Array.isArray(message.valids))
                        return "valids: array expected";
                    for (var i = 0; i < message.valids.length; ++i)
                        if (typeof message.valids[i] !== "boolean")
                            return "valids: boolean[] expected";
                }
                return null;
            };

            /**
             * Creates a ResetExtraServoParamsRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.motor.ResetExtraServoParamsRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.motor.ResetExtraServoParamsRequest} ResetExtraServoParamsRequest
             */
            ResetExtraServoParamsRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.motor.ResetExtraServoParamsRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.motor.ResetExtraServoParamsRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.motor.ResetExtraServoParamsRequest();
                if (object.valids) {
                    if (!$Array.isArray(object.valids))
                        throw $TypeError(".lebai.motor.ResetExtraServoParamsRequest.valids: array expected");
                    message.valids = $Array(object.valids.length);
                    for (var i = 0; i < object.valids.length; ++i)
                        message.valids[i] = $Boolean(object.valids[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a ResetExtraServoParamsRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.motor.ResetExtraServoParamsRequest
             * @static
             * @param {lebai.motor.ResetExtraServoParamsRequest} message ResetExtraServoParamsRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ResetExtraServoParamsRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.valids = [];
                if (message.valids && message.valids.length) {
                    object.valids = $Array(message.valids.length);
                    for (var j = 0; j < message.valids.length; ++j)
                        object.valids[j] = message.valids[j];
                }
                return object;
            };

            /**
             * Converts this ResetExtraServoParamsRequest to JSON.
             * @function toJSON
             * @memberof lebai.motor.ResetExtraServoParamsRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ResetExtraServoParamsRequest.prototype.toJSON = function() {
                return ResetExtraServoParamsRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ResetExtraServoParamsRequest
             * @function getTypeUrl
             * @memberof lebai.motor.ResetExtraServoParamsRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ResetExtraServoParamsRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.motor.ResetExtraServoParamsRequest";
            };

            return ResetExtraServoParamsRequest;
        })();

        motor.MotorService = (function() {

            /**
             * Constructs a new MotorService service.
             * @memberof lebai.motor
             * @classdesc Represents a MotorService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var MotorService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(MotorService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: MotorService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new MotorService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.motor.MotorService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {MotorService} RPC service. Useful where requests and/or responses are streamed.
             */
            MotorService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.motor.MotorService#setServoParams}.
             * @memberof lebai.motor.MotorService
             * @typedef SetServoParamsCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetServoParams.
             * @memberof lebai.motor.MotorService
             * @typedef SetServoParams
             * @type {{
             *   (request: lebai.motor.IServoParams, callback: lebai.motor.MotorService.SetServoParamsCallback): void;
             *   (request: lebai.motor.IServoParams): Promise<google.protobuf.Empty>;
             *   readonly name: "SetServoParams";
             *   readonly path: "/lebai.motor.MotorService/SetServoParams";
             *   readonly requestType: "ServoParams";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetServoParams.
             * @name lebai.motor.MotorService#setServoParams
             * @type {lebai.motor.MotorService.SetServoParams}
             */
            $Object.defineProperties(MotorService.prototype.setServoParams = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotorService.prototype.setServoParams, $root.lebai.motor.ServoParams, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetServoParams" },
                path: { value: "/lebai.motor.MotorService/SetServoParams" },
                requestType: { value: "ServoParams" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motor.MotorService#getServoParams}.
             * @memberof lebai.motor.MotorService
             * @typedef GetServoParamsCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.motor.ServoParams} [response] ServoParams
             */

            /**
             * Calls GetServoParams.
             * @memberof lebai.motor.MotorService
             * @typedef GetServoParams
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.motor.MotorService.GetServoParamsCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.motor.ServoParams>;
             *   readonly name: "GetServoParams";
             *   readonly path: "/lebai.motor.MotorService/GetServoParams";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "ServoParams";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetServoParams.
             * @name lebai.motor.MotorService#getServoParams
             * @type {lebai.motor.MotorService.GetServoParams}
             */
            $Object.defineProperties(MotorService.prototype.getServoParams = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotorService.prototype.getServoParams, $root.google.protobuf.Empty, $root.lebai.motor.ServoParams, request, callback);
            }, {
                name: { value: "GetServoParams" },
                path: { value: "/lebai.motor.MotorService/GetServoParams" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "ServoParams" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motor.MotorService#findZero}.
             * @memberof lebai.motor.MotorService
             * @typedef FindZeroCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls FindZero.
             * @memberof lebai.motor.MotorService
             * @typedef FindZero
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.motor.MotorService.FindZeroCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "FindZero";
             *   readonly path: "/lebai.motor.MotorService/FindZero";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls FindZero.
             * @name lebai.motor.MotorService#findZero
             * @type {lebai.motor.MotorService.FindZero}
             */
            $Object.defineProperties(MotorService.prototype.findZero = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotorService.prototype.findZero, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "FindZero" },
                path: { value: "/lebai.motor.MotorService/FindZero" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motor.MotorService#setZero}.
             * @memberof lebai.motor.MotorService
             * @typedef SetZeroCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetZero.
             * @memberof lebai.motor.MotorService
             * @typedef SetZero
             * @type {{
             *   (request: lebai.motor.ISetZeroRequest, callback: lebai.motor.MotorService.SetZeroCallback): void;
             *   (request: lebai.motor.ISetZeroRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetZero";
             *   readonly path: "/lebai.motor.MotorService/SetZero";
             *   readonly requestType: "SetZeroRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetZero.
             * @name lebai.motor.MotorService#setZero
             * @type {lebai.motor.MotorService.SetZero}
             */
            $Object.defineProperties(MotorService.prototype.setZero = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotorService.prototype.setZero, $root.lebai.motor.SetZeroRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetZero" },
                path: { value: "/lebai.motor.MotorService/SetZero" },
                requestType: { value: "SetZeroRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motor.MotorService#setExtraServoParams}.
             * @memberof lebai.motor.MotorService
             * @typedef SetExtraServoParamsCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetExtraServoParams.
             * @memberof lebai.motor.MotorService
             * @typedef SetExtraServoParams
             * @type {{
             *   (request: lebai.motor.ISetExtraServoParamsRequest, callback: lebai.motor.MotorService.SetExtraServoParamsCallback): void;
             *   (request: lebai.motor.ISetExtraServoParamsRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetExtraServoParams";
             *   readonly path: "/lebai.motor.MotorService/SetExtraServoParams";
             *   readonly requestType: "SetExtraServoParamsRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetExtraServoParams.
             * @name lebai.motor.MotorService#setExtraServoParams
             * @type {lebai.motor.MotorService.SetExtraServoParams}
             */
            $Object.defineProperties(MotorService.prototype.setExtraServoParams = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotorService.prototype.setExtraServoParams, $root.lebai.motor.SetExtraServoParamsRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetExtraServoParams" },
                path: { value: "/lebai.motor.MotorService/SetExtraServoParams" },
                requestType: { value: "SetExtraServoParamsRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.motor.MotorService#resetExtraServoParams}.
             * @memberof lebai.motor.MotorService
             * @typedef ResetExtraServoParamsCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls ResetExtraServoParams.
             * @memberof lebai.motor.MotorService
             * @typedef ResetExtraServoParams
             * @type {{
             *   (request: lebai.motor.IResetExtraServoParamsRequest, callback: lebai.motor.MotorService.ResetExtraServoParamsCallback): void;
             *   (request: lebai.motor.IResetExtraServoParamsRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "ResetExtraServoParams";
             *   readonly path: "/lebai.motor.MotorService/ResetExtraServoParams";
             *   readonly requestType: "ResetExtraServoParamsRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls ResetExtraServoParams.
             * @name lebai.motor.MotorService#resetExtraServoParams
             * @type {lebai.motor.MotorService.ResetExtraServoParams}
             */
            $Object.defineProperties(MotorService.prototype.resetExtraServoParams = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MotorService.prototype.resetExtraServoParams, $root.lebai.motor.ResetExtraServoParamsRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "ResetExtraServoParams" },
                path: { value: "/lebai.motor.MotorService/ResetExtraServoParams" },
                requestType: { value: "ResetExtraServoParamsRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return MotorService;
        })();

        return motor;
    })();

    lebai.multi_devices = (function() {

        /**
         * Namespace multi_devices.
         * @memberof lebai
         * @namespace
         */
        var multi_devices = {};

        multi_devices.DeviceInfo = (function() {

            /**
             * Properties of a DeviceInfo.
             * @typedef {Object} lebai.multi_devices.DeviceInfo.$Properties
             * @property {string|null} [name] DeviceInfo name
             * @property {string|null} [mac] DeviceInfo mac
             * @property {string|null} [ip] DeviceInfo ip
             * @property {boolean|null} [online] DeviceInfo online
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DeviceInfo.
             * @memberof lebai.multi_devices
             * @interface IDeviceInfo
             * @augments lebai.multi_devices.DeviceInfo.$Properties
             * @deprecated Use lebai.multi_devices.DeviceInfo.$Properties instead.
             */

            /**
             * Shape of a DeviceInfo.
             * @typedef {lebai.multi_devices.DeviceInfo.$Properties} lebai.multi_devices.DeviceInfo.$Shape
             */

            /**
             * Constructs a new DeviceInfo.
             * @memberof lebai.multi_devices
             * @classdesc Represents a DeviceInfo.
             * @constructor
             * @param {lebai.multi_devices.DeviceInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var DeviceInfo = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DeviceInfo name.
             * @member {string} name
             * @memberof lebai.multi_devices.DeviceInfo
             * @instance
             */
            DeviceInfo.prototype.name = "";

            /**
             * DeviceInfo mac.
             * @member {string} mac
             * @memberof lebai.multi_devices.DeviceInfo
             * @instance
             */
            DeviceInfo.prototype.mac = "";

            /**
             * DeviceInfo ip.
             * @member {string} ip
             * @memberof lebai.multi_devices.DeviceInfo
             * @instance
             */
            DeviceInfo.prototype.ip = "";

            /**
             * DeviceInfo online.
             * @member {boolean} online
             * @memberof lebai.multi_devices.DeviceInfo
             * @instance
             */
            DeviceInfo.prototype.online = false;

            /**
             * Creates a new DeviceInfo instance using the specified properties.
             * @function create
             * @memberof lebai.multi_devices.DeviceInfo
             * @static
             * @param {lebai.multi_devices.DeviceInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.multi_devices.DeviceInfo} DeviceInfo instance
             * @type {{
             *   (properties: lebai.multi_devices.DeviceInfo.$Shape): lebai.multi_devices.DeviceInfo & lebai.multi_devices.DeviceInfo.$Shape;
             *   (properties?: lebai.multi_devices.DeviceInfo.$Properties): lebai.multi_devices.DeviceInfo;
             * }}
             */
            DeviceInfo.create = function(properties) {
                return new DeviceInfo(properties);
            };

            /**
             * Encodes the specified DeviceInfo message. Does not implicitly {@link lebai.multi_devices.DeviceInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.multi_devices.DeviceInfo
             * @static
             * @param {lebai.multi_devices.DeviceInfo.$Properties} message DeviceInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeviceInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.mac != null && $Object.hasOwnProperty.call(message, "mac") && message.mac !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.mac);
                if (message.ip != null && $Object.hasOwnProperty.call(message, "ip") && message.ip !== "")
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.ip);
                if (message.online != null && $Object.hasOwnProperty.call(message, "online") && message.online !== false)
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.online);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DeviceInfo message, length delimited. Does not implicitly {@link lebai.multi_devices.DeviceInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.multi_devices.DeviceInfo
             * @static
             * @param {lebai.multi_devices.DeviceInfo.$Properties} message DeviceInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeviceInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a DeviceInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.multi_devices.DeviceInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.multi_devices.DeviceInfo & lebai.multi_devices.DeviceInfo.$Shape} DeviceInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeviceInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.multi_devices.DeviceInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.mac = value;
                            else
                                delete message.mac;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.ip = value;
                            else
                                delete message.ip;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.online = value;
                            else
                                delete message.online;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a DeviceInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.multi_devices.DeviceInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.multi_devices.DeviceInfo & lebai.multi_devices.DeviceInfo.$Shape} DeviceInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeviceInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DeviceInfo message.
             * @function verify
             * @memberof lebai.multi_devices.DeviceInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DeviceInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.mac != null && $Object.hasOwnProperty.call(message, "mac"))
                    if (!$util.isString(message.mac))
                        return "mac: string expected";
                if (message.ip != null && $Object.hasOwnProperty.call(message, "ip"))
                    if (!$util.isString(message.ip))
                        return "ip: string expected";
                if (message.online != null && $Object.hasOwnProperty.call(message, "online"))
                    if (typeof message.online !== "boolean")
                        return "online: boolean expected";
                return null;
            };

            /**
             * Creates a DeviceInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.multi_devices.DeviceInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.multi_devices.DeviceInfo} DeviceInfo
             */
            DeviceInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.multi_devices.DeviceInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.multi_devices.DeviceInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.multi_devices.DeviceInfo();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.mac != null)
                    if (typeof object.mac !== "string" || object.mac.length)
                        message.mac = $String(object.mac);
                if (object.ip != null)
                    if (typeof object.ip !== "string" || object.ip.length)
                        message.ip = $String(object.ip);
                if (object.online != null)
                    if (object.online)
                        message.online = $Boolean(object.online);
                return message;
            };

            /**
             * Creates a plain object from a DeviceInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.multi_devices.DeviceInfo
             * @static
             * @param {lebai.multi_devices.DeviceInfo} message DeviceInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DeviceInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.mac = "";
                    object.ip = "";
                    object.online = false;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.mac != null && $Object.hasOwnProperty.call(message, "mac"))
                    object.mac = message.mac;
                if (message.ip != null && $Object.hasOwnProperty.call(message, "ip"))
                    object.ip = message.ip;
                if (message.online != null && $Object.hasOwnProperty.call(message, "online"))
                    object.online = message.online;
                return object;
            };

            /**
             * Converts this DeviceInfo to JSON.
             * @function toJSON
             * @memberof lebai.multi_devices.DeviceInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DeviceInfo.prototype.toJSON = function() {
                return DeviceInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DeviceInfo
             * @function getTypeUrl
             * @memberof lebai.multi_devices.DeviceInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DeviceInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.multi_devices.DeviceInfo";
            };

            return DeviceInfo;
        })();

        multi_devices.DiscoverRobotsResponse = (function() {

            /**
             * Properties of a DiscoverRobotsResponse.
             * @typedef {Object} lebai.multi_devices.DiscoverRobotsResponse.$Properties
             * @property {Array.<lebai.multi_devices.DeviceInfo.$Properties>|null} [devices] DiscoverRobotsResponse devices
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a DiscoverRobotsResponse.
             * @memberof lebai.multi_devices
             * @interface IDiscoverRobotsResponse
             * @augments lebai.multi_devices.DiscoverRobotsResponse.$Properties
             * @deprecated Use lebai.multi_devices.DiscoverRobotsResponse.$Properties instead.
             */

            /**
             * Shape of a DiscoverRobotsResponse.
             * @typedef {lebai.multi_devices.DiscoverRobotsResponse.$Properties} lebai.multi_devices.DiscoverRobotsResponse.$Shape
             */

            /**
             * Constructs a new DiscoverRobotsResponse.
             * @memberof lebai.multi_devices
             * @classdesc Represents a DiscoverRobotsResponse.
             * @constructor
             * @param {lebai.multi_devices.DiscoverRobotsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var DiscoverRobotsResponse = function (properties) {
                this.devices = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * DiscoverRobotsResponse devices.
             * @member {Array.<lebai.multi_devices.DeviceInfo.$Properties>} devices
             * @memberof lebai.multi_devices.DiscoverRobotsResponse
             * @instance
             */
            DiscoverRobotsResponse.prototype.devices = $util.emptyArray;

            /**
             * Creates a new DiscoverRobotsResponse instance using the specified properties.
             * @function create
             * @memberof lebai.multi_devices.DiscoverRobotsResponse
             * @static
             * @param {lebai.multi_devices.DiscoverRobotsResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.multi_devices.DiscoverRobotsResponse} DiscoverRobotsResponse instance
             * @type {{
             *   (properties: lebai.multi_devices.DiscoverRobotsResponse.$Shape): lebai.multi_devices.DiscoverRobotsResponse & lebai.multi_devices.DiscoverRobotsResponse.$Shape;
             *   (properties?: lebai.multi_devices.DiscoverRobotsResponse.$Properties): lebai.multi_devices.DiscoverRobotsResponse;
             * }}
             */
            DiscoverRobotsResponse.create = function(properties) {
                return new DiscoverRobotsResponse(properties);
            };

            /**
             * Encodes the specified DiscoverRobotsResponse message. Does not implicitly {@link lebai.multi_devices.DiscoverRobotsResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.multi_devices.DiscoverRobotsResponse
             * @static
             * @param {lebai.multi_devices.DiscoverRobotsResponse.$Properties} message DiscoverRobotsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DiscoverRobotsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.devices != null && message.devices.length)
                    for (var i = 0; i < message.devices.length; ++i)
                        $root.lebai.multi_devices.DeviceInfo.encode(message.devices[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DiscoverRobotsResponse message, length delimited. Does not implicitly {@link lebai.multi_devices.DiscoverRobotsResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.multi_devices.DiscoverRobotsResponse
             * @static
             * @param {lebai.multi_devices.DiscoverRobotsResponse.$Properties} message DiscoverRobotsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DiscoverRobotsResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a DiscoverRobotsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.multi_devices.DiscoverRobotsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.multi_devices.DiscoverRobotsResponse & lebai.multi_devices.DiscoverRobotsResponse.$Shape} DiscoverRobotsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DiscoverRobotsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.multi_devices.DiscoverRobotsResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.devices && message.devices.length))
                                message.devices = [];
                            message.devices.push($root.lebai.multi_devices.DeviceInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a DiscoverRobotsResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.multi_devices.DiscoverRobotsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.multi_devices.DiscoverRobotsResponse & lebai.multi_devices.DiscoverRobotsResponse.$Shape} DiscoverRobotsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DiscoverRobotsResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DiscoverRobotsResponse message.
             * @function verify
             * @memberof lebai.multi_devices.DiscoverRobotsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DiscoverRobotsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.devices != null && $Object.hasOwnProperty.call(message, "devices")) {
                    if (!$Array.isArray(message.devices))
                        return "devices: array expected";
                    for (var i = 0; i < message.devices.length; ++i) {
                        var error = $root.lebai.multi_devices.DeviceInfo.verify(message.devices[i], _depth + 1);
                        if (error)
                            return "devices." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a DiscoverRobotsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.multi_devices.DiscoverRobotsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.multi_devices.DiscoverRobotsResponse} DiscoverRobotsResponse
             */
            DiscoverRobotsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.multi_devices.DiscoverRobotsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.multi_devices.DiscoverRobotsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.multi_devices.DiscoverRobotsResponse();
                if (object.devices) {
                    if (!$Array.isArray(object.devices))
                        throw $TypeError(".lebai.multi_devices.DiscoverRobotsResponse.devices: array expected");
                    message.devices = $Array(object.devices.length);
                    for (var i = 0; i < object.devices.length; ++i) {
                        if (!$util.isObject(object.devices[i]))
                            throw $TypeError(".lebai.multi_devices.DiscoverRobotsResponse.devices: object expected");
                        message.devices[i] = $root.lebai.multi_devices.DeviceInfo.fromObject(object.devices[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a DiscoverRobotsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.multi_devices.DiscoverRobotsResponse
             * @static
             * @param {lebai.multi_devices.DiscoverRobotsResponse} message DiscoverRobotsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DiscoverRobotsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.devices = [];
                if (message.devices && message.devices.length) {
                    object.devices = $Array(message.devices.length);
                    for (var j = 0; j < message.devices.length; ++j)
                        object.devices[j] = $root.lebai.multi_devices.DeviceInfo.toObject(message.devices[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this DiscoverRobotsResponse to JSON.
             * @function toJSON
             * @memberof lebai.multi_devices.DiscoverRobotsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DiscoverRobotsResponse.prototype.toJSON = function() {
                return DiscoverRobotsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for DiscoverRobotsResponse
             * @function getTypeUrl
             * @memberof lebai.multi_devices.DiscoverRobotsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            DiscoverRobotsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.multi_devices.DiscoverRobotsResponse";
            };

            return DiscoverRobotsResponse;
        })();

        multi_devices.MultiDevicesService = (function() {

            /**
             * Constructs a new MultiDevicesService service.
             * @memberof lebai.multi_devices
             * @classdesc Represents a MultiDevicesService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var MultiDevicesService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(MultiDevicesService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: MultiDevicesService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new MultiDevicesService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.multi_devices.MultiDevicesService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {MultiDevicesService} RPC service. Useful where requests and/or responses are streamed.
             */
            MultiDevicesService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.multi_devices.MultiDevicesService#discoverRobots}.
             * @memberof lebai.multi_devices.MultiDevicesService
             * @typedef DiscoverRobotsCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.multi_devices.DiscoverRobotsResponse} [response] DiscoverRobotsResponse
             */

            /**
             * Calls DiscoverRobots.
             * @memberof lebai.multi_devices.MultiDevicesService
             * @typedef DiscoverRobots
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.multi_devices.MultiDevicesService.DiscoverRobotsCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.multi_devices.DiscoverRobotsResponse>;
             *   readonly name: "DiscoverRobots";
             *   readonly path: "/lebai.multi_devices.MultiDevicesService/DiscoverRobots";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "DiscoverRobotsResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls DiscoverRobots.
             * @name lebai.multi_devices.MultiDevicesService#discoverRobots
             * @type {lebai.multi_devices.MultiDevicesService.DiscoverRobots}
             */
            $Object.defineProperties(MultiDevicesService.prototype.discoverRobots = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, MultiDevicesService.prototype.discoverRobots, $root.google.protobuf.Empty, $root.lebai.multi_devices.DiscoverRobotsResponse, request, callback);
            }, {
                name: { value: "DiscoverRobots" },
                path: { value: "/lebai.multi_devices.MultiDevicesService/DiscoverRobots" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "DiscoverRobotsResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return MultiDevicesService;
        })();

        return multi_devices;
    })();

    lebai.network = (function() {

        /**
         * Namespace network.
         * @memberof lebai
         * @namespace
         */
        var network = {};

        network.HttpRequest = (function() {

            /**
             * Properties of a HttpRequest.
             * @typedef {Object} lebai.network.HttpRequest.$Properties
             * @property {string|null} [method] HttpRequest method
             * @property {string|null} [url] HttpRequest url
             * @property {Object.<string,string>|null} [headers] HttpRequest headers
             * @property {string|null} [body] HttpRequest body
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a HttpRequest.
             * @memberof lebai.network
             * @interface IHttpRequest
             * @augments lebai.network.HttpRequest.$Properties
             * @deprecated Use lebai.network.HttpRequest.$Properties instead.
             */

            /**
             * Shape of a HttpRequest.
             * @typedef {lebai.network.HttpRequest.$Properties} lebai.network.HttpRequest.$Shape
             */

            /**
             * Constructs a new HttpRequest.
             * @memberof lebai.network
             * @classdesc Represents a HttpRequest.
             * @constructor
             * @param {lebai.network.HttpRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var HttpRequest = function (properties) {
                this.headers = {};
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * HttpRequest method.
             * @member {string} method
             * @memberof lebai.network.HttpRequest
             * @instance
             */
            HttpRequest.prototype.method = "";

            /**
             * HttpRequest url.
             * @member {string} url
             * @memberof lebai.network.HttpRequest
             * @instance
             */
            HttpRequest.prototype.url = "";

            /**
             * HttpRequest headers.
             * @member {Object.<string,string>} headers
             * @memberof lebai.network.HttpRequest
             * @instance
             */
            HttpRequest.prototype.headers = $util.emptyObject;

            /**
             * HttpRequest body.
             * @member {string} body
             * @memberof lebai.network.HttpRequest
             * @instance
             */
            HttpRequest.prototype.body = "";

            /**
             * Creates a new HttpRequest instance using the specified properties.
             * @function create
             * @memberof lebai.network.HttpRequest
             * @static
             * @param {lebai.network.HttpRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.network.HttpRequest} HttpRequest instance
             * @type {{
             *   (properties: lebai.network.HttpRequest.$Shape): lebai.network.HttpRequest & lebai.network.HttpRequest.$Shape;
             *   (properties?: lebai.network.HttpRequest.$Properties): lebai.network.HttpRequest;
             * }}
             */
            HttpRequest.create = function(properties) {
                return new HttpRequest(properties);
            };

            /**
             * Encodes the specified HttpRequest message. Does not implicitly {@link lebai.network.HttpRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.network.HttpRequest
             * @static
             * @param {lebai.network.HttpRequest.$Properties} message HttpRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HttpRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.method != null && $Object.hasOwnProperty.call(message, "method") && message.method !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.method);
                if (message.url != null && $Object.hasOwnProperty.call(message, "url") && message.url !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.url);
                if (message.headers != null && $Object.hasOwnProperty.call(message, "headers"))
                    for (var keys = $Object.keys(message.headers), i = 0; i < keys.length; ++i)
                        writer.uint32(/* id 15, wireType 2 =*/122).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.headers[keys[i]]).ldelim();
                if (message.body != null && $Object.hasOwnProperty.call(message, "body") && message.body !== "")
                    writer.uint32(/* id 16, wireType 2 =*/130).string(message.body);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified HttpRequest message, length delimited. Does not implicitly {@link lebai.network.HttpRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.network.HttpRequest
             * @static
             * @param {lebai.network.HttpRequest.$Properties} message HttpRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HttpRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a HttpRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.network.HttpRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.network.HttpRequest & lebai.network.HttpRequest.$Shape} HttpRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HttpRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.network.HttpRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.method = value;
                            else
                                delete message.method;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.url = value;
                            else
                                delete message.url;
                            continue;
                        }
                    case 15: {
                            if (wireType !== 2)
                                break;
                            if (message.headers === $util.emptyObject)
                                message.headers = {};
                            var end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = "";
                            while (reader.pos < end2) {
                                var tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = reader.stringVerify();
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.headers, key);
                            message.headers[key] = value;
                            continue;
                        }
                    case 16: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.body = value;
                            else
                                delete message.body;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a HttpRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.network.HttpRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.network.HttpRequest & lebai.network.HttpRequest.$Shape} HttpRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HttpRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a HttpRequest message.
             * @function verify
             * @memberof lebai.network.HttpRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HttpRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.method != null && $Object.hasOwnProperty.call(message, "method"))
                    if (!$util.isString(message.method))
                        return "method: string expected";
                if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
                    if (!$util.isString(message.url))
                        return "url: string expected";
                if (message.headers != null && $Object.hasOwnProperty.call(message, "headers")) {
                    if (!$util.isObject(message.headers))
                        return "headers: object expected";
                    var key = $Object.keys(message.headers);
                    for (var i = 0; i < key.length; ++i)
                        if (!$util.isString(message.headers[key[i]]))
                            return "headers: string{k:string} expected";
                }
                if (message.body != null && $Object.hasOwnProperty.call(message, "body"))
                    if (!$util.isString(message.body))
                        return "body: string expected";
                return null;
            };

            /**
             * Creates a HttpRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.network.HttpRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.network.HttpRequest} HttpRequest
             */
            HttpRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.network.HttpRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.network.HttpRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.network.HttpRequest();
                if (object.method != null)
                    if (typeof object.method !== "string" || object.method.length)
                        message.method = $String(object.method);
                if (object.url != null)
                    if (typeof object.url !== "string" || object.url.length)
                        message.url = $String(object.url);
                if (object.headers) {
                    if (!$util.isObject(object.headers))
                        throw $TypeError(".lebai.network.HttpRequest.headers: object expected");
                    message.headers = {};
                    for (var keys = $Object.keys(object.headers), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.headers, keys[i]);
                        message.headers[keys[i]] = $String(object.headers[keys[i]]);
                    }
                }
                if (object.body != null)
                    if (typeof object.body !== "string" || object.body.length)
                        message.body = $String(object.body);
                return message;
            };

            /**
             * Creates a plain object from a HttpRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.network.HttpRequest
             * @static
             * @param {lebai.network.HttpRequest} message HttpRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HttpRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.objects || options.defaults)
                    object.headers = {};
                if (options.defaults) {
                    object.method = "";
                    object.url = "";
                    object.body = "";
                }
                if (message.method != null && $Object.hasOwnProperty.call(message, "method"))
                    object.method = message.method;
                if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                var keys2;
                if (message.headers && (keys2 = $Object.keys(message.headers)).length) {
                    object.headers = {};
                    for (var j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.headers, keys2[j]);
                        object.headers[keys2[j]] = message.headers[keys2[j]];
                    }
                }
                if (message.body != null && $Object.hasOwnProperty.call(message, "body"))
                    object.body = message.body;
                return object;
            };

            /**
             * Converts this HttpRequest to JSON.
             * @function toJSON
             * @memberof lebai.network.HttpRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HttpRequest.prototype.toJSON = function() {
                return HttpRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for HttpRequest
             * @function getTypeUrl
             * @memberof lebai.network.HttpRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            HttpRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.network.HttpRequest";
            };

            return HttpRequest;
        })();

        network.HttpResponse = (function() {

            /**
             * Properties of a HttpResponse.
             * @typedef {Object} lebai.network.HttpResponse.$Properties
             * @property {number|null} [status] HttpResponse status
             * @property {Object.<string,string>|null} [headers] HttpResponse headers
             * @property {string|null} [body] HttpResponse body
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a HttpResponse.
             * @memberof lebai.network
             * @interface IHttpResponse
             * @augments lebai.network.HttpResponse.$Properties
             * @deprecated Use lebai.network.HttpResponse.$Properties instead.
             */

            /**
             * Shape of a HttpResponse.
             * @typedef {lebai.network.HttpResponse.$Properties} lebai.network.HttpResponse.$Shape
             */

            /**
             * Constructs a new HttpResponse.
             * @memberof lebai.network
             * @classdesc Represents a HttpResponse.
             * @constructor
             * @param {lebai.network.HttpResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var HttpResponse = function (properties) {
                this.headers = {};
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * HttpResponse status.
             * @member {number} status
             * @memberof lebai.network.HttpResponse
             * @instance
             */
            HttpResponse.prototype.status = 0;

            /**
             * HttpResponse headers.
             * @member {Object.<string,string>} headers
             * @memberof lebai.network.HttpResponse
             * @instance
             */
            HttpResponse.prototype.headers = $util.emptyObject;

            /**
             * HttpResponse body.
             * @member {string} body
             * @memberof lebai.network.HttpResponse
             * @instance
             */
            HttpResponse.prototype.body = "";

            /**
             * Creates a new HttpResponse instance using the specified properties.
             * @function create
             * @memberof lebai.network.HttpResponse
             * @static
             * @param {lebai.network.HttpResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.network.HttpResponse} HttpResponse instance
             * @type {{
             *   (properties: lebai.network.HttpResponse.$Shape): lebai.network.HttpResponse & lebai.network.HttpResponse.$Shape;
             *   (properties?: lebai.network.HttpResponse.$Properties): lebai.network.HttpResponse;
             * }}
             */
            HttpResponse.create = function(properties) {
                return new HttpResponse(properties);
            };

            /**
             * Encodes the specified HttpResponse message. Does not implicitly {@link lebai.network.HttpResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.network.HttpResponse
             * @static
             * @param {lebai.network.HttpResponse.$Properties} message HttpResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HttpResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).int32(message.status);
                if (message.headers != null && $Object.hasOwnProperty.call(message, "headers"))
                    for (var keys = $Object.keys(message.headers), i = 0; i < keys.length; ++i)
                        writer.uint32(/* id 15, wireType 2 =*/122).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.headers[keys[i]]).ldelim();
                if (message.body != null && $Object.hasOwnProperty.call(message, "body") && message.body !== "")
                    writer.uint32(/* id 16, wireType 2 =*/130).string(message.body);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified HttpResponse message, length delimited. Does not implicitly {@link lebai.network.HttpResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.network.HttpResponse
             * @static
             * @param {lebai.network.HttpResponse.$Properties} message HttpResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HttpResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a HttpResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.network.HttpResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.network.HttpResponse & lebai.network.HttpResponse.$Shape} HttpResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HttpResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, key, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.network.HttpResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.status = value;
                            else
                                delete message.status;
                            continue;
                        }
                    case 15: {
                            if (wireType !== 2)
                                break;
                            if (message.headers === $util.emptyObject)
                                message.headers = {};
                            var end2 = reader.uint32() + reader.pos;
                            if (end2 > reader.len)
                                throw $RangeError("index out of range");
                            reader.len = end2;
                            key = "";
                            value = "";
                            while (reader.pos < end2) {
                                var tag2 = reader.tag();
                                wireType = tag2 & 7;
                                switch (tag2 >>>= 3) {
                                case 1:
                                    if (wireType !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (wireType !== 2)
                                        break;
                                    value = reader.stringVerify();
                                    continue;
                                }
                                reader.skipType(wireType, _depth, tag2);
                            }
                            if (reader.pos !== end2)
                                throw $RangeError("index out of range");
                            reader.len = end;
                            if (key === "__proto__")
                                $util.makeProp(message.headers, key);
                            message.headers[key] = value;
                            continue;
                        }
                    case 16: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.body = value;
                            else
                                delete message.body;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a HttpResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.network.HttpResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.network.HttpResponse & lebai.network.HttpResponse.$Shape} HttpResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HttpResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a HttpResponse message.
             * @function verify
             * @memberof lebai.network.HttpResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HttpResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    if (!$util.isInteger(message.status))
                        return "status: integer expected";
                if (message.headers != null && $Object.hasOwnProperty.call(message, "headers")) {
                    if (!$util.isObject(message.headers))
                        return "headers: object expected";
                    var key = $Object.keys(message.headers);
                    for (var i = 0; i < key.length; ++i)
                        if (!$util.isString(message.headers[key[i]]))
                            return "headers: string{k:string} expected";
                }
                if (message.body != null && $Object.hasOwnProperty.call(message, "body"))
                    if (!$util.isString(message.body))
                        return "body: string expected";
                return null;
            };

            /**
             * Creates a HttpResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.network.HttpResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.network.HttpResponse} HttpResponse
             */
            HttpResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.network.HttpResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.network.HttpResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.network.HttpResponse();
                if (object.status != null)
                    if ($Number(object.status) !== 0)
                        message.status = object.status | 0;
                if (object.headers) {
                    if (!$util.isObject(object.headers))
                        throw $TypeError(".lebai.network.HttpResponse.headers: object expected");
                    message.headers = {};
                    for (var keys = $Object.keys(object.headers), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.headers, keys[i]);
                        message.headers[keys[i]] = $String(object.headers[keys[i]]);
                    }
                }
                if (object.body != null)
                    if (typeof object.body !== "string" || object.body.length)
                        message.body = $String(object.body);
                return message;
            };

            /**
             * Creates a plain object from a HttpResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.network.HttpResponse
             * @static
             * @param {lebai.network.HttpResponse} message HttpResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HttpResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.objects || options.defaults)
                    object.headers = {};
                if (options.defaults) {
                    object.status = 0;
                    object.body = "";
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    object.status = message.status;
                var keys2;
                if (message.headers && (keys2 = $Object.keys(message.headers)).length) {
                    object.headers = {};
                    for (var j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.headers, keys2[j]);
                        object.headers[keys2[j]] = message.headers[keys2[j]];
                    }
                }
                if (message.body != null && $Object.hasOwnProperty.call(message, "body"))
                    object.body = message.body;
                return object;
            };

            /**
             * Converts this HttpResponse to JSON.
             * @function toJSON
             * @memberof lebai.network.HttpResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HttpResponse.prototype.toJSON = function() {
                return HttpResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for HttpResponse
             * @function getTypeUrl
             * @memberof lebai.network.HttpResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            HttpResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.network.HttpResponse";
            };

            return HttpResponse;
        })();

        network.NetworkService = (function() {

            /**
             * Constructs a new NetworkService service.
             * @memberof lebai.network
             * @classdesc Represents a NetworkService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var NetworkService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(NetworkService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: NetworkService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new NetworkService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.network.NetworkService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {NetworkService} RPC service. Useful where requests and/or responses are streamed.
             */
            NetworkService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.network.NetworkService#http}.
             * @memberof lebai.network.NetworkService
             * @typedef HttpCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.network.HttpResponse} [response] HttpResponse
             */

            /**
             * Calls Http.
             * @memberof lebai.network.NetworkService
             * @typedef Http
             * @type {{
             *   (request: lebai.network.IHttpRequest, callback: lebai.network.NetworkService.HttpCallback): void;
             *   (request: lebai.network.IHttpRequest): Promise<lebai.network.HttpResponse>;
             *   readonly name: "Http";
             *   readonly path: "/lebai.network.NetworkService/Http";
             *   readonly requestType: "HttpRequest";
             *   readonly responseType: "HttpResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls Http.
             * @name lebai.network.NetworkService#http
             * @type {lebai.network.NetworkService.Http}
             */
            $Object.defineProperties(NetworkService.prototype.http = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, NetworkService.prototype.http, $root.lebai.network.HttpRequest, $root.lebai.network.HttpResponse, request, callback);
            }, {
                name: { value: "Http" },
                path: { value: "/lebai.network.NetworkService/Http" },
                requestType: { value: "HttpRequest" },
                responseType: { value: "HttpResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return NetworkService;
        })();

        return network;
    })();

    lebai.plugin = (function() {

        /**
         * Namespace plugin.
         * @memberof lebai
         * @namespace
         */
        var plugin = {};

        plugin.PluginUrl = (function() {

            /**
             * Properties of a PluginUrl.
             * @typedef {Object} lebai.plugin.PluginUrl.$Properties
             * @property {string|null} [name] PluginUrl name
             * @property {string|null} [url] PluginUrl url
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PluginUrl.
             * @memberof lebai.plugin
             * @interface IPluginUrl
             * @augments lebai.plugin.PluginUrl.$Properties
             * @deprecated Use lebai.plugin.PluginUrl.$Properties instead.
             */

            /**
             * Shape of a PluginUrl.
             * @typedef {lebai.plugin.PluginUrl.$Properties} lebai.plugin.PluginUrl.$Shape
             */

            /**
             * Constructs a new PluginUrl.
             * @memberof lebai.plugin
             * @classdesc Represents a PluginUrl.
             * @constructor
             * @param {lebai.plugin.PluginUrl.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var PluginUrl = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PluginUrl name.
             * @member {string} name
             * @memberof lebai.plugin.PluginUrl
             * @instance
             */
            PluginUrl.prototype.name = "";

            /**
             * PluginUrl url.
             * @member {string} url
             * @memberof lebai.plugin.PluginUrl
             * @instance
             */
            PluginUrl.prototype.url = "";

            /**
             * Creates a new PluginUrl instance using the specified properties.
             * @function create
             * @memberof lebai.plugin.PluginUrl
             * @static
             * @param {lebai.plugin.PluginUrl.$Properties=} [properties] Properties to set
             * @returns {lebai.plugin.PluginUrl} PluginUrl instance
             * @type {{
             *   (properties: lebai.plugin.PluginUrl.$Shape): lebai.plugin.PluginUrl & lebai.plugin.PluginUrl.$Shape;
             *   (properties?: lebai.plugin.PluginUrl.$Properties): lebai.plugin.PluginUrl;
             * }}
             */
            PluginUrl.create = function(properties) {
                return new PluginUrl(properties);
            };

            /**
             * Encodes the specified PluginUrl message. Does not implicitly {@link lebai.plugin.PluginUrl.verify|verify} messages.
             * @function encode
             * @memberof lebai.plugin.PluginUrl
             * @static
             * @param {lebai.plugin.PluginUrl.$Properties} message PluginUrl message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PluginUrl.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.url != null && $Object.hasOwnProperty.call(message, "url") && message.url !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.url);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PluginUrl message, length delimited. Does not implicitly {@link lebai.plugin.PluginUrl.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.plugin.PluginUrl
             * @static
             * @param {lebai.plugin.PluginUrl.$Properties} message PluginUrl message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PluginUrl.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a PluginUrl message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.plugin.PluginUrl
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.plugin.PluginUrl & lebai.plugin.PluginUrl.$Shape} PluginUrl
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PluginUrl.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.plugin.PluginUrl();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.url = value;
                            else
                                delete message.url;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a PluginUrl message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.plugin.PluginUrl
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.plugin.PluginUrl & lebai.plugin.PluginUrl.$Shape} PluginUrl
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PluginUrl.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PluginUrl message.
             * @function verify
             * @memberof lebai.plugin.PluginUrl
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PluginUrl.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
                    if (!$util.isString(message.url))
                        return "url: string expected";
                return null;
            };

            /**
             * Creates a PluginUrl message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.plugin.PluginUrl
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.plugin.PluginUrl} PluginUrl
             */
            PluginUrl.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.plugin.PluginUrl)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.plugin.PluginUrl: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.plugin.PluginUrl();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.url != null)
                    if (typeof object.url !== "string" || object.url.length)
                        message.url = $String(object.url);
                return message;
            };

            /**
             * Creates a plain object from a PluginUrl message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.plugin.PluginUrl
             * @static
             * @param {lebai.plugin.PluginUrl} message PluginUrl
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PluginUrl.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.url = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                return object;
            };

            /**
             * Converts this PluginUrl to JSON.
             * @function toJSON
             * @memberof lebai.plugin.PluginUrl
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PluginUrl.prototype.toJSON = function() {
                return PluginUrl.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PluginUrl
             * @function getTypeUrl
             * @memberof lebai.plugin.PluginUrl
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PluginUrl.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.plugin.PluginUrl";
            };

            return PluginUrl;
        })();

        plugin.PluginStore = (function() {

            /**
             * Properties of a PluginStore.
             * @typedef {Object} lebai.plugin.PluginStore.$Properties
             * @property {Array.<lebai.plugin.PluginUrl.$Properties>|null} [plugins] PluginStore plugins
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PluginStore.
             * @memberof lebai.plugin
             * @interface IPluginStore
             * @augments lebai.plugin.PluginStore.$Properties
             * @deprecated Use lebai.plugin.PluginStore.$Properties instead.
             */

            /**
             * Shape of a PluginStore.
             * @typedef {lebai.plugin.PluginStore.$Properties} lebai.plugin.PluginStore.$Shape
             */

            /**
             * Constructs a new PluginStore.
             * @memberof lebai.plugin
             * @classdesc Represents a PluginStore.
             * @constructor
             * @param {lebai.plugin.PluginStore.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var PluginStore = function (properties) {
                this.plugins = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PluginStore plugins.
             * @member {Array.<lebai.plugin.PluginUrl.$Properties>} plugins
             * @memberof lebai.plugin.PluginStore
             * @instance
             */
            PluginStore.prototype.plugins = $util.emptyArray;

            /**
             * Creates a new PluginStore instance using the specified properties.
             * @function create
             * @memberof lebai.plugin.PluginStore
             * @static
             * @param {lebai.plugin.PluginStore.$Properties=} [properties] Properties to set
             * @returns {lebai.plugin.PluginStore} PluginStore instance
             * @type {{
             *   (properties: lebai.plugin.PluginStore.$Shape): lebai.plugin.PluginStore & lebai.plugin.PluginStore.$Shape;
             *   (properties?: lebai.plugin.PluginStore.$Properties): lebai.plugin.PluginStore;
             * }}
             */
            PluginStore.create = function(properties) {
                return new PluginStore(properties);
            };

            /**
             * Encodes the specified PluginStore message. Does not implicitly {@link lebai.plugin.PluginStore.verify|verify} messages.
             * @function encode
             * @memberof lebai.plugin.PluginStore
             * @static
             * @param {lebai.plugin.PluginStore.$Properties} message PluginStore message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PluginStore.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.plugins != null && message.plugins.length)
                    for (var i = 0; i < message.plugins.length; ++i)
                        $root.lebai.plugin.PluginUrl.encode(message.plugins[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PluginStore message, length delimited. Does not implicitly {@link lebai.plugin.PluginStore.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.plugin.PluginStore
             * @static
             * @param {lebai.plugin.PluginStore.$Properties} message PluginStore message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PluginStore.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a PluginStore message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.plugin.PluginStore
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.plugin.PluginStore & lebai.plugin.PluginStore.$Shape} PluginStore
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PluginStore.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.plugin.PluginStore();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.plugins && message.plugins.length))
                                message.plugins = [];
                            message.plugins.push($root.lebai.plugin.PluginUrl.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a PluginStore message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.plugin.PluginStore
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.plugin.PluginStore & lebai.plugin.PluginStore.$Shape} PluginStore
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PluginStore.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PluginStore message.
             * @function verify
             * @memberof lebai.plugin.PluginStore
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PluginStore.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.plugins != null && $Object.hasOwnProperty.call(message, "plugins")) {
                    if (!$Array.isArray(message.plugins))
                        return "plugins: array expected";
                    for (var i = 0; i < message.plugins.length; ++i) {
                        var error = $root.lebai.plugin.PluginUrl.verify(message.plugins[i], _depth + 1);
                        if (error)
                            return "plugins." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a PluginStore message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.plugin.PluginStore
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.plugin.PluginStore} PluginStore
             */
            PluginStore.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.plugin.PluginStore)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.plugin.PluginStore: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.plugin.PluginStore();
                if (object.plugins) {
                    if (!$Array.isArray(object.plugins))
                        throw $TypeError(".lebai.plugin.PluginStore.plugins: array expected");
                    message.plugins = $Array(object.plugins.length);
                    for (var i = 0; i < object.plugins.length; ++i) {
                        if (!$util.isObject(object.plugins[i]))
                            throw $TypeError(".lebai.plugin.PluginStore.plugins: object expected");
                        message.plugins[i] = $root.lebai.plugin.PluginUrl.fromObject(object.plugins[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a PluginStore message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.plugin.PluginStore
             * @static
             * @param {lebai.plugin.PluginStore} message PluginStore
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PluginStore.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.plugins = [];
                if (message.plugins && message.plugins.length) {
                    object.plugins = $Array(message.plugins.length);
                    for (var j = 0; j < message.plugins.length; ++j)
                        object.plugins[j] = $root.lebai.plugin.PluginUrl.toObject(message.plugins[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this PluginStore to JSON.
             * @function toJSON
             * @memberof lebai.plugin.PluginStore
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PluginStore.prototype.toJSON = function() {
                return PluginStore.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PluginStore
             * @function getTypeUrl
             * @memberof lebai.plugin.PluginStore
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PluginStore.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.plugin.PluginStore";
            };

            return PluginStore;
        })();

        plugin.PluginInfo = (function() {

            /**
             * Properties of a PluginInfo.
             * @typedef {Object} lebai.plugin.PluginInfo.$Properties
             * @property {string|null} [name] PluginInfo name
             * @property {Array.<string>|null} [boxs] PluginInfo boxs
             * @property {Array.<string>|null} [arms] PluginInfo arms
             * @property {string|null} [description] PluginInfo description
             * @property {string|null} [homepage] PluginInfo homepage
             * @property {boolean|null} [autoRestart] PluginInfo autoRestart
             * @property {boolean|null} [web] PluginInfo web
             * @property {boolean|null} [daemon] PluginInfo daemon
             * @property {boolean|null} [cmd] PluginInfo cmd
             * @property {boolean|null} [enable] PluginInfo enable
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PluginInfo.
             * @memberof lebai.plugin
             * @interface IPluginInfo
             * @augments lebai.plugin.PluginInfo.$Properties
             * @deprecated Use lebai.plugin.PluginInfo.$Properties instead.
             */

            /**
             * Shape of a PluginInfo.
             * @typedef {lebai.plugin.PluginInfo.$Properties} lebai.plugin.PluginInfo.$Shape
             */

            /**
             * Constructs a new PluginInfo.
             * @memberof lebai.plugin
             * @classdesc Represents a PluginInfo.
             * @constructor
             * @param {lebai.plugin.PluginInfo.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var PluginInfo = function (properties) {
                this.boxs = [];
                this.arms = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PluginInfo name.
             * @member {string} name
             * @memberof lebai.plugin.PluginInfo
             * @instance
             */
            PluginInfo.prototype.name = "";

            /**
             * PluginInfo boxs.
             * @member {Array.<string>} boxs
             * @memberof lebai.plugin.PluginInfo
             * @instance
             */
            PluginInfo.prototype.boxs = $util.emptyArray;

            /**
             * PluginInfo arms.
             * @member {Array.<string>} arms
             * @memberof lebai.plugin.PluginInfo
             * @instance
             */
            PluginInfo.prototype.arms = $util.emptyArray;

            /**
             * PluginInfo description.
             * @member {string} description
             * @memberof lebai.plugin.PluginInfo
             * @instance
             */
            PluginInfo.prototype.description = "";

            /**
             * PluginInfo homepage.
             * @member {string} homepage
             * @memberof lebai.plugin.PluginInfo
             * @instance
             */
            PluginInfo.prototype.homepage = "";

            /**
             * PluginInfo autoRestart.
             * @member {boolean} autoRestart
             * @memberof lebai.plugin.PluginInfo
             * @instance
             */
            PluginInfo.prototype.autoRestart = false;

            /**
             * PluginInfo web.
             * @member {boolean} web
             * @memberof lebai.plugin.PluginInfo
             * @instance
             */
            PluginInfo.prototype.web = false;

            /**
             * PluginInfo daemon.
             * @member {boolean} daemon
             * @memberof lebai.plugin.PluginInfo
             * @instance
             */
            PluginInfo.prototype.daemon = false;

            /**
             * PluginInfo cmd.
             * @member {boolean} cmd
             * @memberof lebai.plugin.PluginInfo
             * @instance
             */
            PluginInfo.prototype.cmd = false;

            /**
             * PluginInfo enable.
             * @member {boolean} enable
             * @memberof lebai.plugin.PluginInfo
             * @instance
             */
            PluginInfo.prototype.enable = false;

            /**
             * Creates a new PluginInfo instance using the specified properties.
             * @function create
             * @memberof lebai.plugin.PluginInfo
             * @static
             * @param {lebai.plugin.PluginInfo.$Properties=} [properties] Properties to set
             * @returns {lebai.plugin.PluginInfo} PluginInfo instance
             * @type {{
             *   (properties: lebai.plugin.PluginInfo.$Shape): lebai.plugin.PluginInfo & lebai.plugin.PluginInfo.$Shape;
             *   (properties?: lebai.plugin.PluginInfo.$Properties): lebai.plugin.PluginInfo;
             * }}
             */
            PluginInfo.create = function(properties) {
                return new PluginInfo(properties);
            };

            /**
             * Encodes the specified PluginInfo message. Does not implicitly {@link lebai.plugin.PluginInfo.verify|verify} messages.
             * @function encode
             * @memberof lebai.plugin.PluginInfo
             * @static
             * @param {lebai.plugin.PluginInfo.$Properties} message PluginInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PluginInfo.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.boxs != null && message.boxs.length)
                    for (var i = 0; i < message.boxs.length; ++i)
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.boxs[i]);
                if (message.arms != null && message.arms.length)
                    for (var i = 0; i < message.arms.length; ++i)
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.arms[i]);
                if (message.description != null && $Object.hasOwnProperty.call(message, "description") && message.description !== "")
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.description);
                if (message.homepage != null && $Object.hasOwnProperty.call(message, "homepage") && message.homepage !== "")
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.homepage);
                if (message.autoRestart != null && $Object.hasOwnProperty.call(message, "autoRestart") && message.autoRestart !== false)
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.autoRestart);
                if (message.web != null && $Object.hasOwnProperty.call(message, "web") && message.web !== false)
                    writer.uint32(/* id 82, wireType 0 =*/656).bool(message.web);
                if (message.daemon != null && $Object.hasOwnProperty.call(message, "daemon") && message.daemon !== false)
                    writer.uint32(/* id 83, wireType 0 =*/664).bool(message.daemon);
                if (message.cmd != null && $Object.hasOwnProperty.call(message, "cmd") && message.cmd !== false)
                    writer.uint32(/* id 84, wireType 0 =*/672).bool(message.cmd);
                if (message.enable != null && $Object.hasOwnProperty.call(message, "enable") && message.enable !== false)
                    writer.uint32(/* id 91, wireType 0 =*/728).bool(message.enable);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PluginInfo message, length delimited. Does not implicitly {@link lebai.plugin.PluginInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.plugin.PluginInfo
             * @static
             * @param {lebai.plugin.PluginInfo.$Properties} message PluginInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PluginInfo.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a PluginInfo message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.plugin.PluginInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.plugin.PluginInfo & lebai.plugin.PluginInfo.$Shape} PluginInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PluginInfo.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.plugin.PluginInfo();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.boxs && message.boxs.length))
                                message.boxs = [];
                            message.boxs.push(reader.stringVerify());
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.arms && message.arms.length))
                                message.arms = [];
                            message.arms.push(reader.stringVerify());
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.description = value;
                            else
                                delete message.description;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.homepage = value;
                            else
                                delete message.homepage;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.autoRestart = value;
                            else
                                delete message.autoRestart;
                            continue;
                        }
                    case 82: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.web = value;
                            else
                                delete message.web;
                            continue;
                        }
                    case 83: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.daemon = value;
                            else
                                delete message.daemon;
                            continue;
                        }
                    case 84: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.cmd = value;
                            else
                                delete message.cmd;
                            continue;
                        }
                    case 91: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.enable = value;
                            else
                                delete message.enable;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a PluginInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.plugin.PluginInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.plugin.PluginInfo & lebai.plugin.PluginInfo.$Shape} PluginInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PluginInfo.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PluginInfo message.
             * @function verify
             * @memberof lebai.plugin.PluginInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PluginInfo.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.boxs != null && $Object.hasOwnProperty.call(message, "boxs")) {
                    if (!$Array.isArray(message.boxs))
                        return "boxs: array expected";
                    for (var i = 0; i < message.boxs.length; ++i)
                        if (!$util.isString(message.boxs[i]))
                            return "boxs: string[] expected";
                }
                if (message.arms != null && $Object.hasOwnProperty.call(message, "arms")) {
                    if (!$Array.isArray(message.arms))
                        return "arms: array expected";
                    for (var i = 0; i < message.arms.length; ++i)
                        if (!$util.isString(message.arms[i]))
                            return "arms: string[] expected";
                }
                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                    if (!$util.isString(message.description))
                        return "description: string expected";
                if (message.homepage != null && $Object.hasOwnProperty.call(message, "homepage"))
                    if (!$util.isString(message.homepage))
                        return "homepage: string expected";
                if (message.autoRestart != null && $Object.hasOwnProperty.call(message, "autoRestart"))
                    if (typeof message.autoRestart !== "boolean")
                        return "autoRestart: boolean expected";
                if (message.web != null && $Object.hasOwnProperty.call(message, "web"))
                    if (typeof message.web !== "boolean")
                        return "web: boolean expected";
                if (message.daemon != null && $Object.hasOwnProperty.call(message, "daemon"))
                    if (typeof message.daemon !== "boolean")
                        return "daemon: boolean expected";
                if (message.cmd != null && $Object.hasOwnProperty.call(message, "cmd"))
                    if (typeof message.cmd !== "boolean")
                        return "cmd: boolean expected";
                if (message.enable != null && $Object.hasOwnProperty.call(message, "enable"))
                    if (typeof message.enable !== "boolean")
                        return "enable: boolean expected";
                return null;
            };

            /**
             * Creates a PluginInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.plugin.PluginInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.plugin.PluginInfo} PluginInfo
             */
            PluginInfo.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.plugin.PluginInfo)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.plugin.PluginInfo: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.plugin.PluginInfo();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.boxs) {
                    if (!$Array.isArray(object.boxs))
                        throw $TypeError(".lebai.plugin.PluginInfo.boxs: array expected");
                    message.boxs = $Array(object.boxs.length);
                    for (var i = 0; i < object.boxs.length; ++i)
                        message.boxs[i] = $String(object.boxs[i]);
                }
                if (object.arms) {
                    if (!$Array.isArray(object.arms))
                        throw $TypeError(".lebai.plugin.PluginInfo.arms: array expected");
                    message.arms = $Array(object.arms.length);
                    for (var i = 0; i < object.arms.length; ++i)
                        message.arms[i] = $String(object.arms[i]);
                }
                if (object.description != null)
                    if (typeof object.description !== "string" || object.description.length)
                        message.description = $String(object.description);
                if (object.homepage != null)
                    if (typeof object.homepage !== "string" || object.homepage.length)
                        message.homepage = $String(object.homepage);
                if (object.autoRestart != null)
                    if (object.autoRestart)
                        message.autoRestart = $Boolean(object.autoRestart);
                if (object.web != null)
                    if (object.web)
                        message.web = $Boolean(object.web);
                if (object.daemon != null)
                    if (object.daemon)
                        message.daemon = $Boolean(object.daemon);
                if (object.cmd != null)
                    if (object.cmd)
                        message.cmd = $Boolean(object.cmd);
                if (object.enable != null)
                    if (object.enable)
                        message.enable = $Boolean(object.enable);
                return message;
            };

            /**
             * Creates a plain object from a PluginInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.plugin.PluginInfo
             * @static
             * @param {lebai.plugin.PluginInfo} message PluginInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PluginInfo.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.boxs = [];
                    object.arms = [];
                }
                if (options.defaults) {
                    object.name = "";
                    object.description = "";
                    object.homepage = "";
                    object.autoRestart = false;
                    object.web = false;
                    object.daemon = false;
                    object.cmd = false;
                    object.enable = false;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.boxs && message.boxs.length) {
                    object.boxs = $Array(message.boxs.length);
                    for (var j = 0; j < message.boxs.length; ++j)
                        object.boxs[j] = message.boxs[j];
                }
                if (message.arms && message.arms.length) {
                    object.arms = $Array(message.arms.length);
                    for (var j = 0; j < message.arms.length; ++j)
                        object.arms[j] = message.arms[j];
                }
                if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.homepage != null && $Object.hasOwnProperty.call(message, "homepage"))
                    object.homepage = message.homepage;
                if (message.autoRestart != null && $Object.hasOwnProperty.call(message, "autoRestart"))
                    object.autoRestart = message.autoRestart;
                if (message.web != null && $Object.hasOwnProperty.call(message, "web"))
                    object.web = message.web;
                if (message.daemon != null && $Object.hasOwnProperty.call(message, "daemon"))
                    object.daemon = message.daemon;
                if (message.cmd != null && $Object.hasOwnProperty.call(message, "cmd"))
                    object.cmd = message.cmd;
                if (message.enable != null && $Object.hasOwnProperty.call(message, "enable"))
                    object.enable = message.enable;
                return object;
            };

            /**
             * Converts this PluginInfo to JSON.
             * @function toJSON
             * @memberof lebai.plugin.PluginInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PluginInfo.prototype.toJSON = function() {
                return PluginInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PluginInfo
             * @function getTypeUrl
             * @memberof lebai.plugin.PluginInfo
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PluginInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.plugin.PluginInfo";
            };

            return PluginInfo;
        })();

        plugin.PluginIndex = (function() {

            /**
             * Properties of a PluginIndex.
             * @typedef {Object} lebai.plugin.PluginIndex.$Properties
             * @property {string|null} [name] PluginIndex name
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a PluginIndex.
             * @memberof lebai.plugin
             * @interface IPluginIndex
             * @augments lebai.plugin.PluginIndex.$Properties
             * @deprecated Use lebai.plugin.PluginIndex.$Properties instead.
             */

            /**
             * Shape of a PluginIndex.
             * @typedef {lebai.plugin.PluginIndex.$Properties} lebai.plugin.PluginIndex.$Shape
             */

            /**
             * Constructs a new PluginIndex.
             * @memberof lebai.plugin
             * @classdesc Represents a PluginIndex.
             * @constructor
             * @param {lebai.plugin.PluginIndex.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var PluginIndex = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * PluginIndex name.
             * @member {string} name
             * @memberof lebai.plugin.PluginIndex
             * @instance
             */
            PluginIndex.prototype.name = "";

            /**
             * Creates a new PluginIndex instance using the specified properties.
             * @function create
             * @memberof lebai.plugin.PluginIndex
             * @static
             * @param {lebai.plugin.PluginIndex.$Properties=} [properties] Properties to set
             * @returns {lebai.plugin.PluginIndex} PluginIndex instance
             * @type {{
             *   (properties: lebai.plugin.PluginIndex.$Shape): lebai.plugin.PluginIndex & lebai.plugin.PluginIndex.$Shape;
             *   (properties?: lebai.plugin.PluginIndex.$Properties): lebai.plugin.PluginIndex;
             * }}
             */
            PluginIndex.create = function(properties) {
                return new PluginIndex(properties);
            };

            /**
             * Encodes the specified PluginIndex message. Does not implicitly {@link lebai.plugin.PluginIndex.verify|verify} messages.
             * @function encode
             * @memberof lebai.plugin.PluginIndex
             * @static
             * @param {lebai.plugin.PluginIndex.$Properties} message PluginIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PluginIndex.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PluginIndex message, length delimited. Does not implicitly {@link lebai.plugin.PluginIndex.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.plugin.PluginIndex
             * @static
             * @param {lebai.plugin.PluginIndex.$Properties} message PluginIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PluginIndex.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a PluginIndex message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.plugin.PluginIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.plugin.PluginIndex & lebai.plugin.PluginIndex.$Shape} PluginIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PluginIndex.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.plugin.PluginIndex();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a PluginIndex message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.plugin.PluginIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.plugin.PluginIndex & lebai.plugin.PluginIndex.$Shape} PluginIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PluginIndex.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PluginIndex message.
             * @function verify
             * @memberof lebai.plugin.PluginIndex
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PluginIndex.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                return null;
            };

            /**
             * Creates a PluginIndex message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.plugin.PluginIndex
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.plugin.PluginIndex} PluginIndex
             */
            PluginIndex.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.plugin.PluginIndex)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.plugin.PluginIndex: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.plugin.PluginIndex();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                return message;
            };

            /**
             * Creates a plain object from a PluginIndex message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.plugin.PluginIndex
             * @static
             * @param {lebai.plugin.PluginIndex} message PluginIndex
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PluginIndex.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.name = "";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                return object;
            };

            /**
             * Converts this PluginIndex to JSON.
             * @function toJSON
             * @memberof lebai.plugin.PluginIndex
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PluginIndex.prototype.toJSON = function() {
                return PluginIndex.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for PluginIndex
             * @function getTypeUrl
             * @memberof lebai.plugin.PluginIndex
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            PluginIndex.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.plugin.PluginIndex";
            };

            return PluginIndex;
        })();

        plugin.Plugins = (function() {

            /**
             * Properties of a Plugins.
             * @typedef {Object} lebai.plugin.Plugins.$Properties
             * @property {Array.<lebai.plugin.PluginInfo.$Properties>|null} [plugins] Plugins plugins
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Plugins.
             * @memberof lebai.plugin
             * @interface IPlugins
             * @augments lebai.plugin.Plugins.$Properties
             * @deprecated Use lebai.plugin.Plugins.$Properties instead.
             */

            /**
             * Shape of a Plugins.
             * @typedef {lebai.plugin.Plugins.$Properties} lebai.plugin.Plugins.$Shape
             */

            /**
             * Constructs a new Plugins.
             * @memberof lebai.plugin
             * @classdesc Represents a Plugins.
             * @constructor
             * @param {lebai.plugin.Plugins.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Plugins = function (properties) {
                this.plugins = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Plugins plugins.
             * @member {Array.<lebai.plugin.PluginInfo.$Properties>} plugins
             * @memberof lebai.plugin.Plugins
             * @instance
             */
            Plugins.prototype.plugins = $util.emptyArray;

            /**
             * Creates a new Plugins instance using the specified properties.
             * @function create
             * @memberof lebai.plugin.Plugins
             * @static
             * @param {lebai.plugin.Plugins.$Properties=} [properties] Properties to set
             * @returns {lebai.plugin.Plugins} Plugins instance
             * @type {{
             *   (properties: lebai.plugin.Plugins.$Shape): lebai.plugin.Plugins & lebai.plugin.Plugins.$Shape;
             *   (properties?: lebai.plugin.Plugins.$Properties): lebai.plugin.Plugins;
             * }}
             */
            Plugins.create = function(properties) {
                return new Plugins(properties);
            };

            /**
             * Encodes the specified Plugins message. Does not implicitly {@link lebai.plugin.Plugins.verify|verify} messages.
             * @function encode
             * @memberof lebai.plugin.Plugins
             * @static
             * @param {lebai.plugin.Plugins.$Properties} message Plugins message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Plugins.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.plugins != null && message.plugins.length)
                    for (var i = 0; i < message.plugins.length; ++i)
                        $root.lebai.plugin.PluginInfo.encode(message.plugins[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Plugins message, length delimited. Does not implicitly {@link lebai.plugin.Plugins.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.plugin.Plugins
             * @static
             * @param {lebai.plugin.Plugins.$Properties} message Plugins message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Plugins.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Plugins message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.plugin.Plugins
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.plugin.Plugins & lebai.plugin.Plugins.$Shape} Plugins
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Plugins.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.plugin.Plugins();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.plugins && message.plugins.length))
                                message.plugins = [];
                            message.plugins.push($root.lebai.plugin.PluginInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Plugins message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.plugin.Plugins
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.plugin.Plugins & lebai.plugin.Plugins.$Shape} Plugins
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Plugins.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Plugins message.
             * @function verify
             * @memberof lebai.plugin.Plugins
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Plugins.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.plugins != null && $Object.hasOwnProperty.call(message, "plugins")) {
                    if (!$Array.isArray(message.plugins))
                        return "plugins: array expected";
                    for (var i = 0; i < message.plugins.length; ++i) {
                        var error = $root.lebai.plugin.PluginInfo.verify(message.plugins[i], _depth + 1);
                        if (error)
                            return "plugins." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Plugins message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.plugin.Plugins
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.plugin.Plugins} Plugins
             */
            Plugins.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.plugin.Plugins)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.plugin.Plugins: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.plugin.Plugins();
                if (object.plugins) {
                    if (!$Array.isArray(object.plugins))
                        throw $TypeError(".lebai.plugin.Plugins.plugins: array expected");
                    message.plugins = $Array(object.plugins.length);
                    for (var i = 0; i < object.plugins.length; ++i) {
                        if (!$util.isObject(object.plugins[i]))
                            throw $TypeError(".lebai.plugin.Plugins.plugins: object expected");
                        message.plugins[i] = $root.lebai.plugin.PluginInfo.fromObject(object.plugins[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Plugins message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.plugin.Plugins
             * @static
             * @param {lebai.plugin.Plugins} message Plugins
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Plugins.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.plugins = [];
                if (message.plugins && message.plugins.length) {
                    object.plugins = $Array(message.plugins.length);
                    for (var j = 0; j < message.plugins.length; ++j)
                        object.plugins[j] = $root.lebai.plugin.PluginInfo.toObject(message.plugins[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this Plugins to JSON.
             * @function toJSON
             * @memberof lebai.plugin.Plugins
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Plugins.prototype.toJSON = function() {
                return Plugins.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Plugins
             * @function getTypeUrl
             * @memberof lebai.plugin.Plugins
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Plugins.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.plugin.Plugins";
            };

            return Plugins;
        })();

        plugin.RunPluginCmdRequest = (function() {

            /**
             * Properties of a RunPluginCmdRequest.
             * @typedef {Object} lebai.plugin.RunPluginCmdRequest.$Properties
             * @property {string|null} [name] RunPluginCmdRequest name
             * @property {Array.<string>|null} [params] RunPluginCmdRequest params
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a RunPluginCmdRequest.
             * @memberof lebai.plugin
             * @interface IRunPluginCmdRequest
             * @augments lebai.plugin.RunPluginCmdRequest.$Properties
             * @deprecated Use lebai.plugin.RunPluginCmdRequest.$Properties instead.
             */

            /**
             * Shape of a RunPluginCmdRequest.
             * @typedef {lebai.plugin.RunPluginCmdRequest.$Properties} lebai.plugin.RunPluginCmdRequest.$Shape
             */

            /**
             * Constructs a new RunPluginCmdRequest.
             * @memberof lebai.plugin
             * @classdesc Represents a RunPluginCmdRequest.
             * @constructor
             * @param {lebai.plugin.RunPluginCmdRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var RunPluginCmdRequest = function (properties) {
                this.params = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * RunPluginCmdRequest name.
             * @member {string} name
             * @memberof lebai.plugin.RunPluginCmdRequest
             * @instance
             */
            RunPluginCmdRequest.prototype.name = "";

            /**
             * RunPluginCmdRequest params.
             * @member {Array.<string>} params
             * @memberof lebai.plugin.RunPluginCmdRequest
             * @instance
             */
            RunPluginCmdRequest.prototype.params = $util.emptyArray;

            /**
             * Creates a new RunPluginCmdRequest instance using the specified properties.
             * @function create
             * @memberof lebai.plugin.RunPluginCmdRequest
             * @static
             * @param {lebai.plugin.RunPluginCmdRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.plugin.RunPluginCmdRequest} RunPluginCmdRequest instance
             * @type {{
             *   (properties: lebai.plugin.RunPluginCmdRequest.$Shape): lebai.plugin.RunPluginCmdRequest & lebai.plugin.RunPluginCmdRequest.$Shape;
             *   (properties?: lebai.plugin.RunPluginCmdRequest.$Properties): lebai.plugin.RunPluginCmdRequest;
             * }}
             */
            RunPluginCmdRequest.create = function(properties) {
                return new RunPluginCmdRequest(properties);
            };

            /**
             * Encodes the specified RunPluginCmdRequest message. Does not implicitly {@link lebai.plugin.RunPluginCmdRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.plugin.RunPluginCmdRequest
             * @static
             * @param {lebai.plugin.RunPluginCmdRequest.$Properties} message RunPluginCmdRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RunPluginCmdRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.params != null && message.params.length)
                    for (var i = 0; i < message.params.length; ++i)
                        writer.uint32(/* id 22, wireType 2 =*/178).string(message.params[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified RunPluginCmdRequest message, length delimited. Does not implicitly {@link lebai.plugin.RunPluginCmdRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.plugin.RunPluginCmdRequest
             * @static
             * @param {lebai.plugin.RunPluginCmdRequest.$Properties} message RunPluginCmdRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RunPluginCmdRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a RunPluginCmdRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.plugin.RunPluginCmdRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.plugin.RunPluginCmdRequest & lebai.plugin.RunPluginCmdRequest.$Shape} RunPluginCmdRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RunPluginCmdRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.plugin.RunPluginCmdRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 22: {
                            if (wireType !== 2)
                                break;
                            if (!(message.params && message.params.length))
                                message.params = [];
                            message.params.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a RunPluginCmdRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.plugin.RunPluginCmdRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.plugin.RunPluginCmdRequest & lebai.plugin.RunPluginCmdRequest.$Shape} RunPluginCmdRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RunPluginCmdRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RunPluginCmdRequest message.
             * @function verify
             * @memberof lebai.plugin.RunPluginCmdRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RunPluginCmdRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.params != null && $Object.hasOwnProperty.call(message, "params")) {
                    if (!$Array.isArray(message.params))
                        return "params: array expected";
                    for (var i = 0; i < message.params.length; ++i)
                        if (!$util.isString(message.params[i]))
                            return "params: string[] expected";
                }
                return null;
            };

            /**
             * Creates a RunPluginCmdRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.plugin.RunPluginCmdRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.plugin.RunPluginCmdRequest} RunPluginCmdRequest
             */
            RunPluginCmdRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.plugin.RunPluginCmdRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.plugin.RunPluginCmdRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.plugin.RunPluginCmdRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.params) {
                    if (!$Array.isArray(object.params))
                        throw $TypeError(".lebai.plugin.RunPluginCmdRequest.params: array expected");
                    message.params = $Array(object.params.length);
                    for (var i = 0; i < object.params.length; ++i)
                        message.params[i] = $String(object.params[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a RunPluginCmdRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.plugin.RunPluginCmdRequest
             * @static
             * @param {lebai.plugin.RunPluginCmdRequest} message RunPluginCmdRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RunPluginCmdRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.params = [];
                if (options.defaults)
                    object.name = "";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.params && message.params.length) {
                    object.params = $Array(message.params.length);
                    for (var j = 0; j < message.params.length; ++j)
                        object.params[j] = message.params[j];
                }
                return object;
            };

            /**
             * Converts this RunPluginCmdRequest to JSON.
             * @function toJSON
             * @memberof lebai.plugin.RunPluginCmdRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RunPluginCmdRequest.prototype.toJSON = function() {
                return RunPluginCmdRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for RunPluginCmdRequest
             * @function getTypeUrl
             * @memberof lebai.plugin.RunPluginCmdRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            RunPluginCmdRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.plugin.RunPluginCmdRequest";
            };

            return RunPluginCmdRequest;
        })();

        plugin.PluginService = (function() {

            /**
             * Constructs a new PluginService service.
             * @memberof lebai.plugin
             * @classdesc Represents a PluginService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var PluginService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(PluginService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: PluginService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new PluginService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.plugin.PluginService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {PluginService} RPC service. Useful where requests and/or responses are streamed.
             */
            PluginService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.plugin.PluginService#getPluginStore}.
             * @memberof lebai.plugin.PluginService
             * @typedef GetPluginStoreCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.plugin.PluginStore} [response] PluginStore
             */

            /**
             * Calls GetPluginStore.
             * @memberof lebai.plugin.PluginService
             * @typedef GetPluginStore
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.plugin.PluginService.GetPluginStoreCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.plugin.PluginStore>;
             *   readonly name: "GetPluginStore";
             *   readonly path: "/lebai.plugin.PluginService/GetPluginStore";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "PluginStore";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetPluginStore.
             * @name lebai.plugin.PluginService#getPluginStore
             * @type {lebai.plugin.PluginService.GetPluginStore}
             */
            $Object.defineProperties(PluginService.prototype.getPluginStore = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PluginService.prototype.getPluginStore, $root.google.protobuf.Empty, $root.lebai.plugin.PluginStore, request, callback);
            }, {
                name: { value: "GetPluginStore" },
                path: { value: "/lebai.plugin.PluginService/GetPluginStore" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "PluginStore" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.plugin.PluginService#loadPlugin}.
             * @memberof lebai.plugin.PluginService
             * @typedef LoadPluginCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.plugin.PluginInfo} [response] PluginInfo
             */

            /**
             * Calls LoadPlugin.
             * @memberof lebai.plugin.PluginService
             * @typedef LoadPlugin
             * @type {{
             *   (request: lebai.plugin.IPluginIndex, callback: lebai.plugin.PluginService.LoadPluginCallback): void;
             *   (request: lebai.plugin.IPluginIndex): Promise<lebai.plugin.PluginInfo>;
             *   readonly name: "LoadPlugin";
             *   readonly path: "/lebai.plugin.PluginService/LoadPlugin";
             *   readonly requestType: "PluginIndex";
             *   readonly responseType: "PluginInfo";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadPlugin.
             * @name lebai.plugin.PluginService#loadPlugin
             * @type {lebai.plugin.PluginService.LoadPlugin}
             */
            $Object.defineProperties(PluginService.prototype.loadPlugin = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PluginService.prototype.loadPlugin, $root.lebai.plugin.PluginIndex, $root.lebai.plugin.PluginInfo, request, callback);
            }, {
                name: { value: "LoadPlugin" },
                path: { value: "/lebai.plugin.PluginService/LoadPlugin" },
                requestType: { value: "PluginIndex" },
                responseType: { value: "PluginInfo" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.plugin.PluginService#loadPlugins}.
             * @memberof lebai.plugin.PluginService
             * @typedef LoadPluginsCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.plugin.Plugins} [response] Plugins
             */

            /**
             * Calls LoadPlugins.
             * @memberof lebai.plugin.PluginService
             * @typedef LoadPlugins
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.plugin.PluginService.LoadPluginsCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.plugin.Plugins>;
             *   readonly name: "LoadPlugins";
             *   readonly path: "/lebai.plugin.PluginService/LoadPlugins";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "Plugins";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadPlugins.
             * @name lebai.plugin.PluginService#loadPlugins
             * @type {lebai.plugin.PluginService.LoadPlugins}
             */
            $Object.defineProperties(PluginService.prototype.loadPlugins = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PluginService.prototype.loadPlugins, $root.google.protobuf.Empty, $root.lebai.plugin.Plugins, request, callback);
            }, {
                name: { value: "LoadPlugins" },
                path: { value: "/lebai.plugin.PluginService/LoadPlugins" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "Plugins" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.plugin.PluginService#enablePlugin}.
             * @memberof lebai.plugin.PluginService
             * @typedef EnablePluginCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.CommandStdout} [response] CommandStdout
             */

            /**
             * Calls EnablePlugin.
             * @memberof lebai.plugin.PluginService
             * @typedef EnablePlugin
             * @type {{
             *   (request: lebai.plugin.IPluginIndex, callback: lebai.plugin.PluginService.EnablePluginCallback): void;
             *   (request: lebai.plugin.IPluginIndex): Promise<lebai.CommandStdout>;
             *   readonly name: "EnablePlugin";
             *   readonly path: "/lebai.plugin.PluginService/EnablePlugin";
             *   readonly requestType: "PluginIndex";
             *   readonly responseType: "CommandStdout";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls EnablePlugin.
             * @name lebai.plugin.PluginService#enablePlugin
             * @type {lebai.plugin.PluginService.EnablePlugin}
             */
            $Object.defineProperties(PluginService.prototype.enablePlugin = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PluginService.prototype.enablePlugin, $root.lebai.plugin.PluginIndex, $root.lebai.CommandStdout, request, callback);
            }, {
                name: { value: "EnablePlugin" },
                path: { value: "/lebai.plugin.PluginService/EnablePlugin" },
                requestType: { value: "PluginIndex" },
                responseType: { value: "CommandStdout" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.plugin.PluginService#disablePlugin}.
             * @memberof lebai.plugin.PluginService
             * @typedef DisablePluginCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.CommandStdout} [response] CommandStdout
             */

            /**
             * Calls DisablePlugin.
             * @memberof lebai.plugin.PluginService
             * @typedef DisablePlugin
             * @type {{
             *   (request: lebai.plugin.IPluginIndex, callback: lebai.plugin.PluginService.DisablePluginCallback): void;
             *   (request: lebai.plugin.IPluginIndex): Promise<lebai.CommandStdout>;
             *   readonly name: "DisablePlugin";
             *   readonly path: "/lebai.plugin.PluginService/DisablePlugin";
             *   readonly requestType: "PluginIndex";
             *   readonly responseType: "CommandStdout";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls DisablePlugin.
             * @name lebai.plugin.PluginService#disablePlugin
             * @type {lebai.plugin.PluginService.DisablePlugin}
             */
            $Object.defineProperties(PluginService.prototype.disablePlugin = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PluginService.prototype.disablePlugin, $root.lebai.plugin.PluginIndex, $root.lebai.CommandStdout, request, callback);
            }, {
                name: { value: "DisablePlugin" },
                path: { value: "/lebai.plugin.PluginService/DisablePlugin" },
                requestType: { value: "PluginIndex" },
                responseType: { value: "CommandStdout" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.plugin.PluginService#restartPluginDaemon}.
             * @memberof lebai.plugin.PluginService
             * @typedef RestartPluginDaemonCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls RestartPluginDaemon.
             * @memberof lebai.plugin.PluginService
             * @typedef RestartPluginDaemon
             * @type {{
             *   (request: lebai.plugin.IPluginIndex, callback: lebai.plugin.PluginService.RestartPluginDaemonCallback): void;
             *   (request: lebai.plugin.IPluginIndex): Promise<google.protobuf.Empty>;
             *   readonly name: "RestartPluginDaemon";
             *   readonly path: "/lebai.plugin.PluginService/RestartPluginDaemon";
             *   readonly requestType: "PluginIndex";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls RestartPluginDaemon.
             * @name lebai.plugin.PluginService#restartPluginDaemon
             * @type {lebai.plugin.PluginService.RestartPluginDaemon}
             */
            $Object.defineProperties(PluginService.prototype.restartPluginDaemon = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PluginService.prototype.restartPluginDaemon, $root.lebai.plugin.PluginIndex, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "RestartPluginDaemon" },
                path: { value: "/lebai.plugin.PluginService/RestartPluginDaemon" },
                requestType: { value: "PluginIndex" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.plugin.PluginService#getPluginDaemonStdout}.
             * @memberof lebai.plugin.PluginService
             * @typedef GetPluginDaemonStdoutCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.CommandStdout} [response] CommandStdout
             */

            /**
             * Calls GetPluginDaemonStdout.
             * @memberof lebai.plugin.PluginService
             * @typedef GetPluginDaemonStdout
             * @type {{
             *   (request: lebai.plugin.IPluginIndex, callback: lebai.plugin.PluginService.GetPluginDaemonStdoutCallback): void;
             *   (request: lebai.plugin.IPluginIndex): Promise<lebai.CommandStdout>;
             *   readonly name: "GetPluginDaemonStdout";
             *   readonly path: "/lebai.plugin.PluginService/GetPluginDaemonStdout";
             *   readonly requestType: "PluginIndex";
             *   readonly responseType: "CommandStdout";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetPluginDaemonStdout.
             * @name lebai.plugin.PluginService#getPluginDaemonStdout
             * @type {lebai.plugin.PluginService.GetPluginDaemonStdout}
             */
            $Object.defineProperties(PluginService.prototype.getPluginDaemonStdout = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PluginService.prototype.getPluginDaemonStdout, $root.lebai.plugin.PluginIndex, $root.lebai.CommandStdout, request, callback);
            }, {
                name: { value: "GetPluginDaemonStdout" },
                path: { value: "/lebai.plugin.PluginService/GetPluginDaemonStdout" },
                requestType: { value: "PluginIndex" },
                responseType: { value: "CommandStdout" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.plugin.PluginService#runPluginCmd}.
             * @memberof lebai.plugin.PluginService
             * @typedef RunPluginCmdCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.CommandStdout} [response] CommandStdout
             */

            /**
             * Calls RunPluginCmd.
             * @memberof lebai.plugin.PluginService
             * @typedef RunPluginCmd
             * @type {{
             *   (request: lebai.plugin.IRunPluginCmdRequest, callback: lebai.plugin.PluginService.RunPluginCmdCallback): void;
             *   (request: lebai.plugin.IRunPluginCmdRequest): Promise<lebai.CommandStdout>;
             *   readonly name: "RunPluginCmd";
             *   readonly path: "/lebai.plugin.PluginService/RunPluginCmd";
             *   readonly requestType: "RunPluginCmdRequest";
             *   readonly responseType: "CommandStdout";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls RunPluginCmd.
             * @name lebai.plugin.PluginService#runPluginCmd
             * @type {lebai.plugin.PluginService.RunPluginCmd}
             */
            $Object.defineProperties(PluginService.prototype.runPluginCmd = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, PluginService.prototype.runPluginCmd, $root.lebai.plugin.RunPluginCmdRequest, $root.lebai.CommandStdout, request, callback);
            }, {
                name: { value: "RunPluginCmd" },
                path: { value: "/lebai.plugin.PluginService/RunPluginCmd" },
                requestType: { value: "RunPluginCmdRequest" },
                responseType: { value: "CommandStdout" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return PluginService;
        })();

        return plugin;
    })();

    lebai.quality = (function() {

        /**
         * Namespace quality.
         * @memberof lebai
         * @namespace
         */
        var quality = {};

        quality.Auth = (function() {

            /**
             * Properties of an Auth.
             * @typedef {Object} lebai.quality.Auth.$Properties
             * @property {string|null} [time] Auth time
             * @property {string|null} [auth] Auth auth
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an Auth.
             * @memberof lebai.quality
             * @interface IAuth
             * @augments lebai.quality.Auth.$Properties
             * @deprecated Use lebai.quality.Auth.$Properties instead.
             */

            /**
             * Shape of an Auth.
             * @typedef {lebai.quality.Auth.$Properties} lebai.quality.Auth.$Shape
             */

            /**
             * Constructs a new Auth.
             * @memberof lebai.quality
             * @classdesc Represents an Auth.
             * @constructor
             * @param {lebai.quality.Auth.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Auth = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Auth time.
             * @member {string} time
             * @memberof lebai.quality.Auth
             * @instance
             */
            Auth.prototype.time = "";

            /**
             * Auth auth.
             * @member {string} auth
             * @memberof lebai.quality.Auth
             * @instance
             */
            Auth.prototype.auth = "";

            /**
             * Creates a new Auth instance using the specified properties.
             * @function create
             * @memberof lebai.quality.Auth
             * @static
             * @param {lebai.quality.Auth.$Properties=} [properties] Properties to set
             * @returns {lebai.quality.Auth} Auth instance
             * @type {{
             *   (properties: lebai.quality.Auth.$Shape): lebai.quality.Auth & lebai.quality.Auth.$Shape;
             *   (properties?: lebai.quality.Auth.$Properties): lebai.quality.Auth;
             * }}
             */
            Auth.create = function(properties) {
                return new Auth(properties);
            };

            /**
             * Encodes the specified Auth message. Does not implicitly {@link lebai.quality.Auth.verify|verify} messages.
             * @function encode
             * @memberof lebai.quality.Auth
             * @static
             * @param {lebai.quality.Auth.$Properties} message Auth message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Auth.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.time != null && $Object.hasOwnProperty.call(message, "time") && message.time !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.time);
                if (message.auth != null && $Object.hasOwnProperty.call(message, "auth") && message.auth !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.auth);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Auth message, length delimited. Does not implicitly {@link lebai.quality.Auth.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.quality.Auth
             * @static
             * @param {lebai.quality.Auth.$Properties} message Auth message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Auth.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an Auth message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.quality.Auth
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.quality.Auth & lebai.quality.Auth.$Shape} Auth
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Auth.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.quality.Auth();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.time = value;
                            else
                                delete message.time;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.auth = value;
                            else
                                delete message.auth;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an Auth message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.quality.Auth
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.quality.Auth & lebai.quality.Auth.$Shape} Auth
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Auth.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an Auth message.
             * @function verify
             * @memberof lebai.quality.Auth
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Auth.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                    if (!$util.isString(message.time))
                        return "time: string expected";
                if (message.auth != null && $Object.hasOwnProperty.call(message, "auth"))
                    if (!$util.isString(message.auth))
                        return "auth: string expected";
                return null;
            };

            /**
             * Creates an Auth message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.quality.Auth
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.quality.Auth} Auth
             */
            Auth.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.quality.Auth)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.quality.Auth: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.quality.Auth();
                if (object.time != null)
                    if (typeof object.time !== "string" || object.time.length)
                        message.time = $String(object.time);
                if (object.auth != null)
                    if (typeof object.auth !== "string" || object.auth.length)
                        message.auth = $String(object.auth);
                return message;
            };

            /**
             * Creates a plain object from an Auth message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.quality.Auth
             * @static
             * @param {lebai.quality.Auth} message Auth
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Auth.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.time = "";
                    object.auth = "";
                }
                if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                    object.time = message.time;
                if (message.auth != null && $Object.hasOwnProperty.call(message, "auth"))
                    object.auth = message.auth;
                return object;
            };

            /**
             * Converts this Auth to JSON.
             * @function toJSON
             * @memberof lebai.quality.Auth
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Auth.prototype.toJSON = function() {
                return Auth.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Auth
             * @function getTypeUrl
             * @memberof lebai.quality.Auth
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Auth.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.quality.Auth";
            };

            return Auth;
        })();

        quality.EmptyRequest = (function() {

            /**
             * Properties of an EmptyRequest.
             * @typedef {Object} lebai.quality.EmptyRequest.$Properties
             * @property {lebai.quality.Auth.$Properties|null} [auth] EmptyRequest auth
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an EmptyRequest.
             * @memberof lebai.quality
             * @interface IEmptyRequest
             * @augments lebai.quality.EmptyRequest.$Properties
             * @deprecated Use lebai.quality.EmptyRequest.$Properties instead.
             */

            /**
             * Shape of an EmptyRequest.
             * @typedef {lebai.quality.EmptyRequest.$Properties} lebai.quality.EmptyRequest.$Shape
             */

            /**
             * Constructs a new EmptyRequest.
             * @memberof lebai.quality
             * @classdesc Represents an EmptyRequest.
             * @constructor
             * @param {lebai.quality.EmptyRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var EmptyRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * EmptyRequest auth.
             * @member {lebai.quality.Auth.$Properties|null|undefined} auth
             * @memberof lebai.quality.EmptyRequest
             * @instance
             */
            EmptyRequest.prototype.auth = null;

            /**
             * Creates a new EmptyRequest instance using the specified properties.
             * @function create
             * @memberof lebai.quality.EmptyRequest
             * @static
             * @param {lebai.quality.EmptyRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.quality.EmptyRequest} EmptyRequest instance
             * @type {{
             *   (properties: lebai.quality.EmptyRequest.$Shape): lebai.quality.EmptyRequest & lebai.quality.EmptyRequest.$Shape;
             *   (properties?: lebai.quality.EmptyRequest.$Properties): lebai.quality.EmptyRequest;
             * }}
             */
            EmptyRequest.create = function(properties) {
                return new EmptyRequest(properties);
            };

            /**
             * Encodes the specified EmptyRequest message. Does not implicitly {@link lebai.quality.EmptyRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.quality.EmptyRequest
             * @static
             * @param {lebai.quality.EmptyRequest.$Properties} message EmptyRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EmptyRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.auth != null && $Object.hasOwnProperty.call(message, "auth"))
                    $root.lebai.quality.Auth.encode(message.auth, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EmptyRequest message, length delimited. Does not implicitly {@link lebai.quality.EmptyRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.quality.EmptyRequest
             * @static
             * @param {lebai.quality.EmptyRequest.$Properties} message EmptyRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EmptyRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an EmptyRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.quality.EmptyRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.quality.EmptyRequest & lebai.quality.EmptyRequest.$Shape} EmptyRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EmptyRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.quality.EmptyRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.auth = $root.lebai.quality.Auth.decode(reader, reader.uint32(), $undefined, _depth + 1, message.auth);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an EmptyRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.quality.EmptyRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.quality.EmptyRequest & lebai.quality.EmptyRequest.$Shape} EmptyRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EmptyRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EmptyRequest message.
             * @function verify
             * @memberof lebai.quality.EmptyRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EmptyRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.auth != null && $Object.hasOwnProperty.call(message, "auth")) {
                    var error = $root.lebai.quality.Auth.verify(message.auth, _depth + 1);
                    if (error)
                        return "auth." + error;
                }
                return null;
            };

            /**
             * Creates an EmptyRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.quality.EmptyRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.quality.EmptyRequest} EmptyRequest
             */
            EmptyRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.quality.EmptyRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.quality.EmptyRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.quality.EmptyRequest();
                if (object.auth != null) {
                    if (!$util.isObject(object.auth))
                        throw $TypeError(".lebai.quality.EmptyRequest.auth: object expected");
                    message.auth = $root.lebai.quality.Auth.fromObject(object.auth, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from an EmptyRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.quality.EmptyRequest
             * @static
             * @param {lebai.quality.EmptyRequest} message EmptyRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EmptyRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.auth = null;
                if (message.auth != null && $Object.hasOwnProperty.call(message, "auth"))
                    object.auth = $root.lebai.quality.Auth.toObject(message.auth, options, _depth + 1);
                return object;
            };

            /**
             * Converts this EmptyRequest to JSON.
             * @function toJSON
             * @memberof lebai.quality.EmptyRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EmptyRequest.prototype.toJSON = function() {
                return EmptyRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for EmptyRequest
             * @function getTypeUrl
             * @memberof lebai.quality.EmptyRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            EmptyRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.quality.EmptyRequest";
            };

            return EmptyRequest;
        })();

        quality.BoxTestResponse = (function() {

            /**
             * Properties of a BoxTestResponse.
             * @typedef {Object} lebai.quality.BoxTestResponse.$Properties
             * @property {number|null} [status] BoxTestResponse status
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a BoxTestResponse.
             * @memberof lebai.quality
             * @interface IBoxTestResponse
             * @augments lebai.quality.BoxTestResponse.$Properties
             * @deprecated Use lebai.quality.BoxTestResponse.$Properties instead.
             */

            /**
             * Shape of a BoxTestResponse.
             * @typedef {lebai.quality.BoxTestResponse.$Properties} lebai.quality.BoxTestResponse.$Shape
             */

            /**
             * Constructs a new BoxTestResponse.
             * @memberof lebai.quality
             * @classdesc Represents a BoxTestResponse.
             * @constructor
             * @param {lebai.quality.BoxTestResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var BoxTestResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * BoxTestResponse status.
             * @member {number} status
             * @memberof lebai.quality.BoxTestResponse
             * @instance
             */
            BoxTestResponse.prototype.status = 0;

            /**
             * Creates a new BoxTestResponse instance using the specified properties.
             * @function create
             * @memberof lebai.quality.BoxTestResponse
             * @static
             * @param {lebai.quality.BoxTestResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.quality.BoxTestResponse} BoxTestResponse instance
             * @type {{
             *   (properties: lebai.quality.BoxTestResponse.$Shape): lebai.quality.BoxTestResponse & lebai.quality.BoxTestResponse.$Shape;
             *   (properties?: lebai.quality.BoxTestResponse.$Properties): lebai.quality.BoxTestResponse;
             * }}
             */
            BoxTestResponse.create = function(properties) {
                return new BoxTestResponse(properties);
            };

            /**
             * Encodes the specified BoxTestResponse message. Does not implicitly {@link lebai.quality.BoxTestResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.quality.BoxTestResponse
             * @static
             * @param {lebai.quality.BoxTestResponse.$Properties} message BoxTestResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BoxTestResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BoxTestResponse message, length delimited. Does not implicitly {@link lebai.quality.BoxTestResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.quality.BoxTestResponse
             * @static
             * @param {lebai.quality.BoxTestResponse.$Properties} message BoxTestResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BoxTestResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a BoxTestResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.quality.BoxTestResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.quality.BoxTestResponse & lebai.quality.BoxTestResponse.$Shape} BoxTestResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BoxTestResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.quality.BoxTestResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.status = value;
                            else
                                delete message.status;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a BoxTestResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.quality.BoxTestResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.quality.BoxTestResponse & lebai.quality.BoxTestResponse.$Shape} BoxTestResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BoxTestResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BoxTestResponse message.
             * @function verify
             * @memberof lebai.quality.BoxTestResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BoxTestResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    if (!$util.isInteger(message.status))
                        return "status: integer expected";
                return null;
            };

            /**
             * Creates a BoxTestResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.quality.BoxTestResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.quality.BoxTestResponse} BoxTestResponse
             */
            BoxTestResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.quality.BoxTestResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.quality.BoxTestResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.quality.BoxTestResponse();
                if (object.status != null)
                    if ($Number(object.status) !== 0)
                        message.status = object.status | 0;
                return message;
            };

            /**
             * Creates a plain object from a BoxTestResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.quality.BoxTestResponse
             * @static
             * @param {lebai.quality.BoxTestResponse} message BoxTestResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BoxTestResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.status = 0;
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    object.status = message.status;
                return object;
            };

            /**
             * Converts this BoxTestResponse to JSON.
             * @function toJSON
             * @memberof lebai.quality.BoxTestResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BoxTestResponse.prototype.toJSON = function() {
                return BoxTestResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for BoxTestResponse
             * @function getTypeUrl
             * @memberof lebai.quality.BoxTestResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            BoxTestResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.quality.BoxTestResponse";
            };

            return BoxTestResponse;
        })();

        quality.InitRobotRequest = (function() {

            /**
             * Properties of an InitRobotRequest.
             * @typedef {Object} lebai.quality.InitRobotRequest.$Properties
             * @property {lebai.quality.Auth.$Properties|null} [auth] InitRobotRequest auth
             * @property {lebai.system.RobotInfo.$Properties|null} [info] InitRobotRequest info
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an InitRobotRequest.
             * @memberof lebai.quality
             * @interface IInitRobotRequest
             * @augments lebai.quality.InitRobotRequest.$Properties
             * @deprecated Use lebai.quality.InitRobotRequest.$Properties instead.
             */

            /**
             * Shape of an InitRobotRequest.
             * @typedef {lebai.quality.InitRobotRequest.$Properties} lebai.quality.InitRobotRequest.$Shape
             */

            /**
             * Constructs a new InitRobotRequest.
             * @memberof lebai.quality
             * @classdesc Represents an InitRobotRequest.
             * @constructor
             * @param {lebai.quality.InitRobotRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var InitRobotRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * InitRobotRequest auth.
             * @member {lebai.quality.Auth.$Properties|null|undefined} auth
             * @memberof lebai.quality.InitRobotRequest
             * @instance
             */
            InitRobotRequest.prototype.auth = null;

            /**
             * InitRobotRequest info.
             * @member {lebai.system.RobotInfo.$Properties|null|undefined} info
             * @memberof lebai.quality.InitRobotRequest
             * @instance
             */
            InitRobotRequest.prototype.info = null;

            /**
             * Creates a new InitRobotRequest instance using the specified properties.
             * @function create
             * @memberof lebai.quality.InitRobotRequest
             * @static
             * @param {lebai.quality.InitRobotRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.quality.InitRobotRequest} InitRobotRequest instance
             * @type {{
             *   (properties: lebai.quality.InitRobotRequest.$Shape): lebai.quality.InitRobotRequest & lebai.quality.InitRobotRequest.$Shape;
             *   (properties?: lebai.quality.InitRobotRequest.$Properties): lebai.quality.InitRobotRequest;
             * }}
             */
            InitRobotRequest.create = function(properties) {
                return new InitRobotRequest(properties);
            };

            /**
             * Encodes the specified InitRobotRequest message. Does not implicitly {@link lebai.quality.InitRobotRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.quality.InitRobotRequest
             * @static
             * @param {lebai.quality.InitRobotRequest.$Properties} message InitRobotRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InitRobotRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.auth != null && $Object.hasOwnProperty.call(message, "auth"))
                    $root.lebai.quality.Auth.encode(message.auth, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.info != null && $Object.hasOwnProperty.call(message, "info"))
                    $root.lebai.system.RobotInfo.encode(message.info, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified InitRobotRequest message, length delimited. Does not implicitly {@link lebai.quality.InitRobotRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.quality.InitRobotRequest
             * @static
             * @param {lebai.quality.InitRobotRequest.$Properties} message InitRobotRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InitRobotRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an InitRobotRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.quality.InitRobotRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.quality.InitRobotRequest & lebai.quality.InitRobotRequest.$Shape} InitRobotRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InitRobotRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.quality.InitRobotRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.auth = $root.lebai.quality.Auth.decode(reader, reader.uint32(), $undefined, _depth + 1, message.auth);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.info = $root.lebai.system.RobotInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.info);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an InitRobotRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.quality.InitRobotRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.quality.InitRobotRequest & lebai.quality.InitRobotRequest.$Shape} InitRobotRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InitRobotRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an InitRobotRequest message.
             * @function verify
             * @memberof lebai.quality.InitRobotRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            InitRobotRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.auth != null && $Object.hasOwnProperty.call(message, "auth")) {
                    var error = $root.lebai.quality.Auth.verify(message.auth, _depth + 1);
                    if (error)
                        return "auth." + error;
                }
                if (message.info != null && $Object.hasOwnProperty.call(message, "info")) {
                    var error = $root.lebai.system.RobotInfo.verify(message.info, _depth + 1);
                    if (error)
                        return "info." + error;
                }
                return null;
            };

            /**
             * Creates an InitRobotRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.quality.InitRobotRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.quality.InitRobotRequest} InitRobotRequest
             */
            InitRobotRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.quality.InitRobotRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.quality.InitRobotRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.quality.InitRobotRequest();
                if (object.auth != null) {
                    if (!$util.isObject(object.auth))
                        throw $TypeError(".lebai.quality.InitRobotRequest.auth: object expected");
                    message.auth = $root.lebai.quality.Auth.fromObject(object.auth, _depth + 1);
                }
                if (object.info != null) {
                    if (!$util.isObject(object.info))
                        throw $TypeError(".lebai.quality.InitRobotRequest.info: object expected");
                    message.info = $root.lebai.system.RobotInfo.fromObject(object.info, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from an InitRobotRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.quality.InitRobotRequest
             * @static
             * @param {lebai.quality.InitRobotRequest} message InitRobotRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            InitRobotRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.auth = null;
                    object.info = null;
                }
                if (message.auth != null && $Object.hasOwnProperty.call(message, "auth"))
                    object.auth = $root.lebai.quality.Auth.toObject(message.auth, options, _depth + 1);
                if (message.info != null && $Object.hasOwnProperty.call(message, "info"))
                    object.info = $root.lebai.system.RobotInfo.toObject(message.info, options, _depth + 1);
                return object;
            };

            /**
             * Converts this InitRobotRequest to JSON.
             * @function toJSON
             * @memberof lebai.quality.InitRobotRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            InitRobotRequest.prototype.toJSON = function() {
                return InitRobotRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for InitRobotRequest
             * @function getTypeUrl
             * @memberof lebai.quality.InitRobotRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            InitRobotRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.quality.InitRobotRequest";
            };

            return InitRobotRequest;
        })();

        quality.InitRobotResponse = (function() {

            /**
             * Properties of an InitRobotResponse.
             * @typedef {Object} lebai.quality.InitRobotResponse.$Properties
             * @property {string|null} [cup] InitRobotResponse cup
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an InitRobotResponse.
             * @memberof lebai.quality
             * @interface IInitRobotResponse
             * @augments lebai.quality.InitRobotResponse.$Properties
             * @deprecated Use lebai.quality.InitRobotResponse.$Properties instead.
             */

            /**
             * Shape of an InitRobotResponse.
             * @typedef {lebai.quality.InitRobotResponse.$Properties} lebai.quality.InitRobotResponse.$Shape
             */

            /**
             * Constructs a new InitRobotResponse.
             * @memberof lebai.quality
             * @classdesc Represents an InitRobotResponse.
             * @constructor
             * @param {lebai.quality.InitRobotResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var InitRobotResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * InitRobotResponse cup.
             * @member {string} cup
             * @memberof lebai.quality.InitRobotResponse
             * @instance
             */
            InitRobotResponse.prototype.cup = "";

            /**
             * Creates a new InitRobotResponse instance using the specified properties.
             * @function create
             * @memberof lebai.quality.InitRobotResponse
             * @static
             * @param {lebai.quality.InitRobotResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.quality.InitRobotResponse} InitRobotResponse instance
             * @type {{
             *   (properties: lebai.quality.InitRobotResponse.$Shape): lebai.quality.InitRobotResponse & lebai.quality.InitRobotResponse.$Shape;
             *   (properties?: lebai.quality.InitRobotResponse.$Properties): lebai.quality.InitRobotResponse;
             * }}
             */
            InitRobotResponse.create = function(properties) {
                return new InitRobotResponse(properties);
            };

            /**
             * Encodes the specified InitRobotResponse message. Does not implicitly {@link lebai.quality.InitRobotResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.quality.InitRobotResponse
             * @static
             * @param {lebai.quality.InitRobotResponse.$Properties} message InitRobotResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InitRobotResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.cup != null && $Object.hasOwnProperty.call(message, "cup") && message.cup !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.cup);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified InitRobotResponse message, length delimited. Does not implicitly {@link lebai.quality.InitRobotResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.quality.InitRobotResponse
             * @static
             * @param {lebai.quality.InitRobotResponse.$Properties} message InitRobotResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InitRobotResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an InitRobotResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.quality.InitRobotResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.quality.InitRobotResponse & lebai.quality.InitRobotResponse.$Shape} InitRobotResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InitRobotResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.quality.InitRobotResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.cup = value;
                            else
                                delete message.cup;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an InitRobotResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.quality.InitRobotResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.quality.InitRobotResponse & lebai.quality.InitRobotResponse.$Shape} InitRobotResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InitRobotResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an InitRobotResponse message.
             * @function verify
             * @memberof lebai.quality.InitRobotResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            InitRobotResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.cup != null && $Object.hasOwnProperty.call(message, "cup"))
                    if (!$util.isString(message.cup))
                        return "cup: string expected";
                return null;
            };

            /**
             * Creates an InitRobotResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.quality.InitRobotResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.quality.InitRobotResponse} InitRobotResponse
             */
            InitRobotResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.quality.InitRobotResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.quality.InitRobotResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.quality.InitRobotResponse();
                if (object.cup != null)
                    if (typeof object.cup !== "string" || object.cup.length)
                        message.cup = $String(object.cup);
                return message;
            };

            /**
             * Creates a plain object from an InitRobotResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.quality.InitRobotResponse
             * @static
             * @param {lebai.quality.InitRobotResponse} message InitRobotResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            InitRobotResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.cup = "";
                if (message.cup != null && $Object.hasOwnProperty.call(message, "cup"))
                    object.cup = message.cup;
                return object;
            };

            /**
             * Converts this InitRobotResponse to JSON.
             * @function toJSON
             * @memberof lebai.quality.InitRobotResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            InitRobotResponse.prototype.toJSON = function() {
                return InitRobotResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for InitRobotResponse
             * @function getTypeUrl
             * @memberof lebai.quality.InitRobotResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            InitRobotResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.quality.InitRobotResponse";
            };

            return InitRobotResponse;
        })();

        quality.QualityService = (function() {

            /**
             * Constructs a new QualityService service.
             * @memberof lebai.quality
             * @classdesc Represents a QualityService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var QualityService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(QualityService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: QualityService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new QualityService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.quality.QualityService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {QualityService} RPC service. Useful where requests and/or responses are streamed.
             */
            QualityService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.quality.QualityService#boxTest}.
             * @memberof lebai.quality.QualityService
             * @typedef BoxTestCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.quality.BoxTestResponse} [response] BoxTestResponse
             */

            /**
             * Calls BoxTest.
             * @memberof lebai.quality.QualityService
             * @typedef BoxTest
             * @type {{
             *   (request: lebai.quality.IEmptyRequest, callback: lebai.quality.QualityService.BoxTestCallback): void;
             *   (request: lebai.quality.IEmptyRequest): Promise<lebai.quality.BoxTestResponse>;
             *   readonly name: "BoxTest";
             *   readonly path: "/lebai.quality.QualityService/BoxTest";
             *   readonly requestType: "EmptyRequest";
             *   readonly responseType: "BoxTestResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls BoxTest.
             * @name lebai.quality.QualityService#boxTest
             * @type {lebai.quality.QualityService.BoxTest}
             */
            $Object.defineProperties(QualityService.prototype.boxTest = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, QualityService.prototype.boxTest, $root.lebai.quality.EmptyRequest, $root.lebai.quality.BoxTestResponse, request, callback);
            }, {
                name: { value: "BoxTest" },
                path: { value: "/lebai.quality.QualityService/BoxTest" },
                requestType: { value: "EmptyRequest" },
                responseType: { value: "BoxTestResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.quality.QualityService#initRobot}.
             * @memberof lebai.quality.QualityService
             * @typedef InitRobotCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.quality.InitRobotResponse} [response] InitRobotResponse
             */

            /**
             * Calls InitRobot.
             * @memberof lebai.quality.QualityService
             * @typedef InitRobot
             * @type {{
             *   (request: lebai.quality.IInitRobotRequest, callback: lebai.quality.QualityService.InitRobotCallback): void;
             *   (request: lebai.quality.IInitRobotRequest): Promise<lebai.quality.InitRobotResponse>;
             *   readonly name: "InitRobot";
             *   readonly path: "/lebai.quality.QualityService/InitRobot";
             *   readonly requestType: "InitRobotRequest";
             *   readonly responseType: "InitRobotResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls InitRobot.
             * @name lebai.quality.QualityService#initRobot
             * @type {lebai.quality.QualityService.InitRobot}
             */
            $Object.defineProperties(QualityService.prototype.initRobot = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, QualityService.prototype.initRobot, $root.lebai.quality.InitRobotRequest, $root.lebai.quality.InitRobotResponse, request, callback);
            }, {
                name: { value: "InitRobot" },
                path: { value: "/lebai.quality.QualityService/InitRobot" },
                requestType: { value: "InitRobotRequest" },
                responseType: { value: "InitRobotResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return QualityService;
        })();

        return quality;
    })();

    lebai.safety = (function() {

        /**
         * Namespace safety.
         * @memberof lebai
         * @namespace
         */
        var safety = {};

        safety.CollisionTorqueDiff = (function() {

            /**
             * Properties of a CollisionTorqueDiff.
             * @typedef {Object} lebai.safety.CollisionTorqueDiff.$Properties
             * @property {Array.<number>|null} [diffs] CollisionTorqueDiff diffs
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CollisionTorqueDiff.
             * @memberof lebai.safety
             * @interface ICollisionTorqueDiff
             * @augments lebai.safety.CollisionTorqueDiff.$Properties
             * @deprecated Use lebai.safety.CollisionTorqueDiff.$Properties instead.
             */

            /**
             * Shape of a CollisionTorqueDiff.
             * @typedef {lebai.safety.CollisionTorqueDiff.$Properties} lebai.safety.CollisionTorqueDiff.$Shape
             */

            /**
             * Constructs a new CollisionTorqueDiff.
             * @memberof lebai.safety
             * @classdesc Represents a CollisionTorqueDiff.
             * @constructor
             * @param {lebai.safety.CollisionTorqueDiff.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var CollisionTorqueDiff = function (properties) {
                this.diffs = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CollisionTorqueDiff diffs.
             * @member {Array.<number>} diffs
             * @memberof lebai.safety.CollisionTorqueDiff
             * @instance
             */
            CollisionTorqueDiff.prototype.diffs = $util.emptyArray;

            /**
             * Creates a new CollisionTorqueDiff instance using the specified properties.
             * @function create
             * @memberof lebai.safety.CollisionTorqueDiff
             * @static
             * @param {lebai.safety.CollisionTorqueDiff.$Properties=} [properties] Properties to set
             * @returns {lebai.safety.CollisionTorqueDiff} CollisionTorqueDiff instance
             * @type {{
             *   (properties: lebai.safety.CollisionTorqueDiff.$Shape): lebai.safety.CollisionTorqueDiff & lebai.safety.CollisionTorqueDiff.$Shape;
             *   (properties?: lebai.safety.CollisionTorqueDiff.$Properties): lebai.safety.CollisionTorqueDiff;
             * }}
             */
            CollisionTorqueDiff.create = function(properties) {
                return new CollisionTorqueDiff(properties);
            };

            /**
             * Encodes the specified CollisionTorqueDiff message. Does not implicitly {@link lebai.safety.CollisionTorqueDiff.verify|verify} messages.
             * @function encode
             * @memberof lebai.safety.CollisionTorqueDiff
             * @static
             * @param {lebai.safety.CollisionTorqueDiff.$Properties} message CollisionTorqueDiff message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CollisionTorqueDiff.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.diffs != null && message.diffs.length)
                    writer.uint32(/* id 1, wireType 2 =*/10).doubles(message.diffs);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CollisionTorqueDiff message, length delimited. Does not implicitly {@link lebai.safety.CollisionTorqueDiff.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.safety.CollisionTorqueDiff
             * @static
             * @param {lebai.safety.CollisionTorqueDiff.$Properties} message CollisionTorqueDiff message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CollisionTorqueDiff.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a CollisionTorqueDiff message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.safety.CollisionTorqueDiff
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.safety.CollisionTorqueDiff & lebai.safety.CollisionTorqueDiff.$Shape} CollisionTorqueDiff
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CollisionTorqueDiff.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.safety.CollisionTorqueDiff();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.diffs && message.diffs.length))
                                    message.diffs = [];
                                reader.doubles(message.diffs);
                                continue;
                            }
                            if (wireType !== 1)
                                break;
                            if (!(message.diffs && message.diffs.length))
                                message.diffs = [];
                            message.diffs.push(reader.double());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a CollisionTorqueDiff message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.safety.CollisionTorqueDiff
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.safety.CollisionTorqueDiff & lebai.safety.CollisionTorqueDiff.$Shape} CollisionTorqueDiff
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CollisionTorqueDiff.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CollisionTorqueDiff message.
             * @function verify
             * @memberof lebai.safety.CollisionTorqueDiff
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CollisionTorqueDiff.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.diffs != null && $Object.hasOwnProperty.call(message, "diffs")) {
                    if (!$Array.isArray(message.diffs))
                        return "diffs: array expected";
                    for (var i = 0; i < message.diffs.length; ++i)
                        if (typeof message.diffs[i] !== "number")
                            return "diffs: number[] expected";
                }
                return null;
            };

            /**
             * Creates a CollisionTorqueDiff message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.safety.CollisionTorqueDiff
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.safety.CollisionTorqueDiff} CollisionTorqueDiff
             */
            CollisionTorqueDiff.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.safety.CollisionTorqueDiff)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.safety.CollisionTorqueDiff: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.safety.CollisionTorqueDiff();
                if (object.diffs) {
                    if (!$Array.isArray(object.diffs))
                        throw $TypeError(".lebai.safety.CollisionTorqueDiff.diffs: array expected");
                    message.diffs = $Array(object.diffs.length);
                    for (var i = 0; i < object.diffs.length; ++i)
                        message.diffs[i] = $Number(object.diffs[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a CollisionTorqueDiff message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.safety.CollisionTorqueDiff
             * @static
             * @param {lebai.safety.CollisionTorqueDiff} message CollisionTorqueDiff
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CollisionTorqueDiff.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.diffs = [];
                if (message.diffs && message.diffs.length) {
                    object.diffs = $Array(message.diffs.length);
                    for (var j = 0; j < message.diffs.length; ++j)
                        object.diffs[j] = options.json && !$isFinite(message.diffs[j]) ? $String(message.diffs[j]) : message.diffs[j];
                }
                return object;
            };

            /**
             * Converts this CollisionTorqueDiff to JSON.
             * @function toJSON
             * @memberof lebai.safety.CollisionTorqueDiff
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CollisionTorqueDiff.prototype.toJSON = function() {
                return CollisionTorqueDiff.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CollisionTorqueDiff
             * @function getTypeUrl
             * @memberof lebai.safety.CollisionTorqueDiff
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CollisionTorqueDiff.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.safety.CollisionTorqueDiff";
            };

            return CollisionTorqueDiff;
        })();

        /**
         * CollisionDetectorAction enum.
         * @name lebai.safety.CollisionDetectorAction
         * @enum {number}
         * @property {number} ESTOP=0 ESTOP value
         * @property {number} PAUSE=1 PAUSE value
         * @property {number} STOP_MOVE=2 STOP_MOVE value
         * @property {number} NONE=99 NONE value
         */
        safety.CollisionDetectorAction = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "ESTOP"] = 0;
            values[valuesById[1] = "PAUSE"] = 1;
            values[valuesById[2] = "STOP_MOVE"] = 2;
            values[valuesById[99] = "NONE"] = 99;
            return values;
        })();

        safety.CollisionDetector = (function() {

            /**
             * Properties of a CollisionDetector.
             * @typedef {Object} lebai.safety.CollisionDetector.$Properties
             * @property {lebai.safety.CollisionDetectorAction|null} [action] CollisionDetector action
             * @property {number|null} [pauseTime] CollisionDetector pauseTime
             * @property {number|null} [sensitivity] CollisionDetector sensitivity
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CollisionDetector.
             * @memberof lebai.safety
             * @interface ICollisionDetector
             * @augments lebai.safety.CollisionDetector.$Properties
             * @deprecated Use lebai.safety.CollisionDetector.$Properties instead.
             */

            /**
             * Shape of a CollisionDetector.
             * @typedef {lebai.safety.CollisionDetector.$Properties} lebai.safety.CollisionDetector.$Shape
             */

            /**
             * Constructs a new CollisionDetector.
             * @memberof lebai.safety
             * @classdesc Represents a CollisionDetector.
             * @constructor
             * @param {lebai.safety.CollisionDetector.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var CollisionDetector = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CollisionDetector action.
             * @member {lebai.safety.CollisionDetectorAction} action
             * @memberof lebai.safety.CollisionDetector
             * @instance
             */
            CollisionDetector.prototype.action = 0;

            /**
             * CollisionDetector pauseTime.
             * @member {number} pauseTime
             * @memberof lebai.safety.CollisionDetector
             * @instance
             */
            CollisionDetector.prototype.pauseTime = 0;

            /**
             * CollisionDetector sensitivity.
             * @member {number} sensitivity
             * @memberof lebai.safety.CollisionDetector
             * @instance
             */
            CollisionDetector.prototype.sensitivity = 0;

            /**
             * Creates a new CollisionDetector instance using the specified properties.
             * @function create
             * @memberof lebai.safety.CollisionDetector
             * @static
             * @param {lebai.safety.CollisionDetector.$Properties=} [properties] Properties to set
             * @returns {lebai.safety.CollisionDetector} CollisionDetector instance
             * @type {{
             *   (properties: lebai.safety.CollisionDetector.$Shape): lebai.safety.CollisionDetector & lebai.safety.CollisionDetector.$Shape;
             *   (properties?: lebai.safety.CollisionDetector.$Properties): lebai.safety.CollisionDetector;
             * }}
             */
            CollisionDetector.create = function(properties) {
                return new CollisionDetector(properties);
            };

            /**
             * Encodes the specified CollisionDetector message. Does not implicitly {@link lebai.safety.CollisionDetector.verify|verify} messages.
             * @function encode
             * @memberof lebai.safety.CollisionDetector
             * @static
             * @param {lebai.safety.CollisionDetector.$Properties} message CollisionDetector message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CollisionDetector.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.action != null && $Object.hasOwnProperty.call(message, "action") && message.action !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.action);
                if (message.pauseTime != null && $Object.hasOwnProperty.call(message, "pauseTime") && message.pauseTime !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.pauseTime);
                if (message.sensitivity != null && $Object.hasOwnProperty.call(message, "sensitivity") && message.sensitivity !== 0)
                    writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.sensitivity);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CollisionDetector message, length delimited. Does not implicitly {@link lebai.safety.CollisionDetector.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.safety.CollisionDetector
             * @static
             * @param {lebai.safety.CollisionDetector.$Properties} message CollisionDetector message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CollisionDetector.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a CollisionDetector message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.safety.CollisionDetector
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.safety.CollisionDetector & lebai.safety.CollisionDetector.$Shape} CollisionDetector
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CollisionDetector.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.safety.CollisionDetector();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.action = value;
                            else
                                delete message.action;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pauseTime = value;
                            else
                                delete message.pauseTime;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.sensitivity = value;
                            else
                                delete message.sensitivity;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a CollisionDetector message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.safety.CollisionDetector
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.safety.CollisionDetector & lebai.safety.CollisionDetector.$Shape} CollisionDetector
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CollisionDetector.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CollisionDetector message.
             * @function verify
             * @memberof lebai.safety.CollisionDetector
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CollisionDetector.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.action != null && $Object.hasOwnProperty.call(message, "action"))
                    if (typeof message.action !== "number" || (message.action | 0) !== message.action)
                        return "action: enum value expected";
                if (message.pauseTime != null && $Object.hasOwnProperty.call(message, "pauseTime"))
                    if (!$util.isInteger(message.pauseTime))
                        return "pauseTime: integer expected";
                if (message.sensitivity != null && $Object.hasOwnProperty.call(message, "sensitivity"))
                    if (!$util.isInteger(message.sensitivity))
                        return "sensitivity: integer expected";
                return null;
            };

            /**
             * Creates a CollisionDetector message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.safety.CollisionDetector
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.safety.CollisionDetector} CollisionDetector
             */
            CollisionDetector.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.safety.CollisionDetector)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.safety.CollisionDetector: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.safety.CollisionDetector();
                if (object.action !== 0 && (typeof object.action !== "string" || $root.lebai.safety.CollisionDetectorAction[object.action] !== 0))
                    switch (object.action) {
                    case "ESTOP":
                    case 0:
                        message.action = 0;
                        break;
                    case "PAUSE":
                    case 1:
                        message.action = 1;
                        break;
                    case "STOP_MOVE":
                    case 2:
                        message.action = 2;
                        break;
                    case "NONE":
                    case 99:
                        message.action = 99;
                        break;
                    default:
                        if (typeof object.action === "number" && (object.action | 0) === object.action)
                            message.action = object.action;
                    }
                if (object.pauseTime != null)
                    if ($Number(object.pauseTime) !== 0)
                        message.pauseTime = object.pauseTime >>> 0;
                if (object.sensitivity != null)
                    if ($Number(object.sensitivity) !== 0)
                        message.sensitivity = object.sensitivity >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a CollisionDetector message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.safety.CollisionDetector
             * @static
             * @param {lebai.safety.CollisionDetector} message CollisionDetector
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CollisionDetector.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.action = options.enums === $String ? "ESTOP" : 0;
                    object.pauseTime = 0;
                    object.sensitivity = 0;
                }
                if (message.action != null && $Object.hasOwnProperty.call(message, "action"))
                    object.action = options.enums === $String ? $root.lebai.safety.CollisionDetectorAction[message.action] === $undefined ? message.action : $root.lebai.safety.CollisionDetectorAction[message.action] : message.action;
                if (message.pauseTime != null && $Object.hasOwnProperty.call(message, "pauseTime"))
                    object.pauseTime = message.pauseTime;
                if (message.sensitivity != null && $Object.hasOwnProperty.call(message, "sensitivity"))
                    object.sensitivity = message.sensitivity;
                return object;
            };

            /**
             * Converts this CollisionDetector to JSON.
             * @function toJSON
             * @memberof lebai.safety.CollisionDetector
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CollisionDetector.prototype.toJSON = function() {
                return CollisionDetector.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CollisionDetector
             * @function getTypeUrl
             * @memberof lebai.safety.CollisionDetector
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CollisionDetector.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.safety.CollisionDetector";
            };

            return CollisionDetector;
        })();

        safety.JointLimit = (function() {

            /**
             * Properties of a JointLimit.
             * @typedef {Object} lebai.safety.JointLimit.$Properties
             * @property {number|null} [minPosition] JointLimit minPosition
             * @property {number|null} [maxPosition] JointLimit maxPosition
             * @property {number|null} [maxA] JointLimit maxA
             * @property {number|null} [maxV] JointLimit maxV
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a JointLimit.
             * @memberof lebai.safety
             * @interface IJointLimit
             * @augments lebai.safety.JointLimit.$Properties
             * @deprecated Use lebai.safety.JointLimit.$Properties instead.
             */

            /**
             * Shape of a JointLimit.
             * @typedef {lebai.safety.JointLimit.$Properties} lebai.safety.JointLimit.$Shape
             */

            /**
             * Constructs a new JointLimit.
             * @memberof lebai.safety
             * @classdesc Represents a JointLimit.
             * @constructor
             * @param {lebai.safety.JointLimit.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var JointLimit = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * JointLimit minPosition.
             * @member {number} minPosition
             * @memberof lebai.safety.JointLimit
             * @instance
             */
            JointLimit.prototype.minPosition = 0;

            /**
             * JointLimit maxPosition.
             * @member {number} maxPosition
             * @memberof lebai.safety.JointLimit
             * @instance
             */
            JointLimit.prototype.maxPosition = 0;

            /**
             * JointLimit maxA.
             * @member {number} maxA
             * @memberof lebai.safety.JointLimit
             * @instance
             */
            JointLimit.prototype.maxA = 0;

            /**
             * JointLimit maxV.
             * @member {number} maxV
             * @memberof lebai.safety.JointLimit
             * @instance
             */
            JointLimit.prototype.maxV = 0;

            /**
             * Creates a new JointLimit instance using the specified properties.
             * @function create
             * @memberof lebai.safety.JointLimit
             * @static
             * @param {lebai.safety.JointLimit.$Properties=} [properties] Properties to set
             * @returns {lebai.safety.JointLimit} JointLimit instance
             * @type {{
             *   (properties: lebai.safety.JointLimit.$Shape): lebai.safety.JointLimit & lebai.safety.JointLimit.$Shape;
             *   (properties?: lebai.safety.JointLimit.$Properties): lebai.safety.JointLimit;
             * }}
             */
            JointLimit.create = function(properties) {
                return new JointLimit(properties);
            };

            /**
             * Encodes the specified JointLimit message. Does not implicitly {@link lebai.safety.JointLimit.verify|verify} messages.
             * @function encode
             * @memberof lebai.safety.JointLimit
             * @static
             * @param {lebai.safety.JointLimit.$Properties} message JointLimit message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JointLimit.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.minPosition != null && $Object.hasOwnProperty.call(message, "minPosition") && !$Object.is(message.minPosition, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.minPosition);
                if (message.maxPosition != null && $Object.hasOwnProperty.call(message, "maxPosition") && !$Object.is(message.maxPosition, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.maxPosition);
                if (message.maxA != null && $Object.hasOwnProperty.call(message, "maxA") && !$Object.is(message.maxA, 0))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.maxA);
                if (message.maxV != null && $Object.hasOwnProperty.call(message, "maxV") && !$Object.is(message.maxV, 0))
                    writer.uint32(/* id 4, wireType 1 =*/33).double(message.maxV);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified JointLimit message, length delimited. Does not implicitly {@link lebai.safety.JointLimit.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.safety.JointLimit
             * @static
             * @param {lebai.safety.JointLimit.$Properties} message JointLimit message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JointLimit.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a JointLimit message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.safety.JointLimit
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.safety.JointLimit & lebai.safety.JointLimit.$Shape} JointLimit
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JointLimit.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.safety.JointLimit();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.minPosition = value;
                            else
                                delete message.minPosition;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.maxPosition = value;
                            else
                                delete message.maxPosition;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.maxA = value;
                            else
                                delete message.maxA;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.maxV = value;
                            else
                                delete message.maxV;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a JointLimit message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.safety.JointLimit
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.safety.JointLimit & lebai.safety.JointLimit.$Shape} JointLimit
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JointLimit.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a JointLimit message.
             * @function verify
             * @memberof lebai.safety.JointLimit
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            JointLimit.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.minPosition != null && $Object.hasOwnProperty.call(message, "minPosition"))
                    if (typeof message.minPosition !== "number")
                        return "minPosition: number expected";
                if (message.maxPosition != null && $Object.hasOwnProperty.call(message, "maxPosition"))
                    if (typeof message.maxPosition !== "number")
                        return "maxPosition: number expected";
                if (message.maxA != null && $Object.hasOwnProperty.call(message, "maxA"))
                    if (typeof message.maxA !== "number")
                        return "maxA: number expected";
                if (message.maxV != null && $Object.hasOwnProperty.call(message, "maxV"))
                    if (typeof message.maxV !== "number")
                        return "maxV: number expected";
                return null;
            };

            /**
             * Creates a JointLimit message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.safety.JointLimit
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.safety.JointLimit} JointLimit
             */
            JointLimit.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.safety.JointLimit)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.safety.JointLimit: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.safety.JointLimit();
                if (object.minPosition != null)
                    if (!$Object.is($Number(object.minPosition), 0))
                        message.minPosition = $Number(object.minPosition);
                if (object.maxPosition != null)
                    if (!$Object.is($Number(object.maxPosition), 0))
                        message.maxPosition = $Number(object.maxPosition);
                if (object.maxA != null)
                    if (!$Object.is($Number(object.maxA), 0))
                        message.maxA = $Number(object.maxA);
                if (object.maxV != null)
                    if (!$Object.is($Number(object.maxV), 0))
                        message.maxV = $Number(object.maxV);
                return message;
            };

            /**
             * Creates a plain object from a JointLimit message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.safety.JointLimit
             * @static
             * @param {lebai.safety.JointLimit} message JointLimit
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            JointLimit.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.minPosition = 0;
                    object.maxPosition = 0;
                    object.maxA = 0;
                    object.maxV = 0;
                }
                if (message.minPosition != null && $Object.hasOwnProperty.call(message, "minPosition"))
                    object.minPosition = options.json && !$isFinite(message.minPosition) ? $String(message.minPosition) : message.minPosition;
                if (message.maxPosition != null && $Object.hasOwnProperty.call(message, "maxPosition"))
                    object.maxPosition = options.json && !$isFinite(message.maxPosition) ? $String(message.maxPosition) : message.maxPosition;
                if (message.maxA != null && $Object.hasOwnProperty.call(message, "maxA"))
                    object.maxA = options.json && !$isFinite(message.maxA) ? $String(message.maxA) : message.maxA;
                if (message.maxV != null && $Object.hasOwnProperty.call(message, "maxV"))
                    object.maxV = options.json && !$isFinite(message.maxV) ? $String(message.maxV) : message.maxV;
                return object;
            };

            /**
             * Converts this JointLimit to JSON.
             * @function toJSON
             * @memberof lebai.safety.JointLimit
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            JointLimit.prototype.toJSON = function() {
                return JointLimit.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for JointLimit
             * @function getTypeUrl
             * @memberof lebai.safety.JointLimit
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            JointLimit.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.safety.JointLimit";
            };

            return JointLimit;
        })();

        safety.JointsLimit = (function() {

            /**
             * Properties of a JointsLimit.
             * @typedef {Object} lebai.safety.JointsLimit.$Properties
             * @property {Array.<lebai.safety.JointLimit.$Properties>|null} [joints] JointsLimit joints
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a JointsLimit.
             * @memberof lebai.safety
             * @interface IJointsLimit
             * @augments lebai.safety.JointsLimit.$Properties
             * @deprecated Use lebai.safety.JointsLimit.$Properties instead.
             */

            /**
             * Shape of a JointsLimit.
             * @typedef {lebai.safety.JointsLimit.$Properties} lebai.safety.JointsLimit.$Shape
             */

            /**
             * Constructs a new JointsLimit.
             * @memberof lebai.safety
             * @classdesc Represents a JointsLimit.
             * @constructor
             * @param {lebai.safety.JointsLimit.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var JointsLimit = function (properties) {
                this.joints = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * JointsLimit joints.
             * @member {Array.<lebai.safety.JointLimit.$Properties>} joints
             * @memberof lebai.safety.JointsLimit
             * @instance
             */
            JointsLimit.prototype.joints = $util.emptyArray;

            /**
             * Creates a new JointsLimit instance using the specified properties.
             * @function create
             * @memberof lebai.safety.JointsLimit
             * @static
             * @param {lebai.safety.JointsLimit.$Properties=} [properties] Properties to set
             * @returns {lebai.safety.JointsLimit} JointsLimit instance
             * @type {{
             *   (properties: lebai.safety.JointsLimit.$Shape): lebai.safety.JointsLimit & lebai.safety.JointsLimit.$Shape;
             *   (properties?: lebai.safety.JointsLimit.$Properties): lebai.safety.JointsLimit;
             * }}
             */
            JointsLimit.create = function(properties) {
                return new JointsLimit(properties);
            };

            /**
             * Encodes the specified JointsLimit message. Does not implicitly {@link lebai.safety.JointsLimit.verify|verify} messages.
             * @function encode
             * @memberof lebai.safety.JointsLimit
             * @static
             * @param {lebai.safety.JointsLimit.$Properties} message JointsLimit message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JointsLimit.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.joints != null && message.joints.length)
                    for (var i = 0; i < message.joints.length; ++i)
                        $root.lebai.safety.JointLimit.encode(message.joints[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified JointsLimit message, length delimited. Does not implicitly {@link lebai.safety.JointsLimit.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.safety.JointsLimit
             * @static
             * @param {lebai.safety.JointsLimit.$Properties} message JointsLimit message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            JointsLimit.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a JointsLimit message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.safety.JointsLimit
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.safety.JointsLimit & lebai.safety.JointsLimit.$Shape} JointsLimit
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JointsLimit.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.safety.JointsLimit();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.joints && message.joints.length))
                                message.joints = [];
                            message.joints.push($root.lebai.safety.JointLimit.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a JointsLimit message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.safety.JointsLimit
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.safety.JointsLimit & lebai.safety.JointsLimit.$Shape} JointsLimit
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            JointsLimit.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a JointsLimit message.
             * @function verify
             * @memberof lebai.safety.JointsLimit
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            JointsLimit.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.joints != null && $Object.hasOwnProperty.call(message, "joints")) {
                    if (!$Array.isArray(message.joints))
                        return "joints: array expected";
                    for (var i = 0; i < message.joints.length; ++i) {
                        var error = $root.lebai.safety.JointLimit.verify(message.joints[i], _depth + 1);
                        if (error)
                            return "joints." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a JointsLimit message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.safety.JointsLimit
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.safety.JointsLimit} JointsLimit
             */
            JointsLimit.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.safety.JointsLimit)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.safety.JointsLimit: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.safety.JointsLimit();
                if (object.joints) {
                    if (!$Array.isArray(object.joints))
                        throw $TypeError(".lebai.safety.JointsLimit.joints: array expected");
                    message.joints = $Array(object.joints.length);
                    for (var i = 0; i < object.joints.length; ++i) {
                        if (!$util.isObject(object.joints[i]))
                            throw $TypeError(".lebai.safety.JointsLimit.joints: object expected");
                        message.joints[i] = $root.lebai.safety.JointLimit.fromObject(object.joints[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a JointsLimit message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.safety.JointsLimit
             * @static
             * @param {lebai.safety.JointsLimit} message JointsLimit
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            JointsLimit.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.joints = [];
                if (message.joints && message.joints.length) {
                    object.joints = $Array(message.joints.length);
                    for (var j = 0; j < message.joints.length; ++j)
                        object.joints[j] = $root.lebai.safety.JointLimit.toObject(message.joints[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this JointsLimit to JSON.
             * @function toJSON
             * @memberof lebai.safety.JointsLimit
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            JointsLimit.prototype.toJSON = function() {
                return JointsLimit.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for JointsLimit
             * @function getTypeUrl
             * @memberof lebai.safety.JointsLimit
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            JointsLimit.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.safety.JointsLimit";
            };

            return JointsLimit;
        })();

        safety.CartesianLimit = (function() {

            /**
             * Properties of a CartesianLimit.
             * @typedef {Object} lebai.safety.CartesianLimit.$Properties
             * @property {number|null} [maxA] CartesianLimit maxA
             * @property {number|null} [maxV] CartesianLimit maxV
             * @property {number|null} [eqRadius] CartesianLimit eqRadius
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CartesianLimit.
             * @memberof lebai.safety
             * @interface ICartesianLimit
             * @augments lebai.safety.CartesianLimit.$Properties
             * @deprecated Use lebai.safety.CartesianLimit.$Properties instead.
             */

            /**
             * Shape of a CartesianLimit.
             * @typedef {lebai.safety.CartesianLimit.$Properties} lebai.safety.CartesianLimit.$Shape
             */

            /**
             * Constructs a new CartesianLimit.
             * @memberof lebai.safety
             * @classdesc Represents a CartesianLimit.
             * @constructor
             * @param {lebai.safety.CartesianLimit.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var CartesianLimit = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CartesianLimit maxA.
             * @member {number} maxA
             * @memberof lebai.safety.CartesianLimit
             * @instance
             */
            CartesianLimit.prototype.maxA = 0;

            /**
             * CartesianLimit maxV.
             * @member {number} maxV
             * @memberof lebai.safety.CartesianLimit
             * @instance
             */
            CartesianLimit.prototype.maxV = 0;

            /**
             * CartesianLimit eqRadius.
             * @member {number} eqRadius
             * @memberof lebai.safety.CartesianLimit
             * @instance
             */
            CartesianLimit.prototype.eqRadius = 0;

            /**
             * Creates a new CartesianLimit instance using the specified properties.
             * @function create
             * @memberof lebai.safety.CartesianLimit
             * @static
             * @param {lebai.safety.CartesianLimit.$Properties=} [properties] Properties to set
             * @returns {lebai.safety.CartesianLimit} CartesianLimit instance
             * @type {{
             *   (properties: lebai.safety.CartesianLimit.$Shape): lebai.safety.CartesianLimit & lebai.safety.CartesianLimit.$Shape;
             *   (properties?: lebai.safety.CartesianLimit.$Properties): lebai.safety.CartesianLimit;
             * }}
             */
            CartesianLimit.create = function(properties) {
                return new CartesianLimit(properties);
            };

            /**
             * Encodes the specified CartesianLimit message. Does not implicitly {@link lebai.safety.CartesianLimit.verify|verify} messages.
             * @function encode
             * @memberof lebai.safety.CartesianLimit
             * @static
             * @param {lebai.safety.CartesianLimit.$Properties} message CartesianLimit message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CartesianLimit.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.maxA != null && $Object.hasOwnProperty.call(message, "maxA") && !$Object.is(message.maxA, 0))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.maxA);
                if (message.maxV != null && $Object.hasOwnProperty.call(message, "maxV") && !$Object.is(message.maxV, 0))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.maxV);
                if (message.eqRadius != null && $Object.hasOwnProperty.call(message, "eqRadius") && !$Object.is(message.eqRadius, 0))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.eqRadius);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CartesianLimit message, length delimited. Does not implicitly {@link lebai.safety.CartesianLimit.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.safety.CartesianLimit
             * @static
             * @param {lebai.safety.CartesianLimit.$Properties} message CartesianLimit message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CartesianLimit.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a CartesianLimit message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.safety.CartesianLimit
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.safety.CartesianLimit & lebai.safety.CartesianLimit.$Shape} CartesianLimit
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CartesianLimit.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.safety.CartesianLimit();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.maxA = value;
                            else
                                delete message.maxA;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.maxV = value;
                            else
                                delete message.maxV;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 1)
                                break;
                            if (!$Object.is(value = reader.double(), 0))
                                message.eqRadius = value;
                            else
                                delete message.eqRadius;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a CartesianLimit message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.safety.CartesianLimit
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.safety.CartesianLimit & lebai.safety.CartesianLimit.$Shape} CartesianLimit
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CartesianLimit.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CartesianLimit message.
             * @function verify
             * @memberof lebai.safety.CartesianLimit
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CartesianLimit.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.maxA != null && $Object.hasOwnProperty.call(message, "maxA"))
                    if (typeof message.maxA !== "number")
                        return "maxA: number expected";
                if (message.maxV != null && $Object.hasOwnProperty.call(message, "maxV"))
                    if (typeof message.maxV !== "number")
                        return "maxV: number expected";
                if (message.eqRadius != null && $Object.hasOwnProperty.call(message, "eqRadius"))
                    if (typeof message.eqRadius !== "number")
                        return "eqRadius: number expected";
                return null;
            };

            /**
             * Creates a CartesianLimit message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.safety.CartesianLimit
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.safety.CartesianLimit} CartesianLimit
             */
            CartesianLimit.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.safety.CartesianLimit)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.safety.CartesianLimit: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.safety.CartesianLimit();
                if (object.maxA != null)
                    if (!$Object.is($Number(object.maxA), 0))
                        message.maxA = $Number(object.maxA);
                if (object.maxV != null)
                    if (!$Object.is($Number(object.maxV), 0))
                        message.maxV = $Number(object.maxV);
                if (object.eqRadius != null)
                    if (!$Object.is($Number(object.eqRadius), 0))
                        message.eqRadius = $Number(object.eqRadius);
                return message;
            };

            /**
             * Creates a plain object from a CartesianLimit message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.safety.CartesianLimit
             * @static
             * @param {lebai.safety.CartesianLimit} message CartesianLimit
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CartesianLimit.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.maxA = 0;
                    object.maxV = 0;
                    object.eqRadius = 0;
                }
                if (message.maxA != null && $Object.hasOwnProperty.call(message, "maxA"))
                    object.maxA = options.json && !$isFinite(message.maxA) ? $String(message.maxA) : message.maxA;
                if (message.maxV != null && $Object.hasOwnProperty.call(message, "maxV"))
                    object.maxV = options.json && !$isFinite(message.maxV) ? $String(message.maxV) : message.maxV;
                if (message.eqRadius != null && $Object.hasOwnProperty.call(message, "eqRadius"))
                    object.eqRadius = options.json && !$isFinite(message.eqRadius) ? $String(message.eqRadius) : message.eqRadius;
                return object;
            };

            /**
             * Converts this CartesianLimit to JSON.
             * @function toJSON
             * @memberof lebai.safety.CartesianLimit
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CartesianLimit.prototype.toJSON = function() {
                return CartesianLimit.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CartesianLimit
             * @function getTypeUrl
             * @memberof lebai.safety.CartesianLimit
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CartesianLimit.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.safety.CartesianLimit";
            };

            return CartesianLimit;
        })();

        safety.SafetyService = (function() {

            /**
             * Constructs a new SafetyService service.
             * @memberof lebai.safety
             * @classdesc Represents a SafetyService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var SafetyService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(SafetyService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: SafetyService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new SafetyService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.safety.SafetyService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {SafetyService} RPC service. Useful where requests and/or responses are streamed.
             */
            SafetyService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.safety.SafetyService#enableCollisionDetector}.
             * @memberof lebai.safety.SafetyService
             * @typedef EnableCollisionDetectorCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls EnableCollisionDetector.
             * @memberof lebai.safety.SafetyService
             * @typedef EnableCollisionDetector
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.safety.SafetyService.EnableCollisionDetectorCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "EnableCollisionDetector";
             *   readonly path: "/lebai.safety.SafetyService/EnableCollisionDetector";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls EnableCollisionDetector.
             * @name lebai.safety.SafetyService#enableCollisionDetector
             * @type {lebai.safety.SafetyService.EnableCollisionDetector}
             */
            $Object.defineProperties(SafetyService.prototype.enableCollisionDetector = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SafetyService.prototype.enableCollisionDetector, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "EnableCollisionDetector" },
                path: { value: "/lebai.safety.SafetyService/EnableCollisionDetector" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.safety.SafetyService#disableCollisionDetector}.
             * @memberof lebai.safety.SafetyService
             * @typedef DisableCollisionDetectorCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls DisableCollisionDetector.
             * @memberof lebai.safety.SafetyService
             * @typedef DisableCollisionDetector
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.safety.SafetyService.DisableCollisionDetectorCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "DisableCollisionDetector";
             *   readonly path: "/lebai.safety.SafetyService/DisableCollisionDetector";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls DisableCollisionDetector.
             * @name lebai.safety.SafetyService#disableCollisionDetector
             * @type {lebai.safety.SafetyService.DisableCollisionDetector}
             */
            $Object.defineProperties(SafetyService.prototype.disableCollisionDetector = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SafetyService.prototype.disableCollisionDetector, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "DisableCollisionDetector" },
                path: { value: "/lebai.safety.SafetyService/DisableCollisionDetector" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.safety.SafetyService#setCollisionTorqueDiff}.
             * @memberof lebai.safety.SafetyService
             * @typedef SetCollisionTorqueDiffCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetCollisionTorqueDiff.
             * @memberof lebai.safety.SafetyService
             * @typedef SetCollisionTorqueDiff
             * @type {{
             *   (request: lebai.safety.ICollisionTorqueDiff, callback: lebai.safety.SafetyService.SetCollisionTorqueDiffCallback): void;
             *   (request: lebai.safety.ICollisionTorqueDiff): Promise<google.protobuf.Empty>;
             *   readonly name: "SetCollisionTorqueDiff";
             *   readonly path: "/lebai.safety.SafetyService/SetCollisionTorqueDiff";
             *   readonly requestType: "CollisionTorqueDiff";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetCollisionTorqueDiff.
             * @name lebai.safety.SafetyService#setCollisionTorqueDiff
             * @type {lebai.safety.SafetyService.SetCollisionTorqueDiff}
             */
            $Object.defineProperties(SafetyService.prototype.setCollisionTorqueDiff = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SafetyService.prototype.setCollisionTorqueDiff, $root.lebai.safety.CollisionTorqueDiff, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetCollisionTorqueDiff" },
                path: { value: "/lebai.safety.SafetyService/SetCollisionTorqueDiff" },
                requestType: { value: "CollisionTorqueDiff" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.safety.SafetyService#getCollisionTorqueDiff}.
             * @memberof lebai.safety.SafetyService
             * @typedef GetCollisionTorqueDiffCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.safety.CollisionTorqueDiff} [response] CollisionTorqueDiff
             */

            /**
             * Calls GetCollisionTorqueDiff.
             * @memberof lebai.safety.SafetyService
             * @typedef GetCollisionTorqueDiff
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.safety.SafetyService.GetCollisionTorqueDiffCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.safety.CollisionTorqueDiff>;
             *   readonly name: "GetCollisionTorqueDiff";
             *   readonly path: "/lebai.safety.SafetyService/GetCollisionTorqueDiff";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "CollisionTorqueDiff";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetCollisionTorqueDiff.
             * @name lebai.safety.SafetyService#getCollisionTorqueDiff
             * @type {lebai.safety.SafetyService.GetCollisionTorqueDiff}
             */
            $Object.defineProperties(SafetyService.prototype.getCollisionTorqueDiff = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SafetyService.prototype.getCollisionTorqueDiff, $root.google.protobuf.Empty, $root.lebai.safety.CollisionTorqueDiff, request, callback);
            }, {
                name: { value: "GetCollisionTorqueDiff" },
                path: { value: "/lebai.safety.SafetyService/GetCollisionTorqueDiff" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "CollisionTorqueDiff" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.safety.SafetyService#setCollisionDetector}.
             * @memberof lebai.safety.SafetyService
             * @typedef SetCollisionDetectorCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetCollisionDetector.
             * @memberof lebai.safety.SafetyService
             * @typedef SetCollisionDetector
             * @type {{
             *   (request: lebai.safety.ICollisionDetector, callback: lebai.safety.SafetyService.SetCollisionDetectorCallback): void;
             *   (request: lebai.safety.ICollisionDetector): Promise<google.protobuf.Empty>;
             *   readonly name: "SetCollisionDetector";
             *   readonly path: "/lebai.safety.SafetyService/SetCollisionDetector";
             *   readonly requestType: "CollisionDetector";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetCollisionDetector.
             * @name lebai.safety.SafetyService#setCollisionDetector
             * @type {lebai.safety.SafetyService.SetCollisionDetector}
             */
            $Object.defineProperties(SafetyService.prototype.setCollisionDetector = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SafetyService.prototype.setCollisionDetector, $root.lebai.safety.CollisionDetector, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetCollisionDetector" },
                path: { value: "/lebai.safety.SafetyService/SetCollisionDetector" },
                requestType: { value: "CollisionDetector" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.safety.SafetyService#getCollisionDetector}.
             * @memberof lebai.safety.SafetyService
             * @typedef GetCollisionDetectorCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.safety.CollisionDetector} [response] CollisionDetector
             */

            /**
             * Calls GetCollisionDetector.
             * @memberof lebai.safety.SafetyService
             * @typedef GetCollisionDetector
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.safety.SafetyService.GetCollisionDetectorCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.safety.CollisionDetector>;
             *   readonly name: "GetCollisionDetector";
             *   readonly path: "/lebai.safety.SafetyService/GetCollisionDetector";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "CollisionDetector";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetCollisionDetector.
             * @name lebai.safety.SafetyService#getCollisionDetector
             * @type {lebai.safety.SafetyService.GetCollisionDetector}
             */
            $Object.defineProperties(SafetyService.prototype.getCollisionDetector = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SafetyService.prototype.getCollisionDetector, $root.google.protobuf.Empty, $root.lebai.safety.CollisionDetector, request, callback);
            }, {
                name: { value: "GetCollisionDetector" },
                path: { value: "/lebai.safety.SafetyService/GetCollisionDetector" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "CollisionDetector" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.safety.SafetyService#enableLimit}.
             * @memberof lebai.safety.SafetyService
             * @typedef EnableLimitCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls EnableLimit.
             * @memberof lebai.safety.SafetyService
             * @typedef EnableLimit
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.safety.SafetyService.EnableLimitCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "EnableLimit";
             *   readonly path: "/lebai.safety.SafetyService/EnableLimit";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls EnableLimit.
             * @name lebai.safety.SafetyService#enableLimit
             * @type {lebai.safety.SafetyService.EnableLimit}
             */
            $Object.defineProperties(SafetyService.prototype.enableLimit = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SafetyService.prototype.enableLimit, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "EnableLimit" },
                path: { value: "/lebai.safety.SafetyService/EnableLimit" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.safety.SafetyService#disableLimit}.
             * @memberof lebai.safety.SafetyService
             * @typedef DisableLimitCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls DisableLimit.
             * @memberof lebai.safety.SafetyService
             * @typedef DisableLimit
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.safety.SafetyService.DisableLimitCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "DisableLimit";
             *   readonly path: "/lebai.safety.SafetyService/DisableLimit";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls DisableLimit.
             * @name lebai.safety.SafetyService#disableLimit
             * @type {lebai.safety.SafetyService.DisableLimit}
             */
            $Object.defineProperties(SafetyService.prototype.disableLimit = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SafetyService.prototype.disableLimit, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "DisableLimit" },
                path: { value: "/lebai.safety.SafetyService/DisableLimit" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.safety.SafetyService#setJointsLimit}.
             * @memberof lebai.safety.SafetyService
             * @typedef SetJointsLimitCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetJointsLimit.
             * @memberof lebai.safety.SafetyService
             * @typedef SetJointsLimit
             * @type {{
             *   (request: lebai.safety.IJointsLimit, callback: lebai.safety.SafetyService.SetJointsLimitCallback): void;
             *   (request: lebai.safety.IJointsLimit): Promise<google.protobuf.Empty>;
             *   readonly name: "SetJointsLimit";
             *   readonly path: "/lebai.safety.SafetyService/SetJointsLimit";
             *   readonly requestType: "JointsLimit";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetJointsLimit.
             * @name lebai.safety.SafetyService#setJointsLimit
             * @type {lebai.safety.SafetyService.SetJointsLimit}
             */
            $Object.defineProperties(SafetyService.prototype.setJointsLimit = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SafetyService.prototype.setJointsLimit, $root.lebai.safety.JointsLimit, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetJointsLimit" },
                path: { value: "/lebai.safety.SafetyService/SetJointsLimit" },
                requestType: { value: "JointsLimit" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.safety.SafetyService#getJointsLimit}.
             * @memberof lebai.safety.SafetyService
             * @typedef GetJointsLimitCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.safety.JointsLimit} [response] JointsLimit
             */

            /**
             * Calls GetJointsLimit.
             * @memberof lebai.safety.SafetyService
             * @typedef GetJointsLimit
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.safety.SafetyService.GetJointsLimitCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.safety.JointsLimit>;
             *   readonly name: "GetJointsLimit";
             *   readonly path: "/lebai.safety.SafetyService/GetJointsLimit";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "JointsLimit";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetJointsLimit.
             * @name lebai.safety.SafetyService#getJointsLimit
             * @type {lebai.safety.SafetyService.GetJointsLimit}
             */
            $Object.defineProperties(SafetyService.prototype.getJointsLimit = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SafetyService.prototype.getJointsLimit, $root.google.protobuf.Empty, $root.lebai.safety.JointsLimit, request, callback);
            }, {
                name: { value: "GetJointsLimit" },
                path: { value: "/lebai.safety.SafetyService/GetJointsLimit" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "JointsLimit" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.safety.SafetyService#setCartLimit}.
             * @memberof lebai.safety.SafetyService
             * @typedef SetCartLimitCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetCartLimit.
             * @memberof lebai.safety.SafetyService
             * @typedef SetCartLimit
             * @type {{
             *   (request: lebai.safety.ICartesianLimit, callback: lebai.safety.SafetyService.SetCartLimitCallback): void;
             *   (request: lebai.safety.ICartesianLimit): Promise<google.protobuf.Empty>;
             *   readonly name: "SetCartLimit";
             *   readonly path: "/lebai.safety.SafetyService/SetCartLimit";
             *   readonly requestType: "CartesianLimit";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetCartLimit.
             * @name lebai.safety.SafetyService#setCartLimit
             * @type {lebai.safety.SafetyService.SetCartLimit}
             */
            $Object.defineProperties(SafetyService.prototype.setCartLimit = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SafetyService.prototype.setCartLimit, $root.lebai.safety.CartesianLimit, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetCartLimit" },
                path: { value: "/lebai.safety.SafetyService/SetCartLimit" },
                requestType: { value: "CartesianLimit" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.safety.SafetyService#getCartLimit}.
             * @memberof lebai.safety.SafetyService
             * @typedef GetCartLimitCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.safety.CartesianLimit} [response] CartesianLimit
             */

            /**
             * Calls GetCartLimit.
             * @memberof lebai.safety.SafetyService
             * @typedef GetCartLimit
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.safety.SafetyService.GetCartLimitCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.safety.CartesianLimit>;
             *   readonly name: "GetCartLimit";
             *   readonly path: "/lebai.safety.SafetyService/GetCartLimit";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "CartesianLimit";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetCartLimit.
             * @name lebai.safety.SafetyService#getCartLimit
             * @type {lebai.safety.SafetyService.GetCartLimit}
             */
            $Object.defineProperties(SafetyService.prototype.getCartLimit = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SafetyService.prototype.getCartLimit, $root.google.protobuf.Empty, $root.lebai.safety.CartesianLimit, request, callback);
            }, {
                name: { value: "GetCartLimit" },
                path: { value: "/lebai.safety.SafetyService/GetCartLimit" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "CartesianLimit" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return SafetyService;
        })();

        return safety;
    })();

    lebai.serial = (function() {

        /**
         * Namespace serial.
         * @memberof lebai
         * @namespace
         */
        var serial = {};

        serial.SetSerialTimeoutRequest = (function() {

            /**
             * Properties of a SetSerialTimeoutRequest.
             * @typedef {Object} lebai.serial.SetSerialTimeoutRequest.$Properties
             * @property {string|null} [device] SetSerialTimeoutRequest device
             * @property {number|null} [timeout] SetSerialTimeoutRequest timeout
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetSerialTimeoutRequest.
             * @memberof lebai.serial
             * @interface ISetSerialTimeoutRequest
             * @augments lebai.serial.SetSerialTimeoutRequest.$Properties
             * @deprecated Use lebai.serial.SetSerialTimeoutRequest.$Properties instead.
             */

            /**
             * Shape of a SetSerialTimeoutRequest.
             * @typedef {lebai.serial.SetSerialTimeoutRequest.$Properties} lebai.serial.SetSerialTimeoutRequest.$Shape
             */

            /**
             * Constructs a new SetSerialTimeoutRequest.
             * @memberof lebai.serial
             * @classdesc Represents a SetSerialTimeoutRequest.
             * @constructor
             * @param {lebai.serial.SetSerialTimeoutRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetSerialTimeoutRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetSerialTimeoutRequest device.
             * @member {string} device
             * @memberof lebai.serial.SetSerialTimeoutRequest
             * @instance
             */
            SetSerialTimeoutRequest.prototype.device = "";

            /**
             * SetSerialTimeoutRequest timeout.
             * @member {number} timeout
             * @memberof lebai.serial.SetSerialTimeoutRequest
             * @instance
             */
            SetSerialTimeoutRequest.prototype.timeout = 0;

            /**
             * Creates a new SetSerialTimeoutRequest instance using the specified properties.
             * @function create
             * @memberof lebai.serial.SetSerialTimeoutRequest
             * @static
             * @param {lebai.serial.SetSerialTimeoutRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.serial.SetSerialTimeoutRequest} SetSerialTimeoutRequest instance
             * @type {{
             *   (properties: lebai.serial.SetSerialTimeoutRequest.$Shape): lebai.serial.SetSerialTimeoutRequest & lebai.serial.SetSerialTimeoutRequest.$Shape;
             *   (properties?: lebai.serial.SetSerialTimeoutRequest.$Properties): lebai.serial.SetSerialTimeoutRequest;
             * }}
             */
            SetSerialTimeoutRequest.create = function(properties) {
                return new SetSerialTimeoutRequest(properties);
            };

            /**
             * Encodes the specified SetSerialTimeoutRequest message. Does not implicitly {@link lebai.serial.SetSerialTimeoutRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.serial.SetSerialTimeoutRequest
             * @static
             * @param {lebai.serial.SetSerialTimeoutRequest.$Properties} message SetSerialTimeoutRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetSerialTimeoutRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout") && message.timeout !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.timeout);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetSerialTimeoutRequest message, length delimited. Does not implicitly {@link lebai.serial.SetSerialTimeoutRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.serial.SetSerialTimeoutRequest
             * @static
             * @param {lebai.serial.SetSerialTimeoutRequest.$Properties} message SetSerialTimeoutRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetSerialTimeoutRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetSerialTimeoutRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.serial.SetSerialTimeoutRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.serial.SetSerialTimeoutRequest & lebai.serial.SetSerialTimeoutRequest.$Shape} SetSerialTimeoutRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetSerialTimeoutRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.serial.SetSerialTimeoutRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.timeout = value;
                            else
                                delete message.timeout;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetSerialTimeoutRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.serial.SetSerialTimeoutRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.serial.SetSerialTimeoutRequest & lebai.serial.SetSerialTimeoutRequest.$Shape} SetSerialTimeoutRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetSerialTimeoutRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetSerialTimeoutRequest message.
             * @function verify
             * @memberof lebai.serial.SetSerialTimeoutRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetSerialTimeoutRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout"))
                    if (!$util.isInteger(message.timeout))
                        return "timeout: integer expected";
                return null;
            };

            /**
             * Creates a SetSerialTimeoutRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.serial.SetSerialTimeoutRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.serial.SetSerialTimeoutRequest} SetSerialTimeoutRequest
             */
            SetSerialTimeoutRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.serial.SetSerialTimeoutRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.serial.SetSerialTimeoutRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.serial.SetSerialTimeoutRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.timeout != null)
                    if ($Number(object.timeout) !== 0)
                        message.timeout = object.timeout >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a SetSerialTimeoutRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.serial.SetSerialTimeoutRequest
             * @static
             * @param {lebai.serial.SetSerialTimeoutRequest} message SetSerialTimeoutRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetSerialTimeoutRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.timeout = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.timeout != null && $Object.hasOwnProperty.call(message, "timeout"))
                    object.timeout = message.timeout;
                return object;
            };

            /**
             * Converts this SetSerialTimeoutRequest to JSON.
             * @function toJSON
             * @memberof lebai.serial.SetSerialTimeoutRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetSerialTimeoutRequest.prototype.toJSON = function() {
                return SetSerialTimeoutRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetSerialTimeoutRequest
             * @function getTypeUrl
             * @memberof lebai.serial.SetSerialTimeoutRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetSerialTimeoutRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.serial.SetSerialTimeoutRequest";
            };

            return SetSerialTimeoutRequest;
        })();

        serial.SetSerialBaudRateRequest = (function() {

            /**
             * Properties of a SetSerialBaudRateRequest.
             * @typedef {Object} lebai.serial.SetSerialBaudRateRequest.$Properties
             * @property {string|null} [device] SetSerialBaudRateRequest device
             * @property {number|null} [baudRate] SetSerialBaudRateRequest baudRate
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetSerialBaudRateRequest.
             * @memberof lebai.serial
             * @interface ISetSerialBaudRateRequest
             * @augments lebai.serial.SetSerialBaudRateRequest.$Properties
             * @deprecated Use lebai.serial.SetSerialBaudRateRequest.$Properties instead.
             */

            /**
             * Shape of a SetSerialBaudRateRequest.
             * @typedef {lebai.serial.SetSerialBaudRateRequest.$Properties} lebai.serial.SetSerialBaudRateRequest.$Shape
             */

            /**
             * Constructs a new SetSerialBaudRateRequest.
             * @memberof lebai.serial
             * @classdesc Represents a SetSerialBaudRateRequest.
             * @constructor
             * @param {lebai.serial.SetSerialBaudRateRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetSerialBaudRateRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetSerialBaudRateRequest device.
             * @member {string} device
             * @memberof lebai.serial.SetSerialBaudRateRequest
             * @instance
             */
            SetSerialBaudRateRequest.prototype.device = "";

            /**
             * SetSerialBaudRateRequest baudRate.
             * @member {number} baudRate
             * @memberof lebai.serial.SetSerialBaudRateRequest
             * @instance
             */
            SetSerialBaudRateRequest.prototype.baudRate = 0;

            /**
             * Creates a new SetSerialBaudRateRequest instance using the specified properties.
             * @function create
             * @memberof lebai.serial.SetSerialBaudRateRequest
             * @static
             * @param {lebai.serial.SetSerialBaudRateRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.serial.SetSerialBaudRateRequest} SetSerialBaudRateRequest instance
             * @type {{
             *   (properties: lebai.serial.SetSerialBaudRateRequest.$Shape): lebai.serial.SetSerialBaudRateRequest & lebai.serial.SetSerialBaudRateRequest.$Shape;
             *   (properties?: lebai.serial.SetSerialBaudRateRequest.$Properties): lebai.serial.SetSerialBaudRateRequest;
             * }}
             */
            SetSerialBaudRateRequest.create = function(properties) {
                return new SetSerialBaudRateRequest(properties);
            };

            /**
             * Encodes the specified SetSerialBaudRateRequest message. Does not implicitly {@link lebai.serial.SetSerialBaudRateRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.serial.SetSerialBaudRateRequest
             * @static
             * @param {lebai.serial.SetSerialBaudRateRequest.$Properties} message SetSerialBaudRateRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetSerialBaudRateRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.baudRate != null && $Object.hasOwnProperty.call(message, "baudRate") && message.baudRate !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.baudRate);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetSerialBaudRateRequest message, length delimited. Does not implicitly {@link lebai.serial.SetSerialBaudRateRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.serial.SetSerialBaudRateRequest
             * @static
             * @param {lebai.serial.SetSerialBaudRateRequest.$Properties} message SetSerialBaudRateRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetSerialBaudRateRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetSerialBaudRateRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.serial.SetSerialBaudRateRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.serial.SetSerialBaudRateRequest & lebai.serial.SetSerialBaudRateRequest.$Shape} SetSerialBaudRateRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetSerialBaudRateRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.serial.SetSerialBaudRateRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.baudRate = value;
                            else
                                delete message.baudRate;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetSerialBaudRateRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.serial.SetSerialBaudRateRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.serial.SetSerialBaudRateRequest & lebai.serial.SetSerialBaudRateRequest.$Shape} SetSerialBaudRateRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetSerialBaudRateRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetSerialBaudRateRequest message.
             * @function verify
             * @memberof lebai.serial.SetSerialBaudRateRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetSerialBaudRateRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.baudRate != null && $Object.hasOwnProperty.call(message, "baudRate"))
                    if (!$util.isInteger(message.baudRate))
                        return "baudRate: integer expected";
                return null;
            };

            /**
             * Creates a SetSerialBaudRateRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.serial.SetSerialBaudRateRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.serial.SetSerialBaudRateRequest} SetSerialBaudRateRequest
             */
            SetSerialBaudRateRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.serial.SetSerialBaudRateRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.serial.SetSerialBaudRateRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.serial.SetSerialBaudRateRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.baudRate != null)
                    if ($Number(object.baudRate) !== 0)
                        message.baudRate = object.baudRate >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a SetSerialBaudRateRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.serial.SetSerialBaudRateRequest
             * @static
             * @param {lebai.serial.SetSerialBaudRateRequest} message SetSerialBaudRateRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetSerialBaudRateRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.baudRate = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.baudRate != null && $Object.hasOwnProperty.call(message, "baudRate"))
                    object.baudRate = message.baudRate;
                return object;
            };

            /**
             * Converts this SetSerialBaudRateRequest to JSON.
             * @function toJSON
             * @memberof lebai.serial.SetSerialBaudRateRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetSerialBaudRateRequest.prototype.toJSON = function() {
                return SetSerialBaudRateRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetSerialBaudRateRequest
             * @function getTypeUrl
             * @memberof lebai.serial.SetSerialBaudRateRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetSerialBaudRateRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.serial.SetSerialBaudRateRequest";
            };

            return SetSerialBaudRateRequest;
        })();

        /**
         * Parity enum.
         * @name lebai.serial.Parity
         * @enum {number}
         * @property {number} None=0 None value
         * @property {number} Odd=1 Odd value
         * @property {number} Even=2 Even value
         */
        serial.Parity = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "None"] = 0;
            values[valuesById[1] = "Odd"] = 1;
            values[valuesById[2] = "Even"] = 2;
            return values;
        })();

        serial.SetSerialParityRequest = (function() {

            /**
             * Properties of a SetSerialParityRequest.
             * @typedef {Object} lebai.serial.SetSerialParityRequest.$Properties
             * @property {string|null} [device] SetSerialParityRequest device
             * @property {lebai.serial.Parity|null} [parity] SetSerialParityRequest parity
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetSerialParityRequest.
             * @memberof lebai.serial
             * @interface ISetSerialParityRequest
             * @augments lebai.serial.SetSerialParityRequest.$Properties
             * @deprecated Use lebai.serial.SetSerialParityRequest.$Properties instead.
             */

            /**
             * Shape of a SetSerialParityRequest.
             * @typedef {lebai.serial.SetSerialParityRequest.$Properties} lebai.serial.SetSerialParityRequest.$Shape
             */

            /**
             * Constructs a new SetSerialParityRequest.
             * @memberof lebai.serial
             * @classdesc Represents a SetSerialParityRequest.
             * @constructor
             * @param {lebai.serial.SetSerialParityRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetSerialParityRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetSerialParityRequest device.
             * @member {string} device
             * @memberof lebai.serial.SetSerialParityRequest
             * @instance
             */
            SetSerialParityRequest.prototype.device = "";

            /**
             * SetSerialParityRequest parity.
             * @member {lebai.serial.Parity} parity
             * @memberof lebai.serial.SetSerialParityRequest
             * @instance
             */
            SetSerialParityRequest.prototype.parity = 0;

            /**
             * Creates a new SetSerialParityRequest instance using the specified properties.
             * @function create
             * @memberof lebai.serial.SetSerialParityRequest
             * @static
             * @param {lebai.serial.SetSerialParityRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.serial.SetSerialParityRequest} SetSerialParityRequest instance
             * @type {{
             *   (properties: lebai.serial.SetSerialParityRequest.$Shape): lebai.serial.SetSerialParityRequest & lebai.serial.SetSerialParityRequest.$Shape;
             *   (properties?: lebai.serial.SetSerialParityRequest.$Properties): lebai.serial.SetSerialParityRequest;
             * }}
             */
            SetSerialParityRequest.create = function(properties) {
                return new SetSerialParityRequest(properties);
            };

            /**
             * Encodes the specified SetSerialParityRequest message. Does not implicitly {@link lebai.serial.SetSerialParityRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.serial.SetSerialParityRequest
             * @static
             * @param {lebai.serial.SetSerialParityRequest.$Properties} message SetSerialParityRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetSerialParityRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.parity != null && $Object.hasOwnProperty.call(message, "parity") && message.parity !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).int32(message.parity);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetSerialParityRequest message, length delimited. Does not implicitly {@link lebai.serial.SetSerialParityRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.serial.SetSerialParityRequest
             * @static
             * @param {lebai.serial.SetSerialParityRequest.$Properties} message SetSerialParityRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetSerialParityRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetSerialParityRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.serial.SetSerialParityRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.serial.SetSerialParityRequest & lebai.serial.SetSerialParityRequest.$Shape} SetSerialParityRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetSerialParityRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.serial.SetSerialParityRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.parity = value;
                            else
                                delete message.parity;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetSerialParityRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.serial.SetSerialParityRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.serial.SetSerialParityRequest & lebai.serial.SetSerialParityRequest.$Shape} SetSerialParityRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetSerialParityRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetSerialParityRequest message.
             * @function verify
             * @memberof lebai.serial.SetSerialParityRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetSerialParityRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.parity != null && $Object.hasOwnProperty.call(message, "parity"))
                    if (typeof message.parity !== "number" || (message.parity | 0) !== message.parity)
                        return "parity: enum value expected";
                return null;
            };

            /**
             * Creates a SetSerialParityRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.serial.SetSerialParityRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.serial.SetSerialParityRequest} SetSerialParityRequest
             */
            SetSerialParityRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.serial.SetSerialParityRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.serial.SetSerialParityRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.serial.SetSerialParityRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.parity !== 0 && (typeof object.parity !== "string" || $root.lebai.serial.Parity[object.parity] !== 0))
                    switch (object.parity) {
                    case "None":
                    case 0:
                        message.parity = 0;
                        break;
                    case "Odd":
                    case 1:
                        message.parity = 1;
                        break;
                    case "Even":
                    case 2:
                        message.parity = 2;
                        break;
                    default:
                        if (typeof object.parity === "number" && (object.parity | 0) === object.parity)
                            message.parity = object.parity;
                    }
                return message;
            };

            /**
             * Creates a plain object from a SetSerialParityRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.serial.SetSerialParityRequest
             * @static
             * @param {lebai.serial.SetSerialParityRequest} message SetSerialParityRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetSerialParityRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.parity = options.enums === $String ? "None" : 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.parity != null && $Object.hasOwnProperty.call(message, "parity"))
                    object.parity = options.enums === $String ? $root.lebai.serial.Parity[message.parity] === $undefined ? message.parity : $root.lebai.serial.Parity[message.parity] : message.parity;
                return object;
            };

            /**
             * Converts this SetSerialParityRequest to JSON.
             * @function toJSON
             * @memberof lebai.serial.SetSerialParityRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetSerialParityRequest.prototype.toJSON = function() {
                return SetSerialParityRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetSerialParityRequest
             * @function getTypeUrl
             * @memberof lebai.serial.SetSerialParityRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetSerialParityRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.serial.SetSerialParityRequest";
            };

            return SetSerialParityRequest;
        })();

        serial.WriteSerialRequest = (function() {

            /**
             * Properties of a WriteSerialRequest.
             * @typedef {Object} lebai.serial.WriteSerialRequest.$Properties
             * @property {string|null} [device] WriteSerialRequest device
             * @property {Array.<number>|null} [data] WriteSerialRequest data
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a WriteSerialRequest.
             * @memberof lebai.serial
             * @interface IWriteSerialRequest
             * @augments lebai.serial.WriteSerialRequest.$Properties
             * @deprecated Use lebai.serial.WriteSerialRequest.$Properties instead.
             */

            /**
             * Shape of a WriteSerialRequest.
             * @typedef {lebai.serial.WriteSerialRequest.$Properties} lebai.serial.WriteSerialRequest.$Shape
             */

            /**
             * Constructs a new WriteSerialRequest.
             * @memberof lebai.serial
             * @classdesc Represents a WriteSerialRequest.
             * @constructor
             * @param {lebai.serial.WriteSerialRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var WriteSerialRequest = function (properties) {
                this.data = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * WriteSerialRequest device.
             * @member {string} device
             * @memberof lebai.serial.WriteSerialRequest
             * @instance
             */
            WriteSerialRequest.prototype.device = "";

            /**
             * WriteSerialRequest data.
             * @member {Array.<number>} data
             * @memberof lebai.serial.WriteSerialRequest
             * @instance
             */
            WriteSerialRequest.prototype.data = $util.emptyArray;

            /**
             * Creates a new WriteSerialRequest instance using the specified properties.
             * @function create
             * @memberof lebai.serial.WriteSerialRequest
             * @static
             * @param {lebai.serial.WriteSerialRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.serial.WriteSerialRequest} WriteSerialRequest instance
             * @type {{
             *   (properties: lebai.serial.WriteSerialRequest.$Shape): lebai.serial.WriteSerialRequest & lebai.serial.WriteSerialRequest.$Shape;
             *   (properties?: lebai.serial.WriteSerialRequest.$Properties): lebai.serial.WriteSerialRequest;
             * }}
             */
            WriteSerialRequest.create = function(properties) {
                return new WriteSerialRequest(properties);
            };

            /**
             * Encodes the specified WriteSerialRequest message. Does not implicitly {@link lebai.serial.WriteSerialRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.serial.WriteSerialRequest
             * @static
             * @param {lebai.serial.WriteSerialRequest.$Properties} message WriteSerialRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WriteSerialRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.data != null && message.data.length)
                    writer.uint32(/* id 11, wireType 2 =*/90).uint32s(message.data);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified WriteSerialRequest message, length delimited. Does not implicitly {@link lebai.serial.WriteSerialRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.serial.WriteSerialRequest
             * @static
             * @param {lebai.serial.WriteSerialRequest.$Properties} message WriteSerialRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WriteSerialRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a WriteSerialRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.serial.WriteSerialRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.serial.WriteSerialRequest & lebai.serial.WriteSerialRequest.$Shape} WriteSerialRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WriteSerialRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.serial.WriteSerialRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.data && message.data.length))
                                    message.data = [];
                                reader.uint32s(message.data);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.data && message.data.length))
                                message.data = [];
                            message.data.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a WriteSerialRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.serial.WriteSerialRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.serial.WriteSerialRequest & lebai.serial.WriteSerialRequest.$Shape} WriteSerialRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WriteSerialRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a WriteSerialRequest message.
             * @function verify
             * @memberof lebai.serial.WriteSerialRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            WriteSerialRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    if (!$Array.isArray(message.data))
                        return "data: array expected";
                    for (var i = 0; i < message.data.length; ++i)
                        if (!$util.isInteger(message.data[i]))
                            return "data: integer[] expected";
                }
                return null;
            };

            /**
             * Creates a WriteSerialRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.serial.WriteSerialRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.serial.WriteSerialRequest} WriteSerialRequest
             */
            WriteSerialRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.serial.WriteSerialRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.serial.WriteSerialRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.serial.WriteSerialRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.data) {
                    if (!$Array.isArray(object.data))
                        throw $TypeError(".lebai.serial.WriteSerialRequest.data: array expected");
                    message.data = $Array(object.data.length);
                    for (var i = 0; i < object.data.length; ++i)
                        message.data[i] = object.data[i] >>> 0;
                }
                return message;
            };

            /**
             * Creates a plain object from a WriteSerialRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.serial.WriteSerialRequest
             * @static
             * @param {lebai.serial.WriteSerialRequest} message WriteSerialRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            WriteSerialRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.data = [];
                if (options.defaults)
                    object.device = "";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.data && message.data.length) {
                    object.data = $Array(message.data.length);
                    for (var j = 0; j < message.data.length; ++j)
                        object.data[j] = message.data[j];
                }
                return object;
            };

            /**
             * Converts this WriteSerialRequest to JSON.
             * @function toJSON
             * @memberof lebai.serial.WriteSerialRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            WriteSerialRequest.prototype.toJSON = function() {
                return WriteSerialRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for WriteSerialRequest
             * @function getTypeUrl
             * @memberof lebai.serial.WriteSerialRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            WriteSerialRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.serial.WriteSerialRequest";
            };

            return WriteSerialRequest;
        })();

        serial.ReadSerialRequest = (function() {

            /**
             * Properties of a ReadSerialRequest.
             * @typedef {Object} lebai.serial.ReadSerialRequest.$Properties
             * @property {string|null} [device] ReadSerialRequest device
             * @property {number|null} [len] ReadSerialRequest len
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ReadSerialRequest.
             * @memberof lebai.serial
             * @interface IReadSerialRequest
             * @augments lebai.serial.ReadSerialRequest.$Properties
             * @deprecated Use lebai.serial.ReadSerialRequest.$Properties instead.
             */

            /**
             * Shape of a ReadSerialRequest.
             * @typedef {lebai.serial.ReadSerialRequest.$Properties} lebai.serial.ReadSerialRequest.$Shape
             */

            /**
             * Constructs a new ReadSerialRequest.
             * @memberof lebai.serial
             * @classdesc Represents a ReadSerialRequest.
             * @constructor
             * @param {lebai.serial.ReadSerialRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ReadSerialRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ReadSerialRequest device.
             * @member {string} device
             * @memberof lebai.serial.ReadSerialRequest
             * @instance
             */
            ReadSerialRequest.prototype.device = "";

            /**
             * ReadSerialRequest len.
             * @member {number} len
             * @memberof lebai.serial.ReadSerialRequest
             * @instance
             */
            ReadSerialRequest.prototype.len = 0;

            /**
             * Creates a new ReadSerialRequest instance using the specified properties.
             * @function create
             * @memberof lebai.serial.ReadSerialRequest
             * @static
             * @param {lebai.serial.ReadSerialRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.serial.ReadSerialRequest} ReadSerialRequest instance
             * @type {{
             *   (properties: lebai.serial.ReadSerialRequest.$Shape): lebai.serial.ReadSerialRequest & lebai.serial.ReadSerialRequest.$Shape;
             *   (properties?: lebai.serial.ReadSerialRequest.$Properties): lebai.serial.ReadSerialRequest;
             * }}
             */
            ReadSerialRequest.create = function(properties) {
                return new ReadSerialRequest(properties);
            };

            /**
             * Encodes the specified ReadSerialRequest message. Does not implicitly {@link lebai.serial.ReadSerialRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.serial.ReadSerialRequest
             * @static
             * @param {lebai.serial.ReadSerialRequest.$Properties} message ReadSerialRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ReadSerialRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.len != null && $Object.hasOwnProperty.call(message, "len") && message.len !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.len);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ReadSerialRequest message, length delimited. Does not implicitly {@link lebai.serial.ReadSerialRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.serial.ReadSerialRequest
             * @static
             * @param {lebai.serial.ReadSerialRequest.$Properties} message ReadSerialRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ReadSerialRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ReadSerialRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.serial.ReadSerialRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.serial.ReadSerialRequest & lebai.serial.ReadSerialRequest.$Shape} ReadSerialRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ReadSerialRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.serial.ReadSerialRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.len = value;
                            else
                                delete message.len;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ReadSerialRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.serial.ReadSerialRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.serial.ReadSerialRequest & lebai.serial.ReadSerialRequest.$Shape} ReadSerialRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ReadSerialRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ReadSerialRequest message.
             * @function verify
             * @memberof lebai.serial.ReadSerialRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ReadSerialRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                if (message.len != null && $Object.hasOwnProperty.call(message, "len"))
                    if (!$util.isInteger(message.len))
                        return "len: integer expected";
                return null;
            };

            /**
             * Creates a ReadSerialRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.serial.ReadSerialRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.serial.ReadSerialRequest} ReadSerialRequest
             */
            ReadSerialRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.serial.ReadSerialRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.serial.ReadSerialRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.serial.ReadSerialRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                if (object.len != null)
                    if ($Number(object.len) !== 0)
                        message.len = object.len >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a ReadSerialRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.serial.ReadSerialRequest
             * @static
             * @param {lebai.serial.ReadSerialRequest} message ReadSerialRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ReadSerialRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.device = "";
                    object.len = 0;
                }
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.len != null && $Object.hasOwnProperty.call(message, "len"))
                    object.len = message.len;
                return object;
            };

            /**
             * Converts this ReadSerialRequest to JSON.
             * @function toJSON
             * @memberof lebai.serial.ReadSerialRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ReadSerialRequest.prototype.toJSON = function() {
                return ReadSerialRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ReadSerialRequest
             * @function getTypeUrl
             * @memberof lebai.serial.ReadSerialRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ReadSerialRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.serial.ReadSerialRequest";
            };

            return ReadSerialRequest;
        })();

        serial.ReadSerialResponse = (function() {

            /**
             * Properties of a ReadSerialResponse.
             * @typedef {Object} lebai.serial.ReadSerialResponse.$Properties
             * @property {Array.<number>|null} [data] ReadSerialResponse data
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ReadSerialResponse.
             * @memberof lebai.serial
             * @interface IReadSerialResponse
             * @augments lebai.serial.ReadSerialResponse.$Properties
             * @deprecated Use lebai.serial.ReadSerialResponse.$Properties instead.
             */

            /**
             * Shape of a ReadSerialResponse.
             * @typedef {lebai.serial.ReadSerialResponse.$Properties} lebai.serial.ReadSerialResponse.$Shape
             */

            /**
             * Constructs a new ReadSerialResponse.
             * @memberof lebai.serial
             * @classdesc Represents a ReadSerialResponse.
             * @constructor
             * @param {lebai.serial.ReadSerialResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ReadSerialResponse = function (properties) {
                this.data = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ReadSerialResponse data.
             * @member {Array.<number>} data
             * @memberof lebai.serial.ReadSerialResponse
             * @instance
             */
            ReadSerialResponse.prototype.data = $util.emptyArray;

            /**
             * Creates a new ReadSerialResponse instance using the specified properties.
             * @function create
             * @memberof lebai.serial.ReadSerialResponse
             * @static
             * @param {lebai.serial.ReadSerialResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.serial.ReadSerialResponse} ReadSerialResponse instance
             * @type {{
             *   (properties: lebai.serial.ReadSerialResponse.$Shape): lebai.serial.ReadSerialResponse & lebai.serial.ReadSerialResponse.$Shape;
             *   (properties?: lebai.serial.ReadSerialResponse.$Properties): lebai.serial.ReadSerialResponse;
             * }}
             */
            ReadSerialResponse.create = function(properties) {
                return new ReadSerialResponse(properties);
            };

            /**
             * Encodes the specified ReadSerialResponse message. Does not implicitly {@link lebai.serial.ReadSerialResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.serial.ReadSerialResponse
             * @static
             * @param {lebai.serial.ReadSerialResponse.$Properties} message ReadSerialResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ReadSerialResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.data != null && message.data.length)
                    writer.uint32(/* id 11, wireType 2 =*/90).uint32s(message.data);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ReadSerialResponse message, length delimited. Does not implicitly {@link lebai.serial.ReadSerialResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.serial.ReadSerialResponse
             * @static
             * @param {lebai.serial.ReadSerialResponse.$Properties} message ReadSerialResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ReadSerialResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ReadSerialResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.serial.ReadSerialResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.serial.ReadSerialResponse & lebai.serial.ReadSerialResponse.$Shape} ReadSerialResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ReadSerialResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.serial.ReadSerialResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 11: {
                            if (wireType === 2) {
                                if (!(message.data && message.data.length))
                                    message.data = [];
                                reader.uint32s(message.data);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.data && message.data.length))
                                message.data = [];
                            message.data.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ReadSerialResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.serial.ReadSerialResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.serial.ReadSerialResponse & lebai.serial.ReadSerialResponse.$Shape} ReadSerialResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ReadSerialResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ReadSerialResponse message.
             * @function verify
             * @memberof lebai.serial.ReadSerialResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ReadSerialResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    if (!$Array.isArray(message.data))
                        return "data: array expected";
                    for (var i = 0; i < message.data.length; ++i)
                        if (!$util.isInteger(message.data[i]))
                            return "data: integer[] expected";
                }
                return null;
            };

            /**
             * Creates a ReadSerialResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.serial.ReadSerialResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.serial.ReadSerialResponse} ReadSerialResponse
             */
            ReadSerialResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.serial.ReadSerialResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.serial.ReadSerialResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.serial.ReadSerialResponse();
                if (object.data) {
                    if (!$Array.isArray(object.data))
                        throw $TypeError(".lebai.serial.ReadSerialResponse.data: array expected");
                    message.data = $Array(object.data.length);
                    for (var i = 0; i < object.data.length; ++i)
                        message.data[i] = object.data[i] >>> 0;
                }
                return message;
            };

            /**
             * Creates a plain object from a ReadSerialResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.serial.ReadSerialResponse
             * @static
             * @param {lebai.serial.ReadSerialResponse} message ReadSerialResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ReadSerialResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.data = [];
                if (message.data && message.data.length) {
                    object.data = $Array(message.data.length);
                    for (var j = 0; j < message.data.length; ++j)
                        object.data[j] = message.data[j];
                }
                return object;
            };

            /**
             * Converts this ReadSerialResponse to JSON.
             * @function toJSON
             * @memberof lebai.serial.ReadSerialResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ReadSerialResponse.prototype.toJSON = function() {
                return ReadSerialResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ReadSerialResponse
             * @function getTypeUrl
             * @memberof lebai.serial.ReadSerialResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ReadSerialResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.serial.ReadSerialResponse";
            };

            return ReadSerialResponse;
        })();

        serial.ClearSerialRequest = (function() {

            /**
             * Properties of a ClearSerialRequest.
             * @typedef {Object} lebai.serial.ClearSerialRequest.$Properties
             * @property {string|null} [device] ClearSerialRequest device
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ClearSerialRequest.
             * @memberof lebai.serial
             * @interface IClearSerialRequest
             * @augments lebai.serial.ClearSerialRequest.$Properties
             * @deprecated Use lebai.serial.ClearSerialRequest.$Properties instead.
             */

            /**
             * Shape of a ClearSerialRequest.
             * @typedef {lebai.serial.ClearSerialRequest.$Properties} lebai.serial.ClearSerialRequest.$Shape
             */

            /**
             * Constructs a new ClearSerialRequest.
             * @memberof lebai.serial
             * @classdesc Represents a ClearSerialRequest.
             * @constructor
             * @param {lebai.serial.ClearSerialRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ClearSerialRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ClearSerialRequest device.
             * @member {string} device
             * @memberof lebai.serial.ClearSerialRequest
             * @instance
             */
            ClearSerialRequest.prototype.device = "";

            /**
             * Creates a new ClearSerialRequest instance using the specified properties.
             * @function create
             * @memberof lebai.serial.ClearSerialRequest
             * @static
             * @param {lebai.serial.ClearSerialRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.serial.ClearSerialRequest} ClearSerialRequest instance
             * @type {{
             *   (properties: lebai.serial.ClearSerialRequest.$Shape): lebai.serial.ClearSerialRequest & lebai.serial.ClearSerialRequest.$Shape;
             *   (properties?: lebai.serial.ClearSerialRequest.$Properties): lebai.serial.ClearSerialRequest;
             * }}
             */
            ClearSerialRequest.create = function(properties) {
                return new ClearSerialRequest(properties);
            };

            /**
             * Encodes the specified ClearSerialRequest message. Does not implicitly {@link lebai.serial.ClearSerialRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.serial.ClearSerialRequest
             * @static
             * @param {lebai.serial.ClearSerialRequest.$Properties} message ClearSerialRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ClearSerialRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.device != null && $Object.hasOwnProperty.call(message, "device") && message.device !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.device);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ClearSerialRequest message, length delimited. Does not implicitly {@link lebai.serial.ClearSerialRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.serial.ClearSerialRequest
             * @static
             * @param {lebai.serial.ClearSerialRequest.$Properties} message ClearSerialRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ClearSerialRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ClearSerialRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.serial.ClearSerialRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.serial.ClearSerialRequest & lebai.serial.ClearSerialRequest.$Shape} ClearSerialRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ClearSerialRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.serial.ClearSerialRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.device = value;
                            else
                                delete message.device;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ClearSerialRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.serial.ClearSerialRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.serial.ClearSerialRequest & lebai.serial.ClearSerialRequest.$Shape} ClearSerialRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ClearSerialRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ClearSerialRequest message.
             * @function verify
             * @memberof lebai.serial.ClearSerialRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ClearSerialRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    if (!$util.isString(message.device))
                        return "device: string expected";
                return null;
            };

            /**
             * Creates a ClearSerialRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.serial.ClearSerialRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.serial.ClearSerialRequest} ClearSerialRequest
             */
            ClearSerialRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.serial.ClearSerialRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.serial.ClearSerialRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.serial.ClearSerialRequest();
                if (object.device != null)
                    if (typeof object.device !== "string" || object.device.length)
                        message.device = $String(object.device);
                return message;
            };

            /**
             * Creates a plain object from a ClearSerialRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.serial.ClearSerialRequest
             * @static
             * @param {lebai.serial.ClearSerialRequest} message ClearSerialRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ClearSerialRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.device = "";
                if (message.device != null && $Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                return object;
            };

            /**
             * Converts this ClearSerialRequest to JSON.
             * @function toJSON
             * @memberof lebai.serial.ClearSerialRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ClearSerialRequest.prototype.toJSON = function() {
                return ClearSerialRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ClearSerialRequest
             * @function getTypeUrl
             * @memberof lebai.serial.ClearSerialRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ClearSerialRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.serial.ClearSerialRequest";
            };

            return ClearSerialRequest;
        })();

        serial.SerialService = (function() {

            /**
             * Constructs a new SerialService service.
             * @memberof lebai.serial
             * @classdesc Represents a SerialService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var SerialService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(SerialService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: SerialService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new SerialService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.serial.SerialService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {SerialService} RPC service. Useful where requests and/or responses are streamed.
             */
            SerialService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.serial.SerialService#setSerialTimeout}.
             * @memberof lebai.serial.SerialService
             * @typedef SetSerialTimeoutCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetSerialTimeout.
             * @memberof lebai.serial.SerialService
             * @typedef SetSerialTimeout
             * @type {{
             *   (request: lebai.serial.ISetSerialTimeoutRequest, callback: lebai.serial.SerialService.SetSerialTimeoutCallback): void;
             *   (request: lebai.serial.ISetSerialTimeoutRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetSerialTimeout";
             *   readonly path: "/lebai.serial.SerialService/SetSerialTimeout";
             *   readonly requestType: "SetSerialTimeoutRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetSerialTimeout.
             * @name lebai.serial.SerialService#setSerialTimeout
             * @type {lebai.serial.SerialService.SetSerialTimeout}
             */
            $Object.defineProperties(SerialService.prototype.setSerialTimeout = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SerialService.prototype.setSerialTimeout, $root.lebai.serial.SetSerialTimeoutRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetSerialTimeout" },
                path: { value: "/lebai.serial.SerialService/SetSerialTimeout" },
                requestType: { value: "SetSerialTimeoutRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.serial.SerialService#setSerialBaudRate}.
             * @memberof lebai.serial.SerialService
             * @typedef SetSerialBaudRateCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetSerialBaudRate.
             * @memberof lebai.serial.SerialService
             * @typedef SetSerialBaudRate
             * @type {{
             *   (request: lebai.serial.ISetSerialBaudRateRequest, callback: lebai.serial.SerialService.SetSerialBaudRateCallback): void;
             *   (request: lebai.serial.ISetSerialBaudRateRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetSerialBaudRate";
             *   readonly path: "/lebai.serial.SerialService/SetSerialBaudRate";
             *   readonly requestType: "SetSerialBaudRateRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetSerialBaudRate.
             * @name lebai.serial.SerialService#setSerialBaudRate
             * @type {lebai.serial.SerialService.SetSerialBaudRate}
             */
            $Object.defineProperties(SerialService.prototype.setSerialBaudRate = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SerialService.prototype.setSerialBaudRate, $root.lebai.serial.SetSerialBaudRateRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetSerialBaudRate" },
                path: { value: "/lebai.serial.SerialService/SetSerialBaudRate" },
                requestType: { value: "SetSerialBaudRateRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.serial.SerialService#setSerialParity}.
             * @memberof lebai.serial.SerialService
             * @typedef SetSerialParityCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetSerialParity.
             * @memberof lebai.serial.SerialService
             * @typedef SetSerialParity
             * @type {{
             *   (request: lebai.serial.ISetSerialParityRequest, callback: lebai.serial.SerialService.SetSerialParityCallback): void;
             *   (request: lebai.serial.ISetSerialParityRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetSerialParity";
             *   readonly path: "/lebai.serial.SerialService/SetSerialParity";
             *   readonly requestType: "SetSerialParityRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetSerialParity.
             * @name lebai.serial.SerialService#setSerialParity
             * @type {lebai.serial.SerialService.SetSerialParity}
             */
            $Object.defineProperties(SerialService.prototype.setSerialParity = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SerialService.prototype.setSerialParity, $root.lebai.serial.SetSerialParityRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetSerialParity" },
                path: { value: "/lebai.serial.SerialService/SetSerialParity" },
                requestType: { value: "SetSerialParityRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.serial.SerialService#writeSerial}.
             * @memberof lebai.serial.SerialService
             * @typedef WriteSerialCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls WriteSerial.
             * @memberof lebai.serial.SerialService
             * @typedef WriteSerial
             * @type {{
             *   (request: lebai.serial.IWriteSerialRequest, callback: lebai.serial.SerialService.WriteSerialCallback): void;
             *   (request: lebai.serial.IWriteSerialRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "WriteSerial";
             *   readonly path: "/lebai.serial.SerialService/WriteSerial";
             *   readonly requestType: "WriteSerialRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls WriteSerial.
             * @name lebai.serial.SerialService#writeSerial
             * @type {lebai.serial.SerialService.WriteSerial}
             */
            $Object.defineProperties(SerialService.prototype.writeSerial = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SerialService.prototype.writeSerial, $root.lebai.serial.WriteSerialRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "WriteSerial" },
                path: { value: "/lebai.serial.SerialService/WriteSerial" },
                requestType: { value: "WriteSerialRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.serial.SerialService#readSerial}.
             * @memberof lebai.serial.SerialService
             * @typedef ReadSerialCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.serial.ReadSerialResponse} [response] ReadSerialResponse
             */

            /**
             * Calls ReadSerial.
             * @memberof lebai.serial.SerialService
             * @typedef ReadSerial
             * @type {{
             *   (request: lebai.serial.IReadSerialRequest, callback: lebai.serial.SerialService.ReadSerialCallback): void;
             *   (request: lebai.serial.IReadSerialRequest): Promise<lebai.serial.ReadSerialResponse>;
             *   readonly name: "ReadSerial";
             *   readonly path: "/lebai.serial.SerialService/ReadSerial";
             *   readonly requestType: "ReadSerialRequest";
             *   readonly responseType: "ReadSerialResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls ReadSerial.
             * @name lebai.serial.SerialService#readSerial
             * @type {lebai.serial.SerialService.ReadSerial}
             */
            $Object.defineProperties(SerialService.prototype.readSerial = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SerialService.prototype.readSerial, $root.lebai.serial.ReadSerialRequest, $root.lebai.serial.ReadSerialResponse, request, callback);
            }, {
                name: { value: "ReadSerial" },
                path: { value: "/lebai.serial.SerialService/ReadSerial" },
                requestType: { value: "ReadSerialRequest" },
                responseType: { value: "ReadSerialResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.serial.SerialService#clearSerial}.
             * @memberof lebai.serial.SerialService
             * @typedef ClearSerialCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls ClearSerial.
             * @memberof lebai.serial.SerialService
             * @typedef ClearSerial
             * @type {{
             *   (request: lebai.serial.IClearSerialRequest, callback: lebai.serial.SerialService.ClearSerialCallback): void;
             *   (request: lebai.serial.IClearSerialRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "ClearSerial";
             *   readonly path: "/lebai.serial.SerialService/ClearSerial";
             *   readonly requestType: "ClearSerialRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls ClearSerial.
             * @name lebai.serial.SerialService#clearSerial
             * @type {lebai.serial.SerialService.ClearSerial}
             */
            $Object.defineProperties(SerialService.prototype.clearSerial = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SerialService.prototype.clearSerial, $root.lebai.serial.ClearSerialRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "ClearSerial" },
                path: { value: "/lebai.serial.SerialService/ClearSerial" },
                requestType: { value: "ClearSerialRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return SerialService;
        })();

        return serial;
    })();

    lebai.shortcut = (function() {

        /**
         * Namespace shortcut.
         * @memberof lebai
         * @namespace
         */
        var shortcut = {};

        shortcut.ShortcutIndex = (function() {

            /**
             * Properties of a ShortcutIndex.
             * @typedef {Object} lebai.shortcut.ShortcutIndex.$Properties
             * @property {number|null} [id] ShortcutIndex id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ShortcutIndex.
             * @memberof lebai.shortcut
             * @interface IShortcutIndex
             * @augments lebai.shortcut.ShortcutIndex.$Properties
             * @deprecated Use lebai.shortcut.ShortcutIndex.$Properties instead.
             */

            /**
             * Shape of a ShortcutIndex.
             * @typedef {lebai.shortcut.ShortcutIndex.$Properties} lebai.shortcut.ShortcutIndex.$Shape
             */

            /**
             * Constructs a new ShortcutIndex.
             * @memberof lebai.shortcut
             * @classdesc Represents a ShortcutIndex.
             * @constructor
             * @param {lebai.shortcut.ShortcutIndex.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ShortcutIndex = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ShortcutIndex id.
             * @member {number} id
             * @memberof lebai.shortcut.ShortcutIndex
             * @instance
             */
            ShortcutIndex.prototype.id = 0;

            /**
             * Creates a new ShortcutIndex instance using the specified properties.
             * @function create
             * @memberof lebai.shortcut.ShortcutIndex
             * @static
             * @param {lebai.shortcut.ShortcutIndex.$Properties=} [properties] Properties to set
             * @returns {lebai.shortcut.ShortcutIndex} ShortcutIndex instance
             * @type {{
             *   (properties: lebai.shortcut.ShortcutIndex.$Shape): lebai.shortcut.ShortcutIndex & lebai.shortcut.ShortcutIndex.$Shape;
             *   (properties?: lebai.shortcut.ShortcutIndex.$Properties): lebai.shortcut.ShortcutIndex;
             * }}
             */
            ShortcutIndex.create = function(properties) {
                return new ShortcutIndex(properties);
            };

            /**
             * Encodes the specified ShortcutIndex message. Does not implicitly {@link lebai.shortcut.ShortcutIndex.verify|verify} messages.
             * @function encode
             * @memberof lebai.shortcut.ShortcutIndex
             * @static
             * @param {lebai.shortcut.ShortcutIndex.$Properties} message ShortcutIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ShortcutIndex.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ShortcutIndex message, length delimited. Does not implicitly {@link lebai.shortcut.ShortcutIndex.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.shortcut.ShortcutIndex
             * @static
             * @param {lebai.shortcut.ShortcutIndex.$Properties} message ShortcutIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ShortcutIndex.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ShortcutIndex message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.shortcut.ShortcutIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.shortcut.ShortcutIndex & lebai.shortcut.ShortcutIndex.$Shape} ShortcutIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ShortcutIndex.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.shortcut.ShortcutIndex();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ShortcutIndex message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.shortcut.ShortcutIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.shortcut.ShortcutIndex & lebai.shortcut.ShortcutIndex.$Shape} ShortcutIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ShortcutIndex.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ShortcutIndex message.
             * @function verify
             * @memberof lebai.shortcut.ShortcutIndex
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ShortcutIndex.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                return null;
            };

            /**
             * Creates a ShortcutIndex message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.shortcut.ShortcutIndex
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.shortcut.ShortcutIndex} ShortcutIndex
             */
            ShortcutIndex.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.shortcut.ShortcutIndex)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.shortcut.ShortcutIndex: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.shortcut.ShortcutIndex();
                if (object.id != null)
                    if ($Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a ShortcutIndex message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.shortcut.ShortcutIndex
             * @static
             * @param {lebai.shortcut.ShortcutIndex} message ShortcutIndex
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ShortcutIndex.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.id = 0;
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    object.id = message.id;
                return object;
            };

            /**
             * Converts this ShortcutIndex to JSON.
             * @function toJSON
             * @memberof lebai.shortcut.ShortcutIndex
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ShortcutIndex.prototype.toJSON = function() {
                return ShortcutIndex.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ShortcutIndex
             * @function getTypeUrl
             * @memberof lebai.shortcut.ShortcutIndex
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ShortcutIndex.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.shortcut.ShortcutIndex";
            };

            return ShortcutIndex;
        })();

        shortcut.Shortcut = (function() {

            /**
             * Properties of a Shortcut.
             * @typedef {Object} lebai.shortcut.Shortcut.$Properties
             * @property {number|null} [id] Shortcut id
             * @property {string|null} [dir] Shortcut dir
             * @property {string|null} [name] Shortcut name
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Shortcut.
             * @memberof lebai.shortcut
             * @interface IShortcut
             * @augments lebai.shortcut.Shortcut.$Properties
             * @deprecated Use lebai.shortcut.Shortcut.$Properties instead.
             */

            /**
             * Shape of a Shortcut.
             * @typedef {lebai.shortcut.Shortcut.$Properties} lebai.shortcut.Shortcut.$Shape
             */

            /**
             * Constructs a new Shortcut.
             * @memberof lebai.shortcut
             * @classdesc Represents a Shortcut.
             * @constructor
             * @param {lebai.shortcut.Shortcut.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Shortcut = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Shortcut id.
             * @member {number} id
             * @memberof lebai.shortcut.Shortcut
             * @instance
             */
            Shortcut.prototype.id = 0;

            /**
             * Shortcut dir.
             * @member {string} dir
             * @memberof lebai.shortcut.Shortcut
             * @instance
             */
            Shortcut.prototype.dir = "";

            /**
             * Shortcut name.
             * @member {string} name
             * @memberof lebai.shortcut.Shortcut
             * @instance
             */
            Shortcut.prototype.name = "";

            /**
             * Creates a new Shortcut instance using the specified properties.
             * @function create
             * @memberof lebai.shortcut.Shortcut
             * @static
             * @param {lebai.shortcut.Shortcut.$Properties=} [properties] Properties to set
             * @returns {lebai.shortcut.Shortcut} Shortcut instance
             * @type {{
             *   (properties: lebai.shortcut.Shortcut.$Shape): lebai.shortcut.Shortcut & lebai.shortcut.Shortcut.$Shape;
             *   (properties?: lebai.shortcut.Shortcut.$Properties): lebai.shortcut.Shortcut;
             * }}
             */
            Shortcut.create = function(properties) {
                return new Shortcut(properties);
            };

            /**
             * Encodes the specified Shortcut message. Does not implicitly {@link lebai.shortcut.Shortcut.verify|verify} messages.
             * @function encode
             * @memberof lebai.shortcut.Shortcut
             * @static
             * @param {lebai.shortcut.Shortcut.$Properties} message Shortcut message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Shortcut.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.id);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.dir);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Shortcut message, length delimited. Does not implicitly {@link lebai.shortcut.Shortcut.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.shortcut.Shortcut
             * @static
             * @param {lebai.shortcut.Shortcut.$Properties} message Shortcut message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Shortcut.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Shortcut message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.shortcut.Shortcut
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.shortcut.Shortcut & lebai.shortcut.Shortcut.$Shape} Shortcut
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Shortcut.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.shortcut.Shortcut();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Shortcut message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.shortcut.Shortcut
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.shortcut.Shortcut & lebai.shortcut.Shortcut.$Shape} Shortcut
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Shortcut.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Shortcut message.
             * @function verify
             * @memberof lebai.shortcut.Shortcut
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Shortcut.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                return null;
            };

            /**
             * Creates a Shortcut message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.shortcut.Shortcut
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.shortcut.Shortcut} Shortcut
             */
            Shortcut.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.shortcut.Shortcut)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.shortcut.Shortcut: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.shortcut.Shortcut();
                if (object.id != null)
                    if ($Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                return message;
            };

            /**
             * Creates a plain object from a Shortcut message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.shortcut.Shortcut
             * @static
             * @param {lebai.shortcut.Shortcut} message Shortcut
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Shortcut.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.dir = "";
                    object.name = "";
                }
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    object.id = message.id;
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                return object;
            };

            /**
             * Converts this Shortcut to JSON.
             * @function toJSON
             * @memberof lebai.shortcut.Shortcut
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Shortcut.prototype.toJSON = function() {
                return Shortcut.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Shortcut
             * @function getTypeUrl
             * @memberof lebai.shortcut.Shortcut
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Shortcut.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.shortcut.Shortcut";
            };

            return Shortcut;
        })();

        shortcut.ShortcutList = (function() {

            /**
             * Properties of a ShortcutList.
             * @typedef {Object} lebai.shortcut.ShortcutList.$Properties
             * @property {Array.<lebai.shortcut.Shortcut.$Properties>|null} [list] ShortcutList list
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a ShortcutList.
             * @memberof lebai.shortcut
             * @interface IShortcutList
             * @augments lebai.shortcut.ShortcutList.$Properties
             * @deprecated Use lebai.shortcut.ShortcutList.$Properties instead.
             */

            /**
             * Shape of a ShortcutList.
             * @typedef {lebai.shortcut.ShortcutList.$Properties} lebai.shortcut.ShortcutList.$Shape
             */

            /**
             * Constructs a new ShortcutList.
             * @memberof lebai.shortcut
             * @classdesc Represents a ShortcutList.
             * @constructor
             * @param {lebai.shortcut.ShortcutList.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ShortcutList = function (properties) {
                this.list = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ShortcutList list.
             * @member {Array.<lebai.shortcut.Shortcut.$Properties>} list
             * @memberof lebai.shortcut.ShortcutList
             * @instance
             */
            ShortcutList.prototype.list = $util.emptyArray;

            /**
             * Creates a new ShortcutList instance using the specified properties.
             * @function create
             * @memberof lebai.shortcut.ShortcutList
             * @static
             * @param {lebai.shortcut.ShortcutList.$Properties=} [properties] Properties to set
             * @returns {lebai.shortcut.ShortcutList} ShortcutList instance
             * @type {{
             *   (properties: lebai.shortcut.ShortcutList.$Shape): lebai.shortcut.ShortcutList & lebai.shortcut.ShortcutList.$Shape;
             *   (properties?: lebai.shortcut.ShortcutList.$Properties): lebai.shortcut.ShortcutList;
             * }}
             */
            ShortcutList.create = function(properties) {
                return new ShortcutList(properties);
            };

            /**
             * Encodes the specified ShortcutList message. Does not implicitly {@link lebai.shortcut.ShortcutList.verify|verify} messages.
             * @function encode
             * @memberof lebai.shortcut.ShortcutList
             * @static
             * @param {lebai.shortcut.ShortcutList.$Properties} message ShortcutList message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ShortcutList.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.list != null && message.list.length)
                    for (var i = 0; i < message.list.length; ++i)
                        $root.lebai.shortcut.Shortcut.encode(message.list[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ShortcutList message, length delimited. Does not implicitly {@link lebai.shortcut.ShortcutList.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.shortcut.ShortcutList
             * @static
             * @param {lebai.shortcut.ShortcutList.$Properties} message ShortcutList message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ShortcutList.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a ShortcutList message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.shortcut.ShortcutList
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.shortcut.ShortcutList & lebai.shortcut.ShortcutList.$Shape} ShortcutList
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ShortcutList.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.shortcut.ShortcutList();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.list && message.list.length))
                                message.list = [];
                            message.list.push($root.lebai.shortcut.Shortcut.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a ShortcutList message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.shortcut.ShortcutList
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.shortcut.ShortcutList & lebai.shortcut.ShortcutList.$Shape} ShortcutList
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ShortcutList.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ShortcutList message.
             * @function verify
             * @memberof lebai.shortcut.ShortcutList
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ShortcutList.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.list != null && $Object.hasOwnProperty.call(message, "list")) {
                    if (!$Array.isArray(message.list))
                        return "list: array expected";
                    for (var i = 0; i < message.list.length; ++i) {
                        var error = $root.lebai.shortcut.Shortcut.verify(message.list[i], _depth + 1);
                        if (error)
                            return "list." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ShortcutList message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.shortcut.ShortcutList
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.shortcut.ShortcutList} ShortcutList
             */
            ShortcutList.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.shortcut.ShortcutList)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.shortcut.ShortcutList: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.shortcut.ShortcutList();
                if (object.list) {
                    if (!$Array.isArray(object.list))
                        throw $TypeError(".lebai.shortcut.ShortcutList.list: array expected");
                    message.list = $Array(object.list.length);
                    for (var i = 0; i < object.list.length; ++i) {
                        if (!$util.isObject(object.list[i]))
                            throw $TypeError(".lebai.shortcut.ShortcutList.list: object expected");
                        message.list[i] = $root.lebai.shortcut.Shortcut.fromObject(object.list[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a ShortcutList message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.shortcut.ShortcutList
             * @static
             * @param {lebai.shortcut.ShortcutList} message ShortcutList
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ShortcutList.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.list = [];
                if (message.list && message.list.length) {
                    object.list = $Array(message.list.length);
                    for (var j = 0; j < message.list.length; ++j)
                        object.list[j] = $root.lebai.shortcut.Shortcut.toObject(message.list[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this ShortcutList to JSON.
             * @function toJSON
             * @memberof lebai.shortcut.ShortcutList
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ShortcutList.prototype.toJSON = function() {
                return ShortcutList.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ShortcutList
             * @function getTypeUrl
             * @memberof lebai.shortcut.ShortcutList
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ShortcutList.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.shortcut.ShortcutList";
            };

            return ShortcutList;
        })();

        shortcut.ShortcutService = (function() {

            /**
             * Constructs a new ShortcutService service.
             * @memberof lebai.shortcut
             * @classdesc Represents a ShortcutService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var ShortcutService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(ShortcutService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: ShortcutService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new ShortcutService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.shortcut.ShortcutService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {ShortcutService} RPC service. Useful where requests and/or responses are streamed.
             */
            ShortcutService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.shortcut.ShortcutService#setShortPose}.
             * @memberof lebai.shortcut.ShortcutService
             * @typedef SetShortPoseCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetShortPose.
             * @memberof lebai.shortcut.ShortcutService
             * @typedef SetShortPose
             * @type {{
             *   (request: lebai.shortcut.IShortcut, callback: lebai.shortcut.ShortcutService.SetShortPoseCallback): void;
             *   (request: lebai.shortcut.IShortcut): Promise<google.protobuf.Empty>;
             *   readonly name: "SetShortPose";
             *   readonly path: "/lebai.shortcut.ShortcutService/SetShortPose";
             *   readonly requestType: "Shortcut";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetShortPose.
             * @name lebai.shortcut.ShortcutService#setShortPose
             * @type {lebai.shortcut.ShortcutService.SetShortPose}
             */
            $Object.defineProperties(ShortcutService.prototype.setShortPose = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ShortcutService.prototype.setShortPose, $root.lebai.shortcut.Shortcut, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetShortPose" },
                path: { value: "/lebai.shortcut.ShortcutService/SetShortPose" },
                requestType: { value: "Shortcut" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.shortcut.ShortcutService#getShortPose}.
             * @memberof lebai.shortcut.ShortcutService
             * @typedef GetShortPoseCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.shortcut.Shortcut} [response] Shortcut
             */

            /**
             * Calls GetShortPose.
             * @memberof lebai.shortcut.ShortcutService
             * @typedef GetShortPose
             * @type {{
             *   (request: lebai.shortcut.IShortcutIndex, callback: lebai.shortcut.ShortcutService.GetShortPoseCallback): void;
             *   (request: lebai.shortcut.IShortcutIndex): Promise<lebai.shortcut.Shortcut>;
             *   readonly name: "GetShortPose";
             *   readonly path: "/lebai.shortcut.ShortcutService/GetShortPose";
             *   readonly requestType: "ShortcutIndex";
             *   readonly responseType: "Shortcut";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetShortPose.
             * @name lebai.shortcut.ShortcutService#getShortPose
             * @type {lebai.shortcut.ShortcutService.GetShortPose}
             */
            $Object.defineProperties(ShortcutService.prototype.getShortPose = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ShortcutService.prototype.getShortPose, $root.lebai.shortcut.ShortcutIndex, $root.lebai.shortcut.Shortcut, request, callback);
            }, {
                name: { value: "GetShortPose" },
                path: { value: "/lebai.shortcut.ShortcutService/GetShortPose" },
                requestType: { value: "ShortcutIndex" },
                responseType: { value: "Shortcut" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.shortcut.ShortcutService#getShortPoses}.
             * @memberof lebai.shortcut.ShortcutService
             * @typedef GetShortPosesCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.shortcut.ShortcutList} [response] ShortcutList
             */

            /**
             * Calls GetShortPoses.
             * @memberof lebai.shortcut.ShortcutService
             * @typedef GetShortPoses
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.shortcut.ShortcutService.GetShortPosesCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.shortcut.ShortcutList>;
             *   readonly name: "GetShortPoses";
             *   readonly path: "/lebai.shortcut.ShortcutService/GetShortPoses";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "ShortcutList";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetShortPoses.
             * @name lebai.shortcut.ShortcutService#getShortPoses
             * @type {lebai.shortcut.ShortcutService.GetShortPoses}
             */
            $Object.defineProperties(ShortcutService.prototype.getShortPoses = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ShortcutService.prototype.getShortPoses, $root.google.protobuf.Empty, $root.lebai.shortcut.ShortcutList, request, callback);
            }, {
                name: { value: "GetShortPoses" },
                path: { value: "/lebai.shortcut.ShortcutService/GetShortPoses" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "ShortcutList" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.shortcut.ShortcutService#setShortTask}.
             * @memberof lebai.shortcut.ShortcutService
             * @typedef SetShortTaskCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetShortTask.
             * @memberof lebai.shortcut.ShortcutService
             * @typedef SetShortTask
             * @type {{
             *   (request: lebai.shortcut.IShortcut, callback: lebai.shortcut.ShortcutService.SetShortTaskCallback): void;
             *   (request: lebai.shortcut.IShortcut): Promise<google.protobuf.Empty>;
             *   readonly name: "SetShortTask";
             *   readonly path: "/lebai.shortcut.ShortcutService/SetShortTask";
             *   readonly requestType: "Shortcut";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetShortTask.
             * @name lebai.shortcut.ShortcutService#setShortTask
             * @type {lebai.shortcut.ShortcutService.SetShortTask}
             */
            $Object.defineProperties(ShortcutService.prototype.setShortTask = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ShortcutService.prototype.setShortTask, $root.lebai.shortcut.Shortcut, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetShortTask" },
                path: { value: "/lebai.shortcut.ShortcutService/SetShortTask" },
                requestType: { value: "Shortcut" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.shortcut.ShortcutService#getShortTask}.
             * @memberof lebai.shortcut.ShortcutService
             * @typedef GetShortTaskCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.shortcut.Shortcut} [response] Shortcut
             */

            /**
             * Calls GetShortTask.
             * @memberof lebai.shortcut.ShortcutService
             * @typedef GetShortTask
             * @type {{
             *   (request: lebai.shortcut.IShortcutIndex, callback: lebai.shortcut.ShortcutService.GetShortTaskCallback): void;
             *   (request: lebai.shortcut.IShortcutIndex): Promise<lebai.shortcut.Shortcut>;
             *   readonly name: "GetShortTask";
             *   readonly path: "/lebai.shortcut.ShortcutService/GetShortTask";
             *   readonly requestType: "ShortcutIndex";
             *   readonly responseType: "Shortcut";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetShortTask.
             * @name lebai.shortcut.ShortcutService#getShortTask
             * @type {lebai.shortcut.ShortcutService.GetShortTask}
             */
            $Object.defineProperties(ShortcutService.prototype.getShortTask = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ShortcutService.prototype.getShortTask, $root.lebai.shortcut.ShortcutIndex, $root.lebai.shortcut.Shortcut, request, callback);
            }, {
                name: { value: "GetShortTask" },
                path: { value: "/lebai.shortcut.ShortcutService/GetShortTask" },
                requestType: { value: "ShortcutIndex" },
                responseType: { value: "Shortcut" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.shortcut.ShortcutService#getShortTasks}.
             * @memberof lebai.shortcut.ShortcutService
             * @typedef GetShortTasksCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.shortcut.ShortcutList} [response] ShortcutList
             */

            /**
             * Calls GetShortTasks.
             * @memberof lebai.shortcut.ShortcutService
             * @typedef GetShortTasks
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.shortcut.ShortcutService.GetShortTasksCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.shortcut.ShortcutList>;
             *   readonly name: "GetShortTasks";
             *   readonly path: "/lebai.shortcut.ShortcutService/GetShortTasks";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "ShortcutList";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetShortTasks.
             * @name lebai.shortcut.ShortcutService#getShortTasks
             * @type {lebai.shortcut.ShortcutService.GetShortTasks}
             */
            $Object.defineProperties(ShortcutService.prototype.getShortTasks = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, ShortcutService.prototype.getShortTasks, $root.google.protobuf.Empty, $root.lebai.shortcut.ShortcutList, request, callback);
            }, {
                name: { value: "GetShortTasks" },
                path: { value: "/lebai.shortcut.ShortcutService/GetShortTasks" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "ShortcutList" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return ShortcutService;
        })();

        return shortcut;
    })();

    lebai.signal = (function() {

        /**
         * Namespace signal.
         * @memberof lebai
         * @namespace
         */
        var signal = {};

        signal.SetSignalRequest = (function() {

            /**
             * Properties of a SetSignalRequest.
             * @typedef {Object} lebai.signal.SetSignalRequest.$Properties
             * @property {number|null} [key] SetSignalRequest key
             * @property {number|null} [value] SetSignalRequest value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetSignalRequest.
             * @memberof lebai.signal
             * @interface ISetSignalRequest
             * @augments lebai.signal.SetSignalRequest.$Properties
             * @deprecated Use lebai.signal.SetSignalRequest.$Properties instead.
             */

            /**
             * Shape of a SetSignalRequest.
             * @typedef {lebai.signal.SetSignalRequest.$Properties} lebai.signal.SetSignalRequest.$Shape
             */

            /**
             * Constructs a new SetSignalRequest.
             * @memberof lebai.signal
             * @classdesc Represents a SetSignalRequest.
             * @constructor
             * @param {lebai.signal.SetSignalRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetSignalRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetSignalRequest key.
             * @member {number} key
             * @memberof lebai.signal.SetSignalRequest
             * @instance
             */
            SetSignalRequest.prototype.key = 0;

            /**
             * SetSignalRequest value.
             * @member {number} value
             * @memberof lebai.signal.SetSignalRequest
             * @instance
             */
            SetSignalRequest.prototype.value = 0;

            /**
             * Creates a new SetSignalRequest instance using the specified properties.
             * @function create
             * @memberof lebai.signal.SetSignalRequest
             * @static
             * @param {lebai.signal.SetSignalRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.signal.SetSignalRequest} SetSignalRequest instance
             * @type {{
             *   (properties: lebai.signal.SetSignalRequest.$Shape): lebai.signal.SetSignalRequest & lebai.signal.SetSignalRequest.$Shape;
             *   (properties?: lebai.signal.SetSignalRequest.$Properties): lebai.signal.SetSignalRequest;
             * }}
             */
            SetSignalRequest.create = function(properties) {
                return new SetSignalRequest(properties);
            };

            /**
             * Encodes the specified SetSignalRequest message. Does not implicitly {@link lebai.signal.SetSignalRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.signal.SetSignalRequest
             * @static
             * @param {lebai.signal.SetSignalRequest.$Properties} message SetSignalRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetSignalRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.key);
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetSignalRequest message, length delimited. Does not implicitly {@link lebai.signal.SetSignalRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.signal.SetSignalRequest
             * @static
             * @param {lebai.signal.SetSignalRequest.$Properties} message SetSignalRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetSignalRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetSignalRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.signal.SetSignalRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.signal.SetSignalRequest & lebai.signal.SetSignalRequest.$Shape} SetSignalRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetSignalRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.signal.SetSignalRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.key = value;
                            else
                                delete message.key;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetSignalRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.signal.SetSignalRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.signal.SetSignalRequest & lebai.signal.SetSignalRequest.$Shape} SetSignalRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetSignalRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetSignalRequest message.
             * @function verify
             * @memberof lebai.signal.SetSignalRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetSignalRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    if (!$util.isInteger(message.key))
                        return "key: integer expected";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                return null;
            };

            /**
             * Creates a SetSignalRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.signal.SetSignalRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.signal.SetSignalRequest} SetSignalRequest
             */
            SetSignalRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.signal.SetSignalRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.signal.SetSignalRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.signal.SetSignalRequest();
                if (object.key != null)
                    if ($Number(object.key) !== 0)
                        message.key = object.key >>> 0;
                if (object.value != null)
                    if ($Number(object.value) !== 0)
                        message.value = object.value | 0;
                return message;
            };

            /**
             * Creates a plain object from a SetSignalRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.signal.SetSignalRequest
             * @static
             * @param {lebai.signal.SetSignalRequest} message SetSignalRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetSignalRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.key = 0;
                    object.value = 0;
                }
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    object.key = message.key;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this SetSignalRequest to JSON.
             * @function toJSON
             * @memberof lebai.signal.SetSignalRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetSignalRequest.prototype.toJSON = function() {
                return SetSignalRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetSignalRequest
             * @function getTypeUrl
             * @memberof lebai.signal.SetSignalRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetSignalRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.signal.SetSignalRequest";
            };

            return SetSignalRequest;
        })();

        signal.SetSignalsRequest = (function() {

            /**
             * Properties of a SetSignalsRequest.
             * @typedef {Object} lebai.signal.SetSignalsRequest.$Properties
             * @property {number|null} [key] SetSignalsRequest key
             * @property {Array.<number>|null} [values] SetSignalsRequest values
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SetSignalsRequest.
             * @memberof lebai.signal
             * @interface ISetSignalsRequest
             * @augments lebai.signal.SetSignalsRequest.$Properties
             * @deprecated Use lebai.signal.SetSignalsRequest.$Properties instead.
             */

            /**
             * Shape of a SetSignalsRequest.
             * @typedef {lebai.signal.SetSignalsRequest.$Properties} lebai.signal.SetSignalsRequest.$Shape
             */

            /**
             * Constructs a new SetSignalsRequest.
             * @memberof lebai.signal
             * @classdesc Represents a SetSignalsRequest.
             * @constructor
             * @param {lebai.signal.SetSignalsRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SetSignalsRequest = function (properties) {
                this.values = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SetSignalsRequest key.
             * @member {number} key
             * @memberof lebai.signal.SetSignalsRequest
             * @instance
             */
            SetSignalsRequest.prototype.key = 0;

            /**
             * SetSignalsRequest values.
             * @member {Array.<number>} values
             * @memberof lebai.signal.SetSignalsRequest
             * @instance
             */
            SetSignalsRequest.prototype.values = $util.emptyArray;

            /**
             * Creates a new SetSignalsRequest instance using the specified properties.
             * @function create
             * @memberof lebai.signal.SetSignalsRequest
             * @static
             * @param {lebai.signal.SetSignalsRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.signal.SetSignalsRequest} SetSignalsRequest instance
             * @type {{
             *   (properties: lebai.signal.SetSignalsRequest.$Shape): lebai.signal.SetSignalsRequest & lebai.signal.SetSignalsRequest.$Shape;
             *   (properties?: lebai.signal.SetSignalsRequest.$Properties): lebai.signal.SetSignalsRequest;
             * }}
             */
            SetSignalsRequest.create = function(properties) {
                return new SetSignalsRequest(properties);
            };

            /**
             * Encodes the specified SetSignalsRequest message. Does not implicitly {@link lebai.signal.SetSignalsRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.signal.SetSignalsRequest
             * @static
             * @param {lebai.signal.SetSignalsRequest.$Properties} message SetSignalsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetSignalsRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.key);
                if (message.values != null && message.values.length)
                    writer.uint32(/* id 2, wireType 2 =*/18).int32s(message.values);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SetSignalsRequest message, length delimited. Does not implicitly {@link lebai.signal.SetSignalsRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.signal.SetSignalsRequest
             * @static
             * @param {lebai.signal.SetSignalsRequest.$Properties} message SetSignalsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SetSignalsRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SetSignalsRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.signal.SetSignalsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.signal.SetSignalsRequest & lebai.signal.SetSignalsRequest.$Shape} SetSignalsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetSignalsRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.signal.SetSignalsRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.key = value;
                            else
                                delete message.key;
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.values && message.values.length))
                                    message.values = [];
                                reader.int32s(message.values);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push(reader.int32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SetSignalsRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.signal.SetSignalsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.signal.SetSignalsRequest & lebai.signal.SetSignalsRequest.$Shape} SetSignalsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SetSignalsRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SetSignalsRequest message.
             * @function verify
             * @memberof lebai.signal.SetSignalsRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SetSignalsRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    if (!$util.isInteger(message.key))
                        return "key: integer expected";
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (var i = 0; i < message.values.length; ++i)
                        if (!$util.isInteger(message.values[i]))
                            return "values: integer[] expected";
                }
                return null;
            };

            /**
             * Creates a SetSignalsRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.signal.SetSignalsRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.signal.SetSignalsRequest} SetSignalsRequest
             */
            SetSignalsRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.signal.SetSignalsRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.signal.SetSignalsRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.signal.SetSignalsRequest();
                if (object.key != null)
                    if ($Number(object.key) !== 0)
                        message.key = object.key >>> 0;
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".lebai.signal.SetSignalsRequest.values: array expected");
                    message.values = $Array(object.values.length);
                    for (var i = 0; i < object.values.length; ++i)
                        message.values[i] = object.values[i] | 0;
                }
                return message;
            };

            /**
             * Creates a plain object from a SetSignalsRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.signal.SetSignalsRequest
             * @static
             * @param {lebai.signal.SetSignalsRequest} message SetSignalsRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SetSignalsRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (options.defaults)
                    object.key = 0;
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    object.key = message.key;
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (var j = 0; j < message.values.length; ++j)
                        object.values[j] = message.values[j];
                }
                return object;
            };

            /**
             * Converts this SetSignalsRequest to JSON.
             * @function toJSON
             * @memberof lebai.signal.SetSignalsRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SetSignalsRequest.prototype.toJSON = function() {
                return SetSignalsRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SetSignalsRequest
             * @function getTypeUrl
             * @memberof lebai.signal.SetSignalsRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SetSignalsRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.signal.SetSignalsRequest";
            };

            return SetSignalsRequest;
        })();

        signal.WaitSignalRequest = (function() {

            /**
             * Properties of a WaitSignalRequest.
             * @typedef {Object} lebai.signal.WaitSignalRequest.$Properties
             * @property {number|null} [key] WaitSignalRequest key
             * @property {number|null} [value] WaitSignalRequest value
             * @property {lebai.cmp.Relation|null} [relation] WaitSignalRequest relation
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a WaitSignalRequest.
             * @memberof lebai.signal
             * @interface IWaitSignalRequest
             * @augments lebai.signal.WaitSignalRequest.$Properties
             * @deprecated Use lebai.signal.WaitSignalRequest.$Properties instead.
             */

            /**
             * Shape of a WaitSignalRequest.
             * @typedef {lebai.signal.WaitSignalRequest.$Properties} lebai.signal.WaitSignalRequest.$Shape
             */

            /**
             * Constructs a new WaitSignalRequest.
             * @memberof lebai.signal
             * @classdesc Represents a WaitSignalRequest.
             * @constructor
             * @param {lebai.signal.WaitSignalRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var WaitSignalRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * WaitSignalRequest key.
             * @member {number} key
             * @memberof lebai.signal.WaitSignalRequest
             * @instance
             */
            WaitSignalRequest.prototype.key = 0;

            /**
             * WaitSignalRequest value.
             * @member {number} value
             * @memberof lebai.signal.WaitSignalRequest
             * @instance
             */
            WaitSignalRequest.prototype.value = 0;

            /**
             * WaitSignalRequest relation.
             * @member {lebai.cmp.Relation} relation
             * @memberof lebai.signal.WaitSignalRequest
             * @instance
             */
            WaitSignalRequest.prototype.relation = 0;

            /**
             * Creates a new WaitSignalRequest instance using the specified properties.
             * @function create
             * @memberof lebai.signal.WaitSignalRequest
             * @static
             * @param {lebai.signal.WaitSignalRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.signal.WaitSignalRequest} WaitSignalRequest instance
             * @type {{
             *   (properties: lebai.signal.WaitSignalRequest.$Shape): lebai.signal.WaitSignalRequest & lebai.signal.WaitSignalRequest.$Shape;
             *   (properties?: lebai.signal.WaitSignalRequest.$Properties): lebai.signal.WaitSignalRequest;
             * }}
             */
            WaitSignalRequest.create = function(properties) {
                return new WaitSignalRequest(properties);
            };

            /**
             * Encodes the specified WaitSignalRequest message. Does not implicitly {@link lebai.signal.WaitSignalRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.signal.WaitSignalRequest
             * @static
             * @param {lebai.signal.WaitSignalRequest.$Properties} message WaitSignalRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WaitSignalRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.key);
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.value);
                if (message.relation != null && $Object.hasOwnProperty.call(message, "relation") && message.relation !== 0)
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.relation);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified WaitSignalRequest message, length delimited. Does not implicitly {@link lebai.signal.WaitSignalRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.signal.WaitSignalRequest
             * @static
             * @param {lebai.signal.WaitSignalRequest.$Properties} message WaitSignalRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WaitSignalRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a WaitSignalRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.signal.WaitSignalRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.signal.WaitSignalRequest & lebai.signal.WaitSignalRequest.$Shape} WaitSignalRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WaitSignalRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.signal.WaitSignalRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.key = value;
                            else
                                delete message.key;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.relation = value;
                            else
                                delete message.relation;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a WaitSignalRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.signal.WaitSignalRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.signal.WaitSignalRequest & lebai.signal.WaitSignalRequest.$Shape} WaitSignalRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WaitSignalRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a WaitSignalRequest message.
             * @function verify
             * @memberof lebai.signal.WaitSignalRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            WaitSignalRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    if (!$util.isInteger(message.key))
                        return "key: integer expected";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                if (message.relation != null && $Object.hasOwnProperty.call(message, "relation"))
                    if (typeof message.relation !== "number" || (message.relation | 0) !== message.relation)
                        return "relation: enum value expected";
                return null;
            };

            /**
             * Creates a WaitSignalRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.signal.WaitSignalRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.signal.WaitSignalRequest} WaitSignalRequest
             */
            WaitSignalRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.signal.WaitSignalRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.signal.WaitSignalRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.signal.WaitSignalRequest();
                if (object.key != null)
                    if ($Number(object.key) !== 0)
                        message.key = object.key >>> 0;
                if (object.value != null)
                    if ($Number(object.value) !== 0)
                        message.value = object.value | 0;
                if (object.relation !== 0 && (typeof object.relation !== "string" || $root.lebai.cmp.Relation[object.relation] !== 0))
                    switch (object.relation) {
                    case "EQ":
                    case 0:
                        message.relation = 0;
                        break;
                    case "NEQ":
                    case 2:
                        message.relation = 2;
                        break;
                    case "LT":
                    case 3:
                        message.relation = 3;
                        break;
                    case "LTE":
                    case 4:
                        message.relation = 4;
                        break;
                    case "GT":
                    case 5:
                        message.relation = 5;
                        break;
                    case "GTE":
                    case 6:
                        message.relation = 6;
                        break;
                    default:
                        if (typeof object.relation === "number" && (object.relation | 0) === object.relation)
                            message.relation = object.relation;
                    }
                return message;
            };

            /**
             * Creates a plain object from a WaitSignalRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.signal.WaitSignalRequest
             * @static
             * @param {lebai.signal.WaitSignalRequest} message WaitSignalRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            WaitSignalRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.key = 0;
                    object.value = 0;
                    object.relation = options.enums === $String ? "EQ" : 0;
                }
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    object.key = message.key;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                if (message.relation != null && $Object.hasOwnProperty.call(message, "relation"))
                    object.relation = options.enums === $String ? $root.lebai.cmp.Relation[message.relation] === $undefined ? message.relation : $root.lebai.cmp.Relation[message.relation] : message.relation;
                return object;
            };

            /**
             * Converts this WaitSignalRequest to JSON.
             * @function toJSON
             * @memberof lebai.signal.WaitSignalRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            WaitSignalRequest.prototype.toJSON = function() {
                return WaitSignalRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for WaitSignalRequest
             * @function getTypeUrl
             * @memberof lebai.signal.WaitSignalRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            WaitSignalRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.signal.WaitSignalRequest";
            };

            return WaitSignalRequest;
        })();

        signal.GetSignalRequest = (function() {

            /**
             * Properties of a GetSignalRequest.
             * @typedef {Object} lebai.signal.GetSignalRequest.$Properties
             * @property {number|null} [key] GetSignalRequest key
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetSignalRequest.
             * @memberof lebai.signal
             * @interface IGetSignalRequest
             * @augments lebai.signal.GetSignalRequest.$Properties
             * @deprecated Use lebai.signal.GetSignalRequest.$Properties instead.
             */

            /**
             * Shape of a GetSignalRequest.
             * @typedef {lebai.signal.GetSignalRequest.$Properties} lebai.signal.GetSignalRequest.$Shape
             */

            /**
             * Constructs a new GetSignalRequest.
             * @memberof lebai.signal
             * @classdesc Represents a GetSignalRequest.
             * @constructor
             * @param {lebai.signal.GetSignalRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetSignalRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetSignalRequest key.
             * @member {number} key
             * @memberof lebai.signal.GetSignalRequest
             * @instance
             */
            GetSignalRequest.prototype.key = 0;

            /**
             * Creates a new GetSignalRequest instance using the specified properties.
             * @function create
             * @memberof lebai.signal.GetSignalRequest
             * @static
             * @param {lebai.signal.GetSignalRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.signal.GetSignalRequest} GetSignalRequest instance
             * @type {{
             *   (properties: lebai.signal.GetSignalRequest.$Shape): lebai.signal.GetSignalRequest & lebai.signal.GetSignalRequest.$Shape;
             *   (properties?: lebai.signal.GetSignalRequest.$Properties): lebai.signal.GetSignalRequest;
             * }}
             */
            GetSignalRequest.create = function(properties) {
                return new GetSignalRequest(properties);
            };

            /**
             * Encodes the specified GetSignalRequest message. Does not implicitly {@link lebai.signal.GetSignalRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.signal.GetSignalRequest
             * @static
             * @param {lebai.signal.GetSignalRequest.$Properties} message GetSignalRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetSignalRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.key);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetSignalRequest message, length delimited. Does not implicitly {@link lebai.signal.GetSignalRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.signal.GetSignalRequest
             * @static
             * @param {lebai.signal.GetSignalRequest.$Properties} message GetSignalRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetSignalRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetSignalRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.signal.GetSignalRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.signal.GetSignalRequest & lebai.signal.GetSignalRequest.$Shape} GetSignalRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetSignalRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.signal.GetSignalRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.key = value;
                            else
                                delete message.key;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetSignalRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.signal.GetSignalRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.signal.GetSignalRequest & lebai.signal.GetSignalRequest.$Shape} GetSignalRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetSignalRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetSignalRequest message.
             * @function verify
             * @memberof lebai.signal.GetSignalRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetSignalRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    if (!$util.isInteger(message.key))
                        return "key: integer expected";
                return null;
            };

            /**
             * Creates a GetSignalRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.signal.GetSignalRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.signal.GetSignalRequest} GetSignalRequest
             */
            GetSignalRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.signal.GetSignalRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.signal.GetSignalRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.signal.GetSignalRequest();
                if (object.key != null)
                    if ($Number(object.key) !== 0)
                        message.key = object.key >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a GetSignalRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.signal.GetSignalRequest
             * @static
             * @param {lebai.signal.GetSignalRequest} message GetSignalRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetSignalRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.key = 0;
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    object.key = message.key;
                return object;
            };

            /**
             * Converts this GetSignalRequest to JSON.
             * @function toJSON
             * @memberof lebai.signal.GetSignalRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetSignalRequest.prototype.toJSON = function() {
                return GetSignalRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetSignalRequest
             * @function getTypeUrl
             * @memberof lebai.signal.GetSignalRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetSignalRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.signal.GetSignalRequest";
            };

            return GetSignalRequest;
        })();

        signal.GetSignalResponse = (function() {

            /**
             * Properties of a GetSignalResponse.
             * @typedef {Object} lebai.signal.GetSignalResponse.$Properties
             * @property {number|null} [value] GetSignalResponse value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetSignalResponse.
             * @memberof lebai.signal
             * @interface IGetSignalResponse
             * @augments lebai.signal.GetSignalResponse.$Properties
             * @deprecated Use lebai.signal.GetSignalResponse.$Properties instead.
             */

            /**
             * Shape of a GetSignalResponse.
             * @typedef {lebai.signal.GetSignalResponse.$Properties} lebai.signal.GetSignalResponse.$Shape
             */

            /**
             * Constructs a new GetSignalResponse.
             * @memberof lebai.signal
             * @classdesc Represents a GetSignalResponse.
             * @constructor
             * @param {lebai.signal.GetSignalResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetSignalResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetSignalResponse value.
             * @member {number} value
             * @memberof lebai.signal.GetSignalResponse
             * @instance
             */
            GetSignalResponse.prototype.value = 0;

            /**
             * Creates a new GetSignalResponse instance using the specified properties.
             * @function create
             * @memberof lebai.signal.GetSignalResponse
             * @static
             * @param {lebai.signal.GetSignalResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.signal.GetSignalResponse} GetSignalResponse instance
             * @type {{
             *   (properties: lebai.signal.GetSignalResponse.$Shape): lebai.signal.GetSignalResponse & lebai.signal.GetSignalResponse.$Shape;
             *   (properties?: lebai.signal.GetSignalResponse.$Properties): lebai.signal.GetSignalResponse;
             * }}
             */
            GetSignalResponse.create = function(properties) {
                return new GetSignalResponse(properties);
            };

            /**
             * Encodes the specified GetSignalResponse message. Does not implicitly {@link lebai.signal.GetSignalResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.signal.GetSignalResponse
             * @static
             * @param {lebai.signal.GetSignalResponse.$Properties} message GetSignalResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetSignalResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetSignalResponse message, length delimited. Does not implicitly {@link lebai.signal.GetSignalResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.signal.GetSignalResponse
             * @static
             * @param {lebai.signal.GetSignalResponse.$Properties} message GetSignalResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetSignalResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetSignalResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.signal.GetSignalResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.signal.GetSignalResponse & lebai.signal.GetSignalResponse.$Shape} GetSignalResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetSignalResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.signal.GetSignalResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetSignalResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.signal.GetSignalResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.signal.GetSignalResponse & lebai.signal.GetSignalResponse.$Shape} GetSignalResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetSignalResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetSignalResponse message.
             * @function verify
             * @memberof lebai.signal.GetSignalResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetSignalResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                return null;
            };

            /**
             * Creates a GetSignalResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.signal.GetSignalResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.signal.GetSignalResponse} GetSignalResponse
             */
            GetSignalResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.signal.GetSignalResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.signal.GetSignalResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.signal.GetSignalResponse();
                if (object.value != null)
                    if ($Number(object.value) !== 0)
                        message.value = object.value | 0;
                return message;
            };

            /**
             * Creates a plain object from a GetSignalResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.signal.GetSignalResponse
             * @static
             * @param {lebai.signal.GetSignalResponse} message GetSignalResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetSignalResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.value = 0;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this GetSignalResponse to JSON.
             * @function toJSON
             * @memberof lebai.signal.GetSignalResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetSignalResponse.prototype.toJSON = function() {
                return GetSignalResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetSignalResponse
             * @function getTypeUrl
             * @memberof lebai.signal.GetSignalResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetSignalResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.signal.GetSignalResponse";
            };

            return GetSignalResponse;
        })();

        signal.GetSignalsRequest = (function() {

            /**
             * Properties of a GetSignalsRequest.
             * @typedef {Object} lebai.signal.GetSignalsRequest.$Properties
             * @property {number|null} [key] GetSignalsRequest key
             * @property {number|null} [len] GetSignalsRequest len
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetSignalsRequest.
             * @memberof lebai.signal
             * @interface IGetSignalsRequest
             * @augments lebai.signal.GetSignalsRequest.$Properties
             * @deprecated Use lebai.signal.GetSignalsRequest.$Properties instead.
             */

            /**
             * Shape of a GetSignalsRequest.
             * @typedef {lebai.signal.GetSignalsRequest.$Properties} lebai.signal.GetSignalsRequest.$Shape
             */

            /**
             * Constructs a new GetSignalsRequest.
             * @memberof lebai.signal
             * @classdesc Represents a GetSignalsRequest.
             * @constructor
             * @param {lebai.signal.GetSignalsRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetSignalsRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetSignalsRequest key.
             * @member {number} key
             * @memberof lebai.signal.GetSignalsRequest
             * @instance
             */
            GetSignalsRequest.prototype.key = 0;

            /**
             * GetSignalsRequest len.
             * @member {number} len
             * @memberof lebai.signal.GetSignalsRequest
             * @instance
             */
            GetSignalsRequest.prototype.len = 0;

            /**
             * Creates a new GetSignalsRequest instance using the specified properties.
             * @function create
             * @memberof lebai.signal.GetSignalsRequest
             * @static
             * @param {lebai.signal.GetSignalsRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.signal.GetSignalsRequest} GetSignalsRequest instance
             * @type {{
             *   (properties: lebai.signal.GetSignalsRequest.$Shape): lebai.signal.GetSignalsRequest & lebai.signal.GetSignalsRequest.$Shape;
             *   (properties?: lebai.signal.GetSignalsRequest.$Properties): lebai.signal.GetSignalsRequest;
             * }}
             */
            GetSignalsRequest.create = function(properties) {
                return new GetSignalsRequest(properties);
            };

            /**
             * Encodes the specified GetSignalsRequest message. Does not implicitly {@link lebai.signal.GetSignalsRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.signal.GetSignalsRequest
             * @static
             * @param {lebai.signal.GetSignalsRequest.$Properties} message GetSignalsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetSignalsRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.key);
                if (message.len != null && $Object.hasOwnProperty.call(message, "len") && message.len !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.len);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetSignalsRequest message, length delimited. Does not implicitly {@link lebai.signal.GetSignalsRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.signal.GetSignalsRequest
             * @static
             * @param {lebai.signal.GetSignalsRequest.$Properties} message GetSignalsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetSignalsRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetSignalsRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.signal.GetSignalsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.signal.GetSignalsRequest & lebai.signal.GetSignalsRequest.$Shape} GetSignalsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetSignalsRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.signal.GetSignalsRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.key = value;
                            else
                                delete message.key;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.len = value;
                            else
                                delete message.len;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetSignalsRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.signal.GetSignalsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.signal.GetSignalsRequest & lebai.signal.GetSignalsRequest.$Shape} GetSignalsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetSignalsRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetSignalsRequest message.
             * @function verify
             * @memberof lebai.signal.GetSignalsRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetSignalsRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    if (!$util.isInteger(message.key))
                        return "key: integer expected";
                if (message.len != null && $Object.hasOwnProperty.call(message, "len"))
                    if (!$util.isInteger(message.len))
                        return "len: integer expected";
                return null;
            };

            /**
             * Creates a GetSignalsRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.signal.GetSignalsRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.signal.GetSignalsRequest} GetSignalsRequest
             */
            GetSignalsRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.signal.GetSignalsRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.signal.GetSignalsRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.signal.GetSignalsRequest();
                if (object.key != null)
                    if ($Number(object.key) !== 0)
                        message.key = object.key >>> 0;
                if (object.len != null)
                    if ($Number(object.len) !== 0)
                        message.len = object.len >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a GetSignalsRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.signal.GetSignalsRequest
             * @static
             * @param {lebai.signal.GetSignalsRequest} message GetSignalsRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetSignalsRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.key = 0;
                    object.len = 0;
                }
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    object.key = message.key;
                if (message.len != null && $Object.hasOwnProperty.call(message, "len"))
                    object.len = message.len;
                return object;
            };

            /**
             * Converts this GetSignalsRequest to JSON.
             * @function toJSON
             * @memberof lebai.signal.GetSignalsRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetSignalsRequest.prototype.toJSON = function() {
                return GetSignalsRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetSignalsRequest
             * @function getTypeUrl
             * @memberof lebai.signal.GetSignalsRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetSignalsRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.signal.GetSignalsRequest";
            };

            return GetSignalsRequest;
        })();

        signal.GetSignalsResponse = (function() {

            /**
             * Properties of a GetSignalsResponse.
             * @typedef {Object} lebai.signal.GetSignalsResponse.$Properties
             * @property {Array.<number>|null} [values] GetSignalsResponse values
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetSignalsResponse.
             * @memberof lebai.signal
             * @interface IGetSignalsResponse
             * @augments lebai.signal.GetSignalsResponse.$Properties
             * @deprecated Use lebai.signal.GetSignalsResponse.$Properties instead.
             */

            /**
             * Shape of a GetSignalsResponse.
             * @typedef {lebai.signal.GetSignalsResponse.$Properties} lebai.signal.GetSignalsResponse.$Shape
             */

            /**
             * Constructs a new GetSignalsResponse.
             * @memberof lebai.signal
             * @classdesc Represents a GetSignalsResponse.
             * @constructor
             * @param {lebai.signal.GetSignalsResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetSignalsResponse = function (properties) {
                this.values = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetSignalsResponse values.
             * @member {Array.<number>} values
             * @memberof lebai.signal.GetSignalsResponse
             * @instance
             */
            GetSignalsResponse.prototype.values = $util.emptyArray;

            /**
             * Creates a new GetSignalsResponse instance using the specified properties.
             * @function create
             * @memberof lebai.signal.GetSignalsResponse
             * @static
             * @param {lebai.signal.GetSignalsResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.signal.GetSignalsResponse} GetSignalsResponse instance
             * @type {{
             *   (properties: lebai.signal.GetSignalsResponse.$Shape): lebai.signal.GetSignalsResponse & lebai.signal.GetSignalsResponse.$Shape;
             *   (properties?: lebai.signal.GetSignalsResponse.$Properties): lebai.signal.GetSignalsResponse;
             * }}
             */
            GetSignalsResponse.create = function(properties) {
                return new GetSignalsResponse(properties);
            };

            /**
             * Encodes the specified GetSignalsResponse message. Does not implicitly {@link lebai.signal.GetSignalsResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.signal.GetSignalsResponse
             * @static
             * @param {lebai.signal.GetSignalsResponse.$Properties} message GetSignalsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetSignalsResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.values != null && message.values.length)
                    writer.uint32(/* id 1, wireType 2 =*/10).int32s(message.values);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetSignalsResponse message, length delimited. Does not implicitly {@link lebai.signal.GetSignalsResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.signal.GetSignalsResponse
             * @static
             * @param {lebai.signal.GetSignalsResponse.$Properties} message GetSignalsResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetSignalsResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetSignalsResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.signal.GetSignalsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.signal.GetSignalsResponse & lebai.signal.GetSignalsResponse.$Shape} GetSignalsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetSignalsResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.signal.GetSignalsResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.values && message.values.length))
                                    message.values = [];
                                reader.int32s(message.values);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push(reader.int32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetSignalsResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.signal.GetSignalsResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.signal.GetSignalsResponse & lebai.signal.GetSignalsResponse.$Shape} GetSignalsResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetSignalsResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetSignalsResponse message.
             * @function verify
             * @memberof lebai.signal.GetSignalsResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetSignalsResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.values != null && $Object.hasOwnProperty.call(message, "values")) {
                    if (!$Array.isArray(message.values))
                        return "values: array expected";
                    for (var i = 0; i < message.values.length; ++i)
                        if (!$util.isInteger(message.values[i]))
                            return "values: integer[] expected";
                }
                return null;
            };

            /**
             * Creates a GetSignalsResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.signal.GetSignalsResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.signal.GetSignalsResponse} GetSignalsResponse
             */
            GetSignalsResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.signal.GetSignalsResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.signal.GetSignalsResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.signal.GetSignalsResponse();
                if (object.values) {
                    if (!$Array.isArray(object.values))
                        throw $TypeError(".lebai.signal.GetSignalsResponse.values: array expected");
                    message.values = $Array(object.values.length);
                    for (var i = 0; i < object.values.length; ++i)
                        message.values[i] = object.values[i] | 0;
                }
                return message;
            };

            /**
             * Creates a plain object from a GetSignalsResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.signal.GetSignalsResponse
             * @static
             * @param {lebai.signal.GetSignalsResponse} message GetSignalsResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetSignalsResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (message.values && message.values.length) {
                    object.values = $Array(message.values.length);
                    for (var j = 0; j < message.values.length; ++j)
                        object.values[j] = message.values[j];
                }
                return object;
            };

            /**
             * Converts this GetSignalsResponse to JSON.
             * @function toJSON
             * @memberof lebai.signal.GetSignalsResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetSignalsResponse.prototype.toJSON = function() {
                return GetSignalsResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetSignalsResponse
             * @function getTypeUrl
             * @memberof lebai.signal.GetSignalsResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetSignalsResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.signal.GetSignalsResponse";
            };

            return GetSignalsResponse;
        })();

        signal.SignalService = (function() {

            /**
             * Constructs a new SignalService service.
             * @memberof lebai.signal
             * @classdesc Represents a SignalService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var SignalService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(SignalService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: SignalService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new SignalService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.signal.SignalService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {SignalService} RPC service. Useful where requests and/or responses are streamed.
             */
            SignalService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.signal.SignalService#setSignal}.
             * @memberof lebai.signal.SignalService
             * @typedef SetSignalCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetSignal.
             * @memberof lebai.signal.SignalService
             * @typedef SetSignal
             * @type {{
             *   (request: lebai.signal.ISetSignalRequest, callback: lebai.signal.SignalService.SetSignalCallback): void;
             *   (request: lebai.signal.ISetSignalRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetSignal";
             *   readonly path: "/lebai.signal.SignalService/SetSignal";
             *   readonly requestType: "SetSignalRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetSignal.
             * @name lebai.signal.SignalService#setSignal
             * @type {lebai.signal.SignalService.SetSignal}
             */
            $Object.defineProperties(SignalService.prototype.setSignal = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SignalService.prototype.setSignal, $root.lebai.signal.SetSignalRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetSignal" },
                path: { value: "/lebai.signal.SignalService/SetSignal" },
                requestType: { value: "SetSignalRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.signal.SignalService#setSignals}.
             * @memberof lebai.signal.SignalService
             * @typedef SetSignalsCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetSignals.
             * @memberof lebai.signal.SignalService
             * @typedef SetSignals
             * @type {{
             *   (request: lebai.signal.ISetSignalsRequest, callback: lebai.signal.SignalService.SetSignalsCallback): void;
             *   (request: lebai.signal.ISetSignalsRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SetSignals";
             *   readonly path: "/lebai.signal.SignalService/SetSignals";
             *   readonly requestType: "SetSignalsRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetSignals.
             * @name lebai.signal.SignalService#setSignals
             * @type {lebai.signal.SignalService.SetSignals}
             */
            $Object.defineProperties(SignalService.prototype.setSignals = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SignalService.prototype.setSignals, $root.lebai.signal.SetSignalsRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetSignals" },
                path: { value: "/lebai.signal.SignalService/SetSignals" },
                requestType: { value: "SetSignalsRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.signal.SignalService#getSignal}.
             * @memberof lebai.signal.SignalService
             * @typedef GetSignalCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.signal.GetSignalResponse} [response] GetSignalResponse
             */

            /**
             * Calls GetSignal.
             * @memberof lebai.signal.SignalService
             * @typedef GetSignal
             * @type {{
             *   (request: lebai.signal.IGetSignalRequest, callback: lebai.signal.SignalService.GetSignalCallback): void;
             *   (request: lebai.signal.IGetSignalRequest): Promise<lebai.signal.GetSignalResponse>;
             *   readonly name: "GetSignal";
             *   readonly path: "/lebai.signal.SignalService/GetSignal";
             *   readonly requestType: "GetSignalRequest";
             *   readonly responseType: "GetSignalResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetSignal.
             * @name lebai.signal.SignalService#getSignal
             * @type {lebai.signal.SignalService.GetSignal}
             */
            $Object.defineProperties(SignalService.prototype.getSignal = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SignalService.prototype.getSignal, $root.lebai.signal.GetSignalRequest, $root.lebai.signal.GetSignalResponse, request, callback);
            }, {
                name: { value: "GetSignal" },
                path: { value: "/lebai.signal.SignalService/GetSignal" },
                requestType: { value: "GetSignalRequest" },
                responseType: { value: "GetSignalResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.signal.SignalService#getSignals}.
             * @memberof lebai.signal.SignalService
             * @typedef GetSignalsCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.signal.GetSignalsResponse} [response] GetSignalsResponse
             */

            /**
             * Calls GetSignals.
             * @memberof lebai.signal.SignalService
             * @typedef GetSignals
             * @type {{
             *   (request: lebai.signal.IGetSignalsRequest, callback: lebai.signal.SignalService.GetSignalsCallback): void;
             *   (request: lebai.signal.IGetSignalsRequest): Promise<lebai.signal.GetSignalsResponse>;
             *   readonly name: "GetSignals";
             *   readonly path: "/lebai.signal.SignalService/GetSignals";
             *   readonly requestType: "GetSignalsRequest";
             *   readonly responseType: "GetSignalsResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetSignals.
             * @name lebai.signal.SignalService#getSignals
             * @type {lebai.signal.SignalService.GetSignals}
             */
            $Object.defineProperties(SignalService.prototype.getSignals = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SignalService.prototype.getSignals, $root.lebai.signal.GetSignalsRequest, $root.lebai.signal.GetSignalsResponse, request, callback);
            }, {
                name: { value: "GetSignals" },
                path: { value: "/lebai.signal.SignalService/GetSignals" },
                requestType: { value: "GetSignalsRequest" },
                responseType: { value: "GetSignalsResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.signal.SignalService#waitSignal}.
             * @memberof lebai.signal.SignalService
             * @typedef WaitSignalCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls WaitSignal.
             * @memberof lebai.signal.SignalService
             * @typedef WaitSignal
             * @type {{
             *   (request: lebai.signal.IWaitSignalRequest, callback: lebai.signal.SignalService.WaitSignalCallback): void;
             *   (request: lebai.signal.IWaitSignalRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "WaitSignal";
             *   readonly path: "/lebai.signal.SignalService/WaitSignal";
             *   readonly requestType: "WaitSignalRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls WaitSignal.
             * @name lebai.signal.SignalService#waitSignal
             * @type {lebai.signal.SignalService.WaitSignal}
             */
            $Object.defineProperties(SignalService.prototype.waitSignal = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SignalService.prototype.waitSignal, $root.lebai.signal.WaitSignalRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "WaitSignal" },
                path: { value: "/lebai.signal.SignalService/WaitSignal" },
                requestType: { value: "WaitSignalRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.signal.SignalService#addSignal}.
             * @memberof lebai.signal.SignalService
             * @typedef AddSignalCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls AddSignal.
             * @memberof lebai.signal.SignalService
             * @typedef AddSignal
             * @type {{
             *   (request: lebai.signal.ISetSignalRequest, callback: lebai.signal.SignalService.AddSignalCallback): void;
             *   (request: lebai.signal.ISetSignalRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "AddSignal";
             *   readonly path: "/lebai.signal.SignalService/AddSignal";
             *   readonly requestType: "SetSignalRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls AddSignal.
             * @name lebai.signal.SignalService#addSignal
             * @type {lebai.signal.SignalService.AddSignal}
             */
            $Object.defineProperties(SignalService.prototype.addSignal = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, SignalService.prototype.addSignal, $root.lebai.signal.SetSignalRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "AddSignal" },
                path: { value: "/lebai.signal.SignalService/AddSignal" },
                requestType: { value: "SetSignalRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return SignalService;
        })();

        return signal;
    })();

    lebai.storage = (function() {

        /**
         * Namespace storage.
         * @memberof lebai
         * @namespace
         */
        var storage = {};

        storage.ItemIndex = (function() {

            /**
             * Properties of an ItemIndex.
             * @typedef {Object} lebai.storage.ItemIndex.$Properties
             * @property {string|null} [key] ItemIndex key
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an ItemIndex.
             * @memberof lebai.storage
             * @interface IItemIndex
             * @augments lebai.storage.ItemIndex.$Properties
             * @deprecated Use lebai.storage.ItemIndex.$Properties instead.
             */

            /**
             * Shape of an ItemIndex.
             * @typedef {lebai.storage.ItemIndex.$Properties} lebai.storage.ItemIndex.$Shape
             */

            /**
             * Constructs a new ItemIndex.
             * @memberof lebai.storage
             * @classdesc Represents an ItemIndex.
             * @constructor
             * @param {lebai.storage.ItemIndex.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var ItemIndex = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * ItemIndex key.
             * @member {string} key
             * @memberof lebai.storage.ItemIndex
             * @instance
             */
            ItemIndex.prototype.key = "";

            /**
             * Creates a new ItemIndex instance using the specified properties.
             * @function create
             * @memberof lebai.storage.ItemIndex
             * @static
             * @param {lebai.storage.ItemIndex.$Properties=} [properties] Properties to set
             * @returns {lebai.storage.ItemIndex} ItemIndex instance
             * @type {{
             *   (properties: lebai.storage.ItemIndex.$Shape): lebai.storage.ItemIndex & lebai.storage.ItemIndex.$Shape;
             *   (properties?: lebai.storage.ItemIndex.$Properties): lebai.storage.ItemIndex;
             * }}
             */
            ItemIndex.create = function(properties) {
                return new ItemIndex(properties);
            };

            /**
             * Encodes the specified ItemIndex message. Does not implicitly {@link lebai.storage.ItemIndex.verify|verify} messages.
             * @function encode
             * @memberof lebai.storage.ItemIndex
             * @static
             * @param {lebai.storage.ItemIndex.$Properties} message ItemIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ItemIndex.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ItemIndex message, length delimited. Does not implicitly {@link lebai.storage.ItemIndex.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.storage.ItemIndex
             * @static
             * @param {lebai.storage.ItemIndex.$Properties} message ItemIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ItemIndex.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an ItemIndex message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.storage.ItemIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.storage.ItemIndex & lebai.storage.ItemIndex.$Shape} ItemIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ItemIndex.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.storage.ItemIndex();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.key = value;
                            else
                                delete message.key;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an ItemIndex message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.storage.ItemIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.storage.ItemIndex & lebai.storage.ItemIndex.$Shape} ItemIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ItemIndex.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an ItemIndex message.
             * @function verify
             * @memberof lebai.storage.ItemIndex
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ItemIndex.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    if (!$util.isString(message.key))
                        return "key: string expected";
                return null;
            };

            /**
             * Creates an ItemIndex message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.storage.ItemIndex
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.storage.ItemIndex} ItemIndex
             */
            ItemIndex.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.storage.ItemIndex)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.storage.ItemIndex: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.storage.ItemIndex();
                if (object.key != null)
                    if (typeof object.key !== "string" || object.key.length)
                        message.key = $String(object.key);
                return message;
            };

            /**
             * Creates a plain object from an ItemIndex message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.storage.ItemIndex
             * @static
             * @param {lebai.storage.ItemIndex} message ItemIndex
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ItemIndex.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.key = "";
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    object.key = message.key;
                return object;
            };

            /**
             * Converts this ItemIndex to JSON.
             * @function toJSON
             * @memberof lebai.storage.ItemIndex
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ItemIndex.prototype.toJSON = function() {
                return ItemIndex.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for ItemIndex
             * @function getTypeUrl
             * @memberof lebai.storage.ItemIndex
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            ItemIndex.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.storage.ItemIndex";
            };

            return ItemIndex;
        })();

        storage.Item = (function() {

            /**
             * Properties of an Item.
             * @typedef {Object} lebai.storage.Item.$Properties
             * @property {string|null} [key] Item key
             * @property {string|null} [value] Item value
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an Item.
             * @memberof lebai.storage
             * @interface IItem
             * @augments lebai.storage.Item.$Properties
             * @deprecated Use lebai.storage.Item.$Properties instead.
             */

            /**
             * Shape of an Item.
             * @typedef {lebai.storage.Item.$Properties} lebai.storage.Item.$Shape
             */

            /**
             * Constructs a new Item.
             * @memberof lebai.storage
             * @classdesc Represents an Item.
             * @constructor
             * @param {lebai.storage.Item.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Item = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Item key.
             * @member {string} key
             * @memberof lebai.storage.Item
             * @instance
             */
            Item.prototype.key = "";

            /**
             * Item value.
             * @member {string} value
             * @memberof lebai.storage.Item
             * @instance
             */
            Item.prototype.value = "";

            /**
             * Creates a new Item instance using the specified properties.
             * @function create
             * @memberof lebai.storage.Item
             * @static
             * @param {lebai.storage.Item.$Properties=} [properties] Properties to set
             * @returns {lebai.storage.Item} Item instance
             * @type {{
             *   (properties: lebai.storage.Item.$Shape): lebai.storage.Item & lebai.storage.Item.$Shape;
             *   (properties?: lebai.storage.Item.$Properties): lebai.storage.Item;
             * }}
             */
            Item.create = function(properties) {
                return new Item(properties);
            };

            /**
             * Encodes the specified Item message. Does not implicitly {@link lebai.storage.Item.verify|verify} messages.
             * @function encode
             * @memberof lebai.storage.Item
             * @static
             * @param {lebai.storage.Item.$Properties} message Item message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Item.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
                if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.value);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Item message, length delimited. Does not implicitly {@link lebai.storage.Item.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.storage.Item
             * @static
             * @param {lebai.storage.Item.$Properties} message Item message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Item.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an Item message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.storage.Item
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.storage.Item & lebai.storage.Item.$Shape} Item
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Item.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.storage.Item();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.key = value;
                            else
                                delete message.key;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an Item message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.storage.Item
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.storage.Item & lebai.storage.Item.$Shape} Item
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Item.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an Item message.
             * @function verify
             * @memberof lebai.storage.Item
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Item.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    if (!$util.isString(message.key))
                        return "key: string expected";
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    if (!$util.isString(message.value))
                        return "value: string expected";
                return null;
            };

            /**
             * Creates an Item message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.storage.Item
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.storage.Item} Item
             */
            Item.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.storage.Item)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.storage.Item: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.storage.Item();
                if (object.key != null)
                    if (typeof object.key !== "string" || object.key.length)
                        message.key = $String(object.key);
                if (object.value != null)
                    if (typeof object.value !== "string" || object.value.length)
                        message.value = $String(object.value);
                return message;
            };

            /**
             * Creates a plain object from an Item message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.storage.Item
             * @static
             * @param {lebai.storage.Item} message Item
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Item.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.key = "";
                    object.value = "";
                }
                if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                    object.key = message.key;
                if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this Item to JSON.
             * @function toJSON
             * @memberof lebai.storage.Item
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Item.prototype.toJSON = function() {
                return Item.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Item
             * @function getTypeUrl
             * @memberof lebai.storage.Item
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Item.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.storage.Item";
            };

            return Item;
        })();

        storage.GetItemsRequest = (function() {

            /**
             * Properties of a GetItemsRequest.
             * @typedef {Object} lebai.storage.GetItemsRequest.$Properties
             * @property {string|null} [prefix] GetItemsRequest prefix
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a GetItemsRequest.
             * @memberof lebai.storage
             * @interface IGetItemsRequest
             * @augments lebai.storage.GetItemsRequest.$Properties
             * @deprecated Use lebai.storage.GetItemsRequest.$Properties instead.
             */

            /**
             * Shape of a GetItemsRequest.
             * @typedef {lebai.storage.GetItemsRequest.$Properties} lebai.storage.GetItemsRequest.$Shape
             */

            /**
             * Constructs a new GetItemsRequest.
             * @memberof lebai.storage
             * @classdesc Represents a GetItemsRequest.
             * @constructor
             * @param {lebai.storage.GetItemsRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var GetItemsRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * GetItemsRequest prefix.
             * @member {string} prefix
             * @memberof lebai.storage.GetItemsRequest
             * @instance
             */
            GetItemsRequest.prototype.prefix = "";

            /**
             * Creates a new GetItemsRequest instance using the specified properties.
             * @function create
             * @memberof lebai.storage.GetItemsRequest
             * @static
             * @param {lebai.storage.GetItemsRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.storage.GetItemsRequest} GetItemsRequest instance
             * @type {{
             *   (properties: lebai.storage.GetItemsRequest.$Shape): lebai.storage.GetItemsRequest & lebai.storage.GetItemsRequest.$Shape;
             *   (properties?: lebai.storage.GetItemsRequest.$Properties): lebai.storage.GetItemsRequest;
             * }}
             */
            GetItemsRequest.create = function(properties) {
                return new GetItemsRequest(properties);
            };

            /**
             * Encodes the specified GetItemsRequest message. Does not implicitly {@link lebai.storage.GetItemsRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.storage.GetItemsRequest
             * @static
             * @param {lebai.storage.GetItemsRequest.$Properties} message GetItemsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetItemsRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.prefix != null && $Object.hasOwnProperty.call(message, "prefix") && message.prefix !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.prefix);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GetItemsRequest message, length delimited. Does not implicitly {@link lebai.storage.GetItemsRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.storage.GetItemsRequest
             * @static
             * @param {lebai.storage.GetItemsRequest.$Properties} message GetItemsRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GetItemsRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a GetItemsRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.storage.GetItemsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.storage.GetItemsRequest & lebai.storage.GetItemsRequest.$Shape} GetItemsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetItemsRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.storage.GetItemsRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.prefix = value;
                            else
                                delete message.prefix;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a GetItemsRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.storage.GetItemsRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.storage.GetItemsRequest & lebai.storage.GetItemsRequest.$Shape} GetItemsRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GetItemsRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GetItemsRequest message.
             * @function verify
             * @memberof lebai.storage.GetItemsRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GetItemsRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.prefix != null && $Object.hasOwnProperty.call(message, "prefix"))
                    if (!$util.isString(message.prefix))
                        return "prefix: string expected";
                return null;
            };

            /**
             * Creates a GetItemsRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.storage.GetItemsRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.storage.GetItemsRequest} GetItemsRequest
             */
            GetItemsRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.storage.GetItemsRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.storage.GetItemsRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.storage.GetItemsRequest();
                if (object.prefix != null)
                    if (typeof object.prefix !== "string" || object.prefix.length)
                        message.prefix = $String(object.prefix);
                return message;
            };

            /**
             * Creates a plain object from a GetItemsRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.storage.GetItemsRequest
             * @static
             * @param {lebai.storage.GetItemsRequest} message GetItemsRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GetItemsRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.prefix = "";
                if (message.prefix != null && $Object.hasOwnProperty.call(message, "prefix"))
                    object.prefix = message.prefix;
                return object;
            };

            /**
             * Converts this GetItemsRequest to JSON.
             * @function toJSON
             * @memberof lebai.storage.GetItemsRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GetItemsRequest.prototype.toJSON = function() {
                return GetItemsRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for GetItemsRequest
             * @function getTypeUrl
             * @memberof lebai.storage.GetItemsRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            GetItemsRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.storage.GetItemsRequest";
            };

            return GetItemsRequest;
        })();

        storage.Items = (function() {

            /**
             * Properties of an Items.
             * @typedef {Object} lebai.storage.Items.$Properties
             * @property {Array.<lebai.storage.Item.$Properties>|null} [items] Items items
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an Items.
             * @memberof lebai.storage
             * @interface IItems
             * @augments lebai.storage.Items.$Properties
             * @deprecated Use lebai.storage.Items.$Properties instead.
             */

            /**
             * Shape of an Items.
             * @typedef {lebai.storage.Items.$Properties} lebai.storage.Items.$Shape
             */

            /**
             * Constructs a new Items.
             * @memberof lebai.storage
             * @classdesc Represents an Items.
             * @constructor
             * @param {lebai.storage.Items.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Items = function (properties) {
                this.items = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Items items.
             * @member {Array.<lebai.storage.Item.$Properties>} items
             * @memberof lebai.storage.Items
             * @instance
             */
            Items.prototype.items = $util.emptyArray;

            /**
             * Creates a new Items instance using the specified properties.
             * @function create
             * @memberof lebai.storage.Items
             * @static
             * @param {lebai.storage.Items.$Properties=} [properties] Properties to set
             * @returns {lebai.storage.Items} Items instance
             * @type {{
             *   (properties: lebai.storage.Items.$Shape): lebai.storage.Items & lebai.storage.Items.$Shape;
             *   (properties?: lebai.storage.Items.$Properties): lebai.storage.Items;
             * }}
             */
            Items.create = function(properties) {
                return new Items(properties);
            };

            /**
             * Encodes the specified Items message. Does not implicitly {@link lebai.storage.Items.verify|verify} messages.
             * @function encode
             * @memberof lebai.storage.Items
             * @static
             * @param {lebai.storage.Items.$Properties} message Items message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Items.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (var i = 0; i < message.items.length; ++i)
                        $root.lebai.storage.Item.encode(message.items[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Items message, length delimited. Does not implicitly {@link lebai.storage.Items.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.storage.Items
             * @static
             * @param {lebai.storage.Items.$Properties} message Items message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Items.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an Items message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.storage.Items
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.storage.Items & lebai.storage.Items.$Shape} Items
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Items.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.storage.Items();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.lebai.storage.Item.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an Items message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.storage.Items
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.storage.Items & lebai.storage.Items.$Shape} Items
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Items.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an Items message.
             * @function verify
             * @memberof lebai.storage.Items
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Items.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
                    if (!$Array.isArray(message.items))
                        return "items: array expected";
                    for (var i = 0; i < message.items.length; ++i) {
                        var error = $root.lebai.storage.Item.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an Items message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.storage.Items
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.storage.Items} Items
             */
            Items.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.storage.Items)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.storage.Items: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.storage.Items();
                if (object.items) {
                    if (!$Array.isArray(object.items))
                        throw $TypeError(".lebai.storage.Items.items: array expected");
                    message.items = $Array(object.items.length);
                    for (var i = 0; i < object.items.length; ++i) {
                        if (!$util.isObject(object.items[i]))
                            throw $TypeError(".lebai.storage.Items.items: object expected");
                        message.items[i] = $root.lebai.storage.Item.fromObject(object.items[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an Items message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.storage.Items
             * @static
             * @param {lebai.storage.Items} message Items
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Items.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = $Array(message.items.length);
                    for (var j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.lebai.storage.Item.toObject(message.items[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this Items to JSON.
             * @function toJSON
             * @memberof lebai.storage.Items
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Items.prototype.toJSON = function() {
                return Items.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Items
             * @function getTypeUrl
             * @memberof lebai.storage.Items
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Items.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.storage.Items";
            };

            return Items;
        })();

        storage.StorageService = (function() {

            /**
             * Constructs a new StorageService service.
             * @memberof lebai.storage
             * @classdesc Represents a StorageService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var StorageService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(StorageService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: StorageService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new StorageService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.storage.StorageService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {StorageService} RPC service. Useful where requests and/or responses are streamed.
             */
            StorageService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.storage.StorageService#setItem}.
             * @memberof lebai.storage.StorageService
             * @typedef SetItemCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetItem.
             * @memberof lebai.storage.StorageService
             * @typedef SetItem
             * @type {{
             *   (request: lebai.storage.IItem, callback: lebai.storage.StorageService.SetItemCallback): void;
             *   (request: lebai.storage.IItem): Promise<google.protobuf.Empty>;
             *   readonly name: "SetItem";
             *   readonly path: "/lebai.storage.StorageService/SetItem";
             *   readonly requestType: "Item";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetItem.
             * @name lebai.storage.StorageService#setItem
             * @type {lebai.storage.StorageService.SetItem}
             */
            $Object.defineProperties(StorageService.prototype.setItem = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, StorageService.prototype.setItem, $root.lebai.storage.Item, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetItem" },
                path: { value: "/lebai.storage.StorageService/SetItem" },
                requestType: { value: "Item" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.storage.StorageService#getItem}.
             * @memberof lebai.storage.StorageService
             * @typedef GetItemCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.storage.Item} [response] Item
             */

            /**
             * Calls GetItem.
             * @memberof lebai.storage.StorageService
             * @typedef GetItem
             * @type {{
             *   (request: lebai.storage.IItemIndex, callback: lebai.storage.StorageService.GetItemCallback): void;
             *   (request: lebai.storage.IItemIndex): Promise<lebai.storage.Item>;
             *   readonly name: "GetItem";
             *   readonly path: "/lebai.storage.StorageService/GetItem";
             *   readonly requestType: "ItemIndex";
             *   readonly responseType: "Item";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetItem.
             * @name lebai.storage.StorageService#getItem
             * @type {lebai.storage.StorageService.GetItem}
             */
            $Object.defineProperties(StorageService.prototype.getItem = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, StorageService.prototype.getItem, $root.lebai.storage.ItemIndex, $root.lebai.storage.Item, request, callback);
            }, {
                name: { value: "GetItem" },
                path: { value: "/lebai.storage.StorageService/GetItem" },
                requestType: { value: "ItemIndex" },
                responseType: { value: "Item" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.storage.StorageService#getItems}.
             * @memberof lebai.storage.StorageService
             * @typedef GetItemsCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.storage.Items} [response] Items
             */

            /**
             * Calls GetItems.
             * @memberof lebai.storage.StorageService
             * @typedef GetItems
             * @type {{
             *   (request: lebai.storage.IGetItemsRequest, callback: lebai.storage.StorageService.GetItemsCallback): void;
             *   (request: lebai.storage.IGetItemsRequest): Promise<lebai.storage.Items>;
             *   readonly name: "GetItems";
             *   readonly path: "/lebai.storage.StorageService/GetItems";
             *   readonly requestType: "GetItemsRequest";
             *   readonly responseType: "Items";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetItems.
             * @name lebai.storage.StorageService#getItems
             * @type {lebai.storage.StorageService.GetItems}
             */
            $Object.defineProperties(StorageService.prototype.getItems = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, StorageService.prototype.getItems, $root.lebai.storage.GetItemsRequest, $root.lebai.storage.Items, request, callback);
            }, {
                name: { value: "GetItems" },
                path: { value: "/lebai.storage.StorageService/GetItems" },
                requestType: { value: "GetItemsRequest" },
                responseType: { value: "Items" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return StorageService;
        })();

        return storage;
    })();

    lebai.structure = (function() {

        /**
         * Namespace structure.
         * @memberof lebai
         * @namespace
         */
        var structure = {};

        structure.Structure = (function() {

            /**
             * Properties of a Structure.
             * @typedef {Object} lebai.structure.Structure.$Properties
             * @property {boolean|null} [active] Structure active
             * @property {string|null} [name] Structure name
             * @property {lebai.system.ArmModel|null} [kind] Structure kind
             * @property {number|null} [dof] Structure dof
             * @property {string|null} [dh] Structure dh
             * @property {string|null} [dyn] Structure dyn
             * @property {string|null} [servo] Structure servo
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Structure.
             * @memberof lebai.structure
             * @interface IStructure
             * @augments lebai.structure.Structure.$Properties
             * @deprecated Use lebai.structure.Structure.$Properties instead.
             */

            /**
             * Shape of a Structure.
             * @typedef {lebai.structure.Structure.$Properties} lebai.structure.Structure.$Shape
             */

            /**
             * Constructs a new Structure.
             * @memberof lebai.structure
             * @classdesc Represents a Structure.
             * @constructor
             * @param {lebai.structure.Structure.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Structure = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Structure active.
             * @member {boolean} active
             * @memberof lebai.structure.Structure
             * @instance
             */
            Structure.prototype.active = false;

            /**
             * Structure name.
             * @member {string} name
             * @memberof lebai.structure.Structure
             * @instance
             */
            Structure.prototype.name = "";

            /**
             * Structure kind.
             * @member {lebai.system.ArmModel} kind
             * @memberof lebai.structure.Structure
             * @instance
             */
            Structure.prototype.kind = 0;

            /**
             * Structure dof.
             * @member {number} dof
             * @memberof lebai.structure.Structure
             * @instance
             */
            Structure.prototype.dof = 0;

            /**
             * Structure dh.
             * @member {string} dh
             * @memberof lebai.structure.Structure
             * @instance
             */
            Structure.prototype.dh = "";

            /**
             * Structure dyn.
             * @member {string} dyn
             * @memberof lebai.structure.Structure
             * @instance
             */
            Structure.prototype.dyn = "";

            /**
             * Structure servo.
             * @member {string} servo
             * @memberof lebai.structure.Structure
             * @instance
             */
            Structure.prototype.servo = "";

            /**
             * Creates a new Structure instance using the specified properties.
             * @function create
             * @memberof lebai.structure.Structure
             * @static
             * @param {lebai.structure.Structure.$Properties=} [properties] Properties to set
             * @returns {lebai.structure.Structure} Structure instance
             * @type {{
             *   (properties: lebai.structure.Structure.$Shape): lebai.structure.Structure & lebai.structure.Structure.$Shape;
             *   (properties?: lebai.structure.Structure.$Properties): lebai.structure.Structure;
             * }}
             */
            Structure.create = function(properties) {
                return new Structure(properties);
            };

            /**
             * Encodes the specified Structure message. Does not implicitly {@link lebai.structure.Structure.verify|verify} messages.
             * @function encode
             * @memberof lebai.structure.Structure
             * @static
             * @param {lebai.structure.Structure.$Properties} message Structure message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Structure.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.active != null && $Object.hasOwnProperty.call(message, "active") && message.active !== false)
                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message.active);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.name);
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind") && message.kind !== 0)
                    writer.uint32(/* id 12, wireType 0 =*/96).int32(message.kind);
                if (message.dof != null && $Object.hasOwnProperty.call(message, "dof") && message.dof !== 0)
                    writer.uint32(/* id 13, wireType 0 =*/104).uint32(message.dof);
                if (message.dh != null && $Object.hasOwnProperty.call(message, "dh") && message.dh !== "")
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.dh);
                if (message.dyn != null && $Object.hasOwnProperty.call(message, "dyn") && message.dyn !== "")
                    writer.uint32(/* id 15, wireType 2 =*/122).string(message.dyn);
                if (message.servo != null && $Object.hasOwnProperty.call(message, "servo") && message.servo !== "")
                    writer.uint32(/* id 16, wireType 2 =*/130).string(message.servo);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Structure message, length delimited. Does not implicitly {@link lebai.structure.Structure.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.structure.Structure
             * @static
             * @param {lebai.structure.Structure.$Properties} message Structure message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Structure.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Structure message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.structure.Structure
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.structure.Structure & lebai.structure.Structure.$Shape} Structure
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Structure.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.structure.Structure();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 10: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.active = value;
                            else
                                delete message.active;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.kind = value;
                            else
                                delete message.kind;
                            continue;
                        }
                    case 13: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.dof = value;
                            else
                                delete message.dof;
                            continue;
                        }
                    case 14: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dh = value;
                            else
                                delete message.dh;
                            continue;
                        }
                    case 15: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dyn = value;
                            else
                                delete message.dyn;
                            continue;
                        }
                    case 16: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.servo = value;
                            else
                                delete message.servo;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Structure message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.structure.Structure
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.structure.Structure & lebai.structure.Structure.$Shape} Structure
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Structure.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Structure message.
             * @function verify
             * @memberof lebai.structure.Structure
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Structure.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.active != null && $Object.hasOwnProperty.call(message, "active"))
                    if (typeof message.active !== "boolean")
                        return "active: boolean expected";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    if (typeof message.kind !== "number" || (message.kind | 0) !== message.kind)
                        return "kind: enum value expected";
                if (message.dof != null && $Object.hasOwnProperty.call(message, "dof"))
                    if (!$util.isInteger(message.dof))
                        return "dof: integer expected";
                if (message.dh != null && $Object.hasOwnProperty.call(message, "dh"))
                    if (!$util.isString(message.dh))
                        return "dh: string expected";
                if (message.dyn != null && $Object.hasOwnProperty.call(message, "dyn"))
                    if (!$util.isString(message.dyn))
                        return "dyn: string expected";
                if (message.servo != null && $Object.hasOwnProperty.call(message, "servo"))
                    if (!$util.isString(message.servo))
                        return "servo: string expected";
                return null;
            };

            /**
             * Creates a Structure message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.structure.Structure
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.structure.Structure} Structure
             */
            Structure.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.structure.Structure)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.structure.Structure: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.structure.Structure();
                if (object.active != null)
                    if (object.active)
                        message.active = $Boolean(object.active);
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.kind !== 0 && (typeof object.kind !== "string" || $root.lebai.system.ArmModel[object.kind] !== 0))
                    switch (object.kind) {
                    case "J6M1":
                    case 0:
                        message.kind = 0;
                        break;
                    case "J6L1":
                    case 1:
                        message.kind = 1;
                        break;
                    case "J5L1":
                    case 11:
                        message.kind = 11;
                        break;
                    case "J5T1":
                    case 51:
                        message.kind = 51;
                        break;
                    default:
                        if (typeof object.kind === "number" && (object.kind | 0) === object.kind)
                            message.kind = object.kind;
                    }
                if (object.dof != null)
                    if ($Number(object.dof) !== 0)
                        message.dof = object.dof >>> 0;
                if (object.dh != null)
                    if (typeof object.dh !== "string" || object.dh.length)
                        message.dh = $String(object.dh);
                if (object.dyn != null)
                    if (typeof object.dyn !== "string" || object.dyn.length)
                        message.dyn = $String(object.dyn);
                if (object.servo != null)
                    if (typeof object.servo !== "string" || object.servo.length)
                        message.servo = $String(object.servo);
                return message;
            };

            /**
             * Creates a plain object from a Structure message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.structure.Structure
             * @static
             * @param {lebai.structure.Structure} message Structure
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Structure.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.active = false;
                    object.name = "";
                    object.kind = options.enums === $String ? "J6M1" : 0;
                    object.dof = 0;
                    object.dh = "";
                    object.dyn = "";
                    object.servo = "";
                }
                if (message.active != null && $Object.hasOwnProperty.call(message, "active"))
                    object.active = message.active;
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    object.kind = options.enums === $String ? $root.lebai.system.ArmModel[message.kind] === $undefined ? message.kind : $root.lebai.system.ArmModel[message.kind] : message.kind;
                if (message.dof != null && $Object.hasOwnProperty.call(message, "dof"))
                    object.dof = message.dof;
                if (message.dh != null && $Object.hasOwnProperty.call(message, "dh"))
                    object.dh = message.dh;
                if (message.dyn != null && $Object.hasOwnProperty.call(message, "dyn"))
                    object.dyn = message.dyn;
                if (message.servo != null && $Object.hasOwnProperty.call(message, "servo"))
                    object.servo = message.servo;
                return object;
            };

            /**
             * Converts this Structure to JSON.
             * @function toJSON
             * @memberof lebai.structure.Structure
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Structure.prototype.toJSON = function() {
                return Structure.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Structure
             * @function getTypeUrl
             * @memberof lebai.structure.Structure
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Structure.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.structure.Structure";
            };

            return Structure;
        })();

        structure.SaveStructureRequest = (function() {

            /**
             * Properties of a SaveStructureRequest.
             * @typedef {Object} lebai.structure.SaveStructureRequest.$Properties
             * @property {string|null} [name] SaveStructureRequest name
             * @property {lebai.structure.Structure.$Properties|null} [data] SaveStructureRequest data
             * @property {string|null} [dir] SaveStructureRequest dir
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a SaveStructureRequest.
             * @memberof lebai.structure
             * @interface ISaveStructureRequest
             * @augments lebai.structure.SaveStructureRequest.$Properties
             * @deprecated Use lebai.structure.SaveStructureRequest.$Properties instead.
             */

            /**
             * Shape of a SaveStructureRequest.
             * @typedef {lebai.structure.SaveStructureRequest.$Properties} lebai.structure.SaveStructureRequest.$Shape
             */

            /**
             * Constructs a new SaveStructureRequest.
             * @memberof lebai.structure
             * @classdesc Represents a SaveStructureRequest.
             * @constructor
             * @param {lebai.structure.SaveStructureRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var SaveStructureRequest = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * SaveStructureRequest name.
             * @member {string} name
             * @memberof lebai.structure.SaveStructureRequest
             * @instance
             */
            SaveStructureRequest.prototype.name = "";

            /**
             * SaveStructureRequest data.
             * @member {lebai.structure.Structure.$Properties|null|undefined} data
             * @memberof lebai.structure.SaveStructureRequest
             * @instance
             */
            SaveStructureRequest.prototype.data = null;

            /**
             * SaveStructureRequest dir.
             * @member {string} dir
             * @memberof lebai.structure.SaveStructureRequest
             * @instance
             */
            SaveStructureRequest.prototype.dir = "";

            /**
             * Creates a new SaveStructureRequest instance using the specified properties.
             * @function create
             * @memberof lebai.structure.SaveStructureRequest
             * @static
             * @param {lebai.structure.SaveStructureRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.structure.SaveStructureRequest} SaveStructureRequest instance
             * @type {{
             *   (properties: lebai.structure.SaveStructureRequest.$Shape): lebai.structure.SaveStructureRequest & lebai.structure.SaveStructureRequest.$Shape;
             *   (properties?: lebai.structure.SaveStructureRequest.$Properties): lebai.structure.SaveStructureRequest;
             * }}
             */
            SaveStructureRequest.create = function(properties) {
                return new SaveStructureRequest(properties);
            };

            /**
             * Encodes the specified SaveStructureRequest message. Does not implicitly {@link lebai.structure.SaveStructureRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.structure.SaveStructureRequest
             * @static
             * @param {lebai.structure.SaveStructureRequest.$Properties} message SaveStructureRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveStructureRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    $root.lebai.structure.Structure.encode(message.data, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SaveStructureRequest message, length delimited. Does not implicitly {@link lebai.structure.SaveStructureRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.structure.SaveStructureRequest
             * @static
             * @param {lebai.structure.SaveStructureRequest.$Properties} message SaveStructureRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SaveStructureRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a SaveStructureRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.structure.SaveStructureRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.structure.SaveStructureRequest & lebai.structure.SaveStructureRequest.$Shape} SaveStructureRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveStructureRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.structure.SaveStructureRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.data = $root.lebai.structure.Structure.decode(reader, reader.uint32(), $undefined, _depth + 1, message.data);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a SaveStructureRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.structure.SaveStructureRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.structure.SaveStructureRequest & lebai.structure.SaveStructureRequest.$Shape} SaveStructureRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SaveStructureRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SaveStructureRequest message.
             * @function verify
             * @memberof lebai.structure.SaveStructureRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SaveStructureRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                    var error = $root.lebai.structure.Structure.verify(message.data, _depth + 1);
                    if (error)
                        return "data." + error;
                }
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                return null;
            };

            /**
             * Creates a SaveStructureRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.structure.SaveStructureRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.structure.SaveStructureRequest} SaveStructureRequest
             */
            SaveStructureRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.structure.SaveStructureRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.structure.SaveStructureRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.structure.SaveStructureRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.data != null) {
                    if (!$util.isObject(object.data))
                        throw $TypeError(".lebai.structure.SaveStructureRequest.data: object expected");
                    message.data = $root.lebai.structure.Structure.fromObject(object.data, _depth + 1);
                }
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                return message;
            };

            /**
             * Creates a plain object from a SaveStructureRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.structure.SaveStructureRequest
             * @static
             * @param {lebai.structure.SaveStructureRequest} message SaveStructureRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SaveStructureRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.name = "";
                    object.data = null;
                    object.dir = "";
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                    object.data = $root.lebai.structure.Structure.toObject(message.data, options, _depth + 1);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                return object;
            };

            /**
             * Converts this SaveStructureRequest to JSON.
             * @function toJSON
             * @memberof lebai.structure.SaveStructureRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SaveStructureRequest.prototype.toJSON = function() {
                return SaveStructureRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for SaveStructureRequest
             * @function getTypeUrl
             * @memberof lebai.structure.SaveStructureRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            SaveStructureRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.structure.SaveStructureRequest";
            };

            return SaveStructureRequest;
        })();

        structure.StructureService = (function() {

            /**
             * Constructs a new StructureService service.
             * @memberof lebai.structure
             * @classdesc Represents a StructureService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var StructureService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(StructureService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: StructureService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new StructureService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.structure.StructureService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {StructureService} RPC service. Useful where requests and/or responses are streamed.
             */
            StructureService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.structure.StructureService#saveStructure}.
             * @memberof lebai.structure.StructureService
             * @typedef SaveStructureCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SaveStructure.
             * @memberof lebai.structure.StructureService
             * @typedef SaveStructure
             * @type {{
             *   (request: lebai.structure.ISaveStructureRequest, callback: lebai.structure.StructureService.SaveStructureCallback): void;
             *   (request: lebai.structure.ISaveStructureRequest): Promise<google.protobuf.Empty>;
             *   readonly name: "SaveStructure";
             *   readonly path: "/lebai.structure.StructureService/SaveStructure";
             *   readonly requestType: "SaveStructureRequest";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SaveStructure.
             * @name lebai.structure.StructureService#saveStructure
             * @type {lebai.structure.StructureService.SaveStructure}
             */
            $Object.defineProperties(StructureService.prototype.saveStructure = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, StructureService.prototype.saveStructure, $root.lebai.structure.SaveStructureRequest, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SaveStructure" },
                path: { value: "/lebai.structure.StructureService/SaveStructure" },
                requestType: { value: "SaveStructureRequest" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.structure.StructureService#loadStructure}.
             * @memberof lebai.structure.StructureService
             * @typedef LoadStructureCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.structure.Structure} [response] Structure
             */

            /**
             * Calls LoadStructure.
             * @memberof lebai.structure.StructureService
             * @typedef LoadStructure
             * @type {{
             *   (request: lebai.db.ILoadRequest, callback: lebai.structure.StructureService.LoadStructureCallback): void;
             *   (request: lebai.db.ILoadRequest): Promise<lebai.structure.Structure>;
             *   readonly name: "LoadStructure";
             *   readonly path: "/lebai.structure.StructureService/LoadStructure";
             *   readonly requestType: "db.LoadRequest";
             *   readonly responseType: "Structure";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadStructure.
             * @name lebai.structure.StructureService#loadStructure
             * @type {lebai.structure.StructureService.LoadStructure}
             */
            $Object.defineProperties(StructureService.prototype.loadStructure = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, StructureService.prototype.loadStructure, $root.lebai.db.LoadRequest, $root.lebai.structure.Structure, request, callback);
            }, {
                name: { value: "LoadStructure" },
                path: { value: "/lebai.structure.StructureService/LoadStructure" },
                requestType: { value: "db.LoadRequest" },
                responseType: { value: "Structure" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.structure.StructureService#loadStructureList}.
             * @memberof lebai.structure.StructureService
             * @typedef LoadStructureListCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.db.LoadListResponse} [response] LoadListResponse
             */

            /**
             * Calls LoadStructureList.
             * @memberof lebai.structure.StructureService
             * @typedef LoadStructureList
             * @type {{
             *   (request: lebai.db.ILoadListRequest, callback: lebai.structure.StructureService.LoadStructureListCallback): void;
             *   (request: lebai.db.ILoadListRequest): Promise<lebai.db.LoadListResponse>;
             *   readonly name: "LoadStructureList";
             *   readonly path: "/lebai.structure.StructureService/LoadStructureList";
             *   readonly requestType: "db.LoadListRequest";
             *   readonly responseType: "db.LoadListResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadStructureList.
             * @name lebai.structure.StructureService#loadStructureList
             * @type {lebai.structure.StructureService.LoadStructureList}
             */
            $Object.defineProperties(StructureService.prototype.loadStructureList = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, StructureService.prototype.loadStructureList, $root.lebai.db.LoadListRequest, $root.lebai.db.LoadListResponse, request, callback);
            }, {
                name: { value: "LoadStructureList" },
                path: { value: "/lebai.structure.StructureService/LoadStructureList" },
                requestType: { value: "db.LoadListRequest" },
                responseType: { value: "db.LoadListResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return StructureService;
        })();

        return structure;
    })();

    lebai.task = (function() {

        /**
         * Namespace task.
         * @memberof lebai
         * @namespace
         */
        var task = {};

        /**
         * TaskKind enum.
         * @name lebai.task.TaskKind
         * @enum {number}
         * @property {number} LUA=0 LUA value
         * @property {number} APP=10 APP value
         */
        task.TaskKind = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "LUA"] = 0;
            values[valuesById[10] = "APP"] = 10;
            return values;
        })();

        /**
         * TaskState enum.
         * @name lebai.task.TaskState
         * @enum {number}
         * @property {number} NONE=0 NONE value
         * @property {number} RUNNING=1 RUNNING value
         * @property {number} PAUSE=2 PAUSE value
         * @property {number} SUCCESS=3 SUCCESS value
         * @property {number} INTERRUPT=4 INTERRUPT value
         * @property {number} FAIL=5 FAIL value
         * @property {number} WAIT=10 WAIT value
         * @property {number} BEGIN=11 BEGIN value
         * @property {number} INTERRUPTING=14 INTERRUPTING value
         */
        task.TaskState = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "RUNNING"] = 1;
            values[valuesById[2] = "PAUSE"] = 2;
            values[valuesById[3] = "SUCCESS"] = 3;
            values[valuesById[4] = "INTERRUPT"] = 4;
            values[valuesById[5] = "FAIL"] = 5;
            values[valuesById[10] = "WAIT"] = 10;
            values[valuesById[11] = "BEGIN"] = 11;
            values[valuesById[14] = "INTERRUPTING"] = 14;
            return values;
        })();

        task.TaskIndex = (function() {

            /**
             * Properties of a TaskIndex.
             * @typedef {Object} lebai.task.TaskIndex.$Properties
             * @property {number|null} [id] TaskIndex id
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a TaskIndex.
             * @memberof lebai.task
             * @interface ITaskIndex
             * @augments lebai.task.TaskIndex.$Properties
             * @deprecated Use lebai.task.TaskIndex.$Properties instead.
             */

            /**
             * Shape of a TaskIndex.
             * @typedef {lebai.task.TaskIndex.$Properties} lebai.task.TaskIndex.$Shape
             */

            /**
             * Constructs a new TaskIndex.
             * @memberof lebai.task
             * @classdesc Represents a TaskIndex.
             * @constructor
             * @param {lebai.task.TaskIndex.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var TaskIndex = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * TaskIndex id.
             * @member {number} id
             * @memberof lebai.task.TaskIndex
             * @instance
             */
            TaskIndex.prototype.id = 0;

            /**
             * Creates a new TaskIndex instance using the specified properties.
             * @function create
             * @memberof lebai.task.TaskIndex
             * @static
             * @param {lebai.task.TaskIndex.$Properties=} [properties] Properties to set
             * @returns {lebai.task.TaskIndex} TaskIndex instance
             * @type {{
             *   (properties: lebai.task.TaskIndex.$Shape): lebai.task.TaskIndex & lebai.task.TaskIndex.$Shape;
             *   (properties?: lebai.task.TaskIndex.$Properties): lebai.task.TaskIndex;
             * }}
             */
            TaskIndex.create = function(properties) {
                return new TaskIndex(properties);
            };

            /**
             * Encodes the specified TaskIndex message. Does not implicitly {@link lebai.task.TaskIndex.verify|verify} messages.
             * @function encode
             * @memberof lebai.task.TaskIndex
             * @static
             * @param {lebai.task.TaskIndex.$Properties} message TaskIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TaskIndex.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.id);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified TaskIndex message, length delimited. Does not implicitly {@link lebai.task.TaskIndex.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.task.TaskIndex
             * @static
             * @param {lebai.task.TaskIndex.$Properties} message TaskIndex message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TaskIndex.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a TaskIndex message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.task.TaskIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.task.TaskIndex & lebai.task.TaskIndex.$Shape} TaskIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TaskIndex.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.task.TaskIndex();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a TaskIndex message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.task.TaskIndex
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.task.TaskIndex & lebai.task.TaskIndex.$Shape} TaskIndex
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TaskIndex.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a TaskIndex message.
             * @function verify
             * @memberof lebai.task.TaskIndex
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            TaskIndex.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                return null;
            };

            /**
             * Creates a TaskIndex message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.task.TaskIndex
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.task.TaskIndex} TaskIndex
             */
            TaskIndex.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.task.TaskIndex)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.task.TaskIndex: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.task.TaskIndex();
                if (object.id != null)
                    if ($Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a TaskIndex message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.task.TaskIndex
             * @static
             * @param {lebai.task.TaskIndex} message TaskIndex
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            TaskIndex.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults)
                    object.id = 0;
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    object.id = message.id;
                return object;
            };

            /**
             * Converts this TaskIndex to JSON.
             * @function toJSON
             * @memberof lebai.task.TaskIndex
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            TaskIndex.prototype.toJSON = function() {
                return TaskIndex.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for TaskIndex
             * @function getTypeUrl
             * @memberof lebai.task.TaskIndex
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            TaskIndex.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.task.TaskIndex";
            };

            return TaskIndex;
        })();

        task.Task = (function() {

            /**
             * Properties of a Task.
             * @typedef {Object} lebai.task.Task.$Properties
             * @property {number|null} [id] Task id
             * @property {string|null} [blockId] Task blockId
             * @property {number|null} [eventId] Task eventId
             * @property {lebai.task.TaskState|null} [state] Task state
             * @property {number|null} [loopCount] Task loopCount
             * @property {number|null} [loopTo] Task loopTo
             * @property {boolean|null} [isParallel] Task isParallel
             * @property {boolean|null} [isSimu] Task isSimu
             * @property {string|null} [stdout] Task stdout
             * @property {google.protobuf.Timestamp.$Properties|null} [startedAt] Task startedAt
             * @property {google.protobuf.Timestamp.$Properties|null} [endedAt] Task endedAt
             * @property {google.protobuf.Timestamp.$Properties|null} [pauseAt] Task pauseAt
             * @property {number|null} [prePause] Task prePause
             * @property {lebai.task.TaskKind|null} [kind] Task kind
             * @property {string|null} [dir] Task dir
             * @property {string|null} [name] Task name
             * @property {Array.<string>|null} [params] Task params
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Task.
             * @memberof lebai.task
             * @interface ITask
             * @augments lebai.task.Task.$Properties
             * @deprecated Use lebai.task.Task.$Properties instead.
             */

            /**
             * Shape of a Task.
             * @typedef {lebai.task.Task.$Properties} lebai.task.Task.$Shape
             */

            /**
             * Constructs a new Task.
             * @memberof lebai.task
             * @classdesc Represents a Task.
             * @constructor
             * @param {lebai.task.Task.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Task = function (properties) {
                this.params = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Task id.
             * @member {number} id
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.id = 0;

            /**
             * Task blockId.
             * @member {string} blockId
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.blockId = "";

            /**
             * Task eventId.
             * @member {number} eventId
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.eventId = 0;

            /**
             * Task state.
             * @member {lebai.task.TaskState} state
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.state = 0;

            /**
             * Task loopCount.
             * @member {number} loopCount
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.loopCount = 0;

            /**
             * Task loopTo.
             * @member {number} loopTo
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.loopTo = 0;

            /**
             * Task isParallel.
             * @member {boolean} isParallel
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.isParallel = false;

            /**
             * Task isSimu.
             * @member {boolean} isSimu
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.isSimu = false;

            /**
             * Task stdout.
             * @member {string} stdout
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.stdout = "";

            /**
             * Task startedAt.
             * @member {google.protobuf.Timestamp.$Properties|null|undefined} startedAt
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.startedAt = null;

            /**
             * Task endedAt.
             * @member {google.protobuf.Timestamp.$Properties|null|undefined} endedAt
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.endedAt = null;

            /**
             * Task pauseAt.
             * @member {google.protobuf.Timestamp.$Properties|null|undefined} pauseAt
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.pauseAt = null;

            /**
             * Task prePause.
             * @member {number} prePause
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.prePause = 0;

            /**
             * Task kind.
             * @member {lebai.task.TaskKind} kind
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.kind = 0;

            /**
             * Task dir.
             * @member {string} dir
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.dir = "";

            /**
             * Task name.
             * @member {string} name
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.name = "";

            /**
             * Task params.
             * @member {Array.<string>} params
             * @memberof lebai.task.Task
             * @instance
             */
            Task.prototype.params = $util.emptyArray;

            /**
             * Creates a new Task instance using the specified properties.
             * @function create
             * @memberof lebai.task.Task
             * @static
             * @param {lebai.task.Task.$Properties=} [properties] Properties to set
             * @returns {lebai.task.Task} Task instance
             * @type {{
             *   (properties: lebai.task.Task.$Shape): lebai.task.Task & lebai.task.Task.$Shape;
             *   (properties?: lebai.task.Task.$Properties): lebai.task.Task;
             * }}
             */
            Task.create = function(properties) {
                return new Task(properties);
            };

            /**
             * Encodes the specified Task message. Does not implicitly {@link lebai.task.Task.verify|verify} messages.
             * @function encode
             * @memberof lebai.task.Task
             * @static
             * @param {lebai.task.Task.$Properties} message Task message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Task.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.id);
                if (message.blockId != null && $Object.hasOwnProperty.call(message, "blockId") && message.blockId !== "")
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.blockId);
                if (message.eventId != null && $Object.hasOwnProperty.call(message, "eventId") && message.eventId !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.eventId);
                if (message.state != null && $Object.hasOwnProperty.call(message, "state") && message.state !== 0)
                    writer.uint32(/* id 12, wireType 0 =*/96).int32(message.state);
                if (message.loopCount != null && $Object.hasOwnProperty.call(message, "loopCount") && message.loopCount !== 0)
                    writer.uint32(/* id 13, wireType 0 =*/104).uint32(message.loopCount);
                if (message.loopTo != null && $Object.hasOwnProperty.call(message, "loopTo") && message.loopTo !== 0)
                    writer.uint32(/* id 14, wireType 0 =*/112).uint32(message.loopTo);
                if (message.isParallel != null && $Object.hasOwnProperty.call(message, "isParallel") && message.isParallel !== false)
                    writer.uint32(/* id 15, wireType 0 =*/120).bool(message.isParallel);
                if (message.isSimu != null && $Object.hasOwnProperty.call(message, "isSimu") && message.isSimu !== false)
                    writer.uint32(/* id 16, wireType 0 =*/128).bool(message.isSimu);
                if (message.stdout != null && $Object.hasOwnProperty.call(message, "stdout") && message.stdout !== "")
                    writer.uint32(/* id 17, wireType 2 =*/138).string(message.stdout);
                if (message.startedAt != null && $Object.hasOwnProperty.call(message, "startedAt"))
                    $root.google.protobuf.Timestamp.encode(message.startedAt, writer.uint32(/* id 18, wireType 2 =*/146).fork(), _depth + 1).ldelim();
                if (message.endedAt != null && $Object.hasOwnProperty.call(message, "endedAt"))
                    $root.google.protobuf.Timestamp.encode(message.endedAt, writer.uint32(/* id 19, wireType 2 =*/154).fork(), _depth + 1).ldelim();
                if (message.pauseAt != null && $Object.hasOwnProperty.call(message, "pauseAt"))
                    $root.google.protobuf.Timestamp.encode(message.pauseAt, writer.uint32(/* id 20, wireType 2 =*/162).fork(), _depth + 1).ldelim();
                if (message.prePause != null && $Object.hasOwnProperty.call(message, "prePause") && message.prePause !== 0)
                    writer.uint32(/* id 21, wireType 0 =*/168).uint32(message.prePause);
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind") && message.kind !== 0)
                    writer.uint32(/* id 30, wireType 0 =*/240).int32(message.kind);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 31, wireType 2 =*/250).string(message.dir);
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 32, wireType 2 =*/258).string(message.name);
                if (message.params != null && message.params.length)
                    for (var i = 0; i < message.params.length; ++i)
                        writer.uint32(/* id 33, wireType 2 =*/266).string(message.params[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Task message, length delimited. Does not implicitly {@link lebai.task.Task.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.task.Task
             * @static
             * @param {lebai.task.Task.$Properties} message Task message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Task.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Task message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.task.Task
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.task.Task & lebai.task.Task.$Shape} Task
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Task.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.task.Task();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.blockId = value;
                            else
                                delete message.blockId;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.eventId = value;
                            else
                                delete message.eventId;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.state = value;
                            else
                                delete message.state;
                            continue;
                        }
                    case 13: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.loopCount = value;
                            else
                                delete message.loopCount;
                            continue;
                        }
                    case 14: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.loopTo = value;
                            else
                                delete message.loopTo;
                            continue;
                        }
                    case 15: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.isParallel = value;
                            else
                                delete message.isParallel;
                            continue;
                        }
                    case 16: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.isSimu = value;
                            else
                                delete message.isSimu;
                            continue;
                        }
                    case 17: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.stdout = value;
                            else
                                delete message.stdout;
                            continue;
                        }
                    case 18: {
                            if (wireType !== 2)
                                break;
                            message.startedAt = $root.google.protobuf.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.startedAt);
                            continue;
                        }
                    case 19: {
                            if (wireType !== 2)
                                break;
                            message.endedAt = $root.google.protobuf.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.endedAt);
                            continue;
                        }
                    case 20: {
                            if (wireType !== 2)
                                break;
                            message.pauseAt = $root.google.protobuf.Timestamp.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pauseAt);
                            continue;
                        }
                    case 21: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.prePause = value;
                            else
                                delete message.prePause;
                            continue;
                        }
                    case 30: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.kind = value;
                            else
                                delete message.kind;
                            continue;
                        }
                    case 31: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    case 32: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 33: {
                            if (wireType !== 2)
                                break;
                            if (!(message.params && message.params.length))
                                message.params = [];
                            message.params.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Task message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.task.Task
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.task.Task & lebai.task.Task.$Shape} Task
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Task.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Task message.
             * @function verify
             * @memberof lebai.task.Task
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Task.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.blockId != null && $Object.hasOwnProperty.call(message, "blockId"))
                    if (!$util.isString(message.blockId))
                        return "blockId: string expected";
                if (message.eventId != null && $Object.hasOwnProperty.call(message, "eventId"))
                    if (!$util.isInteger(message.eventId))
                        return "eventId: integer expected";
                if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                    if (typeof message.state !== "number" || (message.state | 0) !== message.state)
                        return "state: enum value expected";
                if (message.loopCount != null && $Object.hasOwnProperty.call(message, "loopCount"))
                    if (!$util.isInteger(message.loopCount))
                        return "loopCount: integer expected";
                if (message.loopTo != null && $Object.hasOwnProperty.call(message, "loopTo"))
                    if (!$util.isInteger(message.loopTo))
                        return "loopTo: integer expected";
                if (message.isParallel != null && $Object.hasOwnProperty.call(message, "isParallel"))
                    if (typeof message.isParallel !== "boolean")
                        return "isParallel: boolean expected";
                if (message.isSimu != null && $Object.hasOwnProperty.call(message, "isSimu"))
                    if (typeof message.isSimu !== "boolean")
                        return "isSimu: boolean expected";
                if (message.stdout != null && $Object.hasOwnProperty.call(message, "stdout"))
                    if (!$util.isString(message.stdout))
                        return "stdout: string expected";
                if (message.startedAt != null && $Object.hasOwnProperty.call(message, "startedAt")) {
                    var error = $root.google.protobuf.Timestamp.verify(message.startedAt, _depth + 1);
                    if (error)
                        return "startedAt." + error;
                }
                if (message.endedAt != null && $Object.hasOwnProperty.call(message, "endedAt")) {
                    var error = $root.google.protobuf.Timestamp.verify(message.endedAt, _depth + 1);
                    if (error)
                        return "endedAt." + error;
                }
                if (message.pauseAt != null && $Object.hasOwnProperty.call(message, "pauseAt")) {
                    var error = $root.google.protobuf.Timestamp.verify(message.pauseAt, _depth + 1);
                    if (error)
                        return "pauseAt." + error;
                }
                if (message.prePause != null && $Object.hasOwnProperty.call(message, "prePause"))
                    if (!$util.isInteger(message.prePause))
                        return "prePause: integer expected";
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    if (typeof message.kind !== "number" || (message.kind | 0) !== message.kind)
                        return "kind: enum value expected";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.params != null && $Object.hasOwnProperty.call(message, "params")) {
                    if (!$Array.isArray(message.params))
                        return "params: array expected";
                    for (var i = 0; i < message.params.length; ++i)
                        if (!$util.isString(message.params[i]))
                            return "params: string[] expected";
                }
                return null;
            };

            /**
             * Creates a Task message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.task.Task
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.task.Task} Task
             */
            Task.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.task.Task)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.task.Task: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.task.Task();
                if (object.id != null)
                    if ($Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.blockId != null)
                    if (typeof object.blockId !== "string" || object.blockId.length)
                        message.blockId = $String(object.blockId);
                if (object.eventId != null)
                    if ($Number(object.eventId) !== 0)
                        message.eventId = object.eventId >>> 0;
                if (object.state !== 0 && (typeof object.state !== "string" || $root.lebai.task.TaskState[object.state] !== 0))
                    switch (object.state) {
                    case "NONE":
                    case 0:
                        message.state = 0;
                        break;
                    case "RUNNING":
                    case 1:
                        message.state = 1;
                        break;
                    case "PAUSE":
                    case 2:
                        message.state = 2;
                        break;
                    case "SUCCESS":
                    case 3:
                        message.state = 3;
                        break;
                    case "INTERRUPT":
                    case 4:
                        message.state = 4;
                        break;
                    case "FAIL":
                    case 5:
                        message.state = 5;
                        break;
                    case "WAIT":
                    case 10:
                        message.state = 10;
                        break;
                    case "BEGIN":
                    case 11:
                        message.state = 11;
                        break;
                    case "INTERRUPTING":
                    case 14:
                        message.state = 14;
                        break;
                    default:
                        if (typeof object.state === "number" && (object.state | 0) === object.state)
                            message.state = object.state;
                    }
                if (object.loopCount != null)
                    if ($Number(object.loopCount) !== 0)
                        message.loopCount = object.loopCount >>> 0;
                if (object.loopTo != null)
                    if ($Number(object.loopTo) !== 0)
                        message.loopTo = object.loopTo >>> 0;
                if (object.isParallel != null)
                    if (object.isParallel)
                        message.isParallel = $Boolean(object.isParallel);
                if (object.isSimu != null)
                    if (object.isSimu)
                        message.isSimu = $Boolean(object.isSimu);
                if (object.stdout != null)
                    if (typeof object.stdout !== "string" || object.stdout.length)
                        message.stdout = $String(object.stdout);
                if (object.startedAt != null) {
                    if (!$util.isObject(object.startedAt))
                        throw $TypeError(".lebai.task.Task.startedAt: object expected");
                    message.startedAt = $root.google.protobuf.Timestamp.fromObject(object.startedAt, _depth + 1);
                }
                if (object.endedAt != null) {
                    if (!$util.isObject(object.endedAt))
                        throw $TypeError(".lebai.task.Task.endedAt: object expected");
                    message.endedAt = $root.google.protobuf.Timestamp.fromObject(object.endedAt, _depth + 1);
                }
                if (object.pauseAt != null) {
                    if (!$util.isObject(object.pauseAt))
                        throw $TypeError(".lebai.task.Task.pauseAt: object expected");
                    message.pauseAt = $root.google.protobuf.Timestamp.fromObject(object.pauseAt, _depth + 1);
                }
                if (object.prePause != null)
                    if ($Number(object.prePause) !== 0)
                        message.prePause = object.prePause >>> 0;
                if (object.kind !== 0 && (typeof object.kind !== "string" || $root.lebai.task.TaskKind[object.kind] !== 0))
                    switch (object.kind) {
                    case "LUA":
                    case 0:
                        message.kind = 0;
                        break;
                    case "APP":
                    case 10:
                        message.kind = 10;
                        break;
                    default:
                        if (typeof object.kind === "number" && (object.kind | 0) === object.kind)
                            message.kind = object.kind;
                    }
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.params) {
                    if (!$Array.isArray(object.params))
                        throw $TypeError(".lebai.task.Task.params: array expected");
                    message.params = $Array(object.params.length);
                    for (var i = 0; i < object.params.length; ++i)
                        message.params[i] = $String(object.params[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a Task message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.task.Task
             * @static
             * @param {lebai.task.Task} message Task
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Task.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.params = [];
                if (options.defaults) {
                    object.id = 0;
                    object.blockId = "";
                    object.eventId = 0;
                    object.state = options.enums === $String ? "NONE" : 0;
                    object.loopCount = 0;
                    object.loopTo = 0;
                    object.isParallel = false;
                    object.isSimu = false;
                    object.stdout = "";
                    object.startedAt = null;
                    object.endedAt = null;
                    object.pauseAt = null;
                    object.prePause = 0;
                    object.kind = options.enums === $String ? "LUA" : 0;
                    object.dir = "";
                    object.name = "";
                }
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    object.id = message.id;
                if (message.blockId != null && $Object.hasOwnProperty.call(message, "blockId"))
                    object.blockId = message.blockId;
                if (message.eventId != null && $Object.hasOwnProperty.call(message, "eventId"))
                    object.eventId = message.eventId;
                if (message.state != null && $Object.hasOwnProperty.call(message, "state"))
                    object.state = options.enums === $String ? $root.lebai.task.TaskState[message.state] === $undefined ? message.state : $root.lebai.task.TaskState[message.state] : message.state;
                if (message.loopCount != null && $Object.hasOwnProperty.call(message, "loopCount"))
                    object.loopCount = message.loopCount;
                if (message.loopTo != null && $Object.hasOwnProperty.call(message, "loopTo"))
                    object.loopTo = message.loopTo;
                if (message.isParallel != null && $Object.hasOwnProperty.call(message, "isParallel"))
                    object.isParallel = message.isParallel;
                if (message.isSimu != null && $Object.hasOwnProperty.call(message, "isSimu"))
                    object.isSimu = message.isSimu;
                if (message.stdout != null && $Object.hasOwnProperty.call(message, "stdout"))
                    object.stdout = message.stdout;
                if (message.startedAt != null && $Object.hasOwnProperty.call(message, "startedAt"))
                    object.startedAt = $root.google.protobuf.Timestamp.toObject(message.startedAt, options, _depth + 1);
                if (message.endedAt != null && $Object.hasOwnProperty.call(message, "endedAt"))
                    object.endedAt = $root.google.protobuf.Timestamp.toObject(message.endedAt, options, _depth + 1);
                if (message.pauseAt != null && $Object.hasOwnProperty.call(message, "pauseAt"))
                    object.pauseAt = $root.google.protobuf.Timestamp.toObject(message.pauseAt, options, _depth + 1);
                if (message.prePause != null && $Object.hasOwnProperty.call(message, "prePause"))
                    object.prePause = message.prePause;
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    object.kind = options.enums === $String ? $root.lebai.task.TaskKind[message.kind] === $undefined ? message.kind : $root.lebai.task.TaskKind[message.kind] : message.kind;
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.params && message.params.length) {
                    object.params = $Array(message.params.length);
                    for (var j = 0; j < message.params.length; ++j)
                        object.params[j] = message.params[j];
                }
                return object;
            };

            /**
             * Converts this Task to JSON.
             * @function toJSON
             * @memberof lebai.task.Task
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Task.prototype.toJSON = function() {
                return Task.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Task
             * @function getTypeUrl
             * @memberof lebai.task.Task
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Task.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.task.Task";
            };

            return Task;
        })();

        task.TaskIds = (function() {

            /**
             * Properties of a TaskIds.
             * @typedef {Object} lebai.task.TaskIds.$Properties
             * @property {Array.<number>|null} [ids] TaskIds ids
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a TaskIds.
             * @memberof lebai.task
             * @interface ITaskIds
             * @augments lebai.task.TaskIds.$Properties
             * @deprecated Use lebai.task.TaskIds.$Properties instead.
             */

            /**
             * Shape of a TaskIds.
             * @typedef {lebai.task.TaskIds.$Properties} lebai.task.TaskIds.$Shape
             */

            /**
             * Constructs a new TaskIds.
             * @memberof lebai.task
             * @classdesc Represents a TaskIds.
             * @constructor
             * @param {lebai.task.TaskIds.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var TaskIds = function (properties) {
                this.ids = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * TaskIds ids.
             * @member {Array.<number>} ids
             * @memberof lebai.task.TaskIds
             * @instance
             */
            TaskIds.prototype.ids = $util.emptyArray;

            /**
             * Creates a new TaskIds instance using the specified properties.
             * @function create
             * @memberof lebai.task.TaskIds
             * @static
             * @param {lebai.task.TaskIds.$Properties=} [properties] Properties to set
             * @returns {lebai.task.TaskIds} TaskIds instance
             * @type {{
             *   (properties: lebai.task.TaskIds.$Shape): lebai.task.TaskIds & lebai.task.TaskIds.$Shape;
             *   (properties?: lebai.task.TaskIds.$Properties): lebai.task.TaskIds;
             * }}
             */
            TaskIds.create = function(properties) {
                return new TaskIds(properties);
            };

            /**
             * Encodes the specified TaskIds message. Does not implicitly {@link lebai.task.TaskIds.verify|verify} messages.
             * @function encode
             * @memberof lebai.task.TaskIds
             * @static
             * @param {lebai.task.TaskIds.$Properties} message TaskIds message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TaskIds.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.ids != null && message.ids.length)
                    writer.uint32(/* id 1, wireType 2 =*/10).uint32s(message.ids);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified TaskIds message, length delimited. Does not implicitly {@link lebai.task.TaskIds.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.task.TaskIds
             * @static
             * @param {lebai.task.TaskIds.$Properties} message TaskIds message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TaskIds.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a TaskIds message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.task.TaskIds
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.task.TaskIds & lebai.task.TaskIds.$Shape} TaskIds
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TaskIds.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.task.TaskIds();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.ids && message.ids.length))
                                    message.ids = [];
                                reader.uint32s(message.ids);
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.ids && message.ids.length))
                                message.ids = [];
                            message.ids.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a TaskIds message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.task.TaskIds
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.task.TaskIds & lebai.task.TaskIds.$Shape} TaskIds
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TaskIds.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a TaskIds message.
             * @function verify
             * @memberof lebai.task.TaskIds
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            TaskIds.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.ids != null && $Object.hasOwnProperty.call(message, "ids")) {
                    if (!$Array.isArray(message.ids))
                        return "ids: array expected";
                    for (var i = 0; i < message.ids.length; ++i)
                        if (!$util.isInteger(message.ids[i]))
                            return "ids: integer[] expected";
                }
                return null;
            };

            /**
             * Creates a TaskIds message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.task.TaskIds
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.task.TaskIds} TaskIds
             */
            TaskIds.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.task.TaskIds)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.task.TaskIds: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.task.TaskIds();
                if (object.ids) {
                    if (!$Array.isArray(object.ids))
                        throw $TypeError(".lebai.task.TaskIds.ids: array expected");
                    message.ids = $Array(object.ids.length);
                    for (var i = 0; i < object.ids.length; ++i)
                        message.ids[i] = object.ids[i] >>> 0;
                }
                return message;
            };

            /**
             * Creates a plain object from a TaskIds message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.task.TaskIds
             * @static
             * @param {lebai.task.TaskIds} message TaskIds
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            TaskIds.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.ids = [];
                if (message.ids && message.ids.length) {
                    object.ids = $Array(message.ids.length);
                    for (var j = 0; j < message.ids.length; ++j)
                        object.ids[j] = message.ids[j];
                }
                return object;
            };

            /**
             * Converts this TaskIds to JSON.
             * @function toJSON
             * @memberof lebai.task.TaskIds
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            TaskIds.prototype.toJSON = function() {
                return TaskIds.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for TaskIds
             * @function getTypeUrl
             * @memberof lebai.task.TaskIds
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            TaskIds.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.task.TaskIds";
            };

            return TaskIds;
        })();

        task.Tasks = (function() {

            /**
             * Properties of a Tasks.
             * @typedef {Object} lebai.task.Tasks.$Properties
             * @property {Array.<lebai.task.Task.$Properties>|null} [tasks] Tasks tasks
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Tasks.
             * @memberof lebai.task
             * @interface ITasks
             * @augments lebai.task.Tasks.$Properties
             * @deprecated Use lebai.task.Tasks.$Properties instead.
             */

            /**
             * Shape of a Tasks.
             * @typedef {lebai.task.Tasks.$Properties} lebai.task.Tasks.$Shape
             */

            /**
             * Constructs a new Tasks.
             * @memberof lebai.task
             * @classdesc Represents a Tasks.
             * @constructor
             * @param {lebai.task.Tasks.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Tasks = function (properties) {
                this.tasks = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Tasks tasks.
             * @member {Array.<lebai.task.Task.$Properties>} tasks
             * @memberof lebai.task.Tasks
             * @instance
             */
            Tasks.prototype.tasks = $util.emptyArray;

            /**
             * Creates a new Tasks instance using the specified properties.
             * @function create
             * @memberof lebai.task.Tasks
             * @static
             * @param {lebai.task.Tasks.$Properties=} [properties] Properties to set
             * @returns {lebai.task.Tasks} Tasks instance
             * @type {{
             *   (properties: lebai.task.Tasks.$Shape): lebai.task.Tasks & lebai.task.Tasks.$Shape;
             *   (properties?: lebai.task.Tasks.$Properties): lebai.task.Tasks;
             * }}
             */
            Tasks.create = function(properties) {
                return new Tasks(properties);
            };

            /**
             * Encodes the specified Tasks message. Does not implicitly {@link lebai.task.Tasks.verify|verify} messages.
             * @function encode
             * @memberof lebai.task.Tasks
             * @static
             * @param {lebai.task.Tasks.$Properties} message Tasks message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Tasks.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.tasks != null && message.tasks.length)
                    for (var i = 0; i < message.tasks.length; ++i)
                        $root.lebai.task.Task.encode(message.tasks[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Tasks message, length delimited. Does not implicitly {@link lebai.task.Tasks.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.task.Tasks
             * @static
             * @param {lebai.task.Tasks.$Properties} message Tasks message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Tasks.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Tasks message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.task.Tasks
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.task.Tasks & lebai.task.Tasks.$Shape} Tasks
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Tasks.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.task.Tasks();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.tasks && message.tasks.length))
                                message.tasks = [];
                            message.tasks.push($root.lebai.task.Task.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Tasks message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.task.Tasks
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.task.Tasks & lebai.task.Tasks.$Shape} Tasks
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Tasks.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Tasks message.
             * @function verify
             * @memberof lebai.task.Tasks
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Tasks.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.tasks != null && $Object.hasOwnProperty.call(message, "tasks")) {
                    if (!$Array.isArray(message.tasks))
                        return "tasks: array expected";
                    for (var i = 0; i < message.tasks.length; ++i) {
                        var error = $root.lebai.task.Task.verify(message.tasks[i], _depth + 1);
                        if (error)
                            return "tasks." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Tasks message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.task.Tasks
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.task.Tasks} Tasks
             */
            Tasks.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.task.Tasks)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.task.Tasks: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.task.Tasks();
                if (object.tasks) {
                    if (!$Array.isArray(object.tasks))
                        throw $TypeError(".lebai.task.Tasks.tasks: array expected");
                    message.tasks = $Array(object.tasks.length);
                    for (var i = 0; i < object.tasks.length; ++i) {
                        if (!$util.isObject(object.tasks[i]))
                            throw $TypeError(".lebai.task.Tasks.tasks: object expected");
                        message.tasks[i] = $root.lebai.task.Task.fromObject(object.tasks[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Tasks message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.task.Tasks
             * @static
             * @param {lebai.task.Tasks} message Tasks
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Tasks.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.tasks = [];
                if (message.tasks && message.tasks.length) {
                    object.tasks = $Array(message.tasks.length);
                    for (var j = 0; j < message.tasks.length; ++j)
                        object.tasks[j] = $root.lebai.task.Task.toObject(message.tasks[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this Tasks to JSON.
             * @function toJSON
             * @memberof lebai.task.Tasks
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Tasks.prototype.toJSON = function() {
                return Tasks.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Tasks
             * @function getTypeUrl
             * @memberof lebai.task.Tasks
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Tasks.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.task.Tasks";
            };

            return Tasks;
        })();

        task.TaskStdout = (function() {

            /**
             * Properties of a TaskStdout.
             * @typedef {Object} lebai.task.TaskStdout.$Properties
             * @property {number|null} [id] TaskStdout id
             * @property {boolean|null} [done] TaskStdout done
             * @property {string|null} [stdout] TaskStdout stdout
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a TaskStdout.
             * @memberof lebai.task
             * @interface ITaskStdout
             * @augments lebai.task.TaskStdout.$Properties
             * @deprecated Use lebai.task.TaskStdout.$Properties instead.
             */

            /**
             * Shape of a TaskStdout.
             * @typedef {lebai.task.TaskStdout.$Properties} lebai.task.TaskStdout.$Shape
             */

            /**
             * Constructs a new TaskStdout.
             * @memberof lebai.task
             * @classdesc Represents a TaskStdout.
             * @constructor
             * @param {lebai.task.TaskStdout.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var TaskStdout = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * TaskStdout id.
             * @member {number} id
             * @memberof lebai.task.TaskStdout
             * @instance
             */
            TaskStdout.prototype.id = 0;

            /**
             * TaskStdout done.
             * @member {boolean} done
             * @memberof lebai.task.TaskStdout
             * @instance
             */
            TaskStdout.prototype.done = false;

            /**
             * TaskStdout stdout.
             * @member {string} stdout
             * @memberof lebai.task.TaskStdout
             * @instance
             */
            TaskStdout.prototype.stdout = "";

            /**
             * Creates a new TaskStdout instance using the specified properties.
             * @function create
             * @memberof lebai.task.TaskStdout
             * @static
             * @param {lebai.task.TaskStdout.$Properties=} [properties] Properties to set
             * @returns {lebai.task.TaskStdout} TaskStdout instance
             * @type {{
             *   (properties: lebai.task.TaskStdout.$Shape): lebai.task.TaskStdout & lebai.task.TaskStdout.$Shape;
             *   (properties?: lebai.task.TaskStdout.$Properties): lebai.task.TaskStdout;
             * }}
             */
            TaskStdout.create = function(properties) {
                return new TaskStdout(properties);
            };

            /**
             * Encodes the specified TaskStdout message. Does not implicitly {@link lebai.task.TaskStdout.verify|verify} messages.
             * @function encode
             * @memberof lebai.task.TaskStdout
             * @static
             * @param {lebai.task.TaskStdout.$Properties} message TaskStdout message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TaskStdout.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.id);
                if (message.done != null && $Object.hasOwnProperty.call(message, "done") && message.done !== false)
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.done);
                if (message.stdout != null && $Object.hasOwnProperty.call(message, "stdout") && message.stdout !== "")
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.stdout);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified TaskStdout message, length delimited. Does not implicitly {@link lebai.task.TaskStdout.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.task.TaskStdout
             * @static
             * @param {lebai.task.TaskStdout.$Properties} message TaskStdout message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TaskStdout.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a TaskStdout message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.task.TaskStdout
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.task.TaskStdout & lebai.task.TaskStdout.$Shape} TaskStdout
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TaskStdout.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.task.TaskStdout();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.done = value;
                            else
                                delete message.done;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.stdout = value;
                            else
                                delete message.stdout;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a TaskStdout message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.task.TaskStdout
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.task.TaskStdout & lebai.task.TaskStdout.$Shape} TaskStdout
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TaskStdout.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a TaskStdout message.
             * @function verify
             * @memberof lebai.task.TaskStdout
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            TaskStdout.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.done != null && $Object.hasOwnProperty.call(message, "done"))
                    if (typeof message.done !== "boolean")
                        return "done: boolean expected";
                if (message.stdout != null && $Object.hasOwnProperty.call(message, "stdout"))
                    if (!$util.isString(message.stdout))
                        return "stdout: string expected";
                return null;
            };

            /**
             * Creates a TaskStdout message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.task.TaskStdout
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.task.TaskStdout} TaskStdout
             */
            TaskStdout.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.task.TaskStdout)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.task.TaskStdout: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.task.TaskStdout();
                if (object.id != null)
                    if ($Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.done != null)
                    if (object.done)
                        message.done = $Boolean(object.done);
                if (object.stdout != null)
                    if (typeof object.stdout !== "string" || object.stdout.length)
                        message.stdout = $String(object.stdout);
                return message;
            };

            /**
             * Creates a plain object from a TaskStdout message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.task.TaskStdout
             * @static
             * @param {lebai.task.TaskStdout} message TaskStdout
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            TaskStdout.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.done = false;
                    object.stdout = "";
                }
                if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                    object.id = message.id;
                if (message.done != null && $Object.hasOwnProperty.call(message, "done"))
                    object.done = message.done;
                if (message.stdout != null && $Object.hasOwnProperty.call(message, "stdout"))
                    object.stdout = message.stdout;
                return object;
            };

            /**
             * Converts this TaskStdout to JSON.
             * @function toJSON
             * @memberof lebai.task.TaskStdout
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            TaskStdout.prototype.toJSON = function() {
                return TaskStdout.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for TaskStdout
             * @function getTypeUrl
             * @memberof lebai.task.TaskStdout
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            TaskStdout.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.task.TaskStdout";
            };

            return TaskStdout;
        })();

        task.StartTaskRequest = (function() {

            /**
             * Properties of a StartTaskRequest.
             * @typedef {Object} lebai.task.StartTaskRequest.$Properties
             * @property {string|null} [name] StartTaskRequest name
             * @property {boolean|null} [isParallel] StartTaskRequest isParallel
             * @property {number|null} [loopTo] StartTaskRequest loopTo
             * @property {string|null} [dir] StartTaskRequest dir
             * @property {lebai.task.TaskKind|null} [kind] StartTaskRequest kind
             * @property {Array.<string>|null} [params] StartTaskRequest params
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a StartTaskRequest.
             * @memberof lebai.task
             * @interface IStartTaskRequest
             * @augments lebai.task.StartTaskRequest.$Properties
             * @deprecated Use lebai.task.StartTaskRequest.$Properties instead.
             */

            /**
             * Shape of a StartTaskRequest.
             * @typedef {lebai.task.StartTaskRequest.$Properties} lebai.task.StartTaskRequest.$Shape
             */

            /**
             * Constructs a new StartTaskRequest.
             * @memberof lebai.task
             * @classdesc Represents a StartTaskRequest.
             * @constructor
             * @param {lebai.task.StartTaskRequest.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var StartTaskRequest = function (properties) {
                this.params = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * StartTaskRequest name.
             * @member {string} name
             * @memberof lebai.task.StartTaskRequest
             * @instance
             */
            StartTaskRequest.prototype.name = "";

            /**
             * StartTaskRequest isParallel.
             * @member {boolean} isParallel
             * @memberof lebai.task.StartTaskRequest
             * @instance
             */
            StartTaskRequest.prototype.isParallel = false;

            /**
             * StartTaskRequest loopTo.
             * @member {number} loopTo
             * @memberof lebai.task.StartTaskRequest
             * @instance
             */
            StartTaskRequest.prototype.loopTo = 0;

            /**
             * StartTaskRequest dir.
             * @member {string} dir
             * @memberof lebai.task.StartTaskRequest
             * @instance
             */
            StartTaskRequest.prototype.dir = "";

            /**
             * StartTaskRequest kind.
             * @member {lebai.task.TaskKind} kind
             * @memberof lebai.task.StartTaskRequest
             * @instance
             */
            StartTaskRequest.prototype.kind = 0;

            /**
             * StartTaskRequest params.
             * @member {Array.<string>} params
             * @memberof lebai.task.StartTaskRequest
             * @instance
             */
            StartTaskRequest.prototype.params = $util.emptyArray;

            /**
             * Creates a new StartTaskRequest instance using the specified properties.
             * @function create
             * @memberof lebai.task.StartTaskRequest
             * @static
             * @param {lebai.task.StartTaskRequest.$Properties=} [properties] Properties to set
             * @returns {lebai.task.StartTaskRequest} StartTaskRequest instance
             * @type {{
             *   (properties: lebai.task.StartTaskRequest.$Shape): lebai.task.StartTaskRequest & lebai.task.StartTaskRequest.$Shape;
             *   (properties?: lebai.task.StartTaskRequest.$Properties): lebai.task.StartTaskRequest;
             * }}
             */
            StartTaskRequest.create = function(properties) {
                return new StartTaskRequest(properties);
            };

            /**
             * Encodes the specified StartTaskRequest message. Does not implicitly {@link lebai.task.StartTaskRequest.verify|verify} messages.
             * @function encode
             * @memberof lebai.task.StartTaskRequest
             * @static
             * @param {lebai.task.StartTaskRequest.$Properties} message StartTaskRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StartTaskRequest.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.isParallel != null && $Object.hasOwnProperty.call(message, "isParallel") && message.isParallel !== false)
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isParallel);
                if (message.loopTo != null && $Object.hasOwnProperty.call(message, "loopTo") && message.loopTo !== 0)
                    writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.loopTo);
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.dir);
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind") && message.kind !== 0)
                    writer.uint32(/* id 12, wireType 0 =*/96).int32(message.kind);
                if (message.params != null && message.params.length)
                    for (var i = 0; i < message.params.length; ++i)
                        writer.uint32(/* id 22, wireType 2 =*/178).string(message.params[i]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StartTaskRequest message, length delimited. Does not implicitly {@link lebai.task.StartTaskRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.task.StartTaskRequest
             * @static
             * @param {lebai.task.StartTaskRequest.$Properties} message StartTaskRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StartTaskRequest.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a StartTaskRequest message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.task.StartTaskRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.task.StartTaskRequest & lebai.task.StartTaskRequest.$Shape} StartTaskRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StartTaskRequest.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.task.StartTaskRequest();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.isParallel = value;
                            else
                                delete message.isParallel;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.loopTo = value;
                            else
                                delete message.loopTo;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.dir = value;
                            else
                                delete message.dir;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.kind = value;
                            else
                                delete message.kind;
                            continue;
                        }
                    case 22: {
                            if (wireType !== 2)
                                break;
                            if (!(message.params && message.params.length))
                                message.params = [];
                            message.params.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a StartTaskRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.task.StartTaskRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.task.StartTaskRequest & lebai.task.StartTaskRequest.$Shape} StartTaskRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StartTaskRequest.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StartTaskRequest message.
             * @function verify
             * @memberof lebai.task.StartTaskRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StartTaskRequest.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.isParallel != null && $Object.hasOwnProperty.call(message, "isParallel"))
                    if (typeof message.isParallel !== "boolean")
                        return "isParallel: boolean expected";
                if (message.loopTo != null && $Object.hasOwnProperty.call(message, "loopTo"))
                    if (!$util.isInteger(message.loopTo))
                        return "loopTo: integer expected";
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    if (!$util.isString(message.dir))
                        return "dir: string expected";
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    if (typeof message.kind !== "number" || (message.kind | 0) !== message.kind)
                        return "kind: enum value expected";
                if (message.params != null && $Object.hasOwnProperty.call(message, "params")) {
                    if (!$Array.isArray(message.params))
                        return "params: array expected";
                    for (var i = 0; i < message.params.length; ++i)
                        if (!$util.isString(message.params[i]))
                            return "params: string[] expected";
                }
                return null;
            };

            /**
             * Creates a StartTaskRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.task.StartTaskRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.task.StartTaskRequest} StartTaskRequest
             */
            StartTaskRequest.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.task.StartTaskRequest)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.task.StartTaskRequest: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.task.StartTaskRequest();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = $String(object.name);
                if (object.isParallel != null)
                    if (object.isParallel)
                        message.isParallel = $Boolean(object.isParallel);
                if (object.loopTo != null)
                    if ($Number(object.loopTo) !== 0)
                        message.loopTo = object.loopTo >>> 0;
                if (object.dir != null)
                    if (typeof object.dir !== "string" || object.dir.length)
                        message.dir = $String(object.dir);
                if (object.kind !== 0 && (typeof object.kind !== "string" || $root.lebai.task.TaskKind[object.kind] !== 0))
                    switch (object.kind) {
                    case "LUA":
                    case 0:
                        message.kind = 0;
                        break;
                    case "APP":
                    case 10:
                        message.kind = 10;
                        break;
                    default:
                        if (typeof object.kind === "number" && (object.kind | 0) === object.kind)
                            message.kind = object.kind;
                    }
                if (object.params) {
                    if (!$Array.isArray(object.params))
                        throw $TypeError(".lebai.task.StartTaskRequest.params: array expected");
                    message.params = $Array(object.params.length);
                    for (var i = 0; i < object.params.length; ++i)
                        message.params[i] = $String(object.params[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a StartTaskRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.task.StartTaskRequest
             * @static
             * @param {lebai.task.StartTaskRequest} message StartTaskRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StartTaskRequest.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.params = [];
                if (options.defaults) {
                    object.name = "";
                    object.isParallel = false;
                    object.loopTo = 0;
                    object.dir = "";
                    object.kind = options.enums === $String ? "LUA" : 0;
                }
                if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.isParallel != null && $Object.hasOwnProperty.call(message, "isParallel"))
                    object.isParallel = message.isParallel;
                if (message.loopTo != null && $Object.hasOwnProperty.call(message, "loopTo"))
                    object.loopTo = message.loopTo;
                if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                    object.dir = message.dir;
                if (message.kind != null && $Object.hasOwnProperty.call(message, "kind"))
                    object.kind = options.enums === $String ? $root.lebai.task.TaskKind[message.kind] === $undefined ? message.kind : $root.lebai.task.TaskKind[message.kind] : message.kind;
                if (message.params && message.params.length) {
                    object.params = $Array(message.params.length);
                    for (var j = 0; j < message.params.length; ++j)
                        object.params[j] = message.params[j];
                }
                return object;
            };

            /**
             * Converts this StartTaskRequest to JSON.
             * @function toJSON
             * @memberof lebai.task.StartTaskRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StartTaskRequest.prototype.toJSON = function() {
                return StartTaskRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for StartTaskRequest
             * @function getTypeUrl
             * @memberof lebai.task.StartTaskRequest
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            StartTaskRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.task.StartTaskRequest";
            };

            return StartTaskRequest;
        })();

        task.TaskService = (function() {

            /**
             * Constructs a new TaskService service.
             * @memberof lebai.task
             * @classdesc Represents a TaskService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var TaskService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(TaskService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: TaskService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new TaskService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.task.TaskService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {TaskService} RPC service. Useful where requests and/or responses are streamed.
             */
            TaskService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.task.TaskService#loadTask}.
             * @memberof lebai.task.TaskService
             * @typedef LoadTaskCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.task.Task} [response] Task
             */

            /**
             * Calls LoadTask.
             * @memberof lebai.task.TaskService
             * @typedef LoadTask
             * @type {{
             *   (request: lebai.task.ITaskIndex, callback: lebai.task.TaskService.LoadTaskCallback): void;
             *   (request: lebai.task.ITaskIndex): Promise<lebai.task.Task>;
             *   readonly name: "LoadTask";
             *   readonly path: "/lebai.task.TaskService/LoadTask";
             *   readonly requestType: "TaskIndex";
             *   readonly responseType: "Task";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadTask.
             * @name lebai.task.TaskService#loadTask
             * @type {lebai.task.TaskService.LoadTask}
             */
            $Object.defineProperties(TaskService.prototype.loadTask = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, TaskService.prototype.loadTask, $root.lebai.task.TaskIndex, $root.lebai.task.Task, request, callback);
            }, {
                name: { value: "LoadTask" },
                path: { value: "/lebai.task.TaskService/LoadTask" },
                requestType: { value: "TaskIndex" },
                responseType: { value: "Task" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.task.TaskService#loadTaskList}.
             * @memberof lebai.task.TaskService
             * @typedef LoadTaskListCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.task.TaskIds} [response] TaskIds
             */

            /**
             * Calls LoadTaskList.
             * @memberof lebai.task.TaskService
             * @typedef LoadTaskList
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.task.TaskService.LoadTaskListCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.task.TaskIds>;
             *   readonly name: "LoadTaskList";
             *   readonly path: "/lebai.task.TaskService/LoadTaskList";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "TaskIds";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadTaskList.
             * @name lebai.task.TaskService#loadTaskList
             * @type {lebai.task.TaskService.LoadTaskList}
             */
            $Object.defineProperties(TaskService.prototype.loadTaskList = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, TaskService.prototype.loadTaskList, $root.google.protobuf.Empty, $root.lebai.task.TaskIds, request, callback);
            }, {
                name: { value: "LoadTaskList" },
                path: { value: "/lebai.task.TaskService/LoadTaskList" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "TaskIds" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.task.TaskService#loadRunningTasks}.
             * @memberof lebai.task.TaskService
             * @typedef LoadRunningTasksCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.task.Tasks} [response] Tasks
             */

            /**
             * Calls LoadRunningTasks.
             * @memberof lebai.task.TaskService
             * @typedef LoadRunningTasks
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.task.TaskService.LoadRunningTasksCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.task.Tasks>;
             *   readonly name: "LoadRunningTasks";
             *   readonly path: "/lebai.task.TaskService/LoadRunningTasks";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "Tasks";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls LoadRunningTasks.
             * @name lebai.task.TaskService#loadRunningTasks
             * @type {lebai.task.TaskService.LoadRunningTasks}
             */
            $Object.defineProperties(TaskService.prototype.loadRunningTasks = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, TaskService.prototype.loadRunningTasks, $root.google.protobuf.Empty, $root.lebai.task.Tasks, request, callback);
            }, {
                name: { value: "LoadRunningTasks" },
                path: { value: "/lebai.task.TaskService/LoadRunningTasks" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "Tasks" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.task.TaskService#startTask}.
             * @memberof lebai.task.TaskService
             * @typedef StartTaskCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.task.TaskIndex} [response] TaskIndex
             */

            /**
             * Calls StartTask.
             * @memberof lebai.task.TaskService
             * @typedef StartTask
             * @type {{
             *   (request: lebai.task.IStartTaskRequest, callback: lebai.task.TaskService.StartTaskCallback): void;
             *   (request: lebai.task.IStartTaskRequest): Promise<lebai.task.TaskIndex>;
             *   readonly name: "StartTask";
             *   readonly path: "/lebai.task.TaskService/StartTask";
             *   readonly requestType: "StartTaskRequest";
             *   readonly responseType: "TaskIndex";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls StartTask.
             * @name lebai.task.TaskService#startTask
             * @type {lebai.task.TaskService.StartTask}
             */
            $Object.defineProperties(TaskService.prototype.startTask = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, TaskService.prototype.startTask, $root.lebai.task.StartTaskRequest, $root.lebai.task.TaskIndex, request, callback);
            }, {
                name: { value: "StartTask" },
                path: { value: "/lebai.task.TaskService/StartTask" },
                requestType: { value: "StartTaskRequest" },
                responseType: { value: "TaskIndex" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.task.TaskService#getTaskStdout}.
             * @memberof lebai.task.TaskService
             * @typedef GetTaskStdoutCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.task.TaskStdout} [response] TaskStdout
             */

            /**
             * Calls GetTaskStdout.
             * @memberof lebai.task.TaskService
             * @typedef GetTaskStdout
             * @type {{
             *   (request: lebai.task.ITaskIndex, callback: lebai.task.TaskService.GetTaskStdoutCallback): void;
             *   (request: lebai.task.ITaskIndex): Promise<lebai.task.TaskStdout>;
             *   readonly name: "GetTaskStdout";
             *   readonly path: "/lebai.task.TaskService/GetTaskStdout";
             *   readonly requestType: "TaskIndex";
             *   readonly responseType: "TaskStdout";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetTaskStdout.
             * @name lebai.task.TaskService#getTaskStdout
             * @type {lebai.task.TaskService.GetTaskStdout}
             */
            $Object.defineProperties(TaskService.prototype.getTaskStdout = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, TaskService.prototype.getTaskStdout, $root.lebai.task.TaskIndex, $root.lebai.task.TaskStdout, request, callback);
            }, {
                name: { value: "GetTaskStdout" },
                path: { value: "/lebai.task.TaskService/GetTaskStdout" },
                requestType: { value: "TaskIndex" },
                responseType: { value: "TaskStdout" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.task.TaskService#subTaskStdout}.
             * @memberof lebai.task.TaskService
             * @typedef SubTaskStdoutCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.task.TaskStdout} [response] TaskStdout
             */

            /**
             * Calls SubTaskStdout.
             * @memberof lebai.task.TaskService
             * @typedef SubTaskStdout
             * @type {{
             *   (request: lebai.task.ITaskIndex, callback: lebai.task.TaskService.SubTaskStdoutCallback): void;
             *   (request: lebai.task.ITaskIndex): Promise<lebai.task.TaskStdout>;
             *   readonly name: "SubTaskStdout";
             *   readonly path: "/lebai.task.TaskService/SubTaskStdout";
             *   readonly requestType: "TaskIndex";
             *   readonly responseType: "TaskStdout";
             *   readonly requestStream: undefined;
             *   readonly responseStream: true;
             * }}
             */

            /**
             * Calls SubTaskStdout.
             * @name lebai.task.TaskService#subTaskStdout
             * @type {lebai.task.TaskService.SubTaskStdout}
             */
            $Object.defineProperties(TaskService.prototype.subTaskStdout = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, TaskService.prototype.subTaskStdout, $root.lebai.task.TaskIndex, $root.lebai.task.TaskStdout, request, callback);
            }, {
                name: { value: "SubTaskStdout" },
                path: { value: "/lebai.task.TaskService/SubTaskStdout" },
                requestType: { value: "TaskIndex" },
                responseType: { value: "TaskStdout" },
                requestStream: { value: $undefined },
                responseStream: { value: true }
            });

            /**
             * Callback as used by {@link lebai.task.TaskService#waitTask}.
             * @memberof lebai.task.TaskService
             * @typedef WaitTaskCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.task.TaskStdout} [response] TaskStdout
             */

            /**
             * Calls WaitTask.
             * @memberof lebai.task.TaskService
             * @typedef WaitTask
             * @type {{
             *   (request: lebai.task.ITaskIndex, callback: lebai.task.TaskService.WaitTaskCallback): void;
             *   (request: lebai.task.ITaskIndex): Promise<lebai.task.TaskStdout>;
             *   readonly name: "WaitTask";
             *   readonly path: "/lebai.task.TaskService/WaitTask";
             *   readonly requestType: "TaskIndex";
             *   readonly responseType: "TaskStdout";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls WaitTask.
             * @name lebai.task.TaskService#waitTask
             * @type {lebai.task.TaskService.WaitTask}
             */
            $Object.defineProperties(TaskService.prototype.waitTask = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, TaskService.prototype.waitTask, $root.lebai.task.TaskIndex, $root.lebai.task.TaskStdout, request, callback);
            }, {
                name: { value: "WaitTask" },
                path: { value: "/lebai.task.TaskService/WaitTask" },
                requestType: { value: "TaskIndex" },
                responseType: { value: "TaskStdout" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.task.TaskService#pauseTask}.
             * @memberof lebai.task.TaskService
             * @typedef PauseTaskCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls PauseTask.
             * @memberof lebai.task.TaskService
             * @typedef PauseTask
             * @type {{
             *   (request: lebai.task.ITaskIndex, callback: lebai.task.TaskService.PauseTaskCallback): void;
             *   (request: lebai.task.ITaskIndex): Promise<google.protobuf.Empty>;
             *   readonly name: "PauseTask";
             *   readonly path: "/lebai.task.TaskService/PauseTask";
             *   readonly requestType: "TaskIndex";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls PauseTask.
             * @name lebai.task.TaskService#pauseTask
             * @type {lebai.task.TaskService.PauseTask}
             */
            $Object.defineProperties(TaskService.prototype.pauseTask = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, TaskService.prototype.pauseTask, $root.lebai.task.TaskIndex, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "PauseTask" },
                path: { value: "/lebai.task.TaskService/PauseTask" },
                requestType: { value: "TaskIndex" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.task.TaskService#resumeTask}.
             * @memberof lebai.task.TaskService
             * @typedef ResumeTaskCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls ResumeTask.
             * @memberof lebai.task.TaskService
             * @typedef ResumeTask
             * @type {{
             *   (request: lebai.task.ITaskIndex, callback: lebai.task.TaskService.ResumeTaskCallback): void;
             *   (request: lebai.task.ITaskIndex): Promise<google.protobuf.Empty>;
             *   readonly name: "ResumeTask";
             *   readonly path: "/lebai.task.TaskService/ResumeTask";
             *   readonly requestType: "TaskIndex";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls ResumeTask.
             * @name lebai.task.TaskService#resumeTask
             * @type {lebai.task.TaskService.ResumeTask}
             */
            $Object.defineProperties(TaskService.prototype.resumeTask = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, TaskService.prototype.resumeTask, $root.lebai.task.TaskIndex, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "ResumeTask" },
                path: { value: "/lebai.task.TaskService/ResumeTask" },
                requestType: { value: "TaskIndex" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.task.TaskService#cancelTask}.
             * @memberof lebai.task.TaskService
             * @typedef CancelTaskCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls CancelTask.
             * @memberof lebai.task.TaskService
             * @typedef CancelTask
             * @type {{
             *   (request: lebai.task.ITaskIndex, callback: lebai.task.TaskService.CancelTaskCallback): void;
             *   (request: lebai.task.ITaskIndex): Promise<google.protobuf.Empty>;
             *   readonly name: "CancelTask";
             *   readonly path: "/lebai.task.TaskService/CancelTask";
             *   readonly requestType: "TaskIndex";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls CancelTask.
             * @name lebai.task.TaskService#cancelTask
             * @type {lebai.task.TaskService.CancelTask}
             */
            $Object.defineProperties(TaskService.prototype.cancelTask = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, TaskService.prototype.cancelTask, $root.lebai.task.TaskIndex, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "CancelTask" },
                path: { value: "/lebai.task.TaskService/CancelTask" },
                requestType: { value: "TaskIndex" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return TaskService;
        })();

        return task;
    })();

    lebai.trigger = (function() {

        /**
         * Namespace trigger.
         * @memberof lebai
         * @namespace
         */
        var trigger = {};

        trigger.Condition = (function() {

            /**
             * Properties of a Condition.
             * @typedef {Object} lebai.trigger.Condition.$Properties
             * @property {Array.<lebai.io.ButtonIndex.$Properties>|null} [pressed] Condition pressed
             * @property {lebai.io.ButtonIndex.$Properties|null} [button] Condition button
             * @property {lebai.io.ButtonStatus.$Properties|null} [status] Condition status
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Condition.
             * @memberof lebai.trigger
             * @interface ICondition
             * @augments lebai.trigger.Condition.$Properties
             * @deprecated Use lebai.trigger.Condition.$Properties instead.
             */

            /**
             * Shape of a Condition.
             * @typedef {lebai.trigger.Condition.$Properties} lebai.trigger.Condition.$Shape
             */

            /**
             * Constructs a new Condition.
             * @memberof lebai.trigger
             * @classdesc Represents a Condition.
             * @constructor
             * @param {lebai.trigger.Condition.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Condition = function (properties) {
                this.pressed = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Condition pressed.
             * @member {Array.<lebai.io.ButtonIndex.$Properties>} pressed
             * @memberof lebai.trigger.Condition
             * @instance
             */
            Condition.prototype.pressed = $util.emptyArray;

            /**
             * Condition button.
             * @member {lebai.io.ButtonIndex.$Properties|null|undefined} button
             * @memberof lebai.trigger.Condition
             * @instance
             */
            Condition.prototype.button = null;

            /**
             * Condition status.
             * @member {lebai.io.ButtonStatus.$Properties|null|undefined} status
             * @memberof lebai.trigger.Condition
             * @instance
             */
            Condition.prototype.status = null;

            /**
             * Creates a new Condition instance using the specified properties.
             * @function create
             * @memberof lebai.trigger.Condition
             * @static
             * @param {lebai.trigger.Condition.$Properties=} [properties] Properties to set
             * @returns {lebai.trigger.Condition} Condition instance
             * @type {{
             *   (properties: lebai.trigger.Condition.$Shape): lebai.trigger.Condition & lebai.trigger.Condition.$Shape;
             *   (properties?: lebai.trigger.Condition.$Properties): lebai.trigger.Condition;
             * }}
             */
            Condition.create = function(properties) {
                return new Condition(properties);
            };

            /**
             * Encodes the specified Condition message. Does not implicitly {@link lebai.trigger.Condition.verify|verify} messages.
             * @function encode
             * @memberof lebai.trigger.Condition
             * @static
             * @param {lebai.trigger.Condition.$Properties} message Condition message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Condition.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.pressed != null && message.pressed.length)
                    for (var i = 0; i < message.pressed.length; ++i)
                        $root.lebai.io.ButtonIndex.encode(message.pressed[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.button != null && $Object.hasOwnProperty.call(message, "button"))
                    $root.lebai.io.ButtonIndex.encode(message.button, writer.uint32(/* id 11, wireType 2 =*/90).fork(), _depth + 1).ldelim();
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    $root.lebai.io.ButtonStatus.encode(message.status, writer.uint32(/* id 12, wireType 2 =*/98).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Condition message, length delimited. Does not implicitly {@link lebai.trigger.Condition.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.trigger.Condition
             * @static
             * @param {lebai.trigger.Condition.$Properties} message Condition message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Condition.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Condition message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.trigger.Condition
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.trigger.Condition & lebai.trigger.Condition.$Shape} Condition
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Condition.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.trigger.Condition();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.pressed && message.pressed.length))
                                message.pressed = [];
                            message.pressed.push($root.lebai.io.ButtonIndex.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            message.button = $root.lebai.io.ButtonIndex.decode(reader, reader.uint32(), $undefined, _depth + 1, message.button);
                            continue;
                        }
                    case 12: {
                            if (wireType !== 2)
                                break;
                            message.status = $root.lebai.io.ButtonStatus.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Condition message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.trigger.Condition
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.trigger.Condition & lebai.trigger.Condition.$Shape} Condition
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Condition.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Condition message.
             * @function verify
             * @memberof lebai.trigger.Condition
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Condition.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.pressed != null && $Object.hasOwnProperty.call(message, "pressed")) {
                    if (!$Array.isArray(message.pressed))
                        return "pressed: array expected";
                    for (var i = 0; i < message.pressed.length; ++i) {
                        var error = $root.lebai.io.ButtonIndex.verify(message.pressed[i], _depth + 1);
                        if (error)
                            return "pressed." + error;
                    }
                }
                if (message.button != null && $Object.hasOwnProperty.call(message, "button")) {
                    var error = $root.lebai.io.ButtonIndex.verify(message.button, _depth + 1);
                    if (error)
                        return "button." + error;
                }
                if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
                    var error = $root.lebai.io.ButtonStatus.verify(message.status, _depth + 1);
                    if (error)
                        return "status." + error;
                }
                return null;
            };

            /**
             * Creates a Condition message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.trigger.Condition
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.trigger.Condition} Condition
             */
            Condition.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.trigger.Condition)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.trigger.Condition: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.trigger.Condition();
                if (object.pressed) {
                    if (!$Array.isArray(object.pressed))
                        throw $TypeError(".lebai.trigger.Condition.pressed: array expected");
                    message.pressed = $Array(object.pressed.length);
                    for (var i = 0; i < object.pressed.length; ++i) {
                        if (!$util.isObject(object.pressed[i]))
                            throw $TypeError(".lebai.trigger.Condition.pressed: object expected");
                        message.pressed[i] = $root.lebai.io.ButtonIndex.fromObject(object.pressed[i], _depth + 1);
                    }
                }
                if (object.button != null) {
                    if (!$util.isObject(object.button))
                        throw $TypeError(".lebai.trigger.Condition.button: object expected");
                    message.button = $root.lebai.io.ButtonIndex.fromObject(object.button, _depth + 1);
                }
                if (object.status != null) {
                    if (!$util.isObject(object.status))
                        throw $TypeError(".lebai.trigger.Condition.status: object expected");
                    message.status = $root.lebai.io.ButtonStatus.fromObject(object.status, _depth + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Condition message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.trigger.Condition
             * @static
             * @param {lebai.trigger.Condition} message Condition
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Condition.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.pressed = [];
                if (options.defaults) {
                    object.button = null;
                    object.status = null;
                }
                if (message.pressed && message.pressed.length) {
                    object.pressed = $Array(message.pressed.length);
                    for (var j = 0; j < message.pressed.length; ++j)
                        object.pressed[j] = $root.lebai.io.ButtonIndex.toObject(message.pressed[j], options, _depth + 1);
                }
                if (message.button != null && $Object.hasOwnProperty.call(message, "button"))
                    object.button = $root.lebai.io.ButtonIndex.toObject(message.button, options, _depth + 1);
                if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                    object.status = $root.lebai.io.ButtonStatus.toObject(message.status, options, _depth + 1);
                return object;
            };

            /**
             * Converts this Condition to JSON.
             * @function toJSON
             * @memberof lebai.trigger.Condition
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Condition.prototype.toJSON = function() {
                return Condition.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Condition
             * @function getTypeUrl
             * @memberof lebai.trigger.Condition
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Condition.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.trigger.Condition";
            };

            return Condition;
        })();

        /**
         * Function enum.
         * @name lebai.trigger.Function
         * @enum {number}
         * @property {number} NONE=0 NONE value
         * @property {number} START_TEACH_MODE=1 START_TEACH_MODE value
         * @property {number} END_TEACH_MODE=2 END_TEACH_MODE value
         * @property {number} SET_ZERO=3 SET_ZERO value
         * @property {number} CHANGE_POWER=4 CHANGE_POWER value
         * @property {number} POWERDOWN=5 POWERDOWN value
         * @property {number} SHORT_TASK1=11 SHORT_TASK1 value
         * @property {number} SHORT_TASK2=12 SHORT_TASK2 value
         * @property {number} SHORT_TASK3=13 SHORT_TASK3 value
         * @property {number} SHORT_TASK4=14 SHORT_TASK4 value
         * @property {number} SHORT_TASK5=15 SHORT_TASK5 value
         * @property {number} SHORT_TASK6=16 SHORT_TASK6 value
         * @property {number} SHORT_TASK7=17 SHORT_TASK7 value
         * @property {number} SHORT_TASK8=18 SHORT_TASK8 value
         * @property {number} SHORT_TASK9=19 SHORT_TASK9 value
         */
        trigger.Function = (function() {
            var valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "START_TEACH_MODE"] = 1;
            values[valuesById[2] = "END_TEACH_MODE"] = 2;
            values[valuesById[3] = "SET_ZERO"] = 3;
            values[valuesById[4] = "CHANGE_POWER"] = 4;
            values[valuesById[5] = "POWERDOWN"] = 5;
            values[valuesById[11] = "SHORT_TASK1"] = 11;
            values[valuesById[12] = "SHORT_TASK2"] = 12;
            values[valuesById[13] = "SHORT_TASK3"] = 13;
            values[valuesById[14] = "SHORT_TASK4"] = 14;
            values[valuesById[15] = "SHORT_TASK5"] = 15;
            values[valuesById[16] = "SHORT_TASK6"] = 16;
            values[valuesById[17] = "SHORT_TASK7"] = 17;
            values[valuesById[18] = "SHORT_TASK8"] = 18;
            values[valuesById[19] = "SHORT_TASK9"] = 19;
            return values;
        })();

        trigger.Trigger = (function() {

            /**
             * Properties of a Trigger.
             * @typedef {Object} lebai.trigger.Trigger.$Properties
             * @property {lebai.trigger.Condition.$Properties|null} [condition] Trigger condition
             * @property {lebai.trigger.Function|null} ["function"] Trigger function
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Trigger.
             * @memberof lebai.trigger
             * @interface ITrigger
             * @augments lebai.trigger.Trigger.$Properties
             * @deprecated Use lebai.trigger.Trigger.$Properties instead.
             */

            /**
             * Shape of a Trigger.
             * @typedef {lebai.trigger.Trigger.$Properties} lebai.trigger.Trigger.$Shape
             */

            /**
             * Constructs a new Trigger.
             * @memberof lebai.trigger
             * @classdesc Represents a Trigger.
             * @constructor
             * @param {lebai.trigger.Trigger.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Trigger = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Trigger condition.
             * @member {lebai.trigger.Condition.$Properties|null|undefined} condition
             * @memberof lebai.trigger.Trigger
             * @instance
             */
            Trigger.prototype.condition = null;

            /**
             * Trigger function.
             * @member {lebai.trigger.Function} function
             * @memberof lebai.trigger.Trigger
             * @instance
             */
            Trigger.prototype["function"] = 0;

            /**
             * Creates a new Trigger instance using the specified properties.
             * @function create
             * @memberof lebai.trigger.Trigger
             * @static
             * @param {lebai.trigger.Trigger.$Properties=} [properties] Properties to set
             * @returns {lebai.trigger.Trigger} Trigger instance
             * @type {{
             *   (properties: lebai.trigger.Trigger.$Shape): lebai.trigger.Trigger & lebai.trigger.Trigger.$Shape;
             *   (properties?: lebai.trigger.Trigger.$Properties): lebai.trigger.Trigger;
             * }}
             */
            Trigger.create = function(properties) {
                return new Trigger(properties);
            };

            /**
             * Encodes the specified Trigger message. Does not implicitly {@link lebai.trigger.Trigger.verify|verify} messages.
             * @function encode
             * @memberof lebai.trigger.Trigger
             * @static
             * @param {lebai.trigger.Trigger.$Properties} message Trigger message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Trigger.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.condition != null && $Object.hasOwnProperty.call(message, "condition"))
                    $root.lebai.trigger.Condition.encode(message.condition, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message["function"] != null && $Object.hasOwnProperty.call(message, "function") && message["function"] !== 0)
                    writer.uint32(/* id 11, wireType 0 =*/88).int32(message["function"]);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Trigger message, length delimited. Does not implicitly {@link lebai.trigger.Trigger.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.trigger.Trigger
             * @static
             * @param {lebai.trigger.Trigger.$Properties} message Trigger message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Trigger.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Trigger message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.trigger.Trigger
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.trigger.Trigger & lebai.trigger.Trigger.$Shape} Trigger
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Trigger.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.trigger.Trigger();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.condition = $root.lebai.trigger.Condition.decode(reader, reader.uint32(), $undefined, _depth + 1, message.condition);
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message["function"] = value;
                            else
                                delete message["function"];
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Trigger message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.trigger.Trigger
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.trigger.Trigger & lebai.trigger.Trigger.$Shape} Trigger
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Trigger.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Trigger message.
             * @function verify
             * @memberof lebai.trigger.Trigger
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Trigger.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.condition != null && $Object.hasOwnProperty.call(message, "condition")) {
                    var error = $root.lebai.trigger.Condition.verify(message.condition, _depth + 1);
                    if (error)
                        return "condition." + error;
                }
                if (message["function"] != null && $Object.hasOwnProperty.call(message, "function"))
                    if (typeof message["function"] !== "number" || (message["function"] | 0) !== message["function"])
                        return "function: enum value expected";
                return null;
            };

            /**
             * Creates a Trigger message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.trigger.Trigger
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.trigger.Trigger} Trigger
             */
            Trigger.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.trigger.Trigger)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.trigger.Trigger: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.trigger.Trigger();
                if (object.condition != null) {
                    if (!$util.isObject(object.condition))
                        throw $TypeError(".lebai.trigger.Trigger.condition: object expected");
                    message.condition = $root.lebai.trigger.Condition.fromObject(object.condition, _depth + 1);
                }
                if (object["function"] !== 0 && (typeof object["function"] !== "string" || $root.lebai.trigger.Function[object["function"]] !== 0))
                    switch (object["function"]) {
                    case "NONE":
                    case 0:
                        message["function"] = 0;
                        break;
                    case "START_TEACH_MODE":
                    case 1:
                        message["function"] = 1;
                        break;
                    case "END_TEACH_MODE":
                    case 2:
                        message["function"] = 2;
                        break;
                    case "SET_ZERO":
                    case 3:
                        message["function"] = 3;
                        break;
                    case "CHANGE_POWER":
                    case 4:
                        message["function"] = 4;
                        break;
                    case "POWERDOWN":
                    case 5:
                        message["function"] = 5;
                        break;
                    case "SHORT_TASK1":
                    case 11:
                        message["function"] = 11;
                        break;
                    case "SHORT_TASK2":
                    case 12:
                        message["function"] = 12;
                        break;
                    case "SHORT_TASK3":
                    case 13:
                        message["function"] = 13;
                        break;
                    case "SHORT_TASK4":
                    case 14:
                        message["function"] = 14;
                        break;
                    case "SHORT_TASK5":
                    case 15:
                        message["function"] = 15;
                        break;
                    case "SHORT_TASK6":
                    case 16:
                        message["function"] = 16;
                        break;
                    case "SHORT_TASK7":
                    case 17:
                        message["function"] = 17;
                        break;
                    case "SHORT_TASK8":
                    case 18:
                        message["function"] = 18;
                        break;
                    case "SHORT_TASK9":
                    case 19:
                        message["function"] = 19;
                        break;
                    default:
                        if (typeof object["function"] === "number" && (object["function"] | 0) === object["function"])
                            message["function"] = object["function"];
                    }
                return message;
            };

            /**
             * Creates a plain object from a Trigger message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.trigger.Trigger
             * @static
             * @param {lebai.trigger.Trigger} message Trigger
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Trigger.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.condition = null;
                    object["function"] = options.enums === $String ? "NONE" : 0;
                }
                if (message.condition != null && $Object.hasOwnProperty.call(message, "condition"))
                    object.condition = $root.lebai.trigger.Condition.toObject(message.condition, options, _depth + 1);
                if (message["function"] != null && $Object.hasOwnProperty.call(message, "function"))
                    object["function"] = options.enums === $String ? $root.lebai.trigger.Function[message["function"]] === $undefined ? message["function"] : $root.lebai.trigger.Function[message["function"]] : message["function"];
                return object;
            };

            /**
             * Converts this Trigger to JSON.
             * @function toJSON
             * @memberof lebai.trigger.Trigger
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Trigger.prototype.toJSON = function() {
                return Trigger.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Trigger
             * @function getTypeUrl
             * @memberof lebai.trigger.Trigger
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Trigger.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.trigger.Trigger";
            };

            return Trigger;
        })();

        trigger.Triggers = (function() {

            /**
             * Properties of a Triggers.
             * @typedef {Object} lebai.trigger.Triggers.$Properties
             * @property {Array.<lebai.trigger.Trigger.$Properties>|null} [triggers] Triggers triggers
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Triggers.
             * @memberof lebai.trigger
             * @interface ITriggers
             * @augments lebai.trigger.Triggers.$Properties
             * @deprecated Use lebai.trigger.Triggers.$Properties instead.
             */

            /**
             * Shape of a Triggers.
             * @typedef {lebai.trigger.Triggers.$Properties} lebai.trigger.Triggers.$Shape
             */

            /**
             * Constructs a new Triggers.
             * @memberof lebai.trigger
             * @classdesc Represents a Triggers.
             * @constructor
             * @param {lebai.trigger.Triggers.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Triggers = function (properties) {
                this.triggers = [];
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Triggers triggers.
             * @member {Array.<lebai.trigger.Trigger.$Properties>} triggers
             * @memberof lebai.trigger.Triggers
             * @instance
             */
            Triggers.prototype.triggers = $util.emptyArray;

            /**
             * Creates a new Triggers instance using the specified properties.
             * @function create
             * @memberof lebai.trigger.Triggers
             * @static
             * @param {lebai.trigger.Triggers.$Properties=} [properties] Properties to set
             * @returns {lebai.trigger.Triggers} Triggers instance
             * @type {{
             *   (properties: lebai.trigger.Triggers.$Shape): lebai.trigger.Triggers & lebai.trigger.Triggers.$Shape;
             *   (properties?: lebai.trigger.Triggers.$Properties): lebai.trigger.Triggers;
             * }}
             */
            Triggers.create = function(properties) {
                return new Triggers(properties);
            };

            /**
             * Encodes the specified Triggers message. Does not implicitly {@link lebai.trigger.Triggers.verify|verify} messages.
             * @function encode
             * @memberof lebai.trigger.Triggers
             * @static
             * @param {lebai.trigger.Triggers.$Properties} message Triggers message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Triggers.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.triggers != null && message.triggers.length)
                    for (var i = 0; i < message.triggers.length; ++i)
                        $root.lebai.trigger.Trigger.encode(message.triggers[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Triggers message, length delimited. Does not implicitly {@link lebai.trigger.Triggers.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.trigger.Triggers
             * @static
             * @param {lebai.trigger.Triggers.$Properties} message Triggers message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Triggers.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Triggers message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.trigger.Triggers
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.trigger.Triggers & lebai.trigger.Triggers.$Shape} Triggers
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Triggers.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.trigger.Triggers();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.triggers && message.triggers.length))
                                message.triggers = [];
                            message.triggers.push($root.lebai.trigger.Trigger.decode(reader, reader.uint32(), $undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Triggers message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.trigger.Triggers
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.trigger.Triggers & lebai.trigger.Triggers.$Shape} Triggers
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Triggers.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Triggers message.
             * @function verify
             * @memberof lebai.trigger.Triggers
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Triggers.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.triggers != null && $Object.hasOwnProperty.call(message, "triggers")) {
                    if (!$Array.isArray(message.triggers))
                        return "triggers: array expected";
                    for (var i = 0; i < message.triggers.length; ++i) {
                        var error = $root.lebai.trigger.Trigger.verify(message.triggers[i], _depth + 1);
                        if (error)
                            return "triggers." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Triggers message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.trigger.Triggers
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.trigger.Triggers} Triggers
             */
            Triggers.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.trigger.Triggers)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.trigger.Triggers: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.trigger.Triggers();
                if (object.triggers) {
                    if (!$Array.isArray(object.triggers))
                        throw $TypeError(".lebai.trigger.Triggers.triggers: array expected");
                    message.triggers = $Array(object.triggers.length);
                    for (var i = 0; i < object.triggers.length; ++i) {
                        if (!$util.isObject(object.triggers[i]))
                            throw $TypeError(".lebai.trigger.Triggers.triggers: object expected");
                        message.triggers[i] = $root.lebai.trigger.Trigger.fromObject(object.triggers[i], _depth + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Triggers message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.trigger.Triggers
             * @static
             * @param {lebai.trigger.Triggers} message Triggers
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Triggers.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.triggers = [];
                if (message.triggers && message.triggers.length) {
                    object.triggers = $Array(message.triggers.length);
                    for (var j = 0; j < message.triggers.length; ++j)
                        object.triggers[j] = $root.lebai.trigger.Trigger.toObject(message.triggers[j], options, _depth + 1);
                }
                return object;
            };

            /**
             * Converts this Triggers to JSON.
             * @function toJSON
             * @memberof lebai.trigger.Triggers
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Triggers.prototype.toJSON = function() {
                return Triggers.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Triggers
             * @function getTypeUrl
             * @memberof lebai.trigger.Triggers
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Triggers.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.trigger.Triggers";
            };

            return Triggers;
        })();

        trigger.TriggerService = (function() {

            /**
             * Constructs a new TriggerService service.
             * @memberof lebai.trigger
             * @classdesc Represents a TriggerService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var TriggerService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(TriggerService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: TriggerService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new TriggerService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.trigger.TriggerService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {TriggerService} RPC service. Useful where requests and/or responses are streamed.
             */
            TriggerService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.trigger.TriggerService#setTrigger}.
             * @memberof lebai.trigger.TriggerService
             * @typedef SetTriggerCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls SetTrigger.
             * @memberof lebai.trigger.TriggerService
             * @typedef SetTrigger
             * @type {{
             *   (request: lebai.trigger.ITrigger, callback: lebai.trigger.TriggerService.SetTriggerCallback): void;
             *   (request: lebai.trigger.ITrigger): Promise<google.protobuf.Empty>;
             *   readonly name: "SetTrigger";
             *   readonly path: "/lebai.trigger.TriggerService/SetTrigger";
             *   readonly requestType: "Trigger";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls SetTrigger.
             * @name lebai.trigger.TriggerService#setTrigger
             * @type {lebai.trigger.TriggerService.SetTrigger}
             */
            $Object.defineProperties(TriggerService.prototype.setTrigger = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, TriggerService.prototype.setTrigger, $root.lebai.trigger.Trigger, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "SetTrigger" },
                path: { value: "/lebai.trigger.TriggerService/SetTrigger" },
                requestType: { value: "Trigger" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.trigger.TriggerService#getTriggers}.
             * @memberof lebai.trigger.TriggerService
             * @typedef GetTriggersCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.trigger.Triggers} [response] Triggers
             */

            /**
             * Calls GetTriggers.
             * @memberof lebai.trigger.TriggerService
             * @typedef GetTriggers
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.trigger.TriggerService.GetTriggersCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.trigger.Triggers>;
             *   readonly name: "GetTriggers";
             *   readonly path: "/lebai.trigger.TriggerService/GetTriggers";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "Triggers";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetTriggers.
             * @name lebai.trigger.TriggerService#getTriggers
             * @type {lebai.trigger.TriggerService.GetTriggers}
             */
            $Object.defineProperties(TriggerService.prototype.getTriggers = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, TriggerService.prototype.getTriggers, $root.google.protobuf.Empty, $root.lebai.trigger.Triggers, request, callback);
            }, {
                name: { value: "GetTriggers" },
                path: { value: "/lebai.trigger.TriggerService/GetTriggers" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "Triggers" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return TriggerService;
        })();

        return trigger;
    })();

    lebai.upgrade = (function() {

        /**
         * Namespace upgrade.
         * @memberof lebai
         * @namespace
         */
        var upgrade = {};

        upgrade.CheckUpgradeResponse = (function() {

            /**
             * Properties of a CheckUpgradeResponse.
             * @typedef {Object} lebai.upgrade.CheckUpgradeResponse.$Properties
             * @property {boolean|null} [needUpgrade] CheckUpgradeResponse needUpgrade
             * @property {string|null} [introduction] CheckUpgradeResponse introduction
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a CheckUpgradeResponse.
             * @memberof lebai.upgrade
             * @interface ICheckUpgradeResponse
             * @augments lebai.upgrade.CheckUpgradeResponse.$Properties
             * @deprecated Use lebai.upgrade.CheckUpgradeResponse.$Properties instead.
             */

            /**
             * Shape of a CheckUpgradeResponse.
             * @typedef {lebai.upgrade.CheckUpgradeResponse.$Properties} lebai.upgrade.CheckUpgradeResponse.$Shape
             */

            /**
             * Constructs a new CheckUpgradeResponse.
             * @memberof lebai.upgrade
             * @classdesc Represents a CheckUpgradeResponse.
             * @constructor
             * @param {lebai.upgrade.CheckUpgradeResponse.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var CheckUpgradeResponse = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * CheckUpgradeResponse needUpgrade.
             * @member {boolean} needUpgrade
             * @memberof lebai.upgrade.CheckUpgradeResponse
             * @instance
             */
            CheckUpgradeResponse.prototype.needUpgrade = false;

            /**
             * CheckUpgradeResponse introduction.
             * @member {string} introduction
             * @memberof lebai.upgrade.CheckUpgradeResponse
             * @instance
             */
            CheckUpgradeResponse.prototype.introduction = "";

            /**
             * Creates a new CheckUpgradeResponse instance using the specified properties.
             * @function create
             * @memberof lebai.upgrade.CheckUpgradeResponse
             * @static
             * @param {lebai.upgrade.CheckUpgradeResponse.$Properties=} [properties] Properties to set
             * @returns {lebai.upgrade.CheckUpgradeResponse} CheckUpgradeResponse instance
             * @type {{
             *   (properties: lebai.upgrade.CheckUpgradeResponse.$Shape): lebai.upgrade.CheckUpgradeResponse & lebai.upgrade.CheckUpgradeResponse.$Shape;
             *   (properties?: lebai.upgrade.CheckUpgradeResponse.$Properties): lebai.upgrade.CheckUpgradeResponse;
             * }}
             */
            CheckUpgradeResponse.create = function(properties) {
                return new CheckUpgradeResponse(properties);
            };

            /**
             * Encodes the specified CheckUpgradeResponse message. Does not implicitly {@link lebai.upgrade.CheckUpgradeResponse.verify|verify} messages.
             * @function encode
             * @memberof lebai.upgrade.CheckUpgradeResponse
             * @static
             * @param {lebai.upgrade.CheckUpgradeResponse.$Properties} message CheckUpgradeResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CheckUpgradeResponse.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.needUpgrade != null && $Object.hasOwnProperty.call(message, "needUpgrade") && message.needUpgrade !== false)
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.needUpgrade);
                if (message.introduction != null && $Object.hasOwnProperty.call(message, "introduction") && message.introduction !== "")
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.introduction);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CheckUpgradeResponse message, length delimited. Does not implicitly {@link lebai.upgrade.CheckUpgradeResponse.verify|verify} messages.
             * @function encodeDelimited
             * @memberof lebai.upgrade.CheckUpgradeResponse
             * @static
             * @param {lebai.upgrade.CheckUpgradeResponse.$Properties} message CheckUpgradeResponse message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CheckUpgradeResponse.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a CheckUpgradeResponse message from the specified reader or buffer.
             * @function decode
             * @memberof lebai.upgrade.CheckUpgradeResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {lebai.upgrade.CheckUpgradeResponse & lebai.upgrade.CheckUpgradeResponse.$Shape} CheckUpgradeResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CheckUpgradeResponse.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.lebai.upgrade.CheckUpgradeResponse();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.needUpgrade = value;
                            else
                                delete message.needUpgrade;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.stringVerify()).length)
                                message.introduction = value;
                            else
                                delete message.introduction;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a CheckUpgradeResponse message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof lebai.upgrade.CheckUpgradeResponse
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {lebai.upgrade.CheckUpgradeResponse & lebai.upgrade.CheckUpgradeResponse.$Shape} CheckUpgradeResponse
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CheckUpgradeResponse.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CheckUpgradeResponse message.
             * @function verify
             * @memberof lebai.upgrade.CheckUpgradeResponse
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CheckUpgradeResponse.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.needUpgrade != null && $Object.hasOwnProperty.call(message, "needUpgrade"))
                    if (typeof message.needUpgrade !== "boolean")
                        return "needUpgrade: boolean expected";
                if (message.introduction != null && $Object.hasOwnProperty.call(message, "introduction"))
                    if (!$util.isString(message.introduction))
                        return "introduction: string expected";
                return null;
            };

            /**
             * Creates a CheckUpgradeResponse message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof lebai.upgrade.CheckUpgradeResponse
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {lebai.upgrade.CheckUpgradeResponse} CheckUpgradeResponse
             */
            CheckUpgradeResponse.fromObject = function (object, _depth) {
                if (object instanceof $root.lebai.upgrade.CheckUpgradeResponse)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".lebai.upgrade.CheckUpgradeResponse: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.lebai.upgrade.CheckUpgradeResponse();
                if (object.needUpgrade != null)
                    if (object.needUpgrade)
                        message.needUpgrade = $Boolean(object.needUpgrade);
                if (object.introduction != null)
                    if (typeof object.introduction !== "string" || object.introduction.length)
                        message.introduction = $String(object.introduction);
                return message;
            };

            /**
             * Creates a plain object from a CheckUpgradeResponse message. Also converts values to other types if specified.
             * @function toObject
             * @memberof lebai.upgrade.CheckUpgradeResponse
             * @static
             * @param {lebai.upgrade.CheckUpgradeResponse} message CheckUpgradeResponse
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CheckUpgradeResponse.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    object.needUpgrade = false;
                    object.introduction = "";
                }
                if (message.needUpgrade != null && $Object.hasOwnProperty.call(message, "needUpgrade"))
                    object.needUpgrade = message.needUpgrade;
                if (message.introduction != null && $Object.hasOwnProperty.call(message, "introduction"))
                    object.introduction = message.introduction;
                return object;
            };

            /**
             * Converts this CheckUpgradeResponse to JSON.
             * @function toJSON
             * @memberof lebai.upgrade.CheckUpgradeResponse
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CheckUpgradeResponse.prototype.toJSON = function() {
                return CheckUpgradeResponse.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for CheckUpgradeResponse
             * @function getTypeUrl
             * @memberof lebai.upgrade.CheckUpgradeResponse
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            CheckUpgradeResponse.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lebai.upgrade.CheckUpgradeResponse";
            };

            return CheckUpgradeResponse;
        })();

        upgrade.UpgradeService = (function() {

            /**
             * Constructs a new UpgradeService service.
             * @memberof lebai.upgrade
             * @classdesc Represents an UpgradeService
             * @extends $protobuf.rpc.Service
             * @constructor
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             */
            var UpgradeService = function(rpcImpl, requestDelimited, responseDelimited) {
                $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
            };

            $Object.defineProperty(UpgradeService.prototype = $Object.create($protobuf.rpc.Service.prototype), "constructor", { value: UpgradeService, writable: true, enumerable: false, configurable: true });

            /**
             * Creates new UpgradeService service using the specified rpc implementation.
             * @function create
             * @memberof lebai.upgrade.UpgradeService
             * @static
             * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
             * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
             * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
             * @returns {UpgradeService} RPC service. Useful where requests and/or responses are streamed.
             */
            UpgradeService.create = function(rpcImpl, requestDelimited, responseDelimited) {
                return new this(rpcImpl, requestDelimited, responseDelimited);
            };

            /**
             * Callback as used by {@link lebai.upgrade.UpgradeService#checkUpgrade}.
             * @memberof lebai.upgrade.UpgradeService
             * @typedef CheckUpgradeCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.upgrade.CheckUpgradeResponse} [response] CheckUpgradeResponse
             */

            /**
             * Calls CheckUpgrade.
             * @memberof lebai.upgrade.UpgradeService
             * @typedef CheckUpgrade
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.upgrade.UpgradeService.CheckUpgradeCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.upgrade.CheckUpgradeResponse>;
             *   readonly name: "CheckUpgrade";
             *   readonly path: "/lebai.upgrade.UpgradeService/CheckUpgrade";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "CheckUpgradeResponse";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls CheckUpgrade.
             * @name lebai.upgrade.UpgradeService#checkUpgrade
             * @type {lebai.upgrade.UpgradeService.CheckUpgrade}
             */
            $Object.defineProperties(UpgradeService.prototype.checkUpgrade = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, UpgradeService.prototype.checkUpgrade, $root.google.protobuf.Empty, $root.lebai.upgrade.CheckUpgradeResponse, request, callback);
            }, {
                name: { value: "CheckUpgrade" },
                path: { value: "/lebai.upgrade.UpgradeService/CheckUpgrade" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "CheckUpgradeResponse" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.upgrade.UpgradeService#startUpgrade}.
             * @memberof lebai.upgrade.UpgradeService
             * @typedef StartUpgradeCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {google.protobuf.Empty} [response] Empty
             */

            /**
             * Calls StartUpgrade.
             * @memberof lebai.upgrade.UpgradeService
             * @typedef StartUpgrade
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.upgrade.UpgradeService.StartUpgradeCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<google.protobuf.Empty>;
             *   readonly name: "StartUpgrade";
             *   readonly path: "/lebai.upgrade.UpgradeService/StartUpgrade";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "google.protobuf.Empty";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls StartUpgrade.
             * @name lebai.upgrade.UpgradeService#startUpgrade
             * @type {lebai.upgrade.UpgradeService.StartUpgrade}
             */
            $Object.defineProperties(UpgradeService.prototype.startUpgrade = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, UpgradeService.prototype.startUpgrade, $root.google.protobuf.Empty, $root.google.protobuf.Empty, request, callback);
            }, {
                name: { value: "StartUpgrade" },
                path: { value: "/lebai.upgrade.UpgradeService/StartUpgrade" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "google.protobuf.Empty" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            /**
             * Callback as used by {@link lebai.upgrade.UpgradeService#getUpgradeStdout}.
             * @memberof lebai.upgrade.UpgradeService
             * @typedef GetUpgradeStdoutCallback
             * @type {function}
             * @param {Error|null} error Error, if any
             * @param {lebai.CommandStdout} [response] CommandStdout
             */

            /**
             * Calls GetUpgradeStdout.
             * @memberof lebai.upgrade.UpgradeService
             * @typedef GetUpgradeStdout
             * @type {{
             *   (request: google.protobuf.IEmpty, callback: lebai.upgrade.UpgradeService.GetUpgradeStdoutCallback): void;
             *   (request: google.protobuf.IEmpty): Promise<lebai.CommandStdout>;
             *   readonly name: "GetUpgradeStdout";
             *   readonly path: "/lebai.upgrade.UpgradeService/GetUpgradeStdout";
             *   readonly requestType: "google.protobuf.Empty";
             *   readonly responseType: "CommandStdout";
             *   readonly requestStream: undefined;
             *   readonly responseStream: undefined;
             * }}
             */

            /**
             * Calls GetUpgradeStdout.
             * @name lebai.upgrade.UpgradeService#getUpgradeStdout
             * @type {lebai.upgrade.UpgradeService.GetUpgradeStdout}
             */
            $Object.defineProperties(UpgradeService.prototype.getUpgradeStdout = function(request, callback) {
                return $protobuf.rpc.Service.prototype.rpcCall.call(this, UpgradeService.prototype.getUpgradeStdout, $root.google.protobuf.Empty, $root.lebai.CommandStdout, request, callback);
            }, {
                name: { value: "GetUpgradeStdout" },
                path: { value: "/lebai.upgrade.UpgradeService/GetUpgradeStdout" },
                requestType: { value: "google.protobuf.Empty" },
                responseType: { value: "CommandStdout" },
                requestStream: { value: $undefined },
                responseStream: { value: $undefined }
            });

            return UpgradeService;
        })();

        return upgrade;
    })();

    return lebai;
})();

$root.google = (function() {

    /**
     * Namespace google.
     * @exports google
     * @namespace
     */
    var google = {};

    google.protobuf = (function() {

        /**
         * Namespace protobuf.
         * @memberof google
         * @namespace
         */
        var protobuf = {};

        protobuf.Empty = (function() {

            /**
             * Properties of an Empty.
             * @typedef {Object} google.protobuf.Empty.$Properties
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of an Empty.
             * @memberof google.protobuf
             * @interface IEmpty
             * @augments google.protobuf.Empty.$Properties
             * @deprecated Use google.protobuf.Empty.$Properties instead.
             */

            /**
             * Shape of an Empty.
             * @typedef {google.protobuf.Empty.$Properties} google.protobuf.Empty.$Shape
             */

            /**
             * Constructs a new Empty.
             * @memberof google.protobuf
             * @classdesc Represents an Empty.
             * @constructor
             * @param {google.protobuf.Empty.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Empty = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Creates a new Empty instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Empty
             * @static
             * @param {google.protobuf.Empty.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.Empty} Empty instance
             * @type {{
             *   (properties: google.protobuf.Empty.$Shape): google.protobuf.Empty & google.protobuf.Empty.$Shape;
             *   (properties?: google.protobuf.Empty.$Properties): google.protobuf.Empty;
             * }}
             */
            Empty.create = function(properties) {
                return new Empty(properties);
            };

            /**
             * Encodes the specified Empty message. Does not implicitly {@link google.protobuf.Empty.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Empty
             * @static
             * @param {google.protobuf.Empty.$Properties} message Empty message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Empty.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Empty message, length delimited. Does not implicitly {@link google.protobuf.Empty.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.Empty
             * @static
             * @param {google.protobuf.Empty.$Properties} message Empty message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Empty.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes an Empty message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Empty
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Empty & google.protobuf.Empty.$Shape} Empty
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Empty.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.Empty();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    reader.skipType(tag & 7, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes an Empty message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.Empty
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.Empty & google.protobuf.Empty.$Shape} Empty
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Empty.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an Empty message.
             * @function verify
             * @memberof google.protobuf.Empty
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Empty.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                return null;
            };

            /**
             * Creates an Empty message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Empty
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Empty} Empty
             */
            Empty.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.Empty)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.Empty: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                return new $root.google.protobuf.Empty();
            };

            /**
             * Creates a plain object from an Empty message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Empty
             * @static
             * @param {google.protobuf.Empty} message Empty
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Empty.toObject = function () {
                return {};
            };

            /**
             * Converts this Empty to JSON.
             * @function toJSON
             * @memberof google.protobuf.Empty
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Empty.prototype.toJSON = function() {
                return Empty.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Empty
             * @function getTypeUrl
             * @memberof google.protobuf.Empty
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Empty.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.Empty";
            };

            return Empty;
        })();

        protobuf.Timestamp = (function() {

            /**
             * Properties of a Timestamp.
             * @typedef {Object} google.protobuf.Timestamp.$Properties
             * @property {number|Long|null} [seconds] Timestamp seconds
             * @property {number|null} [nanos] Timestamp nanos
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */

            /**
             * Properties of a Timestamp.
             * @memberof google.protobuf
             * @interface ITimestamp
             * @augments google.protobuf.Timestamp.$Properties
             * @deprecated Use google.protobuf.Timestamp.$Properties instead.
             */

            /**
             * Shape of a Timestamp.
             * @typedef {google.protobuf.Timestamp.$Properties} google.protobuf.Timestamp.$Shape
             */

            /**
             * Constructs a new Timestamp.
             * @memberof google.protobuf
             * @classdesc Represents a Timestamp.
             * @constructor
             * @param {google.protobuf.Timestamp.$Properties=} [properties] Properties to set
             * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
             */
            var Timestamp = function (properties) {
                if (properties)
                    for (var keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            };

            /**
             * Timestamp seconds.
             * @member {number|Long} seconds
             * @memberof google.protobuf.Timestamp
             * @instance
             */
            Timestamp.prototype.seconds = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Timestamp nanos.
             * @member {number} nanos
             * @memberof google.protobuf.Timestamp
             * @instance
             */
            Timestamp.prototype.nanos = 0;

            /**
             * Creates a new Timestamp instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {google.protobuf.Timestamp.$Properties=} [properties] Properties to set
             * @returns {google.protobuf.Timestamp} Timestamp instance
             * @type {{
             *   (properties: google.protobuf.Timestamp.$Shape): google.protobuf.Timestamp & google.protobuf.Timestamp.$Shape;
             *   (properties?: google.protobuf.Timestamp.$Properties): google.protobuf.Timestamp;
             * }}
             */
            Timestamp.create = function(properties) {
                return new Timestamp(properties);
            };

            /**
             * Encodes the specified Timestamp message. Does not implicitly {@link google.protobuf.Timestamp.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {google.protobuf.Timestamp.$Properties} message Timestamp message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Timestamp.encode = function (message, writer, _depth) {
                if (!writer)
                    writer = $Writer.create();
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (message.seconds != null && $Object.hasOwnProperty.call(message, "seconds") && (typeof message.seconds === "object" ? message.seconds.low || message.seconds.high : message.seconds !== 0))
                    writer.uint32(/* id 1, wireType 0 =*/8).int64(message.seconds);
                if (message.nanos != null && $Object.hasOwnProperty.call(message, "nanos") && message.nanos !== 0)
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.nanos);
                if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Timestamp message, length delimited. Does not implicitly {@link google.protobuf.Timestamp.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {google.protobuf.Timestamp.$Properties} message Timestamp message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Timestamp.encodeDelimited = function(message, writer) {
                return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
            };

            /**
             * Decodes a Timestamp message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Timestamp & google.protobuf.Timestamp.$Shape} Timestamp
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Timestamp.decode = function (reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var end, message, value;
                if (length === $undefined)
                    end = reader.len;
                else {
                    end = reader.pos + length;
                    if (end > reader.len)
                        throw $RangeError("index out of range");
                    length = reader.len;
                    reader.len = end;
                }
                message = _target || new $root.google.protobuf.Timestamp();
                while (reader.pos < end) {
                    var start = reader.pos;
                    var tag = reader.tag();
                    if (tag === _end) {
                        _end = $undefined;
                        break;
                    }
                    var wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                                message.seconds = value;
                            else
                                delete message.seconds;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.nanos = value;
                            else
                                delete message.nanos;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                }
                if (length !== $undefined) {
                    if (reader.pos !== end)
                        throw $RangeError("index out of range");
                    reader.len = length;
                }
                if (_end !== $undefined)
                    throw $Error("missing end group");
                return message;
            };

            /**
             * Decodes a Timestamp message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.Timestamp & google.protobuf.Timestamp.$Shape} Timestamp
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Timestamp.decodeDelimited = function(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Timestamp message.
             * @function verify
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Timestamp.verify = function (message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seconds != null && $Object.hasOwnProperty.call(message, "seconds"))
                    if (!$util.isInteger(message.seconds) && !(message.seconds && $util.isInteger(message.seconds.low) && $util.isInteger(message.seconds.high)))
                        return "seconds: integer|Long expected";
                if (message.nanos != null && $Object.hasOwnProperty.call(message, "nanos"))
                    if (!$util.isInteger(message.nanos))
                        return "nanos: integer expected";
                return null;
            };

            /**
             * Creates a Timestamp message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Timestamp} Timestamp
             */
            Timestamp.fromObject = function (object, _depth) {
                if (object instanceof $root.google.protobuf.Timestamp)
                    return object;
                if (!$util.isObject(object))
                    throw $TypeError(".google.protobuf.Timestamp: object expected");
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var message = new $root.google.protobuf.Timestamp();
                if (object.seconds != null)
                    if (typeof object.seconds === "object" ? object.seconds.low || object.seconds.high : $Number(object.seconds) !== 0)
                        if ($util.Long)
                            message.seconds = $util.Long.fromValue(object.seconds, false);
                        else if (typeof object.seconds === "string")
                            message.seconds = $parseInt(object.seconds, 10);
                        else if (typeof object.seconds === "number")
                            message.seconds = object.seconds;
                        else if (typeof object.seconds === "object")
                            message.seconds = new $util.LongBits(object.seconds.low >>> 0, object.seconds.high >>> 0).toNumber();
                if (object.nanos != null)
                    if ($Number(object.nanos) !== 0)
                        message.nanos = object.nanos | 0;
                return message;
            };

            /**
             * Creates a plain object from a Timestamp message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {google.protobuf.Timestamp} message Timestamp
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Timestamp.toObject = function (message, options, _depth) {
                if (!options)
                    options = {};
                if (_depth === $undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var object = {};
                if (options.defaults) {
                    if ($util.Long) {
                        var long = new $util.Long(0, 0, false);
                        object.seconds = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                    } else
                        object.seconds = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                    object.nanos = 0;
                }
                if (message.seconds != null && $Object.hasOwnProperty.call(message, "seconds"))
                    if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                        object.seconds = typeof message.seconds === "number" ? $BigInt(message.seconds) : $util.Long.fromBits(message.seconds.low >>> 0, message.seconds.high >>> 0, false).toBigInt();
                    else if (typeof message.seconds === "number")
                        object.seconds = options.longs === $String ? $String(message.seconds) : message.seconds;
                    else
                        object.seconds = options.longs === $String ? $util.Long.prototype.toString.call(message.seconds) : options.longs === $Number ? new $util.LongBits(message.seconds.low >>> 0, message.seconds.high >>> 0).toNumber() : message.seconds;
                if (message.nanos != null && $Object.hasOwnProperty.call(message, "nanos"))
                    object.nanos = message.nanos;
                return object;
            };

            /**
             * Converts this Timestamp to JSON.
             * @function toJSON
             * @memberof google.protobuf.Timestamp
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Timestamp.prototype.toJSON = function() {
                return Timestamp.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the type url for Timestamp
             * @function getTypeUrl
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
             * @returns {string} The type url
             */
            Timestamp.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/google.protobuf.Timestamp";
            };

            return Timestamp;
        })();

        return protobuf;
    })();

    return google;
})();

module.exports = $root;
